state.json
auth.json
# Runtime-regenerated dashboard HTMLs (written by main.run_daily_check via dashboard_renderer/api.py).
# Tracking them caused git pull --ff-only to abort on the droplet whenever a daily run had updated
# the timestamps (Phase 25 deploy 2026-05-06). The web route serves 503 on missing dashboard.html
# during first-run, which is the expected bootstrap behaviour.
dashboard.html
dashboard-signals.html
dashboard-account.html
dashboard-settings.html
dashboard-market-test.html
last_email.html
.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Phase 23 — backtest parquet cache (per-machine, not committed)
.planning/backtests/data/

# Phase 26 cleanup (2026-05-07) — agent runtime + OS junk + debug artefacts
# These appear from MCP tooling, finder metadata, and ad-hoc debug exports.
**/.DS_Store
**/._*
_debug_new_dashboard.html
.agents/
.claude/
.claude-flow/
.codex/
.cowork/
.cursor/
.mcp.json
.playwright-mcp/
