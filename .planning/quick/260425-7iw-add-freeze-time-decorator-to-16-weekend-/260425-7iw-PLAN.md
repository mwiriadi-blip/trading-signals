---
phase: 260425-7iw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_main.py
autonomous: true
requirements:
  - QUICK-7iw-01
must_haves:
  truths:
    - "All 16 listed tests carry @pytest.mark.freeze_time('2026-04-27T00:00:00+00:00') as the outermost (topmost) decorator"
    - "Each added decorator has 2-space indentation and the trailing comment '# Mon 08:00 AWST'"
    - "Full tests/test_main.py suite passes on any day of the week (weekday gate in run_daily_check no longer short-circuits these tests)"
    - "No non-test files in the repo are modified"
  artifacts:
    - path: "tests/test_main.py"
      provides: "16 new @pytest.mark.freeze_time decorator lines added; no other line edits"
      contains: "@pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')"
  key_links:
    - from: "tests/test_main.py :: 16 target tests"
      to: "pytest-freezer (requirements.txt: pytest-freezer==0.4.9)"
      via: "@pytest.mark.freeze_time decorator"
      pattern: "@pytest\\.mark\\.freeze_time\\('2026-04-27T00:00:00\\+00:00'\\)  # Mon 08:00 AWST"
---

<objective>
Add `@pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST`
to 16 weekend-flaky tests in `tests/test_main.py` so the suite passes every
day of the week. Today is Saturday 2026-04-25; `run_daily_check` short-circuits
at the weekday gate on Sat/Sun and returns `state=None`, breaking post-run
assertions in these tests. Fix follows the Phase 10 pattern already in use
in `TestRunDailyCheckPushesState` (same file, lines 104 / 2511 / 2540 / 2595).

Purpose: Eliminate weekend-only CI/local-suite flakiness without touching
production code. This is pure test-hygiene cleanup; any non-test edit would
indicate a real bug belonging to a separate debug session.

Output: Updated `tests/test_main.py` with 16 new decorator lines, zero other
line diffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/12-https-domain-wiring/deferred-items.md

<!-- Key file: tests/test_main.py is 2623 lines; do NOT load whole file. -->
<!-- Use grep and targeted Read(offset, limit) for each target. -->

<interfaces>
<!-- Established pattern in tests/test_main.py (Phase 10, TestRunDailyCheckPushesState). -->
<!-- Existing examples at lines 104, 2511, 2540, 2595. Literal text to reproduce: -->

Decorator line (2-space indent because tests live inside classes):
```
  @pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST
```

It goes on the line IMMEDIATELY ABOVE `def test_name(` and is the OUTERMOST
(topmost) decorator when other decorators (e.g. @pytest.fixture consumers,
@pytest.mark.parametrize) are present. In all 16 targets below, the `def`
is currently NOT preceded by any `@pytest.mark.*` line, so insertion is
simply one new line directly above the `def`.

Supporting facts:
- `import pytest` is already at the top of tests/test_main.py (no import change needed).
- `pytest-freezer==0.4.9` is pinned in requirements.txt (no dependency change needed).
- The frozen instant `2026-04-27T00:00:00+00:00` is Monday 08:00 AWST (UTC+8),
  matching `system_params.RUN_HOUR_AWST` and bypassing the Sat/Sun gate.
</interfaces>

<targets>
<!-- 16 tests to decorate. Line numbers point to the `def` line at the time -->
<!-- of planning. The EXECUTOR MUST re-verify line locations by grep before -->
<!-- editing, because earlier edits in the same pass shift later line numbers. -->

1. TestCLI::test_force_email_sends_live_email                           (~line 295)
2. TestCLI::test_force_email_captures_post_run_state                    (~line 324)
3. TestCLI::test_test_flag_sends_test_prefixed_email_no_state_mutation  (~line 360)
4. TestCLI::test_force_email_and_test_combined                          (~line 391)
5. TestOrchestrator::test_short_frame_raises_and_no_state_written       (~line 483)
6. TestOrchestrator::test_orchestrator_reads_both_int_and_dict_signal_shape (~line 617)
7. TestOrchestrator::test_reversal_long_to_short_preserves_new_position (~line 671)
8. TestOrchestrator::test_fetch_failure_exits_nonzero_no_save_state     (~line 835)
9. TestOrchestrator::test_dashboard_failure_never_crashes_run           (~line 894)
10. TestOrchestrator::test_dashboard_import_time_failure_never_crashes_run (~line 938)
11. TestEmailNeverCrash::test_email_runtime_failure_never_crashes_run   (~line 1041)
12. TestEmailNeverCrash::test_email_import_time_failure_never_crashes_run (~line 1064)
13. TestRunDailyCheckTupleReturn::test_run_daily_check_returns_4_tuple  (~line 1107)
14. TestRunDailyCheckTupleReturn::test_run_daily_check_test_mode_returns_in_memory_state (~line 1127)
15. TestCrashEmailBoundary::test_crash_email_includes_last_loaded_state (~line 1775)
16. TestWarningCarryOverFlow::test_happy_path_save_state_called_exactly_twice (~line 1909)
</targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @pytest.mark.freeze_time decorator to 16 weekend-flaky tests</name>
  <files>tests/test_main.py</files>
  <action>
Insert the decorator line
`  @pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST`
(exactly — 2-space indent, single quotes, ISO-8601 with explicit `+00:00`
offset, trailing comment `# Mon 08:00 AWST`) on the line IMMEDIATELY ABOVE
the `def` of each of the 16 tests listed in `<targets>` above. No other
edits to tests/test_main.py in this task.

Recommended execution order — edit BOTTOM-UP (line 1909 first, line 295 last)
so earlier edits do not invalidate later line numbers. Alternatively, use
Edit with unique `old_string` anchors (e.g. the full `def test_name(` line)
so line numbers are irrelevant.

Pattern reference (already in this file — DO NOT modify these, they are
the exemplar): lines 104, 2511, 2540, 2595.

Do NOT:
- Add `import pytest` (already imported at module top).
- Modify requirements.txt (pytest-freezer==0.4.9 already pinned).
- Touch any file other than tests/test_main.py.
- Edit anything inside the test bodies — decorator-only change.
- Change any existing decorator. If a target already has a
  `@pytest.mark.freeze_time(...)` decorator, STOP and report — the task
  facts stated none of the 16 currently do, so this would indicate
  conflicting state.
- Add per-test variations (e.g. different frozen instants) — all 16 use
  the identical decorator line to match the Phase 10 exemplar.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.claude/worktrees/optimistic-kowalevski-64885a &amp;&amp; .venv/bin/python -m pytest tests/test_main.py -q</automated>
  </verify>
  <done>
- `.venv/bin/python -m pytest tests/test_main.py -q` exits 0.
- `grep -c "@pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST" tests/test_main.py`
  shows exactly `20` (4 pre-existing exemplars at lines 104/2511/2540/2595 + 16 new).
- `git diff --stat tests/test_main.py` shows `+16` lines inserted and `-0` deletions.
- `git diff --stat` reports exactly ONE file changed: `tests/test_main.py`.
- For each of the 16 target tests, the line immediately preceding `def test_…`
  is the new decorator line. Quick audit:
  `grep -B1 "def test_force_email_sends_live_email" tests/test_main.py` shows the decorator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-day suite verification (freeze wrapper + full matrix)</name>
  <files>tests/test_main.py</files>
  <action>
Confirm the suite is now day-of-week independent by running pytest under two
distinct system-time assumptions (no code changes — this is a read-only
verification task using the `freezegun` library that pytest-freezer wraps,
invoked from the command line).

Step 1 — natural system time (today is Sat 2026-04-25, the weekend that
originally exposed the flake):
  `.venv/bin/python -m pytest tests/test_main.py -q`
  → MUST exit 0.

Step 2 — simulate a Sunday run (covers the OTHER weekend day) by launching
pytest from a short inline Python wrapper that freezes the system clock via
freezegun before pytest imports:
  `.venv/bin/python -c "import freezegun; freezegun.freeze_time('2026-04-26T12:00:00+00:00').start(); import pytest, sys; sys.exit(pytest.main(['tests/test_main.py', '-q']))"`
  → MUST exit 0.

If either run fails, do NOT edit production code. Instead:
1. Capture the failing test names + tracebacks.
2. Recheck whether any of the 16 targets was missed in Task 1 (use the
   `grep -B1 "def test_name"` audit from Task 1's `<done>`).
3. If all 16 are correctly decorated and a DIFFERENT test is failing on
   weekends, STOP and escalate — that is a separate weekend-flaky test
   not listed in `<task_facts>` and belongs to a new quick/debug cycle,
   NOT this task.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.claude/worktrees/optimistic-kowalevski-64885a &amp;&amp; .venv/bin/python -m pytest tests/test_main.py -q &amp;&amp; .venv/bin/python -c "import freezegun; freezegun.freeze_time('2026-04-26T12:00:00+00:00').start(); import pytest, sys; sys.exit(pytest.main(['tests/test_main.py', '-q']))"</automated>
  </verify>
  <done>
- Both pytest invocations (natural Sat clock + frozen-Sun clock) exit 0.
- Combined output shows the same pass count on both runs (no new
  skips/xfails introduced).
- No edits made in this task (`git diff tests/test_main.py` unchanged from
  end of Task 1).
  </done>
</task>

</tasks>

<verification>
Phase-level checks (beyond individual task verifies):

1. Diff is minimal and test-only:
   `git diff --stat` → exactly one file: `tests/test_main.py`, +16 / -0.

2. Decorator parity with Phase 10 exemplar:
   `grep -c "@pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST" tests/test_main.py`
   → `20` (4 pre-existing + 16 new).

3. Every target test has the decorator directly above its `def`:
   For each of the 16 test names, `grep -B1 "def <name>" tests/test_main.py`
   shows the decorator line immediately before the def.

4. Suite is green on weekday AND weekend clocks (Task 2 commands).

5. No production code touched:
   `git diff --name-only` lists ONLY `tests/test_main.py`.
</verification>

<success_criteria>
- 16 decorators added, 0 lines deleted, 0 other files modified.
- `.venv/bin/python -m pytest tests/test_main.py -q` passes on a Saturday
  (today) and under a frozen-Sunday wrapper.
- Pattern matches the Phase 10 exemplar byte-for-byte (grep count = 20).
- Tech-debt item from
  `.planning/phases/12-https-domain-wiring/deferred-items.md` is cleared
  and can be checked off.
</success_criteria>

<output>
After completion, create
`.planning/quick/260425-7iw-add-freeze-time-decorator-to-16-weekend-/260425-7iw-01-SUMMARY.md`
summarizing:
- 16 tests decorated (list by class::name).
- Pytest green on Saturday + frozen-Sunday runs.
- Zero production-code changes.
- Tech-debt item from deferred-items.md cleared.
</output>
