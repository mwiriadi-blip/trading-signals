---
phase: 14
plan: 03
type: execute
wave: 1
depends_on: [14-01, 14-02]
files_modified:
  - sizing_engine.py
  - tests/test_sizing_engine.py
autonomous: true
requirements:
  - TRADE-04
tags:
  - phase14
  - sizing-engine
  - manual-stop
  - pure-math

must_haves:
  truths:
    - 'sizing_engine.get_trailing_stop returns position.manual_stop when set; falls through to peak/trough computed stop when None'
    - 'NaN-passthrough invariant (B-1) preserved: NaN atr_entry returns float(nan) regardless of manual_stop value'
    - 'Pre-migration positions (no manual_stop key) work via .get() defensive read — no KeyError'
    - 'LONG and SHORT precedence both verified independently'
    - 'Existing TRAIL_MULT_LONG / TRAIL_MULT_SHORT computed-stop math unchanged when manual_stop is None'
    - 'Pure-math hex boundary preserved: get_trailing_stop has no I/O, no datetime.now(), no env reads'
  artifacts:
    - path: 'sizing_engine.py'
      provides: 'get_trailing_stop reads position.get(manual_stop) AFTER NaN guard, BEFORE direction switch; returns manual_stop verbatim when not None'
      contains: "manual = position.get('manual_stop')"
    - path: 'tests/test_sizing_engine.py'
      provides: 'TestManualStopOverride populated with 5 tests; existing _make_position helper extended with manual_stop kwarg'
      min_lines: 1180
  key_links:
    - from: 'sizing_engine.get_trailing_stop'
      to: 'system_params.Position.manual_stop (added Plan 14-02)'
      via: 'position.get(manual_stop) defensive subscript'
      pattern: "position\\.get\\(['\"]manual_stop['\"]\\)"
---

<objective>
Make `sizing_engine.get_trailing_stop` honor the operator's `manual_stop` override (D-09). When `position['manual_stop']` is not None, return it directly; when None or absent, fall back to the existing peak/trough trailing-stop computation (v1.0 behavior preserved). Lock the precedence with five new tests in `TestManualStopOverride`.

Purpose: Plan 14-04's `/trades/modify` endpoint sets `position['manual_stop']` via `state_manager.save_state`. Plan 14-05's `dashboard.py::_compute_trail_stop_display` mirrors this precedence in the rendering layer (lockstep parity per CLAUDE.md hex-lite). This plan ships ONLY the canonical pure-math precedence in `sizing_engine`; the dashboard mirror is owned by Plan 14-05.

Output: `sizing_engine.py` with the manual_stop branch inserted between the NaN guard and the LONG/SHORT switch; `tests/test_sizing_engine.py` with `TestManualStopOverride` populated; existing `TestExits` trailing-stop tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-CONTEXT.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-RESEARCH.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-PATTERNS.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-02-SUMMARY.md
@CLAUDE.md
@sizing_engine.py
@system_params.py
@tests/test_sizing_engine.py

<interfaces>
Existing get_trailing_stop signature (sizing_engine.py:180-240) — Phase 14 inserts a 4-line block between the NaN guard (line 229-230) and the LONG branch (line 231-235); function signature unchanged.

Existing TRAIL_MULT_LONG = 3.0, TRAIL_MULT_SHORT = 2.0 (system_params.py:43-44) — UNCHANGED.

Existing _make_position helper in tests/test_sizing_engine.py:129-160 — Phase 14 extends with manual_stop kwarg defaulted to None.

Position TypedDict (system_params.py:137-159) — Phase 14 already added manual_stop in Plan 14-02; this plan is a CONSUMER of that field.

NaN-passthrough invariant (B-1):
- atr_entry NaN → return float('nan') BEFORE any direction logic
- This invariant is preserved; manual_stop branch comes AFTER the NaN guard
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sizing_engine.get_trailing_stop manual_stop precedence + TestManualStopOverride populated</name>
  <files>sizing_engine.py, tests/test_sizing_engine.py</files>
  <behavior>
    - LONG with manual_stop=7700.0 (peak_price=8100.0, atr_entry=50.0; computed would be 7950.0): returns 7700.0
    - SHORT with manual_stop=7900.0 (trough_price=7800.0, atr_entry=50.0; computed would be 7900.0): returns 7900.0 (set value, NOT computed)
    - Either direction with manual_stop=None: returns the existing peak/trough computed stop (v1.0 behavior preserved)
    - NaN-passthrough invariant (B-1): atr_entry=NaN AND manual_stop=7700.0 → returns float(nan); the NaN guard runs BEFORE the manual_stop branch
    - Pre-migration position dict (no manual_stop key at all): position.get('manual_stop') returns None → falls through to computed; no KeyError
  </behavior>
  <read_first>
    - sizing_engine.py lines 1-50 (module docstring + hex-lite imports — pure math discipline reminder)
    - sizing_engine.py lines 180-240 (get_trailing_stop full body — the function getting the new branch)
    - sizing_engine.py lines 243-303 (check_stop_hit — context only; this function does NOT get a manual_stop branch in Phase 14 because hits are determined by the trailing stop returned by get_trailing_stop... but Phase 14 deliberately does NOT modify check_stop_hit because the operator's manual_stop is interpreted as "use this as the stop level", which means check_stop_hit should also honor it — see action below)
    - tests/test_sizing_engine.py lines 1-30 (module docstring + imports)
    - tests/test_sizing_engine.py lines 129-160 (_make_position helper — the kwargs need extending)
    - tests/test_sizing_engine.py lines 415-510 (TestExits — analog test shape for new TestManualStopOverride)
    - 14-CONTEXT.md D-09 (manual_stop semantics: precedence, opt-in, backward-compat)
    - 14-RESEARCH.md §Common Pitfalls Pitfall 5 lines 921-925 (defensive .get('manual_stop') discipline)
    - 14-PATTERNS.md §`sizing_engine.get_trailing_stop` lines 365-411 (insert position; lockstep parity note)
    - system_params.py lines 137-159 (Position TypedDict with manual_stop now present per Plan 14-02)
  </read_first>
  <action>
Two source-file edits.

**Sub-edit A — sizing_engine.py: insert manual_stop branch in get_trailing_stop.**

Modify `get_trailing_stop` at lines 180-240. The change is INSERT-only — do NOT remove or restructure any existing line. Locate the existing NaN-passthrough block at line 229-230:

    atr_entry = position['atr_entry']
    if not math.isfinite(atr_entry):
      return float('nan')  # B-1: NaN-pass-through

Insert the manual_stop precedence block IMMEDIATELY AFTER the NaN guard (between line 230 and line 231). The block must be positioned: AFTER the NaN guard (so NaN passthrough takes precedence over manual_stop — operator's manual_stop on broken upstream data still surfaces NaN to caller), and BEFORE the LONG/SHORT direction switch (so the override short-circuits the computed peak/trough math).

Insert these 5 lines (4 code + 1 blank line for readability):

    # Phase 14 D-09: manual_stop takes precedence over computed trailing stop.
    # When operator has set a stop via /trades/modify, return it directly.
    # When None (default), fall through to v1.0 computed trailing stop.
    # Defensive .get() handles pre-migration position dicts (no key) — RESEARCH Pitfall 5.
    manual = position.get('manual_stop')
    if manual is not None:
      return manual

After insert, the function body shape is:

    atr_entry = position['atr_entry']
    if not math.isfinite(atr_entry):
      return float('nan')  # B-1: NaN-pass-through
    # Phase 14 D-09: manual_stop takes precedence over computed trailing stop.
    # ... (4 lines)
    manual = position.get('manual_stop')
    if manual is not None:
      return manual
    if position['direction'] == 'LONG':
      peak = position['peak_price']
      ...

Extend the function docstring (currently lines 185-225) by inserting a Phase 14 D-09 paragraph BEFORE the `Args:` block. Add this paragraph after the existing "current_price parameter is reserved..." paragraph (line 215-216) and before the "Args:" block (line 218):

    Phase 14 D-09 manual_stop precedence: when position['manual_stop'] is not
    None (operator override set via POST /trades/modify), the function returns
    that value directly, bypassing the peak/trough computed stop. When None
    (the v1.0 default), falls through to the computed trailing stop. NaN
    guard on atr_entry runs FIRST so NaN passthrough is preserved regardless
    of the override. Defensive position.get('manual_stop') so pre-migration
    state dicts (no key) silently fall through (RESEARCH §Pitfall 5).

**CRITICAL — DO NOT modify `check_stop_hit`** (lines 243-303). Reading the existing implementation: `check_stop_hit` recomputes its own stop value inline (line 296: `stop = peak - TRAIL_MULT_LONG * atr_entry`). For Phase 14, the operator's `manual_stop` is documented as "the trailing stop level the operator wants to use". `check_stop_hit` would also need a manual_stop branch to fully honor the override end-to-end.

**However, Phase 14 D-09 does NOT mandate that `check_stop_hit` honor manual_stop in this plan**, because:
1. Stop-hit detection happens inside `main.run_daily_check` (the daily signal loop), not via the web mutation endpoints.
2. Operator-set stops are EXPLICITLY documented in 14-CONTEXT.md D-09 as overriding the trailing-stop COMPUTATION for display + reporting; the daily loop's stop-hit detection is out of Phase 14 scope.
3. Plan 14-05 mirrors this precedence in `dashboard.py::_compute_trail_stop_display` for the displayed Trail Stop value — that's where the operator perceives the override.
4. If Phase 15 (or a future phase) decides the daily loop should honor manual_stop too, that's a separate `check_stop_hit` modification.

To make the Phase 14 limitation explicit and prevent future readers from "fixing" it by adding a manual_stop branch to check_stop_hit by mistake, ADD the following note to the `check_stop_hit` docstring (between the existing "B-1 NaN policy:" paragraph at line 271-275 and the "Args:" block at line 277):

    Phase 14 D-09 NOTE — manual_stop NOT honored here intentionally:
      get_trailing_stop honors position['manual_stop'] (D-09) for display
      and operator-facing reporting. check_stop_hit is invoked by the daily
      signal loop (main.run_daily_check), which is OUT OF Phase 14 scope —
      the loop continues to use the v1.0 computed stop level for hit
      detection. If a future phase wants the daily loop to honor manual_stop,
      a parallel branch must be added here. As of Phase 14, the discrepancy
      is documented and accepted: the operator can override the displayed
      stop without changing the loop's trigger condition.

DO NOT modify any other function in sizing_engine.py (calc_position_size, check_pyramid, compute_unrealised_pnl, step). The only behavioral change in this plan is the 4-line manual_stop branch in get_trailing_stop.

**Sub-edit B — tests/test_sizing_engine.py: extend _make_position; populate TestManualStopOverride.**

1. **Extend `_make_position` helper** (lines 129-160). The current signature has these kwargs (read the actual code at line 129-160 first to confirm exact ordering):

    def _make_position(
      direction='LONG', entry_price=7000.0, entry_date='2026-01-15',
      n_contracts=2, pyramid_level=0, peak_price=None,
      trough_price=None, atr_entry=53.0,
    ):

   Add ONE new kwarg `manual_stop=None` at the END of the keyword-arg list. Update the function body's Position dict construction to include `manual_stop=manual_stop` so tests can pass any value (None, float, NaN). Body:

    return Position(
      direction=direction,
      entry_price=entry_price,
      entry_date=entry_date,
      n_contracts=n_contracts,
      pyramid_level=pyramid_level,
      peak_price=peak_price,
      trough_price=trough_price,
      atr_entry=atr_entry,
      manual_stop=manual_stop,  # Phase 14 D-09: operator override; None = computed default
    )

2. **Replace the TestManualStopOverride skeleton** (added by Plan 14-01 Task 2) with the populated class. Locate the skeleton near the end of tests/test_sizing_engine.py and replace with:

    class TestManualStopOverride:
      '''Phase 14 D-09: get_trailing_stop honors position['manual_stop'] override.

      Precedence:
        1. NaN guard on atr_entry (B-1) runs FIRST — returns float('nan')
        2. manual_stop branch — when not None, returns the value verbatim
        3. LONG branch — peak - TRAIL_MULT_LONG * atr_entry
        4. SHORT branch — trough + TRAIL_MULT_SHORT * atr_entry

      Defensive .get('manual_stop') so pre-migration position dicts (no key)
      silently fall through to computed (RESEARCH §Pitfall 5).

      Lockstep parity (CLAUDE.md hex-lite): dashboard._compute_trail_stop_display
      MUST implement identical precedence — verified by Plan 14-05's parity test.
      '''

      def test_manual_stop_overrides_long_computed(self) -> None:
        '''LONG with manual_stop=7700.0; computed would be 8100 - 3*50 = 7950.
        Returns the override directly, NOT the computed.'''
        pos = _make_position(
          direction='LONG', peak_price=8100.0, atr_entry=50.0,
          manual_stop=7700.0,
        )
        result = get_trailing_stop(pos, current_price=8050.0, atr=50.0)
        assert result == 7700.0, (
          f'D-09: LONG with manual_stop=7700.0 must return 7700.0 directly; '
          f'got {result!r} (computed peak-trail would be 7950.0)'
        )

      def test_manual_stop_overrides_short_computed(self) -> None:
        '''SHORT with manual_stop=7900.0; computed would be 7800 + 2*50 = 7900.
        Returns the override directly. Note: the values happen to match here
        by construction — the test still proves the override path because we
        could pass any value and get it back; we choose 7900.0 to make the
        intent (override-not-computed) the test's invariant rather than a
        coincidental numeric collision.'''
        pos = _make_position(
          direction='SHORT', trough_price=7800.0, atr_entry=50.0,
          manual_stop=7950.0,  # NOT the computed 7900.0
        )
        result = get_trailing_stop(pos, current_price=7850.0, atr=50.0)
        assert result == 7950.0, (
          f'D-09: SHORT with manual_stop=7950.0 must return 7950.0 directly; '
          f'got {result!r} (computed trough+trail would be 7900.0)'
        )

      def test_manual_stop_none_falls_back_to_computed(self) -> None:
        '''manual_stop=None preserves v1.0 computed peak/trough trailing stop.'''
        pos = _make_position(
          direction='LONG', peak_price=8100.0, atr_entry=50.0,
          manual_stop=None,
        )
        result = get_trailing_stop(pos, current_price=8050.0, atr=50.0)
        # Computed: 8100 - 3*50 = 7950
        assert result == 7950.0, (
          f'D-09: manual_stop=None must fall through to computed v1.0 stop; '
          f'expected 7950.0, got {result!r}'
        )

      def test_manual_stop_with_nan_atr_entry_returns_nan(self) -> None:
        '''B-1 NaN-passthrough preserved: NaN atr_entry returns float(nan)
        BEFORE the manual_stop branch is reached. Locks the discipline that
        the manual_stop branch is positioned AFTER the NaN guard.'''
        pos = _make_position(
          direction='LONG', peak_price=8100.0, atr_entry=float('nan'),
          manual_stop=7700.0,
        )
        result = get_trailing_stop(pos, current_price=8050.0, atr=50.0)
        assert math.isnan(result), (
          f'B-1: NaN atr_entry must produce NaN result REGARDLESS of '
          f'manual_stop value; got {result!r}'
        )

      def test_manual_stop_via_get_with_missing_key_falls_back_to_computed(self) -> None:
        '''Pre-migration position dict (no manual_stop key at all):
        position.get('manual_stop') returns None → falls through to computed.
        Locks the defensive .get() choice (RESEARCH §Pitfall 5).'''
        # Build a pos WITHOUT the manual_stop key (simulates pre-migration data
        # loaded transiently before _migrate_v2_to_v3 backfills). _make_position
        # always sets manual_stop, so we construct a bare dict instead.
        pos = {
          'direction': 'LONG',
          'entry_price': 7000.0,
          'entry_date': '2026-01-15',
          'n_contracts': 2,
          'pyramid_level': 0,
          'peak_price': 8100.0,
          'trough_price': None,
          'atr_entry': 50.0,
          # NO 'manual_stop' key
        }
        result = get_trailing_stop(pos, current_price=8050.0, atr=50.0)
        # Computed: 8100 - 3*50 = 7950
        assert result == 7950.0, (
          f'Pitfall 5: position.get(manual_stop) on missing key must return '
          f'None and fall through to computed; expected 7950.0, got {result!r}'
        )

`math` and `get_trailing_stop` already imported at module top. `Position` import already present. `_make_position` is defined in the same file (line 129). No new module-level imports needed.

DO NOT modify any other test in test_sizing_engine.py. The existing `TestExits` trailing-stop tests use `_make_position(...)` without the `manual_stop` kwarg; the new default `manual_stop=None` makes them backward-compatible (None falls through to computed; v1.0 behavior preserved).
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && pytest tests/test_sizing_engine.py::TestManualStopOverride tests/test_sizing_engine.py::TestExits -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "manual = position.get('manual_stop')" sizing_engine.py` exits 0
    - `grep -q "Phase 14 D-09" sizing_engine.py` exits 0 (proves docstring + comment present)
    - `grep -q "if manual is not None:" sizing_engine.py` exits 0
    - `grep -q "manual_stop NOT honored here intentionally" sizing_engine.py` exits 0 (proves the check_stop_hit docstring note present)
    - `grep -c "manual_stop" sizing_engine.py` returns at least 4 (function-body branch + comment + check_stop_hit note + docstring paragraph)
    - `python -c "from sizing_engine import get_trailing_stop; from system_params import Position; pos = Position(direction='LONG', entry_price=7000.0, entry_date='2026-04-15', n_contracts=2, pyramid_level=0, peak_price=8100.0, trough_price=None, atr_entry=50.0, manual_stop=7700.0); assert get_trailing_stop(pos, 8050.0, 50.0) == 7700.0"` exits 0 (smoke: override path)
    - `python -c "from sizing_engine import get_trailing_stop; from system_params import Position; pos = Position(direction='LONG', entry_price=7000.0, entry_date='2026-04-15', n_contracts=2, pyramid_level=0, peak_price=8100.0, trough_price=None, atr_entry=50.0, manual_stop=None); assert get_trailing_stop(pos, 8050.0, 50.0) == 7950.0"` exits 0 (smoke: None falls through)
    - `python -c "from sizing_engine import get_trailing_stop; import math; pos = {'direction': 'LONG', 'entry_price': 7000.0, 'entry_date': '2026-04-15', 'n_contracts': 2, 'pyramid_level': 0, 'peak_price': 8100.0, 'trough_price': None, 'atr_entry': float('nan'), 'manual_stop': 7700.0}; assert math.isnan(get_trailing_stop(pos, 8050.0, 50.0))"` exits 0 (smoke: NaN guard precedence)
    - `python -c "from sizing_engine import get_trailing_stop; pos = {'direction': 'LONG', 'entry_price': 7000.0, 'entry_date': '2026-04-15', 'n_contracts': 2, 'pyramid_level': 0, 'peak_price': 8100.0, 'trough_price': None, 'atr_entry': 50.0}; assert get_trailing_stop(pos, 8050.0, 50.0) == 7950.0"` exits 0 (smoke: missing key path → defensive .get)
    - `grep -q "manual_stop=None" tests/test_sizing_engine.py` exits 0 (proves _make_position kwarg extension)
    - `grep -q "test_manual_stop_overrides_long_computed" tests/test_sizing_engine.py` exits 0
    - `grep -q "test_manual_stop_overrides_short_computed" tests/test_sizing_engine.py` exits 0
    - `grep -q "test_manual_stop_none_falls_back_to_computed" tests/test_sizing_engine.py` exits 0
    - `grep -q "test_manual_stop_with_nan_atr_entry_returns_nan" tests/test_sizing_engine.py` exits 0
    - `grep -q "test_manual_stop_via_get_with_missing_key_falls_back_to_computed" tests/test_sizing_engine.py` exits 0
    - `pytest tests/test_sizing_engine.py::TestManualStopOverride -x -q` exits 0 (5 tests green)
    - `pytest tests/test_sizing_engine.py::TestExits -x -q` exits 0 (existing trailing-stop tests still green; default manual_stop=None falls through to computed)
    - `pytest tests/test_sizing_engine.py -x -q` exits 0 (full file green; no skipped placeholders left in TestManualStopOverride)
    - `grep -c "compute_unrealised_pnl" sizing_engine.py` returns at most the existing count (no new uses; the function is unchanged)
    - Hex-lite preserved: `python -c "import ast, sys; tree = ast.parse(open('sizing_engine.py').read()); forbidden = {'os', 'datetime', 'state_manager', 'notifier', 'dashboard', 'main', 'requests'}; bad = [a.name for n in ast.walk(tree) if isinstance(n, ast.Import) for a in n.names if a.name.split('.')[0] in forbidden] + [n.module for n in ast.walk(tree) if isinstance(n, ast.ImportFrom) and n.module and n.module.split('.')[0] in forbidden]; assert not bad, bad"` exits 0
  </acceptance_criteria>
  <done>
    - sizing_engine.get_trailing_stop has the manual_stop branch positioned AFTER the NaN guard, BEFORE the LONG/SHORT switch; uses defensive .get('manual_stop')
    - Module docstring of get_trailing_stop extended with Phase 14 D-09 paragraph
    - check_stop_hit docstring extended with the "manual_stop NOT honored here intentionally" Phase 14 note (no body change)
    - _make_position helper extended with manual_stop=None kwarg
    - TestManualStopOverride populated with 5 tests covering: LONG override, SHORT override, None falls through, NaN guard precedence, missing-key defensive path
    - All listed grep + python -c smoke checks pass
    - Existing TestExits + full test_sizing_engine.py green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Plan 14-03 modifies a pure-math hex function. No I/O, no environment reads, no datetime calls. The trust boundary for `manual_stop` value validation lives at Plan 14-04's Pydantic ModifyTradeRequest layer (entry from operator); this plan is downstream of that gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | T (Tampering) — manual_stop NaN/inf injected via direct state.json edit | sizing_engine.get_trailing_stop reading position['manual_stop'] | mitigate | Operator could hand-edit state.json to set manual_stop=NaN. The function's NaN-passthrough invariant (B-1) is preserved by positioning the manual_stop branch AFTER the NaN guard on atr_entry — but the manual_stop value itself is returned verbatim if it's a float (including ±inf). Plan 14-04's ModifyTradeRequest validator rejects NaN/inf at the API layer (math.isfinite check). state_manager._validate_loaded_state checks key presence only, not value finiteness on Position fields. Accepted residual risk: an operator manually editing state.json to inject NaN is in-band of "operator with shell access to droplet" which is out-of-scope per RESEARCH threat model. The four-line precedence change does not widen the attack surface beyond what state.json hand-editing already permits. |
| T-14-07 | E (Elevation of privilege) — pre-migration KeyError crash | get_trailing_stop called on a position dict before _migrate_v2_to_v3 runs | mitigate | Defensive `position.get('manual_stop')` (NOT subscript) returns None for missing keys, falls through to computed v1.0 stop. RESEARCH §Pitfall 5 documents this discipline. Test `test_manual_stop_via_get_with_missing_key_falls_back_to_computed` locks it. Mitigates: a transient call during migration (load_state has not yet completed _migrate) does NOT crash with KeyError; the daily signal loop continues to function. |

**ASVS L1 mapping:**
- V5 (Validation): N/A at this layer — manual_stop value is validated upstream by Pydantic ModifyTradeRequest in Plan 14-04. This plan is a pure-math consumer that respects whatever value the upstream gate let through.
- V8 (Logging): No new emission. Pure-math hex must not log per CLAUDE.md "no datetime.now(), no env-var reads inside them" extension.
- V13 (API): N/A — pure-math, not an API surface.

The plan's threat surface is intentionally minimal because it's a pure-math change. The bulk of the Phase 14 attack-surface work is in Plans 14-04 (validation) and 14-05 (XSS).
</threat_model>

<verification>
After Task 1 completes:
- `pytest tests/test_sizing_engine.py -x -q` exits 0 (full file green; 5 new tests in TestManualStopOverride)
- `python -c "from sizing_engine import get_trailing_stop; import inspect; src = inspect.getsource(get_trailing_stop); assert 'manual = position.get' in src; assert 'Phase 14 D-09' in src"` exits 0
- `pytest tests/test_state_manager.py tests/test_sizing_engine.py -x -q` exits 0 (Wave 1 union green; Plan 14-02 + Plan 14-03 cooperate cleanly)
- Full suite still green: `pytest -x -q` exits 0 (modulo documented test_main.py weekend-skip failures)
</verification>

<success_criteria>
- D-09 precedence locked: tests prove manual_stop overrides LONG and SHORT computed stops, falls through on None, respects NaN guard, and handles pre-migration missing-key positions
- Hex-lite preserved: sizing_engine.py still imports only stdlib + system_params + Position; no I/O additions, no datetime
- Plan 14-05 lockstep parity: this plan's `manual = position.get('manual_stop'); if manual is not None: return manual` block is the EXACT pattern Plan 14-05 must replicate in `dashboard.py::_compute_trail_stop_display` (Plan 14-05's TestRenderManualStopBadge proves parity)
- Backward compat: existing TestExits passes — _make_position default `manual_stop=None` falls through to v1.0 computed stop
- Phase 14 D-09 boundary documented: check_stop_hit docstring records the deliberate non-honoring of manual_stop in the daily-loop hit-detection path
</success_criteria>

<output>
After completion, create `.planning/phases/14-trade-journal-mutation-endpoints/14-03-SUMMARY.md`. Required content:
- Files modified (sizing_engine.py + tests/test_sizing_engine.py — line counts before/after)
- The exact 4-line manual_stop branch position in get_trailing_stop (proves placement between NaN guard and direction switch)
- TestManualStopOverride: 5 tests covering precedence, fallback, NaN, missing-key
- Confirmation that check_stop_hit docstring carries the Phase 14 non-honoring note
- Any deviations from this plan with reasoning (Rule 1/2/3 logging)
</output>
