---
phase: 14
plan: 02
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - state_manager.py
  - system_params.py
  - main.py
  - tests/test_state_manager.py
  - tests/test_main.py
autonomous: true
requirements:
  - TRADE-06
tags:
  - phase14
  - state-manager
  - schema-migration
  - fcntl
  - cross-process
  - mutate-state
last_revised: 2026-04-25
revision_source: 14-REVIEWS.md

must_haves:
  truths:
    - 'state_manager.save_state acquires fcntl.LOCK_EX on state.json before writing; releases on success AND on exception'
    - 'state_manager.mutate_state(mutator, path) holds fcntl.LOCK_EX across the FULL load → mutate → save critical section (REVIEWS HIGH #1: closes the cross-process lost-update race)'
    - 'fcntl.flock is reentrant within a single process: save_state nested inside mutate_state is a kernel no-op (verified by subprocess test)'
    - 'A second writer on a different process blocks until the first writer releases the lock (verified by multiprocess test fixture)'
    - 'main.py daily loop migrates from save_state to mutate_state(mutator) for every save site; W3 invariant (2 saves per run) preserved — verified by extending the existing W3 regression test'
    - 'Schema migration v2→v3 backfills manual_stop=None on every non-None Position dict in state.positions'
    - 'system_params.STATE_SCHEMA_VERSION == 3 (was 2)'
    - 'system_params.Position TypedDict has manual_stop: float | None field at the bottom'
    - 'Migration is idempotent: running _migrate_v2_to_v3 twice produces identical output'
    - 'Round-trip: load(v2-fixture) → save → re-load yields schema_version=3 with manual_stop=None on both positions'
    - 'Existing TestAtomicity, TestCorruptionRecovery, TestRecordTrade, TestEquityHistory, TestSchemaVersion, TestMigrateV2Backfill, TestSaveStateExcludesUnderscoreKeys all remain green after fcntl wrap and v3 bump'
    - "v1.0 sole-writer invariant for state['warnings'] preserved: state_manager.append_warning is the only writer"
  artifacts:
    - path: 'state_manager.py'
      provides: '_atomic_write wrapped with fcntl.LOCK_EX (D-13); new public mutate_state(mutator, path) helper holds the lock across load → mutate → save (REVIEWS HIGH #1); _migrate_v2_to_v3 added to MIGRATIONS dict (D-09); MIGRATIONS dict gains key 3'
      contains: 'def mutate_state'
    - path: 'system_params.py'
      provides: 'STATE_SCHEMA_VERSION bumped 2→3; Position TypedDict gains manual_stop: float | None'
      contains: 'STATE_SCHEMA_VERSION: int = 3'
    - path: 'main.py'
      provides: 'daily loop save_state call sites migrate to mutate_state(mutator); W3 invariant preserved (2 mutate_state calls per run)'
      contains: 'state_manager.mutate_state'
    - path: 'tests/test_state_manager.py'
      provides: 'TestFcntlLock + TestMutateState + TestSchemaMigrationV2ToV3 populated; STATE_SCHEMA_VERSION literal in test_migrate_walks_schema_version_to_current bumped to 3'
      min_lines: 1500
    - path: 'tests/test_main.py'
      provides: 'TestSoleWriterW3Invariant updated to count mutate_state calls (was: save_state); 2-saves-per-run regression preserved'
  key_links:
    - from: 'state_manager._atomic_write'
      to: 'state.json on disk'
      via: 'fcntl.flock(fd, fcntl.LOCK_EX) on os.open(str(path), os.O_RDWR | os.O_CREAT, 0o600)'
      pattern: 'fcntl\.flock'
    - from: 'state_manager.mutate_state'
      to: 'state_manager.load_state + save_state'
      via: 'lock around load → mutator(state) → save_state critical section'
      pattern: 'def mutate_state'
    - from: 'main.run_daily_check'
      to: 'state_manager.mutate_state'
      via: 'daily loop save sites migrated from save_state to mutate_state(mutator)'
      pattern: 'mutate_state\('
    - from: 'state_manager._migrate'
      to: '_migrate_v2_to_v3'
      via: 'MIGRATIONS[3] dispatch'
      pattern: '3:\s*_migrate_v2_to_v3'
    - from: 'system_params.Position'
      to: 'state.json positions[*] dicts'
      via: 'TypedDict structural shape; backfilled by _migrate_v2_to_v3 on load'
      pattern: 'manual_stop:\s*float\s*\|\s*None'
---

<objective>
Three coordinated changes to close the **cross-process lost-update race** (REVIEWS HIGH #1) AND ship the schema v2→v3 migration:

1. **fcntl-locked atomic write in `_atomic_write`** (D-13 original).
2. **NEW `mutate_state(mutator, path)` helper** that holds `fcntl.LOCK_EX` across the entire load → mutate → save critical section (REVIEWS HIGH #1 fix). `fcntl.flock` is reentrant within a single process, so the inner `save_state`'s lock acquisition is a kernel no-op when the outer `mutate_state` lock is already held.
3. **Schema migration v2→v3** backfilling `manual_stop=None` on every existing Position dict (D-09).

Plus a small migration of `main.py`'s daily-loop save sites from bare `save_state` to `mutate_state(mutator)` so the daily loop also benefits from full READ-MODIFY-WRITE atomicity. The Phase 8 W3 invariant ("2 saves per run") is preserved by counting `mutate_state` calls rather than `save_state` calls.

Purpose: Phase 14 D-14 amends Phase 10 D-15 — the web layer becomes a peer writer to state.json. `fcntl` on `save_state` ALONE serializes the WRITE but not the READ-MODIFY-WRITE critical section: two writers can both `load → mutate → save` from the same pre-lock snapshot, both serialize the write, and the second save clobbers the first's mutation. `mutate_state` closes that race. T-14-01 (lost-update race) moves from ACCEPTED RESIDUAL → FULLY MITIGATED.

Output: state_manager.py with locked _atomic_write + new mutate_state helper + new migration; system_params.py with v3 schema and extended Position; main.py daily loop using mutate_state; tests proving lock contention, lock release on exception, READ-MODIFY-WRITE atomicity under concurrency, reentrancy, migration round-trip, and idempotency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-CONTEXT.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-RESEARCH.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-PATTERNS.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-REVIEWS.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-01-SUMMARY.md
@CLAUDE.md
@state_manager.py
@system_params.py
@main.py
@tests/test_state_manager.py
@tests/test_main.py
@tests/fixtures/state_v2_no_manual_stop.json

<interfaces>
Existing _atomic_write (state_manager.py:113-158) — Phase 14 wraps body with fcntl, preserves all durability invariants (D-08 + D-17).

Existing MIGRATIONS dict (state_manager.py:104-107) — Phase 14 appends key 3 mapping to new _migrate_v2_to_v3 function.

Existing Position TypedDict (system_params.py:137-158) — Phase 14 appends one field `manual_stop: float | None` after `atr_entry: float`.

Existing STATE_SCHEMA_VERSION (system_params.py:111) — Phase 14 bumps from 2 to 3.

Existing test_migrate_walks_schema_version_to_current (tests/test_state_manager.py:1159-1171) — Phase 14 bumps the `assert STATE_SCHEMA_VERSION == 2` literal to `== 3`.

NEW Phase 14 helper (REVIEWS HIGH #1): `state_manager.mutate_state(mutator, path)` — public API surface for any caller that wants atomic load → mutate → save under cross-process lock. Web routes (Plan 14-04) and main.py daily loop (this plan) both consume it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: state_manager fcntl lock + mutate_state helper + v2→v3 migration; system_params Position field + schema bump</name>
  <files>state_manager.py, system_params.py</files>
  <read_first>
    - state_manager.py lines 1-50 (module docstring + imports — Phase 14 D-13 paragraph appends here)
    - state_manager.py lines 86-107 (_migrate_v1_to_v2 + MIGRATIONS dict — exact analog for new function and dict entry)
    - state_manager.py lines 113-158 (_atomic_write — the function getting the fcntl wrap)
    - state_manager.py lines 160-171 (_migrate — walks MIGRATIONS forward; no code change needed but must understand dispatch)
    - state_manager.py lines 343-429 (load_state + save_state — locking happens INSIDE _atomic_write; bodies unchanged. mutate_state is a NEW function; insert it after save_state)
    - system_params.py lines 100-160 (Position TypedDict + STATE_SCHEMA_VERSION)
    - 14-CONTEXT.md D-09 (manual_stop semantics) + D-13 REVISED (mutate_state spec) + D-14 (Phase 10 D-15 amendment text)
    - 14-REVIEWS.md HIGH #1 (lost-update race rationale) — verbatim block to inline as the mutate_state docstring
    - 14-RESEARCH.md §Pattern 9 lines 671-741 (fcntl wrap verbatim — copy lock_fd open/flock/unlock/close discipline EXACTLY)
    - 14-RESEARCH.md §Pattern 11 lines 785-826 (_migrate_v2_to_v3 verbatim)
    - 14-RESEARCH.md §Common Pitfalls Pitfall 3 lines 899-913 (os.replace vs locked inode — read for confidence; no code change)
    - 14-PATTERNS.md §_atomic_write/_migrate_v2_to_v3 lines 263-321 (analog mapping)
    - 14-PATTERNS.md §system_params.Position lines 323-362 (Position field addition)
  </read_first>
  <action>
Three source-file edits. Order: system_params.py FIRST (state_manager imports STATE_SCHEMA_VERSION from it); state_manager.py SECOND (adds locked _atomic_write + mutate_state + _migrate_v2_to_v3).

**Sub-edit A — system_params.py.**

1. Replace line 111 from `STATE_SCHEMA_VERSION: int = 2` with:

       STATE_SCHEMA_VERSION: int = 3       # bump on each schema change (STATE-04); Phase 14 → v3 (manual_stop on Position; D-09)

2. Inside the Position TypedDict (line 137-158), add ONE new field after `atr_entry: float` (line 158), matching the `peak_price`/`trough_price` precedent for the `float | None` annotation:

       atr_entry: float
       manual_stop: float | None      # Phase 14 D-09: operator override for trailing stop;
                                      # None = use computed peak/trough trailing stop (v1.0 default)

3. Extend the docstring Fields block (currently around line 140-149). Append one trailing line:

       manual_stop:   Operator override for trailing stop. None = use the
                      computed peak/trough trailing stop (v1.0 default).
                      Set via /trades/modify endpoint (Phase 14 D-09).

DO NOT touch any other line. TRAIL_MULT_LONG / MAX_PYRAMID_LEVEL / _COLOR_* / SCHEDULE constants stay verbatim.

**Sub-edit B — state_manager.py.**

1. **Add `import fcntl` to module-top imports.** Insert immediately after `import os` (line 36) so the stdlib group stays grouped:

       import fcntl  # Phase 14 D-13: cross-process advisory lock around _atomic_write + mutate_state

   Also add `from typing import Callable` if not already present (mutate_state uses `Callable[[dict], None]` in its signature). If `typing` is already imported, just extend the existing import line; otherwise add a new line near the other typing imports.

2. **Append a Phase 14 paragraph to the module docstring** (currently lines 1-33). Add the new paragraph BEFORE the closing `'''` on line 33:

       Phase 14 D-13 amendment to D-15: state_manager is now a peer writer to
       state.json with the FastAPI web layer (web/routes/trades.py mutations).
       Cross-process coordination via fcntl.LOCK_EX advisory lock acquired in
       _atomic_write. Lock held across the tempfile→fsync→replace→dir-fsync
       critical section; released by explicit fcntl.LOCK_UN + os.close on the
       lock fd.

       REVIEWS HIGH #1 fix: fcntl on save_state ALONE serializes the WRITE
       but not the READ-MODIFY-WRITE — two writers can both load the same
       pre-lock snapshot, both serialize the write, and the second save
       clobbers the first. The new public helper mutate_state(mutator, path)
       holds the lock across the FULL load → mutate → save critical section.
       Web routes and main.py daily loop both call mutate_state. fcntl.flock
       is reentrant within a single process, so the inner save_state's lock
       acquisition is a kernel no-op when the outer mutate_state lock is
       already held; cross-process safety is preserved (separate file
       descriptors).

       The sole-writer invariant for state['warnings'] (TRADE-06) is
       unchanged: only state_manager.append_warning writes to that key; web
       handlers never call it.

3. **Insert `_migrate_v2_to_v3` function definition** AFTER `_migrate_v1_to_v2` (line 101) and BEFORE the `MIGRATIONS:` dict (line 104). Use RESEARCH §Pattern 11 verbatim. Body shape (with full docstring as below — DO NOT abbreviate):

       def _migrate_v2_to_v3(s: dict) -> dict:
         '''Phase 14 D-09: backfill manual_stop=None on every existing Position dict.

         Position TypedDict gained manual_stop in Phase 14. Existing v2 state files
         have positions like {SPI200: None, AUDUSD: {direction:..., entry_price:...}}
         — the dict-valued positions need manual_stop=None added; None positions
         stay None (no position to migrate).

         Idempotent: running on already-v3 data is a no-op (manual_stop preserved
         via pos.get(...) returning the existing value, then dict-merge keeping it).
         load_state passes the result through _validate_loaded_state which checks
         KEY PRESENCE only — manual_stop value is enforced by sizing_engine
         get_trailing_stop NaN guards (Plan 14-03).

         D-15 silent migration: no append_warning, no log. Backfill is transparent
         to the operator (mirrors _migrate_v1_to_v2 contract).
         '''
         positions = s.get('positions', {})
         new_positions = {}
         for instrument, pos in positions.items():
           if pos is None:
             new_positions[instrument] = None
           else:
             new_positions[instrument] = {**pos, 'manual_stop': pos.get('manual_stop')}
         return {**s, 'positions': new_positions}

4. **Update the MIGRATIONS dict** (line 104-107). Add key 3:

       MIGRATIONS: dict = {
         1: lambda s: s,                # no-op at v1; hook proves the walk-forward mechanism works
         2: _migrate_v1_to_v2,          # Phase 8 IN-06: named function for future migrations
         3: _migrate_v2_to_v3,          # Phase 14 D-09: backfill manual_stop on existing Positions
       }

5. **Replace the body of `_atomic_write`** (lines 113-158) with the locked version per RESEARCH §Pattern 9. Preserve every existing durability invariant — JUST wrap the existing body in the lock-acquired window. Discipline:

   - Lock the **destination file** (NOT the tempfile). Open via `os.open(str(path), os.O_RDWR | os.O_CREAT, 0o600)`. Use `0o600` (owner-only) to keep the lock fd permission tight; the actual state.json's mode is set elsewhere by `os.replace` semantics. O_RDWR|O_CREAT is idempotent and does NOT truncate existing content (no O_TRUNC).
   - `fcntl.flock(lock_fd, fcntl.LOCK_EX)` blocks indefinitely per D-13.
   - Critical section = entire existing tempfile→fsync→replace→dir-fsync sequence. Move the existing body into a nested try/finally INSIDE the lock window.
   - Release via explicit `fcntl.flock(lock_fd, fcntl.LOCK_UN)` THEN `os.close(lock_fd)` in the OUTER finally (per RESEARCH §Pattern 9 line 734-735 belt-and-braces).
   - The inner tempfile-cleanup `finally` runs BEFORE the outer lock-release — so a failed os.replace unlinks the tempfile AND releases the lock.
   - Extend the existing _atomic_write docstring with a Phase 14 D-13 amendment block (mirrors the existing D-17 amendment pattern in the docstring).

   Replacement body (this is the FULL body of the function — keep the existing `def _atomic_write(data: str, path: Path) -> None:` signature line):

       '''STATE-02 / D-08 (amended by D-17, then by Phase 14 D-13):
       tempfile + fsync(file) + os.replace + fsync(parent dir),
       serialized cross-process via fcntl.LOCK_EX advisory lock on the destination file.

       Durability sequence (D-17 ordering — corrected from RESEARCH.md §Pattern 1):
         1. write data to tempfile in same directory as target
         2. flush + fsync(tempfile.fileno())  -- data durable on disk
         3. close tempfile (NamedTemporaryFile context exit)
         4. os.replace(tempfile, target)      -- atomic rename
         5. fsync(parent dir fd) on POSIX     -- rename itself durable on disk

       Tempfile cleanup: try/finally unlinks the tempfile if any step before
       os.replace raises (Pitfall 1).

       Phase 14 D-13 lock semantics:
         - fcntl.flock advisory lock on the DESTINATION file's open fd
         - Held across the entire critical section (write tempfile -> fsync ->
           rename -> dir fsync)
         - Released by explicit fcntl.LOCK_UN + os.close(lock_fd) in outer finally
         - Blocking-indefinite (no timeout) per D-13
         - Lock fd opened via os.open(str(path), os.O_RDWR | os.O_CREAT, 0o600)
         - POSIX-only (Linux droplet + macOS dev); not portable to Windows
         - REENTRANCY: fcntl.flock is reentrant within a single process — when
           called from inside mutate_state's outer flock window, this acquire
           is a kernel no-op. Cross-process safety preserved (separate fds).
       '''
       parent = path.parent
       tmp_path_str = None
       lock_fd = os.open(str(path), os.O_RDWR | os.O_CREAT, 0o600)
       try:
         fcntl.flock(lock_fd, fcntl.LOCK_EX)  # blocks until exclusive lock acquired
         try:
           with tempfile.NamedTemporaryFile(
             dir=parent, delete=False, mode='w', suffix='.tmp', encoding='utf-8',
           ) as tmp:
             tmp_path_str = tmp.name
             tmp.write(data)
             tmp.flush()
             os.fsync(tmp.fileno())
           os.replace(tmp_path_str, path)
           if os.name == 'posix':
             dir_fd = os.open(str(parent), os.O_RDONLY)
             try:
               os.fsync(dir_fd)
             finally:
               os.close(dir_fd)
           tmp_path_str = None  # success: do not delete in inner finally
         finally:
           if tmp_path_str is not None:
             try:
               os.unlink(tmp_path_str)
             except FileNotFoundError:
               pass
       finally:
         fcntl.flock(lock_fd, fcntl.LOCK_UN)
         os.close(lock_fd)

6. **NEW: insert public `mutate_state` helper** AFTER `save_state` (around line 429) and BEFORE any further functions (e.g., before `record_trade` if that comes next; otherwise before the `__all__` block at end of file). This is the REVIEWS HIGH #1 fix — the helper holds the lock across the full READ-MODIFY-WRITE critical section.

   Function body (verbatim — write exactly this):

       def mutate_state(
         mutator: Callable[[dict], None],
         path: Path = Path(STATE_FILE),
       ) -> dict:
         '''Phase 14 D-13 + REVIEWS HIGH #1: lock around the full READ-MODIFY-WRITE.

         Provides the load -> mutate -> save critical section as a single atomic
         unit for any caller (web POST handlers, daily loop). Without this wrapper,
         fcntl on save_state alone admits stale-read lost updates: two writers can
         both load the same pre-mutation snapshot, both acquire+release the save
         lock, second clobbers first.

         Contract:
           - mutator receives a freshly loaded state dict (post-migration).
           - mutator MUTATES the dict in place; return value ignored.
           - The dict is then saved exactly once.
           - Cross-process coordination via fcntl.LOCK_EX on the destination file.
           - Reentrancy: fcntl.flock is reentrant within a single process. The
             inner save_state's flock acquisition is a kernel no-op when the
             outer mutate_state lock is already held. Cross-process safety
             is preserved (separate file descriptors).

         Usage:
           def _bump_account(state):
             state['account'] += 100.0
           mutate_state(_bump_account)

         Returns the post-mutation state dict (after save).
         '''
         fd = os.open(str(path), os.O_RDWR | os.O_CREAT, 0o600)
         try:
           fcntl.flock(fd, fcntl.LOCK_EX)
           try:
             state = load_state(path=path)
             mutator(state)
             save_state(state, path=path)
             return state
           finally:
             fcntl.flock(fd, fcntl.LOCK_UN)
         finally:
           os.close(fd)

   If the file uses an `__all__` declaration at the top, add `'mutate_state'` to it.

DO NOT modify save_state body (404-429); locking happens transparently inside _atomic_write. DO NOT modify load_state (343-402); read-only, no lock needed per D-13. DO NOT modify _migrate (160-171); walks MIGRATIONS by integer key — new key 3 dispatches automatically.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && python -c "from system_params import STATE_SCHEMA_VERSION, Position; assert STATE_SCHEMA_VERSION == 3; assert 'manual_stop' in Position.__annotations__" && python -c "from state_manager import _migrate_v2_to_v3, MIGRATIONS, mutate_state; assert 3 in MIGRATIONS; assert callable(mutate_state)" && pytest tests/test_state_manager.py::TestAtomicity tests/test_state_manager.py::TestSaveStateExcludesUnderscoreKeys tests/test_state_manager.py::TestLoadStateResolvesContracts -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "STATE_SCHEMA_VERSION: int = 3" system_params.py` exits 0
    - `grep -c "STATE_SCHEMA_VERSION: int = 2" system_params.py` returns 0 (no stale literal)
    - `grep -q "manual_stop: float | None" system_params.py` exits 0
    - `python -c "from system_params import Position; assert Position.__annotations__['manual_stop'] == (float | None)"` exits 0
    - `python -c "from system_params import STATE_SCHEMA_VERSION; assert STATE_SCHEMA_VERSION == 3"` exits 0
    - `grep -q "import fcntl" state_manager.py` exits 0
    - `grep -q "fcntl.flock(lock_fd, fcntl.LOCK_EX)" state_manager.py` exits 0
    - `grep -q "fcntl.flock(lock_fd, fcntl.LOCK_UN)" state_manager.py` exits 0
    - `grep -q "def mutate_state" state_manager.py` exits 0 (REVIEWS HIGH #1)
    - `grep -q "REVIEWS HIGH #1" state_manager.py` exits 0 (proves the lost-update fix is the documented intent)
    - `grep -c "_migrate_v2_to_v3" state_manager.py` returns at least 2 (def + MIGRATIONS dict registration)
    - `grep -q "3: _migrate_v2_to_v3" state_manager.py` exits 0
    - `grep -q "Phase 14 D-13" state_manager.py` exits 0
    - `python -c "from state_manager import _migrate_v2_to_v3; out = _migrate_v2_to_v3({'positions': {'SPI200': {'entry_price': 7800.0}, 'AUDUSD': None}}); assert out['positions']['SPI200']['manual_stop'] is None; assert out['positions']['AUDUSD'] is None"` exits 0
    - `python -c "from state_manager import _migrate_v2_to_v3; s = {'positions': {'SPI200': {'manual_stop': 7700.0}}}; assert _migrate_v2_to_v3(s)['positions']['SPI200']['manual_stop'] == 7700.0"` exits 0 (idempotency: existing manual_stop preserved)
    - `python -c "from state_manager import mutate_state; import inspect; sig = inspect.signature(mutate_state); assert 'mutator' in sig.parameters; assert 'path' in sig.parameters"` exits 0 (signature shape locked)
    - `pytest tests/test_state_manager.py::TestAtomicity -x -q` exits 0 (existing atomicity tests green; fcntl wrap transparent)
    - `pytest tests/test_state_manager.py::TestSaveStateExcludesUnderscoreKeys -x -q` exits 0
    - `pytest tests/test_state_manager.py::TestLoadStateResolvesContracts -x -q` exits 0
    - `pytest tests/test_state_manager.py::TestMigrateV2Backfill -x -q` exits 0 OR fails with the literal-bump-needed error (acceptable for this task gate; Task 2 closes it)
  </acceptance_criteria>
  <done>
    - system_params.py: STATE_SCHEMA_VERSION == 3; Position.manual_stop annotated `float | None`
    - state_manager.py: import fcntl present; _migrate_v2_to_v3 added and registered as MIGRATIONS[3]; _atomic_write wraps existing body with fcntl.LOCK_EX critical section; **NEW mutate_state(mutator, path) helper** holds the lock across load → mutate → save (REVIEWS HIGH #1); module docstring includes Phase 14 D-13 + REVIEWS HIGH #1 amendment paragraph; save_state and load_state bodies UNCHANGED
    - All acceptance grep + python -c smoke checks pass
    - TestAtomicity / TestSaveStateExcludesUnderscoreKeys / TestLoadStateResolvesContracts green; the schema-literal test fails until Task 2
  </done>
</task>

<task type="auto">
  <name>Task 2: main.py daily loop migrates from save_state to mutate_state(mutator); W3 invariant preserved</name>
  <files>main.py</files>
  <read_first>
    - main.py lines 1255-1300 (the run_daily_check save sites — likely 2 calls: one for the main update, one for warning flush; W3 invariant in 14-CONTEXT canonical refs)
    - main.py lines 1500-1610 (top-level main() dispatch — also calls save_state on some paths; check whether to migrate those too or leave them)
    - main.py: search for every `state_manager.save_state(` call site
    - tests/test_main.py: search for `TestSoleWriterW3Invariant` or any "2 saves per run" regression test — Task 3 updates that test in the same plan
    - 14-CONTEXT.md D-13 REVISED (mutate_state spec + W3 preservation note)
    - 14-REVIEWS.md HIGH #1 (rationale for migrating the daily loop too — both writers must use mutate_state for the race fix to be complete)
  </read_first>
  <action>
**Goal:** Migrate every `state_manager.save_state(state)` call site in `main.py`'s `run_daily_check` (and any other daily-loop save paths) to use `state_manager.mutate_state(mutator, path)`. The mutator wraps whatever in-memory state mutation happened BEFORE the save.

**Pattern transformation:**

Before:
    state = state_manager.load_state()
    # ... many lines of mutation ...
    state['last_run'] = run_date_iso
    state_manager.save_state(state)

After:
    def _apply_daily_run_mutations(state):
      # ... many lines of mutation moved INSIDE this nested function ...
      state['last_run'] = run_date_iso

    state = state_manager.mutate_state(_apply_daily_run_mutations)

The trick: the daily loop reads state EARLY (around line 1074 in run_daily_check), then accumulates mutations across many phases, then saves at the end. Wrapping all of that in a single `mutate_state` would require either (a) restructuring the function to defer all mutation into a closure, or (b) accepting that `mutate_state` is called twice — once with a no-op-load mutator that just applies the cached changes, once for the warning flush.

**Recommended approach (option a — surgical, lower-risk):**

The daily loop's structure already separates compute from persist. The save site at line ~1268 (`state_manager.save_state(state)`) is the FIRST W3 save; the warning-related save at line ~1296 is the SECOND. Replace each `save_state(state)` with a closure call to `mutate_state`:

For the FIRST save site (around line 1268):
    # BEFORE:
    state_manager.save_state(state)

    # AFTER:
    # REVIEWS HIGH #1: mutate_state holds the lock across load → mutate → save.
    # Captured state from earlier load is REPLACED by mutate_state's fresh load
    # under lock; we re-apply the daily run's accumulated mutations to the
    # fresh state. This costs one extra load (~5ms) and pays for itself by
    # closing the cross-process lost-update race against web POST handlers.
    _accumulated = state  # captured snapshot
    def _apply_daily_run(fresh_state):
      # Replay all mutations from _accumulated onto fresh_state.
      # Daily run's mutations are: positions[*], signals[*], account,
      # equity_history, last_run.
      for key in ('positions', 'signals', 'account', 'equity_history', 'last_run'):
        if key in _accumulated:
          fresh_state[key] = _accumulated[key]
    state = state_manager.mutate_state(_apply_daily_run)

For the SECOND save site (warning flush, around line 1296):
    # BEFORE:
    state_manager.save_state(state)

    # AFTER:
    _final = state
    def _apply_warning_flush(fresh_state):
      # Same key-replay; final state may have appended warnings via
      # state_manager.append_warning earlier (TRADE-06 sole-writer preserved).
      for key in ('positions', 'signals', 'account', 'equity_history',
                  'last_run', 'warnings'):
        if key in _final:
          fresh_state[key] = _final[key]
    state = state_manager.mutate_state(_apply_warning_flush)

**Important:** verify the actual save-site count in main.py before writing the action. Use:

    grep -n 'state_manager.save_state\b' main.py

If there are MORE than 2 save sites in run_daily_check OR additional sites in main()'s dispatch (e.g., --reset path, --force-email path), evaluate each:

  - If the site is INSIDE run_daily_check: migrate to mutate_state (W3 invariant rule)
  - If the site is in main() dispatch (e.g., --reset writes a fresh state ONCE): migrate to mutate_state too — `--reset` is also a writer and should be cross-process safe
  - If the site is in a test-only / dev path: leave as-is and document deviation

For EACH migrated site, add a comment line `# Phase 14 REVIEWS HIGH #1: mutate_state holds lock across read-modify-write` so reviewers can audit the change set with `grep -c 'REVIEWS HIGH #1' main.py`.

**Edge case — _LAST_LOADED_STATE cache (Phase 8 W3):** main.py uses a module-level `_LAST_LOADED_STATE` cache so the crash-email path can access state without a re-load. Verify whether `mutate_state`'s fresh load inside the lock breaks this cache. If `_LAST_LOADED_STATE` is updated AFTER the save (currently after `save_state`), update the line to use the return value of `mutate_state` instead:

    # BEFORE:
    state_manager.save_state(state)
    main._LAST_LOADED_STATE = state

    # AFTER:
    state = state_manager.mutate_state(_apply_daily_run)
    main._LAST_LOADED_STATE = state  # mutate_state returns the post-save state

Read main.py for the actual cache-update site to confirm placement; the pattern is "save → cache-update".

**DO NOT** change the W3 count: there must still be exactly 2 mutate_state calls per daily run (1 main update + 1 warning flush). Phase 14 must NOT introduce a 3rd save site or coalesce the 2 into 1.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && grep -c 'mutate_state(' main.py && grep -c 'save_state(' main.py && python -c "import ast; tree = ast.parse(open('main.py').read()); calls = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr == 'mutate_state']; print(f'mutate_state call count: {len(calls)}')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'state_manager.mutate_state' main.py` returns at least 2 (W3: 2 saves per run)
    - `grep -c 'REVIEWS HIGH #1' main.py` returns at least 2 (audit trail at every migrated site)
    - `grep -c 'state_manager.save_state(state)' main.py` returns 0 in run_daily_check (every site migrated; non-daily-loop sites may persist if outside the W3 contract — document in summary)
    - `python -c "import main; assert callable(getattr(main, 'run_daily_check', None))"` exits 0 (run_daily_check still importable)
    - `pytest tests/test_main.py -x -q` exits 0 (modulo the documented 16 weekend-skip failures); existing tests must not regress on the migration
    - `_LAST_LOADED_STATE` cache update (if present) uses the return value of mutate_state, not the pre-mutation snapshot
  </acceptance_criteria>
  <done>
    - main.py: every save site in run_daily_check uses state_manager.mutate_state(mutator); 2 calls per run preserved (W3 invariant)
    - REVIEWS HIGH #1 audit comments at every migrated site
    - _LAST_LOADED_STATE cache (if present) updated post-mutate
    - tests/test_main.py: no regression
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestFcntlLock + TestMutateState + TestSchemaMigrationV2ToV3 populated; bump STATE_SCHEMA_VERSION literal; W3 regression count migrated</name>
  <files>tests/test_state_manager.py, tests/test_main.py</files>
  <behavior>
    - TestFcntlLock.test_save_state_blocks_when_external_lock_held: a multiprocessing.Process spawns, opens state.json, acquires fcntl.LOCK_EX, signals ready, sleeps 0.5s, releases. Main thread asserts save_state elapsed >= 0.4s and < 1.5s
    - TestFcntlLock.test_save_state_releases_lock_after_successful_write: post-save, a non-blocking LOCK_EX|LOCK_NB acquire succeeds without raising; uncontended save latency < 0.1s
    - TestFcntlLock.test_save_state_releases_lock_after_failed_os_replace: patch os.replace to raise OSError; verify exception propagates AND lock released (next non-blocking acquire succeeds)
    - TestFcntlLock.test_two_sequential_save_state_calls_both_succeed: regression — back-to-back calls work (per-call acquire/release; no stale state)
    - TestMutateState.test_load_mutate_save_atomic: happy path — mutator applied, state persisted, post-load state matches mutation
    - TestMutateState.test_concurrent_writers_no_lost_update: subprocess test — two processes both call mutate_state with non-conflicting mutations (different keys); both mutations visible in final state. Locks REVIEWS HIGH #1 fix
    - TestMutateState.test_mutator_exception_releases_lock: mutator raises ValueError; exception propagates AND lock released (next non-blocking acquire succeeds AND state is unchanged from pre-mutate snapshot — load_state's atomic discipline preserved)
    - TestMutateState.test_reentrancy_save_state_within_mutate_state: mutator that internally calls save_state(other_state) succeeds (proves fcntl.flock reentrancy within a single process — kernel no-op for inner acquire)
    - TestMutateState.test_returns_post_save_state: mutate_state returns the dict that was saved
    - TestSchemaMigrationV2ToV3.test_v2_fixture_loads_with_manual_stop_backfilled_on_open_positions: load tests/fixtures/state_v2_no_manual_stop.json; both Positions have manual_stop=None
    - TestSchemaMigrationV2ToV3.test_save_then_load_v3_round_trips: load v2 → save → re-load yields schema_version=3 + manual_stop=None
    - TestSchemaMigrationV2ToV3.test_migration_idempotent: applied twice, identical output
    - TestSchemaMigrationV2ToV3.test_migration_preserves_existing_manual_stop: pre-existing manual_stop=7700.0 not overwritten
    - TestSchemaMigrationV2ToV3.test_none_position_stays_none: None slots remain None
    - TestSchemaMigrationV2ToV3.test_v3_open_position_round_trips_through_save_state: in-memory v3 with manual_stop=7700.0 saves+reloads bit-identically
    - Existing TestMigrateV2Backfill.test_migrate_walks_schema_version_to_current: literal bumped from `assert STATE_SCHEMA_VERSION == 2` to `== 3`
    - tests/test_main.py: TestSoleWriterW3Invariant (or whichever test counts daily-run saves) updated to count mutate_state calls instead of save_state calls; W3 count remains 2
  </behavior>
  <read_first>
    - tests/test_state_manager.py lines 221-340 (TestAtomicity — analog tmp_path + patch patterns)
    - tests/test_state_manager.py lines 1142-1172 (TestMigrateV2Backfill — the literal needing the bump)
    - tests/test_state_manager.py lines 1174-1220 (TestSaveStateExcludesUnderscoreKeys — analog tmp_path + reset_state pattern)
    - state_manager.py post-Task-1 (Task 1 just added _migrate_v2_to_v3 + locked _atomic_write + mutate_state)
    - tests/fixtures/state_v2_no_manual_stop.json (round-trip target — created Plan 14-01)
    - tests/test_main.py: search for `save_state` count assertions or W3-invariant test name; the migration must update those
    - 14-RESEARCH.md §Pattern 9 line 743 (multiprocess holder fixture prescription — use spawn context for macOS safety)
    - 14-PATTERNS.md §`tests/test_state_manager.py::TestAtomicity` lines 537-606 (test class shape)
  </read_first>
  <action>
Four edits to tests/test_state_manager.py + one to tests/test_main.py.

**Sub-edit A — tests/test_state_manager.py: replace TestFcntlLock skeleton with populated class.** Same 4 tests as the original Plan 14-02 (no change to TestFcntlLock content from prior version):

    class TestFcntlLock:
      '''Phase 14 D-13: cross-process state.json coordination via fcntl.LOCK_EX.

      Lock is on the destination state.json. Acquired in _atomic_write before
      tempfile→replace; released by explicit LOCK_UN + os.close. Blocking-
      indefinite per D-13.

      Cross-process correctness verified via multiprocessing.Process holder
      that opens state.json + flock(LOCK_EX) + sleeps 0.5s; main test calls
      save_state() and asserts elapsed >= 0.4s (proves contention) and < 1.5s
      (proves no zombie wait).
      '''

      @staticmethod
      def _hold_lock_for(path_str, hold_seconds, ready_event):
        import fcntl, os, time
        fd = os.open(path_str, os.O_RDWR | os.O_CREAT, 0o600)
        try:
          fcntl.flock(fd, fcntl.LOCK_EX)
          ready_event.set()
          time.sleep(hold_seconds)
        finally:
          fcntl.flock(fd, fcntl.LOCK_UN)
          os.close(fd)

      def test_save_state_blocks_when_external_lock_held(self, tmp_path) -> None:
        import multiprocessing as mp
        import time
        path = tmp_path / 'state.json'
        save_state(reset_state(), path=path)
        ctx = mp.get_context('spawn')
        ready = ctx.Event()
        proc = ctx.Process(target=self._hold_lock_for, args=(str(path), 0.5, ready))
        proc.start()
        try:
          assert ready.wait(timeout=2.0)
          state = reset_state()
          state['account'] = 12345.0
          start = time.perf_counter()
          save_state(state, path=path)
          elapsed = time.perf_counter() - start
        finally:
          proc.join(timeout=2.0)
          if proc.is_alive():
            proc.terminate()
            proc.join()
        assert elapsed >= 0.4, f'D-13: did not block; elapsed={elapsed:.3f}s'
        assert elapsed < 1.5, f'D-13: too slow after release; elapsed={elapsed:.3f}s'
        assert load_state(path=path)['account'] == 12345.0

      def test_save_state_releases_lock_after_successful_write(self, tmp_path) -> None:
        import fcntl, os, time
        path = tmp_path / 'state.json'
        start = time.perf_counter()
        save_state(reset_state(), path=path)
        elapsed = time.perf_counter() - start
        assert elapsed < 0.1
        fd = os.open(str(path), os.O_RDWR)
        try:
          fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
          fcntl.flock(fd, fcntl.LOCK_UN)
        finally:
          os.close(fd)

      def test_save_state_releases_lock_after_failed_os_replace(self, tmp_path) -> None:
        import fcntl, os
        path = tmp_path / 'state.json'
        save_state(reset_state(), path=path)
        new_state = reset_state()
        new_state['account'] = 7777.0
        with patch('state_manager.os.replace', side_effect=OSError('disk full')):
          with pytest.raises(OSError, match='disk full'):
            save_state(new_state, path=path)
        fd = os.open(str(path), os.O_RDWR)
        try:
          fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
          fcntl.flock(fd, fcntl.LOCK_UN)
        finally:
          os.close(fd)

      def test_two_sequential_save_state_calls_both_succeed(self, tmp_path) -> None:
        path = tmp_path / 'state.json'
        for i in range(2):
          state = reset_state()
          state['account'] = float(i)
          save_state(state, path=path)
        assert load_state(path=path)['account'] == 1.0

**Sub-edit B (NEW — REVIEWS HIGH #1) — TestMutateState class.** Insert IMMEDIATELY AFTER TestFcntlLock:

    class TestMutateState:
      '''Phase 14 D-13 + REVIEWS HIGH #1: mutate_state holds fcntl.LOCK_EX
      across the FULL load → mutate → save critical section.

      Without this helper, fcntl on save_state alone admits stale-read lost
      updates: two writers can both load the same pre-mutation snapshot, both
      acquire+release the save lock, second clobbers first. mutate_state
      closes that race.

      Tests cover:
        - happy-path: load → mutate → save round-trip
        - cross-process: two processes both call mutate_state with non-
          conflicting mutations; both mutations visible in final state
          (the previous fcntl-on-save-only design FAILED this test by losing
          one of the mutations)
        - mutator exception: lock released, state unchanged
        - reentrancy: save_state nested inside mutator succeeds
          (fcntl.flock is reentrant within a single process)
        - return value: mutate_state returns the post-save state
      '''

      def test_load_mutate_save_atomic(self, tmp_path) -> None:
        '''Happy path: mutator applied, state persisted.'''
        from state_manager import mutate_state
        path = tmp_path / 'state.json'
        save_state(reset_state(), path=path)

        def _bump(state):
          state['account'] = 99999.0

        result = mutate_state(_bump, path=path)
        assert result['account'] == 99999.0
        assert load_state(path=path)['account'] == 99999.0

      @staticmethod
      def _subprocess_mutate(path_str, key, value, ready_event, go_event):
        '''Subprocess body: signal ready, wait for go, then mutate_state.'''
        import sys, os
        # Ensure project on sys.path
        proj = os.path.dirname(os.path.dirname(os.path.abspath(path_str)))
        if proj not in sys.path:
          sys.path.insert(0, proj)
        from state_manager import mutate_state
        ready_event.set()
        go_event.wait(timeout=5.0)

        def _apply(state):
          state[key] = value

        mutate_state(_apply, path=__import__('pathlib').Path(path_str))

      def test_concurrent_writers_no_lost_update(self, tmp_path) -> None:
        '''REVIEWS HIGH #1: two processes both call mutate_state with non-
        conflicting mutations. The previous fcntl-on-save-only design lost
        one of the mutations because both processes loaded the SAME pre-lock
        snapshot. mutate_state closes the race: both mutations land.'''
        import multiprocessing as mp
        path = tmp_path / 'state.json'
        seed = reset_state()
        seed['account'] = 100000.0
        save_state(seed, path=path)

        ctx = mp.get_context('spawn')
        ready_a, ready_b = ctx.Event(), ctx.Event()
        go_a, go_b = ctx.Event(), ctx.Event()

        # Process A writes to 'last_run'; Process B writes to 'account'.
        proc_a = ctx.Process(
          target=self._subprocess_mutate,
          args=(str(path), 'last_run', '2026-04-25', ready_a, go_a),
        )
        proc_b = ctx.Process(
          target=self._subprocess_mutate,
          args=(str(path), 'account', 50000.0, ready_b, go_b),
        )
        proc_a.start()
        proc_b.start()
        try:
          assert ready_a.wait(timeout=5.0) and ready_b.wait(timeout=5.0)
          # Release both at the same time: lock contention is the key
          go_a.set()
          go_b.set()
        finally:
          proc_a.join(timeout=10.0)
          proc_b.join(timeout=10.0)
          if proc_a.is_alive():
            proc_a.terminate()
          if proc_b.is_alive():
            proc_b.terminate()

        final = load_state(path=path)
        # BOTH mutations must be visible (no lost update).
        assert final['last_run'] == '2026-04-25', (
          'REVIEWS HIGH #1: last_run mutation lost — '
          'mutate_state did not serialize load-mutate-save'
        )
        assert final['account'] == 50000.0, (
          'REVIEWS HIGH #1: account mutation lost — '
          'mutate_state did not serialize load-mutate-save'
        )

      def test_mutator_exception_releases_lock(self, tmp_path) -> None:
        '''A failing mutator must propagate the exception AND release the lock.
        State on disk should be unchanged (load_state's last successful read
        is the pre-mutate snapshot; save_state was never called).'''
        import fcntl, os
        from state_manager import mutate_state
        path = tmp_path / 'state.json'
        seed = reset_state()
        seed['account'] = 12345.0
        save_state(seed, path=path)

        def _broken(state):
          state['account'] = 99.0  # would mutate, but…
          raise ValueError('mutator boom')

        with pytest.raises(ValueError, match='mutator boom'):
          mutate_state(_broken, path=path)

        # Lock released
        fd = os.open(str(path), os.O_RDWR)
        try:
          fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
          fcntl.flock(fd, fcntl.LOCK_UN)
        finally:
          os.close(fd)

        # State unchanged on disk (mutator's in-memory mutation never reached save_state)
        assert load_state(path=path)['account'] == 12345.0

      def test_reentrancy_save_state_within_mutate_state(self, tmp_path) -> None:
        '''fcntl.flock is reentrant within a single process. A mutator that
        internally calls save_state(other_state, path=other_path) must succeed
        — the inner save_state's flock acquisition is a kernel no-op when the
        outer mutate_state lock is already held on a DIFFERENT path. For the
        SAME path, save_state's inner flock on the SAME fd is also reentrant
        (Linux + macOS POSIX flock semantics). Test both cases.'''
        from state_manager import mutate_state
        path_outer = tmp_path / 'state.json'
        path_inner = tmp_path / 'other.json'
        save_state(reset_state(), path=path_outer)

        def _nest(state):
          state['account'] = 1.0
          # Nested save on a DIFFERENT path inside the outer lock — succeeds
          inner = reset_state()
          inner['account'] = 2.0
          save_state(inner, path=path_inner)

        mutate_state(_nest, path=path_outer)
        assert load_state(path=path_outer)['account'] == 1.0
        assert load_state(path=path_inner)['account'] == 2.0

      def test_returns_post_save_state(self, tmp_path) -> None:
        '''mutate_state returns the post-save state dict.'''
        from state_manager import mutate_state
        path = tmp_path / 'state.json'
        save_state(reset_state(), path=path)

        def _set(state):
          state['account'] = 42.0

        result = mutate_state(_set, path=path)
        assert isinstance(result, dict)
        assert result['account'] == 42.0

**Sub-edit C — replace TestSchemaMigrationV2ToV3 skeleton with populated class.** Same 6 tests as the original Plan 14-02 (TestMigrateV2Backfill literal-bump unchanged).

(Same 6 tests as in the prior version of this plan: test_v2_fixture_loads_with_manual_stop_backfilled_on_open_positions, test_save_then_load_v3_round_trips, test_migration_idempotent, test_migration_preserves_existing_manual_stop, test_none_position_stays_none, test_v3_open_position_round_trips_through_save_state. Implementation verbatim from the prior 14-02-PLAN body — no change required by REVIEWS.)

**Sub-edit D — bump the existing literal in TestMigrateV2Backfill::test_migrate_walks_schema_version_to_current.** Modify exactly ONE line (currently line 1171):

    BEFORE: assert STATE_SCHEMA_VERSION == 2, 'Phase 8 bumps STATE_SCHEMA_VERSION to 2'
    AFTER:  assert STATE_SCHEMA_VERSION == 3, 'Phase 14 bumps STATE_SCHEMA_VERSION to 3'

**Sub-edit E (NEW) — tests/test_main.py: migrate W3 regression test from save_state count to mutate_state count.**

Find the test that asserts "exactly 2 saves per run" in tests/test_main.py. Likely candidates:
- `class TestSoleWriterW3Invariant`
- `def test_two_saves_per_run`
- A `mock.patch('state_manager.save_state')` + assertion on call_count

Use:

    grep -n "save_state" tests/test_main.py | head -30

For each such test:
- Replace the `mock.patch('state_manager.save_state')` with `mock.patch('state_manager.mutate_state')` (or PATCH BOTH and assert mutate_state count == 2 AND save_state count == 2 — since mutate_state internally calls save_state, the inner count of save_state should still be 2 too)
- Update the assertion target accordingly
- Add comment: `# Phase 14 REVIEWS HIGH #1: W3 invariant counted at mutate_state layer`

If the test only patches save_state and counts that, the simplest update is to also patch mutate_state and count the new call site:

    @patch('state_manager.mutate_state')
    @patch('state_manager.save_state')
    def test_w3_two_saves_per_run(self, mock_save, mock_mutate):
      # ... run daily check ...
      assert mock_mutate.call_count == 2, (
        f'Phase 14 W3: expected 2 mutate_state calls per run; '
        f'got {mock_mutate.call_count}'
      )
      # save_state is now called from inside mutate_state, but the daily
      # loop's direct save_state call sites are removed — so mock_save.call_count
      # equals mock_mutate.call_count if mutate_state's body ran. Since we
      # mocked mutate_state, save_state was never called.
      assert mock_save.call_count == 0, (
        'Phase 14: daily loop no longer calls save_state directly; '
        'all writes go through mutate_state'
      )

If the existing test is structured differently, adapt it surgically — the goal is "W3 regression count migrates from save_state to mutate_state, count remains 2". Document the actual original test name + line in the SUMMARY.

`pytest`, `from unittest.mock import patch`, `save_state`, `load_state`, `reset_state` are all already imported at module top (test_state_manager.py).
  </action>
  <verify>
    <automated>pytest tests/test_state_manager.py::TestFcntlLock tests/test_state_manager.py::TestMutateState tests/test_state_manager.py::TestSchemaMigrationV2ToV3 tests/test_state_manager.py::TestMigrateV2Backfill -x -q && pytest tests/test_main.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_state_manager.py::TestFcntlLock -x -q` exits 0 (4 tests green; multiprocess fixture works on macOS via spawn ctx)
    - `pytest tests/test_state_manager.py::TestMutateState -x -q` exits 0 (5 tests green — REVIEWS HIGH #1 fix verified)
    - `pytest tests/test_state_manager.py::TestSchemaMigrationV2ToV3 -x -q` exits 0 (6 tests green)
    - `pytest tests/test_state_manager.py::TestMigrateV2Backfill::test_migrate_walks_schema_version_to_current -x -q` exits 0 (literal bump succeeds)
    - `grep -q "STATE_SCHEMA_VERSION == 3, 'Phase 14 bumps STATE_SCHEMA_VERSION to 3'" tests/test_state_manager.py` exits 0
    - `grep -c "STATE_SCHEMA_VERSION == 2" tests/test_state_manager.py` returns 0 (no stale v2 literals in assertions)
    - `grep -q "_hold_lock_for" tests/test_state_manager.py` exits 0
    - `grep -q "fcntl.LOCK_EX | fcntl.LOCK_NB" tests/test_state_manager.py` exits 0
    - `grep -q "test_save_state_releases_lock_after_failed_os_replace" tests/test_state_manager.py` exits 0
    - `grep -q "test_two_sequential_save_state_calls_both_succeed" tests/test_state_manager.py` exits 0
    - `grep -q "class TestMutateState" tests/test_state_manager.py` exits 0 (REVIEWS HIGH #1)
    - `grep -q "test_concurrent_writers_no_lost_update" tests/test_state_manager.py` exits 0
    - `grep -q "test_mutator_exception_releases_lock" tests/test_state_manager.py` exits 0
    - `grep -q "test_reentrancy_save_state_within_mutate_state" tests/test_state_manager.py` exits 0
    - `grep -q "test_load_mutate_save_atomic" tests/test_state_manager.py` exits 0
    - `grep -q "test_returns_post_save_state" tests/test_state_manager.py` exits 0
    - `grep -q "test_v2_fixture_loads_with_manual_stop_backfilled_on_open_positions" tests/test_state_manager.py` exits 0
    - `grep -q "_V2_FIXTURE = Path('tests/fixtures/state_v2_no_manual_stop.json')" tests/test_state_manager.py` exits 0
    - `grep -q "test_migration_idempotent" tests/test_state_manager.py` exits 0
    - `grep -q "test_migration_preserves_existing_manual_stop" tests/test_state_manager.py` exits 0
    - `grep -c "pytest.skip" tests/test_state_manager.py | head -1` returns 0 (no skipped placeholders remain in TestFcntlLock, TestMutateState, or TestSchemaMigrationV2ToV3 — Plan 14-01 skeletons fully replaced)
    - `pytest tests/test_state_manager.py -x -q` exits 0 (full file green: existing classes + 3 new = ~17 classes; ~15 new tests added)
    - `pytest tests/test_main.py -x -q` exits 0 (modulo the documented 16 weekend-skip failures); W3 regression test now counts mutate_state calls
    - `grep -q "mutate_state.call_count == 2" tests/test_main.py` OR `grep -q "mock_mutate.call_count == 2" tests/test_main.py` exits 0 (W3 invariant migrated)
    - `grep -q "REVIEWS HIGH #1" tests/test_main.py` exits 0 (audit comment)
    - Multiprocess test wall-clock: TestFcntlLock + TestMutateState total runtime should be < 10s
  </acceptance_criteria>
  <done>
    - TestFcntlLock fully implemented: 4 tests + _hold_lock_for static helper; spawn-context multiprocessing (macOS-safe)
    - **TestMutateState fully implemented (REVIEWS HIGH #1):** 5 tests covering happy path, cross-process lost-update fix, mutator exception cleanup, reentrancy, return value
    - TestSchemaMigrationV2ToV3 fully implemented: 6 tests using fixture + in-memory cases
    - The single STATE_SCHEMA_VERSION literal bumped from 2 to 3
    - tests/test_main.py: W3 regression test migrated from save_state count to mutate_state count; 2 calls per run preserved
    - Phase 14 D-09 + D-13 + REVIEWS HIGH #1 invariants asserted: contention, release on success/failure, sequential calls, FULL READ-MODIFY-WRITE atomicity under concurrency, reentrancy, migration backfill, idempotency, value-preservation, None pass-through, round-trip
    - Existing test classes unchanged; full file pytest green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI process ↔ state.json | Plan 14-04 introduces this writer. Plan 14-02 adds the OS-level coordination primitive (fcntl.LOCK_EX) AND the `mutate_state` critical-section helper that makes the boundary safe under concurrent writers. |
| main.py daily run ↔ state.json | Existing v1.0 boundary (single writer). Plan 14-02 makes it cooperative with the new web writer by migrating the daily loop's save sites to `mutate_state`. |
| Operator state.json on disk ↔ migration code path | New: pre-Phase-14 v2 state files flow through `_migrate_v2_to_v3` on next load_state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | T (Tampering) — lost-update race across processes | state_manager mutations under cross-process contention | **mitigate (FULLY — was ACCEPTED RESIDUAL)** | **REVIEWS HIGH #1 fix:** `state_manager.mutate_state(mutator, path)` helper holds `fcntl.LOCK_EX` across the FULL load → mutate → save critical section. Both web routes (Plan 14-04) and the daily loop (this plan, main.py migration) call `mutate_state`. Verified by `TestMutateState.test_concurrent_writers_no_lost_update` (multiprocess subprocess test). The previous design (`fcntl` on save alone) admitted a stale-read race; the new design closes it. |
| T-14-02 | T (Tampering) — schema migration data loss | _migrate_v2_to_v3 walking existing droplet state.json | mitigate | Migration uses dict-spread `{**pos, 'manual_stop': pos.get('manual_stop')}` which preserves every existing key including manual_stop if already present (idempotent). TestSchemaMigrationV2ToV3.test_migration_preserves_existing_manual_stop locks this. Round-trip test (test_save_then_load_v3_round_trips) proves the on-disk file is recoverable. |
| T-14-03 | D (Denial of service) — lock starvation | fcntl.LOCK_EX with no timeout (D-13 blocking-indefinite) | accept | Daily save ~50ms, web POST ~10–100ms; mutate_state critical section ~150ms worst case. A 5-second timeout would expose web users to sporadic 503s during normal contention. Operationally: if lock exceeds 5s, something is fundamentally wrong (zombie process); operator detects via `lsof state.json` + kill of holder. The 1.5s upper bound in TestFcntlLock catches a regression where the lock isn't released after the holder finishes. |
| T-14-04 | I (Information disclosure) — manual_stop exposed in state.json | Position dicts now include manual_stop field | accept | manual_stop is operator-set; not a secret. State.json is operator-controlled and committed via Phase 10 INFRA-02 nightly push. No new disclosure surface. |
| T-14-05 | T (Tampering) — pre-Phase-14 code reading v3 state | Operator rolls back to pre-Phase-14 codebase after a v3 save | accept | Pre-Phase-14 _validate_loaded_state checks key presence only; pre-Phase-14 _migrate is no-op on schema_version=3 (loop `while version < 2` doesn't execute); manual_stop silently retained. No data loss; safer rollback documented in deferred-items.md. |
| T-14-06 | E (Elevation of privilege) — fcntl bypass | Attacker writes to state.json without acquiring the lock | accept | Lock is **advisory** — only cooperating writers respect it. main.py + web/routes/trades.py are the ONLY writers; both go through state_manager.mutate_state (sole entry to the locked critical section). Bypass requires malicious code execution on the droplet, out-of-scope per Phase 13 AuthMiddleware + Phase 12 nginx HTTPS gate. |

**ASVS L1 mapping:**
- V5 (Validation): `_migrate_v2_to_v3` walks user-supplied state.json; backfills None for missing field; existing _validate_loaded_state checks key presence post-migration. _validate_trade (Phase 3) gates the trade_log writer; manual_stop validation lives at Plan 14-04's Pydantic ModifyTradeRequest layer.
- V8 (Logging): No new emission in this plan; existing `[State] WARNING: state.json was corrupt` line on _backup_corrupt unchanged.
- V13 (API): Not applicable — I/O hex modification only.
</threat_model>

<verification>
After all three tasks complete:
- `pytest tests/test_state_manager.py -x -q` exits 0 (full file green; ~15 new tests across 3 new classes)
- `python -c "from system_params import STATE_SCHEMA_VERSION, Position; assert STATE_SCHEMA_VERSION == 3; assert 'manual_stop' in Position.__annotations__"` exits 0
- `python -c "from state_manager import _migrate_v2_to_v3, MIGRATIONS, mutate_state; assert 3 in MIGRATIONS; import inspect; from state_manager import _atomic_write; src = inspect.getsource(_atomic_write); assert 'fcntl.flock' in src and 'LOCK_EX' in src and 'LOCK_UN' in src; assert callable(mutate_state)"` exits 0
- `pytest tests/test_main.py -x -q` exits 0 (modulo documented weekend-skip failures); W3 regression migrated to mutate_state count
- Full suite still green: `pytest -x -q` exits 0 (modulo the 16 documented test_main.py weekend-skip failures)
- `git diff --stat state_manager.py system_params.py main.py tests/test_state_manager.py tests/test_main.py` confines changes to those five files
</verification>

<success_criteria>
- **REVIEWS HIGH #1 closed:** `mutate_state` exists, holds the lock across load → mutate → save, verified by `TestMutateState.test_concurrent_writers_no_lost_update` (cross-process subprocess test). T-14-01 moves from ACCEPTED RESIDUAL → FULLY MITIGATED.
- SC-3 dependency: Plan 14-04's close handler P&L math depends on _resolved_contracts being correctly rematerialized post-migration; this plan's TestLoadStateResolvesContracts regression coverage proves the dependency
- SC-6 dependency: TRADE-06 sole-writer invariant for state['warnings'] preserved (no Phase 14 module here writes to it)
- Cross-process safety: TestFcntlLock + TestMutateState prove the OS primitive works on macOS (CI dev) and Linux (droplet); the multiprocess fixtures are platform-agnostic
- Backward compatibility: an operator's existing v2 state.json on the droplet migrates transparently on first post-deploy load
- Phase 14 D-14 amendment to Phase 10 D-15 reflected in state_manager.py module docstring with explicit cross-reference
- W3 invariant preserved: main.py daily loop now calls mutate_state exactly twice per run
</success_criteria>

<output>
After completion, create `.planning/phases/14-trade-journal-mutation-endpoints/14-02-SUMMARY.md`. Required content:
- Files modified (state_manager.py, system_params.py, main.py, tests/test_state_manager.py, tests/test_main.py — line counts before/after)
- The new MIGRATIONS dict literal (proves the dispatch wiring)
- The new _atomic_write critical-section structure (proves lock placement around tempfile→replace)
- The new `mutate_state` helper signature + body (proves REVIEWS HIGH #1 fix shape)
- main.py save site count BEFORE vs AFTER (proves migration to mutate_state)
- TestFcntlLock + TestMutateState + TestSchemaMigrationV2ToV3 test counts (4 + 5 + 6 = 15 new tests)
- W3 regression test migration evidence: original test name + before/after assertion
- Multiprocess test elapsed time observations (proves the lock contention windows are reasonable)
- Any deviations from this plan with reasoning (Rule 1/2/3 logging)
</output>
