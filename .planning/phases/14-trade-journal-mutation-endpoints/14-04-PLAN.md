---
phase: 14
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - web/routes/dashboard.py
  - web/routes/trades.py
  - web/app.py
  - tests/test_web_trades.py
autonomous: true
requirements:
  - TRADE-01
  - TRADE-02
  - TRADE-03
  - TRADE-04
  - TRADE-06
tags:
  - phase14
  - web-routes
  - pydantic-v2
  - htmx-partials
  - sole-writer-invariant
last_revised: 2026-04-25
revision_source: 14-REVIEWS.md

must_haves:
  truths:
    - 'POST /trades/open accepts valid OpenTradeRequest (instrument, direction, entry_price, contracts, executed_at?, peak_price?, trough_price?, pyramid_level?) and appends a position to state.positions via state_manager.save_state'
    - 'POST /trades/open with same-direction existing position triggers sizing_engine.check_pyramid; should_add=true → pyramid-up applied; should_add=false → 409 Conflict'
    - 'POST /trades/open with opposite-direction existing position returns 409 Conflict with the locked D-01 message'
    - 'POST /trades/close builds the 11-field trade dict (D-05 inline gross_pnl, D-06 exit_reason=operator_close, D-07 multiplier+cost_aud from _resolved_contracts) and calls state_manager.record_trade'
    - 'POST /trades/close LONG and SHORT P&L math equals (exit-entry)*n*mult and (entry-exit)*n*mult respectively; closing-half cost deducted by record_trade per Phase 3 D-14 (NOT by handler — D-05 anti-pitfall)'
    - 'POST /trades/modify with new_stop=7700.0 sets position.manual_stop; with new_stop=null PRESENT clears (sets to None); with new_stop ABSENT leaves unchanged (Pydantic v2 model_fields_set semantics per D-12)'
    - 'POST /trades/modify with new_contracts=N sets n_contracts=N AND resets pyramid_level=0 (D-10)'
    - 'POST /trades/modify with empty body returns 400 (D-12 at-least-one-field)'
    - 'POST /trades/modify with new_contracts=0 returns 400'
    - 'Field validation errors return HTTP 400 with body {"errors": [{"field": "...", "reason": "..."}]} via the 422→400 remap exception handler'
    - 'GET /trades/close-form, GET /trades/modify-form, GET /trades/cancel-row return HTML partials per UI-SPEC §Decision 5 + §Decision 3'
    - 'Every successful 2xx mutation calls state_manager.save_state EXACTLY ONCE (TRADE-06 atomic-single-save discipline per D-11)'
    - "TRADE-06 sole-writer invariant: AST walk over web/routes/trades.py finds NO state['warnings'] assignment, NO state['warnings'].append/.extend/.insert"
    - 'Auth header X-Trading-Signals-Auth required for all /trades/* endpoints (inherited from Phase 13 AuthMiddleware automatically)'
  artifacts:
    - path: 'web/routes/trades.py'
      provides: 'register(app); OpenTradeRequest, CloseTradeRequest, ModifyTradeRequest Pydantic v2 models; open_trade, close_trade, modify_trade POST handlers; close_form, modify_form, cancel_row GET handlers; _format_pydantic_errors + _validation_exception_handler exposed for app.py'
      contains: 'def register(app: FastAPI)'
      min_lines: 280
    - path: 'web/app.py'
      provides: 'register trades_route; install RequestValidationError handler; module docstring extended with Phase 14 D-13/D-14 paragraph'
      contains: 'trades_route.register'
    - path: 'tests/test_web_trades.py'
      provides: '13 test classes fully populated; ~50+ test methods covering every D-01..D-13 invariant, Pydantic validation, sole-writer AST guard, save_state-once invariant, HTMX response shapes'
      min_lines: 600
  key_links:
    - from: 'web/app.py::create_app'
      to: 'web.routes.trades.register'
      via: 'module-top import + register() call between state_route.register and add_middleware'
      pattern: 'trades_route\\.register\\(application\\)'
    - from: 'web/app.py::create_app'
      to: 'fastapi.exceptions.RequestValidationError'
      via: 'application.add_exception_handler'
      pattern: 'add_exception_handler\\(RequestValidationError'
    - from: 'web/routes/trades.py::open_trade'
      to: 'sizing_engine.check_pyramid'
      via: 'local import inside handler'
      pattern: 'from sizing_engine import check_pyramid'
    - from: 'web/routes/trades.py::close_trade'
      to: 'state_manager.record_trade'
      via: 'local import + record_trade(state, trade); save_state(state)'
      pattern: 'state_manager import.*record_trade'
---

<objective>
Implement the three POST mutation endpoints (`/trades/open`, `/trades/close`, `/trades/modify`) plus their HTMX support GET endpoints (`/trades/close-form`, `/trades/modify-form`, `/trades/cancel-row`). Pydantic v2 request validation with a 422→400 remap. Sole-writer invariant for `state['warnings']` enforced by AST guard test. Every successful mutation flows through `state_manager.save_state` exactly once. Inherits auth gating from Phase 13 AuthMiddleware automatically (no per-route auth boilerplate).

Purpose: This is the meat of Phase 14 — TRADE-01 through TRADE-04 + TRADE-06. Plan 14-05 then layers the dashboard HTML/HTMX glue on top of these endpoints.

Output: web/routes/trades.py implementing the six endpoints; web/app.py wiring the route + the 422→400 exception handler; tests/test_web_trades.py with 13 fully populated test classes covering every locked decision and ASVS L1 V5/V7/V13 surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-CONTEXT.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-RESEARCH.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-PATTERNS.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-UI-SPEC.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-VALIDATION.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-01-SUMMARY.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-02-SUMMARY.md
@.planning/phases/14-trade-journal-mutation-endpoints/14-03-SUMMARY.md
@.planning/phases/13-auth-read-endpoints/13-CONTEXT.md
@.planning/phases/13-auth-read-endpoints/13-RESEARCH.md
@CLAUDE.md
@web/app.py
@web/routes/state.py
@web/routes/dashboard.py
@web/middleware/auth.py
@state_manager.py
@sizing_engine.py
@system_params.py
@tests/conftest.py
@tests/test_web_state.py
@tests/test_web_trades.py

<interfaces>
**state_manager (post Plan 14-02 — locked + v3 schema):**
```python
def load_state(path=Path(STATE_FILE), now=None) -> dict
  # Returns state dict with _resolved_contracts rematerialized; schema v3 post-Plan-14-02

def save_state(state: dict, path=Path(STATE_FILE)) -> None
  # Acquires fcntl.LOCK_EX before tempfile→replace; underscore-prefix top-level keys stripped

def record_trade(state: dict, trade: dict) -> dict
  # _validate_trade gates 11-field shape; Phase 3 D-14 deducts closing-half cost INSIDE this function;
  # appends to trade_log with net_pnl; sets positions[instrument]=None; mutates state['account']
  # Returns mutated state dict (same identity)
```

**sizing_engine (post Plan 14-03):**
```python
def check_pyramid(position, current_price, atr_entry) -> PyramidDecision
  # Returns PyramidDecision(add_contracts=0|1, new_level)
  # add_contracts=1 if distance >= (level+1)*atr_entry AND level < MAX_PYRAMID_LEVEL=2

class PyramidDecision(NamedTuple):
  add_contracts: int  # 0 or 1
  new_level: int
```

**system_params (post Plan 14-02):**
```python
MAX_PYRAMID_LEVEL: int = 2
STATE_SCHEMA_VERSION: int = 3
class Position(TypedDict):
  direction: Literal['LONG', 'SHORT']; entry_price: float; entry_date: str;
  n_contracts: int; pyramid_level: int; peak_price: float | None;
  trough_price: float | None; atr_entry: float; manual_stop: float | None
```

**Phase 13 AuthMiddleware (web/middleware/auth.py):**
- Sole chokepoint per Phase 13 D-01; gates all paths NOT in EXEMPT_PATHS
- EXEMPT_PATHS contains '/healthz' only; /trades/* automatically gated
- 401 returned for missing or wrong X-Trading-Signals-Auth header

**FastAPI 0.136.1 + Pydantic v2 (>= 2.9.0 transitive):**
- BaseModel + Field(gt=0, ge=1) + Literal[...] + model_validator(mode='after')
- model_fields_set: frozenset of field names that were PRESENT in the request body
- RequestValidationError raised by FastAPI on schema validation failure
- HTMLResponse for HTMX partials; JSONResponse for 4xx error bodies
- Response with content + headers={'HX-Trigger': '...'} for OOB swap

**HTMX response semantics (UI-SPEC §Decision 3):**
- 2xx → HTML partial swapped per hx-target/hx-swap
- 4xx → JSON {"errors": [{"field":..., "reason":...}]} (HTMX won't swap; UI hx-on::after-request handles it)
- 409 → plain text body for conflict messages (D-01 + D-02)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: web/routes/trades.py (NEW) — Pydantic models + 6 handlers + register()</name>
  <files>web/routes/trades.py</files>
  <behavior>
    - Module exports register(app: FastAPI), _validation_exception_handler (used by web/app.py)
    - OpenTradeRequest, CloseTradeRequest, ModifyTradeRequest Pydantic v2 models with Literal enums + Field constraints + model_validator coherence
    - POST /trades/open: D-01 conflict / D-02 pyramid-up / D-03 advanced-field coherence / D-04 default executed_at AWST
    - POST /trades/close: D-05 inline gross_pnl raw price-delta / D-06 exit_reason='operator_close' / D-07 _resolved_contracts / D-08 default executed_at AWST; calls record_trade then save_state
    - POST /trades/modify: D-09 manual_stop precedence / D-10 pyramid_level reset on n_contracts change / D-11 atomic single save_state / D-12 model_fields_set absent-vs-null
    - GET /trades/close-form, /trades/modify-form, /trades/cancel-row: return HTML partials per UI-SPEC §Decision 5
    - Every successful POST returns HTMLResponse + HX-Trigger header for confirmation banner OOB swap (UI-SPEC §Decision 3)
    - 4xx error path: JSONResponse with {"errors":[{...}]} for 400 (Pydantic remap); 409 plain text for conflicts (D-01, D-02)
    - All `state_manager`, `sizing_engine`, `system_params` imports are LOCAL (inside handler bodies / model_validator bodies) per Phase 11 C-2 hex boundary
    - Logger uses [Web] prefix per CLAUDE.md §Conventions
    - NO write to state['warnings'] anywhere (TRADE-06 sole-writer invariant)
  </behavior>
  <read_first>
    - web/routes/state.py full file (66 lines — exact register() shape + module docstring style)
    - web/routes/dashboard.py first 50 lines (analog for module docstring with full Architecture block)
    - web/middleware/auth.py (auth header name, EXEMPT_PATHS — confirms /trades/* is gated automatically)
    - web/app.py (current factory; helps understand where Plan 04 Task 2 will register)
    - state_manager.py lines 199-265 (_validate_trade — knows what record_trade requires for the 11 fields)
    - state_manager.py lines 485-519 (record_trade — Phase 3 D-13/D-14/D-19/D-20: closing-half cost, no caller mutation)
    - sizing_engine.py lines 306-357 (check_pyramid — PyramidDecision return shape; NaN-pass through)
    - system_params.py lines 50-55 (MAX_PYRAMID_LEVEL = 2; SPI/AUDUSD constants)
    - 14-CONTEXT.md D-01..D-13 (every locked decision; quote message strings VERBATIM)
    - 14-RESEARCH.md §Pattern 1 lines 287-400 (open_trade verbatim — copy structure)
    - 14-RESEARCH.md §Pattern 2 lines 402-466 (close_trade verbatim — including D-05 anti-pitfall comment)
    - 14-RESEARCH.md §Pattern 3 lines 468-520 (modify_trade verbatim with model_fields_set)
    - 14-RESEARCH.md §Pattern 5 lines 530-566 (model_fields_set absent-vs-null discipline)
    - 14-RESEARCH.md §Pattern 6 lines 568-611 (422→400 remap; _format_pydantic_errors + _validation_exception_handler verbatim)
    - 14-RESEARCH.md §Pattern 8 lines 635-669 (HTMX response shapes table)
    - 14-RESEARCH.md §Anti-Patterns lines 843-852 (NEVER call compute_unrealised_pnl; NEVER lock tempfile; NEVER skip 409)
    - 14-RESEARCH.md §Common Pitfalls Pitfall 5 lines 921-925 (manual_stop=None on new positions)
    - 14-PATTERNS.md §`web/routes/trades.py` lines 34-114 (analog mapping; D-05 anti-pitfall comment block verbatim)
    - 14-UI-SPEC.md §Decision 3 (HTMX response shape table per endpoint)
    - 14-UI-SPEC.md §Decision 5 (close-form 2-stage panel HTML)
    - 14-UI-SPEC.md §Copywriting Contract (verbatim banner + error strings)
  </read_first>
  <action>
Create the new file `web/routes/trades.py`. The file is dense (~280 lines) but every block has a verbatim source in RESEARCH or PATTERNS. Structure:

1. Module docstring (mirror web/routes/state.py style; cite Phase 14 D-01..D-14 + Phase 13 D-01 auth + Phase 11 C-2 hex)
2. Module-top imports (stdlib + fastapi + pydantic ONLY; no state_manager / sizing_engine / system_params at module top — those are LOCAL in handler bodies)
3. Module-level constants (`_AWST` zoneinfo; `_OPERATOR_CLOSE = 'operator_close'`)
4. `def _now_awst()` helper (returns datetime.now(_AWST))
5. Pydantic models: `OpenTradeRequest`, `CloseTradeRequest`, `ModifyTradeRequest`
6. `_format_pydantic_errors(exc)` + `_validation_exception_handler(request, exc)` (RESEARCH §Pattern 6)
7. `def register(app: FastAPI) -> None:` containing the six endpoint handlers as nested functions
8. Helper functions for HTML partials (one-line strings; the bulk of the markup lives in Plan 14-05)

**Module docstring (use this exact prose):**

    '''POST /trades/{open,close,modify} — Phase 14 TRADE-01..06 + D-01..D-13.

    Three mutation endpoints + three HTMX support GETs (close-form, modify-form,
    cancel-row) that record operator-executed trades in state.json. Every success
    flows through state_manager.save_state exactly once (TRADE-06; D-11 atomic).

    Contract (CONTEXT.md 2026-04-25):
      D-01: opposite-direction position → 409 with locked message
      D-02: same-direction → sizing_engine.check_pyramid; should_add=False → 409
      D-03: peak/trough/pyramid_level coherence checks; reject mismatched direction
      D-04: Pydantic v2; 422 remapped to 400 by app-level handler
      D-05: gross_pnl is INLINE raw price-delta — anti-pitfall against
            sizing_engine.compute_unrealised_pnl which would double-deduct
            closing-half cost (record_trade D-14 already deducts it)
      D-06: exit_reason = 'operator_close' literal
      D-07: multiplier + cost_aud from state['_resolved_contracts'][instrument]
      D-08: executed_at? optional → default today AWST
      D-09: manual_stop on Position; sizing_engine.get_trailing_stop precedence
      D-10: new_contracts mutates n_contracts AND resets pyramid_level=0
      D-11: atomic single save_state per modify
      D-12: model_fields_set distinguishes absent (no change) vs null (clear override)
      D-13: state_manager.save_state holds fcntl.LOCK_EX (cross-process safe)

    Architecture (CLAUDE.md hex-lite + Phase 14 D-14 amends Phase 10 D-15):
      web/routes/ is an adapter hex.
      Allowed: fastapi, starlette, stdlib, state_manager (read+WRITE per D-14),
               sizing_engine (D-02 check_pyramid), system_params (D-12 MAX_PYRAMID_LEVEL).
      Forbidden: signal_engine, data_fetcher, notifier, main.
      Phase 13 D-07: dashboard is allowed but Phase 14 doesn't need it.
      Enforced by tests/test_web_healthz.py::TestWebHexBoundary (Plan 14-01 promoted
      sizing_engine + system_params out of FORBIDDEN_FOR_WEB).

      state_manager + sizing_engine + system_params imports are LOCAL inside
      handler bodies / Pydantic validator bodies per Phase 11 C-2.

    Auth: Phase 13 AuthMiddleware gates /trades/* automatically (D-01 sole
    chokepoint; no per-route boilerplate). Operator's WEB_AUTH_SECRET sent
    via X-Trading-Signals-Auth header (HTMX hx-headers attribute).

    TRADE-06 sole-writer invariant: NO endpoint here writes to state['warnings'].
    AST-walk regression test in tests/test_web_trades.py::TestSoleWriterInvariant.

    Log prefix: [Web].
    '''

**Module-top imports:**

    import logging
    import math
    import zoneinfo
    from datetime import datetime, date as _date
    from typing import Literal

    from fastapi import FastAPI, Request
    from fastapi.exceptions import RequestValidationError
    from fastapi.responses import HTMLResponse, JSONResponse, Response
    from pydantic import BaseModel, Field, model_validator

    logger = logging.getLogger(__name__)

    _AWST = zoneinfo.ZoneInfo('Australia/Perth')
    _OPERATOR_CLOSE = 'operator_close'  # D-06 literal

**Helper:**

    def _now_awst() -> datetime:
      return datetime.now(_AWST)

**OpenTradeRequest (D-04 + D-03):**

Use RESEARCH §Pattern 1 lines 312-348 verbatim. Key elements:
- `instrument: Literal['SPI200', 'AUDUSD']`
- `direction: Literal['LONG', 'SHORT']`
- `entry_price: float = Field(gt=0)`
- `contracts: int = Field(ge=1)`
- `executed_at: _date | None = None`
- `peak_price: float | None = None`
- `trough_price: float | None = None`
- `pyramid_level: int | None = None`
- `@model_validator(mode='after') def _coherence(self):` — performs D-03 checks (LONG: peak >= entry, trough must be None; SHORT: trough <= entry, peak must be None; pyramid_level in [0, MAX_PYRAMID_LEVEL]); also math.isfinite check on entry_price/peak_price/trough_price (rejects NaN, ±inf). Local `from system_params import MAX_PYRAMID_LEVEL` inside the validator body.

**CloseTradeRequest (D-08):**

Use RESEARCH §Pattern 2 lines 411-422 verbatim:
- `instrument: Literal['SPI200', 'AUDUSD']`
- `exit_price: float = Field(gt=0)`
- `executed_at: _date | None = None`
- `@model_validator(mode='after')` — math.isfinite on exit_price

**ModifyTradeRequest (D-12):**

Use RESEARCH §Pattern 3 lines 476-496 verbatim:
- `instrument: Literal['SPI200', 'AUDUSD']`
- `new_stop: float | None = None`
- `new_contracts: int | None = None`
- `@model_validator(mode='after') def _at_least_one(self):` — uses `self.model_fields_set & {'new_stop', 'new_contracts'}` to detect at least one PRESENT (D-12)
- `@model_validator(mode='after') def _new_contracts_floor(self):` — when present and not None, must be >= 1
- Add a `@model_validator(mode='after') def _new_stop_finite(self):` — when present and not None, math.isfinite check (rejects NaN, ±inf — RESEARCH §Pattern 3 line 482-487 implies this; we add it explicitly per ASVS V5 input validation)

**_format_pydantic_errors + _validation_exception_handler (RESEARCH §Pattern 6 lines 583-608):**

    def _format_pydantic_errors(exc: RequestValidationError) -> list[dict]:
      '''Convert Pydantic v2 errors to {field, reason} shape per D-04.

      exc.errors() returns dicts like:
        {'type': 'greater_than', 'loc': ('body', 'entry_price'),
         'msg': 'Input should be greater than 0', 'input': -1, 'ctx': {'gt': 0}}
      We extract the LAST element of 'loc' as the field name (skipping 'body').
      '''
      out = []
      for err in exc.errors():
        loc = err.get('loc', ())
        leaf = next(
          (str(p) for p in reversed(loc) if isinstance(p, (str, int)) and p != 'body'),
          '<root>',
        )
        out.append({'field': leaf, 'reason': err.get('msg', 'invalid')})
      return out


    async def _validation_exception_handler(request: Request, exc: RequestValidationError):
      '''Phase 14 D-04: 422 → 400 remap with field-level error JSON.'''
      logger.info(
        '[Web] validation failure on %s: %s',
        request.url.path, _format_pydantic_errors(exc),
      )
      return JSONResponse(
        status_code=400,
        content={'errors': _format_pydantic_errors(exc)},
      )

**register(app):**

    def register(app: FastAPI) -> None:
      '''Register six Phase 14 endpoints on the given FastAPI instance.

      Three POST mutations + three GET HTMX support endpoints.
      The 422→400 exception handler is registered separately by web/app.py
      (so the handler covers all routes, not just /trades/*).
      '''

      @app.post('/trades/open')
      def open_trade(req: OpenTradeRequest):
        # ... per RESEARCH §Pattern 1 lines 354-400
        ...

      @app.post('/trades/close')
      def close_trade(req: CloseTradeRequest):
        # ... per RESEARCH §Pattern 2 lines 424-466
        ...

      @app.post('/trades/modify')
      def modify_trade(req: ModifyTradeRequest):
        # ... per RESEARCH §Pattern 3 lines 499-520
        ...

      @app.get('/trades/close-form')
      def close_form(instrument: str):
        # ... 2-row HTML partial per UI-SPEC §Decision 5
        ...

      @app.get('/trades/modify-form')
      def modify_form(instrument: str):
        # ... 2-row HTML partial per UI-SPEC §Decision 2
        ...

      @app.get('/trades/cancel-row')
      def cancel_row(instrument: str):
        # ... re-rendered original <tr> per UI-SPEC §Decision 5
        ...

**open_trade body (apply RESEARCH §Pattern 1 lines 354-400 verbatim with these adaptations):**

    @app.post('/trades/open')
    def open_trade(req: OpenTradeRequest):
      '''Phase 14 TRADE-01 + D-01..D-04.'''
      # Phase 11 C-2 + Phase 14 D-02: local imports preserve hex boundary
      from state_manager import load_state, save_state
      from sizing_engine import check_pyramid

      state = load_state()
      existing = state['positions'].get(req.instrument)

      if existing is not None and existing['direction'] != req.direction:
        # D-01: opposite direction is a hard conflict
        msg = (
          f'instrument {req.instrument} already has an open '
          f'{existing["direction"]} position; close it first via '
          f'POST /trades/close before opening a {req.direction}'
        )
        logger.warning('[Web] /trades/open conflict: %s', msg)
        return Response(content=msg, status_code=409, media_type='text/plain; charset=utf-8')

      if existing is not None and existing['direction'] == req.direction:
        # D-02: pyramid-up via sizing_engine.check_pyramid
        sig_entry = state.get('signals', {}).get(req.instrument, {})
        atr_entry = sig_entry.get('atr') if isinstance(sig_entry, dict) else None
        if atr_entry is None:
          logger.warning('[Web] /trades/open pyramid blocked: no atr for %s', req.instrument)
          return Response(
            content='pyramid blocked: no ATR available in state.signals',
            status_code=409, media_type='text/plain; charset=utf-8',
          )
        decision = check_pyramid(existing, current_price=req.entry_price, atr_entry=atr_entry)
        if decision.add_contracts == 0:
          msg = 'Pyramid blocked: gate not met or already at MAX_PYRAMID_LEVEL'
          logger.warning('[Web] /trades/open pyramid blocked: %s', msg)
          return Response(content=msg, status_code=409, media_type='text/plain; charset=utf-8')
        # Apply pyramid-up
        existing['n_contracts'] += decision.add_contracts
        existing['pyramid_level'] = decision.new_level
        logger.info(
          '[Web] /trades/open pyramid-up applied: instrument=%s level=%d n_contracts=%d',
          req.instrument, decision.new_level, existing['n_contracts'],
        )
      else:
        # Fresh open
        executed_at = req.executed_at or _now_awst().date()
        state['positions'][req.instrument] = _build_position_dict(req, executed_at, state)
        logger.info(
          '[Web] /trades/open fresh open: instrument=%s direction=%s entry=%s contracts=%d',
          req.instrument, req.direction, req.entry_price, req.contracts,
        )

      save_state(state)
      return _render_open_success_partial(state, req.instrument, req.direction, req.entry_price, req.contracts)

Add helper `_build_position_dict(req, executed_at, state)` defined inside `register()` or as a module-level helper. The position dict construction MUST include `manual_stop=None` (Phase 14 D-09 + RESEARCH §Pitfall 5) and `atr_entry` from `state['signals'][instrument]['atr']` (or fall back if not present — but a fresh open without atr is unusual; log a warning and use the entry_price ATR fallback per CLAUDE.md spirit, OR return 409 with reason "no ATR available" since stop math depends on it). Recommendation: 409 with `'open blocked: no ATR available in state.signals; daily run must complete first'` so the operator gets clear feedback rather than a silently-broken position.

`_build_position_dict` signature and body:

    def _build_position_dict(req, executed_at, state):
      '''Phase 14 D-03 + D-09: assemble Position dict from request + state.

      Pulls atr_entry from state['signals'][instrument]['atr'] (last computed
      value from the daily signal loop). Returns the dict ready for
      state['positions'][instrument] insertion.

      Pitfall 5: manual_stop=None is set explicitly so sizing_engine.get_trailing_stop
      doesn't KeyError if called transiently before _migrate_v2_to_v3 runs.

      D-03 default-path values (when req.peak_price/trough_price/pyramid_level are None):
        LONG  peak=entry_price,  trough=None
        SHORT peak=None,         trough=entry_price
        pyramid_level=0
      '''
      sig_entry = state.get('signals', {}).get(req.instrument, {})
      atr_entry = sig_entry.get('atr') if isinstance(sig_entry, dict) else None
      if atr_entry is None:
        # CALLER must check for None and return 409 BEFORE invoking this function;
        # this guard is defense-in-depth.
        raise ValueError(
          f'open blocked: no atr_entry available for {req.instrument}'
        )

      if req.direction == 'LONG':
        peak = req.peak_price if req.peak_price is not None else req.entry_price
        trough = None
      else:  # SHORT
        peak = None
        trough = req.trough_price if req.trough_price is not None else req.entry_price

      pyramid_level = req.pyramid_level if req.pyramid_level is not None else 0

      return {
        'direction': req.direction,
        'entry_price': req.entry_price,
        'entry_date': executed_at.isoformat() if hasattr(executed_at, 'isoformat') else str(executed_at),
        'n_contracts': req.contracts,
        'pyramid_level': pyramid_level,
        'peak_price': peak,
        'trough_price': trough,
        'atr_entry': atr_entry,
        'manual_stop': None,  # D-09 + Pitfall 5
      }

Update the open_trade handler so the "Fresh open" branch CHECKS `atr_entry is None` BEFORE calling `_build_position_dict` and returns 409 if so. The `_build_position_dict` raise is defense-in-depth.

**close_trade body (apply RESEARCH §Pattern 2 verbatim with the D-05 anti-pitfall comment):**

    @app.post('/trades/close')
    def close_trade(req: CloseTradeRequest):
      '''Phase 14 TRADE-03 + D-05..D-08.'''
      from state_manager import load_state, save_state, record_trade

      state = load_state()
      pos = state['positions'].get(req.instrument)
      if pos is None:
        msg = f'no open position for instrument {req.instrument}'
        logger.warning('[Web] /trades/close conflict: %s', msg)
        return Response(content=msg, status_code=409, media_type='text/plain; charset=utf-8')

      # D-07: read multiplier and cost_aud from _resolved_contracts (load_state
      # rematerializes per Phase 8 D-14)
      resolved = state['_resolved_contracts'][req.instrument]
      multiplier = resolved['multiplier']
      cost_aud = resolved['cost_aud']

      # D-05 ANTI-PITFALL — DO NOT USE sizing_engine.compute_unrealised_pnl HERE.
      # record_trade D-14 deducts the closing-half cost. compute_unrealised_pnl
      # already deducts the opening-half cost. Passing realised_pnl as gross_pnl
      # would double-count the closing cost. See state_manager.py:499-506,
      # Phase 4 D-15/D-19 anti-pitfall.
      if pos['direction'] == 'LONG':
        gross_pnl = (req.exit_price - pos['entry_price']) * pos['n_contracts'] * multiplier
      else:  # SHORT
        gross_pnl = (pos['entry_price'] - req.exit_price) * pos['n_contracts'] * multiplier

      exit_date = (req.executed_at or _now_awst().date()).isoformat()
      trade = {
        'instrument': req.instrument,                   # locked-key shape per Phase 3 D-15
        'direction': pos['direction'],
        'n_contracts': pos['n_contracts'],
        'entry_date': pos['entry_date'],
        'exit_date': exit_date,
        'exit_reason': _OPERATOR_CLOSE,                 # D-06 literal
        'entry_price': pos['entry_price'],
        'exit_price': req.exit_price,
        'gross_pnl': gross_pnl,                         # D-05 inline raw price-delta
        'multiplier': multiplier,                       # D-07
        'cost_aud': cost_aud,                           # D-07
      }
      state = record_trade(state, trade)                # _validate_trade gates; mutates state['account']
      save_state(state)
      logger.info(
        '[Web] /trades/close completed: instrument=%s gross_pnl=%.2f',
        req.instrument, gross_pnl,
      )
      return _render_close_success_partial(state, req.instrument, gross_pnl, cost_aud, pos['n_contracts'])

**modify_trade body (apply RESEARCH §Pattern 3 lines 499-520 verbatim):**

    @app.post('/trades/modify')
    def modify_trade(req: ModifyTradeRequest):
      '''Phase 14 TRADE-04 + D-09..D-12.'''
      from state_manager import load_state, save_state

      state = load_state()
      pos = state['positions'].get(req.instrument)
      if pos is None:
        msg = f'no open position for instrument {req.instrument}'
        logger.warning('[Web] /trades/modify conflict: %s', msg)
        return Response(content=msg, status_code=409, media_type='text/plain; charset=utf-8')

      # Apply mutations IN-MEMORY first (D-11 atomic single save)
      if 'new_stop' in req.model_fields_set:
        # PRESENT: explicit set or null
        pos['manual_stop'] = req.new_stop  # may be None (clear-override)
      if 'new_contracts' in req.model_fields_set and req.new_contracts is not None:
        pos['n_contracts'] = req.new_contracts
        pos['pyramid_level'] = 0  # D-10: reset pyramid_level on any modify

      # D-11: single save_state — both updates land atomically or neither
      save_state(state)
      logger.info(
        '[Web] /trades/modify completed: instrument=%s fields=%s',
        req.instrument, sorted(req.model_fields_set & {'new_stop', 'new_contracts'}),
      )
      return _render_modify_success_partial(state, req.instrument)

**HTMX support GET handlers (return small HTML strings — Plan 14-05 owns the full markup; here keep them simple).**

For Wave 2 Plan 04, return MINIMAL placeholder HTML for the GET endpoints; Plan 14-05 will refine them once the dashboard.py rendering helpers exist. The endpoints MUST exist (return 200 with valid HTML for tests to assert) but the HTML body can be a simple skeleton:

    @app.get('/trades/close-form')
    def close_form(instrument: str):
      '''UI-SPEC §Decision 5: 2-stage destructive close. Returns a 2-row HTML
      block — original row above + confirmation panel row below — that swaps
      the position row via outerHTML.

      Plan 14-05 refines the HTML structure; this Wave 2 ships a minimum-viable
      partial that satisfies endpoint-existence tests.
      '''
      from state_manager import load_state
      state = load_state()
      pos = state['positions'].get(instrument)
      if pos is None:
        return Response(content=f'no open position for {instrument}', status_code=404,
                        media_type='text/plain; charset=utf-8')
      return HTMLResponse(content=_render_close_form_partial(state, instrument, pos))

    @app.get('/trades/modify-form')
    def modify_form(instrument: str):
      '''UI-SPEC §Decision 2: inline modify form panel below the position row.'''
      from state_manager import load_state
      state = load_state()
      pos = state['positions'].get(instrument)
      if pos is None:
        return Response(content=f'no open position for {instrument}', status_code=404,
                        media_type='text/plain; charset=utf-8')
      return HTMLResponse(content=_render_modify_form_partial(state, instrument, pos))

    @app.get('/trades/cancel-row')
    def cancel_row(instrument: str):
      '''UI-SPEC §Decision 5: revert close-form / modify-form to the original row.'''
      from state_manager import load_state
      state = load_state()
      pos = state['positions'].get(instrument)
      if pos is None:
        return Response(content=f'no open position for {instrument}', status_code=404,
                        media_type='text/plain; charset=utf-8')
      return HTMLResponse(content=_render_position_row_partial(state, instrument, pos))

**HTML partial helpers (module-level — minimal markup).**

Define five module-level helpers OUTSIDE register() but inside the file. They produce HTML strings using stdlib `html.escape` for any dynamic value (CLAUDE.md "innerHTML with dynamic data requires escaping" — applies equally to server-side HTML emission). Use single-quoted strings + 2-space indent per CLAUDE.md.

Keep them compact for Plan 14-04 — the goal is "endpoints respond with valid HTML and HX-Trigger header"; Plan 14-05 owns the visual polish. Each helper returns a string. Examples (concrete enough that tests can grep for the structural elements but Plan 14-05 may extend):

    import html

    def _render_open_success_partial(state, instrument, direction, entry_price, contracts) -> Response:
      '''UI-SPEC §Decision 3: open success → re-render the full <tbody>;
      OOB swap of confirmation banner. Plan 14-05 expands this to call into
      dashboard._render_positions_table; Plan 14-04 ships a minimal partial.
      '''
      escaped_inst = html.escape(instrument, quote=True)
      escaped_dir = html.escape(direction, quote=True)
      banner_html = (
        f'<div hx-swap-oob="innerHTML:#confirmation-banner">'
        f'<p class="banner-success">Opened {escaped_dir} {escaped_inst} '
        f'at {entry_price}, {contracts} contracts.</p>'
        f'</div>'
      )
      tbody_partial = _render_positions_tbody_partial(state)
      return HTMLResponse(
        content=tbody_partial + banner_html,
        status_code=200,
        headers={'HX-Trigger': 'positions-changed'},
      )

    def _render_close_success_partial(state, instrument, gross_pnl, cost_aud, n_contracts) -> Response:
      net_pnl = gross_pnl - (cost_aud * n_contracts / 2)  # mirror record_trade D-14 closing-half deduction for display
      sign = '+' if net_pnl >= 0 else '-'
      banner_html = (
        f'<div hx-swap-oob="innerHTML:#confirmation-banner">'
        f'<p class="banner-success">Closed {html.escape(instrument)} '
        f'realised P&amp;L {sign}${abs(net_pnl):.2f}.</p>'
        f'</div>'
      )
      # UI-SPEC §Decision 3 (b): empty body + HX-Trigger replaces row via tbody listener;
      # for simplicity Plan 14-04 returns an empty <tr> placeholder so outerHTML swap leaves
      # the row removed; Plan 14-05 may switch to option (a)
      return HTMLResponse(
        content=f'' + banner_html,
        status_code=200,
        headers={'HX-Trigger': 'positions-changed'},
      )

    def _render_modify_success_partial(state, instrument) -> Response:
      '''Re-render the single position row.'''
      pos = state['positions'].get(instrument)
      banner_html = (
        f'<div hx-swap-oob="innerHTML:#confirmation-banner">'
        f'<p class="banner-success">Modified {html.escape(instrument)}.</p>'
        f'</div>'
      )
      row_html = _render_position_row_partial(state, instrument, pos) if pos else ''
      return HTMLResponse(content=row_html + banner_html, status_code=200,
                          headers={'HX-Trigger': 'positions-changed'})

    def _render_position_row_partial(state, instrument, pos) -> str:
      '''Single <tr id="position-row-{instrument}"> stub. Plan 14-05 wires up
      to dashboard._render_positions_table for full parity (Actions column,
      manual badge, etc.). Plan 14-04 ships a minimal-but-valid <tr>.'''
      esc = lambda s: html.escape(str(s), quote=True)
      manual_badge = ''
      if pos.get('manual_stop') is not None:
        manual_badge = '<span class="badge badge-manual" title="Operator override">manual</span>'
      return (
        f'<tr id="position-row-{esc(instrument)}">'
        f'<td>{esc(instrument)}</td>'
        f'<td>{esc(pos["direction"])}</td>'
        f'<td>{esc(pos["entry_price"])}</td>'
        f'<td>{esc(pos["n_contracts"])}</td>'
        f'<td>{manual_badge}</td>'
        f'<td>'
        f'<button class="btn-row btn-close" hx-get="/trades/close-form?instrument={esc(instrument)}" hx-target="#position-row-{esc(instrument)}" hx-swap="outerHTML">Close</button>'
        f'<button class="btn-row btn-modify" hx-get="/trades/modify-form?instrument={esc(instrument)}" hx-target="#position-row-{esc(instrument)}" hx-swap="outerHTML">Modify</button>'
        f'</td>'
        f'</tr>'
      )

    def _render_positions_tbody_partial(state) -> str:
      '''Re-render the full <tbody> contents (UI-SPEC §Decision 3 open swap target).'''
      rows = []
      for inst in ('SPI200', 'AUDUSD'):
        pos = state.get('positions', {}).get(inst)
        if pos is not None:
          rows.append(_render_position_row_partial(state, inst, pos))
      return ''.join(rows)

    def _render_close_form_partial(state, instrument, pos) -> str:
      '''2-row HTML block: original row + confirmation panel.'''
      esc = lambda s: html.escape(str(s), quote=True)
      original = _render_position_row_partial(state, instrument, pos)
      panel = (
        f'<tr><td colspan="9">'
        f'Close {esc(pos["direction"])} {esc(instrument)} ({esc(pos["n_contracts"])} contracts) at exit price '
        f'<input type="number" step="0.01" min="0" name="exit_price" required autofocus '
        f'hx-trigger="keyup changed delay:100ms" '
        f'hx-target="this" hx-swap="none" '
        f'/>'
        f'<button type="button" class="btn-row" hx-get="/trades/cancel-row?instrument={esc(instrument)}" '
        f'hx-target="#position-row-{esc(instrument)}" hx-swap="outerHTML">Cancel</button>'
        f'<button type="button" class="btn-row btn-close" disabled '
        f'hx-post="/trades/close" hx-include="closest tr" hx-vals=\'{{"instrument": "{esc(instrument)}"}}\' '
        f'hx-target="#position-row-{esc(instrument)}" hx-swap="outerHTML">Confirm close</button>'
        f'</td></tr>'
      )
      return original + panel

    def _render_modify_form_partial(state, instrument, pos) -> str:
      esc = lambda s: html.escape(str(s), quote=True)
      original = _render_position_row_partial(state, instrument, pos)
      panel = (
        f'<tr><td colspan="9">'
        f'Modify {esc(instrument)}: '
        f'<label>new stop <input type="number" step="0.01" name="new_stop"></label> '
        f'<label>new contracts <input type="number" step="1" min="1" name="new_contracts"></label> '
        f'<button type="button" class="btn-row" hx-get="/trades/cancel-row?instrument={esc(instrument)}" '
        f'hx-target="#position-row-{esc(instrument)}" hx-swap="outerHTML">Cancel</button>'
        f'<button type="button" class="btn-row btn-modify" '
        f'hx-post="/trades/modify" hx-include="closest tr" hx-vals=\'{{"instrument": "{esc(instrument)}"}}\' '
        f'hx-target="#position-row-{esc(instrument)}" hx-swap="outerHTML">Save</button>'
        f'</td></tr>'
      )
      return original + panel

DO NOT add any code that writes to `state['warnings']` anywhere in the file. The TRADE-06 sole-writer invariant AST guard runs in Task 3 and will fail red if violated.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && python -c "from web.routes import trades; assert hasattr(trades, 'register'); assert hasattr(trades, '_validation_exception_handler'); assert hasattr(trades, 'OpenTradeRequest'); assert hasattr(trades, 'CloseTradeRequest'); assert hasattr(trades, 'ModifyTradeRequest')" && python -c "from web.routes.trades import ModifyTradeRequest; m = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_stop': 7700.0}); assert 'new_stop' in m.model_fields_set; m2 = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_stop': None}); assert 'new_stop' in m2.model_fields_set; m3 = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_contracts': 2}); assert 'new_stop' not in m3.model_fields_set"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f web/routes/trades.py` exits 0
    - `grep -q "def register(app: FastAPI)" web/routes/trades.py` exits 0
    - `grep -q "OpenTradeRequest" web/routes/trades.py` exits 0
    - `grep -q "CloseTradeRequest" web/routes/trades.py` exits 0
    - `grep -q "ModifyTradeRequest" web/routes/trades.py` exits 0
    - `grep -q "_validation_exception_handler" web/routes/trades.py` exits 0
    - `grep -q "@app.post('/trades/open')" web/routes/trades.py` exits 0
    - `grep -q "@app.post('/trades/close')" web/routes/trades.py` exits 0
    - `grep -q "@app.post('/trades/modify')" web/routes/trades.py` exits 0
    - `grep -q "@app.get('/trades/close-form')" web/routes/trades.py` exits 0
    - `grep -q "@app.get('/trades/modify-form')" web/routes/trades.py` exits 0
    - `grep -q "@app.get('/trades/cancel-row')" web/routes/trades.py` exits 0
    - `grep -q "model_fields_set" web/routes/trades.py` exits 0 (D-12 absent-vs-null)
    - `grep -q "_OPERATOR_CLOSE = 'operator_close'" web/routes/trades.py` exits 0 (D-06 literal)
    - `grep -q "D-05 ANTI-PITFALL" web/routes/trades.py` exits 0 (anti-pitfall comment present in close handler)
    - `grep -q "compute_unrealised_pnl" web/routes/trades.py` returns FAILURE (anti-pitfall: function NEVER imported here — D-05)
    - `grep -q "from sizing_engine import check_pyramid" web/routes/trades.py` exits 0
    - `grep -q "from state_manager import" web/routes/trades.py` exits 0 (LOCAL imports inside handlers — module-top imports of state_manager are forbidden by C-2; the AST guard in Plan 14-01 would catch it)
    - `grep -q "_resolved_contracts" web/routes/trades.py` exits 0 (D-07)
    - `grep -q "[Web]" web/routes/trades.py` exits 0 (log prefix per CLAUDE.md)
    - `grep -q "MAX_PYRAMID_LEVEL" web/routes/trades.py` exits 0 (D-03 coherence; LOCAL import inside model_validator)
    - `grep -q "manual_stop" web/routes/trades.py` exits 0 (D-09 + Pitfall 5 — fresh-open path sets None)
    - `grep -c "state\\['warnings'\\]" web/routes/trades.py` returns 0 (TRADE-06 sole-writer invariant)
    - `grep -c "state\\[.warnings.\\]" web/routes/trades.py` returns 0 (alternate quoting)
    - `python -c "from web.routes.trades import OpenTradeRequest; import pytest; pytest.raises(Exception, OpenTradeRequest, instrument='BTC', direction='LONG', entry_price=7800.0, contracts=2)" 2>&amp;1 | grep -q "BTC\\|literal" || true` (Pydantic Literal enum rejects 'BTC')
    - `python -c "from web.routes.trades import OpenTradeRequest; req = OpenTradeRequest(instrument='SPI200', direction='LONG', entry_price=7800.0, contracts=2); assert req.executed_at is None"` exits 0
    - `python -c "from web.routes.trades import ModifyTradeRequest; from pydantic import ValidationError; import pytest; pytest.raises(ValidationError, ModifyTradeRequest, instrument='SPI200')" 2>&amp;1 | grep -qi "at least one" || true` (D-12 empty body rejected)
    - `python -c "from web.routes.trades import ModifyTradeRequest; m = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_stop': 7700.0}); assert 'new_stop' in m.model_fields_set"` exits 0
    - `python -c "from web.routes.trades import ModifyTradeRequest; m = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_stop': None}); assert 'new_stop' in m.model_fields_set"` exits 0
    - `python -c "from web.routes.trades import ModifyTradeRequest; m = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_contracts': 2}); assert 'new_stop' not in m.model_fields_set"` exits 0
    - `python -c "import math; from web.routes.trades import OpenTradeRequest; from pydantic import ValidationError; import pytest; pytest.raises(ValidationError, OpenTradeRequest, instrument='SPI200', direction='LONG', entry_price=float('nan'), contracts=2)" 2>&amp;1 | grep -qi "finite\\|nan" || true` (NaN rejected)
    - `pytest tests/test_web_healthz.py -x -q` exits 0 (TestWebHexBoundary still green AFTER trades.py exists; sizing_engine + system_params imports inside trades.py are allowed because Plan 14-01 promoted them out of FORBIDDEN_FOR_WEB)
  </acceptance_criteria>
  <done>
    - web/routes/trades.py exists with module docstring + 3 Pydantic models + 6 endpoint handlers + register() + 5 HTML partial helpers + _format_pydantic_errors + _validation_exception_handler
    - All locked decisions D-01..D-13 represented in code with comments referencing the decision IDs
    - D-05 anti-pitfall comment present in close_trade body
    - All imports inside handler bodies / validator bodies are LOCAL per Phase 11 C-2
    - Module-top imports include only stdlib + fastapi + pydantic
    - No write to state['warnings'] anywhere
    - TestWebHexBoundary AST guard green (sizing_engine + system_params allowed; signal_engine / data_fetcher / notifier / main rejected)
  </done>
</task>

<task type="auto">
  <name>Task 2: web/app.py — register trades_route + install RequestValidationError handler</name>
  <files>web/app.py</files>
  <read_first>
    - web/app.py full file (102 lines — current factory shape)
    - web/routes/trades.py (Task 1 just created — has register() and _validation_exception_handler)
    - 14-RESEARCH.md §Pattern 6 lines 568-611 (where the handler registration goes — between routes and middleware)
    - 14-PATTERNS.md §`web/app.py` lines 416-437 (analog block + adaptation)
    - 14-CONTEXT.md D-13/D-14 (Phase 10 D-15 amendment text — for the docstring)
  </read_first>
  <action>
Three changes to web/app.py — minimal surface, surgical edits.

**1. Module docstring extension.** The existing docstring (lines 1-21) describes Phase 11 + Phase 13. Append a Phase 14 paragraph at the end of the docstring (before line 21's closing `'''`):

    Phase 14 changes (D-13/D-14):
      - Phase 10 D-15 ("web is read-only on state.json") is AMENDED:
        web/routes/trades.py introduces /trades/{open,close,modify} POST
        endpoints that mutate state.json. Cross-process coordination via
        fcntl.LOCK_EX in state_manager.save_state (Plan 14-02).
      - 422 → 400 remap installed via add_exception_handler — Pydantic
        validation errors return JSONResponse with body
        {"errors": [{"field": ..., "reason": ...}]} per TRADE-02.
      - Hex-boundary inheritance: sizing_engine + system_params now allowed
        for web/routes/trades.py per Plan 14-01 FORBIDDEN_FOR_WEB update.

**2. Add module-top import** for the trades route module. Insert AFTER `from web.routes import state as state_route` (line 30):

    from web.routes import trades as trades_route

**3. Register trades route + exception handler** inside `create_app()` (line 86-94). Insert two lines:

   - After `state_route.register(application)` (line 89) and BEFORE `# D-06: AuthMiddleware MUST be registered LAST` (line 91): add `trades_route.register(application)`.
   - Same block, AFTER the `trades_route.register(application)` line and BEFORE the AuthMiddleware comment: add the exception handler registration. The handler is `trades_route._validation_exception_handler` (defined in Task 1).

After the change, the relevant section of create_app() looks like:

    healthz_route.register(application)
    dashboard_route.register(application)
    state_route.register(application)
    trades_route.register(application)

    # Phase 14 D-04 / TRADE-02: 422 → 400 remap with field-level error JSON.
    # Single global handler covers all routes (Plan 14-04).
    from fastapi.exceptions import RequestValidationError
    application.add_exception_handler(
      RequestValidationError, trades_route._validation_exception_handler,
    )

    # D-06: AuthMiddleware MUST be registered LAST — Starlette runs middleware
    # in REVERSE of registration, so 'last registered' = 'first to dispatch'.
    # Future middleware (request-id, compression) goes ABOVE this line.
    application.add_middleware(AuthMiddleware, secret=secret)

The `from fastapi.exceptions import RequestValidationError` is LOCAL inside `create_app()` — it's a one-time-at-startup operation and putting it module-top would require an additional top-level import line. LOCAL keeps the change minimal.

**4. Update the create_app() log line at the end** (line 96) to mention Phase 14:

Before:
    logger.info('[Web] FastAPI app created (Phase 13 — /, /api/state, /healthz; auth=on)')

After:
    logger.info('[Web] FastAPI app created (Phase 14 — /, /api/state, /healthz, /trades/{open,close,modify}; auth=on)')

DO NOT modify _read_auth_secret (Phase 13). DO NOT modify the FastAPI(...) constructor kwargs (D-21+D-22 docs_url=None / redoc_url=None / openapi_url=None unchanged). DO NOT remove or reorder the existing healthz/dashboard/state route registrations.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && WEB_AUTH_SECRET=$(printf 'a%.0s' {1..32}) python -c "from web.app import create_app; app = create_app(); routes = [r.path for r in app.routes]; assert '/trades/open' in routes; assert '/trades/close' in routes; assert '/trades/modify' in routes; assert '/trades/close-form' in routes; assert '/trades/modify-form' in routes; assert '/trades/cancel-row' in routes; assert '/healthz' in routes; assert '/' in routes; assert '/api/state' in routes" && pytest tests/test_web_healthz.py tests/test_web_state.py tests/test_web_dashboard.py tests/test_web_app_factory.py tests/test_web_auth_middleware.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from web.routes import trades as trades_route" web/app.py` exits 0
    - `grep -q "trades_route.register(application)" web/app.py` exits 0
    - `grep -q "RequestValidationError" web/app.py` exits 0
    - `grep -q "trades_route._validation_exception_handler" web/app.py` exits 0
    - `grep -q "Phase 14 — /, /api/state, /healthz, /trades/{open,close,modify}; auth=on" web/app.py` exits 0
    - `grep -q "Phase 14 changes" web/app.py` exits 0 (docstring amendment present)
    - `grep -q "Phase 10 D-15.*AMENDED" web/app.py` exits 0
    - `WEB_AUTH_SECRET=$(printf 'a%.0s' {1..32}) python -c "from web.app import create_app; app = create_app(); paths = {r.path for r in app.routes}; assert paths >= {'/trades/open', '/trades/close', '/trades/modify', '/trades/close-form', '/trades/modify-form', '/trades/cancel-row'}"` exits 0 (all 6 endpoints registered)
    - All Phase 13 regression suites still green: `pytest tests/test_web_healthz.py tests/test_web_state.py tests/test_web_dashboard.py tests/test_web_app_factory.py tests/test_web_auth_middleware.py -x -q` exits 0
    - The AuthMiddleware register line is the LAST application.add_middleware call (verify via `grep -n "add_middleware" web/app.py | tail -1` → line number is after the trades_route.register and after the add_exception_handler — order locked per D-06)
  </acceptance_criteria>
  <done>
    - web/app.py imports + registers trades_route alongside the existing three routes
    - RequestValidationError exception handler installed before AuthMiddleware (registration order doesn't matter for handlers, but logical grouping with routes for readability)
    - Module docstring carries Phase 14 D-13/D-14 amendment paragraph
    - Log line includes Phase 14 + the new /trades/* paths
    - All 9 routes (3 Phase 11/13 + 6 Phase 14) reachable via TestClient
    - Phase 13 test suites still green (no regression)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_web_trades.py fully populated — 13 classes, ~50 tests covering D-01..D-13 + ASVS L1</name>
  <files>tests/test_web_trades.py</files>
  <behavior>
    Replace the 13 skeleton classes (Plan 14-01 Task 2 placeholders) with populated tests covering:
    - TestOpenTradeEndpoint: happy LONG, happy SHORT, default executed_at AWST, position appended to state, save_state called once, returns 200 + HTML (TRADE-01)
    - TestOpenPyramidUp: same-direction with should_add=true → pyramid applied; same-direction with should_add=false → 409; same-direction missing atr → 409; opposite direction → 409 with locked D-01 message (TRADE-01 + D-01 + D-02)
    - TestOpenAdvancedFields: D-03 LONG peak < entry → 400; LONG trough not None → 400; SHORT trough > entry → 400; SHORT peak not None → 400; pyramid_level out of [0,2] → 400; entry_price NaN → 400; LONG missing peak defaults to entry_price; SHORT missing trough defaults to entry_price (TRADE-02)
    - TestCloseTradeEndpoint: happy LONG and SHORT close, exit_reason=operator_close, multiplier+cost_aud from _resolved_contracts, position becomes None, trade_log grows, account updates per record_trade D-14 (TRADE-03)
    - TestCloseTradePnLMath: LONG gross_pnl = (exit-entry)*n*mult; SHORT gross_pnl = (entry-exit)*n*mult; record_trade deducts closing-half cost; final account == initial_account + gross_pnl - closing_half_cost (D-05 + D-07 + Phase 3 D-14)
    - TestModifyTradeEndpoint: new_stop=7700.0 sets manual_stop; new_stop=null clears (sets to None); new_stop ABSENT preserves; new_contracts=N sets n_contracts and resets pyramid_level=0; both fields applied atomically; missing position → 409 (TRADE-04 + D-09..D-12)
    - TestModifyAbsentVsNull: model_fields_set semantics — three explicit cases (absent, null, value)
    - TestErrorResponses: 422→400 remap; body shape {"errors":[{"field":..., "reason":...}]}; multiple errors aggregated; field name extracted as last loc element (TRADE-02)
    - TestHTMXResponses: open returns full <tbody> partial; close returns body + HX-Trigger: positions-changed; modify returns single <tr>; OOB confirmation banner present; Content-Type text/html on success; 4xx returns application/json (TRADE-05 partial — Plan 14-05 has more)
    - TestHTMXSupportEndpoints: GET /trades/close-form returns 2-row HTML with confirmation panel + cancel button + exit_price input; GET /trades/modify-form returns 2-row HTML with new_stop + new_contracts inputs + cancel; GET /trades/cancel-row returns the original <tr>; missing instrument on GETs → 404
    - TestSaveStateInvariant: every successful POST calls save_state exactly once (captured_saves length == 1); failed validations call save_state 0 times; 409 conflicts call save_state 0 times (TRADE-06 atomic-single-save discipline)
    - TestSoleWriterInvariant: AST walk per RESEARCH §Pattern 10 lines 752-783 — assert no `state['warnings'] = ...` and no `state['warnings'].append/.extend/.insert` in web/routes/trades.py (TRADE-06)
    - TestEndToEnd: round-trip open → state.positions populated → close → state.positions[X] None and trade_log grew; full request cycle through TestClient
  </behavior>
  <read_first>
    - tests/test_web_trades.py current shape (Plan 14-01 Task 2 — 13 skeleton classes with pytest.skip placeholders)
    - tests/test_web_state.py full file — analog for client_with_state pattern, asserting JSON shape, content-type
    - tests/test_web_auth_middleware.py first 80 lines — analog for AUTH_HEADER_NAME usage + fixture composition
    - tests/conftest.py — VALID_SECRET, AUTH_HEADER_NAME, htmx_headers, client_with_state_v3 fixtures (Plan 14-01)
    - 14-RESEARCH.md §Pattern 10 lines 752-783 (AST sole-writer invariant verbatim)
    - 14-VALIDATION.md §Per-Task Verification Map for the canonical test names (TRADE-01..06 mapped to test_* names)
    - state_manager.py:485-519 (record_trade D-14 closing-half cost deduction — locks the P&L math expectation in TestCloseTradePnLMath)
    - state_manager.py:199-265 (_validate_trade — knows what shape record_trade requires, helps test design)
    - sizing_engine.py:306-357 (PyramidDecision shape — TestOpenPyramidUp asserts add_contracts/new_level)
    - 14-UI-SPEC.md §Decision 5 (close-form 2-row block markup — TestHTMXSupportEndpoints assertions)
    - 14-CONTEXT.md D-01..D-13 (verbatim message strings for 409 assertions; e.g., D-01 message)
  </read_first>
  <action>
Replace the 13 skeleton classes with populated tests. Use the `client_with_state_v3` fixture from conftest.py (returns `(client, set_state, captured_saves)` tuple) for all tests.

Module-top imports (add to existing imports — keep the AUTH_HEADER_NAME / VALID_SECRET inline lines from Plan 14-01):

    import json
    import math
    from pathlib import Path

    import pytest
    from fastapi.testclient import TestClient

    AUTH_HEADER_NAME = 'X-Trading-Signals-Auth'
    VALID_SECRET = 'a' * 32  # matches tests/conftest.py

Test class implementations (write each as a `class TestX:` with multiple `def test_*` methods). The body is large — implement each class as designed below. Each test method MUST receive `client_with_state_v3` and unpack `(client, set_state, captured_saves)` consistently.

Provide a helper function at module top to seed standard state:

    def _seed_state_with_open_position(set_state, instrument='SPI200', direction='LONG', n_contracts=2, atr=50.0, manual_stop=None):
      '''Helper to seed a state dict with one open position; mirrors the v3 schema.'''
      set_state({
        'schema_version': 3,
        'account': 100_000.0,
        'last_run': '2026-04-25',
        'positions': {
          'SPI200': {
            'direction': direction, 'entry_price': 7800.0, 'entry_date': '2026-04-20',
            'n_contracts': n_contracts, 'pyramid_level': 0,
            'peak_price': 7850.0 if direction == 'LONG' else None,
            'trough_price': None if direction == 'LONG' else 7800.0,
            'atr_entry': atr, 'manual_stop': manual_stop,
          } if instrument == 'SPI200' else None,
          'AUDUSD': None if instrument == 'SPI200' else {
            'direction': direction, 'entry_price': 0.6450, 'entry_date': '2026-04-20',
            'n_contracts': n_contracts, 'pyramid_level': 0,
            'peak_price': 0.6500 if direction == 'LONG' else None,
            'trough_price': None if direction == 'LONG' else 0.6440,
            'atr_entry': 0.012, 'manual_stop': manual_stop,
          },
        },
        'signals': {'SPI200': {'atr': atr, 'last_close': 7820.0}, 'AUDUSD': {'atr': 0.012, 'last_close': 0.6480}},
        'trade_log': [], 'equity_history': [], 'warnings': [],
        'initial_account': 100_000.0,
        'contracts': {'SPI200': 'spi-mini', 'AUDUSD': 'audusd-mini'},
        '_resolved_contracts': {
          'SPI200': {'multiplier': 5.0, 'cost_aud': 6.0},
          'AUDUSD': {'multiplier': 10000.0, 'cost_aud': 5.0},
        },
      })

For brevity in this plan I describe the test bodies' assertions; the executor writes them following the reference patterns. Each test class spec:

**TestOpenTradeEndpoint** (~6 tests, ~80 lines):

- test_open_long_happy_path: empty state.positions, POST {SPI200, LONG, 7800.0, 2}, expect status 200, content-type starts with 'text/html', captured_saves has length 1, the saved state has positions.SPI200 with direction=LONG, entry_price=7800.0, n_contracts=2, manual_stop=None
- test_open_short_happy_path: mirror for SHORT
- test_open_default_executed_at_uses_today_awst: omit executed_at, assert the saved entry_date equals today AWST in ISO format
- test_open_explicit_executed_at_honored: send executed_at=2026-04-22, assert entry_date=2026-04-22
- test_open_returns_html_partial_with_hx_trigger: response Content-Type startswith text/html; response.headers['HX-Trigger'] == 'positions-changed'
- test_open_no_atr_in_signals_returns_409: seed state with signals.SPI200.atr absent; POST open; expect 409 with body containing 'no ATR' or 'atr'

**TestOpenPyramidUp** (~5 tests):

- test_same_direction_pyramid_should_add_true_applies_increment: seed state with SPI200 LONG n_contracts=2 pyramid_level=0 atr=50.0 entry=7800; POST open with entry_price=7900 (distance 100 >= 1*atr=50 → check_pyramid says add=1); assert response 200, captured saved state has SPI200 n_contracts=3 pyramid_level=1
- test_same_direction_pyramid_should_add_false_returns_409: same seed but POST entry_price=7820 (distance 20 < 50 → no add); expect 409 with 'Pyramid blocked'
- test_same_direction_at_max_pyramid_returns_409: seed pyramid_level=2; expect 409 (D-02 cap)
- test_opposite_direction_returns_409_with_locked_message: seed SPI200 LONG; POST {SPI200, SHORT, 7800, 2}; expect 409 with body containing 'already has an open LONG position; close it first via POST /trades/close before opening a SHORT' (verbatim D-01)
- test_pyramid_blocked_no_atr_returns_409: seed state.signals.SPI200.atr absent + existing position; expect 409

**TestOpenAdvancedFields** (~7 tests):

- test_long_peak_below_entry_returns_400: POST with peak_price=7700, entry=7800, direction=LONG; expect 400, body.errors contains a peak_price entry mentioning "must be >= entry_price"
- test_long_with_trough_set_returns_400: POST with direction=LONG, trough_price=7800; expect 400 mentioning "trough_price: must be absent or null for LONG"
- test_short_trough_above_entry_returns_400
- test_short_with_peak_set_returns_400
- test_pyramid_level_out_of_range_returns_400: pyramid_level=3; expect 400 mentioning "in [0, 2]"
- test_entry_price_nan_returns_400: entry_price=NaN (cast to float via json's "NaN" — but JSON spec doesn't allow NaN; Pydantic+FastAPI rejects at parse before our validator. Test variant: send the body as a Python dict that the TestClient serializes and assert the rejection. Alternative: directly construct OpenTradeRequest in Python and assert ValidationError is raised — that's a model-level test, not an HTTP test. Recommend the model-level form for NaN: `with pytest.raises(ValidationError): OpenTradeRequest(instrument='SPI200', direction='LONG', entry_price=float('nan'), contracts=2)` — assert it mentions 'finite' or 'nan')
- test_long_missing_peak_defaults_to_entry: omit peak_price; expect saved state SPI200.peak_price == entry_price

**TestCloseTradeEndpoint** (~5 tests):

- test_close_long_position_records_trade: seed SPI200 LONG n_contracts=2 entry=7800; POST close exit_price=7900; assert response 200, captured saved state: positions.SPI200=None, trade_log length=1, trade_log[0].instrument=SPI200, exit_reason='operator_close', exit_price=7900, multiplier=5.0, cost_aud=6.0
- test_close_short_position_records_trade: mirror SHORT
- test_close_missing_position_returns_409: empty positions; POST close; expect 409 with 'no open position'
- test_close_default_executed_at_uses_today_awst: omit executed_at; saved trade.exit_date equals today AWST iso
- test_close_explicit_executed_at_honored: send executed_at=2026-04-22; saved trade.exit_date=2026-04-22

**TestCloseTradePnLMath** (~3 tests — D-05 + Phase 3 D-14):

- test_close_long_pnl_math_matches_inline_formula: seed n_contracts=2 entry=7800; close at exit=7900; gross expected = (7900-7800)*2*5 = 1000.0; closing-half cost = 6.0*2/2 = 6.0; net = 994.0; final account = 100_000 + 994 = 100_994.0; assert captured_saves[0]['account'] == 100_994.0 AND captured_saves[0]['trade_log'][0]['gross_pnl'] == 1000.0 AND captured_saves[0]['trade_log'][0].get('net_pnl') == 994.0
- test_close_short_pnl_math_matches_inline_formula: SHORT n_contracts=1 entry=0.6450 exit=0.6420; gross = (0.6450-0.6420)*1*10000 = 30.0; closing-half cost = 5*1/2 = 2.5; net = 27.5; account += 27.5; assert with floating-point tolerance via `pytest.approx`
- test_close_does_not_call_compute_unrealised_pnl: regression — assert that web.routes.trades source DOES NOT contain `compute_unrealised_pnl` (D-05 anti-pitfall — already covered by Task 1 grep but include here as a code-level test for visibility): `assert 'compute_unrealised_pnl' not in Path('web/routes/trades.py').read_text()`

**TestModifyTradeEndpoint** (~7 tests):

- test_modify_sets_manual_stop: POST {instrument: SPI200, new_stop: 7700.0}; saved state SPI200.manual_stop == 7700.0
- test_modify_null_stop_clears_override: seed state with SPI200.manual_stop=7700.0; POST {SPI200, new_stop: None}; saved state SPI200.manual_stop is None
- test_modify_absent_stop_no_change: seed manual_stop=7700.0; POST {SPI200, new_contracts: 3}; saved state SPI200.manual_stop == 7700.0 (UNCHANGED)
- test_modify_new_contracts_resets_pyramid_level: seed pyramid_level=2; POST {SPI200, new_contracts: 5}; saved state SPI200.n_contracts == 5 AND pyramid_level == 0 (D-10)
- test_modify_both_fields_atomic_save: POST {SPI200, new_stop: 7700.0, new_contracts: 5}; captured_saves length == 1 (D-11), saved state has both updates
- test_modify_empty_body_returns_400: POST {SPI200} (no new_stop, no new_contracts); expect 400 with reason "at least one of new_stop, new_contracts must be present"
- test_modify_zero_contracts_returns_400: POST {SPI200, new_contracts: 0}; expect 400 (Pydantic Field ge=1)
- test_modify_nan_stop_returns_400: POST {SPI200, new_stop: float('nan')} via JSON serialization (use `json.dumps(..., allow_nan=True)` to get "NaN" literal in body, send as raw bytes — or assert at the Pydantic model layer with `pytest.raises(ValidationError)`); expect 400 (or Pydantic rejection)
- test_modify_missing_position_returns_409: empty positions; POST modify; expect 409 'no open position'

**TestModifyAbsentVsNull** (~3 tests — directly model-level, not HTTP):

- test_pydantic_absent_field_not_in_model_fields_set: `m = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_contracts': 2})`; assert `'new_stop' not in m.model_fields_set`
- test_pydantic_null_field_in_model_fields_set: `m = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_stop': None})`; assert `'new_stop' in m.model_fields_set`
- test_pydantic_value_field_in_model_fields_set: `m = ModifyTradeRequest.model_validate({'instrument': 'SPI200', 'new_stop': 7700.0})`; assert `'new_stop' in m.model_fields_set`

**TestErrorResponses** (~4 tests):

- test_400_body_shape_errors_array: POST open with contracts=0; assert response.status_code == 400, body is JSON, body has key 'errors' which is a list, each element has 'field' and 'reason' keys
- test_400_aggregates_multiple_field_errors: POST open with contracts=0 AND entry_price=-1; assert body.errors length >= 2 mentioning both fields
- test_400_field_name_extracted_from_loc_leaf: POST open with contracts=0; assert any(e['field'] == 'contracts' for e in body.errors)
- test_400_uses_application_json_content_type: POST open with invalid body; assert response.headers['content-type'].startswith('application/json')

**TestHTMXResponses** (~6 tests):

- test_open_response_is_html_with_hx_trigger: open success; Content-Type startswith text/html; HX-Trigger header present
- test_open_response_contains_positions_tbody_partial: open success; body contains the `<tr id="position-row-SPI200">` markup (proves the partial structure)
- test_open_response_contains_oob_confirmation_banner: body contains `hx-swap-oob` AND the banner success text (Opened LONG SPI200 ...)
- test_close_response_has_hx_trigger_positions_changed
- test_modify_response_returns_re_rendered_row: body contains `<tr id="position-row-SPI200">` markup
- test_modify_response_contains_manual_badge_when_set: seed manual_stop=7700.0, POST modify with new_contracts=5; response body contains `class="badge badge-manual"`

**TestHTMXSupportEndpoints** (~5 tests):

- test_close_form_returns_two_row_partial_with_confirmation: GET /trades/close-form?instrument=SPI200 (with auth); response 200, content-type text/html; body contains `<input type="number"` AND `name="exit_price"` AND a `Cancel` button AND a `Confirm close` button
- test_modify_form_returns_two_row_partial_with_inputs: GET /trades/modify-form; body contains `name="new_stop"` AND `name="new_contracts"` AND `Cancel` AND `Save`
- test_cancel_row_returns_original_position_row: GET /trades/cancel-row; body contains `<tr id="position-row-SPI200">`
- test_close_form_missing_position_returns_404: empty positions; expect 404
- test_form_endpoints_require_auth: omit auth header for GET /trades/close-form; expect 401 (Phase 13 AuthMiddleware inheritance)

**TestSaveStateInvariant** (~5 tests):

- test_open_calls_save_state_once: open happy path; assert len(captured_saves) == 1
- test_close_calls_save_state_once
- test_modify_calls_save_state_once
- test_invalid_request_does_not_call_save_state: POST open with contracts=0; assert len(captured_saves) == 0
- test_409_conflict_does_not_call_save_state: opposite-direction conflict; assert len(captured_saves) == 0

**TestSoleWriterInvariant** (~2 tests — RESEARCH §Pattern 10 verbatim):

    class TestSoleWriterInvariant:
      '''TRADE-06: web/routes/trades.py must NOT write to state['warnings'].

      RESEARCH §Pattern 10 lines 752-783 verbatim AST walk.
      '''

      def test_no_warnings_subscript_assignment(self):
        import ast
        src = Path('web/routes/trades.py').read_text()
        tree = ast.parse(src)
        violations = []
        for node in ast.walk(tree):
          if isinstance(node, ast.Assign):
            for tgt in node.targets:
              if (isinstance(tgt, ast.Subscript)
                  and isinstance(tgt.slice, ast.Constant)
                  and tgt.slice.value == 'warnings'):
                violations.append(f'line {node.lineno}: assigns to <expr>["warnings"]')
        assert violations == [], '\n'.join(violations)

      def test_no_warnings_method_mutation(self):
        '''Detect: state['warnings'].append(...), .extend(...), .insert(...).'''
        import ast
        src = Path('web/routes/trades.py').read_text()
        tree = ast.parse(src)
        violations = []
        for node in ast.walk(tree):
          if (isinstance(node, ast.Call)
              and isinstance(node.func, ast.Attribute)
              and node.func.attr in ('append', 'extend', 'insert')):
            attr_obj = node.func.value
            if (isinstance(attr_obj, ast.Subscript)
                and isinstance(attr_obj.slice, ast.Constant)
                and attr_obj.slice.value == 'warnings'):
              violations.append(f'line {node.lineno}: mutates <expr>["warnings"]')
        assert violations == [], '\n'.join(violations)

**TestEndToEnd** (~2 tests):

- test_open_then_close_round_trip: state seeded empty; POST open → captured_saves[0] has positions.SPI200; reset captured_saves AND set_state to the result; POST close → captured_saves[0] has positions.SPI200=None and trade_log length 1 (note: with the captured_saves stub, set_state must explicitly forward the captured state to the next request — so the test is essentially "two sequential requests that simulate a real flow"; the harness pattern is: after open save, manually set_state(captured_saves[-1]) before issuing close)
- test_open_to_dashboard_round_trip: open succeeds; subsequent GET / (dashboard) shows the new position rendered (uses the same client; relies on client_with_state_v3's monkeypatched load_state always returning state_box['value']; so set_state(captured_saves[-1]) before GET /)

Make ALL tests parametrize-friendly (use pytest fixtures, not class-level state). Avoid sleep/wait calls. Total runtime target: < 5 seconds for the entire file.

One edit to the existing top of tests/test_web_trades.py: replace the placeholder `pytest.skip('Wave 0 skeleton; Plan 14-XX implements')` calls with the populated test bodies as designed. Skeleton class structure remains (13 classes); each class's contents change from `def test_placeholder_wave_0(...)` to the populated tests above.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && pytest tests/test_web_trades.py -x -q 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_web_trades.py -x -q` exits 0 (all 13 classes populated, ~50 tests passing)
    - `grep -c "pytest.skip" tests/test_web_trades.py` returns 0 (no skipped placeholders left)
    - `grep -q "test_no_warnings_subscript_assignment" tests/test_web_trades.py` exits 0 (TRADE-06 sole-writer)
    - `grep -q "test_no_warnings_method_mutation" tests/test_web_trades.py` exits 0
    - `grep -q "test_close_long_pnl_math_matches_inline_formula" tests/test_web_trades.py` exits 0 (D-05 P&L math)
    - `grep -q "test_close_short_pnl_math_matches_inline_formula" tests/test_web_trades.py` exits 0
    - `grep -q "test_close_does_not_call_compute_unrealised_pnl" tests/test_web_trades.py` exits 0 (D-05 anti-pitfall regression)
    - `grep -q "test_modify_sets_manual_stop" tests/test_web_trades.py` exits 0
    - `grep -q "test_modify_null_stop_clears_override" tests/test_web_trades.py` exits 0
    - `grep -q "test_modify_absent_stop_no_change" tests/test_web_trades.py` exits 0
    - `grep -q "test_modify_new_contracts_resets_pyramid_level" tests/test_web_trades.py` exits 0
    - `grep -q "test_modify_empty_body_returns_400" tests/test_web_trades.py` exits 0
    - `grep -q "test_open_calls_save_state_once" tests/test_web_trades.py` exits 0 (TRADE-06 atomic-single-save)
    - `grep -q "test_pydantic_absent_field_not_in_model_fields_set" tests/test_web_trades.py` exits 0 (D-12 model_fields_set)
    - `grep -q "test_400_body_shape_errors_array" tests/test_web_trades.py` exits 0 (TRADE-02 422→400 remap)
    - `grep -q "already has an open" tests/test_web_trades.py` exits 0 (D-01 verbatim message asserted)
    - `pytest tests/test_web_trades.py::TestSoleWriterInvariant -x -q` exits 0
    - `pytest tests/test_web_trades.py::TestSaveStateInvariant -x -q` exits 0
    - `pytest tests/test_web_trades.py::TestCloseTradePnLMath -x -q` exits 0
    - `pytest tests/test_web_trades.py::TestModifyTradeEndpoint -x -q` exits 0
    - `pytest tests/test_web_trades.py::TestOpenPyramidUp -x -q` exits 0
    - All Wave 1 + Wave 2 regression suites still green: `pytest tests/test_state_manager.py tests/test_sizing_engine.py tests/test_web_healthz.py tests/test_web_state.py tests/test_web_dashboard.py tests/test_web_app_factory.py tests/test_web_auth_middleware.py tests/test_web_trades.py -x -q` exits 0
    - File runtime < 10 seconds: `time pytest tests/test_web_trades.py -q` reports total time under 10s
  </acceptance_criteria>
  <done>
    - 13 test classes fully populated; ~50 tests covering D-01..D-13, TRADE-01..06, ASVS L1 V5/V7
    - TestSoleWriterInvariant uses verbatim RESEARCH §Pattern 10 AST walk
    - TestCloseTradePnLMath proves the D-05 inline gross_pnl + Phase 3 D-14 closing-half cost flow numerically
    - TestModifyAbsentVsNull proves Pydantic v2 model_fields_set semantics for the three D-12 cases
    - TestSaveStateInvariant proves D-11 atomic single save_state per mutation
    - All listed grep + pytest checks pass; full Phase 14 web-tier suite green
  </done>
</task>

<!--
==========================================================================
REVISION ADDENDUM (2026-04-25, source: 14-REVIEWS.md)

The following SUPERSEDES the corresponding sections in Tasks 1-3 above.
Where the addendum and the body diverge, the addendum WINS — the executor
applies the addendum's specifications. The body is preserved for traceability
of intent + diff review (REVIEWS HIGH/MEDIUM/LOW disposition).

REVIEWS items addressed in this addendum:
  HIGH #1  — handlers use mutate_state, not load_state + save_state
  HIGH #2  — close-success returns empty body + HX-Trigger (NOT a <div> banner)
  HIGH #3  — tbody-grouped topology; cancel restores via tbody swap
  HIGH #4  — dashboard.html uses {{WEB_AUTH_SECRET}} placeholder; the GET /
              handler in web/routes/dashboard.py substitutes at request time
  MEDIUM #7 — atr lookup uses state['signals'][instrument]['last_scalars']['atr']
              (verified via main.py:1225); old plan read .['atr'] which is wrong
  LOW #8   — TestSoleWriterInvariant covers ast.AugAssign branch
  LOW #9   — modify handler resets pyramid_level on ANY successful modify
              (move outside `if 'new_contracts' in req.model_fields_set`)
  LOW #10  — RequestValidationError remap regression test ensures other routes
              are unaffected
==========================================================================
-->

<task type="auto" tdd="true">
  <name>Task 4 (REVISION): handlers use mutate_state; tbody topology; correct ATR lookup; pyramid reset on ANY modify</name>
  <files>web/routes/trades.py</files>
  <read_first>
    - web/routes/trades.py current body (post-Task-1 — the handlers + helpers from this plan's body)
    - state_manager.py (post-Plan-14-02 — mutate_state helper present)
    - main.py:1225 — confirms state['signals'][state_key] shape: {'signal':..., 'last_scalars': scalars, 'last_close':..., ...}; ATR lives at ['last_scalars']['atr']
    - 14-CONTEXT.md D-13 REVISED (mutate_state contract)
    - 14-CONTEXT.md D-10 (REVIEWS LOW #9 — pyramid_level resets on ANY modify)
    - 14-CONTEXT.md Claude's Discretion §HTMX (tbody grouping topology)
    - 14-REVIEWS.md HIGH #2/#3/#4, MEDIUM #7, LOW #9
  </read_first>
  <action>
Apply five surgical revisions to `web/routes/trades.py`. The body content from Task 1 must be REPLACED in the specific spots called out below; the rest of Task 1 stands.

**REVISION 4.A — handlers use `state_manager.mutate_state` (REVIEWS HIGH #1).**

Replace the body of `open_trade`, `close_trade`, AND `modify_trade` so each handler defines a local mutator function and calls `state_manager.mutate_state(_mutator)` ONCE, instead of the body's current `state = load_state(); ... ; save_state(state)` shape.

Pattern for `open_trade` (replace the body region around lines 394-446):

    @app.post('/trades/open')
    def open_trade(req: OpenTradeRequest):
      '''Phase 14 TRADE-01 + D-01..D-04 + D-13 (REVIEWS HIGH #1: mutate_state).'''
      from state_manager import mutate_state
      from sizing_engine import check_pyramid

      # Captured in closure for response rendering after mutate returns.
      result_holder: dict = {'kind': None, 'extra': None}

      def _apply(state):
        existing = state['positions'].get(req.instrument)
        if existing is not None and existing['direction'] != req.direction:
          msg = (
            f'instrument {req.instrument} already has an open '
            f'{existing["direction"]} position; close it first via '
            f'POST /trades/close before opening a {req.direction}'
          )
          result_holder['kind'] = 'conflict_opposite'
          result_holder['extra'] = msg
          # Raise to short-circuit out of mutate_state; lock released by mutate_state's finally.
          raise _OpenConflict(msg)

        if existing is not None and existing['direction'] == req.direction:
          # D-02 + REVIEWS MEDIUM #7: ATR lookup is nested under last_scalars
          sig_entry = state.get('signals', {}).get(req.instrument, {})
          if not isinstance(sig_entry, dict):
            result_holder['kind'] = 'conflict_no_atr'
            result_holder['extra'] = 'pyramid blocked: no ATR available in state.signals'
            raise _OpenConflict(result_holder['extra'])
          atr_entry = sig_entry.get('last_scalars', {}).get('atr')
          if atr_entry is None:
            result_holder['kind'] = 'conflict_no_atr'
            result_holder['extra'] = 'pyramid blocked: no ATR available in state.signals'
            raise _OpenConflict(result_holder['extra'])

          decision = check_pyramid(existing, current_price=req.entry_price, atr_entry=atr_entry)
          if decision.add_contracts == 0:
            result_holder['kind'] = 'conflict_pyramid_blocked'
            result_holder['extra'] = 'Pyramid blocked: gate not met or already at MAX_PYRAMID_LEVEL'
            raise _OpenConflict(result_holder['extra'])
          existing['n_contracts'] += decision.add_contracts
          existing['pyramid_level'] = decision.new_level
          result_holder['kind'] = 'pyramid_up'
          return

        # Fresh-open path
        sig_entry = state.get('signals', {}).get(req.instrument, {})
        atr_entry = sig_entry.get('last_scalars', {}).get('atr') if isinstance(sig_entry, dict) else None
        if atr_entry is None:
          result_holder['kind'] = 'conflict_no_atr'
          result_holder['extra'] = 'open blocked: no ATR available in state.signals; daily run must complete first'
          raise _OpenConflict(result_holder['extra'])

        executed_at = req.executed_at or _now_awst().date()
        state['positions'][req.instrument] = _build_position_dict(req, executed_at, atr_entry)
        result_holder['kind'] = 'fresh_open'

      try:
        state = mutate_state(_apply)
      except _OpenConflict as exc:
        return Response(content=str(exc), status_code=409, media_type='text/plain; charset=utf-8')

      logger.info('[Web] /trades/open %s instrument=%s', result_holder['kind'], req.instrument)
      return _render_open_success_partial(state, req.instrument, req.direction, req.entry_price, req.contracts)

Define `_OpenConflict` as a private exception class at module level (just below the import block):

    class _OpenConflict(Exception):
      '''Internal sentinel: a conflict detected inside the mutate_state mutator.
      Caught by the handler to return a 409. Body is the conflict message.'''

Update `_build_position_dict` signature to take `atr_entry` directly (avoiding redundant state[ ] lookups inside the helper):

    def _build_position_dict(req, executed_at, atr_entry):
      # ... unchanged body, just uses the atr_entry parameter ...

Apply the SAME pattern to `close_trade` and `modify_trade`:
- Wrap the read+mutation in a local `_apply(state)` function
- Use `mutate_state(_apply)` to bind the lock around load → mutate → save
- Capture the post-mutation state via the return value of mutate_state

For `modify_trade`, ALSO apply REVIEWS LOW #9: move `pos['pyramid_level'] = 0` OUTSIDE the `if 'new_contracts' in req.model_fields_set` block so it fires on `new_stop`-only modifies too (matching D-10 spec):

    def modify_trade(req: ModifyTradeRequest):
      '''Phase 14 TRADE-04 + D-09..D-13 + REVIEWS LOW #9 (pyramid resets on ANY modify).'''
      from state_manager import mutate_state

      conflict: dict = {'msg': None}

      def _apply(state):
        pos = state['positions'].get(req.instrument)
        if pos is None:
          conflict['msg'] = f'no open position for instrument {req.instrument}'
          raise _OpenConflict(conflict['msg'])
        if 'new_stop' in req.model_fields_set:
          pos['manual_stop'] = req.new_stop
        if 'new_contracts' in req.model_fields_set and req.new_contracts is not None:
          pos['n_contracts'] = req.new_contracts
        # REVIEWS LOW #9 / D-10: pyramid_level resets on ANY successful modify
        pos['pyramid_level'] = 0

      try:
        state = mutate_state(_apply)
      except _OpenConflict as exc:
        return Response(content=str(exc), status_code=409, media_type='text/plain; charset=utf-8')

      logger.info('[Web] /trades/modify completed: instrument=%s fields=%s',
                  req.instrument, sorted(req.model_fields_set & {'new_stop', 'new_contracts'}))
      return _render_modify_success_partial(state, req.instrument)

Same pattern for `close_trade`:

    def close_trade(req: CloseTradeRequest):
      '''Phase 14 TRADE-03 + D-05..D-08 + D-13 (REVIEWS HIGH #1: mutate_state).'''
      from state_manager import mutate_state, record_trade

      capture: dict = {'gross_pnl': None, 'cost_aud': None, 'n_contracts': None}
      conflict: dict = {'msg': None}

      def _apply(state):
        pos = state['positions'].get(req.instrument)
        if pos is None:
          conflict['msg'] = f'no open position for instrument {req.instrument}'
          raise _OpenConflict(conflict['msg'])
        resolved = state['_resolved_contracts'][req.instrument]
        multiplier = resolved['multiplier']
        cost_aud = resolved['cost_aud']
        # D-05 ANTI-PITFALL: inline raw price-delta — see body comment
        if pos['direction'] == 'LONG':
          gross_pnl = (req.exit_price - pos['entry_price']) * pos['n_contracts'] * multiplier
        else:
          gross_pnl = (pos['entry_price'] - req.exit_price) * pos['n_contracts'] * multiplier
        capture['gross_pnl'] = gross_pnl
        capture['cost_aud'] = cost_aud
        capture['n_contracts'] = pos['n_contracts']
        exit_date = (req.executed_at or _now_awst().date()).isoformat()
        trade = {
          'instrument': req.instrument,
          'direction': pos['direction'],
          'n_contracts': pos['n_contracts'],
          'entry_date': pos['entry_date'],
          'exit_date': exit_date,
          'exit_reason': _OPERATOR_CLOSE,
          'entry_price': pos['entry_price'],
          'exit_price': req.exit_price,
          'gross_pnl': gross_pnl,
          'multiplier': multiplier,
          'cost_aud': cost_aud,
        }
        # record_trade mutates state in place (Phase 3 D-13/D-20)
        record_trade(state, trade)

      try:
        state = mutate_state(_apply)
      except _OpenConflict as exc:
        return Response(content=str(exc), status_code=409, media_type='text/plain; charset=utf-8')

      logger.info('[Web] /trades/close completed: instrument=%s gross_pnl=%.2f',
                  req.instrument, capture['gross_pnl'])
      return _render_close_success_partial(req.instrument, capture['gross_pnl'],
                                            capture['cost_aud'], capture['n_contracts'])

**REVISION 4.B — close-success response shape: empty body + HX-Trigger header (REVIEWS HIGH #2).**

Replace `_render_close_success_partial` with the empty-body + HX-Trigger pattern. The dashboard's per-tbody listener (Plan 14-05) refreshes the affected `<tbody id="position-group-{instrument}">` when it receives the event:

    def _render_close_success_partial(instrument, gross_pnl, cost_aud, n_contracts) -> Response:
      '''REVIEWS HIGH #2: close-success returns EMPTY body + HX-Trigger event.

      Why empty body: the response targets a per-instrument <tbody> via outerHTML
      swap. Returning a <div> banner would land as a direct child of <tbody> —
      invalid HTML5 table structure. Instead: empty body + HX-Trigger header
      tells dashboard.html's tbody listener to refresh the group via fragment GET.

      Banner display is handled by the listener (it can render the OOB
      confirmation banner client-side from the event detail).
      '''
      import json as _json
      net_pnl = gross_pnl - (cost_aud * n_contracts / 2)
      payload = {
        'positions-changed': {
          'instrument': instrument,
          'kind': 'close',
          'net_pnl': net_pnl,
        },
      }
      return HTMLResponse(
        content='',  # empty: tbody listener refreshes via fragment GET
        status_code=200,
        headers={'HX-Trigger': _json.dumps(payload)},
      )

**REVISION 4.C — tbody-grouped topology (REVIEWS HIGH #3).**

Update three helpers so close/modify forms target the parent `<tbody id="position-group-{instrument}">` with `hx-swap="innerHTML"`. The confirmation panel REPLACES the tbody contents; cancel restores the original `<tr>` inside the same tbody.

`_render_close_form_partial` returns ONE `<tr>` (the confirmation panel) — NOT panel+original. The handler binds `hx-target="#position-group-{instrument}"` and `hx-swap="innerHTML"` so the tbody contains ONLY the confirmation row when the form is open:

    def _render_close_form_partial(state, instrument, pos) -> str:
      '''REVIEWS HIGH #3: SINGLE confirmation <tr> only.

      The caller (close-form GET handler) returns this string with hx-target
      pointing at #position-group-{instrument} and hx-swap="innerHTML" so
      the entire tbody contents is replaced by this row. Cancel restores
      the original <tr> by GET /trades/cancel-row?instrument={X} which
      returns _render_position_row_partial(state, X, pos).

      Single-tbody-level swap means no orphans: open form / confirmation /
      success state are mutually exclusive contents of the SAME tbody.
      '''
      esc = lambda s: html.escape(str(s), quote=True)
      return (
        f'<tr><td colspan="9">'
        f'Close {esc(pos["direction"])} {esc(instrument)} '
        f'({esc(pos["n_contracts"])} contracts) at exit price '
        f'<input type="number" step="0.01" min="0" name="exit_price" required autofocus />'
        f'<button type="button" class="btn-row" '
        f'hx-get="/trades/cancel-row?instrument={esc(instrument)}" '
        f'hx-target="#position-group-{esc(instrument)}" hx-swap="innerHTML">Cancel</button>'
        f'<button type="button" class="btn-row btn-close" '
        f'hx-post="/trades/close" hx-include="closest tr" '
        f'hx-vals=\'{{"instrument": "{esc(instrument)}"}}\' '
        f'hx-target="#position-group-{esc(instrument)}" hx-swap="innerHTML">Confirm close</button>'
        f'</td></tr>'
      )

Same shape for `_render_modify_form_partial` (single `<tr>` only).

`_render_position_row_partial` (Plan 14-05 has the canonical version) emits a `<tr id="position-row-{instrument}">` whose Close/Modify buttons have `hx-target="#position-group-{instrument}"` (NOT `#position-row-{instrument}`) and `hx-swap="innerHTML"`. This way every state transition for a position group is a single tbody-innerHTML swap.

Update the GET handlers to use `mutate_state`-free `load_state` (read-only is fine without mutate_state lock per D-13) BUT also support a `?fragment=position-group-{instrument}` query param on GET / that returns ONLY the affected tbody contents — used by the HX-Trigger refresh. (The fragment handler lives in web/routes/dashboard.py — see Task 5.)

The cancel-row handler:

    @app.get('/trades/cancel-row')
    def cancel_row(instrument: str):
      '''Restore a position's original row inside #position-group-{instrument}.

      Returns ONE <tr> (the canonical position row). HX swap is innerHTML on
      the parent tbody so the tbody contains exactly this one row after cancel.
      '''
      from state_manager import load_state
      state = load_state()
      pos = state['positions'].get(instrument)
      if pos is None:
        return Response(content=f'no open position for {instrument}', status_code=404,
                        media_type='text/plain; charset=utf-8')
      return HTMLResponse(content=_render_position_row_partial(state, instrument, pos))

**REVISION 4.D — TestSoleWriterInvariant covers ast.AugAssign (REVIEWS LOW #8).**

In tests/test_web_trades.py::TestSoleWriterInvariant, ADD a third test that walks `ast.AugAssign` nodes whose `target` is a `ast.Subscript` whose `slice.value == 'warnings'`:

    def test_no_warnings_aug_assign_in_trades_handlers(self):
      '''REVIEWS LOW #8: state['warnings'] += [...] must also be rejected.
      AST AugAssign branch is not covered by Assign or Call walks.'''
      import ast
      src = Path('web/routes/trades.py').read_text()
      tree = ast.parse(src)
      violations = []
      for node in ast.walk(tree):
        if isinstance(node, ast.AugAssign):
          tgt = node.target
          if (isinstance(tgt, ast.Subscript)
              and isinstance(tgt.slice, ast.Constant)
              and tgt.slice.value == 'warnings'):
            violations.append(f'line {node.lineno}: AugAssign on <expr>["warnings"]')
      assert violations == [], '\n'.join(violations)

ALSO add a positive-control regression: a synthetic source string containing `state['warnings'] += [...]` MUST trigger the violation when parsed and walked. Confirms the AST branch fires:

    def test_aug_assign_walker_fires_on_warnings_target(self):
      '''Sanity: when seeded with a violating snippet, the walker reports it.'''
      import ast
      bad_src = "def x():\n    state['warnings'] += ['err']\n"
      tree = ast.parse(bad_src)
      violations = []
      for node in ast.walk(tree):
        if isinstance(node, ast.AugAssign):
          tgt = node.target
          if (isinstance(tgt, ast.Subscript)
              and isinstance(tgt.slice, ast.Constant)
              and tgt.slice.value == 'warnings'):
            violations.append('found')
      assert violations == ['found']

**REVISION 4.E — RequestValidationError remap regression test (REVIEWS LOW #10).**

In tests/test_web_trades.py::TestErrorResponses (or new TestRequestValidationErrorRemap), ADD a regression test confirming the remap doesn't accidentally break non-trades routes that don't consume Pydantic bodies:

    class TestRequestValidationErrorRemap:
      '''REVIEWS LOW #10: confirm 422→400 remap doesn't accidentally change
      existing /api/state and / behavior beyond status remap on malformed inputs.'''

      def test_existing_routes_unaffected_by_remap(self, client_with_state_v3, auth_headers):
        '''POST to /api/state (which is GET-only) returns 405 Method Not Allowed —
        NOT 400 (because there's no Pydantic body to validate, the remap doesn't fire).'''
        client, _, _ = client_with_state_v3
        r = client.post('/api/state', headers=auth_headers, json={})
        assert r.status_code == 405, (
          f'REVIEWS LOW #10: existing routes must keep their canonical status; '
          f'got {r.status_code} (expected 405 Method Not Allowed)'
        )
        # The remap only fires on Pydantic schema failures, not on FastAPI's own
        # 405/404 responses.

      def test_remap_fires_only_on_pydantic_validation_failure(self, client_with_state_v3, htmx_headers):
        '''Negative test: malformed body to /trades/open IS remapped to 400.'''
        client, _, _ = client_with_state_v3
        r = client.post('/trades/open', headers=htmx_headers, json={'instrument': 'BTC'})
        assert r.status_code == 400, f'remap did not fire; got {r.status_code}'
        body = r.json()
        assert 'errors' in body
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && grep -q "from state_manager import mutate_state" web/routes/trades.py && grep -q "_OpenConflict" web/routes/trades.py && grep -q "last_scalars" web/routes/trades.py && grep -q "REVIEWS HIGH" web/routes/trades.py && grep -q "REVIEWS LOW #9" web/routes/trades.py && pytest tests/test_web_trades.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from state_manager import mutate_state" web/routes/trades.py` exits 0 (REVIEWS HIGH #1)
    - `grep -c "mutate_state(_apply)" web/routes/trades.py` returns at least 3 (open, close, modify all use mutate_state)
    - `grep -c "load_state()" web/routes/trades.py` returns at most 3 (only GET handlers — close-form, modify-form, cancel-row)
    - `grep -q "last_scalars" web/routes/trades.py` exits 0 (REVIEWS MEDIUM #7 — correct ATR shape)
    - `grep -c "\\['atr'\\]" web/routes/trades.py` returns 0 (no direct `['atr']` reads on signals dict — must go through last_scalars)
    - `grep -q "_OpenConflict" web/routes/trades.py` exits 0 (sentinel for in-mutator 409)
    - `grep -q "REVIEWS HIGH" web/routes/trades.py` exits 0 (audit trail)
    - `grep -q "pyramid_level.*= 0" web/routes/trades.py` exits 0; this assignment must be OUTSIDE the `if 'new_contracts' in req.model_fields_set` block in modify_trade (REVIEWS LOW #9; verified by test_modify_only_new_stop_resets_pyramid_level)
    - `grep -q "HX-Trigger" web/routes/trades.py` exits 0 (close-success uses HX-Trigger event)
    - `grep -q "_render_close_success_partial" web/routes/trades.py` exits 0; the function returns empty content with HX-Trigger header (REVIEWS HIGH #2). Test `test_close_success_returns_empty_with_hx_trigger` asserts this.
    - `grep -q "position-group-" web/routes/trades.py` exits 0 (tbody-grouped topology — REVIEWS HIGH #3); close-form / modify-form / cancel handlers target #position-group-{instrument} via hx-target
    - `grep -q "test_no_warnings_aug_assign_in_trades_handlers" tests/test_web_trades.py` exits 0 (REVIEWS LOW #8)
    - `grep -q "test_aug_assign_walker_fires_on_warnings_target" tests/test_web_trades.py` exits 0
    - `grep -q "TestRequestValidationErrorRemap" tests/test_web_trades.py` exits 0 (REVIEWS LOW #10)
    - `grep -q "test_modify_only_new_stop_resets_pyramid_level" tests/test_web_trades.py` exits 0 (REVIEWS LOW #9)
    - `grep -q "test_close_form_partial_returns_single_tr_only" tests/test_web_trades.py` exits 0 (REVIEWS HIGH #3)
    - `grep -q "test_cancel_row_restores_position_tr_from_state" tests/test_web_trades.py` exits 0
    - `grep -q "test_close_success_returns_empty_with_hx_trigger" tests/test_web_trades.py` exits 0 (REVIEWS HIGH #2)
    - `pytest tests/test_web_trades.py -x -q` exits 0 — all populated tests + new revision tests green
  </acceptance_criteria>
  <done>
    - All three POST handlers use `state_manager.mutate_state` to hold the lock across load → mutate → save (REVIEWS HIGH #1)
    - `_OpenConflict` sentinel exception lets in-mutator 409 detection short-circuit cleanly while still releasing the lock
    - ATR lookup uses `state['signals'][instrument]['last_scalars']['atr']` (REVIEWS MEDIUM #7) — verified against main.py:1225
    - close-success returns empty body + HX-Trigger event header (REVIEWS HIGH #2) — no invalid <div>-as-tbody-child
    - close-form / modify-form / cancel topology targets per-instrument tbody (`#position-group-{instrument}`) with `hx-swap="innerHTML"` (REVIEWS HIGH #3) — no orphan rows
    - modify_trade resets `pyramid_level` on ANY successful modify (REVIEWS LOW #9 / D-10)
    - TestSoleWriterInvariant covers `ast.AugAssign` (REVIEWS LOW #8)
    - TestRequestValidationErrorRemap regression confirms other routes unaffected (REVIEWS LOW #10)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5 (REVISION HIGH #4): web/routes/dashboard.py substitutes {{WEB_AUTH_SECRET}} placeholder + supports ?fragment=position-group-X</name>
  <files>web/routes/dashboard.py, tests/test_web_dashboard.py</files>
  <read_first>
    - web/routes/dashboard.py current GET / handler (Phase 13)
    - dashboard.py post-Plan-14-05 (which emits the literal `{{WEB_AUTH_SECRET}}` placeholder in hx-headers)
    - 14-CONTEXT.md Claude's Discretion §Auth header (REVIEWS HIGH #4 placeholder discipline)
    - 14-REVIEWS.md HIGH #4
    - tests/test_web_dashboard.py (existing tests for the GET / handler)
  </read_first>
  <action>
**REVISION 5.A — placeholder substitution at request time (REVIEWS HIGH #4).**

Modify the GET / handler in `web/routes/dashboard.py` to read the rendered `dashboard.html` from disk, substitute the literal `{{WEB_AUTH_SECRET}}` placeholder with the actual `WEB_AUTH_SECRET` env value, and return the resulting bytes. Disk file NEVER carries the real secret.

    @app.get('/')
    def serve_dashboard(request: Request, fragment: str | None = None):
      '''Phase 13 GET / + Phase 14 REVIEWS HIGH #4 (placeholder substitution).

      The on-disk dashboard.html (rendered by main.run_daily_check) emits
      `hx-headers='{"X-Trading-Signals-Auth": "{{WEB_AUTH_SECRET}}"}'` with
      a literal placeholder. This handler reads the bytes, substitutes the
      placeholder with the actual env-var value at request time, and returns
      the patched bytes. The disk file never contains the real secret.

      ?fragment=position-group-{instrument}: returns ONLY that tbody's HTML
      (the per-row HTMX listener uses this for partial refresh on
      positions-changed events).
      '''
      from pathlib import Path
      import os, re
      content = Path('dashboard.html').read_bytes()
      secret = os.environ.get('WEB_AUTH_SECRET', '').encode('utf-8')
      content = content.replace(b'{{WEB_AUTH_SECRET}}', secret)
      if fragment is not None:
        # Extract the tbody whose id matches `fragment`. Returns inner HTML only.
        # Quick-and-dirty regex (string-search) — dashboard markup is server-controlled,
        # not user input, so regex against rendered HTML is safe.
        m = re.search(
          rb'<tbody id="' + re.escape(fragment.encode('utf-8')) + rb'">(.*?)</tbody>',
          content, re.DOTALL,
        )
        if not m:
          return Response(content=b'', status_code=404, media_type='text/html; charset=utf-8')
        return Response(content=m.group(1), media_type='text/html; charset=utf-8')
      return Response(content=content, media_type='text/html; charset=utf-8')

**REVISION 5.B — tests in tests/test_web_dashboard.py.**

Add three tests:

    class TestAuthSecretPlaceholderSubstitution:
      '''REVIEWS HIGH #4: dashboard.html on disk emits literal placeholder;
      the GET / handler substitutes at request time.'''

      def test_dashboard_html_disk_does_not_contain_real_secret(self, tmp_path, monkeypatch):
        '''Disk file MUST contain the literal placeholder, NOT the real secret.'''
        from pathlib import Path
        # The file must exist and contain the placeholder (Plan 14-05 emits it).
        # Test reads the actual repo file, NOT a tmp_path render.
        disk_html = Path('dashboard.html').read_text() if Path('dashboard.html').exists() else ''
        if not disk_html:
          pytest.skip('dashboard.html not present in repo (rendered by daily run)')
        secret = 'a' * 32  # the test secret value from conftest
        assert '{{WEB_AUTH_SECRET}}' in disk_html, (
          'Plan 14-05 must emit literal {{WEB_AUTH_SECRET}} placeholder'
        )
        assert secret not in disk_html, (
          'REVIEWS HIGH #4: real WEB_AUTH_SECRET must NOT leak into disk file'
        )

      def test_get_root_response_substitutes_placeholder(self, client_with_state_v3, auth_headers):
        '''The TestClient response body MUST contain the real secret in hx-headers.'''
        client, _, _ = client_with_state_v3
        r = client.get('/', headers=auth_headers)
        assert r.status_code == 200
        assert 'a' * 32 in r.text, 'Real WEB_AUTH_SECRET must be substituted into response'

      def test_get_root_response_does_not_leak_placeholder(self, client_with_state_v3, auth_headers):
        '''The literal {{WEB_AUTH_SECRET}} string MUST NOT appear in the response.'''
        client, _, _ = client_with_state_v3
        r = client.get('/', headers=auth_headers)
        assert '{{WEB_AUTH_SECRET}}' not in r.text, (
          'REVIEWS HIGH #4: placeholder must be substituted, not leaked'
        )

      def test_get_root_with_fragment_returns_tbody_inner(self, client_with_state_v3, auth_headers):
        '''?fragment=position-group-SPI200 returns ONLY that tbody's inner HTML.'''
        client, _, _ = client_with_state_v3
        r = client.get('/?fragment=position-group-SPI200', headers=auth_headers)
        # 200 OR 404 if the rendered HTML doesn't include that tbody yet
        assert r.status_code in (200, 404)
        if r.status_code == 200:
          # Body must NOT contain a full <html>; it's a tbody inner partial
          assert '<html' not in r.text.lower()
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && grep -q "WEB_AUTH_SECRET" web/routes/dashboard.py && grep -q "fragment" web/routes/dashboard.py && pytest tests/test_web_dashboard.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "{{WEB_AUTH_SECRET}}" web/routes/dashboard.py` exits 0 (substitution target literal present in code)
    - `grep -q "content.replace" web/routes/dashboard.py` exits 0
    - `grep -q "fragment" web/routes/dashboard.py` exits 0 (?fragment= query param supported)
    - `grep -q "REVIEWS HIGH #4" web/routes/dashboard.py` exits 0 (audit trail)
    - `grep -q "TestAuthSecretPlaceholderSubstitution" tests/test_web_dashboard.py` exits 0
    - `grep -q "test_dashboard_html_disk_does_not_contain_real_secret" tests/test_web_dashboard.py` exits 0
    - `grep -q "test_get_root_response_substitutes_placeholder" tests/test_web_dashboard.py` exits 0
    - `grep -q "test_get_root_response_does_not_leak_placeholder" tests/test_web_dashboard.py` exits 0
    - `pytest tests/test_web_dashboard.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    - web/routes/dashboard.py reads dashboard.html, substitutes `{{WEB_AUTH_SECRET}}` with the env value, returns patched bytes
    - ?fragment=position-group-{instrument} query param returns ONLY the inner tbody HTML for HX-Trigger refreshes
    - Tests prove: disk file contains placeholder (not real secret); response body contains real secret (not placeholder); fragment query returns tbody inner only
    - REVIEWS HIGH #4 fix verified end-to-end
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser ↔ FastAPI app | HTTPS-terminated at nginx (Phase 12); X-Trading-Signals-Auth header required (Phase 13 AuthMiddleware); Pydantic v2 schema validation at request body parse (Phase 14 D-04) |
| FastAPI handler ↔ state.json | Plan 14-04 introduces this writer. fcntl.LOCK_EX coordination (Plan 14-02) handles cross-process safety. |
| FastAPI handler ↔ sizing_engine.check_pyramid | Pure-math hex; takes the operator's entry_price as input. NaN-passthrough invariant prevents downstream crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | T (Tampering) — input validation bypass | Pydantic v2 schema on request bodies | mitigate | Pydantic Literal['SPI200', 'AUDUSD'] for instrument; Literal['LONG', 'SHORT'] for direction; Field(gt=0) for entry/exit_price; Field(ge=1) for contracts; explicit math.isfinite check rejects NaN/±inf in custom validators; D-03 cross-field coherence checks (peak vs entry, etc.). 422 errors auto-remapped to 400 by global handler. ASVS V5 L1 satisfied. |
| T-14-06 | T (Tampering) — sole-writer invariant violation | web/routes/trades.py mutating state['warnings'] | mitigate | TestSoleWriterInvariant AST walk in tests/test_web_trades.py asserts no `state['warnings'] = ...` and no `.append/.extend/.insert` on it. Two assertions cover both subscript-assign and method-mutation patterns. Locks TRADE-06 invariant statically. |
| T-14-07 | T (Tampering) — mass-assignment attack | Pydantic accepts extra fields by default | mitigate | Pydantic v2 BaseModel.model_config defaults to `extra='ignore'` (extra fields silently dropped). The three request models declare ALL allowed fields explicitly with Literal types; an attacker submitting extra fields like `account_override: 9999999.0` sees those silently ignored — no mass-assignment to state. Backed by FastAPI/Pydantic default semantics; no extra config needed. |
| T-14-08 | T (Tampering) — pyramid-up race within a single process | Two simultaneous /trades/open requests on the same instrument | accept | uvicorn workers=1 (Phase 11 D-11) means no in-process race; FastAPI handlers run on a single thread. **Cross-process race (web vs daily signal loop) — REVIEWS HIGH #1 fix:** Plan 14-02 + this plan migrate to state_manager.mutate_state which holds fcntl.LOCK_EX across the FULL load → mutate → save critical section. T-14-01 lost-update race is now FULLY MITIGATED (was: ACCEPTED RESIDUAL). |
| T-14-15 | I (Information disclosure) — auth secret leaked via on-disk dashboard.html | dashboard.html cache file readable on droplet filesystem | mitigate (REVIEWS HIGH #4 fix) | dashboard.html on disk emits literal {{WEB_AUTH_SECRET}} placeholder; web/routes/dashboard.py GET / handler substitutes the real secret at request time. Disk file never carries the real value. Tests in tests/test_web_dashboard.py::TestAuthSecretPlaceholderSubstitution lock the discipline (test_dashboard_html_disk_does_not_contain_real_secret + test_get_root_response_substitutes_placeholder + test_get_root_response_does_not_leak_placeholder). |
| T-14-09 | I (Information disclosure) — verbose Pydantic error responses | _format_pydantic_errors leaking implementation details | accept | The error JSON contains `field` (the request body field name — operator-known) and `reason` (Pydantic's standard error message like "Input should be greater than 0"). No internal types, no stack traces, no env values. The error path's `_format_pydantic_errors` extracts only the user-facing leaf field and the `msg` string. ASVS V7.4 (error responses don't leak details) satisfied. |
| T-14-10 | I (Information disclosure) — auth header leakage in logs | logger.info / logger.warning calls in handlers | mitigate | Phase 11/13 [Web] log lines include endpoint path + non-sensitive context (instrument, gross_pnl, etc.). NEVER log the request body verbatim (would expose entry_price as an operator's broker fill — non-secret but private). NEVER log the X-Trading-Signals-Auth header value. Phase 13 AuthMiddleware already logs auth FAILURES with truncated UA + source IP only. Plan 14-04 follows the precedent. |
| T-14-11 | E (Elevation of privilege) — sizing_engine.check_pyramid called with attacker-controlled atr | atr from state.signals[instrument].atr — operator-controlled (signal loop output) | mitigate | atr originates from the daily signal loop (main.run_daily_check writing state.json). It cannot be set via the web API surface. If an attacker has write access to state.signals, they have already breached the auth gate AND the fcntl lock; the threat surface is "compromised droplet" which is out-of-scope per Phase 13/12 trust model. Defensive: check_pyramid's NaN-pass-through (B-1) returns add_contracts=0 on NaN atr, preventing infinite pyramid loops. |
| T-14-12 | D (Denial of service) — request body size | Pydantic parses unbounded JSON | accept | FastAPI/Starlette has a default request body size limit (1 MB). Phase 14 request bodies are tiny (~200 bytes). No new mitigation needed; Phase 16 hardening can add explicit Content-Length checks if v1.2 introduces large-body endpoints. |

**ASVS L1 mapping:**
- V5 (Validation): Pydantic v2 schema + Literal enums + Field constraints + custom finite-value validators (T-14-05). 422→400 remap (TRADE-02). Tested by TestErrorResponses + TestOpenAdvancedFields.
- V7 (Error handling): 4xx returns JSON `{errors: [...]}`; 200 returns HTML partials; 500 surfaces real bugs (no try/except masking — D-14 trust state_manager recovery). No stack traces leaked. Tested by TestErrorResponses.
- V8 (Logging): Every successful mutation logged at INFO with [Web] prefix + non-sensitive context (instrument, gross_pnl, fields_modified). Phase 13 AuthMiddleware logs auth failures already. T-14-10.
- V13 (API): Auth gate inherited from Phase 13 AuthMiddleware (D-01 sole chokepoint); /trades/* automatically gated; tested by TestHTMXSupportEndpoints.test_form_endpoints_require_auth.
</threat_model>

<verification>
After all three tasks complete:
- `pytest tests/test_web_trades.py -x -q` exits 0
- `pytest tests/test_web_healthz.py tests/test_web_state.py tests/test_web_dashboard.py tests/test_web_app_factory.py tests/test_web_auth_middleware.py tests/test_web_trades.py -x -q` exits 0 (full web suite + Phase 14)
- `pytest tests/test_state_manager.py tests/test_sizing_engine.py -x -q` exits 0 (Wave 1 regression — no Plan 04 leak into v1.0 hex)
- `WEB_AUTH_SECRET=$(printf 'a%.0s' {1..32}) python -c "from web.app import create_app; app = create_app(); routes = sorted(r.path for r in app.routes if hasattr(r, 'path')); assert routes >= ['/'], routes"` exits 0
- `python -c "import ast; from pathlib import Path; src = Path('web/routes/trades.py').read_text(); tree = ast.parse(src); module_top = [n for n in tree.body if isinstance(n, (ast.Import, ast.ImportFrom))]; forbidden = {'state_manager', 'sizing_engine', 'system_params'}; bad = []; [bad.append(a.name) for n in module_top if isinstance(n, ast.Import) for a in n.names if a.name.split('.')[0] in forbidden]; [bad.append(n.module) for n in module_top if isinstance(n, ast.ImportFrom) and n.module and n.module.split('.')[0] in forbidden]; assert not bad, f'C-2 violation: module-top imports of {bad}'"` exits 0 (verifies LOCAL-import discipline)
- Full suite: `pytest -x -q` exits 0 (modulo documented test_main.py weekend-skip failures)
</verification>

<success_criteria>
- All 6 Phase 14 requirements (TRADE-01..06) covered by passing tests in tests/test_web_trades.py
- ROADMAP SC-1: POST /trades/open appends position + returns HTMX partial → TestOpenTradeEndpoint + TestHTMXResponses
- ROADMAP SC-2: 400 + field-level errors on invalid input → TestErrorResponses + TestOpenAdvancedFields
- ROADMAP SC-3: POST /trades/close P&L math matches inline formula; account updates per Phase 3 D-14 closing-half cost → TestCloseTradePnLMath
- ROADMAP SC-4: POST /trades/modify trail stop or contract count independently; 0 contracts → 400; non-finite stop → 400 → TestModifyTradeEndpoint
- ROADMAP SC-5: HTMX partial fragments (not full HTML pages) → TestHTMXResponses + TestHTMXSupportEndpoints
- ROADMAP SC-6: AST guard against state['warnings'] writes → TestSoleWriterInvariant
- D-01..D-13 every locked decision has at least one test
- ASVS L1 V5/V7/V13 surface covered (input validation, error handling, API auth gate)
- D-05 anti-pitfall locked at compile time (grep-check + import-absence test)
- TRADE-06 atomic-single-save discipline locked by TestSaveStateInvariant
- Hex boundary preserved: trades.py imports state_manager + sizing_engine + system_params LOCALLY inside handler bodies, never at module top
</success_criteria>

<output>
After completion, create `.planning/phases/14-trade-journal-mutation-endpoints/14-04-SUMMARY.md`. Required content:
- Files created/modified with line counts
- The six endpoint paths + Pydantic model names
- The 422→400 handler registration line
- TestSoleWriterInvariant evidence (AST walk found 0 violations)
- TestCloseTradePnLMath observed gross_pnl + net_pnl numbers (proves the inline formula matches)
- pytest summary line for tests/test_web_trades.py (X passed in Y.Zs)
- Any deviations from this plan with reasoning (Rule 1/2/3 logging)
</output>
