---
phase: 13-auth-read-endpoints
verified: 2026-04-25T00:00:00Z
status: human_needed
score: 5/5 must-haves verified (automated); 4 manual-only items pending operator droplet verification
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "nginx forwards X-Forwarded-For correctly to FastAPI on the live droplet"
    expected: "journalctl -u trading-signals-web -f shows ip=<real-client-ip> in [Web] auth failure log line when curl -H 'X-Forwarded-For: 1.2.3.4' https://signals.<domain>/ is issued (or the nginx-rewritten real client IP if nginx overrides XFF)"
    why_human: "Requires live nginx + uvicorn + curl. Automated XFF parse is unit-tested at the middleware layer (test_log_extracts_ip_from_xff_first_entry passes); the real nginx XFF wiring cannot be exercised inside TestClient. SC-5 droplet half."
  - test: "dashboard.html regenerates on stale state in real droplet operator workflow"
    expected: "On droplet: `touch state.json` then refresh https://signals.<domain>/ in a browser; dashboard shows fresh 'Rendered at' timestamp and updated content"
    why_human: "Requires live signal loop writing state.json atomically + browser refresh to observe. Bytes-equality assertion locked in test_stale_state_triggers_regen_and_serves_regenerated_bytes proves the in-memory invariant; live droplet+browser proves the full chain. SC-2 droplet half."
  - test: "401 WARN log line appears in journald with the locked format"
    expected: "On droplet: `curl https://signals.<domain>/api/state` (no header) followed by `journalctl -u trading-signals-web --since '5 min ago' | grep 'auth failure'` shows the locked format `[Web] auth failure: ip=<ip> ua=<repr-truncated-120> path=<path>`"
    why_human: "Requires live systemd journald + curl probe. Automated caplog tests cover format; systemd integration cannot be tested inside TestClient. SC-5 droplet half."
  - test: "WEB_AUTH_SECRET missing at startup → systemd surfaces RuntimeError in journald"
    expected: "On droplet: remove WEB_AUTH_SECRET from .env, `sudo systemctl restart trading-signals-web`, `journalctl -u trading-signals-web -n 20 --no-pager` shows the locked RuntimeError message ('WEB_AUTH_SECRET env var is missing or empty — refusing to start. Add WEB_AUTH_SECRET=<32+ chars> to /home/trader/trading-signals/.env'). Restore .env afterward."
    why_human: "Requires systemd Restart=on-failure loop + journald inspection. Automated test (TestSecretValidation::test_missing_secret_raises) proves create_app() raises; the systemd surfacing of the error is environmental. D-16 droplet half."
overrides: []
---

# Phase 13: Auth + Read Endpoints Verification Report

**Phase Goal:** Gate every non-healthz endpoint behind a shared-secret header, and expose the existing v1.0 dashboard (`GET /`) and state snapshot (`GET /api/state`) over HTTPS. After this phase the operator can securely browse the v1.0 dashboard from any device.
**Verified:** 2026-04-25
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth (ROADMAP SC-1..SC-5)   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | SC-1: `curl /` (no auth) returns 401 with body `unauthorized` (plain text, no header hint); wrong secret also 401; only correct WEB_AUTH_SECRET yields 200 | ✓ VERIFIED | Live TestClient probe returned `401 'unauthorized' text/plain; charset=utf-8` for missing-header case AND `401 'unauthorized'` for wrong-header case. Locked by `tests/test_web_auth_middleware.py::TestAuthRequired` (3 tests) + `TestUnauthorizedResponse` (4 tests including `test_body_does_not_leak_header_or_env_var_names` and `test_no_www_authenticate_header`). Source uses `hmac.compare_digest` (D-03), AST-guarded by `TestConstantTimeCompare` (2 tests). |
| 2   | SC-2: `GET /` with auth returns dashboard.html bytes with `text/html`; stale state triggers regen-before-serve (verified by `test_get_root_regenerates_on_stale_dashboard` — actual test name: `test_stale_state_triggers_regen_and_serves_regenerated_bytes`) | ✓ VERIFIED | Live probe confirmed: dashboard.html written with `<html>old</html>`, state.json mtime 60s ahead, `_track_render` writer overwrites with `<html>NEW</html>` — response body is `<html>NEW</html>` (regenerated bytes), not `<html>old</html>`. REVIEWS MEDIUM #3 bytes-equality lock at `tests/test_web_dashboard.py:164`. The PLAN test name `test_get_root_regenerates_on_stale_dashboard` referenced in ROADMAP is implemented as `test_stale_state_triggers_regen_and_serves_regenerated_bytes` per REVIEWS MEDIUM #3 (stronger assertion: regen-call-count AND bytes-equality). |
| 3   | SC-3: `GET /api/state` with auth returns full state.json with `application/json` and every top-level key present (`schema_version`, `account`, `last_run`, `positions`, `signals`, `trade_log`, `equity_history`, `warnings`, `contracts`) | ✓ VERIFIED | Live probe returned exactly the 9-key set: `['account', 'contracts', 'equity_history', 'last_run', 'positions', 'schema_version', 'signals', 'trade_log', 'warnings']`; `_resolved_contracts` stripped; Content-Type=application/json; Cache-Control=no-store; compact JSON. Locked verbatim in `tests/test_web_state.py::TestStateResponse::test_full_top_level_key_set_preserved_except_runtime_keys` (REVIEWS MEDIUM #2). |
| 4   | SC-4: `GET /healthz` works with NO auth header (still 200) — exempt from auth dependency | ✓ VERIFIED | Live probe returned `200 {'status': 'ok', 'last_run': '2026-04-23', 'stale': False}` for `/healthz` with no auth header. Locked by `tests/test_web_auth_middleware.py::TestExemption` — 4 tests: happy path (no header, wrong header) PLUS REVIEWS LOW #5 negative tests (`test_healthz_trailing_slash_is_NOT_exempt`, `test_healthz_uppercase_is_NOT_exempt`) proving the exemption is exact-match only. EXEMPT_PATHS = frozenset({'/healthz'}) at `web/middleware/auth.py:41`. |
| 5   | SC-5: Every 401 writes journald WARN log including source IP (XFF first entry) and truncated User-Agent (first 120 chars) | ✓ VERIFIED (automated half) / ⚠ HUMAN NEEDED (droplet half) | Live caplog showed `[Web] auth failure: ip=testclient ua='testclient' path=/` matching the locked D-05 format. Automated tests `tests/test_web_auth_middleware.py::TestAuditLog` (5 tests): WARN-level logged, XFF first-entry extracted (1.2.3.4 from `1.2.3.4, 10.0.0.1, 10.0.0.2`), client.host fallback when XFF absent, UA truncated to exactly 120 chars, control-char injection blocked via `%r` format. Real journald + real nginx XFF wiring requires droplet — manual-only per VALIDATION.md. |

**Score:** 5/5 truths verified (automated)

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `web/app.py` | Factory with secret validation + 3-route registration + AuthMiddleware-last + all-3 OpenAPI-disable kwargs | ✓ VERIFIED | 102 lines. `_read_auth_secret()` at L37-57 raises RuntimeError on missing/empty (D-16) or <32 chars (D-17). `FastAPI(...docs_url=None, redoc_url=None, openapi_url=None)` at L80-82. Three routes registered (healthz, dashboard, state). `add_middleware(AuthMiddleware, secret=secret)` at L94 — registered LAST per D-06. |
| `web/middleware/auth.py` | Full AuthMiddleware with hmac.compare_digest, EXEMPT_PATHS={'/healthz'}, 401 plain-text body, audit log shape | ✓ VERIFIED | 85 lines. EXEMPT_PATHS frozenset at L41 (D-02). `hmac.compare_digest` at L59 (D-03). 401 response with body `'unauthorized'`, media_type `'text/plain; charset=utf-8'` at L62-65 (D-04). `_log_failure` extracts XFF first entry, falls back to client.host, truncates UA to 120 chars (D-05). |
| `web/routes/dashboard.py` | GET / handler with mtime-staleness regen, never-crash, 503 first-run, FileResponse | ✓ VERIFIED | 109 lines. `_is_stale()` helper uses `os.stat(path).st_mtime_ns` strict greater-than (D-08), handles FileNotFoundError on both files. `get_dashboard()` does local imports of dashboard + state_manager (D-07 hex extension). Try/except wraps render_dashboard with `BLE001` annotation, logs WARN with locked shape `[Web] dashboard regen failed, serving stale: <ExcType>: <msg>` (D-10). 503 PlainTextResponse with body `'dashboard not ready'` when dashboard.html missing. FileResponse with `text/html; charset=utf-8` (D-07). |
| `web/routes/state.py` | GET /api/state with top-level _* strip, Cache-Control: no-store, compact JSON, no try/except | ✓ VERIFIED | 65 lines. Local import of `load_state` (D-14 trust-load — no try/except). Dict comprehension `{k: v for k, v in state.items() if not k.startswith('_')}` strips top-level `_*` keys (D-12 — nested keys preserved). `JSONResponse(content=clean, headers={'Cache-Control': 'no-store'})` (D-13). No `indent=` kwarg → compact JSON (D-15). |
| `tests/conftest.py` | Autouse WEB_AUTH_SECRET fixture + VALID_SECRET single source + AUTH_HEADER_NAME constant | ✓ VERIFIED | 66 lines. `_set_web_auth_secret_for_web_tests` autouse fixture at L35-53 setenv's WEB_AUTH_SECRET=`'a'*32` for any test in `test_web_*.py`. VALID_SECRET single source at L29 (REVIEWS LOW #6). AUTH_HEADER_NAME constant at L32. Helper fixtures `valid_secret` + `auth_headers`. |
| `tests/test_web_auth_middleware.py` | 19 methods across 6 classes (AuthRequired, AuthPasses, Exemption with REVIEWS LOW #5, UnauthorizedResponse, AuditLog, ConstantTimeCompare) | ✓ VERIFIED | 19 test methods present (verified via grep). REVIEWS LOW #5 negatives (`test_healthz_trailing_slash_is_NOT_exempt` at L148, `test_healthz_uppercase_is_NOT_exempt` at L165) lock exact-match exemption. AST guard `test_source_does_not_use_equality_for_secret_compare` walks Compare nodes for `==` against secret-related identifiers (D-03). |
| `tests/test_web_state.py` | 8 methods including REVIEWS MEDIUM #2 SC-3 key-set lock | ✓ VERIFIED | 8 methods. `test_full_top_level_key_set_preserved_except_runtime_keys` at L117 asserts the exact 9-key canonical set verbatim. Nested `_*` keys preserved test at L156. |
| `tests/test_web_dashboard.py` | 12 methods across 4 classes including REVIEWS MEDIUM #3 SC-2 bytes-equality lock | ✓ VERIFIED | 12 methods (4 TestDashboardResponse + 4 TestStaleness + 1 TestRenderFailure + 3 TestFirstRun). REVIEWS MEDIUM #3 lock at L164: `test_stale_state_triggers_regen_and_serves_regenerated_bytes` asserts BOTH bytes-equality (`r.text == '<html>regenerated</html>'`) AND `len(calls) == 1`. |
| `tests/test_web_app_factory.py` | 8 methods across TestSecretValidation + TestDocsDisabled | ✓ VERIFIED | 8 methods. D-16 missing/empty secret raises (2 tests), D-17 <32-char raises + 32-char accepted (2 tests). D-21 + D-22 docs/redoc/openapi.json all 404 with auth, openapi.json 401 without auth (4 tests — D-22 research extension proves middleware reaches /openapi.json BEFORE the 404 logic). |
| `SETUP-DROPLET.md` | New `## Configure auth secret (Phase 13 AUTH-01)` section with openssl + chmod 600 + systemctl restart | ✓ VERIFIED | Section at L48 (between unit-install and sudoers). openssl rand -hex 16 at L55, chmod 600 at L69, sudo systemctl restart trading-signals-web at L75. Rotation note at L90. |
| `tests/test_setup_droplet_doc.py` | 5 regression tests for the auth-secret section | ✓ VERIFIED | 5 tests at L44-68: `test_section_configure_auth_secret`, `test_auth_secret_section_has_openssl_command`, `test_auth_secret_section_has_chmod_600`, `test_auth_secret_section_has_systemctl_restart`, `test_auth_secret_min_length_documented`. |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `web/app.py::create_app()` | WEB_AUTH_SECRET env var | `_read_auth_secret()` helper raises RuntimeError on missing/short | ✓ WIRED | Confirmed at runtime: `WEB_AUTH_SECRET` env var read at L46, `secret = _read_auth_secret()` at L74 fires before FastAPI() construction. Live test: `RuntimeError: WEB_AUTH_SECRET env var is missing or empty` raised when env var deleted. |
| `web/app.py::create_app()` | `FastAPI(openapi_url=None, ...)` | kwargs at instantiation | ✓ WIRED | All three kwargs at L80-82 (docs_url=None, redoc_url=None, openapi_url=None). Live: `/openapi.json` returns 404 with auth (proves D-22 schema suppression). |
| `web/app.py::create_app()` | AuthMiddleware | `application.add_middleware(AuthMiddleware, secret=secret)` as LAST registration | ✓ WIRED | At L94, AFTER all 3 `register(application)` calls (healthz at L87, dashboard at L88, state at L89). Live: `app.user_middleware` shows exactly 1 middleware: `[AuthMiddleware]`. Phase 13 D-06 invariant: registered LAST = first to dispatch. |
| `tests/conftest.py::_set_web_auth_secret_for_web_tests` | WEB_AUTH_SECRET env var | autouse fixture; `monkeypatch.setenv` before create_app() for any test_web_*.py | ✓ WIRED | autouse=True at L35, file-path filter `'test_web_' in str(request.node.fspath)` at L52. Live: 64 tests pass across the 5 test_web_*.py files; the Phase 11 healthz tests' 11 direct `create_app()` invocations all see the env var set. |
| `web/middleware/auth.py::dispatch` | `/healthz` exemption | `if request.url.path in EXEMPT_PATHS: return await call_next(request)` | ✓ WIRED | At L55-56, exemption check is FIRST line of dispatch (before secret comparison). Live: `/healthz` returns 200 with no auth; `/healthz/` (trailing slash) and `/HEALTHZ` (uppercase) both require auth. |
| `web/middleware/auth.py::dispatch` | `hmac.compare_digest` | constant-time secret compare with bytes-typed operands | ✓ WIRED | At L59 — both operands UTF-8 encoded (`presented` at L58, `self._secret_bytes` at L51). AST guard test `test_source_does_not_use_equality_for_secret_compare` walks Compare nodes for `==` and finds zero violations (D-03). |
| `web/middleware/auth.py::_log_failure` | journald via Python logging | `logger.warning('[Web] auth failure: ip=%s ua=%r path=%s', client_ip, ua, path)` | ✓ WIRED (in-process) / ⚠ HUMAN NEEDED (real journald) | At L79-84 — exact format string locked. caplog tests verify shape; real systemd journald wiring requires droplet (manual-only per VALIDATION.md). |
| `web/routes/dashboard.py::get_dashboard` | `dashboard.render_dashboard` | local import + invocation when stale | ✓ WIRED | Local `from dashboard import render_dashboard` at L82, `render_dashboard(load_state())` at L87. Hex boundary preserved — module-top imports forbidden by `tests/test_web_healthz.py::TestWebHexBoundary::test_web_adapter_imports_are_local_not_module_top`. |
| `web/routes/dashboard.py::get_dashboard` | filesystem mtime via `_is_stale()` | `os.stat(...).st_mtime_ns` strict-greater | ✓ WIRED | `_is_stale()` at L57 uses `os.stat(_DASHBOARD_PATH).st_mtime_ns` and `os.stat(_STATE_PATH).st_mtime_ns`, returns `state_mtime > html_mtime` at L73. Handles FileNotFoundError on both. |
| `web/routes/dashboard.py::get_dashboard` | FileResponse('dashboard.html') | default path serving with media_type | ✓ WIRED | At L105-108: `FileResponse(_DASHBOARD_PATH, media_type='text/html; charset=utf-8')`. Regen completes BEFORE FileResponse construction (SC-2 invariant pinned by code comment at L102-104). |
| `web/routes/state.py::get_state` | `state_manager.load_state` | local import inside handler | ✓ WIRED | Local `from state_manager import load_state` at L51, `state = load_state()` at L53. No try/except (D-14 trust posture). |
| `web/routes/state.py::get_state` | underscore-key strip | dict comprehension | ✓ WIRED | `clean = {k: v for k, v in state.items() if not k.startswith('_')}` at L58. Live verification: `_resolved_contracts` stripped from response. |
| `web/routes/state.py::get_state` | JSONResponse with Cache-Control: no-store | headers kwarg | ✓ WIRED | `JSONResponse(content=clean, headers={'Cache-Control': 'no-store'})` at L60-62. Live: response Cache-Control header equals `no-store`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `web/routes/state.py::get_state` | `state` (then `clean`) | `state_manager.load_state()` (Phase 3 module — reads state.json from disk, recovers via reset_state on corrupt file) | Yes — load_state returns the full state.json contents (canonical 9-key set) | ✓ FLOWING — Live test: 9 canonical keys returned with non-empty payloads when state seeded |
| `web/routes/dashboard.py::get_dashboard` | `dashboard.html` bytes | `dashboard.render_dashboard(load_state())` writes file via Phase 5 atomic tempfile+replace, then `FileResponse(_DASHBOARD_PATH)` streams the file | Yes — render_dashboard generates real HTML from state | ✓ FLOWING — Live test: stale state triggers regen, served bytes equal regenerated bytes (SC-2 lock) |
| `web/middleware/auth.py::dispatch` | `client_ip`, `ua`, `path` | request.headers.get('x-forwarded-for') OR request.client.host; request.headers.get('user-agent'); request.url.path | Yes — Starlette populates these from the live HTTP request | ✓ FLOWING — Live caplog: `[Web] auth failure: ip=testclient ua='testclient' path=/` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Phase 13 web tests pass | `.venv/bin/python -m pytest tests/test_web_*.py -q` | 170 passed (Phase 11 + Phase 13 web suite + setup-droplet-doc + systemd-unit) | ✓ PASS |
| Auth + read endpoints subset passes | `.venv/bin/python -m pytest tests/test_web_auth_middleware.py tests/test_web_dashboard.py tests/test_web_state.py tests/test_web_app_factory.py tests/test_web_healthz.py -q` | 64 passed | ✓ PASS |
| Module imports cleanly with WEB_AUTH_SECRET set | `.venv/bin/python -c "import os; os.environ['WEB_AUTH_SECRET']='a'*32; from web.app import create_app; app = create_app()"` | succeeds; 3 routes registered (`/`, `/api/state`, `/healthz`); 1 middleware (`AuthMiddleware`) | ✓ PASS |
| SC-1 live: 401 plain-text on missing/wrong header, 200 with correct value on /healthz | TestClient probe documented above | `401 'unauthorized' text/plain; charset=utf-8` (no auth and wrong auth); 200 with correct auth | ✓ PASS |
| SC-2 live: stale state regenerates and serves regenerated bytes | TestClient probe with `<html>old</html>` seeded + state.json 60s ahead + `_track_render` writer | response body = `<html>NEW</html>`, status 200 | ✓ PASS |
| SC-3 live: /api/state full key set + Cache-Control + content-type | TestClient probe with seeded state | 9 canonical keys, Cache-Control: no-store, application/json, compact JSON | ✓ PASS |
| SC-4 live: /healthz exempt | TestClient probe with no auth header | 200 + status:'ok' | ✓ PASS |
| Full project suite | `.venv/bin/python -m pytest -q` | 945 passed, 16 failed | ⚠ 16 failures pre-existing in tests/test_main.py (date-sensitive weekend-skip tests), documented in `.planning/phases/13-auth-read-endpoints/deferred-items.md`, confirmed pre-existing on base commit `c7f5c76` by all three executor agents — out of scope for Phase 13 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| AUTH-01 | 13-01 (autouse fixture), 13-02 (factory + middleware code), 13-03 (middleware tests) | All non-/healthz endpoints require shared-secret header `X-Trading-Signals-Auth` (value from `WEB_AUTH_SECRET`) | ✓ SATISFIED | AuthMiddleware at `web/middleware/auth.py` enforces; `EXEMPT_PATHS = {'/healthz'}`; `hmac.compare_digest` for constant-time compare. Locked by `TestAuthRequired` (3 tests), `TestAuthPasses` (1 test), `TestConstantTimeCompare` (2 AST tests). |
| AUTH-02 | 13-03 (middleware tests + UI-SPEC docs) | Missing/wrong auth → 401 with plain-text `unauthorized` body, no leaked info, no hints | ✓ SATISFIED | `web/middleware/auth.py:62-65` returns body `'unauthorized'` with `text/plain; charset=utf-8`. Locked by `TestUnauthorizedResponse` (4 tests including `test_no_www_authenticate_header` and `test_body_does_not_leak_header_or_env_var_names`). |
| AUTH-03 | 13-03 (audit log tests) | Auth failures log at WARN with source IP and truncated user-agent to journald | ✓ SATISFIED (in-process) / ⚠ HUMAN NEEDED (real journald) | `_log_failure` at `web/middleware/auth.py:69-84` with locked shape `[Web] auth failure: ip=%s ua=%r path=%s`. Locked by `TestAuditLog` (5 tests: WARN level + XFF first-entry + client.host fallback + 120-char truncation + control-char escape). Real journald wiring is droplet-only (manual). |
| WEB-05 | 13-05 (dashboard route + tests) | `GET /` returns current `dashboard.html` content; refresh triggers regeneration if state changed | ✓ SATISFIED | `web/routes/dashboard.py::get_dashboard` with `_is_stale()` mtime check + `render_dashboard(load_state())` regen path + FileResponse. Locked by `TestDashboardResponse` (4) + `TestStaleness` (4 — including REVIEWS MEDIUM #3 bytes-equality SC-2 lock) + `TestRenderFailure` (1) + `TestFirstRun` (3). |
| WEB-06 | 13-04 (state route + tests) | `GET /api/state` returns full `state.json` as `application/json` | ✓ SATISFIED | `web/routes/state.py::get_state` with top-level `_*` strip + Cache-Control: no-store + compact JSON. Locked by `TestStateResponse` (8 tests including REVIEWS MEDIUM #2 SC-3 verbatim key-set lock). |

**No orphaned requirements.** REQUIREMENTS.md §Per-phase counts (line 139) maps Phase 13 = `AUTH-01, AUTH-02, AUTH-03, WEB-05, WEB-06` — 5 IDs, all 5 covered by plan frontmatter `requirements:` fields and verified above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | - | No TODO/FIXME/PLACEHOLDER comments in any Phase 13 production file | - | - |
| (none) | - | No empty handlers (`return null`, `=> {}`, etc.) — Plan 02 stubs were replaced in Plans 04 and 05 | - | - |
| (none) | - | No hardcoded empty data in production files (state.py uses real load_state(), dashboard.py uses real FileResponse) | - | - |

Scanned files: `web/app.py`, `web/middleware/auth.py`, `web/middleware/__init__.py`, `web/routes/dashboard.py`, `web/routes/state.py`, `tests/conftest.py`, `tests/test_web_auth_middleware.py`, `tests/test_web_dashboard.py`, `tests/test_web_state.py`, `tests/test_web_app_factory.py`, `SETUP-DROPLET.md`. Zero anti-patterns found.

The `# noqa: BLE001` annotation at `web/routes/dashboard.py:88` is intentional and documented (D-10 never-crash policy) — broad exception is required to keep the dashboard endpoint from crashing on render failures.

### Human Verification Required

4 items require live droplet verification — these are structurally unverifiable inside automated tests because they exercise the systemd + nginx + journald layer that does not exist inside `TestClient`. They are explicitly catalogued as Manual-Only in `.planning/phases/13-auth-read-endpoints/13-VALIDATION.md`.

#### 1. nginx X-Forwarded-For wiring (SC-5 droplet half)

**Test:** On the droplet, with the live nginx + uvicorn stack running, issue:
```
curl -H 'X-Forwarded-For: 1.2.3.4' https://signals.<owned-domain>/
sudo journalctl -u trading-signals-web --since '2 min ago' | grep 'auth failure'
```
**Expected:** the journald log line shows `ip=1.2.3.4` (or the nginx-detected real client IP if nginx rewrites XFF — verify nginx config does not strip / rewrite XFF unexpectedly).
**Why human:** Real nginx XFF wiring cannot be tested inside TestClient. Automated unit test (`test_log_extracts_ip_from_xff_first_entry`) exercises the middleware's parsing logic only.

#### 2. dashboard.html regenerates in real droplet operator workflow (SC-2 droplet half)

**Test:** On the droplet:
```
touch /home/trader/trading-signals/state.json
# Then refresh https://signals.<owned-domain>/ in browser on phone or laptop
```
**Expected:** dashboard renders with a fresh "Rendered at" timestamp. Browser receives updated content (not a cached previous render).
**Why human:** Requires live signal loop writing state.json + browser refresh against the hosted URL. Bytes-equality is locked in-process by `test_stale_state_triggers_regen_and_serves_regenerated_bytes`; the live-droplet half proves the end-to-end chain.

#### 3. 401 WARN log line surfaces in journald (SC-5 droplet half)

**Test:** On the droplet:
```
curl https://signals.<owned-domain>/api/state
journalctl -u trading-signals-web --since '5 min ago' | grep 'auth failure'
```
**Expected:** journald shows the locked format `[Web] auth failure: ip=<ip> ua=<repr-truncated-120> path=/api/state`.
**Why human:** Requires live systemd journald + curl probe over real network. caplog tests prove format; the systemd integration (logger → journald handler) is environmental.

#### 4. WEB_AUTH_SECRET missing → systemd surfaces RuntimeError in journald (D-16 droplet half)

**Test:** On the droplet (with care — restore .env afterward):
```
sudo cp /home/trader/trading-signals/.env /home/trader/trading-signals/.env.bak
sudo sed -i '/WEB_AUTH_SECRET/d' /home/trader/trading-signals/.env
sudo systemctl restart trading-signals-web
journalctl -u trading-signals-web -n 20 --no-pager
sudo cp /home/trader/trading-signals/.env.bak /home/trader/trading-signals/.env
sudo systemctl restart trading-signals-web
```
**Expected:** journald shows `RuntimeError: WEB_AUTH_SECRET env var is missing or empty — refusing to start. Add WEB_AUTH_SECRET=<32+ chars> to /home/trader/trading-signals/.env`.
**Why human:** Requires systemd Restart=on-failure loop + journald inspection. `TestSecretValidation::test_missing_secret_raises` proves create_app() raises in-process; the systemd surfacing of the error is environmental.

### Gaps Summary

**No automated gaps.** All 5 ROADMAP success criteria (SC-1..SC-5) are implemented in production code AND covered by automated tests. Live TestClient spot-checks confirm each SC behaves correctly in-process. All 5 plan-frontmatter requirement IDs (AUTH-01, AUTH-02, AUTH-03, WEB-05, WEB-06) are SATISFIED with implementation evidence and per-test traceability.

**4 manual-only items remain** — these are NOT gaps in the Phase 13 deliverable. They are structurally unverifiable in CI (systemd journald, real nginx XFF, real droplet boot/restart loop). They are explicitly catalogued as Manual-Only in `13-VALIDATION.md` from the discuss/research phase. These items belong to the operator's droplet-side acceptance run before the milestone is closed.

**Pre-existing test_main.py failures (16) are out of scope** for Phase 13. Confirmed pre-existing on base commit `c7f5c76` by all three executor agents (per `.planning/phases/13-auth-read-endpoints/deferred-items.md`); rooted in date-sensitive weekend-skip behavior in `main.py` rather than any Phase 13 change. Suggested follow-up: a `/gsd-debug` session or fold into Phase 16 hardening.

**Code review status:** `13-REVIEW.md` flagged the codebase as clean. `13-REVIEWS.md` cross-AI review identified 3 MEDIUM and 1 LOW finding — all addressed:
- REVIEWS HIGH (autouse fixture for direct create_app() calls) → fixed in Plan 13-01 (`tests/conftest.py:35-53`)
- REVIEWS MEDIUM #2 (SC-3 verbatim key-set lock) → fixed in Plan 13-04 (`tests/test_web_state.py::test_full_top_level_key_set_preserved_except_runtime_keys`)
- REVIEWS MEDIUM #3 (SC-2 bytes-equality lock) → fixed in Plan 13-05 (`tests/test_web_dashboard.py::test_stale_state_triggers_regen_and_serves_regenerated_bytes`)
- REVIEWS LOW #5 (D-02 exact-match negatives) → fixed in Plan 13-03 (`test_healthz_trailing_slash_is_NOT_exempt` + `test_healthz_uppercase_is_NOT_exempt`)
- REVIEWS LOW #6 (VALID_SECRET single source) → fixed in Plan 13-01 (`tests/conftest.py:29` is the sole module-top definition; test files inline mirrors with comments pointing back to conftest as the conceptual single source — this is a minor deviation auto-fixed during execution because pytest does not put `tests/` on sys.path automatically; documented in 13-04-SUMMARY.md and 13-05-SUMMARY.md as Rule 1 deviation)

---

_Verified: 2026-04-25_
_Verifier: Claude (gsd-verifier)_
