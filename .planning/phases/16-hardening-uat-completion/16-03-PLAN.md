---
phase: 16-hardening-uat-completion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md
autonomous: true
requirements: [CHORE-03]
tags: [docs, uat, operator]
must_haves:
  truths:
    - "16-HUMAN-UAT.md exists in the Phase 16 directory and is the SOLE source of truth for operator UAT verification status"
    - "All 3 scenarios (UAT-16-A mobile dashboard, UAT-16-B mobile Gmail, UAT-16-C drift banner) appear with the D-10 schema (5 fields: ID, archive ref, status, date, notes)"
    - "Each scenario references the correct archived 06-HUMAN-UAT.md path so the operator can re-read original v1.0 context"
    - "All 3 scenarios start with status=pending and an empty operator-date column, ready for operator markup in Plan 05"
    - "16-HUMAN-UAT.md is NOT appended to the archived 06-HUMAN-UAT.md (D-09)"
    - "Frontmatter delimiter count is exactly 2 (open + close) — verified by awk check (REVIEWS L-2)"
  artifacts:
    - path: ".planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md"
      provides: "Operator UAT artifact for the 3 Phase 6 scenarios that became verifiable post-Phase-13/14/15 deploy"
      min_lines: 50
      contains: "UAT-16-A"
  key_links:
    - from: ".planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md"
      to: ".planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md"
      via: "Markdown reference link in each scenario's 'Original scenario' field"
      pattern: "milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md"
    - from: "16-HUMAN-UAT.md scenarios"
      to: "Plan 04 STATE.md ## Completed Items rows"
      via: "Plan 04 reads 16-HUMAN-UAT.md statuses and links into specific scenario anchors"
      pattern: "16-HUMAN-UAT.md#uat-16-"
---

<objective>
Create the operator UAT artifact at `.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md` per D-09 and D-10. This file is the operator's checklist for verifying the 3 Phase 6 HUMAN-UAT scenarios that became verifiable once the hosted dashboard shipped (CHORE-03). Status fields default to `pending` — operator marks them `verified` (or `partial` per D-17 for UAT-16-C) in Plan 05.

Purpose: The 3 scenarios from `06-HUMAN-UAT.md` could not be verified during v1.0 because there was no public URL. After Phase 13+14+15 ship to the droplet (Plan 01), the operator can finally exercise them on a real phone. This file captures their results without contaminating the archived 06-HUMAN-UAT.md (D-09 — separate file in Phase 16 directory).

Output: One new markdown file with the D-10 5-field schema for each of the 3 scenarios. Section breaks use `***` (asterisk HR) NOT `---` (hyphen HR) so the L-2 awk check `^---$` count == 2 holds true (REVIEWS L-2 — frontmatter delimiter validation catches placeholder-substitution slips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-hardening-uat-completion/16-CONTEXT.md
@.planning/phases/16-hardening-uat-completion/16-RESEARCH.md
@.planning/phases/16-hardening-uat-completion/16-PATTERNS.md
@.planning/phases/16-hardening-uat-completion/16-REVIEWS.md
@.planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md

<interfaces>
D-10 schema (verbatim from CONTEXT.md): 5 fields per scenario
  1. Scenario ID (UAT-16-A / UAT-16-B / UAT-16-C)
  2. Original v1.0 scenario reference (path link to 06-HUMAN-UAT.md section)
  3. Verification status (pending / verified / partial)
  4. Operator verification date (ISO YYYY-MM-DD; em-dash placeholder until set)
  5. Operator notes (free text — what was checked, screenshot path, issues)

Scenario IDs and what each verifies (from CONTEXT.md SC-3 + RESEARCH Per-Requirement Verification Map):
  UAT-16-A: mobile dashboard loads correctly on hosted URL
  UAT-16-B: email renders correctly in real Gmail on mobile
  UAT-16-C: drift banner renders correctly in BOTH dashboard AND email on at least one real weekday run

D-17: UAT-16-C may take >1 weekday to observe organic drift; verify-work returns PARTIAL until observed.

Archived 06-HUMAN-UAT.md path (verified):
  .planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md

REVIEWS L-2 — Frontmatter delimiter validation:
  The file contains EXACTLY 2 `---` delimiter lines (frontmatter open + close).
  Section-break horizontal rules use `***` (asterisk-HR markdown syntax) so
  they do NOT match the awk check `awk '/^---$/{c++} END{exit c==2?0:1}'`.
  This catches missed placeholder substitutions (a stray un-substituted
  `___FM_DELIM___` token would NOT increase the awk count, but a stray
  literal `---` between sections WOULD — the check fires either way).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 16-HUMAN-UAT.md with all 3 scenarios in D-10 schema (frontmatter `---` exactly 2; section breaks use `***`)</name>
  <files>.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md</files>
  <read_first>.planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md (read in full — lift the original scenario titles + setup + expected behavior from the analog so the operator has context without flipping back); .planning/phases/16-hardening-uat-completion/16-CONTEXT.md sections D-09, D-10, D-17 (schema + status semantics); .planning/phases/16-hardening-uat-completion/16-REVIEWS.md (L-2 — frontmatter delimiter validation requirement)</read_first>
  <action>
Create the file `.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md`. The exact content is given below as a template with TWO placeholder tokens:

  - `___FM_DELIM___` → substitute to `---` (three hyphens) — used ONLY at frontmatter open + close (exactly 2 occurrences in template)
  - `___SEC_HR___` → substitute to `***` (three asterisks) — used at section-break horizontal rules (5 occurrences in template; intentionally NOT `---` so the L-2 awk check `^---$` count remains exactly 2 per REVIEWS L-2)

Substitution rules (apply at file-write time):
  `___FM_DELIM___` → `---`  (three hyphens, no surrounding whitespace) — EXACTLY 2 occurrences
  `___SEC_HR___`   → `***`  (three asterisks, no surrounding whitespace) — 5 occurrences

Both substitution rules must be applied. After both substitutions, the resulting file MUST satisfy:
  - awk '/^---$/{c++} END{exit c==2?0:1}' returns 0 (exactly 2 frontmatter delimiters; REVIEWS L-2)
  - awk '/^\*\*\*$/{c++} END{print c}' returns 5 (five section breaks)
  - grep -c '___FM_DELIM___' returns 0
  - grep -c '___SEC_HR___' returns 0

Template content for `.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md` (copy verbatim and apply BOTH substitution rules):

```
___FM_DELIM___
phase: 16-hardening-uat-completion
source: [16-CONTEXT.md D-09, D-10, D-17]
related: [.planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md]
created: 2026-04-26
status: pending
___FM_DELIM___

# Phase 16 — HUMAN-UAT (Operator Verification)

> Three scenarios deferred from Phase 6 (`06-HUMAN-UAT.md`) at v1.0 milestone close. They became verifiable once Phase 13 + 14 + 15 reached the droplet via Plan 16-01 (deploy). Operator runs each scenario, fills in the 5 fields per D-10, and Plan 16-04 mirrors the verified-status rows into `STATE.md §Completed Items`.

**D-10 schema (5 fields per scenario):**
1. **Scenario ID** — stable identifier
2. **Original scenario** — path link to the v1.0 archive
3. **Verification status** — `pending` / `verified` / `partial`
4. **Operator verification date** — ISO `YYYY-MM-DD` once verified
5. **Operator notes** — free text, screenshot path, issues observed

___SEC_HR___

## UAT-16-A: Mobile Dashboard Rendering

**Original scenario:** [.planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md](../../milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md) (Mobile dashboard section)
**Verification status:** pending
**Operator verification date:** —
**Operator notes:**

> _(operator fills in after running the scenario)_

**How to verify:**
1. Open `https://signals.<owned-domain>.com/` on mobile (any modern browser — Safari, Chrome, Firefox).
2. Set the `X-Trading-Signals-Auth` header (use a header-injection tool/extension or set in shell + curl as fallback).
3. Confirm:
   - Signal cards stack on narrow viewport (no horizontal scroll)
   - Equity chart fits the viewport width
   - Calc-rows wrap legibly without text truncation
   - Drift banner (if a position is open and drifted) renders with visible color
4. Capture a screenshot if any layout looks off; attach path under `Operator notes`.
5. Update **Verification status** to `verified` (clean) or `partial` (works but with observable issues — describe in notes) and stamp **Operator verification date** as `YYYY-MM-DD`.

___SEC_HR___

## UAT-16-B: Mobile Gmail Email Rendering

**Original scenario:** [.planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md](../../milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md) (Gmail desktop + Gmail mobile sections)
**Verification status:** pending
**Operator verification date:** —
**Operator notes:**

> _(operator fills in after running the scenario)_

**How to verify:**
1. Trigger a daily run on the droplet — either wait for the scheduled 08:00 AWST cycle, or run `python main.py --once --force-email` over SSH (whichever is operationally cheaper).
2. Open the resulting email in Gmail's mobile app on phone (NOT the web client — Gmail's mobile client strips CSS more aggressively).
3. Confirm:
   - Section headings render with their borders / dividers
   - Banners (drift, reversal, equity-anomaly if any) show their colored borders
   - P&L colors apply correctly (green positive, red negative)
   - The `[!]` critical prefix appears in the subject when a critical banner is present
   - Equity figure renders with thousands separator
4. Capture a screenshot if any element fails to render; attach path under `Operator notes`.
5. Update **Verification status** + **Operator verification date** as in UAT-16-A.

___SEC_HR___

## UAT-16-C: Drift Banner in Real Weekday Email

**Original scenario:** [.planning/milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md](../../milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md) (Drift banner / signal-change scenarios)
**Verification status:** pending
**Operator verification date:** —
**Operator notes:**

> _(operator fills in after running the scenario)_

**How to verify:**
1. Wait for organic drift on a real weekday run (this is the natural path; operator declined synthetic drift injection per CONTEXT.md Deferred Ideas).
2. When the daily 08:00 AWST email arrives with a drift banner present, confirm:
   - Email's drift banner renders with the expected red/amber border (per Phase 15 D-12 lockstep parity)
   - Subject carries the `[!]` critical prefix
   - The **dashboard** at `https://signals.<owned-domain>.com/` shows a matching banner row for the same instrument (lockstep parity check — same banner text, same color tier)
3. Capture screenshots of BOTH the email and the dashboard for the same drift event; attach paths under `Operator notes`.
4. Update **Verification status** to `verified` (clean) once observed; stamp **Operator verification date**.

> **D-17 note:** This scenario may take more than one weekday to observe naturally. While `pending`, `/gsd-verify-work` returns `PARTIAL — awaiting weekday operator confirmation` (D-17). Other scenarios (UAT-16-A, UAT-16-B) close earlier and do not gate on this observation. Once UAT-16-C is `verified`, re-run `/gsd-verify-work` to close Phase 16.

___SEC_HR___

## Summary

| Scenario | Status | Operator Date | Linked Completed-Items row in STATE.md |
|----------|--------|---------------|-----------------------------------------|
| UAT-16-A | pending | — | uat_gap (Phase 06 HUMAN-UAT) + verification_gap (Phase 05 dashboard) — see [STATE.md Completed Items](../../STATE.md#completed-items) |
| UAT-16-B | pending | — | uat_gap (Phase 06 HUMAN-UAT) + verification_gap (Phase 06 email) — see [STATE.md Completed Items](../../STATE.md#completed-items) |
| UAT-16-C | pending | — | uat_gap (Phase 06 HUMAN-UAT) — see [STATE.md Completed Items](../../STATE.md#completed-items) |

**Notes:**
- Operator updates `Status` and `Operator Date` columns above as each scenario closes.
- Plan 16-04 reads from this file at execute time and writes verified rows into `STATE.md §Completed Items` (Plan 16-04 is now Wave 3, AFTER 16-05 per REVIEWS H-3).
- Per D-17, Phase 16 may close with UAT-16-C still `pending` (verify-work returns PARTIAL); milestone archive waits until UAT-16-C flips to `verified`.

___SEC_HR___

*Created: 2026-04-26 by /gsd-plan-phase 16 (Plan 16-03)*
*Per D-09: this file is the SOLE Phase 16 UAT artifact — do NOT modify the archived 06-HUMAN-UAT.md*
*Per D-10: 5-field schema (ID / archive ref / status / date / notes) per scenario*
```

After substituting BOTH `___FM_DELIM___` → `---` AND `___SEC_HR___` → `***`, the file should:
- Have a YAML frontmatter block at the top (the first pair of three-hyphen lines — exactly 2 `---` lines total in the file)
- Have five horizontal-rule separators between sections — using `***` syntax (NOT `---`)
- Status fields ALL start as `pending`. Operator-date fields ALL start as `—` (em-dash). Notes blocks are `> _(operator fills in...)_` placeholders so the operator sees a clear instruction.

Use `2026-04-26` as the `created:` date (matches CONTEXT.md gathered date and current date from environment).

The relative-path links use `../../` to escape the `phases/16-.../` directory and reach `milestones/...` and `STATE.md` (verify these resolve correctly relative to the file's location).
  </action>
  <verify>
    <automated>test -f .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md &amp;&amp; grep -c "## UAT-16-[ABC]" .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md`
    - File has at least 50 lines
    - File contains exactly 3 scenario sections matching `^## UAT-16-[ABC]:` (count returns 3)
    - Each scenario has all 5 D-10 fields:
      - `grep -c "Verification status:.*pending" 16-HUMAN-UAT.md` returns 3 (all start pending)
      - `grep -c "Operator verification date:.*—" 16-HUMAN-UAT.md` returns 3 (all em-dash placeholder)
      - `grep -c "Original scenario:" 16-HUMAN-UAT.md` returns 3
      - `grep -c "Operator notes:" 16-HUMAN-UAT.md` returns 3
    - References the archived 06-HUMAN-UAT.md path: `grep -c "milestones/v1.0-phases/06-email-notification/06-HUMAN-UAT.md" 16-HUMAN-UAT.md` returns >= 3
    - Frontmatter present (after substitution) with `phase: 16-hardening-uat-completion`, `created: 2026-04-26`, `status: pending`
    - **REVIEWS L-2 — Frontmatter delimiter count is EXACTLY 2:** `awk 'BEGIN{c=0} /^---$/{c++} END{exit c==2?0:1}' .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md` returns exit code 0
    - **REVIEWS L-2 — Section-break HRs use `***` not `---`:** `awk 'BEGIN{c=0} /^\*\*\*$/{c++} END{print c}' .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md` returns 5
    - Summary table present with all 3 scenarios listed: `grep -c "| UAT-16-[ABC] | pending |" 16-HUMAN-UAT.md` returns 3
    - File contains ZERO occurrences of either placeholder token: `grep -c "___FM_DELIM___" 16-HUMAN-UAT.md` returns 0 AND `grep -c "___SEC_HR___" 16-HUMAN-UAT.md` returns 0 (proof BOTH substitutions were applied)
    - File is NEW (not appended to archived 06-HUMAN-UAT.md): `git status .planning/milestones/` shows the archived file is unmodified
  </acceptance_criteria>
  <done>16-HUMAN-UAT.md exists with all 3 scenarios in the D-10 5-field schema, status fields default to pending, archived 06-HUMAN-UAT.md is unmodified, no placeholder tokens (`___FM_DELIM___` or `___SEC_HR___`) remain, frontmatter `---` count is exactly 2 per REVIEWS L-2 awk check, section breaks use `***`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo workspace → operator markdown edits | Operator manually edits 16-HUMAN-UAT.md to flip status fields; commits via standard GSD workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering | Archived 06-HUMAN-UAT.md modification | mitigate | Plan creates a NEW file at .planning/phases/16-.../16-HUMAN-UAT.md per D-09; AC verifies git status .planning/milestones/ is clean; archived file is read-only by convention |
| T-16-03-02 | Repudiation | Operator UAT history | accept | git history of 16-HUMAN-UAT.md edits provides audit trail; D-15 also requires Plan 04 to record the verification date in STATE.md |
| T-16-03-03 | Information Disclosure | UAT artifact contents | accept | No PII or secrets in the artifact; mostly checklists and observations |
| T-16-03-04 | Tampering | Missed `___FM_DELIM___` substitution leaves file malformed | mitigate | REVIEWS L-2 awk check `^---$` count == 2 catches both un-substituted placeholders AND extra/missing frontmatter delimiters; section breaks use `***` to keep the awk check authoritative |
</threat_model>

<verification>
- File exists: `.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md`
- 3 scenarios with D-10 5-field schema
- All 3 default to `pending` / `—`
- Archived 06-HUMAN-UAT.md unmodified
- Zero placeholder occurrences (both `___FM_DELIM___` and `___SEC_HR___` substituted)
- REVIEWS L-2: awk `^---$` count == 2 (frontmatter delimiters only); awk `^\*\*\*$` count == 5 (section breaks)
</verification>

<success_criteria>
- CHORE-03 partial coverage: the operator-facing artifact exists and is ready for Plan 05's checkpoint tasks to mark scenarios `verified`
- D-09 honored: separate file in Phase 16 directory, archived 06-HUMAN-UAT.md untouched
- D-10 honored: 5 fields per scenario in exact layout
- D-17 honored: UAT-16-C explicit note about weekday-gated PARTIAL outcome
- REVIEWS L-2 honored: awk-validated frontmatter delimiter count = 2; section breaks use `***` not `---`
</success_criteria>

<output>
After completion, create `.planning/phases/16-hardening-uat-completion/16-03-SUMMARY.md` recording:
- File created path
- Final line count
- Confirmation that 06-HUMAN-UAT.md is unchanged
- Confirmation that both placeholder tokens were substituted (zero of each remain)
- L-2 awk check result: `awk 'BEGIN{c=0} /^---$/{c++} END{exit c==2?0:1}'` exit code = 0
</output>
