---
phase: 16-hardening-uat-completion
plan: 05
type: execute
wave: 3
depends_on: [16-01, 16-03]
files_modified:
  - .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md
autonomous: false
requirements: [CHORE-03]
tags: [uat, operator, checkpoint, gate]
must_haves:
  truths:
    - "Operator has run UAT-16-A and either marked it `verified` (clean) or `partial` (with documented issues) in 16-HUMAN-UAT.md"
    - "Operator has run UAT-16-B and either marked it `verified` or `partial` in 16-HUMAN-UAT.md"
    - "Operator has either run UAT-16-C and marked it `verified`, OR explicitly accepted `pending (weekday-gated)` per D-17 (verify-work returns PARTIAL until UAT-16-C closes)"
    - "Each verified scenario has the operator's date stamped in YYYY-MM-DD form and free-text notes filled in (per D-10 5-field schema)"
    - "Plan 04's STATE.md ## Completed Items rows can be finalized at /gsd-verify-work time by reading the now-populated 16-HUMAN-UAT.md statuses"
  artifacts:
    - path: ".planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md"
      provides: "Operator UAT verdict for each of the 3 scenarios (verified / partial / pending-with-rationale)"
      contains: "Verification status:"
  key_links:
    - from: "Plan 16-01 hosted droplet stack"
      to: "UAT-16-A and UAT-16-B execution (operator runs scenarios against the live URL + email pipeline)"
      via: "https://signals.<owned-domain>.com on phone + droplet's daily 08:00 AWST email"
      pattern: "signals\\.<owned-domain>"
    - from: "Operator markup in 16-HUMAN-UAT.md"
      to: "/gsd-verify-work 16 → STATE.md ## Completed Items finalization"
      via: "verify-work reads scenario statuses + dates and updates the Verified/Date columns in Plan 04's table"
      pattern: "verified.*\\d{4}-\\d{2}-\\d{2}"
---

<objective>
Operator gate plan: 3 human-verify checkpoints — one per UAT scenario from 16-HUMAN-UAT.md (UAT-16-A mobile dashboard, UAT-16-B mobile Gmail, UAT-16-C drift banner real weekday). Operator runs each scenario, observes the result, and updates the matching scenario's 5-field block in 16-HUMAN-UAT.md per D-10. Per D-17, UAT-16-C may stay `pending` past Plan 05 completion — `/gsd-verify-work` returns PARTIAL until it flips.

Purpose: This is the SC-3 portion of Phase 16. Plans 01-04 prepared the runway (deploy, F1 test, UAT artifact, STATE.md structure); Plan 05 is the operator's hands-on verification. This plan creates no new code or files — it provides three structured pause-points where the operator does the eyeballing only a human can do (mobile rendering, Gmail mobile rendering, real-day drift banner sighting).

Output: 16-HUMAN-UAT.md with each scenario's `Verification status` + `Operator verification date` + `Operator notes` populated (or UAT-16-C explicitly left `pending` with a documented rationale per D-17).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-hardening-uat-completion/16-CONTEXT.md
@.planning/phases/16-hardening-uat-completion/16-RESEARCH.md
@.planning/phases/16-hardening-uat-completion/16-VALIDATION.md
@.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md

<interfaces>
<!-- Each task is a checkpoint:human-verify per the GSD checkpoint contract. -->
<!-- Operator runs the scenario, then updates 16-HUMAN-UAT.md and resumes. -->

<!-- D-10 schema fields each operator action must populate per scenario: -->
<!--   1. Verification status: pending → verified (clean) | partial (with issues) | pending (only UAT-16-C, per D-17) -->
<!--   2. Operator verification date: ISO YYYY-MM-DD -->
<!--   3. Operator notes: what was checked, screenshot path if any, issues observed -->

<!-- D-17: UAT-16-C may take >1 weekday for organic drift to occur. -->
<!--   Operator may flip UAT-16-A and UAT-16-B to verified earlier; UAT-16-C stays pending. -->
<!--   verify-work then returns PARTIAL until UAT-16-C closes. -->

<!-- Operator-only assets (NOT writable by Claude): -->
<!--   - https://signals.<owned-domain>.com mobile browser session -->
<!--   - Gmail mobile app inbox -->
<!--   - Phone screenshot capability -->
<!--   - SSH access to droplet (for triggering the 08:00 AWST email out-of-band if needed for UAT-16-B) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT-16-A — Mobile dashboard rendering on hosted URL</name>
  <files>.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md (operator updates UAT-16-A's 5 D-10 fields)</files>
  <action>
[Operator-driven] Operator opens https://signals.<owned-domain>.com/ on phone with the X-Trading-Signals-Auth header, visually verifies layout (signal cards stack, equity chart fits, calc-rows wrap, drift banner color), captures a screenshot if anything is off, and updates the §UAT-16-A block in 16-HUMAN-UAT.md per D-10: status = `verified` or `partial`, date = today's ISO YYYY-MM-DD, notes = what was checked + screenshot path + any issues. Detailed step-by-step instructions and the resume-signal protocol are in the &lt;how-to-verify&gt; block below.
  </action>
  <verify>
    <automated>grep -A 2 "## UAT-16-A:" .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md | grep -E "Verification status:.*(verified|partial)"</automated>
  </verify>
  <what-built>Plan 01 deployed Phase 13/14/15 to the droplet. The hosted dashboard at `https://signals.<owned-domain>.com/` now reflects the v1.1 UI (auth, calc-rows, drift banners, HTMX trade forms). Operator must visually verify it renders correctly on a mobile viewport.</what-built>
  <how-to-verify>
1. On phone (any modern mobile browser — Safari iOS, Chrome Android, Firefox Android), open:
   ```
   https://signals.<owned-domain>.com/
   ```
   You will need to set the `X-Trading-Signals-Auth` header. Two practical options:
     - Use a browser extension that injects custom headers (e.g. `Header Injector` for Chrome/Firefox).
     - Or run `curl` from a terminal app on the phone:
       ```
       curl -H "X-Trading-Signals-Auth: $WEB_AUTH_SECRET" https://signals.<owned-domain>.com/ > /tmp/dash.html
       ```
       (less ergonomic for visual verification — prefer the browser+header-injector path).

2. Verify on the rendered page:
   - **Layout:** Signal cards stack vertically on narrow viewport (no horizontal scrolling). Equity chart fits screen width.
   - **Calc-row legibility:** Per-instrument trailing-stop / pyramid / forward-stop fields wrap legibly (no truncated values).
   - **Drift banner (if any position is drifted):** Renders with visible amber (drift) or red (reversal) border.
   - **HTMX trade forms (open / close / modify):** Form elements visible; tap targets >= 44pt; not overlapping with other content.
   - **Equity figure:** Renders with $ prefix and thousands separator.

3. Capture a screenshot of the dashboard. Note any layout issues (overflow, illegible text, broken banner colors).

4. Update `.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md §UAT-16-A`:
   - Set `Verification status:` to `verified` (clean) or `partial` (works but with documented issues — describe them in `Operator notes`).
   - Set `Operator verification date:` to today's ISO date `YYYY-MM-DD`.
   - Replace the `_(operator fills in...)_` placeholder under `Operator notes:` with:
     - What you checked
     - Screenshot path (if captured — recommended `~/screenshots/uat-16-a-YYYY-MM-DD.png` or similar)
     - Any issues observed (if `partial`)

5. Commit the 16-HUMAN-UAT.md update via standard GSD workflow (`/gsd-quick` or directly through this plan's executor agent if it remains in context).
  </how-to-verify>
  <resume-signal>Type "UAT-16-A verified: status=verified|partial, date=YYYY-MM-DD, notes=<short summary>" with the actual values, OR describe issues that prevent flipping the status (e.g. "auth header injection failed on iOS Safari — investigating").</resume-signal>
  <acceptance_criteria>
    - `grep -A 1 "## UAT-16-A:" 16-HUMAN-UAT.md | grep "Verification status:" | grep -E "(verified|partial)"` matches (NOT pending)
    - `grep -A 2 "## UAT-16-A:" 16-HUMAN-UAT.md | grep -E "Operator verification date:.*\d{4}-\d{2}-\d{2}"` matches (date stamped)
    - The Operator notes block under UAT-16-A is no longer the placeholder `_(operator fills in...)_`
    - If status is `partial`, the notes describe the specific issue
  </acceptance_criteria>
  <done>UAT-16-A scenario marked verified or partial in 16-HUMAN-UAT.md with date + notes; mobile dashboard rendering observed firsthand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT-16-B — Mobile Gmail email rendering</name>
  <files>.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md (operator updates UAT-16-B's 5 D-10 fields)</files>
  <action>
[Operator-driven] Operator triggers a daily run on the droplet (wait for 08:00 AWST cycle OR run `python main.py --once --force-email` over SSH), opens the resulting email in the Gmail MOBILE APP on phone (not the web client), visually verifies subject prefix + section headings + banner borders + P&amp;L colors + equity formatting, captures a screenshot, and updates §UAT-16-B in 16-HUMAN-UAT.md per D-10 (status, date, notes). Detailed step-by-step instructions are in the &lt;how-to-verify&gt; block below.
  </action>
  <verify>
    <automated>grep -A 2 "## UAT-16-B:" .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md | grep -E "Verification status:.*(verified|partial)"</automated>
  </verify>
  <what-built>Phase 13/14/15 deploy means the droplet's daily 08:00 AWST cron now sends emails from `signals@<verified-domain>` (per Phase 12 INFRA-01) with the v1.1 markup (Phase 8 critical-banner classifier + Phase 15 SENTINEL-03 drift surface). Operator must verify the email renders correctly in Gmail's mobile app — Gmail strips CSS more aggressively than its web client, so this can't be tested in pytest.</what-built>
  <how-to-verify>
1. Trigger a daily run on the droplet. Two paths:
   - **Wait** for the natural 08:00 AWST scheduled run (cheapest if you're not in a hurry).
   - **Or trigger out-of-band** by SSH'ing to the droplet and running:
     ```
     ssh trader@<droplet-ip>
     cd ~/trading-signals && .venv/bin/python main.py --once --force-email
     ```
     Note: this writes a real `[Sched]` log line to journald and sends a real email. Don't do this multiple times in close succession (Resend rate limits + email noise).

2. On phone, open Gmail's MOBILE APP (not the web client at gmail.com — the web client renders differently). Find the email from `signals@<verified-domain>` with the day's date in subject.

3. Verify:
   - **Subject prefix:** Subject contains the per-day emoji (📊 routine, 🔴 signal-changed, or starts with `[!]` if a critical banner is present like drift/reversal).
   - **Section headings:** Each section (positions, signals, equity, banners) has its visible divider/heading.
   - **Banners:** Any drift, reversal, or equity-anomaly banner shows its expected colored border/background.
   - **P&L colors:** Green for positive, red for negative. (Gmail mobile will respect inline `style="color: ..."` but may strip `class`-based colors — Phase 6+ rendering uses inline styles for this reason.)
   - **Equity figure:** Thousands separator and $ prefix render correctly.
   - **No broken images / no plain-text fallback fully replacing the HTML.**

4. Capture a screenshot of the email body in Gmail mobile.

5. Update `16-HUMAN-UAT.md §UAT-16-B`:
   - Set `Verification status:` to `verified` (clean) or `partial` (functional but with rendering issues — Gmail's CSS-stripping may surface here).
   - Set `Operator verification date:` to today's ISO date.
   - Replace the placeholder under `Operator notes:` with what you checked + screenshot path + any issues.

6. Commit the update.
  </how-to-verify>
  <resume-signal>Type "UAT-16-B verified: status=verified|partial, date=YYYY-MM-DD, notes=<short summary>" with actual values, OR describe issues (e.g. "email arrived but P&L color stripped by Gmail mobile — needs CSS rework").</resume-signal>
  <acceptance_criteria>
    - `grep -A 1 "## UAT-16-B:" 16-HUMAN-UAT.md | grep "Verification status:" | grep -E "(verified|partial)"` matches
    - `grep -A 2 "## UAT-16-B:" 16-HUMAN-UAT.md | grep -E "Operator verification date:.*\d{4}-\d{2}-\d{2}"` matches
    - Operator notes under UAT-16-B no longer has the placeholder text
  </acceptance_criteria>
  <done>UAT-16-B scenario marked verified or partial in 16-HUMAN-UAT.md with date + notes; mobile Gmail email rendering observed firsthand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT-16-C — Drift banner in real weekday email (D-17 weekday-gated)</name>
  <files>.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md (operator updates UAT-16-C's 5 D-10 fields)</files>
  <action>
[Operator-driven; weekday-gated per D-17] Operator waits for organic drift to occur on a real weekday run. When the morning email arrives WITH a drift banner present, within ~1 hour also opens the dashboard on phone and verifies lockstep parity (per Phase 15 D-12): same instrument, same banner color tier, matching banner text in BOTH email AND dashboard, and `[!]` critical prefix in subject. Operator captures paired screenshots (email + dashboard for the same drift event) and updates §UAT-16-C in 16-HUMAN-UAT.md per D-10. Per D-17, status may stay `pending` until organic drift occurs (verify-work returns PARTIAL); after 5+ weekdays without drift, operator may close as `partial` with a documented rationale. Detailed step-by-step instructions are in the &lt;how-to-verify&gt; block below.
  </action>
  <verify>
    <automated>grep -A 2 "## UAT-16-C:" .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md | grep -E "Verification status:.*(verified|partial|pending)"</automated>
  </verify>
  <what-built>Phase 15 added drift detection (SENTINEL-01/02/03) which surfaces banners on dashboard AND in email. UAT-16-C is the lockstep parity check (Phase 15 D-12): a single drift event must render in BOTH the dashboard banner AND the email banner with matching content. Operator must observe this in a real weekday cycle.</what-built>
  <how-to-verify>
1. **Wait for organic drift on a real weekday run.** Per CONTEXT.md §Deferred Ideas, operator declined synthetic drift injection — natural occurrence is the path. Operator may have to wait several weekdays for drift to occur (e.g. position is open while signal flips to FLAT or opposite direction). Per D-17, this is acceptable; verify-work returns PARTIAL until UAT-16-C closes.

2. When the morning email arrives WITH a drift banner present (subject contains `[!]`, body contains `━━━ Drift detected ━━━`), within ~1 hour also open `https://signals.<owned-domain>.com/` on phone.

3. Verify lockstep parity:
   - **Email** banner present with red (reversal) or amber (drift) border + drift bullet text (e.g., `"You hold LONG SPI200 but today's signal is FLAT — consider closing"`).
   - **Dashboard** shows a matching banner row for the same instrument with the same color tier and the same banner text.
   - Subject carries the `[!]` critical prefix (proves Phase 15's `_has_critical_banner` extension to source='drift').

4. Capture screenshots of BOTH the email AND the dashboard for the same drift event (paired evidence).

5. Update `16-HUMAN-UAT.md §UAT-16-C`:
   - Set `Verification status:` to `verified` (organic drift observed; banners match).
   - Set `Operator verification date:` to the date the drift was observed.
   - Replace the placeholder under `Operator notes:` with: which instrument drifted, signal direction at the time, paired screenshot paths, any banner mismatch (would be `partial` if mismatch found).

6. Commit the update. If `verify-work 16` had previously returned PARTIAL because of UAT-16-C, re-run it now — it should close Phase 16 cleanly.

**If after 5+ weekdays no organic drift has occurred and operator wants to close Phase 16 anyway** (acceptable per D-17, but documented as a residual gap):
- Update `16-HUMAN-UAT.md §UAT-16-C` `Operator notes:` with: `5+ weekdays elapsed without organic drift — closed without observation; will re-test on first natural drift event in v1.2 cycle.`
- Set `Verification status:` to `partial` (NOT verified).
- This makes the residual gap explicit in the audit trail; milestone archive can proceed with this caveat.
  </how-to-verify>
  <resume-signal>Type "UAT-16-C verified: status=verified|partial, date=YYYY-MM-DD, notes=<drift event summary>" with actual values, OR (per D-17) "UAT-16-C still pending — no organic drift observed in N weekdays since deploy; verify-work returns PARTIAL".</resume-signal>
  <acceptance_criteria>
    - Either:
      - `grep -A 1 "## UAT-16-C:" 16-HUMAN-UAT.md | grep "Verification status:" | grep -E "(verified|partial)"` matches AND `grep -A 2 "## UAT-16-C:" 16-HUMAN-UAT.md | grep -E "Operator verification date:.*\d{4}-\d{2}-\d{2}"` matches AND notes describe the observed drift event (or the "no organic drift in N weekdays" rationale)
      - OR per D-17: status remains `pending`, operator explicitly invokes the PARTIAL outcome — verify-work for Phase 16 returns `PARTIAL — awaiting weekday operator confirmation` and Phase 16 stays open
    - In either case, the Operator notes block under UAT-16-C no longer has the placeholder text — it either documents the observation OR documents why observation is still pending
  </acceptance_criteria>
  <done>UAT-16-C either verified (organic drift observed with paired screenshots) OR explicitly left pending per D-17 with a documented rationale; lockstep parity (or its absence) recorded in operator notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's phone → hosted dashboard | HTTPS via Phase 12 nginx + Let's Encrypt; auth via Phase 13 shared-secret header |
| Operator's phone → Gmail mobile app | Email transit via Resend → Gmail SPF/DKIM verified via Phase 12 INFRA-01 |
| Operator's eyes → 16-HUMAN-UAT.md | Markdown edit committed via standard GSD workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Spoofing | Email looks legit but isn't from droplet | mitigate | Phase 12 INFRA-01 SPF/DKIM/DMARC; operator can verify via "show original" in Gmail mobile |
| T-16-05-02 | Information Disclosure | WEB_AUTH_SECRET in mobile header injector | accept | Operator-managed; secret never leaves operator's device; operator may opt to rotate post-UAT if concerned |
| T-16-05-03 | Repudiation | Did the operator actually verify? | mitigate | D-10 schema requires date stamp + free-text notes + screenshot path — provides forensic evidence in 16-HUMAN-UAT.md history |
| T-16-05-04 | Tampering | False-positive verification (operator says verified but it's broken) | mitigate | The `partial` status with documented issues is a valid outcome; the lockstep parity check in UAT-16-C surfaces dashboard↔email mismatches as `partial`; verify-work cross-references Plan 04's Completed Items rows |
| T-16-05-05 | Denial of Service | UAT-16-C waits indefinitely for organic drift | accept | D-17 explicitly allows PARTIAL outcome; operator escape hatch documented in Task 3 (close as `partial` after 5+ weekdays) |
</threat_model>

<verification>
After all 3 tasks resume:
- `grep -E "## UAT-16-[ABC]" -A 2 16-HUMAN-UAT.md | grep "Verification status:" | grep -v pending | wc -l` reports >= 2 (UAT-16-A and UAT-16-B closed; UAT-16-C may still be pending per D-17)
- All 3 scenarios have non-placeholder Operator notes
- `/gsd-verify-work 16` returns PASS if all 3 are verified, OR PARTIAL if only UAT-16-C is pending (per D-17)
</verification>

<success_criteria>
- CHORE-03 SC-3a: UAT-16-A verified (or partial) with date + notes — closes Phase 6 mobile dashboard verification gap
- CHORE-03 SC-3b: UAT-16-B verified (or partial) with date + notes — closes Phase 6 email rendering verification gap
- CHORE-03 SC-3c: UAT-16-C either verified (lockstep parity confirmed) OR explicitly left pending per D-17 with documented rationale
- D-10 5-field schema honored for all 3: status / date / notes populated (no placeholders remain)
- Plan 04's STATE.md ## Completed Items rows are now ready for /gsd-verify-work to flip from `pending`/`—` to `yes`/`YYYY-MM-DD`
</success_criteria>

<output>
After completion (or partial completion per D-17), create `.planning/phases/16-hardening-uat-completion/16-05-SUMMARY.md` recording:
- Final status of each scenario (verified / partial / pending-with-rationale for UAT-16-C)
- Date(s) of operator verification
- Screenshot paths captured (if any)
- Any rendering issues observed (especially if `partial` status was used)
- Recommendation to operator on whether to invoke /gsd-verify-work 16 immediately (all 3 verified) OR wait for organic drift first (UAT-16-C pending per D-17)
</output>
