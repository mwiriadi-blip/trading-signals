---
phase: 16-hardening-uat-completion
plan: 04
type: execute
wave: 3
depends_on: [16-05]
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [CHORE-03]
tags: [docs, state, cleanup]
must_haves:
  truths:
    - "STATE.md has a NEW ## Completed Items section ABOVE the existing ## Deferred Items section (D-14)"
    - "The 3 v1.0-deferred items mapped to Phase 16 (uat_gap Phase 06 HUMAN-UAT, verification_gap Phase 05 dashboard, verification_gap Phase 06 email) appear as rows in ## Completed Items"
    - "Each Completed row records REAL operator-verification dates read from 16-HUMAN-UAT.md (REVIEWS H-3 — NO `pending` placeholders ever land in STATE.md; migration runs AFTER operator confirmation in Plan 16-05)"
    - "If UAT-16-C is still `pending` at execute time per D-17, that single row is recorded as `partial` with the same fallback pattern OR deferred until UAT-16-C closes (operator decision; default `partial` so STATE.md stays consistent)"
    - "The 4th deferred item (quick_task 260421-723) STAYS in ## Deferred Items unchanged — it is NOT v1.1 scope (D-14)"
    - "The existing ## Deferred Items section continues to work; the table just loses 3 rows that moved to Completed"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "## Completed Items section with 3 migrated rows linked to 16-HUMAN-UAT.md scenarios; verification dates are REAL (read from 16-HUMAN-UAT.md operator markup) per REVIEWS H-3"
      contains: "## Completed Items"
  key_links:
    - from: "Plan 16-05 operator markup in 16-HUMAN-UAT.md"
      to: "STATE.md ## Completed Items rows (Verified column = `yes`, Date column = operator-stamped YYYY-MM-DD)"
      via: "This plan reads the operator-marked statuses + dates from 16-HUMAN-UAT.md and writes them into STATE.md (REVIEWS H-3 — Wave 3, after 16-05)"
      pattern: "16-HUMAN-UAT.md.*verified.*\\d{4}-\\d{2}-\\d{2}"
    - from: "STATE.md ## Completed Items rows"
      to: ".planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md scenario anchors"
      via: "Markdown reference link in Artifact column"
      pattern: "16-HUMAN-UAT.md#uat-16-"
    - from: "STATE.md ## Completed Items"
      to: "STATE.md ## Deferred Items"
      via: "Adjacency in the same file (Completed appears immediately above Deferred per D-14)"
      pattern: "## Completed Items.*## Deferred Items"
---

<objective>
Restructure `.planning/STATE.md` per D-14 and D-15: add a NEW `## Completed Items` section ABOVE the existing `## Deferred Items` section. Migrate the 3 v1.0-deferred items mapped to Phase 16 from Deferred to Completed, populating the rows with REAL operator-verification dates read from 16-HUMAN-UAT.md (which Plan 16-05 just populated).

Per REVIEWS H-3 (Path A), this plan now runs AFTER Plan 16-05 (Wave 3, not Wave 2) — semantically aligned with SC-4: STATE.md only gets `## Completed Items` rows AFTER the operator has actually verified the scenarios. NO `pending` / `—` placeholders ever land in STATE.md (the previous design did, which was the H-3 finding).

Purpose: SC-4 of Phase 16 requires STATE.md to no longer list the 3 Phase 6 HUMAN-UAT items + Phase 5 dashboard verification + Phase 6 email verification in Deferred Items — they must appear in a new Completed section with verification dates and artifact links. Per REVIEWS H-3, those dates come from the operator's actual markup in 16-HUMAN-UAT.md (Plan 16-05's output), not from a forward-prepared placeholder.

Output: STATE.md with the new section structure. The Verified column reads `yes` (or `partial` for UAT-16-C if D-17 weekday-gated PARTIAL outcome was used) and the Date column reads the operator's stamped ISO YYYY-MM-DD from 16-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-hardening-uat-completion/16-CONTEXT.md
@.planning/phases/16-hardening-uat-completion/16-PATTERNS.md
@.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md
@.planning/phases/16-hardening-uat-completion/16-REVIEWS.md

<interfaces>
<!-- Existing STATE.md ## Deferred Items table (lines ~211-222 — verified by reading STATE.md): -->

<!-- ## Deferred Items
<!--
<!-- Items acknowledged and deferred at v1.0 milestone close on 2026-04-24:
<!--
<!-- | Category | Item | Status | v1.1 disposition |
<!-- |----------|------|--------|------------------|
<!-- | quick_task | 260421-723-add-oracle-hash-comment-test-compute-ind | missing | Still deferred (not v1.1 scope) |
<!-- | uat_gap | Phase 06 HUMAN-UAT (3 pending scenarios — Gmail rendering verification) | partial | **Folded into v1.1 Phase 16 as CHORE-03** |
<!-- | verification_gap | Phase 05 VERIFICATION (dashboard HTML visual check) | human_needed | Becomes verifiable via hosted dashboard (v1.1 Phase 16 SC-4) |
<!-- | verification_gap | Phase 06 VERIFICATION (email rendering visual check) | human_needed | Becomes verifiable via Phase 12 Resend domain verification + Phase 16 operator run |

<!-- Three rows must MOVE OUT of this table into a new ## Completed Items section above it:
<!--   - row 2 (uat_gap, Phase 06 HUMAN-UAT)
<!--   - row 3 (verification_gap, Phase 05 dashboard)
<!--   - row 4 (verification_gap, Phase 06 email)

<!-- Row 1 (quick_task 260421-723) STAYS — D-14 explicitly says it's not v1.1 scope. -->

<!-- New ## Completed Items table schema (per PATTERNS analog at lines 484-495 + D-15 + REVIEWS H-3):
<!--
<!-- | Category | Item | Verified | Date | Artifact |
<!-- |----------|------|----------|------|----------|
<!--
<!-- Where:
<!--   - Category + Item: lifted verbatim from the original Deferred row
<!--   - Verified: 'yes' (NOT 'pending' — REVIEWS H-3) when operator stamped the scenario in 16-HUMAN-UAT.md as `verified`;
<!--               'partial' if operator stamped `partial` (UAT-16-C may use this per D-17)
<!--   - Date: ISO YYYY-MM-DD operator verification date READ FROM 16-HUMAN-UAT.md (NOT '—' — REVIEWS H-3)
<!--   - Artifact: path link to the closing 16-HUMAN-UAT.md scenario anchor (D-15) -->

<!-- Anchor convention for 16-HUMAN-UAT.md (auto-generated by markdown from h2 headings): -->
<!--   ## UAT-16-A: Mobile Dashboard Rendering -> #uat-16-a-mobile-dashboard-rendering
<!--   ## UAT-16-B: Mobile Gmail Email Rendering -> #uat-16-b-mobile-gmail-email-rendering
<!--   ## UAT-16-C: Drift Banner in Real Weekday Email -> #uat-16-c-drift-banner-in-real-weekday-email -->

<!-- Mapping per CONTEXT.md §D-14: -->
<!--   uat_gap Phase 06 HUMAN-UAT     -> closed by all 3 UAT-16-A/B/C (it WAS the 3 scenarios) -->
<!--   verification_gap Phase 05 dashboard -> closed by UAT-16-A (mobile dashboard rendering) -->
<!--   verification_gap Phase 06 email     -> closed by UAT-16-B (mobile Gmail email rendering) -->

<!-- REVIEWS H-3 sequencing rule (Path A): -->
<!--   This plan runs in WAVE 3, after Plan 16-05 (which is now Wave 2 per REVIEWS L-1). -->
<!--   By the time this plan executes, 16-HUMAN-UAT.md has been populated by the operator -->
<!--   with REAL `verified`/`partial` statuses + ISO dates. -->
<!--   This plan READS those values and writes them into STATE.md. -->
<!--   No `pending` placeholders are ever written into STATE.md. -->

<!-- D-17 fallback for UAT-16-C: -->
<!--   If UAT-16-C is still `pending` in 16-HUMAN-UAT.md at the time this plan runs -->
<!--   (operator chose to close UAT-16-A/B but wait for organic drift on UAT-16-C), -->
<!--   the row tied SPECIFICALLY to UAT-16-C is recorded as: -->
<!--     - Verified: `partial` -->
<!--     - Date: today's date (the migration date) with a note in operator-text -->
<!--   Note: UAT-16-C only directly maps to the `uat_gap Phase 06 HUMAN-UAT` row. -->
<!--   The verification_gap rows tie to UAT-16-A and UAT-16-B which close earlier. -->
<!--   So the `partial` fallback only applies to the uat_gap row IF UAT-16-C is still pending. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read operator-marked dates from 16-HUMAN-UAT.md, then insert ## Completed Items section above ## Deferred Items in STATE.md (REVIEWS H-3 — migration AFTER operator confirmation)</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md (read the post-Plan-16-05 file in full; extract the Verification status and Operator verification date from each of UAT-16-A, UAT-16-B, UAT-16-C — these are the values written into STATE.md per REVIEWS H-3); .planning/STATE.md (read the existing ## Deferred Items section in full — lines ~211-222 — to confirm exact row text BEFORE editing); .planning/phases/16-hardening-uat-completion/16-PATTERNS.md (lines 463-499 — analog pattern for the new Completed Items section); .planning/phases/16-hardening-uat-completion/16-REVIEWS.md (H-3 — sequencing fix Path A: migration runs after operator confirmation)</read_first>
  <action>
This task runs in Wave 3, AFTER Plan 16-05 has populated 16-HUMAN-UAT.md with operator markup (REVIEWS H-3 Path A).

**Step 1 — Read operator dates from 16-HUMAN-UAT.md.** Extract three values:
  - `UAT_A_STATUS` and `UAT_A_DATE` from the §UAT-16-A block (`Verification status:` and `Operator verification date:`)
  - `UAT_B_STATUS` and `UAT_B_DATE` from the §UAT-16-B block
  - `UAT_C_STATUS` and `UAT_C_DATE` from the §UAT-16-C block

Use a small bash one-liner OR direct read+grep:
```
A_STATUS=$(awk '/^## UAT-16-A:/,/^## UAT-16-B:/' .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md | grep '^\*\*Verification status:\*\*' | head -1 | sed 's/.*\*\*Verification status:\*\* *//')
A_DATE=$(awk '/^## UAT-16-A:/,/^## UAT-16-B:/' .planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md | grep '^\*\*Operator verification date:\*\*' | head -1 | sed 's/.*\*\*Operator verification date:\*\* *//')
# Repeat for B (delimiter ## UAT-16-B: → ## UAT-16-C:) and C (delimiter ## UAT-16-C: → ## Summary)
```

**Step 2 — Determine the row mapping** (per CONTEXT.md D-14 + interfaces above):
  - Row 1 (uat_gap Phase 06 HUMAN-UAT) — closed by ALL 3 scenarios. The Verified value depends on the worst-case status across UAT-16-A/B/C (verified+verified+verified → `yes`; any `partial` → `partial`; UAT-16-C still `pending` per D-17 → `partial` fallback per H-3 plan rule). The Date value is the LATEST of the three operator dates (most recent verification).
  - Row 2 (verification_gap Phase 05 dashboard) — closed by UAT-16-A only. Verified = `yes` if UAT-16-A is `verified`, else `partial`. Date = `UAT_A_DATE`.
  - Row 3 (verification_gap Phase 06 email) — closed by UAT-16-B only. Verified = `yes` if UAT-16-B is `verified`, else `partial`. Date = `UAT_B_DATE`.

**Step 3 — Validate prerequisites before edit:**
  - At least UAT-16-A and UAT-16-B MUST be `verified` or `partial` in 16-HUMAN-UAT.md (NOT `pending`). If either is still `pending`, STOP and surface to operator: "Plan 16-04 cannot run until UAT-16-A and UAT-16-B close in Plan 16-05."
  - UAT-16-C MAY be `pending` per D-17. If `pending`, set the uat_gap row's Verified value to `partial` and Date to today's ISO date with a note: `(uat-16-c pending; verification_gap rows for A and B closed earlier — see 16-HUMAN-UAT.md)`. This is the H-3 D-17 fallback.

**Step 4 — Edit STATE.md.** Two surgical changes:

**Change 1 — Insert the new ## Completed Items section IMMEDIATELY ABOVE the existing ## Deferred Items section (per D-14).**

The new section text (verbatim — substitute `{UAT_X_STATUS}` and `{UAT_X_DATE}` placeholders with the values read in Step 1; substitute `{ROW_1_VERIFIED}` and `{ROW_1_DATE}` with the values derived in Step 2):

```markdown
## Completed Items

Items deferred at v1.0 milestone close (2026-04-24) and verified closed via Phase 16 operator UAT (per D-14, D-15; migration runs after operator confirmation per REVIEWS H-3):

| Category | Item | Verified | Date | Artifact |
|----------|------|----------|------|----------|
| uat_gap | Phase 06 HUMAN-UAT (3 pending scenarios — Gmail rendering verification) | {ROW_1_VERIFIED} | {ROW_1_DATE} | [16-HUMAN-UAT.md §UAT-16-A/B/C](./phases/16-hardening-uat-completion/16-HUMAN-UAT.md) |
| verification_gap | Phase 05 VERIFICATION (dashboard HTML visual check) | {UAT_A_STATUS_AS_YES_OR_PARTIAL} | {UAT_A_DATE} | [16-HUMAN-UAT.md §UAT-16-A](./phases/16-hardening-uat-completion/16-HUMAN-UAT.md#uat-16-a-mobile-dashboard-rendering) |
| verification_gap | Phase 06 VERIFICATION (email rendering visual check) | {UAT_B_STATUS_AS_YES_OR_PARTIAL} | {UAT_B_DATE} | [16-HUMAN-UAT.md §UAT-16-B](./phases/16-hardening-uat-completion/16-HUMAN-UAT.md#uat-16-b-mobile-gmail-email-rendering) |

> **Verification source:** Each row's `Verified` and `Date` columns were read from `.planning/phases/16-hardening-uat-completion/16-HUMAN-UAT.md` after Plan 16-05 closed (REVIEWS H-3). The uat_gap row's Verified column is `yes` only if ALL THREE scenarios (UAT-16-A, UAT-16-B, UAT-16-C) are `verified`; otherwise it is `partial` and the operator notes in 16-HUMAN-UAT.md document why. Per D-17, UAT-16-C may stay `pending` for multiple weekdays — in that case the uat_gap row is recorded as `partial` with today's migration date and the verification_gap rows for UAT-16-A and UAT-16-B close as `yes` earlier.

```

The blank line at the end of the block above is intentional — it separates the new section from the existing `## Deferred Items` heading.

Where the substitution rules are:
  - `{UAT_A_STATUS_AS_YES_OR_PARTIAL}` = `yes` if UAT_A_STATUS == `verified`, else `partial`
  - `{UAT_A_DATE}` = the date from Step 1 verbatim (ISO YYYY-MM-DD)
  - Same for B
  - `{ROW_1_VERIFIED}` = `yes` if (UAT_A_STATUS, UAT_B_STATUS, UAT_C_STATUS) all == `verified`, else `partial`
  - `{ROW_1_DATE}` = MAX(UAT_A_DATE, UAT_B_DATE, UAT_C_DATE if UAT_C_STATUS != pending else today's date)

**Change 2 — Remove the 3 migrated rows from the existing ## Deferred Items table.**

Find the existing table:

```markdown
## Deferred Items

Items acknowledged and deferred at v1.0 milestone close on 2026-04-24:

| Category | Item | Status | v1.1 disposition |
|----------|------|--------|------------------|
| quick_task | 260421-723-add-oracle-hash-comment-test-compute-ind | missing | Still deferred (not v1.1 scope) |
| uat_gap | Phase 06 HUMAN-UAT (3 pending scenarios — Gmail rendering verification) | partial | **Folded into v1.1 Phase 16 as CHORE-03** |
| verification_gap | Phase 05 VERIFICATION (dashboard HTML visual check) | human_needed | Becomes verifiable via hosted dashboard (v1.1 Phase 16 SC-4) |
| verification_gap | Phase 06 VERIFICATION (email rendering visual check) | human_needed | Becomes verifiable via Phase 12 Resend domain verification + Phase 16 operator run |

These items require operator eyeballing (real Gmail inbox check, visual dashboard inspection) that cannot be automated in a GSD session. ...
```

Delete EXACTLY these 3 rows (the uat_gap row + both verification_gap rows). Leave the `quick_task 260421-723` row intact — D-14 says it stays in Deferred (not v1.1 scope).

ALSO update the prose paragraph below the table so it no longer claims those items "require operator eyeballing" — they HAVE been eyeballed by Phase 16. Replace the prose paragraph with:

```markdown
The remaining `quick_task` item is not v1.1 scope. The 3 Phase 6 HUMAN-UAT and verification-gap items moved to `## Completed Items` above (closed via Phase 16 operator UAT — see [16-HUMAN-UAT.md](./phases/16-hardening-uat-completion/16-HUMAN-UAT.md)).
```

**Resulting Deferred Items section after the change:**

```markdown
## Deferred Items

Items acknowledged and deferred at v1.0 milestone close on 2026-04-24:

| Category | Item | Status | v1.1 disposition |
|----------|------|--------|------------------|
| quick_task | 260421-723-add-oracle-hash-comment-test-compute-ind | missing | Still deferred (not v1.1 scope) |

The remaining `quick_task` item is not v1.1 scope. The 3 Phase 6 HUMAN-UAT and verification-gap items moved to `## Completed Items` above (closed via Phase 16 operator UAT — see [16-HUMAN-UAT.md](./phases/16-hardening-uat-completion/16-HUMAN-UAT.md)).
```

**Important do-NOTs:**
- Do NOT touch frontmatter (`gsd_state_version`, `milestone`, `progress`, etc.).
- Do NOT touch any other section (## Performance Metrics, ## Accumulated Context, ## Session Continuity, footer plan-completion entries, etc.).
- Do NOT bump `last_updated` or `last_activity` in frontmatter — `/gsd-verify-work` does that.
- Do NOT remove the existing per-table prose context lines that don't relate to the 3 migrated rows; only the prose paragraph that explicitly mentions "operator eyeballing" gets shortened as shown above.
- **Do NOT write `pending` or `—` placeholders into STATE.md** (REVIEWS H-3 — migration runs AFTER operator confirmation, so all rows have real values; only the D-17 fallback case writes `partial` for the uat_gap row, never `pending`).

Use the Edit tool with two precise old_string/new_string pairs (one for inserting the new section, one for editing the existing table+paragraph). DO NOT use Write tool on STATE.md — it would rewrite unrelated sections.
  </action>
  <verify>
    <automated>grep -c "^## Completed Items$" .planning/STATE.md && grep -c "^## Deferred Items$" .planning/STATE.md && grep -c "16-HUMAN-UAT.md" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - **REVIEWS H-3 — prerequisite read:** Plan ran AFTER Plan 16-05 (Wave 3 per H-3); 16-HUMAN-UAT.md was read at Step 1 and at least UAT-16-A and UAT-16-B were `verified` or `partial` (NOT pending) before STATE.md was edited
    - `grep -c "^## Completed Items$" .planning/STATE.md` returns 1
    - `grep -c "^## Deferred Items$" .planning/STATE.md` returns 1 (existing section preserved)
    - The Completed Items section appears BEFORE the Deferred Items section in file order: `awk '/^## Completed Items$/ {c=NR} /^## Deferred Items$/ {d=NR} END {print (c < d) ? "ok" : "FAIL"}' .planning/STATE.md` returns `ok`
    - Completed Items table has at least 3 data rows (excluding header + separator): `awk '/^## Completed Items$/,/^## Deferred Items$/' .planning/STATE.md | grep -c "^|" ` returns >= 5 (header + separator + 3 data rows)
    - Each migrated row appears once in Completed and zero times in Deferred:
      - `grep -c "Phase 06 HUMAN-UAT (3 pending scenarios — Gmail rendering verification)" .planning/STATE.md` returns 1 (Completed only)
      - `grep -c "Phase 05 VERIFICATION (dashboard HTML visual check)" .planning/STATE.md` returns 1
      - `grep -c "Phase 06 VERIFICATION (email rendering visual check)" .planning/STATE.md` returns 1
    - The quick_task row STAYS in Deferred: `grep -c "260421-723-add-oracle-hash-comment-test-compute-ind" .planning/STATE.md` returns 1 AND `awk '/^## Deferred Items$/,EOF' .planning/STATE.md | grep -c "260421-723"` returns 1
    - All 3 Completed rows link to 16-HUMAN-UAT.md: `awk '/^## Completed Items$/,/^## Deferred Items$/' .planning/STATE.md | grep -c "16-HUMAN-UAT.md"` returns >= 3
    - **REVIEWS H-3 — Verified column has REAL values, NOT `pending`:** `awk '/^## Completed Items$/,/^## Deferred Items$/' .planning/STATE.md | grep -c "| pending |"` returns 0 (NO pending placeholders; either `yes` or `partial`)
    - **REVIEWS H-3 — Date column has REAL ISO YYYY-MM-DD values, NOT `—`:** `awk '/^## Completed Items$/,/^## Deferred Items$/' .planning/STATE.md | grep -c "| — |"` returns 0 AND `awk '/^## Completed Items$/,/^## Deferred Items$/' .planning/STATE.md | grep -cE "[0-9]{4}-[0-9]{2}-[0-9]{2}"` returns >= 3
    - At least one row's Verified column is `yes` (the verification_gap rows for UAT-16-A and UAT-16-B should both be `yes` per typical happy path): `awk '/^## Completed Items$/,/^## Deferred Items$/' .planning/STATE.md | grep -cE "\\| yes \\|"` returns >= 2
    - No other section was modified: `git diff .planning/STATE.md | grep -c "^[+-][^+-]"` is bounded — only the inserted Completed section + the 3 deleted Deferred rows + the rewritten prose paragraph below the Deferred table appear in the diff
    - Frontmatter unchanged: `git diff .planning/STATE.md | grep -E "^[+-]gsd_state_version|^[+-]milestone:|^[+-]progress:" | wc -l` returns 0
  </acceptance_criteria>
  <done>STATE.md has the new ## Completed Items section above ## Deferred Items with the 3 migrated rows populated with REAL values (NOT `pending`/`—` placeholders) read from 16-HUMAN-UAT.md per REVIEWS H-3; the migrated rows are gone from Deferred; quick_task row remains in Deferred; no other section touched. UAT-16-C D-17 fallback applied to the uat_gap row if UAT-16-C is still pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo workspace → STATE.md edits | Direct markdown edit; integrity ensured by git history and pre-commit review |
| 16-HUMAN-UAT.md operator markup → STATE.md content | This plan reads operator-marked dates and writes them into STATE.md; trust anchor is the operator's commit to 16-HUMAN-UAT.md in Plan 16-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | Accidental edit to unrelated STATE.md sections | mitigate | Edit tool with precise old_string/new_string (NOT Write tool); AC includes `git diff` size bounds and frontmatter-unchanged check |
| T-16-04-02 | Repudiation | When did the migration happen | accept | git log of STATE.md provides audit trail; the rewritten prose paragraph names Phase 16 + 16-HUMAN-UAT.md as the closing artifact |
| T-16-04-03 | Information Disclosure | STATE.md content sensitivity | accept | STATE.md is committed to a public-or-controlled repo; no PII or secrets in the migrated rows |
| T-16-04-04 | Tampering | False-positive verification rows (writing `yes` when operator only confirmed `partial`) | mitigate | REVIEWS H-3: this plan READS operator markup from 16-HUMAN-UAT.md verbatim; no synthesis. Verified column is `yes` only when 16-HUMAN-UAT.md says `verified`; mapped to `partial` otherwise |
| T-16-04-05 | Repudiation | "Pending" rows landing in STATE.md mask actual completion state | mitigate | REVIEWS H-3: AC asserts ZERO `| pending |` and ZERO `| — |` substrings in Completed Items; only real values land |
</threat_model>

<verification>
- `grep "^## Completed Items$" .planning/STATE.md` matches once
- The 3 migrated rows appear in Completed and not in Deferred
- The quick_task row stays in Deferred
- All 3 Completed rows link to 16-HUMAN-UAT.md
- Verified column has only `yes` or `partial` values (NO `pending` per REVIEWS H-3)
- Date column has only ISO YYYY-MM-DD values (NO `—` per REVIEWS H-3)
- Frontmatter and unrelated sections unchanged (git diff scoped to the migration)
</verification>

<success_criteria>
- D-14 honored: ## Completed Items section appears above ## Deferred Items
- D-15 honored: each Completed row records original description + verification value + artifact link
- REVIEWS H-3 honored: migration runs in Wave 3 (after Plan 16-05); rows have REAL operator-stamped values; no `pending` / `—` placeholders ever land in STATE.md
- D-17 honored: UAT-16-C still pending → uat_gap row records `partial` with today's date (not `pending`)
- SC-4 satisfied (structural part): the 3 deferred items no longer appear in ## Deferred Items
- The 4th deferred item (quick_task) stays put
- /gsd-verify-work 16 sees the populated rows and closes Phase 16
</success_criteria>

<output>
After completion, create `.planning/phases/16-hardening-uat-completion/16-04-SUMMARY.md` recording:
- The exact line range of the inserted Completed Items section
- The 3 row contents that were deleted from Deferred Items
- The values read from 16-HUMAN-UAT.md (UAT-16-A/B/C status + date) and how they mapped to the 3 Completed rows
- Whether the D-17 fallback was triggered (i.e. UAT-16-C still `pending` at execute time)
- Confirmation that quick_task row + frontmatter + unrelated sections were untouched
- Confirmation per REVIEWS H-3: zero `| pending |` and zero `| — |` substrings present in the Completed Items section
</output>
