---
phase: 37
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - system_params.py
  - notifier/dispatch.py
  - notifier/transport.py
  - notifier/__init__.py
  - per_user_fanout.py
  - tests/test_per_user_fanout.py
autonomous: true
requirements:
  - UMAIL-01
  - UMAIL-02
  - UMAIL-03

must_haves:
  truths:
    - "per_user_fanout.run(state, run_date) is callable from main.py and returns a list of per-user outcome dicts"
    - "asyncio.Semaphore(FANOUT_SEMAPHORE_LIMIT) throttles concurrent Resend calls to 2"
    - "D-01: send_per_user_email produces a personal section (stop-loss alerts + paper P&L) AND a shared signal block for each F&F user"
    - "D-02: admin's existing daily email is unchanged; D-03: send_cycle_summary_email sends a SEPARATE end-of-cycle summary email to admin EVERY cycle (not only on failure)"
    - "send_per_user_email injects RFC 8058 List-Unsubscribe + List-Unsubscribe-Post headers"
    - "Per-user crash boundary: one user's exception does not abort the gather; outcome dict carries ok=False, reason='<Exception>'"
    - "D-14: per_user_fanout.py is a top-level I/O orchestrator peer of daily_run.py; Fan-out invokes exactly ONE mutate_state call (W3 #2 invariant); all per-user alert state batched in that call"
    - "Skipped users (email_enabled=False OR pause_until>=today) produce no Resend call"
  artifacts:
    - path: "per_user_fanout.py"
      provides: "Top-level orchestrator with run(state, run_date), _fan_out_all, _send_one"
      exports: ["run", "_fan_out_all", "_send_one"]
      min_lines: 80
    - path: "notifier/dispatch.py"
      provides: "send_per_user_email, send_invite_email, send_cycle_summary_email"
      contains: "def send_per_user_email"
    - path: "notifier/transport.py"
      provides: "_post_to_resend(..., email_headers: dict | None = None) — extended kwarg for RFC 8058 injection"
      contains: "email_headers"
    - path: "system_params.py"
      provides: "FANOUT_SEMAPHORE_LIMIT = 2"
      contains: "FANOUT_SEMAPHORE_LIMIT"
    - path: "notifier/__init__.py"
      provides: "Re-export send_per_user_email, send_invite_email, send_cycle_summary_email"
  key_links:
    - from: "per_user_fanout.py"
      to: "notifier.dispatch.send_per_user_email"
      via: "asyncio.to_thread inside Semaphore-guarded async function"
      pattern: "asyncio.to_thread\\(.*send_per_user_email"
    - from: "send_per_user_email"
      to: "_post_to_resend(email_headers={...})"
      via: "kwarg propagation"
      pattern: "List-Unsubscribe"
    - from: "per_user_fanout.run"
      to: "state_manager.mutate_state"
      via: "single terminal call after asyncio.run completes"
      pattern: "mutate_state\\(.*last_cycle"
---

<objective>
Build the orchestration spine of Phase 37: the top-level `per_user_fanout.py` module, three new notifier dispatch functions, the RFC 8058 header-injection hook on `_post_to_resend`, and `FANOUT_SEMAPHORE_LIMIT` in `system_params.py`. Replace `pytest.skip(...)` stubs in `tests/test_per_user_fanout.py` with real assertions for UMAIL-01/02/03.

Purpose: Per D-14, fan-out is a top-level orchestrator (peer of `daily_run.py`) called from `main.py` AFTER the daily cycle. This plan does NOT wire `main.py` — that happens in Plan 05 to keep the wiring change atomic with the admin/dashboard surface.

Output: A standalone, importable fan-out module callable by tests via `asyncio.run(per_user_fanout._fan_out_all(...))`; three new dispatch functions; a single new line in `system_params.py`; `tests/test_per_user_fanout.py` turns from all-skipped to all-green for UMAIL-01/02/03 classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-CONTEXT.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md
@notifier/dispatch.py
@notifier/transport.py
@notifier/__init__.py
@state_manager/__init__.py
@system_params.py

<interfaces>
From notifier/transport.py (existing):
  - `class SendStatus(NamedTuple): ok: bool; reason: str | None`
  - `class ResendError(Exception)`
  - `_post_to_resend(api_key, from_addr, to_addr, subject, html_body, text_body=None, timeout_s: int = HTTP_TIMEOUT_S)` — returns dict; raises ResendError on 4xx after retries exhausted
  - `_resolve_email_to_or_skip(state, *, context)` — returns recipient or None

From notifier/dispatch.py (existing — analog):
  - `send_daily_email(state, signals, run_date, ...)` — never-raise; returns SendStatus
  - `send_crash_email(exc_type, exc_value, tb_str)` — never-raise; returns SendStatus
  - `send_magic_link_email(to_addr, link_url)` — never-raise; returns SendStatus

From notifier/__init__.py (existing re-exports):
  - send_daily_email, send_crash_email, send_magic_link_email, send_stop_alert_email, send_backup_stale_email
  - Late-bind proxy pattern at dispatch.py lines 51-54 — new functions must mirror it

From state_manager (existing):
  - `mutate_state(mutator: Callable[[dict], None], path: Path | None = None) -> None` — flock-guarded atomic write
  - `load_user_state(uid: str, path: Path = Path(STATE_FILE)) -> dict` — returns per-user sub-dict
  - W3 invariant: exactly two mutate_state calls per cycle (daily_run = #1, per_user_fanout terminal = #2)

From auth_store (existing — used by per_user_fanout):
  - `list_users(path=None) -> list[User]` — returns User TypedDict rows (uid, email, role, disabled)

From system_params (existing):
  - `HTTP_TIMEOUT_S: int = 30`
  - `STATE_SCHEMA_VERSION: int = 12`

Constants this plan introduces:
  - `system_params.FANOUT_SEMAPHORE_LIMIT: int = 2` (UMAIL-03)

Module surface this plan creates (per_user_fanout.py):
  - `def run(state: dict, run_date: str) -> list[dict]` — sync entry point; calls asyncio.run; single terminal mutate_state
  - `async def _fan_out_all(users: list[dict], shared_signals: dict, run_date: str) -> list[dict]`
  - `async def _send_one(sem: asyncio.Semaphore, uid: str, user_row: dict, shared_signals: dict, run_date: str) -> dict`
  - Per-user outcome shape: `{"uid": str, "ok": bool, "reason": str | None}`

RFC 8058 header values (exact, from RESEARCH.md):
  - `List-Unsubscribe: <{BASE_URL}/settings>`
  - `List-Unsubscribe-Post: List-Unsubscribe=One-Click`  (literal string, exact casing)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FANOUT_SEMAPHORE_LIMIT + extend _post_to_resend with email_headers kwarg + add three never-raise dispatch functions</name>
  <files>system_params.py, notifier/transport.py, notifier/dispatch.py, notifier/__init__.py</files>
  <read_first>
    - notifier/dispatch.py (send_daily_email lines 77–180 — template for never-raise pattern; send_magic_link_email lines 286–360 — template for send_invite_email)
    - notifier/transport.py (_post_to_resend signature, payload construction, retry+backoff loop)
    - notifier/__init__.py (current __all__ list + late-bind proxy convention)
    - system_params.py (HTTP_TIMEOUT_S placement — add FANOUT_SEMAPHORE_LIMIT in the same nearby block)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §notifier/dispatch.py (verbatim template for the three new functions)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md §Pattern 3 (RFC 8058 exact header format)
    - CLAUDE.md (2-space indent, Decimal not relevant here)
  </read_first>
  <behavior>
    - Test 1: `system_params.FANOUT_SEMAPHORE_LIMIT == 2` (UMAIL-03 throttle)
    - Test 2: `_post_to_resend(api_key, from_addr, to_addr, subject, html_body, email_headers={'List-Unsubscribe': '<https://x/settings>', 'List-Unsubscribe-Post': 'List-Unsubscribe=One-Click'})` — payload sent to Resend contains a `headers` dict with both keys; default callers passing no `email_headers` produce a payload WITHOUT a `headers` key (no regression)
    - Test 3: `send_per_user_email(uid='u1', user_state={'email': 'a@x.com'}, shared_signals={}, run_date='2026-05-14')` with patched `_post_to_resend` returns `SendStatus(ok=True, reason=None)`; captured payload contains `headers['List-Unsubscribe-Post'] == 'List-Unsubscribe=One-Click'` and `headers['List-Unsubscribe']` matches `r'^<https?://.+/settings>$'`
    - Test 4: `send_per_user_email` with `user_state['email'] == ''` returns `SendStatus(ok=False, reason='missing_recipient')` — never raises
    - Test 5: `send_per_user_email` with `_post_to_resend` raising arbitrary `RuntimeError('boom')` returns `SendStatus(ok=False, reason=<starts with 'RuntimeError'>)` — never raises (BLE001 guard)
    - Test 6: `send_invite_email(to_email='a@x.com', invite_url='https://x/accept-invite?token=raw')` returns SendStatus; invite_url appears in the captured html_body; raw token is NOT logged (assert via caplog: no log record contains the substring `token=raw`)
    - Test 7: `send_cycle_summary_email(outcomes=[{'uid':'u1','ok':True,'reason':None},{'uid':'u2','ok':False,'reason':'KeyError: x'}], run_date='2026-05-14')` returns SendStatus; captured subject matches `r'^\[Cycle 2026-05-14\] 1/2 OK$'`; text_body contains both `u1` and `u2` substrings and the failure reason
    - Test 8: All three new functions appear in `notifier.__all__` (re-export surface)
  </behavior>
  <action>
    Step 1 — `system_params.py`: append a single new constant in the same block as `HTTP_TIMEOUT_S` (search for that name and place `FANOUT_SEMAPHORE_LIMIT` directly after with a one-line comment noting "Phase 37 UMAIL-03 — Resend rate-limit throttle for per-user fan-out (2 req/sec default; operator may bump to 5 for newer Resend accounts)". Value `2`, type annotation `int`.

    Step 2 — `notifier/transport.py`: extend `_post_to_resend` signature to add a single optional keyword-only parameter `email_headers: dict[str, str] | None = None` (place it AFTER the existing `timeout_s` kwarg). Inside the function, BEFORE the existing `payload = {...}` build, branch: if `email_headers` is provided AND non-empty, set `payload['headers'] = dict(email_headers)` after the base payload is constructed (mutate after build to keep diff small). Do NOT change the retry/backoff/4xx error path. Do NOT change the secret-redaction logic. All existing callers continue to work because the new kwarg defaults to None.

    Step 3 — `notifier/dispatch.py`: append THREE new public functions following the verbatim never-raise template in 37-PATTERNS.md §notifier/dispatch.py. Use the existing late-bind `_post_to_resend` proxy at lines 51-54 (do not import from .transport directly). Functions:
      (a) `send_per_user_email(uid: str, user_state: dict, shared_signals: dict, run_date: str) -> SendStatus` — reads SIGNALS_EMAIL_FROM + RESEND_API_KEY + BASE_URL from os.environ; reads `to_addr` from `user_state.get('email', '')`; constructs `extra_headers` dict with `List-Unsubscribe: <{base_url}/settings>` and `List-Unsubscribe-Post: List-Unsubscribe=One-Click` (literal); passes `email_headers=extra_headers` to `_post_to_resend`; never raises (try/except ResendError, try/except Exception with BLE001 noqa). Subject `f'Trading Signals — {run_date}'`. For the html_body, call a private helper `_render_per_user_email_html(uid, user_state, shared_signals, run_date)` that returns a minimal HTML string (placeholder content acceptable in this plan — just renders `'<p>uid={uid}</p>'` plus a stringified `shared_signals`; full templating can be expanded later without breaking the dispatch contract). The personal section + shared-signal-block content is data-shape stable here; richer rendering can replace `_render_per_user_email_html` body later.
      (b) `send_invite_email(to_email: str, invite_url: str) -> SendStatus` — mirror `send_magic_link_email`; subject `"You're invited to Trading Signals"`; html_body via `_render_invite_email_html(invite_url)` private helper (minimal HTML with a single `<a href="{invite_url}">Accept your invite</a>`); never logs the raw token (the URL is logged only as `[Email] invite sent to=<to_email>` — no URL/token in log message); returns `SendStatus(ok=False, reason='missing_base_url')` ONLY if `to_email` is empty — invite_url is passed in from the caller (admin route) and is trusted.
      (c) `send_cycle_summary_email(outcomes: list[dict], run_date: str) -> SendStatus` — admin-recipient via `_resolve_email_to_or_skip(state=None, context='send_cycle_summary_email')`; subject `f'[Cycle {run_date}] {ok_count}/{total} OK'`; text_body (no html_body) lists each failed uid with reason; never-raise.

    Step 4 — `notifier/__init__.py`: import the three new names from `.dispatch` and add them to `__all__` (preserving alphabetical or grouping convention already in the file). Re-export `_render_per_user_email_html` and `_render_invite_email_html` as private helpers ONLY if they are needed by tests; otherwise keep them module-private to `notifier/dispatch.py`.

    Step 5 — `tests/test_per_user_fanout.py`: replace `TestRFC8058Headers` stub with real tests for behaviors 2 and 3 above (monkeypatch `notifier.transport._post_to_resend` to capture kwargs). Keep other test classes as Wave 0 skips for now — Task 2 replaces them.

    Constraints: 2-space indent. Do NOT run `ruff format`. Do NOT change the `_post_to_resend` retry/backoff logic. Do NOT log raw invite tokens. Do NOT add new dependencies.
  </action>
  <acceptance_criteria>
    - `python -c "from system_params import FANOUT_SEMAPHORE_LIMIT; assert FANOUT_SEMAPHORE_LIMIT == 2"` exits 0
    - `python -c "from notifier import send_per_user_email, send_invite_email, send_cycle_summary_email; print('ok')"` prints `ok`
    - `python -c "from notifier.transport import _post_to_resend; import inspect; assert 'email_headers' in inspect.signature(_post_to_resend).parameters"` exits 0
    - `grep -n "FANOUT_SEMAPHORE_LIMIT" system_params.py` returns exactly one definition line (filter comments: `grep -nv '^#' system_params.py | grep "FANOUT_SEMAPHORE_LIMIT ="` returns 1 hit)
    - `grep -n "List-Unsubscribe-Post" notifier/dispatch.py` finds at least one literal `'List-Unsubscribe-Post': 'List-Unsubscribe=One-Click'` occurrence
    - `.venv/bin/pytest tests/test_per_user_fanout.py::TestRFC8058Headers -x` passes (was previously skipped)
    - Full notifier test suite green: `.venv/bin/pytest tests/test_notifier.py -x --tb=short` returns 0
    - No new dependency added to `requirements.txt`
    - `grep -v '^#' notifier/dispatch.py | grep -E "log.*token=[a-zA-Z0-9_-]{10,}"` returns empty (no raw-token logging)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_per_user_fanout.py::TestRFC8058Headers tests/test_notifier.py -x --tb=short</automated>
  </verify>
  <done>FANOUT_SEMAPHORE_LIMIT exists; _post_to_resend accepts email_headers kwarg; three never-raise dispatch functions land and are re-exported; RFC 8058 headers verified via test; existing notifier tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create per_user_fanout.py with asyncio.Semaphore(2) fan-out, per-user crash boundary, W3-invariant single terminal mutate_state — implement remaining UMAIL-01/02/03 tests</name>
  <files>per_user_fanout.py, tests/test_per_user_fanout.py</files>
  <read_first>
    - notifier/dispatch.py (verify send_per_user_email signature from Task 1)
    - state_manager/__init__.py (mutate_state + load_user_state signatures; W3 invariant doc)
    - auth_store/__init__.py + auth_store/_users.py (list_users return shape)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §per_user_fanout.py (verbatim async pattern + anti-patterns)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md §Common Pitfalls (W3 invariant, asyncio.run inside event loop, Semaphore semantics)
    - tests/test_per_user_fanout.py (Wave 0 stubs created by Plan 01)
    - CLAUDE.md (2-space indent; signal-only; Hexagonal-lite — per_user_fanout is I/O orchestrator, NOT pure-math hex)
  </read_first>
  <behavior>
    - Test (TestFanOutEmail): With patched `send_per_user_email` capturing args, `asyncio.run(per_user_fanout._fan_out_all([{'uid':'u1','email':'a@x.com','role':'ff','disabled':False,'email_enabled':True,'pause_until':None}], shared_signals={'spi':'LONG'}, run_date='2026-05-14'))` returns one outcome dict with ok=True; captured call args show user_state contains the user's email and shared_signals contains 'spi'='LONG'; the full `state` dict is NOT passed to send_per_user_email (Pitfall 4)
    - Test (TestCrashBoundary): With patched `send_per_user_email` that raises RuntimeError for uid='broken' and returns SendStatus(ok=True) for others, `_fan_out_all([broken, healthy_a, healthy_b], ...)` returns three outcomes — broken has ok=False and reason starts with 'RuntimeError:', healthy_a and healthy_b have ok=True; gather did not propagate the exception
    - Test (TestW3Invariant): A counting `mutate_state` monkeypatch records exactly ONE call when `per_user_fanout.run(state, run_date)` is invoked with 3 active users (the W3 #2 terminal write)
    - Test (TestSemaphoreThrottle): With `send_per_user_email` mocked to `time.sleep(0.01)` per call, 50-user fan-out completes in < 30s and produces 50 ok=True outcomes; concurrent slot count never exceeds 2 (instrument via a shared counter incremented at sleep start, decremented at sleep end, asserting max(observed) == 2)
    - Test (TestEmailPrefsSkip): Users with `email_enabled=False` produce outcome `{'ok': True, 'reason': 'skipped:disabled'}` and trigger ZERO calls to send_per_user_email; users with `pause_until` >= today_iso produce `{'ok': True, 'reason': 'skipped:paused'}` and ZERO calls; users past pause_until produce a real send
    - Test (TestUnicodeDisplayName): When user_state contains `'display_name': 'Müller'` and `'email': 'm@x.com'`, the call to `send_per_user_email` receives the user_state intact; the underlying notifier call (mocked) uses `email.utils.formataddr` to build the To: header — assert `formataddr(('Müller', 'm@x.com'))` produces `'=?utf-8?q?M=C3=BCller?= <m@x.com>'` (stdlib invariant; the test pins the behaviour at the boundary)
  </behavior>
  <action>
    Step 1 — Create `per_user_fanout.py` at repo root following 37-PATTERNS.md §per_user_fanout.py verbatim structure, but implement the EXACT module surface from this plan's `<interfaces>` block:
      - Module imports: `asyncio`, `logging`, `from datetime import date`, `from zoneinfo import ZoneInfo`, `from state_manager import mutate_state, load_user_state`, `from notifier.dispatch import send_per_user_email, send_cycle_summary_email`, `from system_params import FANOUT_SEMAPHORE_LIMIT`. Local `_AWST = ZoneInfo('Australia/Perth')`. Logger via `logging.getLogger(__name__)`.
      - `async def _send_one(sem, uid, user_row, shared_signals, run_date)`: enter `async with sem`; compute `today = date.today()` (or parse from run_date); skip-check `email_enabled = user_row.get('email_enabled', True)`; skip-check `pause_until = user_row.get('pause_until')` against `today <= date.fromisoformat(pause_until)`; on skip return `{'uid': uid, 'ok': True, 'reason': 'skipped:disabled'}` or `'skipped:paused'` WITHOUT calling load_user_state or send_per_user_email; otherwise call `user_state = load_user_state(uid)`, build `shared_signals` slice (caller already constructed), and `result = await asyncio.to_thread(send_per_user_email, uid, user_state, shared_signals, run_date)`. Return outcome dict. Catch `Exception` with `# noqa: BLE001` and return `{'uid': uid, 'ok': False, 'reason': f'{type(exc).__name__}: {exc}'[:200]}`. NEVER raise.
      - `async def _fan_out_all(users, shared_signals, run_date)`: `sem = asyncio.Semaphore(FANOUT_SEMAPHORE_LIMIT)`; build tasks list of `_send_one(sem, u['uid'], u, shared_signals, run_date) for u in users`; `return await asyncio.gather(*tasks, return_exceptions=False)` (each task already catches internally, so gather never sees a raise).
      - `def run(state, run_date) -> list[dict]`: import `list_users` locally to defer auth_store import; build `all_users = list_users()`; filter active F&F: `[u for u in all_users if u.get('role') == 'ff' and not u.get('disabled')]`; merge per-user state fields by reading `state.get('users', {}).get(u['uid'], {})` and unpacking `email_enabled`, `pause_until`, `email`, `display_name` into the user_row dict passed to `_fan_out_all`; build `shared_signals = dict(state.get('signals', {}))`; `outcomes = asyncio.run(_fan_out_all(active_ff, shared_signals, run_date))`; define `_batch_write(s)` closure that sets `s['last_cycle'] = {'date': run_date, 'users': outcomes}` (W3 #2 — exactly ONE call). The `_batch_write` closure MUST carry the following docstring verbatim: `'''Phase 37 _batch_write writes last_cycle only; no per-user alert state mutations in fan-out (stop-loss alerts are read-only in this phase). Future phases may accumulate per-user fields here per D-14 intent.'''`. Call `mutate_state(_batch_write)`; AFTER mutate_state returns, call `send_cycle_summary_email(outcomes, run_date)` (must be OUTSIDE the mutate_state callback to avoid flock-while-network); return outcomes.
      - Module-level note in docstring: "MUST NOT be called from inside an existing event loop (e.g., a FastAPI route) — asyncio.run() rejects nested loops. Synchronous orchestration only (main.py)."

    Step 2 — Replace the remaining six Wave 0 `pytest.skip(...)` stubs in `tests/test_per_user_fanout.py` (TestFanOutEmail, TestCrashBoundary, TestW3Invariant, TestSemaphoreThrottle, TestEmailPrefsSkip, TestUnicodeDisplayName) with real test bodies matching the behavior list above. For each test:
      - Use `monkeypatch.setattr('per_user_fanout.send_per_user_email', _mock)` to inject controlled SendStatus or exception-raising mocks.
      - Use `monkeypatch.setattr('per_user_fanout.list_users', _mock_list_users)` and `monkeypatch.setattr('per_user_fanout.load_user_state', _mock_load_user_state)` for `run()`-level tests.
      - For W3 invariant, use `monkeypatch.setattr('per_user_fanout.mutate_state', _counting_mutate_state)` where the counter records each call.
      - For semaphore observability, the mock send tracks an `active_count`/`max_active` via shared state and asserts max_active <= 2.
      - All tests use `pytest.mark.freeze_time` ONLY if they depend on `date.today()`; otherwise pass an explicit `run_date='2026-05-14'`. Per LEARNINGS G-pattern about calendar-sensitive tests, the pause-until tests freeze a known weekday.

    Step 3 — Confirm that `per_user_fanout` does NOT import `numpy`, `pandas`, `signal_engine`, `sizing_engine` (it is an I/O orchestrator, not pure-math hex; verify via grep gate as a self-check, not a hard new AST rule).

    Constraints: 2-space indent everywhere. `# noqa: BLE001` on the broad-except. No `mutate_state` call inside `asyncio.gather` tasks. No call to per_user_fanout.run from a FastAPI route. Module is stdlib + state_manager + notifier + auth_store + system_params only.
  </action>
  <acceptance_criteria>
    - `python -c "import per_user_fanout; print(per_user_fanout.run.__doc__[:50] if per_user_fanout.run.__doc__ else 'ok')"` exits 0
    - `python -c "import per_user_fanout, inspect; assert 'run' in dir(per_user_fanout); assert callable(per_user_fanout.run); assert inspect.iscoroutinefunction(per_user_fanout._fan_out_all); assert inspect.iscoroutinefunction(per_user_fanout._send_one)"` exits 0
    - `grep -c "mutate_state" per_user_fanout.py` equals 2 (one import line + one terminal call) — filter: `grep -nv '^#' per_user_fanout.py | grep -c "mutate_state"` ≤ 2
    - `grep -c "asyncio.Semaphore" per_user_fanout.py` >= 1
    - `grep -q "Phase 37 _batch_write writes last_cycle only" per_user_fanout.py` succeeds (D-14 intent note present on the _batch_write closure)
    - `grep -c "FANOUT_SEMAPHORE_LIMIT" per_user_fanout.py` >= 1
    - `.venv/bin/pytest tests/test_per_user_fanout.py -x --tb=short` returns 0 with all classes GREEN (no skips remaining)
    - W3 invariant test passes: exactly ONE mutate_state call observed in per_user_fanout.run
    - Semaphore throttle test passes in < 30s wall clock
    - Crash boundary test confirms one uid's RuntimeError does not abort the gather; other uids complete normally
    - No `numpy`, `pandas`, `signal_engine`, `sizing_engine` import: `python -c "import per_user_fanout, sys; assert 'numpy' not in sys.modules and 'pandas' not in sys.modules and 'signal_engine' not in sys.modules"` exits 0
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_per_user_fanout.py -x --tb=short</automated>
  </verify>
  <done>per_user_fanout.py exists with run/_fan_out_all/_send_one; W3 invariant + per-user crash boundary + Semaphore(2) throttle + email-prefs skip all verified by tests; UMAIL-01/02/03 covered at the orchestrator level. Plan 05 wires this into main.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per_user_fanout.run → asyncio.gather → send_per_user_email | One user's exception MUST NOT propagate; per-user crash boundary enforces UMAIL-02 |
| send_per_user_email → _post_to_resend (network) | Outbound HTTPS to Resend API; throttled to 2 concurrent per UMAIL-03 |
| _post_to_resend → log records | Secrets MUST NOT leak (RESEND_API_KEY, raw invite tokens) |
| per_user_fanout.run → mutate_state | Exactly ONE terminal call (W3 #2); state.json flock held during write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Information Disclosure | RESEND_API_KEY in log records | mitigate | Existing `redact_secret` prefix on ResendError emission (Plan 27-03) covers all dispatch paths via the shared `_post_to_resend`. New functions reuse this transport. |
| T-37-02-02 | Information Disclosure | Raw invite token logged via send_invite_email | mitigate | `send_invite_email` log line uses only `to_email` (no URL/token interpolation). Acceptance criteria grep gate verifies. |
| T-37-02-03 | Information Disclosure | Admin/other-user data leaks into F&F email body | mitigate | send_per_user_email accepts ONLY `(uid, user_state, shared_signals, run_date)` — never the full state dict (Pitfall 4 in RESEARCH.md). Caller in per_user_fanout.run constructs `shared_signals = dict(state.get('signals', {}))` slice. |
| T-37-02-04 | Denial of Service | One user's broken state aborts the whole cycle | mitigate | Per-user `try/except Exception # noqa: BLE001` in `_send_one`; `gather(return_exceptions=False)` is safe because tasks self-catch. UMAIL-02 SC. |
| T-37-02-05 | Denial of Service | Resend 429 due to burst above 2 req/sec | mitigate | `asyncio.Semaphore(FANOUT_SEMAPHORE_LIMIT=2)` acquired BEFORE `to_thread` call — slot held across the entire blocking `_post_to_resend` (including 10s retry backoff). |
| T-37-02-06 | Tampering | mutate_state called from inside an asyncio task → flock deadlock | mitigate | Architectural rule documented in module docstring; single mutate_state call in `run()` AFTER `asyncio.run()` returns; acceptance grep gate caps mutate_state at ≤ 2 occurrences (1 import + 1 call). |
| T-37-02-07 | Spoofing | Per-user email recipient (`user_state['email']`) could be admin's email if state corruption | mitigate | `_send_one` reads recipient from `load_user_state(uid)['email']` only — uid is the auth-store-trusted identifier; if user_state is corrupt, send_per_user_email returns `missing_recipient` without sending. |
| T-37-02-08 | Repudiation | No record of which users received emails | mitigate | `state['last_cycle']` records every outcome with uid + ok + reason; `/healthz/last-cycle` (Plan 05) exposes this to admin. |
</threat_model>

<verification>
- `pytest tests/test_per_user_fanout.py -x --tb=short` returns 0 with seven test classes GREEN.
- `pytest tests/test_notifier.py -x --tb=short` stays GREEN (no regression on existing dispatch tests).
- `pytest -x --tb=short` full suite stays GREEN (no regression on the 2218 prior tests).
- Static check: `grep -nv '^#' per_user_fanout.py | grep -c "mutate_state"` ≤ 2.
- Boundary check: `python -c "import per_user_fanout, sys; assert not any(m in sys.modules for m in ('numpy','pandas','signal_engine','sizing_engine'))"`.
</verification>

<success_criteria>
- `system_params.FANOUT_SEMAPHORE_LIMIT == 2`
- `_post_to_resend` accepts `email_headers: dict | None = None` kwarg without breaking existing callers
- `notifier.send_per_user_email`, `notifier.send_invite_email`, `notifier.send_cycle_summary_email` are importable and never raise
- `per_user_fanout.run(state, run_date)` performs exactly one `mutate_state` call and returns a list of outcome dicts
- `asyncio.Semaphore(FANOUT_SEMAPHORE_LIMIT)` throttles concurrent Resend calls; 50-user mock completes < 30s
- Per-user crash boundary verified by test (one RuntimeError does not abort others)
- RFC 8058 List-Unsubscribe + List-Unsubscribe-Post headers verified by test
- Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-02-SUMMARY.md` documenting:
- New `per_user_fanout.py` module (line count, public surface)
- Three new notifier dispatch functions + `_post_to_resend` `email_headers` kwarg
- `FANOUT_SEMAPHORE_LIMIT = 2` constant
- W3 invariant proven by test (exactly one mutate_state call)
- Per-user crash boundary proven by test
- 50-user semaphore-throttle test wall-clock time
- Note that main.py wiring is deferred to Plan 05 (atomic with admin + dashboard surface).
</output>
