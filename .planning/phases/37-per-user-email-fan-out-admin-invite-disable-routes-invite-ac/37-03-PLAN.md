---
phase: 37
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - requirements.txt
  - auth_store/_schema.py
  - auth_store/_users.py
  - auth_store/__init__.py
  - tests/test_auth_store_users.py
autonomous: true
requirements:
  - RBAC-03

must_haves:
  truths:
    - "auth_store.hash_password(plaintext) returns a bcrypt $2b$12$ hash"
    - "auth_store.verify_password(plaintext, stored_hash) is timing-safe and fail-closed"
    - "D-06: password_hash field added to User TypedDict (str | None); stored in auth.json users[] row via consume_and_create_user"
    - "consume_and_create_user accepts password_hash kwarg and stores it on the new User row"
    - "auth_store._peek_invite_token validates a raw token without consuming it (returns email or raises InviteAlreadyConsumed / InviteExpired)"
    - "auth_store.list_pending_invites returns all PendingInvite rows"
    - "auth_store.revoke_invite(token_hash) marks a pending invite consumed and returns True; returns False if not found"
    - "Existing admin row without password_hash key keeps working — every read uses .get('password_hash')"
  artifacts:
    - path: "auth_store/_schema.py"
      provides: "User TypedDict with optional password_hash: str | None"
      contains: "password_hash"
    - path: "auth_store/_users.py"
      provides: "hash_password, verify_password, _peek_invite_token, list_pending_invites, revoke_invite; extended consume_and_create_user"
      contains: "def hash_password"
    - path: "auth_store/__init__.py"
      provides: "re-exports hash_password, verify_password, _peek_invite_token, list_pending_invites, revoke_invite"
    - path: "requirements.txt"
      provides: "bcrypt==5.0.0 pin"
      contains: "bcrypt==5.0.0"
  key_links:
    - from: "auth_store._users.consume_and_create_user"
      to: "auth_store._users.hash_password"
      via: "caller passes pre-hashed password_hash kwarg; consume stores it on new row"
      pattern: "password_hash"
    - from: "auth_store._users._peek_invite_token"
      to: "auth_store._users._verify_token (hmac.compare_digest)"
      via: "timing-safe verify without consuming"
      pattern: "_verify_token"
---

<objective>
Add the auth_store primitives that Plan 04 (invite wizard) and Plan 05 (admin routes) consume: bcrypt password hashing, password_hash on the User TypedDict, peek-without-consume invite validation, list_pending_invites, revoke_invite. Install bcrypt==5.0.0.

Purpose: Phase 34 D-05 deferred password_hash to Phase 37. This plan lands that field plus the surrounding helpers, isolated from web-route work so the auth_store surface is verifiable independently.

Output: bcrypt installed and pinned; auth_store schema and helpers extended; comprehensive unit tests in tests/test_auth_store_users.py; existing admin login path unaffected (admin row has no password_hash; all reads use .get with None default).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-CONTEXT.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md
@.planning/phases/34-user-registry-invite-token-storage/34-CONTEXT.md
@auth_store/_schema.py
@auth_store/_users.py
@auth_store/__init__.py
@requirements.txt

<interfaces>
From auth_store/_schema.py (existing):
  - `class User(TypedDict): uid, email, role, created_at, disabled` — Phase 37 adds `password_hash: str | None`
  - `class PendingInvite(TypedDict): token_hash, email, invited_by, created_at, expires_at, consumed[, consumed_at]`
  - `_normalize_v2(data: dict) -> dict` — schema-version normaliser (no change needed)

From auth_store/_users.py (existing — exact lines per 37-PATTERNS.md):
  - `class InviteAlreadyConsumed(ValueError)` (line 40)
  - `class InviteExpired(ValueError)` (line 44)
  - `_verify_token(unhashed_token, stored_hash) -> bool` (line 71) — wraps `hmac.compare_digest`
  - `mint_invite_token(invited_by_uid, email, ...) -> tuple[str, str]` (line 174)
  - `consume_and_create_user(unhashed_token, new_user_fields, path=None) -> dict` (line 201) — flock-guarded; Phase 37 adds `password_hash=None` kwarg
  - `list_users(path=None) -> list` (line 150)
  - `set_user_disabled(uid, disabled, path=None) -> dict | None` (line 155)
  - `load_auth`, `save_auth` (internal I/O helpers)

From auth_store/__init__.py (existing re-exports):
  - Public surface re-exported for `from auth_store import ...` — add the new names here

Functions this plan creates (signatures locked):
  - `def hash_password(plaintext: str) -> str` — returns `$2b$12$...` bcrypt hash
  - `def verify_password(plaintext: str, stored_hash: str) -> bool` — fail-closed on any Exception
  - `def _peek_invite_token(unhashed_token: str, path: Path | None = None) -> str` — returns the email on a valid pending invite; raises InviteAlreadyConsumed if consumed/not-found; raises InviteExpired if past expires_at
  - `def list_pending_invites(path: Path | None = None) -> list` — returns `data.get('pending_invites', [])` (consumed + active)
  - `def revoke_invite(token_hash: str, path: Path | None = None) -> bool` — marks pending invite consumed (sets `consumed=True`, `consumed_at=ISO`); returns False if not found or already consumed

Dependency to install:
  - `bcrypt==5.0.0` (verified available via `pip index versions bcrypt`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install bcrypt==5.0.0; add password_hash to User TypedDict; add hash_password + verify_password helpers</name>
  <files>requirements.txt, auth_store/_schema.py, auth_store/_users.py, auth_store/__init__.py, tests/test_auth_store_users.py</files>
  <read_first>
    - auth_store/_schema.py (current User TypedDict at lines 49–57; PendingInvite at 59–69; AuthData at 71–82; _normalize_v2 at line 84)
    - auth_store/_users.py (full file — consume_and_create_user lines 201–281; _verify_token line 71; class hierarchy)
    - auth_store/__init__.py (re-export pattern — pre-existing functions and what's exposed)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §auth_store/_schema.py and §auth_store/_users.py (verbatim diffs)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md §Pattern 2 (bcrypt usage) and §Pitfalls Pitfall 5 (.get() everywhere)
    - tests/test_auth_store_users.py (existing test style — find the test class for consume_and_create_user)
    - CLAUDE.md (2-space indent; no ruff format)
  </read_first>
  <behavior>
    - Test 1: `bcrypt` import succeeds; `from auth_store import hash_password, verify_password` succeeds
    - Test 2: `hash_password('correct horse battery staple')` returns a `str` starting with `$2b$12$` and length 60 characters
    - Test 3: Two calls to `hash_password('same_input')` produce DIFFERENT hashes (salt is random)
    - Test 4: `verify_password('correct horse battery staple', hash_password('correct horse battery staple'))` returns True
    - Test 5: `verify_password('wrong', hash_password('right'))` returns False
    - Test 6: `verify_password('any', 'not-a-bcrypt-hash')` returns False (fail-closed — does NOT raise)
    - Test 7: `verify_password('any', '')` returns False (fail-closed)
    - Test 8: `verify_password('any', None)` returns False (fail-closed — bcrypt raises TypeError, we catch)
    - Test 9: User TypedDict has `password_hash` annotation: `from auth_store._schema import User; from typing import get_type_hints; assert 'password_hash' in get_type_hints(User)`
    - Test 10: Existing admin row (no password_hash key) round-trips through load_auth → save_auth without ValueError (User TypedDict permits the missing-key case via `str | None`)
    - Test 11: `user.get('password_hash')` returns None for the existing admin row (no KeyError)
  </behavior>
  <action>
    Step 1 — Install bcrypt: append `bcrypt==5.0.0` to `requirements.txt` (alphabetical position; the executor MUST run `.venv/bin/pip install bcrypt==5.0.0` AS A SEPARATE COMMAND before running tests — this is a development environment update, not a code change).

    Step 2 — `auth_store/_schema.py`: extend the `User` TypedDict to add `password_hash: str | None` as the final field. Add an inline comment `# Phase 37 D-06: optional; None for legacy admin row (admin bypasses password auth)`. Do NOT modify `_normalize_v2` — the migration is implicit-by-omission: existing rows simply lack the key; readers use `.get('password_hash')`.

    Step 3 — `auth_store/_users.py`: add two new public helpers at the module top (after the existing exception classes, before `_verify_token`). Lazy-import bcrypt at the call site of the first helper if at module top is problematic (but module-top is fine here — bcrypt is now a hard dependency). Helpers:
      - `def hash_password(plaintext: str) -> str` — body: `return bcrypt.hashpw(plaintext.encode('utf-8'), bcrypt.gensalt(rounds=12)).decode('utf-8')`. Add docstring noting `$2b$12$ prefix encodes algorithm/version/cost in the stored hash (OWASP minimum rounds=12)`.
      - `def verify_password(plaintext: str, stored_hash: str | None) -> bool` — body: try block calls `bcrypt.checkpw(plaintext.encode('utf-8'), stored_hash.encode('utf-8'))`; except `Exception` (broad, deliberate fail-closed; `# noqa: BLE001`) returns `False`. Add docstring `Timing-safe (bcrypt.checkpw uses constant-time comparison internally). Fail-closed on any exception (None hash, malformed hash, encoding errors).`

    Step 4 — `auth_store/__init__.py`: import `hash_password` and `verify_password` from `auth_store._users` and add to the existing re-export list / `__all__` (preserve existing alphabetical or grouping convention).

    Step 5 — `tests/test_auth_store_users.py`: append a new test class `TestPasswordHashing` covering behaviors 1–8 above. Use the existing `isolated_auth_json` fixture if any test needs auth.json side effects (most password-hashing tests are pure and need no fixture). Also add `TestUserSchemaPasswordHashField` covering behaviors 9–11.

    Constraints: 2-space indent. The new helpers MUST be safe to call from any thread (bcrypt is GIL-aware; no special locking). No change to existing public functions in this task — `consume_and_create_user` extension is Task 2.
  </action>
  <acceptance_criteria>
    - `grep -E "^bcrypt==5\.0\.0" requirements.txt` returns 1 line
    - `.venv/bin/python -c "import bcrypt; print(bcrypt.__version__)"` prints `5.0.0`
    - `.venv/bin/python -c "from auth_store import hash_password, verify_password; h = hash_password('x'); assert h.startswith('\$2b\$12\$'); assert verify_password('x', h); assert not verify_password('y', h); assert not verify_password('x', None); assert not verify_password('x', '')"` exits 0
    - `grep -n "password_hash" auth_store/_schema.py` returns at least 1 hit on a TypedDict field line (filter: `grep -nv '^#' auth_store/_schema.py | grep "password_hash:"` returns ≥1)
    - `.venv/bin/pytest tests/test_auth_store_users.py::TestPasswordHashing tests/test_auth_store_users.py::TestUserSchemaPasswordHashField -x --tb=short` returns 0
    - Existing auth_store tests stay green: `.venv/bin/pytest tests/test_auth_store_users.py tests/test_auth_store_devices.py tests/test_auth_store_magic_links.py -x --tb=short` returns 0
    - Existing admin login path unaffected: `.venv/bin/pytest tests/test_web_login.py -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_auth_store_users.py tests/test_web_login.py -x --tb=short</automated>
  </verify>
  <done>bcrypt installed and pinned; hash_password + verify_password public functions exist; User TypedDict has password_hash: str | None; existing admin row continues to load (no KeyError); existing tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend consume_and_create_user with password_hash kwarg; add _peek_invite_token, list_pending_invites, revoke_invite</name>
  <files>auth_store/_users.py, auth_store/__init__.py, tests/test_auth_store_users.py</files>
  <read_first>
    - auth_store/_users.py (consume_and_create_user lines 201–281 — find the new_user dict construction, typically lines ~266–273; _verify_token at line 71; flock pattern; _ensure_aware helper)
    - auth_store/__init__.py
    - tests/test_auth_store_users.py (existing TestConsumeAndCreateUser class — augment, do not duplicate)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §auth_store/_users.py (verbatim diff for consume_and_create_user; _peek_invite_token, list_pending_invites, revoke_invite verbatim bodies)
    - .planning/phases/34-user-registry-invite-token-storage/34-CONTEXT.md (Phase 34 D-01 — flock invariant; D-05 — password_hash deferred to Phase 37)
    - CLAUDE.md
  </read_first>
  <behavior>
    - Test (consume_with_password_hash): `consume_and_create_user(raw_token, {'email':'e@x.com','role':'ff'}, password_hash='$2b$12$....')` returns a User dict with `password_hash == '$2b$12$....'`; the saved auth.json contains the same hash on the new user row
    - Test (consume_legacy_no_password_hash): `consume_and_create_user(raw_token, {'email':'e@x.com','role':'ff'})` (no kwarg) returns a User dict where `.get('password_hash')` is None; auth.json has the key set to None on the new row (or omits the key, depending on implementation — verify the .get() invariant either way)
    - Test (peek_valid_token): `_peek_invite_token(raw_token)` returns the email associated with a freshly-minted unconsumed invite — and does NOT mutate `consumed` (subsequent peek returns the same email; subsequent `consume_and_create_user` still succeeds, proving non-consumption)
    - Test (peek_consumed_token): `_peek_invite_token(raw_token)` on a consumed invite raises `InviteAlreadyConsumed`
    - Test (peek_expired_token): `_peek_invite_token(raw_token)` on an invite with `expires_at` < now raises `InviteExpired`
    - Test (peek_unknown_token): `_peek_invite_token('not-a-token')` raises `InviteAlreadyConsumed` with message "not found or invalid token"
    - Test (list_pending_invites_returns_all_rows): includes both consumed and unconsumed (returns the unfiltered list)
    - Test (revoke_invite_marks_consumed): `revoke_invite(token_hash)` on a pending unconsumed invite returns True; auth.json row now has `consumed=True` and `consumed_at` is a valid ISO datetime
    - Test (revoke_invite_unknown_hash): returns False; auth.json unchanged
    - Test (revoke_invite_already_consumed): returns False (per PATTERNS doc — the helper checks `not row.get('consumed')` before mutating)
    - Test (timing_safety_unknown_token): `_peek_invite_token('a' * 64)` raises InviteAlreadyConsumed (`_verify_token` walks the list with hmac.compare_digest — no early return on token-prefix match)
  </behavior>
  <action>
    Step 1 — `auth_store/_users.py::consume_and_create_user`: extend the signature with `password_hash: str | None = None` (place it AFTER `new_user_fields` and BEFORE `path`). Inside the flock-protected new_user construction (currently around lines 266–273 per 37-PATTERNS.md), add `'password_hash': password_hash` to the new_user dict literal. The default `None` keeps legacy callers (if any) and the magic-link flow working unchanged.

    Step 2 — Add `_peek_invite_token(unhashed_token: str, path: Path | None = None) -> str` (private helper, but exposed via re-export for `web/routes/invite/` consumption — name retains the leading underscore as a "stable internal" signal). Follow 37-PATTERNS.md §_peek_invite_token verbatim:
      - `data = load_auth(path=path); data = _normalize_v2(data)` (no save — peek only)
      - Iterate `data.get('pending_invites', [])`; on `_verify_token(unhashed_token, row.get('token_hash', ''))` match: check `row.get('consumed')` → raise InviteAlreadyConsumed; parse `row['expires_at']` via `datetime.fromisoformat` and `_ensure_aware`; if `datetime.now(timezone.utc) > expires_dt` → raise InviteExpired; otherwise return `row.get('email', '')`.
      - Loop exhausts without match → raise InviteAlreadyConsumed('not found or invalid token').

    Step 3 — Add `list_pending_invites(path: Path | None = None) -> list` — body: `return load_auth(path=path).get('pending_invites', [])`. No filtering; admin UI may want to show consumed history too.

    Step 4 — Add `revoke_invite(token_hash: str, path: Path | None = None) -> bool` — body per 37-PATTERNS.md: load_auth, iterate pending_invites, on match check `not row.get('consumed')`, mutate `consumed=True` + `consumed_at=datetime.now(timezone.utc).isoformat()`, save_auth, return True; otherwise loop falls through to return False. Note: this does NOT use flock — concurrent revoke has a safe outcome (idempotent; race produces either True/False but never corrupts state); Phase 34 D-01 reserves the flock invariant for `consume_and_create_user` where atomicity matters.

    Step 5 — `auth_store/__init__.py`: add `_peek_invite_token`, `list_pending_invites`, `revoke_invite` to the re-export list and `__all__`.

    Step 6 — `tests/test_auth_store_users.py`: append a new test class `TestPasswordHashOnConsume` (behaviors 1–2 above), `TestPeekInviteToken` (behaviors 3–6 + 11), `TestListPendingInvites` (behavior 7), `TestRevokeInvite` (behaviors 8–10). Use existing fixture patterns (isolated_auth_json + mint_invite_token to set up). For the expired-token test, freeze time to a future date past `INVITE_TOKEN_TTL_DAYS=7` after minting, OR construct the invite row directly via a helper that takes a custom `expires_at` ISO string in the past.

    Constraints: 2-space indent. Do NOT acquire flock in `revoke_invite` or `list_pending_invites` — Phase 34 D-01 reserves flock for the consume path. `_peek_invite_token` uses `hmac.compare_digest` via the existing `_verify_token` helper (no timing-safe code duplication).
  </action>
  <acceptance_criteria>
    - `python -c "from auth_store import _peek_invite_token, list_pending_invites, revoke_invite, consume_and_create_user; import inspect; assert 'password_hash' in inspect.signature(consume_and_create_user).parameters"` exits 0
    - `.venv/bin/pytest tests/test_auth_store_users.py::TestPasswordHashOnConsume tests/test_auth_store_users.py::TestPeekInviteToken tests/test_auth_store_users.py::TestListPendingInvites tests/test_auth_store_users.py::TestRevokeInvite -x --tb=short` returns 0
    - All four classes produce ≥ 10 named test methods total
    - `grep -n "def _peek_invite_token\|def list_pending_invites\|def revoke_invite" auth_store/_users.py` returns 3 hits
    - Existing consume_and_create_user tests stay green (no signature regression): `.venv/bin/pytest tests/test_auth_store_users.py -x --tb=short` returns 0
    - Existing magic-link tests stay green: `.venv/bin/pytest tests/test_auth_store_magic_links.py -x --tb=short` returns 0
    - Existing admin/devices tests stay green: `.venv/bin/pytest tests/test_auth_store_devices.py tests/test_web_admin.py -x --tb=short` returns 0
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_auth_store_users.py tests/test_auth_store_devices.py tests/test_auth_store_magic_links.py -x --tb=short</automated>
  </verify>
  <done>consume_and_create_user stores password_hash; _peek_invite_token + list_pending_invites + revoke_invite exist and are tested; auth_store re-exports updated; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /accept-invite (Plan 04 caller) → auth_store._peek_invite_token | Untrusted raw token from invitee; must validate timing-safely without consuming |
| /accept-invite POST (Plan 04) → auth_store.consume_and_create_user | Token + plaintext password cross the boundary; password is bcrypt-hashed at the boundary (Plan 04 calls hash_password, then passes the hash here) |
| /admin/invites DELETE (Plan 05) → auth_store.revoke_invite | token_hash supplied by admin; sha256: prefix already enforced (Phase 34) |
| auth.json read/write | Atomic via flock for consume; revoke is non-flocked (idempotent race-safe per Phase 34 D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Tampering | Invite token brute force | mitigate | secrets.token_urlsafe(32) = 256 bits entropy (Phase 34); `_verify_token` uses hmac.compare_digest; rate-limit is upstream on /accept-invite route (Plan 04 / middleware) |
| T-37-03-02 | Information Disclosure | bcrypt hash exposed in HTTP response or log | mitigate | hash_password is called only inside route handlers and stored to auth.json; auth.json is gitignored; `redact_secret` (Plan 27-03) covers Resend API key but bcrypt hash storage is by-design — never serialized to response or log. Acceptance criteria add a grep gate (no `password_hash` in any log/print statement). |
| T-37-03-03 | Spoofing | Timing oracle reveals which tokens exist | mitigate | `_verify_token` uses `hmac.compare_digest`; `_peek_invite_token` walks the entire pending_invites list and only raises after iteration completes — no early-return shortcut on partial match |
| T-37-03-04 | Tampering | Wrong-prefix token_hash stored | mitigate | Phase 34 already enforces `sha256:` prefix on `add_invite_token`; this plan reuses the existing storage layer — no new entry points |
| T-37-03-05 | Information Disclosure | password_hash key absent on admin row → KeyError on read path | mitigate | RESEARCH Pitfall 5: every read uses `.get('password_hash')` not `user['password_hash']`. Acceptance test (Task 1 behavior 11) verifies admin row .get returns None. |
| T-37-03-06 | Repudiation | revoke_invite produces no audit trail | accept | Phase 34 D-01 accepts this for v1.3 scope; `consumed_at` ISO timestamp on the row IS the audit trail; admin UI shows it on /admin/users |
| T-37-03-07 | Denial of Service | bcrypt cost=12 makes login slow (~150 ms) | accept | OWASP minimum; F&F login is infrequent; rate-limit on /accept-invite POST (existing middleware rate limit) prevents flood |
| T-37-03-08 | Information Disclosure | password_hash in bcrypt format printed at debug log level | mitigate | No log statement in hash_password or verify_password. Acceptance criteria: `grep -nE "log.*\$2b\$|print.*\$2b\$" auth_store/` returns empty. |
</threat_model>

<verification>
- `.venv/bin/pip show bcrypt | grep "^Version: 5\.0\.0"` confirms version pin
- `.venv/bin/pytest tests/test_auth_store_users.py -x --tb=short` returns 0
- `.venv/bin/pytest -x --tb=short` full suite returns 0
- `grep -nE "log.*\$2b\$|print.*\$2b\$|password_hash.*log|password_hash.*print" auth_store/` returns empty
- `python -c "from auth_store import hash_password, verify_password, _peek_invite_token, list_pending_invites, revoke_invite, consume_and_create_user, InviteAlreadyConsumed, InviteExpired; print('ok')"` prints `ok`
</verification>

<success_criteria>
- bcrypt 5.0.0 installed and pinned in requirements.txt
- User TypedDict carries password_hash: str | None
- hash_password + verify_password + _peek_invite_token + list_pending_invites + revoke_invite all importable from auth_store
- consume_and_create_user accepts and stores password_hash kwarg without breaking legacy callers
- Comprehensive unit tests cover bcrypt happy/sad paths, peek-without-consume, revoke idempotence, timing-safe lookup
- Existing admin login path still green (no KeyError on .get('password_hash'))
- Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-03-SUMMARY.md` documenting:
- bcrypt 5.0.0 install + version
- User TypedDict diff (+1 optional field)
- 5 new public functions in auth_store
- Test class names + test counts added to test_auth_store_users.py
- Verification that admin login path unaffected
- Pointer to Plans 04 and 05 which consume these primitives
</output>
