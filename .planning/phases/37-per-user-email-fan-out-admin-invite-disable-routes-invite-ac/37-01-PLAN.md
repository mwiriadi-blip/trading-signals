---
phase: 37
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_per_user_fanout.py
  - tests/test_web_invite.py
  - tests/test_web_dashboard_email_prefs.py
  - tests/test_web_admin_invite.py
  - tests/test_main.py
  - tests/conftest.py
autonomous: true
requirements:
  - UMAIL-01
  - UMAIL-02
  - UMAIL-03
  - UMAIL-04
  - RBAC-03

must_haves:
  truths:
    - "All Phase 37 test files exist as importable stubs"
    - "Each test class named in 37-VALIDATION.md is present as `pytest.skip` placeholder so collection succeeds"
    - "Full suite collection (pytest --collect-only) succeeds with new test files"
    - "tests/test_main.py contains TestFetchCountInvariant stub class for UMAIL-01 SC-1 fetch-count invariant"
    - "tests/conftest.py exposes `pending_invite_auth_json` and `multi_user_state_json` fixtures for downstream waves (review #11)"
  artifacts:
    - path: "tests/test_per_user_fanout.py"
      provides: "Wave 0 stubs for UMAIL-01..04 test classes (TestFanOutEmail, TestCrashBoundary, TestW3Invariant, TestSemaphoreThrottle, TestRFC8058Headers, TestEmailPrefsSkip, TestUnicodeDisplayName)"
    - path: "tests/test_web_invite.py"
      provides: "Wave 0 stubs for RBAC-03 (TestStep1Password, TestStep3Device, TestExpiredToken)"
    - path: "tests/test_web_dashboard_email_prefs.py"
      provides: "Wave 0 stubs for UMAIL-04 (PATCH /settings/email-prefs)"
    - path: "tests/test_web_admin_invite.py"
      provides: "Wave 0 stubs for RBAC-03 admin side (TestAdminInviteIssue, TestAdminInviteRevoke, TestLastCycle)"
    - path: "tests/test_main.py"
      provides: "Wave 0 stub for UMAIL-01 SC-1 (TestFetchCountInvariant: fetch_ohlcv call count == 2 per cycle regardless of F&F user count)"
    - path: "tests/conftest.py"
      provides: "pending_invite_auth_json + multi_user_state_json shared fixtures for Phase 37 downstream tests"
      contains: "pending_invite_auth_json"
  key_links:
    - from: "tests/test_per_user_fanout.py"
      to: "per_user_fanout (module not yet created)"
      via: "import inside test body wrapped in pytest.skip"
      pattern: "pytest.skip.*Wave 0"
---

<objective>
Create Wave 0 test scaffolding for Phase 37. Every test class named in 37-VALIDATION.md and 37-RESEARCH.md gets a stub so later Waves can fail RED and turn GREEN incrementally. No production code is created here; this plan only emits skipped test functions and import scaffolding.

Purpose: Lock the test surface for Phase 37 so all downstream plans have a green collection baseline and can target specific node IDs (e.g. `pytest tests/test_per_user_fanout.py::TestRFC8058Headers -x`).

Output: 4 new test files + 2 new conftest fixtures; pytest collection succeeds; no production modules are imported at module top level (all production imports live inside skipped test bodies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-CONTEXT.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-VALIDATION.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-REVIEWS.md
@tests/conftest.py
@tests/test_web_admin.py

<interfaces>
Existing conftest fixtures (do not redefine):
- `valid_secret` — 32-char `'a'*32` WEB_AUTH_SECRET
- `valid_cookie_token`, `valid_pending_token`, `valid_enroll_token` — itsdangerous tokens
- `isolated_auth_json(tmp_path, monkeypatch)` — redirects auth.json to tmp_path
- `auth_headers(valid_secret)` — `{'X-Trading-Signals-Auth': ...}`

New conftest fixtures THIS plan adds (Task 5; satisfies review consensus #11):
- `pending_invite_auth_json(tmp_path, monkeypatch, isolated_auth_json)` — auth.json with admin row + ONE freshly-minted unconsumed PendingInvite row (email='invitee@x.com', non-expired). Yields a tuple `(auth_path, raw_token, token_hash, email)` so downstream tests can call `_peek_invite_token(raw_token)` or `consume_and_create_user(raw_token, ...)` deterministically. Stub the body with `pytest.skip` raise-style internally (NOT used as a fixture-only skip — see action notes) is NOT acceptable: this is a real fixture that must work; the GREEN implementation lands in Plan 03 once `mint_invite_token` is available. For Wave 0, the fixture YIELDS a fixture-stub-only sentinel and the consuming tests stay skipped — see Task 5 action body for the deferred-binding approach.
- `multi_user_state_json(tmp_path, monkeypatch)` — state.json with three users under `state['users']`: u_active (`role='ff', disabled=False, email_enabled=True, pause_until=None`), u_paused (`pause_until` = today+7d ISO), u_disabled (`disabled=True`). Yields a tuple `(state_path, uids_dict)` for downstream use.

Test naming map (37-VALIDATION.md columns):
- `TestFanOutEmail` (UMAIL-01) — per-user email dispatched with personal section + shared signals
- `TestCrashBoundary` (UMAIL-02) — one user failure does not abort the cycle
- `TestW3Invariant` (UMAIL-02) — `per_user_fanout.run()` performs exactly one `mutate_state` call
- `TestSemaphoreThrottle` (UMAIL-03) — 50-user mock completes < 30s under Semaphore(2)
- `TestRFC8058Headers` (UMAIL-03) — `List-Unsubscribe` + `List-Unsubscribe-Post` headers present
- `TestEmailPrefsSkip` (UMAIL-04) — skips disabled + paused users without burning Resend quota
- `TestUnicodeDisplayName` (UMAIL-01) — `email.utils.formataddr` round-trips Unicode names
- `TestStep1Password` (RBAC-03) — `/accept-invite` POST validates password + bcrypt-hashes
- `TestStep3Device` (RBAC-03) — `/accept-invite/device` issues trust cookie
- `TestExpiredToken` (RBAC-03) — expired/consumed token renders 200 error page (not redirect)
- `TestAdminInviteIssue` (RBAC-03 admin side) — `POST /admin/invites` mints token + sends email
- `TestAdminInviteRevoke` (RBAC-03 admin side) — `DELETE /admin/invites/{token_hash}` revokes
- `TestLastCycle` (UMAIL-02) — `/healthz/last-cycle` (admin-gated, registered in web/routes/healthz.py per D-15) returns per-user outcomes JSON
- `TestPatchEmailPrefs` (UMAIL-04) — `PATCH /settings/email-prefs` persists `email_enabled` + `pause_until`
- `TestFetchCountInvariant` (UMAIL-01 SC-1) — `data_fetcher.fetch_ohlcv` is invoked exactly TWICE per cycle (SPI200 + BHP) regardless of how many F&F users receive emails (UMAIL-01 invariant: fan-out reads from shared signal state, NEVER refetches yfinance per user)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_per_user_fanout.py with 7 skipped test classes</name>
  <files>tests/test_per_user_fanout.py</files>
  <read_first>
    - tests/conftest.py (fixture surface — `valid_secret`, `isolated_auth_json`, autouse env fixture)
    - tests/test_web_admin.py (style of admin route tests — TestClient construction, `auth_headers` use, 2-space indent)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-VALIDATION.md (per-task verification map; canonical test class names)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md (test patterns)
    - CLAUDE.md (2-space indent; no `ruff format`)
  </read_first>
  <action>
    Create tests/test_per_user_fanout.py with module docstring noting "Phase 37 Wave 0 stubs — pytest.skip until per_user_fanout.py lands in Plan 02." Define seven test classes EXACTLY named per the interfaces block above: TestFanOutEmail, TestCrashBoundary, TestW3Invariant, TestSemaphoreThrottle, TestRFC8058Headers, TestEmailPrefsSkip, TestUnicodeDisplayName. Each class contains at least one method named `test_placeholder` whose first body line is `pytest.skip('Wave 0 stub — implementation lands in Plan 37-02')`. Do NOT import `per_user_fanout` at module top; if any production import is needed for a future GREEN pass, place it inside the test body after the skip (unreachable). Use 2-space indent throughout. Add `import pytest` at top. Do not subclass anything (plain classes). No fixtures created in this file — fixtures stay in conftest.
  </action>
  <acceptance_criteria>
    - File `tests/test_per_user_fanout.py` exists and parses under `python -m py_compile tests/test_per_user_fanout.py`
    - `pytest tests/test_per_user_fanout.py --collect-only -q` lists at least 7 test items, one per class
    - `grep -c "^class Test" tests/test_per_user_fanout.py` >= 7
    - `grep -c "pytest.skip" tests/test_per_user_fanout.py` >= 7
    - `grep "^import per_user_fanout\|^from per_user_fanout" tests/test_per_user_fanout.py` returns no module-level matches (filter: `grep -v '^#' tests/test_per_user_fanout.py | grep -E "^(import|from) per_user_fanout"` returns empty)
    - File uses 2-space indent only (no lines start with 4 spaces of code: `grep -nP "^    [^ ]" tests/test_per_user_fanout.py | grep -v "^#"` returns empty for code lines)
    - All seven class names present: `for c in TestFanOutEmail TestCrashBoundary TestW3Invariant TestSemaphoreThrottle TestRFC8058Headers TestEmailPrefsSkip TestUnicodeDisplayName; do grep -q "^class $c" tests/test_per_user_fanout.py || exit 1; done`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_per_user_fanout.py --collect-only -q 2>&1 | grep -E "7 tests collected|[7-9] tests collected|1[0-9] tests collected"</automated>
  </verify>
  <done>Seven Phase 37 fanout test classes exist as Wave 0 skip stubs; collection passes; no production imports leak to module top.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_web_invite.py with 3 skipped test classes</name>
  <files>tests/test_web_invite.py</files>
  <read_first>
    - tests/conftest.py (fixture surface)
    - tests/test_web_totp.py (style of TOTP-cookie-driven route tests — preferred analog for invite wizard tests)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-VALIDATION.md (RBAC-03 row entries)
    - CLAUDE.md
  </read_first>
  <action>
    Create tests/test_web_invite.py with module docstring noting "Phase 37 Wave 0 stubs for invite acceptance wizard (RBAC-03) — implementation in Plan 37-04." Define three test classes: TestStep1Password, TestStep3Device, TestExpiredToken (names exactly as listed in interfaces block — RBAC-03 row of 37-VALIDATION.md). Each class contains at least one method whose first line is `pytest.skip('Wave 0 stub — implementation lands in Plan 37-04')`. Do NOT import the invite route module at module top. Add `import pytest` at top. 2-space indent. The TestStep1Password class must also include a placeholder for `test_password_too_short_returns_400`, `test_passwords_do_not_match_returns_400`, `test_password_over_72_bytes_returns_400` (review #9 — bcrypt 72-byte cap), and `test_valid_password_bcrypt_hashed_and_user_created` (four methods, all skipped). TestStep3Device must include `test_trust_device_checkbox_sets_tsi_trusted_cookie` and `test_no_trust_device_redirects_to_dashboard_only`. TestExpiredToken must include `test_expired_token_renders_error_page_200_status` and `test_consumed_token_renders_error_page_200_status`.
  </action>
  <acceptance_criteria>
    - File `tests/test_web_invite.py` exists and parses under `python -m py_compile tests/test_web_invite.py`
    - `pytest tests/test_web_invite.py --collect-only -q` lists at least 8 test items (4 + 2 + 2)
    - `grep -q "^class TestStep1Password" tests/test_web_invite.py` succeeds
    - `grep -q "^class TestStep3Device" tests/test_web_invite.py` succeeds
    - `grep -q "^class TestExpiredToken" tests/test_web_invite.py` succeeds
    - `grep -q "test_password_over_72_bytes_returns_400" tests/test_web_invite.py` succeeds (review #9)
    - No module-top import of `web.routes.invite` (filter: `grep -v '^#' tests/test_web_invite.py | grep -E "^(import|from) web\.routes\.invite"` returns empty)
    - `grep -c "pytest.skip" tests/test_web_invite.py` >= 8
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_invite.py --collect-only -q 2>&1 | grep -E "[8-9] tests collected|1[0-9] tests collected"</automated>
  </verify>
  <done>Three invite-wizard test classes exist as Wave 0 stubs with named methods covering RBAC-03 SCs (including bcrypt 72-byte cap); collection passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_web_dashboard_email_prefs.py and tests/test_web_admin_invite.py stubs</name>
  <files>tests/test_web_dashboard_email_prefs.py, tests/test_web_admin_invite.py</files>
  <read_first>
    - tests/test_web_admin.py (admin route test style — already exists; matches Plan 05's admin-invite tests; the /healthz/last-cycle test lives alongside admin-invite tests in test_web_admin_invite.py)
    - tests/conftest.py
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-VALIDATION.md (UMAIL-04 + RBAC-03 admin rows)
    - CLAUDE.md
  </read_first>
  <action>
    File A — tests/test_web_dashboard_email_prefs.py: module docstring "Phase 37 Wave 0 stubs for UMAIL-04 — implementation in Plan 37-05." Define class TestPatchEmailPrefs with methods `test_patch_persists_email_enabled_true`, `test_patch_persists_email_enabled_false`, `test_patch_persists_pause_until_iso_date`, `test_patch_rejects_invalid_pause_date`, `test_patch_requires_authenticated_uid`. All bodies start with `pytest.skip('Wave 0 stub — implementation lands in Plan 37-05')`.

    File B — tests/test_web_admin_invite.py: module docstring "Phase 37 Wave 0 stubs for RBAC-03 admin surface — implementation in Plan 37-05." Define three classes: TestAdminInviteIssue (methods: `test_post_admin_invites_mints_token`, `test_post_admin_invites_sends_invite_email`, `test_post_admin_invites_returns_inline_invite_url_fragment`, `test_post_admin_invites_requires_admin_role`), TestAdminInviteRevoke (methods: `test_delete_admin_invite_marks_consumed`, `test_delete_admin_invite_404_for_unknown_hash`, `test_delete_admin_invite_requires_admin_role`), TestLastCycle (methods: `test_last_cycle_returns_empty_when_no_cycle`, `test_last_cycle_returns_per_user_outcomes`, `test_last_cycle_requires_admin_role`, `test_last_cycle_returns_crash_field_on_total_fanout_failure` — review #5). Every method body starts with `pytest.skip('Wave 0 stub — implementation lands in Plan 37-05')`. 2-space indent, `import pytest` only at module top. Do NOT import `auth_store`, `web.routes.invite`, `per_user_fanout`, or `notifier.dispatch` at module top.
  </action>
  <acceptance_criteria>
    - Both files exist and parse under `python -m py_compile`
    - `pytest tests/test_web_dashboard_email_prefs.py tests/test_web_admin_invite.py --collect-only -q` lists at least 16 test items (5 + 4 + 3 + 4)
    - `grep -q "^class TestPatchEmailPrefs" tests/test_web_dashboard_email_prefs.py` succeeds
    - All three admin classes present: `for c in TestAdminInviteIssue TestAdminInviteRevoke TestLastCycle; do grep -q "^class $c" tests/test_web_admin_invite.py || exit 1; done`
    - `grep -q "test_last_cycle_returns_crash_field_on_total_fanout_failure" tests/test_web_admin_invite.py` succeeds (review #5)
    - No top-level imports of `auth_store`, `per_user_fanout`, `web.routes.invite`, `notifier.dispatch` in either file (filter `^(import|from) (auth_store|per_user_fanout|web\.routes\.invite|notifier\.dispatch)` in non-comment lines returns empty)
    - `grep -c "pytest.skip" tests/test_web_dashboard_email_prefs.py` >= 5
    - `grep -c "pytest.skip" tests/test_web_admin_invite.py` >= 11
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_dashboard_email_prefs.py tests/test_web_admin_invite.py --collect-only -q 2>&1 | grep -E "1[6-9] tests collected|2[0-9] tests collected"</automated>
  </verify>
  <done>UMAIL-04 dashboard test class and RBAC-03 admin test classes (including last_cycle crash-field stub per review #5) exist as Wave 0 stubs; full suite collection (pytest --collect-only) succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Append TestFetchCountInvariant + TestW3InvariantEndToEnd skipped stubs to tests/test_main.py</name>
  <files>tests/test_main.py</files>
  <read_first>
    - tests/test_main.py (existing monkeypatch pattern — `monkeypatch.setattr(main.data_fetcher, 'fetch_ohlcv', _tracking_fetch)` at line ~130 is the canonical analog; 2-space indent throughout)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-VALIDATION.md (UMAIL-01 SC-1 row: yfinance fetch count == 2 per cycle regardless of F&F user count)
    - CLAUDE.md
  </read_first>
  <action>
    Append two new test classes to `tests/test_main.py` (do NOT modify existing classes).

    Class A — `TestFetchCountInvariant`: contains a single method `test_placeholder` whose first body line is `pytest.skip('Wave 0 stub — implementation lands in Plan 37-05 Task 3 alongside per_user_fanout wiring')`. The class docstring must state: "UMAIL-01 SC-1 invariant: data_fetcher.fetch_ohlcv invoked exactly TWICE per cycle (SPI200 + BHP) regardless of N F&F users. Fan-out reads from shared signal state — NEVER refetches yfinance per user."

    Class B (NEW — review #3 cross-module integration test gap) — `TestW3InvariantEndToEnd`: docstring "UMAIL-02 W3 invariant proven end-to-end across main.py → daily_run → per_user_fanout (review #3 — cross-module integration test, not just per_user_fanout unit test). Asserts per_user_fanout.run is invoked exactly ONCE per cycle and calls mutate_state exactly ONCE (W3 #2)." Add three method stubs all calling `pytest.skip('Wave 0 stub — implementation lands in Plan 37-05 Task 3')`:
      - `test_per_user_fanout_run_invoked_exactly_once_per_cycle`
      - `test_per_user_fanout_run_calls_mutate_state_exactly_once`
      - `test_call_order_daily_run_then_dispatch_then_fanout`

    Do NOT add any production imports at module top beyond what already exists in tests/test_main.py. Use 2-space indent.
  </action>
  <acceptance_criteria>
    - `grep -q "^class TestFetchCountInvariant" tests/test_main.py` succeeds
    - `grep -q "^class TestW3InvariantEndToEnd" tests/test_main.py` succeeds (review #3)
    - `grep -A2 "^class TestFetchCountInvariant" tests/test_main.py | grep -q "UMAIL-01"` succeeds
    - `grep -A2 "^class TestW3InvariantEndToEnd" tests/test_main.py | grep -q "cross-module integration"` succeeds
    - `grep -c "pytest.skip.*Wave 0 stub.*Plan 37-05" tests/test_main.py` >= 4 (1 fetch + 3 W3 e2e)
    - `python -m py_compile tests/test_main.py` exits 0
    - Existing tests still collect: `.venv/bin/pytest tests/test_main.py --collect-only -q 2>&1 | tail -1 | grep -q "tests collected"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_main.py --collect-only -q 2>&1 | tail -3</automated>
  </verify>
  <done>TestFetchCountInvariant + TestW3InvariantEndToEnd stub classes exist in tests/test_main.py; Plan 37-05 Task 3 turns them GREEN.</done>
</task>

<task type="auto">
  <name>Task 5: Add pending_invite_auth_json + multi_user_state_json shared fixtures to tests/conftest.py (review consensus #11)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (FULL FILE — current fixture surface, autouse env fixture, isolated_auth_json pattern, valid_secret/valid_pending_token fixture structure)
    - auth_store/_users.py (PendingInvite row shape — `token_hash`, `email`, `invited_by`, `created_at`, `expires_at`, `consumed`; sha256: prefix on token_hash; `INVITE_TOKEN_TTL_DAYS=7`)
    - auth_store/_schema.py (PendingInvite TypedDict + AuthData v2 shape)
    - state_manager/__init__.py (load_state shape — `state['users']` per-user dict layout)
    - CLAUDE.md (2-space indent)
  </read_first>
  <action>
    Append TWO new pytest fixtures to `tests/conftest.py`. Both are session-scoped or function-scoped (function-scoped is fine — they use tmp_path). Do NOT modify existing fixtures.

    Fixture A — `pending_invite_auth_json`:
      Signature: `def pending_invite_auth_json(tmp_path, monkeypatch, isolated_auth_json)`. The fixture builds auth.json contents directly (synthesise the row deterministically — do NOT call `mint_invite_token` because that lives in production code which Wave 0 may not yet touch via the fixture):
        - Generate a deterministic raw token: `raw_token = 'a' * 43` (43 base64url chars ≈ secrets.token_urlsafe(32) length)
        - Compute the hash exactly as auth_store does: `import hashlib; token_hash = 'sha256:' + hashlib.sha256(raw_token.encode('utf-8')).hexdigest()`
        - Build now / expires: `from datetime import datetime, timezone, timedelta; now = datetime.now(timezone.utc); created_at = now.isoformat(); expires_at = (now + timedelta(days=7)).isoformat()`
        - Compose the auth.json structure: `auth_data = {'schema_version': 2, 'users': [{'uid': 'admin-uid', 'email': 'admin@x.com', 'role': 'admin', 'created_at': created_at, 'disabled': False}], 'pending_invites': [{'token_hash': token_hash, 'email': 'invitee@x.com', 'invited_by': 'admin-uid', 'created_at': created_at, 'expires_at': expires_at, 'consumed': False}], 'trusted_devices': [], 'magic_links': []}`
        - Write JSON to `isolated_auth_json` path (the path object yielded by `isolated_auth_json`).
        - `yield {'auth_path': isolated_auth_json, 'raw_token': raw_token, 'token_hash': token_hash, 'email': 'invitee@x.com', 'admin_uid': 'admin-uid'}`
      Docstring: "Phase 37 Wave 0 shared fixture (review consensus #11). Provides auth.json with admin row + ONE unconsumed PendingInvite. Used by Plan 03 (auth_store) and Plan 04 (invite wizard) tests."

    Fixture B — `multi_user_state_json`:
      Signature: `def multi_user_state_json(tmp_path, monkeypatch)`. The fixture writes state.json with three users under `state['users']`:
        - u_active: `{'role': 'ff', 'disabled': False, 'email': 'active@x.com', 'email_enabled': True, 'pause_until': None}`
        - u_paused: `{'role': 'ff', 'disabled': False, 'email': 'paused@x.com', 'email_enabled': True, 'pause_until': (date.today() + timedelta(days=7)).isoformat()}`
        - u_disabled: `{'role': 'ff', 'disabled': True, 'email': 'disabled@x.com', 'email_enabled': True, 'pause_until': None}`
      Compose `state_data = {'schema_version': 12, 'positions': {}, 'signals': {}, 'last_cycle': None, 'users': {'u_active': {...}, 'u_paused': {...}, 'u_disabled': {...}}}` (use the existing v12 schema; if `state_manager.STATE_SCHEMA_VERSION` differs from 12, read it dynamically).
      Write to `tmp_path / 'state.json'`; `monkeypatch.setenv('STATE_FILE', str(state_path))` AND `monkeypatch.setattr('state_manager.STATE_FILE', str(state_path), raising=False)` so both env-driven and import-cached code paths see the redirect.
      `yield {'state_path': state_path, 'uids': {'active': 'u_active', 'paused': 'u_paused', 'disabled': 'u_disabled'}}`
      Docstring: "Phase 37 Wave 0 shared fixture (review consensus #11). state.json with 3 users (active/paused/disabled) for fan-out skip-rule tests."

    Constraints: 2-space indent. Both fixtures yield (not return) so tests can use them as context managers if needed. Both fixtures DO NOT import production modules they're trying to set up for (e.g., do not import `auth_store.mint_invite_token` — synthesise rows directly via the documented schema). This keeps the fixtures stable even if Plan 03 refactors helper signatures.
  </action>
  <acceptance_criteria>
    - `grep -n "def pending_invite_auth_json" tests/conftest.py` returns 1 hit
    - `grep -n "def multi_user_state_json" tests/conftest.py` returns 1 hit
    - Fixtures are usable: `.venv/bin/pytest tests/conftest.py --collect-only -q 2>&1 | grep -q "fixture"` OR a probe test passes — verify with `.venv/bin/python -c "import ast; tree=ast.parse(open('tests/conftest.py').read()); names=[n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'pending_invite_auth_json' in names; assert 'multi_user_state_json' in names; print('ok')"` prints `ok`
    - No production module import at the new fixture-body top level: `grep -nE "^(import|from) (auth_store|per_user_fanout|web\.routes)" tests/conftest.py | grep -v "^#"` returns empty (or matches only pre-existing lines if any)
    - Both fixtures yield a dict (not None): `grep -nE "yield \{" tests/conftest.py` returns >= 2 hits
    - Full suite still collects: `.venv/bin/pytest --collect-only -q 2>&1 | tail -1 | grep -q "tests collected"` succeeds
    - Existing tests stay green: `.venv/bin/pytest tests/test_auth_store_users.py tests/test_web_admin.py -x --tb=short` returns 0 (fixture additions must not break existing tests)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import ast; tree=ast.parse(open('tests/conftest.py').read()); names=[n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'pending_invite_auth_json' in names and 'multi_user_state_json' in names; print('ok')"</automated>
  </verify>
  <done>Two shared fixtures (`pending_invite_auth_json`, `multi_user_state_json`) added to tests/conftest.py per review consensus #11; downstream Plan 03/04/05 tests can consume them instead of duplicating setup; full suite collection unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Wave 0 stubs MUST NOT import production modules that do not yet exist (Plan 02..05); imports stay inside skipped bodies to prevent collection-time ImportError |
| Shared fixtures → auth.json / state.json on disk | Synthesised directly from documented schemas; no production-import dependency at fixture-body top |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Tampering | Test stub falsely passing when production is broken | mitigate | Every stub uses `pytest.skip(...)`, not a no-op return — pytest reports SKIPPED, not PASSED. Reviewer can distinguish unimplemented from green. |
| T-37-01-02 | Denial of Service | Module-top production import collapses entire test suite | mitigate | All production imports stay inside test bodies after `pytest.skip()`; acceptance criteria grep gates enforce this. |
| T-37-01-03 | Information Disclosure | Test stub contains secret or token literal | accept | Stubs contain no secrets; the deterministic `'a' * 43` in `pending_invite_auth_json` is a fixed test sentinel, not a credential. |
| T-37-01-04 | Tampering | Shared fixture writes corrupted state.json shape | mitigate | Fixture synthesises rows from the documented v12 schema; acceptance criteria assert fixture functions parse via AST. |
</threat_model>

<verification>
- All four new test files parse and collect under `pytest --collect-only`.
- Full suite `pytest --collect-only -q 2>&1 | tail -1` reports "N tests collected" with no errors.
- No grep hits for top-level production imports in any of the new files.
- `grep -c "^class Test" tests/test_per_user_fanout.py tests/test_web_invite.py tests/test_web_dashboard_email_prefs.py tests/test_web_admin_invite.py` returns 4 nonzero lines summing to ≥ 14 classes total.
- `pending_invite_auth_json` and `multi_user_state_json` fixtures are reachable from conftest.
</verification>

<success_criteria>
- 4 new test files exist with named classes per 37-VALIDATION.md
- `pytest --collect-only -q` includes all Phase 37 test files without errors
- Every test method body begins with `pytest.skip('Wave 0 stub ...')`
- No production module is imported at module top of any new test file
- 2-space indent throughout; ruff clean (`ruff check tests/test_per_user_fanout.py tests/test_web_invite.py tests/test_web_dashboard_email_prefs.py tests/test_web_admin_invite.py`)
- conftest.py exposes pending_invite_auth_json + multi_user_state_json (review consensus #11)
- tests/test_main.py contains TestFetchCountInvariant + TestW3InvariantEndToEnd stubs (review consensus #3)
</success_criteria>

<output>
After completion, create `.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-01-SUMMARY.md` documenting:
- 4 new test files created with class/method inventory
- 2 new conftest fixtures (`pending_invite_auth_json`, `multi_user_state_json`)
- `TestW3InvariantEndToEnd` stubbed in tests/test_main.py (review #3)
- pytest --collect-only count delta (before vs after this plan)
- Confirmation that no production imports leak to module top
- Pointer that Plans 02..05 will replace `pytest.skip()` calls with real assertions
</output>
</content>
</invoke>