---
phase: 37
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - web/routes/invite/__init__.py
  - web/routes/invite/_renderers.py
  - web/middleware/auth.py
  - web/routes/totp/__init__.py
  - web/app.py
  - tests/test_web_invite.py
autonomous: true
requirements:
  - RBAC-03

must_haves:
  truths:
    - "GET /accept-invite?token=<raw> validates the token without consuming it and renders the step-1 password page (D-04, D-05)"
    - "POST /accept-invite validates password (length>=12 + match), bcrypt-hashes it, calls consume_and_create_user with password_hash, and redirects to step 2 (TOTP enrollment)"
    - "POST /accept-invite/device sets the tsi_trusted cookie if trust_device=on, clears the wizard cookie, and redirects to dashboard (D-04 step 3)"
    - "Expired or already-consumed token renders a 200 HTML error page (D-07) — NOT a redirect"
    - "/accept-invite and /accept-invite/device are reachable WITHOUT prior session (PUBLIC_PATHS extended)"
    - "tsi_invite_wizard cookie is HttpOnly + Secure + SameSite=Strict + max_age=3600"
    - "Wizard cookie payload step transitions are enforced server-side (step='password' → step='totp' → step='device'); a step-N cookie cannot reach step-N+2 directly"
  artifacts:
    - path: "web/routes/invite/__init__.py"
      provides: "register(app) with GET /accept-invite, POST /accept-invite, POST /accept-invite/device"
      exports: ["register"]
      min_lines: 150
    - path: "web/routes/invite/_renderers.py"
      provides: "_render_step1_password_page, _render_invite_error_page, _render_step3_device_page (HTML render helpers)"
    - path: "web/middleware/auth.py"
      provides: "PUBLIC_PATHS frozenset includes /accept-invite and /accept-invite/device"
    - path: "web/app.py"
      provides: "invite_route.register(application) registered BEFORE add_middleware"
  key_links:
    - from: "web/routes/invite/__init__.py::post_accept_invite"
      to: "auth_store.consume_and_create_user(password_hash=...)"
      via: "bcrypt-hashed password passed at flock boundary"
      pattern: "consume_and_create_user.*password_hash"
    - from: "web/routes/invite/__init__.py::get_accept_invite"
      to: "auth_store._peek_invite_token"
      via: "validate-without-consume on initial GET"
      pattern: "_peek_invite_token"
    - from: "web/routes/invite/__init__.py"
      to: "itsdangerous.URLSafeTimedSerializer(salt='tsi-invite-wizard')"
      via: "wizard step-state cookie"
      pattern: "tsi-invite-wizard"
---

<objective>
Implement the public, unauthenticated `/accept-invite` wizard (RBAC-03 acceptance flow). Multi-step flow per D-04: GET /accept-invite?token=<raw> → step 1 password page → POST validates+hashes password+consumes token+creates user → redirect to TOTP enroll (step 2 reuses existing /enroll-totp) → POST /accept-invite/device → optional trust-device cookie → redirect to dashboard.

Purpose: Phase 34 minted invite tokens; Phase 37 wires the user-facing acceptance flow. The wizard must reject expired/consumed tokens with a dedicated 200 error page (D-07, NOT a redirect to /login), prevent step-skipping via server-validated step cookie, and never log raw tokens.

Output: New `web/routes/invite/` package; PUBLIC_PATHS extended; app.py registration order preserved; `tests/test_web_invite.py` flips from Wave 0 stubs to RED→GREEN coverage for RBAC-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-CONTEXT.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md
@web/routes/totp/__init__.py
@web/routes/totp/_renderers.py
@web/middleware/auth.py
@web/app.py

<interfaces>
From auth_store (Plan 03 output):
  - `hash_password(plaintext) -> str` — returns $2b$12$... bcrypt hash
  - `_peek_invite_token(raw_token, path=None) -> str` — returns email or raises InviteAlreadyConsumed/InviteExpired
  - `consume_and_create_user(raw_token, fields, password_hash=None, path=None) -> dict` — flock-guarded; returns new User dict
  - `InviteAlreadyConsumed`, `InviteExpired` exception classes

From web/routes/totp/__init__.py (existing — wizard analog):
  - itsdangerous URLSafeTimedSerializer pattern with salts `tsi-session-cookie`, `tsi-pending-cookie`, `tsi-enroll-cookie`, `tsi-trusted-cookie`
  - `_validate_enroll_cookie(request)` mirrors the pattern this plan uses for `tsi_invite_wizard`
  - `_COOKIE_ATTRS = '; Path=/; Secure; HttpOnly; SameSite=Strict'`
  - `_COOKIE_ATTRS_DELETE = '; Max-Age=0; Path=/; Secure; HttpOnly; SameSite=Strict'`
  - Multi-cookie response uses `resp.raw_headers.append((b'set-cookie', cookie_str.encode('latin-1')))`
  - The `/enroll-totp` route accepts a `tsi_enroll` cookie issued by login OR magic-link; Phase 37 wizard issues a fresh `tsi_enroll` cookie at step-1 POST so the user can flow into the existing TOTP enrollment without re-authentication

From web/middleware/auth.py (existing):
  - `PUBLIC_PATHS = frozenset({'/login', '/logout', '/enroll-totp', '/verify-totp', '/forgot-2fa', '/reset-totp'})` (lines ~66–69)
  - Path comparison is exact-match (`request.url.path in PUBLIC_PATHS`); `/accept-invite/device` must be listed explicitly

From web/app.py (existing — registration order):
  - All public routes registered BEFORE `application.add_middleware(AuthMiddleware, ...)` (Phase 13 D-06)
  - `application.include_router(admin_router)` at line ~213 (admin sub-router)
  - `_login_service.register(application)` and similar registration order to mirror

Cookie schema (this plan):
  - Name: `tsi_invite_wizard`
  - Salt: `tsi-invite-wizard` (URLSafeTimedSerializer)
  - Max age: 3600 seconds (1 hour to complete all 3 steps)
  - Attributes: `Path=/; Secure; HttpOnly; SameSite=Strict`
  - Payload step values: `'password'` (after GET /accept-invite), `'totp'` (after POST /accept-invite), `'device'` (after TOTP enroll completion — but TOTP route does not set this cookie; instead step 3 is reached via direct navigation from TOTP success page wiring deferred to a follow-up, OR by checking BOTH `tsi_invite_wizard.step == 'totp'` AND TOTP enrolled-status on GET /accept-invite/device — see implementation note in Task 1)

Render contracts (37-UI-SPEC.md):
  - Step 1 password page: centered card, max-width 360px, `.eyebrow` "TRADING SIGNALS", h1 "Set your password", `.step-indicator` "Step 1 of 3", form with two `password` inputs (`new-password` autocomplete), minlength=12, submit "Set password"
  - Step 3 trust-device page: centered card, max-width 420px, h1 "Trust this device?", `.step-indicator` "Step 3 of 3", checkbox `trust_device`, submit "Continue to dashboard"
  - Error page: centered card, max-width 360px, h1 "Link expired", body "This invite link has expired or has already been used. Contact the administrator for a new invite.", link "Back to sign in" → /login. STATUS CODE 200 (D-07 — NOT redirect)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create web/routes/invite/ package with register(app), GET /accept-invite, POST /accept-invite, POST /accept-invite/device; wire PUBLIC_PATHS and app.py</name>
  <files>web/routes/invite/__init__.py, web/routes/invite/_renderers.py, web/middleware/auth.py, web/app.py, tests/test_web_invite.py</files>
  <read_first>
    - web/routes/totp/__init__.py (FULL FILE — the canonical wizard analog; _validate_enroll_cookie, multi-cookie set, raw_headers append pattern, cookie attribute strings)
    - web/routes/totp/_renderers.py (HTML render helper style; inline-CSS token block reuse; _LOGIN_INLINE_CSS / _TOTP_INLINE_CSS constants)
    - web/middleware/auth.py lines 66–69 (PUBLIC_PATHS frozenset)
    - web/app.py (route registration order; admin_router inclusion; add_middleware call site)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §web/routes/invite/__init__.py (verbatim structure — copy carefully but adapt the broken parts noted in PATTERNS open question)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md §Surfaces 2/4/5 (exact HTML for step 1, step 3, error page)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md §Pattern 4 (itsdangerous wizard cookie pattern) and §Pitfalls Pitfall 3 (wizard cookie TTL + resume)
    - CLAUDE.md
  </read_first>
  <behavior>
    - TestExpiredToken.test_expired_token_renders_error_page_200_status: GET `/accept-invite?token=<expired_or_unknown>` returns status 200; response body contains the literal string "This invite link has expired or has already been used"; response is NOT a redirect (no Location header)
    - TestExpiredToken.test_consumed_token_renders_error_page_200_status: same, for a token already consumed
    - TestExpiredToken.test_no_token_param_renders_error_page_200_status: GET `/accept-invite` (no token query param) returns 200 error page
    - TestStep1Password.test_get_valid_token_renders_password_form: GET `/accept-invite?token=<valid_raw>` returns 200 HTML containing "Set your password" and "Step 1 of 3" and a hidden form input or wizard cookie (set-cookie header for `tsi_invite_wizard`)
    - TestStep1Password.test_password_too_short_returns_400: with wizard cookie at step=password, POST `/accept-invite` with `password='short'` + `password_confirm='short'` returns status 400 OR re-renders the step-1 page with error region; response body contains "at least 12 characters"
    - TestStep1Password.test_passwords_do_not_match_returns_400: with wizard cookie at step=password, POST with `password='thisisalongpassword'` + `password_confirm='differentvalue'` returns 400 or re-renders step-1 with mismatch error
    - TestStep1Password.test_valid_password_bcrypt_hashed_and_user_created: with wizard cookie at step=password and a freshly minted invite token, POST with `password='thisisalongpassword'` + matching confirm causes (a) `consume_and_create_user` to be called with `password_hash` starting `$2b$12$`, (b) response status 302 with Location header `/enroll-totp`, (c) wizard cookie updated to step='totp', (d) a `tsi_enroll` set-cookie header is also issued so the user can flow into TOTP enrollment
    - TestStep1Password.test_post_without_wizard_cookie_redirects_to_login: POST `/accept-invite` with no wizard cookie returns 302 with Location `/login` (no token reuse possible)
    - TestStep1Password.test_post_with_wrong_step_in_cookie_redirects_to_login: POST `/accept-invite` with wizard cookie at step='device' returns 302 to `/login` — step-skipping defense
    - TestStep3Device.test_trust_device_checkbox_sets_tsi_trusted_cookie: with wizard cookie at step=device, POST `/accept-invite/device` with `trust_device=1` returns 302 to `/`; response set-cookie headers include both a `tsi_invite_wizard` deletion and a `tsi_trusted` cookie
    - TestStep3Device.test_no_trust_device_redirects_to_dashboard_only: with wizard cookie at step=device, POST `/accept-invite/device` with no trust_device param returns 302 to `/`; set-cookie deletes `tsi_invite_wizard` but does NOT set `tsi_trusted`
    - TestStep3Device.test_device_step_without_cookie_redirects_to_login: POST `/accept-invite/device` with no wizard cookie returns 302 to `/login`
    - Public access: TestClient sees status 200 (not 401) on GET `/accept-invite?token=invalid` even without any auth header or session cookie — `/accept-invite` is in PUBLIC_PATHS
  </behavior>
  <action>
    Step 1 — Create directory `web/routes/invite/` and files `__init__.py` + `_renderers.py`. Mirror the structure of `web/routes/totp/`.

    Step 2 — `web/routes/invite/__init__.py`: module docstring noting "Phase 37 D-04/D-05 — public unauthenticated invite acceptance wizard." Imports per 37-PATTERNS.md §web/routes/invite/__init__.py. Define module-level constants `INVITE_WIZARD_SALT = 'tsi-invite-wizard'`, `_WIZARD_COOKIE_MAX_AGE = 3600`, `_COOKIE_ATTRS_WIZARD = '; Path=/; Secure; HttpOnly; SameSite=Strict'`, `_COOKIE_ATTRS_DELETE = '; Max-Age=0; Path=/; Secure; HttpOnly; SameSite=Strict'`.

    Step 3 — `register(app: FastAPI) -> None`: construct `wizard_serializer = URLSafeTimedSerializer(WEB_AUTH_SECRET, salt=INVITE_WIZARD_SALT)` inside the closure (mirror totp pattern). Define inner helper `_validate_wizard_cookie(request) -> dict | None` exactly mirroring `_validate_enroll_cookie` in totp.

    Step 4 — `@app.get('/accept-invite')` handler: signature `get_accept_invite(request: Request, token: str = '')`. Flow:
      - If `not token`: return `HTMLResponse(_render_invite_error_page(), status_code=200)` (D-07).
      - Else: try `email = _peek_invite_token(token)`. If `InviteAlreadyConsumed` or `InviteExpired` is raised: return `HTMLResponse(_render_invite_error_page(), status_code=200)`.
      - On success: build payload `{'step': 'password', 'raw_token': token, 'email': email}`, dump via wizard_serializer, set cookie `tsi_invite_wizard` with `_WIZARD_COOKIE_MAX_AGE` and the wizard attributes. Return `HTMLResponse(_render_step1_password_page(email))` with the set-cookie header (via `resp.raw_headers.append`).
      - Do NOT log the raw token; logging may include `email` and a fixed event marker `[Invite] accept-invite peek ok email=<email>`.

    Step 5 — `@app.post('/accept-invite')` handler: signature `post_accept_invite(request, password: str = Form(...), password_confirm: str = Form(...))`. Flow:
      - Validate wizard cookie via `_validate_wizard_cookie`. If None or step != 'password': return `Response(status_code=302, headers={'Location': '/login'})`.
      - Validate password: if `len(password) < 12` OR `password != password_confirm`: return step-1 page re-render with `.error` region populated. Status code per UI-SPEC may be 400 or 200 with error region — implement as 400 with the same render helper accepting an `error: str | None = None` parameter (matches existing form validation patterns elsewhere in the project).
      - Hash password: `pw_hash = hash_password(password)`. Consume token: try `new_user = consume_and_create_user(raw_token, {'email': email, 'role': 'ff'}, password_hash=pw_hash)`. On InviteAlreadyConsumed/InviteExpired: return error page (defense in depth — concurrent race).
      - Issue TWO set-cookie headers via `resp.raw_headers.append`: (a) `tsi_invite_wizard` updated to step='totp' with same max_age and attributes; (b) `tsi_enroll` — a freshly-signed tsi_enroll cookie carrying `{'uid': new_user['uid'], 'email': email}` valid for 600 seconds, mirroring the `tsi_enroll` issuance in `web/routes/totp/__init__.py` (this cookie unblocks the existing `/enroll-totp` route — the wizard route does NOT duplicate TOTP enrollment logic, it just hands off).
      - Return `Response(status_code=302, headers={'Location': '/enroll-totp'})` with both set-cookie headers.

    Step 6 — `@app.post('/accept-invite/device')` handler: signature `post_invite_device(request, trust_device: str = Form(default=''))`. Flow:
      - Validate wizard cookie. If None or step != 'device': 302 to `/login`. (The cookie-transition from step='totp' to step='device' is implemented in Task 2 of this plan — the TOTP success boundary updates the wizard cookie. For Task 1's tests, set the wizard cookie at step='device' directly via test scaffolding.)
      - If `trust_device` is truthy ('1', 'on', 'true'): issue a `tsi_trusted` set-cookie (use the existing `trusted_serializer` from `web/routes/totp/__init__.py` — re-import it; do NOT duplicate the key-derivation). The trusted cookie payload mirrors what TOTP issues on `?trust_device=on`.
      - Always: delete `tsi_invite_wizard` cookie (Max-Age=0). Return 302 to `/`.

    Step 7 — `_renderers.py`: implement three private render helpers using inline-CSS token block reused verbatim from `web/routes/totp/_renderers.py` `_LOGIN_INLINE_CSS` / `_TOTP_INLINE_CSS`. Helpers:
      - `_render_step1_password_page(email: str, error: str | None = None) -> str` — returns HTML per 37-UI-SPEC.md Surface 2 (centered card, `.step-indicator` "Step 1 of 3", two password inputs, submit "Set password"). If `error` is provided, include a `<div class="error" role="alert">` region with the error message. Use `html.escape(quote=True)` on every interpolation.
      - `_render_step3_device_page() -> str` — 37-UI-SPEC.md Surface 4 (h1 "Trust this device?", `.step-indicator` "Step 3 of 3", checkbox, submit "Continue to dashboard").
      - `_render_invite_error_page() -> str` — 37-UI-SPEC.md Surface 5 (h1 "Link expired", error region with full message, link to `/login` "Back to sign in"). Status code is set by the caller (200), so this helper just returns the HTML string.

    Step 8 — `web/middleware/auth.py`: extend `PUBLIC_PATHS` frozenset to add `/accept-invite` AND `/accept-invite/device`. Both paths must be listed explicitly because the middleware uses `in PUBLIC_PATHS` exact match (verify this by reading line ~208 `if request.url.path in PUBLIC_PATHS`).

    Step 9 — `web/app.py`: add `from web.routes import invite as invite_route` to imports. Call `invite_route.register(application)` BEFORE `application.add_middleware(AuthMiddleware, ...)` — place it next to `login_route.register(application)` and `reset_route.register(application)` for clarity (Phase 13 D-06 registration order rule).

    Step 10 — `tests/test_web_invite.py`: replace ALL Wave 0 skip stubs in TestStep1Password / TestStep3Device / TestExpiredToken with real test bodies covering the behaviors above. (TestStep2HandoffFromTOTP is added in Task 2 below alongside the TOTP-side wizard hook.)

    Constraints: 2-space indent. Do NOT log raw tokens. Do NOT use `print` — use `logger.info` with no token interpolation. `html.escape(value, quote=True)` on every dynamic value in HTML render helpers. The wizard cookie MUST NOT contain plaintext password — only step, raw_token (already-validated), and email. All cookies are HttpOnly + Secure + SameSite=Strict.
  </action>
  <acceptance_criteria>
    - Directory `web/routes/invite/` exists with `__init__.py` and `_renderers.py`
    - `grep -q "/accept-invite" web/middleware/auth.py` succeeds AND `grep -q "/accept-invite/device" web/middleware/auth.py` succeeds (both paths in PUBLIC_PATHS)
    - `grep -q "invite_route.register" web/app.py` succeeds; verify it appears BEFORE the line `add_middleware(AuthMiddleware`
    - `.venv/bin/pytest tests/test_web_invite.py::TestStep1Password tests/test_web_invite.py::TestStep3Device tests/test_web_invite.py::TestExpiredToken -x --tb=short` returns 0 with all three classes GREEN (no skips remaining)
    - `.venv/bin/pytest tests/test_web_totp.py -x --tb=short` returns 0 (TOTP-side wizard-cookie hook does not break existing TOTP tests)
    - `grep -nE "logger\.(info|warning|error|debug).*raw_token|logger\.(info|warning|error|debug).*token=" web/routes/invite/__init__.py` returns empty (no raw token in log lines)
    - Render helpers use `html.escape(.*quote=True)` on dynamic values: `grep -c "html.escape" web/routes/invite/_renderers.py` >= 1; `grep -nE "html\.escape\([^)]*\)" web/routes/invite/_renderers.py | grep -v "quote=True"` returns empty (every escape call uses quote=True)
    - Cookie attributes are correct: `grep -q "Secure" web/routes/invite/__init__.py && grep -q "HttpOnly" web/routes/invite/__init__.py && grep -q "SameSite=Strict" web/routes/invite/__init__.py`
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_invite.py tests/test_web_login.py -x --tb=short</automated>
  </verify>
  <done>Public /accept-invite wizard core wired (GET + POST + /device + PUBLIC_PATHS + app.py registration); expired/consumed tokens render 200 error page (not redirect); step-skipping prevented via server-validated step cookie; existing login flows unaffected; TOTP-side wizard transition lands in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TOTP-side wizard transition hook (step='totp' → step='device') in web/routes/totp/__init__.py + TestStep2HandoffFromTOTP</name>
  <files>web/routes/totp/__init__.py, tests/test_web_invite.py</files>
  <read_first>
    - web/routes/totp/__init__.py (FULL FILE — locate the TOTP verify-success path where the redirect to `/` is constructed after a successful enrollment; identify the exact Response/RedirectResponse construction site)
    - web/routes/invite/__init__.py (Task 1 output — INVITE_WIZARD_SALT constant, wizard cookie payload shape, `_COOKIE_ATTRS_WIZARD`, `_WIZARD_COOKIE_MAX_AGE`)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §web/routes/totp/__init__.py (if a TOTP-side hook snippet exists; if not, derive from Task 1's wizard cookie shape)
    - tests/test_web_totp.py (existing TOTP-success path test analog for monkeypatch + cookie inspection)
    - CLAUDE.md (2-space indent)
  </read_first>
  <behavior>
    - TestStep2HandoffFromTOTP.test_totp_success_with_wizard_cookie_at_totp_transitions_to_device: with a `tsi_invite_wizard` cookie at step='totp' present on the request and a successful TOTP verify, the response (a) carries a `Location` of `/accept-invite/device` instead of `/`, AND (b) includes a set-cookie header for `tsi_invite_wizard` whose decoded payload has step='device' (decode with the wizard serializer using salt 'tsi-invite-wizard').
    - TestStep2HandoffFromTOTP.test_totp_success_without_wizard_cookie_unchanged: with NO `tsi_invite_wizard` cookie present on the request, a successful TOTP verify still redirects to `/` and does NOT issue a `tsi_invite_wizard` set-cookie. Existing TOTP behavior is preserved.
    - TestStep2HandoffFromTOTP.test_totp_success_with_wizard_cookie_at_wrong_step_ignored: with a `tsi_invite_wizard` cookie at step='password' (not 'totp'), the TOTP success path treats it as absent — redirects to `/`, no wizard cookie set, no error. (Defense-in-depth — only the totp→device transition is wired.)
  </behavior>
  <action>
    Step 1 — `web/routes/totp/__init__.py`: locate the TOTP verify-success Response construction site (the one that redirects to `/` after marking the user TOTP-enrolled). BEFORE that Response is returned, inspect `request.cookies.get('tsi_invite_wizard')`:
      - Construct a wizard serializer inline: `wizard_serializer = URLSafeTimedSerializer(secret, salt='tsi-invite-wizard')` (the `secret` variable is already in scope in the totp register() closure, mirroring the existing `_enroll_serializer` construction). Do NOT import from `web.routes.invite` — keep the modules decoupled.
      - Try `payload = wizard_serializer.loads(cookie_val, max_age=3600)` inside try/except `BadSignature, SignatureExpired, Exception` (broad catch with `# noqa: BLE001` — fail-closed to existing behavior).
      - If `payload.get('step') == 'totp'`: rewrite payload to `{**payload, 'step': 'device'}`, dump, and append a `set-cookie` header for `tsi_invite_wizard` with the same attributes (`Path=/; Secure; HttpOnly; SameSite=Strict; Max-Age=3600`). Override the redirect `Location` header from `/` to `/accept-invite/device`.
      - Else (no cookie OR step != 'totp' OR parse failure): leave the existing Response unchanged.

    Step 2 — `tests/test_web_invite.py`: add a new class `TestStep2HandoffFromTOTP` with the three test methods enumerated in the behavior block. Use the existing TOTP success-path test scaffolding (load fixtures, mint a tsi_pending+tsi_enroll cookie, POST a valid TOTP code) and additionally attach a `tsi_invite_wizard` cookie via the test client. Decode set-cookie headers using `URLSafeTimedSerializer(WEB_AUTH_SECRET, salt='tsi-invite-wizard').loads(value, max_age=3600)`.

    Constraints: 2-space indent. Do NOT change the TOTP enroll/verify success criteria beyond the redirect-location override. The serializer construction MUST mirror the existing `_enroll_serializer` pattern (do not introduce a new shared module).
  </action>
  <acceptance_criteria>
    - `grep -n "tsi_invite_wizard\|tsi-invite-wizard" web/routes/totp/__init__.py` returns at least 2 hits (cookie read + serializer salt)
    - `grep -n "/accept-invite/device" web/routes/totp/__init__.py` returns at least 1 hit (the rewritten Location)
    - `.venv/bin/pytest tests/test_web_invite.py::TestStep2HandoffFromTOTP -x --tb=short` returns 0 with all three test methods GREEN
    - Existing TOTP tests stay green: `.venv/bin/pytest tests/test_web_totp.py -x --tb=short` returns 0
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_invite.py::TestStep2HandoffFromTOTP tests/test_web_totp.py -x --tb=short</automated>
  </verify>
  <done>TOTP-success hook transitions the wizard cookie from step='totp' to step='device' and overrides the redirect Location to `/accept-invite/device`; absence-of-cookie and wrong-step cases preserve existing TOTP behavior; tests in TestStep2HandoffFromTOTP verify all three branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public HTTP → /accept-invite (PUBLIC_PATHS) | Unauthenticated; token in query string is the auth mechanism |
| Form POST → /accept-invite | Plaintext password crosses HTTPS boundary; bcrypt-hashed before storage |
| /accept-invite POST → auth_store.consume_and_create_user | Atomic consume via flock (Phase 34 D-01) |
| tsi_invite_wizard cookie | Server-signed; carries step state + raw_token (validated, awaiting consume) + email — all server-trusted at signature-time |
| TOTP success path → /accept-invite/device | Wizard cookie step='totp' → step='device' transition is the only handoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Spoofing | Forged wizard cookie | mitigate | itsdangerous URLSafeTimedSerializer with WEB_AUTH_SECRET salt='tsi-invite-wizard'; max_age=3600 enforced on every read; SignatureExpired/BadSignature → return None → 302 to /login |
| T-37-04-02 | Tampering | Step skipping (jump from step=password to step=device) | mitigate | Each handler asserts `payload['step'] == <expected>`; mismatch redirects to /login. Acceptance test `test_post_with_wrong_step_in_cookie_redirects_to_login` verifies. |
| T-37-04-03 | Tampering | CSRF on POST /accept-invite | mitigate | SameSite=Strict cookie + form requires the wizard cookie set on the same browser; no Origin-bypass route exists. Plus: rate-limit middleware (existing) caps request bursts. |
| T-37-04-04 | Repudiation | No audit trail for invite acceptance | mitigate | `consume_and_create_user` writes the new User row with `created_at` ISO timestamp; auth.json IS the audit log |
| T-37-04-05 | Information Disclosure | Raw invite token in log lines | mitigate | Acceptance criteria grep gate: no log statement contains `raw_token` or `token=` interpolation. Wizard cookie value (which contains the raw token in its signed payload) is opaque in logs (HttpOnly browser-only). |
| T-37-04-06 | Information Disclosure | bcrypt hash in HTTP response body | mitigate | Hash is computed AFTER form receipt and stored to auth.json; never serialized to any response body. Step-2 redirect (302 to /enroll-totp) carries no body. |
| T-37-04-07 | Information Disclosure | Expired/consumed token state-disclosure timing | accept | D-07 specifies the same error page for both expired AND consumed; `_peek_invite_token` walks the full list with hmac.compare_digest before raising — no early-return on partial match. |
| T-37-04-08 | Elevation of Privilege | /accept-invite reachable bypasses authentication | mitigate | This is BY DESIGN — invitee has no session yet; token IS the auth. /accept-invite added to PUBLIC_PATHS per RBAC-03. Once flow completes, user holds a real session (TOTP-verified + cookie issued by /enroll-totp). |
| T-37-04-09 | Elevation of Privilege | Race between two GETs on the same valid token | accept | GET is peek-only (no consume); both invitees would see step-1 form. POST is flock-guarded; only one wins, the other gets InviteAlreadyConsumed page. |
| T-37-04-10 | Denial of Service | Cookie TTL too short, user abandons mid-flow | mitigate | max_age=3600 = 1 hour; if user returns past TTL, GET /accept-invite with the same token will detect `consumed=True` (their first GET consumed it on step-1 POST), and render the error page. Wizard cookie cleared on success or step-3 abandonment. |
| T-37-04-11 | Tampering | Password-validation bypass via Form parsing | mitigate | FastAPI Form parsing rejects missing fields with 422; explicit `len(password) >= 12` + `password == password_confirm` server-side check; no client-side trust |
| T-37-04-12 | Session Fixation | wizard cookie reused across step transitions | mitigate | A NEW cookie value is set at EVERY step transition (different payload → different signed value) per RFC 8058 §V3 mitigation in 37-RESEARCH.md |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_web_invite.py tests/test_web_totp.py tests/test_web_login.py tests/test_auth_store_users.py -x --tb=short` returns 0
- `.venv/bin/pytest -x --tb=short` full suite returns 0
- Manual sanity (deferred to Plan 05 / live UAT): browser at `/accept-invite?token=<freshly-minted>` renders step-1 form; submit → /enroll-totp; TOTP verify → /accept-invite/device; submit → /
- `grep -nE "raw_token|token=[a-zA-Z0-9_-]{8,}" web/routes/invite/__init__.py` finds matches only in code (variable usage), NOT in log/print statements (`grep -nE "(logger|print).*raw_token" web/routes/invite/__init__.py` returns empty)
- `grep -nE "tsi_invite_wizard" web/routes/invite/__init__.py | wc -l` >= 4 (set on GET, validated on POST, updated on POST, deleted on /device)
</verification>

<success_criteria>
- /accept-invite GET, POST, /device POST all routable
- Expired/consumed token → 200 error page (not redirect)
- Password validated server-side (length 12, match) before bcrypt hash + consume_and_create_user
- Wizard cookie carries step state; step-skipping rejected
- TOTP success path updates the wizard cookie from step=totp to step=device
- /accept-invite and /accept-invite/device reachable WITHOUT prior session
- All cookies HttpOnly + Secure + SameSite=Strict + max_age=3600
- No raw token in any log line
- Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-04-SUMMARY.md` documenting:
- New `web/routes/invite/` package (line counts)
- PUBLIC_PATHS diff (+2 entries)
- app.py registration order
- Cookie schema (name, salt, max_age, attributes, payload shape per step)
- TOTP-side wizard transition hook (in web/routes/totp/__init__.py)
- Test class + method inventory for tests/test_web_invite.py
- Confirmation that no raw token appears in any log line
</output>
