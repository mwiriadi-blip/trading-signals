---
phase: 37
plan: 05
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - web/routes/admin/__init__.py
  - web/routes/admin/_models.py
  - web/routes/admin/_renderers.py
  - web/routes/dashboard/__init__.py
  - web/routes/dashboard/_renderers.py
  - web/routes/healthz.py
  - main.py
  - daily_run_helpers.py
  - per_user_fanout.py
  - tests/test_web_admin_invite.py
  - tests/test_web_dashboard_email_prefs.py
  - tests/test_main.py
autonomous: true
requirements:
  - UMAIL-01
  - UMAIL-02
  - UMAIL-04
  - RBAC-03

must_haves:
  truths:
    - "GET /admin/users with HX-Request: true (HTMX) returns HTML fragment; with Accept: application/json returns JSON list[PublicUserSummary] (Phase 36 backward-compat); else Accept: text/html → full HTML page. EXPLICIT PRECEDENCE per review consensus #8: HX-Request > Accept JSON > Accept HTML fallback."
    - "POST /admin/invites mints an invite token via auth_store.mint_invite_token, sends an invite email via per_user_fanout.send_invite_email (REVIEW #10 — dispatch helpers live in per_user_fanout.py NOT notifier/dispatch.py), and HTMX-swaps an inline `<code>` invite URL fragment (D-09)"
    - "DELETE /admin/invites/{token_hash} revokes a pending invite via auth_store.revoke_invite"
    - "GET /healthz/last-cycle returns the explicit 7-key schema (date, total, ok, failed, users, errors, crash) from state['last_cycle'] — admin-gated via require_admin Depends; registered as standalone route in web/routes/healthz.py (D-15)"
    - "PATCH /settings/email-prefs persists email_enabled (bool) and pause_until (ISO date string or None) to state['users'][uid] via mutate_user_state (UMAIL-04 / D-13)"
    - "Admin dashboard nav has a 'Users' link pointing to /admin/users (D-10)"
    - "Dashboard Settings section renders email-prefs form (D-11/D-12) with a checkbox + native <input type='date'> picker"
    - "main.py + daily_run_helpers.py call per_user_fanout.run(state, run_date.strftime('%Y-%m-%d')) AFTER daily_run + _dispatch_email_and_maintain_warnings completes — on ALL THREE orchestration paths (force/test, once, scheduler-loop)"
    - "CYCLE-DATE IDEMPOTENCY GUARD (REVIEW CONSENSUS #4): per_user_fanout.run() checks `state.get('last_cycle', {}).get('date') == run_date` at entry; if true, SKIP entirely (log + return existing outcomes). Prevents duplicate-send on path retry."
    - "FAN-OUT CRASH HANDLING (REVIEW CONSENSUS #5): main.py wrapper catches any exception from per_user_fanout.run; on crash, calls a separate `per_user_fanout.record_cycle_crash(run_date, exc)` helper that writes last_cycle.crash=str(exc) via mutate_state AND sends a CRASH-tagged cycle summary email via send_cycle_summary_email(outcomes=[], run_date, crash=str(exc)). Admin always receives a cycle email."
    - "W3 END-TO-END TEST (REVIEW CONSENSUS #3): TestW3InvariantEndToEnd in tests/test_main.py proves main.py → daily_run → per_user_fanout flow calls per_user_fanout.run exactly ONCE per cycle and mutate_state exactly TWICE total (W3 #1 in daily_run, W3 #2 in per_user_fanout). Not just per_user_fanout unit tests — full cross-module integration test."
    - "PublicUserSummary.last_seen_date is populated from the most-recent trusted device last_seen across the user's TrustedDevice rows"
  artifacts:
    - path: "web/routes/admin/__init__.py"
      provides: "HX-Request-aware GET /admin/users; POST /admin/invites; DELETE /admin/invites/{token_hash}"
      contains: "/admin/invites"
    - path: "web/routes/healthz.py"
      provides: "GET /healthz/last-cycle (admin-gated via require_admin Depends) returning 7-key last_cycle schema (D-15; review #5)"
      contains: "/healthz/last-cycle"
    - path: "web/routes/admin/_renderers.py"
      provides: "_render_admin_users_page, _render_invite_url_fragment HTML helpers"
    - path: "web/routes/admin/_models.py"
      provides: "PendingInviteSummary Pydantic model; PublicUserSummary.last_seen_date populated"
    - path: "web/routes/dashboard/__init__.py"
      provides: "PATCH /settings/email-prefs handler wired into register()"
    - path: "main.py"
      provides: "per_user_fanout.run() call AFTER run_daily_check + dispatch on force/test + once paths; crash-handler wrapper per review #5"
      contains: "per_user_fanout.run"
    - path: "daily_run_helpers.py"
      provides: "per_user_fanout.run() in scheduler-loop wrapper (_run_daily_check_caught analog)"
      contains: "per_user_fanout.run"
    - path: "per_user_fanout.py"
      provides: "record_cycle_crash(run_date, exc_str) helper for review #5 crash-reporting; cycle-date idempotency guard at run() entry (review #4)"
      contains: "record_cycle_crash"
  key_links:
    - from: "main.py"
      to: "per_user_fanout.run"
      via: "synchronous call after daily-run success + warnings dispatch; wrapped in try/except → record_cycle_crash on failure (review #5)"
      pattern: "per_user_fanout\\.run\\("
    - from: "per_user_fanout.run"
      to: "state.get('last_cycle', {}).get('date') == run_date"
      via: "early-return idempotency guard (review #4)"
      pattern: "last_cycle.*date.*==.*run_date"
    - from: "web/routes/admin/__init__.py::admin_issue_invite"
      to: "per_user_fanout.send_invite_email"
      via: "import path (review #10 — helpers live in per_user_fanout.py, NOT notifier)"
      pattern: "from per_user_fanout import.*send_invite_email|per_user_fanout\\.send_invite_email"
    - from: "web/routes/admin/__init__.py::admin_list_users"
      to: "HX-Request header check"
      via: "explicit negotiation precedence: HX-Request > Accept:application/json > Accept:text/html (review #8)"
      pattern: "HX-Request"
---

<objective>
Wire the remaining Phase 37 surface: admin HTML user-management page with invite issue/revoke + `/healthz/last-cycle` endpoint (admin-gated via require_admin Depends per D-15), dashboard email-preferences form, admin nav link, last_seen_date population, AND the `main.py` + `daily_run_helpers.py` call into `per_user_fanout.run(...)` that completes UMAIL-01 end-to-end.

This revision incorporates five review-consensus fixes:
- **REVIEW #3 (W3 end-to-end)**: dedicated cross-module integration test in tests/test_main.py
- **REVIEW #4 (cycle-date idempotency)**: per_user_fanout.run early-return when state.last_cycle.date == run_date
- **REVIEW #5 (fan-out failure handling)**: main.py wrapper catches crash → records last_cycle.crash + sends CRASH-tagged summary email so admin always gets cycle visibility
- **REVIEW #8 (Accept-header negotiation)**: explicit precedence HX-Request > Accept:application/json > Accept:text/html
- **REVIEW #10 (function placement)**: admin route imports send_invite_email from per_user_fanout (NOT notifier.dispatch)

Output: Admin can issue + revoke invites and see per-cycle outcomes (including crashes); user can toggle daily email + set pause-until from dashboard; daily cycle dispatches per-user emails through per_user_fanout with idempotency + crash protection; all Phase 37 test files flip from skipped/RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-CONTEXT.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-REVIEWS.md
@web/routes/admin/__init__.py
@web/routes/admin/_models.py
@web/routes/dashboard/__init__.py
@web/routes/dashboard/_renderers.py
@web/dependencies.py
@main.py
@daily_run_helpers.py
@state_manager/__init__.py
@per_user_fanout.py

<interfaces>
From Plans 02–04 (already landed):
  - `per_user_fanout.run(state: dict, run_date: str) -> list[dict]` — synchronous entry point
  - `per_user_fanout.send_invite_email(to_email: str, invite_url: str) -> SendStatus` — REVIEW #10: imports from per_user_fanout, NOT notifier
  - `per_user_fanout.send_per_user_email`, `per_user_fanout.send_cycle_summary_email`
  - `auth_store.hash_password` (with 72-byte cap), `verify_password`, `_peek_invite_token`, `list_pending_invites`, `revoke_invite`, `mint_invite_token`, `consume_and_create_user`

From existing admin router (web/routes/admin/__init__.py):
  - `router = APIRouter(prefix='/admin', dependencies=[Depends(require_admin)])` — all new routes inherit require_admin
  - `@router.get('/users', response_model=list[PublicUserSummary])` — Phase 36 JSON endpoint (extend with HX-Request-aware negotiation per review #8)
  - `@router.patch('/users/{uid}/disable')` — Phase 36 endpoint (unchanged)

From web/dependencies.py:
  - `current_user_id` — Depends factory returning the authenticated user_id
  - `require_admin` — Depends factory enforcing admin role; raises 403 otherwise

From web/routes/healthz.py (existing):
  - `register(app: FastAPI) -> None` — Phase 11 standalone-route pattern; currently exposes GET /healthz. Phase 37 ADDS GET /healthz/last-cycle in the same module's register() closure with `require_admin` Depends.

From state_manager:
  - `mutate_state(mutator, path=None) -> None` — atomic write
  - `mutate_user_state(uid, mutator, path=None) -> None` — per-user flock + atomic write
  - `load_state(path=None) -> dict`

From main.py (relevant call sites):
  - Around line ~109: `rc, state, old_signals, run_date = run_daily_check(args)` (force/test path)
  - Line ~114: `_dispatch_email_and_maintain_warnings(state, old_signals, run_date, ...)`
  - Around line ~122: `rc, once_state, _old_signals, _run_date = run_daily_check(args)` (--once path)

From daily_run_helpers.py:
  - `_run_daily_check_caught(...)` — scheduler-loop wrapper that unpacks the 4-tuple and dispatches email

last_cycle schema (from Plan 02 per review #5/#7 — overwrite-only single dict, 7 keys):
  - `{'date': 'YYYY-MM-DD', 'total': int, 'ok': int, 'failed': int, 'users': list, 'errors': list, 'crash': str | None}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin HTML /admin/users page with HX-Request precedence (review #8) + POST/DELETE /admin/invites + GET /healthz/last-cycle (returns 7-key schema per review #5) + PendingInviteSummary + last_seen_date population</name>
  <files>web/routes/admin/__init__.py, web/routes/admin/_models.py, web/routes/admin/_renderers.py, web/routes/healthz.py, tests/test_web_admin_invite.py</files>
  <read_first>
    - web/routes/admin/__init__.py (current router; admin_list_users + admin_disable_user)
    - web/routes/admin/_models.py (PublicUserSummary; last_seen_date deferred field)
    - web/routes/healthz.py (existing /healthz route pattern — never-crash D-19; register() closure)
    - auth_store/_users.py (list_users, list_pending_invites, mint_invite_token, revoke_invite)
    - per_user_fanout.py (send_invite_email — REVIEW #10: this is the import path, NOT notifier/dispatch.py)
    - state_manager/__init__.py (load_state shape — state['last_cycle'] read path)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md (verbatim diffs)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md (Surface 1 + Copywriting + Interaction Contracts)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-REVIEWS.md (review #5 last_cycle schema; review #8 HX-Request precedence; review #10 import path)
    - CLAUDE.md
  </read_first>
  <behavior>
    - TestAdminInviteIssue.test_post_admin_invites_mints_token: POST /admin/invites with email=`new@x.com` returns 200; `auth_store.list_pending_invites()` includes a row with email=`new@x.com` and `consumed=False`
    - TestAdminInviteIssue.test_post_admin_invites_sends_invite_email: same POST captures one call to `per_user_fanout.send_invite_email` (REVIEW #10 — verify the import is from per_user_fanout, NOT notifier.dispatch); first arg == `'new@x.com'`; second arg contains `/accept-invite?token=`
    - TestAdminInviteIssue.test_post_admin_invites_returns_inline_invite_url_fragment: response body contains a `<code>` element AND the full invite URL
    - TestAdminInviteIssue.test_post_admin_invites_requires_admin_role: same POST without admin role returns 403
    - TestAdminInviteRevoke.test_delete_admin_invite_marks_consumed: DELETE returns 200; row has `consumed=True`
    - TestAdminInviteRevoke.test_delete_admin_invite_404_for_unknown_hash: returns 404
    - TestAdminInviteRevoke.test_delete_admin_invite_requires_admin_role: returns 403
    - TestLastCycle.test_last_cycle_returns_empty_when_no_cycle: GET /healthz/last-cycle with state['last_cycle'] absent returns 7-key schema with `{'status':'ok','date':None,'total':0,'ok':0,'failed':0,'users':[],'errors':[],'crash':None}` (review #5)
    - TestLastCycle.test_last_cycle_returns_per_user_outcomes: with state['last_cycle'] populated with all 7 keys, GET returns the same 7-key dict
    - TestLastCycle.test_last_cycle_requires_admin_role: returns 403
    - TestLastCycle.test_last_cycle_returns_crash_field_on_total_fanout_failure (REVIEW #5): with state['last_cycle'] = {date,total=0,ok=0,failed=0,users=[],errors=[],crash='RuntimeError: total fan-out failure'}, GET returns the crash field intact
    - REVIEW #8 — Accept-negotiation precedence test: GET /admin/users with `HX-Request: true` (regardless of Accept header) returns an HTML FRAGMENT (not full page); with `Accept: application/json` (and NO HX-Request) returns JSON; with `Accept: text/html` (and NO HX-Request, NO json) returns full HTML page. Three explicit tests pin the precedence order.
    - last_seen_date population: a User with two trusted devices (last_seen='2026-05-10T00:00:00Z' and '2026-05-13T00:00:00Z') produces `PublicUserSummary.last_seen_date='2026-05-13'`; zero trusted devices produces None
  </behavior>
  <action>
    Step 1 — `web/routes/admin/_models.py`: add `PendingInviteSummary(BaseModel)` with fields `token_hash: str`, `email: str`, `invited_by: str`, `created_at: str`, `expires_at: str`, `consumed: bool`. Update `PublicUserSummary.last_seen_date` to be populated by a `_compute_last_seen_date(uid, trusted_devices_for_user)` helper returning max `last_seen` ISO date (YYYY-MM-DD substring) or None.

    Step 2 — Create `web/routes/admin/_renderers.py`. Implement two render helpers:
      - `_render_admin_users_page(summaries, pending) -> str` — full HTML per UI-SPEC Surface 1
      - `_render_invite_url_fragment(invite_url, email, expires_at) -> str` — HTMX swap fragment with `<code class="invite-url">{invite_url}</code>`
      - `_render_admin_users_html_fragment(summaries, pending) -> str` — NEW (review #8): the HX-Request-only fragment (just the tables, no chrome) for HTMX swaps. Used when `HX-Request: true` is present.

    Step 3 — `web/routes/admin/__init__.py`: extend the router.
      - **REVIEW #8 — Accept-header precedence**: Modify `admin_list_users` to accept `request: Request` and implement EXPLICIT precedence:
          ```
          if request.headers.get('HX-Request') == 'true':
            return HTMLResponse(_render_admin_users_html_fragment(summaries, pending))
          accept = request.headers.get('accept', '')
          if 'application/json' in accept:
            return summaries  # Phase 36 JSON response_model behaviour
          # default: full HTML page (covers Accept: text/html and unspecified)
          return HTMLResponse(_render_admin_users_page(summaries, pending))
          ```
        The order is strict: HX-Request first (HTMX takes precedence over Accept), then JSON, then HTML fallback. Add a docstring noting "Accept-header precedence per review consensus #8: HX-Request: true → fragment; Accept: application/json → JSON list; else → full HTML page."
      - Add `@router.post('/invites')` handler `admin_issue_invite(request, email: str = Form(...), admin_uid: str = Depends(current_user_id))`. Read `BASE_URL = os.environ.get('BASE_URL', '').strip()`; if empty raise 500. Call `raw_token, expires_at = mint_invite_token(invited_by_uid=admin_uid, email=email)`. Construct `invite_url = f'{base_url}/accept-invite?token={raw_token}'`. **REVIEW #10**: import as `from per_user_fanout import send_invite_email` (function-local import for late binding; OR top-level import — verify the dispatch helper lives in per_user_fanout per Plan 02). Call `send_invite_email(to_email=email, invite_url=invite_url)`; return `HTMLResponse(_render_invite_url_fragment(invite_url, email, expires_at))`.
      - Add `@router.delete('/invites/{token_hash}')` handler `admin_revoke_invite(token_hash)`. Call `revoke_invite(token_hash)`; if False raise 404; return `{'ok': True, 'token_hash': token_hash}`.

    Step 4 — `web/routes/healthz.py`: extend with `GET /healthz/last-cycle` route inside the same `register(app)` closure. Admin-gated via EXPLICIT `Depends(require_admin)` (function-local import). Handler:
      ```
      @app.get('/healthz/last-cycle')
      def healthz_last_cycle(_: None = Depends(require_admin)) -> dict:
        try:
          from state_manager import load_state
          state = load_state()
          lc = state.get('last_cycle')
          if lc is None:
            return {'status':'ok','date':None,'total':0,'ok':0,'failed':0,'users':[],'errors':[],'crash':None}
          # REVIEW #5: return the 7-key schema directly; defensive .get() for legacy data
          return {
            'status':'ok',
            'date':lc.get('date'),
            'total':lc.get('total', 0),
            'ok':lc.get('ok', 0),
            'failed':lc.get('failed', 0),
            'users':lc.get('users', []),
            'errors':lc.get('errors', []),
            'crash':lc.get('crash'),
          }
        except Exception:  # noqa: BLE001 — never crash healthz (D-19)
          return {'status':'ok','date':None,'total':0,'ok':0,'failed':0,'users':[],'errors':[],'crash':None}
      ```
      Update module docstring to note "Phase 11 D-13..D-19 (/healthz) + Phase 37 D-15 (/healthz/last-cycle, admin-gated; returns 7-key last_cycle schema per review consensus #5)".

    Step 5 — `tests/test_web_admin_invite.py`: replace all Wave 0 skip stubs with real test bodies. Use the `pending_invite_auth_json` fixture from Plan 01 Task 5 for the admin-invite-revoke tests. Add explicit precedence tests for review #8 (three tests covering HX-Request, Accept JSON, Accept HTML). Add `test_last_cycle_returns_crash_field_on_total_fanout_failure` per review #5.

    Constraints: 2-space indent. DELETE handler does NOT need flock. POST handler logs `[Invite] issued by_admin=<uid> to_email=<email>` at INFO — MUST NOT log raw token. `html.escape(quote=True)` on every interpolation.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_web_admin_invite.py -x --tb=short` returns 0 with all classes GREEN
    - `grep -nE "@router\.(get|post|delete)\('/invites" web/routes/admin/__init__.py` returns >= 2 hits; `/last-cycle` MUST NOT appear in the admin router
    - `grep -nE "@app\.get\('/healthz/last-cycle'\)" web/routes/healthz.py` returns 1 hit
    - `grep -n "require_admin" web/routes/healthz.py` returns >= 1 hit (explicit Depends on the new route)
    - **REVIEW #5 acceptance — 7-key schema**: `grep -E "'date':|'total':|'ok':|'failed':|'users':|'errors':|'crash':" web/routes/healthz.py | wc -l` >= 7 (all 7 keys present in healthz route source)
    - **REVIEW #8 acceptance — HX-Request precedence**: `grep -q "HX-Request" web/routes/admin/__init__.py` succeeds; precedence order verified by test
    - **REVIEW #10 acceptance — import path**: `grep -nE "from per_user_fanout import.*send_invite_email|per_user_fanout\.send_invite_email" web/routes/admin/__init__.py` returns >= 1 hit; AND `grep -nE "from notifier.*import.*send_invite_email|from notifier\.dispatch import.*send_invite_email" web/routes/admin/__init__.py` returns 0 hits (helpers are NOT imported from notifier)
    - Existing Phase 36 admin tests stay green: `.venv/bin/pytest tests/test_web_admin.py -x --tb=short` returns 0
    - No raw token in admin logs: `grep -nE "logger\.(info|debug).*raw_token|logger\.(info|debug).*token=[a-zA-Z0-9_-]{10,}" web/routes/admin/__init__.py` returns empty
    - `grep -c "html.escape" web/routes/admin/_renderers.py` >= 4; `grep -nE "html\.escape\([^)]*\)" web/routes/admin/_renderers.py | grep -v "quote=True"` returns empty
    - `python -c "from web.routes.admin._models import PendingInviteSummary, PublicUserSummary; print('ok')"` prints `ok`
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_admin_invite.py tests/test_web_admin.py -x --tb=short</automated>
  </verify>
  <done>Admin /admin/users HTML page renders with HX-Request precedence (review #8); POST/DELETE /admin/invites work; GET /healthz/last-cycle returns 7-key schema including crash field (review #5); send_invite_email imported from per_user_fanout (review #10); PendingInviteSummary model exists; last_seen_date populated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH /settings/email-prefs dashboard route + Settings section render + admin nav 'Users' link</name>
  <files>web/routes/dashboard/__init__.py, web/routes/dashboard/_renderers.py, tests/test_web_dashboard_email_prefs.py</files>
  <read_first>
    - web/routes/dashboard/__init__.py (register(app) closure; existing PATCH/POST HTMX handlers)
    - web/routes/dashboard/_renderers.py (settings section render entry point)
    - state_manager/__init__.py (mutate_user_state signature; load_user_state)
    - web/dependencies.py (current_user_id factory)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md (PATCH /settings/email-prefs verbatim handler; mutate_user_state for HTMX Mutations)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md (Surface 6)
    - CLAUDE.md
  </read_first>
  <behavior>
    - TestPatchEmailPrefs.test_patch_persists_email_enabled_true: PATCH with `email_enabled=on, pause_until=` returns 200; `load_user_state('u1')['email_enabled'] == True`
    - TestPatchEmailPrefs.test_patch_persists_email_enabled_false: PATCH with checkbox absent results in `email_enabled == False`
    - TestPatchEmailPrefs.test_patch_persists_pause_until_iso_date: PATCH with `pause_until=2026-06-01` persists the ISO date
    - TestPatchEmailPrefs.test_patch_rejects_invalid_pause_date: PATCH with `pause_until=not-a-date` returns 200 silently coercing to None; `load_user_state('u1').get('pause_until') is None`
    - TestPatchEmailPrefs.test_patch_requires_authenticated_uid: PATCH without auth returns 401
    - Settings section render: GET on dashboard settings includes `<form hx-patch="/settings/email-prefs">` with checkbox + native date input matching UI-SPEC Surface 6
    - Admin nav: admin-authenticated GET on dashboard includes `<a href="/admin/users">Users</a>`; non-admin does NOT
  </behavior>
  <action>
    Step 1 — `web/routes/dashboard/__init__.py`: inside the existing `register(app)` closure, add `@app.patch('/settings/email-prefs')` handler `patch_email_prefs(request, email_enabled: str = Form(default=''), pause_until: str = Form(default=''), uid: str = Depends(current_user_id)) -> Response`. Parse: `enabled_bool = (email_enabled == 'on' or email_enabled == '1')`; strip pause_until; if non-empty, validate via `date.fromisoformat` inside try/except — on ValueError set pause_date=None. Define `_apply(state)` mutator that writes to `state['users'][uid]`. Call `mutate_user_state(uid, _apply)`. Return `HTMLResponse('<p>Email preferences saved.</p>')`.

    Step 2 — `web/routes/dashboard/_renderers.py`: add a render helper for the email-prefs section per UI-SPEC Surface 6. Form posts via `hx-patch="/settings/email-prefs"`. Inputs: checkbox `email_enabled`, native `<input type="date" id="pause-until" name="pause_until" value="{{current}}" min="{{today_iso}}">`. Current values read from `load_user_state(uid)`.

    Step 3 — Admin nav link: in the dashboard nav render, add a conditional emitting `<a href="/admin/users">Users</a>` only if the current user's role is admin. Read `auth_store.get_user(uid).get('role') == 'admin'`.

    Step 4 — `tests/test_web_dashboard_email_prefs.py`: replace all Wave 0 skip stubs with real test bodies. Use the `multi_user_state_json` fixture from Plan 01 Task 5 if it simplifies setup.

    Constraints: 2-space indent. Use mutate_user_state (NOT raw mutate_state). Handler must NOT crash on malformed pause_until.
  </action>
  <acceptance_criteria>
    - `grep -n "@app.patch('/settings/email-prefs')" web/routes/dashboard/__init__.py` returns >= 1 hit
    - `grep -n "mutate_user_state" web/routes/dashboard/__init__.py` returns >= 1 hit
    - `.venv/bin/pytest tests/test_web_dashboard_email_prefs.py -x --tb=short` returns 0 with TestPatchEmailPrefs GREEN
    - Dashboard renders admin nav link only for admin role
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_dashboard_email_prefs.py tests/test_web_dashboard.py tests/test_web_admin.py -x --tb=short</automated>
  </verify>
  <done>PATCH /settings/email-prefs persists email_enabled + pause_until via mutate_user_state; Settings section renders; admin nav has 'Users' link visible only to admin (D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire per_user_fanout.run into main.py + daily_run_helpers.py on ALL three orchestration paths; add cycle-date idempotency guard (review #4); add crash-handler (review #5); add W3 end-to-end test (review #3); fetch-count + W3-e2e GREEN</name>
  <files>main.py, daily_run_helpers.py, per_user_fanout.py, tests/test_main.py</files>
  <read_first>
    - main.py (call sites at lines ~109, ~114, ~122 — orchestration entry points)
    - daily_run.py (run_daily_check return shape: (rc, state, old_signals, run_date))
    - daily_run_helpers.py (_run_daily_check_caught or similar wrapper; dispatch ordering)
    - per_user_fanout.py (Plan 02 output — run(state, run_date) signature; last_cycle schema)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md (call-site-in-main.py)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md (Anti-Patterns — call ordering)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-REVIEWS.md (review #3 W3 e2e, #4 idempotency, #5 crash handling)
    - tests/test_main.py (existing W3-invariant test; freeze_time pattern per LEARNINGS G-78 / Calendar-sensitive tests)
    - CLAUDE.md
  </read_first>
  <behavior>
    - main.py + daily_run_helpers.py wire per_user_fanout on all three paths: force/test (line ~109), once (~122), scheduler-loop (in daily_run_helpers); each call wrapped in try/except that on failure invokes `per_user_fanout.record_cycle_crash(run_date_str, str(exc))` (review #5)
    - **REVIEW #4 — cycle-date idempotency**: With `state['last_cycle'] = {'date': '2026-05-11', 'total': 2, ...}` pre-populated AND `run_date=2026-05-11` passed to `per_user_fanout.run(state, '2026-05-11')`, the function returns the existing `last_cycle['users']` list WITHOUT calling `mutate_state` again AND WITHOUT calling `send_per_user_email` for any user. Test `TestCycleDateIdempotency.test_same_run_date_skips_fanout` pins this.
    - **REVIEW #5 — crash handling**: When `per_user_fanout.run` is monkeypatched to raise RuntimeError, main.py's wrapper catches it AND calls `per_user_fanout.record_cycle_crash(run_date, exc_str)`. The helper writes `state['last_cycle'] = {'date': run_date, 'total': 0, 'ok': 0, 'failed': 0, 'users': [], 'errors': [], 'crash': 'RuntimeError: <message>'}` via a single mutate_state call AND invokes `send_cycle_summary_email(outcomes=[], run_date, crash=str(exc))`. Test `TestCycleCrashHandling.test_fanout_crash_records_last_cycle_crash_and_sends_summary` pins this.
    - **REVIEW #3 — W3 end-to-end**: `TestW3InvariantEndToEnd` GREEN with three tests covering: (a) per_user_fanout.run invoked exactly ONCE per cycle (`call_count == 1`), (b) mutate_state called exactly TWICE per cycle (1 in daily_run + 1 in per_user_fanout) — verified via monkeypatched counting mutate_state, (c) call order is run_daily_check → _dispatch_email_and_maintain_warnings → per_user_fanout.run via spy list. All tests use `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')` (Monday).
    - TestFetchCountInvariant GREEN: With 3 F&F users and a counting `data_fetcher.fetch_ohlcv` mock, `main.main(['--force-email'])` results in `call_count == 2` (one for SPI200, one for BHP). Number of F&F users does NOT change fetch count. Uses `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')`.
    - per_user_fanout.run failure does NOT flip rc to non-zero — daily cycle still succeeds.
  </behavior>
  <action>
    Step 1 — `per_user_fanout.py`: add TWO new module-level additions before the existing `run()` function definition:

      (a) **REVIEW #4 — cycle-date idempotency guard at run() entry**: at the top of `def run(state, run_date)`, BEFORE the asyncio.run line, add:
        ```
        existing = state.get('last_cycle') or {}
        if existing.get('date') == run_date:
          logger.info('[Fan-out] idempotency: last_cycle.date == run_date == %s; skipping (review #4)', run_date)
          return existing.get('users', [])
        ```
        This early-return prevents duplicate sends if any orchestration path is retried after a partial success.

      (b) **REVIEW #5 — crash-recording helper**: add a new public function `def record_cycle_crash(run_date: str, exc_str: str) -> None`:
        ```
        def record_cycle_crash(run_date: str, exc_str: str) -> None:
          '''REVIEW #5: Called by main.py when per_user_fanout.run() raises. Records the crash in state['last_cycle'] AND sends a CRASH-tagged admin summary email.'''
          def _write_crash(s):
            s['last_cycle'] = {
              'date': run_date,
              'total': 0, 'ok': 0, 'failed': 0,
              'users': [], 'errors': [],
              'crash': exc_str[:500],
            }
          try:
            mutate_state(_write_crash)
          except Exception:  # noqa: BLE001
            logger.exception('[Fan-out] record_cycle_crash: mutate_state failed')
          try:
            send_cycle_summary_email(outcomes=[], run_date=run_date, crash=exc_str)
          except Exception:  # noqa: BLE001
            logger.exception('[Fan-out] record_cycle_crash: summary email failed')
        ```
      Add `record_cycle_crash` to module docstring's public surface section.

    Step 2 — `main.py`: import `per_user_fanout` at the top. Use `import per_user_fanout` (NOT `from per_user_fanout import run` — preserves monkeypatch path stability).

    Step 3 — `main.py` --force-email / --test path (around line 109): after `_dispatch_email_and_maintain_warnings(...)` completes, add the wrapper:
      ```
      if rc == 0 and state is not None and run_date is not None:
        run_date_str = run_date.strftime('%Y-%m-%d')
        try:
          per_user_fanout.run(state, run_date_str)
        except Exception as exc:  # noqa: BLE001 — fan-out is additive; never abort main on its failure
          logger.warning('[FanOut] WARN per_user_fanout.run failed: %s: %s', type(exc).__name__, exc)
          # REVIEW #5: record crash to state + send CRASH-tagged admin summary
          per_user_fanout.record_cycle_crash(run_date_str, f'{type(exc).__name__}: {exc}')
      ```
      Place AFTER the dispatch call, BEFORE the function returns.

    Step 4 — `main.py` --once path (around line 122): same pattern with `once_state` and `_run_date`.

    Step 5 — `daily_run_helpers.py` scheduler-loop wrapper (`_run_daily_check_caught` or analogue): inside the wrapper, AFTER the existing dispatch call (per LEARNINGS G — "Refactor that adds return values must update every caller"), add the SAME wrapped per_user_fanout.run + record_cycle_crash pattern. Locate the dispatch site via `grep -n "_dispatch_email_and_maintain_warnings" daily_run_helpers.py` and place the fan-out call immediately after.

    Step 6 — `tests/test_main.py`: replace ALL Wave 0 skip stubs and add new tests:
      - `TestFetchCountInvariant` (turn the Plan 01 stub GREEN): use the existing `_install_fixture_fetch` pattern, wrap it with a counter, seed state with 3 F&F users, patch `per_user_fanout.send_per_user_email` to no-op, run `main.main(['--force-email'])`, assert `len(call_count) == 2`. Use `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')`. Remove the `pytest.skip` line.

      - `TestW3InvariantEndToEnd` (turn the Plan 01 stubs GREEN — review #3 cross-module): three tests:
        - `test_per_user_fanout_run_invoked_exactly_once_per_cycle`: monkeypatch `per_user_fanout.run` with a counter; run `main.main(['--force-email'])`; assert counter == 1
        - `test_per_user_fanout_run_calls_mutate_state_exactly_once`: monkeypatch the LOCAL mutate_state inside per_user_fanout with a counter; run full cycle; assert per_user_fanout's mutate_state counter == 1 (separate from daily_run's mutate_state)
        - `test_call_order_daily_run_then_dispatch_then_fanout`: install spies on the three functions; assert `call_order == ['run_daily_check', '_dispatch_email_and_maintain_warnings', 'per_user_fanout.run']`
        All use freeze_time.

      - `TestCycleDateIdempotency` (REVIEW #4): two tests:
        - `test_same_run_date_skips_fanout`: seed state with `last_cycle={'date':'2026-05-11', ...}`; call `per_user_fanout.run(state, '2026-05-11')` directly (not via main.main); patch send_per_user_email + mutate_state with counters; assert send counter == 0 AND mutate_state counter == 0; return value matches the pre-existing `last_cycle['users']` list.
        - `test_different_run_date_runs_fanout_normally`: seed with `last_cycle={'date':'2026-05-10', ...}`; call run with run_date='2026-05-11'; assert normal fan-out occurs (send counter > 0, mutate_state counter == 1).

      - `TestCycleCrashHandling` (REVIEW #5): two tests:
        - `test_fanout_crash_records_last_cycle_crash_and_sends_summary`: monkeypatch `per_user_fanout.run` to raise RuntimeError('boom'); install a spy on `per_user_fanout.record_cycle_crash`; install a spy on `per_user_fanout.send_cycle_summary_email`; run `main.main(['--force-email'])`. Assert: (a) record_cycle_crash invoked exactly once with run_date_str AND a string containing 'RuntimeError: boom', (b) send_cycle_summary_email invoked with `crash=...` kwarg, (c) `main.main` returns rc == 0 (cycle not flipped to failed).
        - `test_record_cycle_crash_writes_seven_key_schema`: call `per_user_fanout.record_cycle_crash('2026-05-11', 'RuntimeError: x')` directly; load state; assert `state['last_cycle']` has all 7 keys with the expected values (review #5 schema).

    Constraints: 2-space indent. Do NOT swallow daily_run errors; only swallow per_user_fanout errors. Maintain rc semantics (rc==0 means daily cycle succeeded; F&F fan-out failure does not flip rc). Every new test uses `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')`.
  </action>
  <acceptance_criteria>
    - `grep -c "per_user_fanout.run" main.py` >= 2 (force-email + once paths)
    - `grep -c "per_user_fanout.run" daily_run_helpers.py` >= 1 (scheduler-loop path)
    - `grep -c "per_user_fanout.record_cycle_crash" main.py daily_run_helpers.py 2>/dev/null` >= 2 (review #5 crash-handler wired on every path that calls run)
    - `grep -q "def record_cycle_crash" per_user_fanout.py` succeeds (review #5)
    - **REVIEW #4 acceptance — idempotency guard in source**: `grep -E "last_cycle.*\.get\('date'\)\s*==.*run_date|existing\.get\('date'\)\s*==.*run_date" per_user_fanout.py` returns >= 1 hit
    - `.venv/bin/pytest tests/test_main.py -x --tb=short` returns 0 with all new test classes GREEN
    - `.venv/bin/pytest tests/test_main.py::TestFetchCountInvariant -x --tb=short` returns 0 (UMAIL-01 SC-1 fetch invariant GREEN)
    - `.venv/bin/pytest tests/test_main.py::TestW3InvariantEndToEnd -x --tb=short` returns 0 (review #3 cross-module W3 e2e GREEN)
    - `.venv/bin/pytest tests/test_main.py::TestCycleDateIdempotency -x --tb=short` returns 0 (review #4 GREEN)
    - `.venv/bin/pytest tests/test_main.py::TestCycleCrashHandling -x --tb=short` returns 0 (review #5 GREEN)
    - `grep -A3 "class TestFetchCountInvariant" tests/test_main.py | grep -q "pytest.skip"` returns NO match (Wave 0 stub removed)
    - `grep -A3 "class TestW3InvariantEndToEnd" tests/test_main.py | grep -q "pytest.skip"` returns NO match (Wave 0 stub removed)
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
    - `python -c "import main; assert hasattr(main, 'per_user_fanout')"` exits 0
    - LEARNINGS compliance: every new test in test_main.py that touches run_daily_check uses freeze_time
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_main.py tests/test_per_user_fanout.py -x --tb=short</automated>
  </verify>
  <done>main.py + daily_run_helpers.py wire per_user_fanout.run on all three paths; cycle-date idempotency guard prevents duplicate sends (review #4); crash-handler wrapper records last_cycle.crash + sends CRASH-tagged admin summary (review #5); W3 end-to-end test proves cross-module invariant (review #3); fetch-count invariant GREEN (UMAIL-01 SC-1); fan-out failure does not abort cycle. Phase 37 end-to-end UMAIL-01..04 + RBAC-03 wiring complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTTP → POST /admin/invites | Authenticated admin; email param goes into invite mint + outbound email |
| Admin HTTP → DELETE /admin/invites/{hash} | Authenticated admin; sha256: prefix verified by Phase 34 |
| Admin HTTP → GET /healthz/last-cycle | Admin-gated via explicit Depends(require_admin); returns 7-key schema incl. crash |
| Authenticated F&F HTTP → PATCH /settings/email-prefs | uid via current_user_id; writes per-user state via mutate_user_state |
| main.py → per_user_fanout.run | Internal call; wrapped in try/except → record_cycle_crash on failure (review #5) |
| per_user_fanout.run → cycle-date idempotency guard | Prevents duplicate sends on path retry (review #4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Elevation of Privilege | /admin/* reachable by non-admin | mitigate | router inherits Depends(require_admin); tests verify 403 |
| T-37-05-02 | Tampering | Cross-user write via PATCH /settings/email-prefs | mitigate | uid from Depends(current_user_id) only; no path param controls target uid |
| T-37-05-03 | Information Disclosure | Raw invite token in admin response | accept | D-09 explicitly requires inline display for copy-paste |
| T-37-05-04 | Information Disclosure | Raw invite token in admin log | mitigate | grep gate enforces no raw-token logging |
| T-37-05-05 | Tampering | CSRF on PATCH /settings/email-prefs | mitigate | SameSite=Strict cookie; AuthMiddleware verifies |
| T-37-05-06 | Information Disclosure | /healthz/last-cycle leaks per-user data | mitigate | Explicit Depends(require_admin) on the route |
| T-37-05-07 | Denial of Service | Malformed pause_until crashes route | mitigate | try/except around date.fromisoformat; coerce to None |
| T-37-05-08 | Information Disclosure | Admin nav visible to non-admin | mitigate | Role check at render time |
| T-37-05-09 | Tampering | per_user_fanout.run from FastAPI route → asyncio.run nested | mitigate | Architectural rule; called only from main.py/daily_run_helpers |
| T-37-05-10 | Denial of Service | per_user_fanout.run failure aborts daily cycle | mitigate | Wrapper try/except; rc unchanged. Tests verify. |
| T-37-05-11 | Tampering | Path retry sends duplicate emails (REVIEW #4) | mitigate | Cycle-date idempotency guard: per_user_fanout.run early-returns if `state.last_cycle.date == run_date`. Test pins behaviour. |
| T-37-05-12 | Denial of Service | Total fan-out crash leaves admin blind (REVIEW #5) | mitigate | main.py wrapper calls record_cycle_crash → writes last_cycle.crash + sends CRASH-tagged summary email. Admin always gets a cycle email. |
| T-37-05-13 | Repudiation | Mixed Accept headers cause inconsistent rendering (REVIEW #8) | mitigate | Explicit precedence: HX-Request > Accept:application/json > Accept:text/html. Three tests pin each branch. |
| T-37-05-14 | Information Disclosure | last_seen_date exposes activity to admin | accept | Admin role legitimately needs login visibility (RBAC-04 / D-08) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_web_admin_invite.py tests/test_web_dashboard_email_prefs.py tests/test_main.py tests/test_per_user_fanout.py tests/test_web_invite.py tests/test_auth_store_users.py -x --tb=short` returns 0
- `.venv/bin/pytest -x --tb=short` full suite returns 0
- REVIEW #3 verified: TestW3InvariantEndToEnd GREEN with 3 tests proving cross-module W3 invariant
- REVIEW #4 verified: TestCycleDateIdempotency GREEN; idempotency guard in source
- REVIEW #5 verified: TestCycleCrashHandling GREEN; record_cycle_crash + send_cycle_summary_email(crash=...) wired; healthz returns 7-key schema incl. crash
- REVIEW #8 verified: HX-Request precedence test passes; explicit precedence in admin_list_users source
- REVIEW #10 verified: admin route imports send_invite_email from per_user_fanout (not notifier)
- Coverage map: RBAC-03 covered by tests/test_web_invite.py + tests/test_web_admin_invite.py + tests/test_auth_store_users.py; UMAIL-01 by TestFanOutEmail + TestW3InvariantEndToEnd + TestFetchCountInvariant; UMAIL-02 by TestCrashBoundary + TestW3Invariant + TestLastCycle + TestCycleCrashHandling; UMAIL-03 by TestSemaphoreThrottle + TestRFC8058Headers; UMAIL-04 by TestEmailPrefsSkip + TestPatchEmailPrefs
</verification>

<success_criteria>
- Admin /admin/users HTML page renders with explicit HX-Request precedence (review #8)
- POST /admin/invites mints + emails (via per_user_fanout.send_invite_email per review #10) + HTMX-swaps inline URL
- DELETE /admin/invites/{hash} revokes
- GET /healthz/last-cycle returns 7-key schema including crash field (review #5)
- PATCH /settings/email-prefs persists prefs
- Admin nav has Users link visible only to admin
- last_seen_date populated from trusted devices
- main.py + daily_run_helpers call per_user_fanout.run on all three orchestration paths
- Cycle-date idempotency guard in per_user_fanout.run (review #4)
- Crash-handler wraps every call site (review #5)
- W3 invariant proven end-to-end across modules (review #3)
- Full suite green
- All Phase 37 requirements covered: RBAC-03, UMAIL-01, UMAIL-02, UMAIL-03, UMAIL-04
</success_criteria>

<output>
After completion, create `.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-05-SUMMARY.md` documenting:
- Admin route additions (POST /invites, DELETE /invites/{hash}, HX-Request-aware GET /users)
- /healthz/last-cycle 7-key schema (review #5)
- Dashboard route additions (PATCH /settings/email-prefs, admin nav link)
- main.py + daily_run_helpers.py wiring (3 call sites + crash-handler per review #5)
- per_user_fanout cycle-date idempotency guard (review #4)
- per_user_fanout.record_cycle_crash helper (review #5)
- HX-Request precedence (review #8) verified by 3 tests
- send_invite_email imported from per_user_fanout (review #10) — no notifier dependency in admin
- W3 invariant proven end-to-end (review #3)
- Confirmation no raw token leaks to logs
- Phase 37 ready for `/gsd-verify-work 37`
</output>
</content>
</invoke>