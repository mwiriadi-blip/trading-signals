---
phase: 37
plan: 05
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - web/routes/admin/__init__.py
  - web/routes/admin/_models.py
  - web/routes/admin/_renderers.py
  - web/routes/dashboard/__init__.py
  - web/routes/dashboard/_renderers.py
  - web/routes/healthz.py
  - main.py
  - tests/test_web_admin_invite.py
  - tests/test_web_dashboard_email_prefs.py
  - tests/test_main.py
autonomous: true
requirements:
  - UMAIL-01
  - UMAIL-02
  - UMAIL-04
  - RBAC-03

must_haves:
  truths:
    - "GET /admin/users with Accept: text/html returns the admin user-management HTML page (D-08); same path with Accept: application/json returns the existing list[PublicUserSummary] JSON (Phase 36 backward-compat)"
    - "POST /admin/invites mints an invite token via auth_store.mint_invite_token, sends an invite email via notifier.send_invite_email, and HTMX-swaps an inline `<code>` invite URL fragment (D-09)"
    - "DELETE /admin/invites/{token_hash} revokes a pending invite via auth_store.revoke_invite"
    - "GET /healthz/last-cycle returns {'status','cycle_date','users':[...]} from state['last_cycle'] — admin-gated via require_admin Depends; registered as standalone route in web/routes/healthz.py (D-15, per /gsd-discuss-phase decision)"
    - "PATCH /settings/email-prefs persists email_enabled (bool) and pause_until (ISO date string or None) to state['users'][uid] via mutate_user_state (UMAIL-04 / D-13)"
    - "Admin dashboard nav has a 'Users' link pointing to /admin/users (D-10)"
    - "Dashboard Settings section renders email-prefs form (D-11/D-12) with a checkbox + native <input type='date'> picker"
    - "main.py calls per_user_fanout.run(state, run_date.strftime('%Y-%m-%d')) AFTER daily_run.run_daily_check returns rc==0 and AFTER _dispatch_email_and_maintain_warnings completes"
    - "PublicUserSummary.last_seen_date is populated from the most-recent trusted device last_seen across the user's TrustedDevice rows"
  artifacts:
    - path: "web/routes/admin/__init__.py"
      provides: "Accept-negotiated GET /admin/users; POST /admin/invites; DELETE /admin/invites/{token_hash}"
      contains: "/admin/invites"
    - path: "web/routes/healthz.py"
      provides: "GET /healthz/last-cycle (admin-gated via require_admin Depends) returning state['last_cycle'] outcomes (D-15)"
      contains: "/healthz/last-cycle"
    - path: "web/routes/admin/_renderers.py"
      provides: "_render_admin_users_page, _render_invite_url_fragment HTML helpers"
    - path: "web/routes/admin/_models.py"
      provides: "PendingInviteSummary Pydantic model; PublicUserSummary.last_seen_date populated"
    - path: "web/routes/dashboard/__init__.py"
      provides: "PATCH /settings/email-prefs handler wired into register()"
    - path: "main.py"
      provides: "per_user_fanout.run() call AFTER run_daily_check + dispatch"
      contains: "per_user_fanout.run"
  key_links:
    - from: "main.py"
      to: "per_user_fanout.run"
      via: "synchronous call after daily-run success + warnings dispatch"
      pattern: "per_user_fanout\\.run\\("
    - from: "web/routes/admin/__init__.py::admin_issue_invite"
      to: "auth_store.mint_invite_token + notifier.send_invite_email"
      via: "mint then email; HTMX swap shows URL inline"
      pattern: "mint_invite_token.*send_invite_email"
    - from: "web/routes/admin/__init__.py::admin_revoke_invite"
      to: "auth_store.revoke_invite"
      via: "HTMX delete with hx-confirm"
      pattern: "revoke_invite"
    - from: "web/routes/dashboard/__init__.py::patch_email_prefs"
      to: "state_manager.mutate_user_state"
      via: "per-user flock state write"
      pattern: "mutate_user_state"
---

<objective>
Wire the remaining Phase 37 surface: admin HTML user-management page with invite issue/revoke + `/healthz/last-cycle` endpoint (registered in web/routes/healthz.py with explicit `require_admin` Depends per D-15), dashboard email-preferences form, admin nav link, last_seen_date population on PublicUserSummary, AND the `main.py` call into `per_user_fanout.run(...)` that completes UMAIL-01 end-to-end.

Purpose: Plans 02–04 built the orchestrator, auth_store extensions, and public invite wizard. This plan exposes everything to the admin and the F&F user via the dashboard, and turns on the daily fan-out by wiring it into the production loop.

Output: Admin can issue + revoke invites and see per-cycle outcomes; user can toggle daily email + set pause-until from dashboard; daily cycle dispatches per-user emails through per_user_fanout; all Phase 37 test files flip from skipped/RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-CONTEXT.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md
@.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md
@web/routes/admin/__init__.py
@web/routes/admin/_models.py
@web/routes/dashboard/__init__.py
@web/routes/dashboard/_renderers.py
@web/dependencies.py
@main.py
@state_manager/__init__.py

<interfaces>
From Plans 02–04 (already landed):
  - `per_user_fanout.run(state: dict, run_date: str) -> list[dict]` — synchronous entry point
  - `notifier.send_invite_email(to_email: str, invite_url: str) -> SendStatus`
  - `notifier.send_per_user_email`, `notifier.send_cycle_summary_email`
  - `auth_store.hash_password`, `verify_password`, `_peek_invite_token`, `list_pending_invites`, `revoke_invite`, `mint_invite_token`, `consume_and_create_user`

From existing admin router (web/routes/admin/__init__.py):
  - `router = APIRouter(prefix='/admin', dependencies=[Depends(require_admin)])` — all new routes inherit require_admin
  - `@router.get('/users', response_model=list[PublicUserSummary])` — Phase 36 JSON endpoint (extend with Accept-negotiation, do NOT rename)
  - `@router.patch('/users/{uid}/disable')` — Phase 36 endpoint (unchanged)
  - `@router.get('/ping')` — Phase 35 sentinel (unchanged)

From web/dependencies.py:
  - `current_user_id` — Depends factory returning the authenticated user_id
  - `require_admin` — Depends factory enforcing admin role; raises 403 otherwise

From web/routes/healthz.py (existing):
  - `register(app: FastAPI) -> None` — Phase 11 standalone-route pattern (no APIRouter); currently exposes GET /healthz. Phase 37 ADDS a second route GET /healthz/last-cycle in the same module's register() closure with `require_admin` Depends. The existing GET /healthz remains public (unchanged); only /healthz/last-cycle is admin-gated.

From state_manager:
  - `mutate_user_state(uid: str, mutator: Callable[[dict], None], path: Path | None = None) -> None` — per-user flock + atomic write

From main.py (relevant call sites):
  - Around line ~109: `rc, state, old_signals, run_date = run_daily_check(args)` (force/test path)
  - Line ~114: `_dispatch_email_and_maintain_warnings(state, old_signals, run_date, ...)`
  - Around line ~122: `rc, once_state, _old_signals, _run_date = run_daily_check(args)` (--once path)
  - Around line ~130: `_run_daily_check_caught(run_daily_check, args)` (scheduler loop path via daily_run_helpers)

Per-cycle outcome shape (from Plan 02):
  - state['last_cycle'] = {'date': 'YYYY-MM-DD', 'users': [{'uid','ok','reason'}, ...]}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin HTML /admin/users page + POST/DELETE /admin/invites + GET /healthz/last-cycle (in web/routes/healthz.py) + PendingInviteSummary + last_seen_date population</name>
  <files>web/routes/admin/__init__.py, web/routes/admin/_models.py, web/routes/admin/_renderers.py, tests/test_web_admin_invite.py</files>
  <read_first>
    - web/routes/admin/__init__.py (current router; admin_list_users + admin_disable_user)
    - web/routes/admin/_models.py (PublicUserSummary; last_seen_date deferred field)
    - auth_store/_users.py (list_users, list_pending_invites, mint_invite_token, revoke_invite — Plan 03 surface)
    - notifier/dispatch.py (send_invite_email — Plan 02 surface)
    - state_manager/__init__.py (load_state shape — state['last_cycle'] read path)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §web/routes/admin/__init__.py and §_models.py (verbatim diffs)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md §Surface 1 (admin users page) and §Copywriting + §Interaction Contracts
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md §Pattern 5 (Accept-header negotiation)
    - CLAUDE.md
  </read_first>
  <behavior>
    - TestAdminInviteIssue.test_post_admin_invites_mints_token: POST /admin/invites with email=`new@x.com` (admin-authenticated) returns 200; `auth_store.list_pending_invites()` now includes a row with email=`new@x.com` and `consumed=False`
    - TestAdminInviteIssue.test_post_admin_invites_sends_invite_email: same POST captures one call to `notifier.send_invite_email`; first kwarg/positional == `'new@x.com'`; second arg is a string containing `/accept-invite?token=` and the BASE_URL prefix
    - TestAdminInviteIssue.test_post_admin_invites_returns_inline_invite_url_fragment: response body contains a `<code>` element AND the full invite URL (HTMX swap fragment per D-09 / UI-SPEC)
    - TestAdminInviteIssue.test_post_admin_invites_requires_admin_role: same POST without admin role returns 403 (require_admin Depends inherited from router)
    - TestAdminInviteRevoke.test_delete_admin_invite_marks_consumed: DELETE /admin/invites/{token_hash} returns 200; the row in `list_pending_invites()` for that token_hash has `consumed=True`
    - TestAdminInviteRevoke.test_delete_admin_invite_404_for_unknown_hash: DELETE /admin/invites/nonexistent returns 404
    - TestAdminInviteRevoke.test_delete_admin_invite_requires_admin_role: DELETE without admin role returns 403
    - TestLastCycle.test_last_cycle_returns_empty_when_no_cycle: GET /healthz/last-cycle with state['last_cycle'] absent returns `{"status":"ok","cycle_date":null,"users":[]}` (route registered in web/routes/healthz.py per D-15)
    - TestLastCycle.test_last_cycle_returns_per_user_outcomes: with state['last_cycle'] = {'date':'2026-05-14','users':[{'uid':'u1','ok':True,'reason':None}]}, GET /healthz/last-cycle returns that data unchanged
    - TestLastCycle.test_last_cycle_requires_admin_role: GET /healthz/last-cycle without admin returns 403 (require_admin Depends explicit on the route)
    - Accept-negotiation test: GET /admin/users with `Accept: text/html` returns `Content-Type: text/html` and body contains "Users" heading + "Issue invite" form
    - Accept-negotiation test: GET /admin/users with `Accept: application/json` returns JSON list[PublicUserSummary] (unchanged Phase 36 behaviour)
    - last_seen_date population: a fixture User with two trusted devices (last_seen='2026-05-10T00:00:00Z' and '2026-05-13T00:00:00Z') produces PublicUserSummary.last_seen_date='2026-05-13' (most-recent device); a user with zero trusted devices produces last_seen_date=None
  </behavior>
  <action>
    Step 1 — `web/routes/admin/_models.py`: add `PendingInviteSummary(BaseModel)` with fields `token_hash: str`, `email: str`, `invited_by: str`, `created_at: str`, `expires_at: str`, `consumed: bool` (use existing Pydantic BaseModel import). Document in docstring that this is a publicly-safe summary — no raw token.

    Step 2 — `web/routes/admin/_models.py`: update `PublicUserSummary.last_seen_date` field to be derived from the user's trusted devices. Add a private helper `_compute_last_seen_date(uid: str, trusted_devices_for_user: list) -> str | None` that returns the max `last_seen` ISO date (YYYY-MM-DD substring) across the list, or None if empty. The caller in `admin_list_users` will pass the relevant list. Add a unit test `test_compute_last_seen_date` covering empty, single, multiple cases.

    Step 3 — Create `web/routes/admin/_renderers.py` (new file). Implement two render helpers using the same inline-CSS token block as `dashboard.html` and the existing admin layout (mirror the Phase 36 admin patterns; if no separate renderer file exists yet, this is the home for them):
      - `_render_admin_users_page(summaries: list[PublicUserSummary], pending: list) -> str` — full HTML per 37-UI-SPEC.md Surface 1: page heading "Users", issue-invite form ABOVE tables, pending invites table (Email · Issued · Expires · Actions), users table (UID · Email · Status · Last seen · Actions). Each pending row has a `<button hx-delete="/admin/invites/{token_hash}" hx-confirm="Revoke this invite? The link will stop working immediately." class="btn-row btn-close">Revoke invite</button>`. Each user row has a disable button per UI-SPEC. All dynamic values pass through `html.escape(.., quote=True)`. Empty state rows per copy spec.
      - `_render_invite_url_fragment(invite_url: str, email: str, expires_at: str) -> str` — HTMX swap fragment per UI-SPEC: `<div class="banner-success" role="status" aria-live="polite"><p>Invite sent to {email}. Copy the link below if needed:</p><code class="invite-url">{invite_url}</code></div>`. Escape email; escape invite_url (it's a URL, but still escape per defense-in-depth). NO inline JS.

    Step 4 — `web/routes/admin/__init__.py`: extend the router (do not introduce a parallel one — the require_admin gate is inherited).
      - Modify `admin_list_users` to accept `request: Request` and inspect `request.headers.get('accept', '')`. If `'text/html' in accept`: build summaries (including last_seen_date populated from `auth_store.list_users()` row's `trusted_devices` field — or a new `auth_store.list_trusted_devices_for_uid(uid)` helper if cleaner; either way, populate `last_seen_date` via `_compute_last_seen_date`); call `list_pending_invites()`; render via `_render_admin_users_page`; return `HTMLResponse(html)`. Else: keep the existing Phase 36 JSON behavior (`response_model=list[PublicUserSummary]` returns the list).
      - Add `@router.post('/invites')` handler `admin_issue_invite(request: Request, email: str = Form(...), admin_uid: str = Depends(current_user_id))`. Read `BASE_URL = os.environ.get('BASE_URL', '').strip()`. If empty: raise HTTPException(500, 'BASE_URL not configured'). Call `raw_token, expires_at = mint_invite_token(invited_by_uid=admin_uid, email=email)`. Construct `invite_url = f'{base_url}/accept-invite?token={raw_token}'`. Call `send_invite_email(to_email=email, invite_url=invite_url)` — never raises; result ignored for the route's success path (admin gets a separate cycle-summary signal if dispatch fails). Return `HTMLResponse(_render_invite_url_fragment(invite_url, email, expires_at))`.
      - Add `@router.delete('/invites/{token_hash}')` handler `admin_revoke_invite(token_hash: str)`. Call `revoke_invite(token_hash)`. If False: raise HTTPException(404, 'invite not found'). Return `{'ok': True, 'token_hash': token_hash}` (HTMX-compatible JSON; the table-row removal is handled by client-side `hx-swap="outerHTML"` from the `<button>` element's enclosing `<tr>`).
      - (Do NOT add /last-cycle to the admin router — per D-15 and the revision-mode resolution, GET /healthz/last-cycle is implemented in web/routes/healthz.py instead, see Step 5 below.)

    Step 5 — `web/routes/healthz.py`: extend the existing module to add a second route `GET /healthz/last-cycle` registered inside the same `register(app)` closure as the existing `/healthz` route (follow the existing `@app.get('/healthz')` pattern; do NOT introduce an APIRouter). The new route MUST be admin-gated via an EXPLICIT `Depends(require_admin)` (import `from web.dependencies import require_admin` at function-local scope to keep module-top imports stable). The existing public `GET /healthz` route remains unchanged. Handler signature: `@app.get('/healthz/last-cycle')` `def healthz_last_cycle(_: None = Depends(require_admin)) -> dict`. Body: local-import `from state_manager import load_state`; `state = load_state()`; `lc = state.get('last_cycle')`; if `lc is None`: return `{'status': 'ok', 'cycle_date': None, 'users': []}`; else: return `{'status': 'ok', 'cycle_date': lc.get('date'), 'users': lc.get('users', [])}`. Wrap in try/except `# noqa: BLE001` returning the empty-cycle response on load_state failure (mirror the existing /healthz never-crash D-19 pattern). Update the module docstring to note "Phase 11 D-13..D-19 (/healthz) + Phase 37 D-15 (/healthz/last-cycle, admin-gated)".

    Step 6 — `tests/test_web_admin_invite.py`: replace all Wave 0 skip stubs with real test bodies covering the behaviors above. Use the existing `auth_headers` fixture for admin-authenticated requests (admin role is the default in conftest); use a new helper or fixture to construct a non-admin TestClient for the 403 cases.

    Constraints: 2-space indent. The DELETE handler does NOT need a flock (Phase 34 D-01 — revoke is idempotent race-safe). The POST handler MUST log a structured `[Invite] issued by_admin=<uid> to_email=<email>` line at INFO — but MUST NOT log the raw token. `html.escape(quote=True)` on every interpolation in renderers.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_web_admin_invite.py -x --tb=short` returns 0 with all classes GREEN
    - `grep -nE "@router\.(get|post|delete)\('/invites" web/routes/admin/__init__.py` returns >= 2 hits (POST /invites, DELETE /invites/{token_hash}); `/last-cycle` MUST NOT appear in the admin router (`grep -n "/last-cycle" web/routes/admin/__init__.py` returns empty)
    - `grep -nE "@app\.get\('/healthz/last-cycle'\)" web/routes/healthz.py` returns 1 hit
    - `grep -n "require_admin" web/routes/healthz.py` returns at least 1 hit (explicit Depends on the new route)
    - Existing Phase 36 admin tests stay green: `.venv/bin/pytest tests/test_web_admin.py -x --tb=short` returns 0
    - `grep -nE "logger\.(info|debug).*raw_token|logger\.(info|debug).*token=[a-zA-Z0-9_-]{10,}" web/routes/admin/__init__.py` returns empty (no raw-token logging)
    - `grep -c "html.escape" web/routes/admin/_renderers.py` >= 4; every `html.escape(...)` uses `quote=True` (`grep -nE "html\.escape\([^)]*\)" web/routes/admin/_renderers.py | grep -v "quote=True"` returns empty)
    - `PendingInviteSummary` importable from web/routes/admin/_models: `python -c "from web.routes.admin._models import PendingInviteSummary, PublicUserSummary; print('ok')"` prints `ok`
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_admin_invite.py tests/test_web_admin.py -x --tb=short</automated>
  </verify>
  <done>Admin /admin/users HTML page renders; POST/DELETE /admin/invites work via HTMX; GET /healthz/last-cycle (in web/routes/healthz.py with explicit require_admin Depends per D-15) exposes per-user cycle outcomes; PendingInviteSummary model exists; PublicUserSummary.last_seen_date populated from trusted devices; existing JSON endpoint unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH /settings/email-prefs dashboard route + Settings section render + admin nav 'Users' link</name>
  <files>web/routes/dashboard/__init__.py, web/routes/dashboard/_renderers.py, tests/test_web_dashboard_email_prefs.py</files>
  <read_first>
    - web/routes/dashboard/__init__.py (register(app) closure; existing PATCH/POST HTMX handlers)
    - web/routes/dashboard/_renderers.py (settings section render entry point; how other sections are composed)
    - state_manager/__init__.py (mutate_user_state signature; load_user_state)
    - web/dependencies.py (current_user_id factory)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §web/routes/dashboard/__init__.py (PATCH /settings/email-prefs verbatim handler) and §Shared Patterns mutate_user_state for HTMX Mutations
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-UI-SPEC.md §Surface 6 (Dashboard Settings — Email preferences section)
    - CLAUDE.md
  </read_first>
  <behavior>
    - TestPatchEmailPrefs.test_patch_persists_email_enabled_true: PATCH /settings/email-prefs with form `email_enabled=on, pause_until=` (authenticated as user u1) returns 200; `load_user_state('u1')['email_enabled'] == True`; `load_user_state('u1').get('pause_until') is None`
    - TestPatchEmailPrefs.test_patch_persists_email_enabled_false: PATCH with form `email_enabled=` (checkbox unchecked → absent or empty) results in `load_user_state('u1')['email_enabled'] == False`
    - TestPatchEmailPrefs.test_patch_persists_pause_until_iso_date: PATCH with form `email_enabled=on, pause_until=2026-06-01` returns 200; `load_user_state('u1')['pause_until'] == '2026-06-01'`
    - TestPatchEmailPrefs.test_patch_rejects_invalid_pause_date: PATCH with `pause_until=not-a-date` returns 200 (form silently coerces to None) AND `load_user_state('u1').get('pause_until') is None` — invalid input is silently dropped rather than raising
    - TestPatchEmailPrefs.test_patch_requires_authenticated_uid: PATCH without authentication returns 401 (AuthMiddleware enforces; the route is not in PUBLIC_PATHS)
    - Settings section render: a GET on the dashboard settings page (whatever route currently renders dashboard-settings.html) includes a `<form hx-patch="/settings/email-prefs">` with `<input id="email-enabled" name="email_enabled" type="checkbox">` and `<input id="pause-until" name="pause_until" type="date">` matching UI-SPEC Surface 6
    - Admin nav: an admin-authenticated GET on the dashboard root includes a `<a href="/admin/users">Users</a>` link (D-10); a non-admin user's dashboard does NOT include this link
  </behavior>
  <action>
    Step 1 — `web/routes/dashboard/__init__.py`: inside the existing `register(app)` closure, add a new handler `@app.patch('/settings/email-prefs')` following 37-PATTERNS.md §web/routes/dashboard/__init__.py verbatim:
      - Signature: `def patch_email_prefs(request: Request, email_enabled: str = Form(default=''), pause_until: str = Form(default=''), uid: str = Depends(current_user_id)) -> Response`
      - Parse: `enabled_bool = (email_enabled == 'on' or email_enabled == '1')`; strip pause_until; if non-empty, validate via `date.fromisoformat` inside try/except — on ValueError set pause_date=None.
      - Define mutator `_apply(state)`: `users = state.setdefault('users', {}); bucket = users.setdefault(uid, {}); bucket['email_enabled'] = enabled_bool; bucket['pause_until'] = pause_date`
      - Call `mutate_user_state(uid, _apply)` — per Phase 36 D-01, this acquires the per-user flock outer + state.json flock inner.
      - Return `HTMLResponse(content='<p>Email preferences saved.</p>')` (HTMX swap-none target, just needs a success body).

    Step 2 — `web/routes/dashboard/_renderers.py`: add a new render helper for the email-prefs section, OR locate the existing dashboard-settings template and inject the form HTML per 37-UI-SPEC.md Surface 6. The form posts via `hx-patch="/settings/email-prefs"` with `hx-headers='{"X-Trading-Signals-Auth": "{{WEB_AUTH_SECRET}}"}'` for CSRF parity. Inputs: checkbox `email_enabled`, native `<input type="date" id="pause-until" name="pause_until" value="{{current}}" min="{{today_iso}}">`. The `min` attribute prevents back-dating pauses. The current values are read from `load_user_state(uid)` and passed to the renderer.

    Step 3 — Admin nav link: in the dashboard nav render (locate the existing nav rendering — likely in `web/routes/dashboard/_renderers.py` or a shared layout helper), add a conditional that emits `<a href="/admin/users">Users</a>` if and only if the current request's user has admin role. The admin-role check should reuse `require_admin` logic — but at render time, this is a simple inspection of the user's role field. Implement: read `auth_store.get_user(uid).get('role') == 'admin'`. If admin, include the nav link; if not, omit it.

    Step 4 — `tests/test_web_dashboard_email_prefs.py`: replace all Wave 0 skip stubs in TestPatchEmailPrefs with real test bodies. Authenticate as a non-admin user (use a fixture user `u_ff` with role='ff' — create in conftest if needed, or use isolated_auth_json + create_user). For the 401 test, use a TestClient with no session cookie.

    Constraints: 2-space indent. Use mutate_user_state (NOT raw mutate_state — Phase 36 D-01). Do NOT save_state directly. The handler must NOT crash on malformed pause_until input; silently coerce to None and let the form re-render with the field empty.
  </action>
  <acceptance_criteria>
    - `grep -n "@app.patch('/settings/email-prefs')" web/routes/dashboard/__init__.py` returns at least 1 hit
    - `grep -n "mutate_user_state" web/routes/dashboard/__init__.py` returns at least 1 hit
    - `.venv/bin/pytest tests/test_web_dashboard_email_prefs.py -x --tb=short` returns 0 with TestPatchEmailPrefs GREEN
    - Dashboard renders admin nav link only for admin role (covered by tests in test_web_admin.py or test_web_dashboard.py — verify with a parametrized test if not already covered)
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_web_dashboard_email_prefs.py tests/test_web_dashboard.py tests/test_web_admin.py -x --tb=short</automated>
  </verify>
  <done>PATCH /settings/email-prefs persists email_enabled + pause_until via mutate_user_state; dashboard Settings section renders the email-prefs form; admin nav has a 'Users' link visible only to admin role (D-10 verified via tests/test_web_admin.py).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire per_user_fanout.run into main.py after daily_run + dispatch; regression-test W3 invariant + dispatch ordering</name>
  <files>main.py, tests/test_main.py</files>
  <read_first>
    - main.py (call sites at lines ~109, ~114, ~122, ~130 — three orchestration entry points: --force-email/--test, --once, scheduler-loop)
    - daily_run.py (run_daily_check return shape: (rc, state, old_signals, run_date))
    - daily_run_helpers.py (_dispatch_email_and_maintain_warnings signature)
    - per_user_fanout.py (Plan 02 — public surface `run(state, run_date)`)
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-PATTERNS.md §per_user_fanout.py call-site-in-main.py
    - .planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-RESEARCH.md §Anti-Patterns (per_user_fanout call ordering — must run AFTER daily_run dispatch completes; do NOT call from FastAPI route)
    - tests/test_main.py (existing W3-invariant test for daily-run path; existing weekday-gate freeze_time pattern per LEARNINGS Calendar-sensitive tests)
    - CLAUDE.md
  </read_first>
  <behavior>
    - main.py wires per_user_fanout: in the --force-email/--test path (around line 109) and the scheduler-loop path (around line 130 / via daily_run_helpers), after `_dispatch_email_and_maintain_warnings` returns successfully (rc==0 and state is not None), call `per_user_fanout.run(state, run_date.strftime('%Y-%m-%d'))`. The --once path (line ~122) also calls it.
    - W3 invariant across the whole cycle: full main.py execution against a fixture state with 2 active F&F users performs exactly TWO `mutate_state` calls — one inside daily_run (#1) and one inside per_user_fanout.run (#2). A new regression test in test_main.py asserts this count.
    - Calendar-sensitive: every new test that exercises run_daily_check OR per_user_fanout uses `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')` (a Monday) per LEARNINGS "Calendar-sensitive tests must use freeze_time".
    - Dispatch ordering: `run_daily_check` → `_dispatch_email_and_maintain_warnings` → `per_user_fanout.run` (verified by a test that captures the call order via monkeypatched function spies)
    - Failure isolation: if `per_user_fanout.run` raises (it shouldn't — it's never-raise by design but defense in depth), main.py logs the error and continues to exit normally; the daily cycle is NOT marked as failed because the F&F fan-out is an additive layer.
    - Test (existing W3 regression): `tests/test_main.py::TestW3Invariant` (if it exists) updated to expect 2 calls instead of the pre-Phase-37 count (adjust ONLY if the existing test's invariant changes; otherwise add a new test class `TestPhase37W3Invariant` covering the full cycle).
    - Test (TestFetchCountInvariant — turn Plan 01 stub GREEN, UMAIL-01 SC-1): With 3 F&F users active and `main.data_fetcher.fetch_ohlcv` monkeypatched by a counter that increments on every call and returns the existing fixture DataFrame (mirror `_install_fixture_fetch` / `_tracking_fetch` pattern in tests/test_main.py at line ~130), running the full --force-email path through main.main asserts the counter == 2 (one fetch for SPI200, one for BHP). The number of F&F users MUST NOT change the fetch count — fan-out reads from shared `state['signals']` already populated by daily_run, NEVER refetches yfinance per user. The test uses `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')` (Monday).
  </behavior>
  <action>
    Step 1 — `main.py`: import `per_user_fanout` at the top with the existing app-module imports (after `daily_run` but maintain alphabetical or logical grouping). Use `import per_user_fanout` (NOT `from per_user_fanout import run`) to preserve test monkeypatch-via-module-path conventions.

    Step 2 — `main.py` --force-email / --test path (around line 109): after `_dispatch_email_and_maintain_warnings(state, old_signals, run_date, ...)` completes, add:
      ```
      if rc == 0 and state is not None and run_date is not None:
        try:
          per_user_fanout.run(state, run_date.strftime('%Y-%m-%d'))
        except Exception as exc:  # noqa: BLE001 — fan-out is additive; never abort main on its failure
          logger.warning('[FanOut] WARN per_user_fanout.run failed: %s: %s', type(exc).__name__, exc)
      ```
      Place this AFTER the dispatch call, BEFORE the function returns.

    Step 3 — `main.py` --once path (around line 122): same pattern, but use `once_state` and `_run_date`.

    Step 4 — Scheduler loop path: `_run_daily_check_caught` (in `daily_run_helpers.py` or wherever it lives — find via grep) unpacks the 4-tuple and dispatches the daily email. Per LEARNINGS G-pattern "Refactor that adds return values must update every caller", verify the wrapper currently calls dispatch on rc==0. Add the same `per_user_fanout.run(...)` call after dispatch in this path. This is the production path; missing it would silently break daily F&F email.

    Step 5 — `tests/test_main.py`: replace the `TestFetchCountInvariant` Wave 0 skip stub (created by Plan 01 Task 4) with a real test method. Use the existing `_install_fixture_fetch` helper as a model: install a fixture-returning fetch BUT wrap it in a counter that increments on every call (`call_count = []; def _tracking_fetch(*args, **kwargs): call_count.append(1); return real_fixture_df`). Monkeypatch via `monkeypatch.setattr(main.data_fetcher, 'fetch_ohlcv', _tracking_fetch)`. Seed state with 3 active F&F users (`{'uid':'u1'..'u3', 'role':'ff', 'email_enabled': True, 'pause_until': None}`). Patch `per_user_fanout.send_per_user_email` to a no-op returning `SendStatus(ok=True, reason=None)`. Run the --force-email path via `main.main(['--force-email'])` (or the existing harness used elsewhere in TestCLI). Assert `len(call_count) == 2`. The test uses `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')`. After turning GREEN, the Plan 01 stub line `pytest.skip('Wave 0 stub — implementation lands in Plan 37-05 Task 3 alongside per_user_fanout wiring')` is REMOVED.

    Step 6 — `tests/test_main.py`: add `TestPhase37W3Invariant` test class covering:
      - Full --force-email simulation with a fixture state having 2 F&F users; assert `mutate_state` is called exactly 2 times (1 inside daily_run, 1 inside per_user_fanout.run).
      - Test call ordering: monkeypatch run_daily_check, _dispatch_email_and_maintain_warnings, per_user_fanout.run with spies that append to a shared `call_order` list; assert `call_order == ['run_daily_check', '_dispatch_email_and_maintain_warnings', 'per_user_fanout.run']`.
      - Test that per_user_fanout.run raising RuntimeError does NOT cause main.main to return a non-zero rc.
      - All tests use `@pytest.mark.freeze_time('2026-05-11T00:00:00+00:00')`.

    Constraints: 2-space indent. Do NOT swallow daily_run errors; only swallow per_user_fanout errors (because it's additive). Maintain the existing rc semantics (rc==0 means daily cycle succeeded; F&F fan-out failure is logged but does not flip rc).
  </action>
  <acceptance_criteria>
    - `grep -c "per_user_fanout.run" main.py` >= 2 (force-email + once paths; scheduler-loop path may live in daily_run_helpers)
    - `grep -c "per_user_fanout.run\|per_user_fanout\.run" daily_run_helpers.py main.py 2>/dev/null` >= 3 (cover all three orchestration entry points)
    - `.venv/bin/pytest tests/test_main.py -x --tb=short` returns 0 with new TestPhase37W3Invariant GREEN
    - `.venv/bin/pytest tests/test_main.py::TestPhase37W3Invariant -x --tb=short` shows exactly the expected call order and 2-mutate_state count
    - `.venv/bin/pytest tests/test_main.py::TestFetchCountInvariant -x --tb=short` returns 0 (formerly skipped Plan 01 stub now GREEN; UMAIL-01 SC-1 fetch-count invariant proven)
    - `grep -A3 "class TestFetchCountInvariant" tests/test_main.py | grep -q "pytest.skip"` returns NO match (the Wave 0 skip line is removed)
    - Full suite stays green: `.venv/bin/pytest -x --tb=short` returns 0
    - `python -c "import main; assert hasattr(main, 'per_user_fanout')"` exits 0 (module import is wired)
    - LEARNINGS compliance: every new test in test_main.py that touches run_daily_check uses freeze_time (`grep -B5 "def test_" tests/test_main.py | grep "freeze_time" | wc -l` is non-zero for added tests)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_main.py tests/test_per_user_fanout.py -x --tb=short</automated>
  </verify>
  <done>main.py wires per_user_fanout.run after daily_run + dispatch on all three orchestration paths; W3 invariant proven across the full cycle (exactly 2 mutate_state calls); call-ordering test pins run_daily_check → _dispatch_email_and_maintain_warnings → per_user_fanout.run; fan-out failure does not abort the cycle. Phase 37 end-to-end UMAIL-01..04 wiring complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTTP → POST /admin/invites | Authenticated admin; email param goes into invite mint + outbound email |
| Admin HTTP → DELETE /admin/invites/{hash} | Authenticated admin; token_hash is a path param — sha256: prefix verified by Phase 34 storage layer |
| Admin HTTP → GET /healthz/last-cycle | Admin-gated via explicit `Depends(require_admin)` in web/routes/healthz.py; the parent /healthz route remains public (D-19 contract preserved) |
| Authenticated F&F HTTP → PATCH /settings/email-prefs | uid via Depends(current_user_id); writes per-user state via mutate_user_state (per-user flock) |
| main.py → per_user_fanout.run | Internal call; never-raise contract; W3 invariant terminal mutate_state |
| Dashboard render → admin nav | Role-based conditional rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Elevation of Privilege | /admin/* reachable by non-admin | mitigate | `router = APIRouter(prefix='/admin', dependencies=[Depends(require_admin)])` — Phase 35 invariant. New routes inherit gate. Acceptance tests verify 403 for non-admin. |
| T-37-05-02 | Tampering | Cross-user write via PATCH /settings/email-prefs (user A writes user B's prefs) | mitigate | Handler reads `uid: str = Depends(current_user_id)` — the authenticated user only; no path param or query param controls the target uid. `mutate_user_state(uid, ...)` writes only to `state['users'][uid]`. Phase 36 D-02 invariant. |
| T-37-05-03 | Information Disclosure | Raw invite token in admin response body | accept | D-09 EXPLICITLY requires inline display of the raw invite URL on the admin page for copy-paste. The token leaves the server only to admin (authenticated) AND to the invitee via email. Token is single-use (Phase 34 flock); compromise window is bounded by 7-day TTL. |
| T-37-05-04 | Information Disclosure | Raw invite token in admin log lines | mitigate | Acceptance criteria grep gate: no log statement in `admin_issue_invite` interpolates the raw token. Only `[Invite] issued by_admin=<uid> to_email=<email>` is logged. |
| T-37-05-05 | Information Disclosure | bcrypt password_hash on PATCH /settings/email-prefs response | not applicable | This handler does not touch password_hash at all. |
| T-37-05-06 | Tampering | CSRF on PATCH /settings/email-prefs | mitigate | Existing session cookie is SameSite=Strict; HTMX form carries `hx-headers` with WEB_AUTH_SECRET. AuthMiddleware verifies on every authenticated request. |
| T-37-05-07 | Information Disclosure | /healthz/last-cycle leaks per-user data to non-admin | mitigate | Explicit `Depends(require_admin)` on the route declaration in web/routes/healthz.py (NOT inherited from a router); non-admin → 403. Test `TestLastCycle.test_last_cycle_requires_admin_role` verifies. |
| T-37-05-08 | Denial of Service | Malformed pause_until crashes the route | mitigate | Try/except around `date.fromisoformat`; invalid input coerced to None; handler always returns 200. |
| T-37-05-09 | Information Disclosure | Admin nav link visible to non-admin | mitigate | Role check at render time via `auth_store.get_user(uid).get('role') == 'admin'`; non-admin dashboards omit the link. Defense-in-depth — even if a non-admin guesses /admin/users, the require_admin gate returns 403. |
| T-37-05-10 | Tampering | per_user_fanout.run from a FastAPI route → asyncio.run inside event loop → RuntimeError | mitigate | Architectural rule documented; per_user_fanout.run is called ONLY from main.py (sync orchestration). Acceptance test verifies call-order; no web route imports per_user_fanout. |
| T-37-05-11 | Denial of Service | per_user_fanout.run failure aborts daily cycle | mitigate | Wrapper try/except in main.py catches any exception, logs it, and returns rc as set by daily_run (not flipped). Acceptance test verifies. |
| T-37-05-12 | Information Disclosure | last_seen_date exposes user activity to admin | accept | Admin role legitimately needs to see when users last logged in (RBAC-04 / D-08 admin UX). last_seen_date is the most-recent trusted-device last_seen ISO date — date-only granularity. No equity figures, no trade content. PublicUserSummary contract from Phase 36 D-12 enforces this. |
| T-37-05-13 | Repudiation | revoke_invite produces no audit trail | accept | Phase 34 D-01 accepts; `consumed_at` ISO timestamp on the row is the audit record. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_web_admin_invite.py tests/test_web_dashboard_email_prefs.py tests/test_main.py tests/test_per_user_fanout.py tests/test_web_invite.py tests/test_auth_store_users.py -x --tb=short` returns 0
- `.venv/bin/pytest -x --tb=short` full suite returns 0
- Coverage map: RBAC-03 covered by tests/test_web_invite.py + tests/test_web_admin_invite.py + tests/test_auth_store_users.py; UMAIL-01 covered by tests/test_per_user_fanout.py::TestFanOutEmail + tests/test_main.py::TestPhase37W3Invariant + tests/test_main.py::TestFetchCountInvariant (SC-1 fetch-count); UMAIL-02 by TestCrashBoundary + TestW3Invariant + TestLastCycle (GET /healthz/last-cycle); UMAIL-03 by TestSemaphoreThrottle + TestRFC8058Headers; UMAIL-04 by TestEmailPrefsSkip + TestPatchEmailPrefs
- All five plans (37-01..37-05) deliverables present and committed
- No raw invite token in any log line across web/routes/admin, web/routes/invite, notifier/dispatch
</verification>

<success_criteria>
- Admin /admin/users HTML page renders with invite issue form + pending invites table + users table
- POST /admin/invites mints + emails + HTMX-swaps inline invite URL
- DELETE /admin/invites/{hash} revokes (HTMX hx-confirm protected)
- GET /healthz/last-cycle returns per-user outcomes JSON (admin-gated via require_admin Depends; registered in web/routes/healthz.py per D-15)
- PATCH /settings/email-prefs persists email_enabled + pause_until via mutate_user_state
- Admin nav has Users link visible only to admin role
- last_seen_date populated from trusted devices in PublicUserSummary
- main.py calls per_user_fanout.run on all three orchestration paths (force/test, once, scheduler-loop)
- W3 invariant proven: exactly 2 mutate_state calls per cycle end-to-end
- Call-ordering test pins run_daily_check → _dispatch_email_and_maintain_warnings → per_user_fanout.run
- per_user_fanout failure does not abort the daily cycle
- Full suite green
- All Phase 37 requirements covered: RBAC-03, UMAIL-01, UMAIL-02, UMAIL-03, UMAIL-04
</success_criteria>

<output>
After completion, create `.planning/phases/37-per-user-email-fan-out-admin-invite-disable-routes-invite-ac/37-05-SUMMARY.md` documenting:
- Admin route additions (POST /invites, DELETE /invites/{hash}, GET /last-cycle, HTML on /users)
- Dashboard route additions (PATCH /settings/email-prefs, admin nav link)
- main.py wiring diff (3 call sites)
- W3 invariant proven (test counts mutate_state == 2)
- Call ordering proven
- Confirmation no raw token leaks to logs
- Phase 37 ready for `/gsd-verify-work 37`
</output>
