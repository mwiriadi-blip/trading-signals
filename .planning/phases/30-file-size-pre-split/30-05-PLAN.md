---
plan: "30-05"
phase: 30
wave: 1
depends_on: []
autonomous: true
files_modified:
  - web/routes/login.py
  - web/routes/login/__init__.py
  - web/routes/login/_renderers.py
requirements:
  - OPS-01
must_haves:
  truths:
    - "web/routes/login.py (single-file 608 LOC) no longer exists; web/routes/login/ package replaces it"
    - "`from web.routes import login as login_route` in web/app.py still resolves; `login_route.register(app)` still works"
    - "`from web.routes.login import _is_safe_next` (consumed by web/routes/totp.py:43 and any future cross-route import) continues to resolve via __init__.py re-export"
    - "Every daughter file is ≤500 LOC"
    - "Full test suite passes for login routes (auth flow, forgot-2fa, magic-link, logout)"
  artifacts:
    - path: "web/routes/login/__init__.py"
      provides: "register(app) + import surface re-exports (includes _is_safe_next for sibling-route imports)"
      contains: "def register"
    - path: "web/routes/login/_renderers.py"
      provides: "All render helpers (_render_login_form, _render_forgot_2fa_form, _render_check_email_page, _render_logout_confirmation, _log_login_failure) + _is_safe_next helper"
      contains: "_render_login_form"
  key_links:
    - from: "web/app.py:44"
      to: "web.routes.login.register"
      via: "package __init__.py export"
      pattern: "from web.routes import login as login_route"
    - from: "web/routes/totp/__init__.py (after Plan 30-04 lands)"
      to: "web.routes.login._is_safe_next"
      via: "__init__.py re-export"
      pattern: "from web.routes.login import _is_safe_next"
---

<objective>
Convert `web/routes/login.py` (608 LOC) into a `web/routes/login/` package with two daughter files (`__init__.py`, `_renderers.py`), each ≤500 LOC. Behaviour-preserving — zero changes to login form, basic-auth flow, cookie-session minting, forgot-2fa flow, magic-link email flow, or logout confirmation.

Purpose: Phase 31+ user_id injection on a 608-LOC login module produces noisy diffs; splitting first keeps later diffs clean.

**Cross-module dependency surface:** `web/routes/totp.py:43` (becoming `web/routes/totp/__init__.py` after Plan 30-04) does `from web.routes.login import _is_safe_next`. This plan's `__init__.py` MUST re-export `_is_safe_next` so totp's cross-route import keeps resolving. D-03 import-surface preservation is the contract.

Output: One deleted file (`web/routes/login.py`), two new files inside `web/routes/login/`. The `register(app)` function lives in `__init__.py` exactly as before; `_is_safe_next` is re-exported from `__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-file-size-pre-split/30-CONTEXT.md

@web/routes/login.py
@web/app.py

<interfaces>
<!-- Public surface that MUST be preserved -->
<!-- web/app.py imports `from web.routes import login as login_route` then calls .register(app). -->
<!-- web/routes/totp.py (now web/routes/totp/__init__.py) imports `from web.routes.login import _is_safe_next`. -->

External callers rely on these names being importable from `web.routes.login`:
- `register` (callable: `register(app: FastAPI) -> None`)
- `_is_safe_next` (helper, imported by sibling route module totp)

Module-level structure today (web/routes/login.py top-level defs):
- `_is_safe_next(next_value) -> bool` (line 58) — pure helper used by totp.py
- `_render_login_form(...)` (line 212)
- `_render_forgot_2fa_form(error: str | None = None)` (line 263)
- `_render_check_email_page()` (line 312)
- `_render_logout_confirmation()` (line 341)
- `register(app: FastAPI) -> None` (line 363)
- `_log_login_failure(request: Request, reason: str)` (line 584)

Per D-05: all render helpers + `_log_login_failure` move to `_renderers.py`. `_is_safe_next` is not explicitly named in D-05 but per D-03 it MUST remain importable from `web.routes.login` (totp's import). The cleanest location is `_renderers.py` (with `_is_safe_next` placed there alongside the render helpers) and re-exported from `__init__.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split web/routes/login.py into a 2-file package (D-05 boundary)</name>
  <files>web/routes/login.py, web/routes/login/__init__.py, web/routes/login/_renderers.py</files>
  <read_first>
    - web/routes/login.py (entire file — every helper must be inspected to confirm closure-freeness before moving)
    - web/app.py lines 40-55 (confirm `from web.routes import login as login_route` import shape)
    - web/routes/totp.py line 43 (confirm `from web.routes.login import _is_safe_next` — this import path MUST keep resolving after the split)
    - .planning/phases/30-file-size-pre-split/30-CONTEXT.md (D-05 — render helpers move to _renderers.py; D-03 — import surface preserved)
  </read_first>
  <action>
    Convert `web/routes/login.py` into a Python package. Behaviour-preserving — zero edits to login form, basic-auth check, cookie minting, magic-link email content, or any log-line text.

    **Step A — Confirm closure-freeness:**
    Each of `_is_safe_next`, `_render_login_form`, `_render_forgot_2fa_form`, `_render_check_email_page`, `_render_logout_confirmation`, `_log_login_failure` must be confirmed to NOT capture any variable defined inside `register()` (they all look module-level today, but verify by reading the file). If any helper captures a `register()`-local, leave it inside `register()` and surface the surprise.

    **Step B — Create `web/routes/login/_renderers.py`:**
    Move into `_renderers.py`:
    - `_is_safe_next` (line 58 to its closing line — pure helper)
    - `_render_login_form` (line 212-262)
    - `_render_forgot_2fa_form` (line 263-311)
    - `_render_check_email_page` (line 312-340)
    - `_render_logout_confirmation` (line 341-362)
    - `_log_login_failure` (line 584-end)

    Plus the imports those helpers need (urllib.parse for `_is_safe_next`, html, logging, system_params palette, etc. — copy only what `_renderers.py` actually references).

    **Step C — Create `web/routes/login/__init__.py`:**
    Place at the top of `__init__.py`:
    ```
    from web.routes.login._renderers import (
        _is_safe_next,
        _render_login_form,
        _render_forgot_2fa_form,
        _render_check_email_page,
        _render_logout_confirmation,
        _log_login_failure,
    )
    ```
    Plus the FastAPI / Request / Response / logging / state_manager / auth_store / notifier / system_params imports the body of `register()` itself needs.

    Move the `register(app: FastAPI) -> None` function (line 363-583) verbatim. References inside the body to moved helpers resolve via the imports above.

    **Step D — Re-export the import surface (D-03):**
    At the bottom of `__init__.py`:
    ```
    # D-03 import-surface preservation — sibling route module totp imports
    # `_is_safe_next` directly from `web.routes.login` (was line 43 of
    # web/routes/totp.py, now web/routes/totp/__init__.py). Re-export from the
    # package so the import path is unchanged.
    __all__ = ['register', '_is_safe_next']
    ```
    Before finalising, run `grep -rn "from web.routes.login import" --include='*.py' .` — every name imported externally must appear in `__all__` (or be reachable via `web.routes.login._renderers.X`, but D-03 says re-export from the package surface).

    **Step E — Delete `web/routes/login.py`:**
    `git rm web/routes/login.py`.

    **Step F — LOC budget verification:**
    Each daughter file MUST be ≤500 LOC. With the four render helpers + `_is_safe_next` + `_log_login_failure` plus imports, `_renderers.py` should land near 270 LOC. `__init__.py` holds `register()` (~220 LOC line 363-583) plus imports — comfortably under 500. If either file exceeds 500, surface as BLOCKER and STOP.

    **Conventions:**
    - 2-space indentation throughout
    - `_private` prefix preserved
    - Do NOT rename any function, class, constant
    - Do NOT change route URLs (`/login`, `/logout`, `/forgot-2fa`, etc.), methods, status codes, cookie names, cookie attrs, or response bytes
    - Do NOT change magic-link email body content or subject line (notifier dependency)
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; test ! -f web/routes/login.py &amp;&amp; test -d web/routes/login &amp;&amp; test -f web/routes/login/__init__.py &amp;&amp; test -f web/routes/login/_renderers.py &amp;&amp; for f in web/routes/login/__init__.py web/routes/login/_renderers.py; do loc=$(wc -l &lt; "$f"); test "$loc" -le 500 || { echo "FAIL: $f is $loc LOC (cap 500)"; exit 1; }; done &amp;&amp; python -c "from web.routes import login as l; assert callable(l.register); from web.routes.login import _is_safe_next; assert callable(_is_safe_next); from web.routes.login._renderers import _render_login_form, _render_forgot_2fa_form, _render_check_email_page, _render_logout_confirmation, _log_login_failure; assert all(callable(x) for x in [_render_login_form, _render_forgot_2fa_form, _render_check_email_page, _render_logout_confirmation, _log_login_failure]); print('import surface OK')" &amp;&amp; pytest tests/ -x -q -k "login" &amp;&amp; ruff check web/routes/login/</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f web/routes/login.py` passes
    - `test -d web/routes/login` passes
    - `test -f web/routes/login/__init__.py` AND `test -f web/routes/login/_renderers.py` both pass
    - `wc -l web/routes/login/__init__.py` returns ≤500
    - `wc -l web/routes/login/_renderers.py` returns ≤500
    - `grep -c "^def register" web/routes/login/__init__.py` returns exactly 1
    - `grep -c "^def _is_safe_next" web/routes/login/_renderers.py` returns exactly 1
    - `grep -c "^def _render_login_form" web/routes/login/_renderers.py` returns exactly 1
    - `grep -c "^def _render_forgot_2fa_form" web/routes/login/_renderers.py` returns exactly 1
    - `grep -c "^def _render_check_email_page" web/routes/login/_renderers.py` returns exactly 1
    - `grep -c "^def _render_logout_confirmation" web/routes/login/_renderers.py` returns exactly 1
    - `grep -c "^def _log_login_failure" web/routes/login/_renderers.py` returns exactly 1
    - `grep -c "'_is_safe_next'" web/routes/login/__init__.py` returns at least 1 (in the __all__ list — D-03 re-export)
    - `python -c "from web.routes.login import _is_safe_next; assert callable(_is_safe_next)"` exits 0 (totp's cross-route import path resolves)
    - `python -c "from web.routes import login as login_route; assert callable(login_route.register)"` exits 0
    - `pytest tests/ -x -q -k "login"` exits 0 (all login-related tests green)
    - `ruff check web/routes/login/` exits 0
    - `grep -rn "from web.routes.login import" --include='*.py' . | wc -l` reports the same count as before the split
  </acceptance_criteria>
  <done>Package replaces the 608-LOC single file; every daughter ≤500 LOC; `_is_safe_next` re-exported for sibling totp use; login tests green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login form ↔ user browser | Password POST, cookie minting, redirect handling all use `_is_safe_next` for redirect validation — moving the helper without re-exporting breaks the open-redirect defence |
| login route ↔ totp route | Cross-route import `_is_safe_next` carries the redirect-safety contract; D-03 re-export keeps the contract intact across the split |
| login route ↔ notifier | Magic-link email body / subject derived inside register() must remain byte-identical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | Spoofing | Open-redirect via `next=` parameter resurfaces because `_is_safe_next` semantics changes during the move | mitigate | Function body moved verbatim; acceptance gate runs login tests which include `_is_safe_next` regression coverage (existing tests) |
| T-30-05-02 | Tampering | Sibling totp module's `from web.routes.login import _is_safe_next` breaks silently | mitigate | Acceptance gate `python -c "from web.routes.login import _is_safe_next; assert callable(_is_safe_next)"` exits 0; `__all__` includes `_is_safe_next` per D-03 |
| T-30-05-03 | Information Disclosure | `_log_login_failure` log line text changes during the move and operator's auth-failure monitoring breaks silently | mitigate | Function body moved verbatim; grep gate confirms exact function name |
</threat_model>

<verification>
- Daughter files exist, none exceed 500 LOC
- `register` and `_is_safe_next` importable from `web.routes.login`
- Login-flow tests green
- `ruff check web/routes/login/` exits 0
</verification>

<success_criteria>
- OPS-01 acceptance for `login.py` satisfied: behaviour-preserving split, every daughter ≤500 LOC.
- Phase 30 success criterion #2 satisfied for this file.
- web/app.py import statement unchanged.
- Cross-route `_is_safe_next` import (from totp) continues to resolve.
- Zero impact on parallel Wave 1 split plans (this plan touches only `web/routes/login*` files).
</success_criteria>

<output>
After completion, create `.planning/phases/30-file-size-pre-split/30-05-SUMMARY.md`
</output>
