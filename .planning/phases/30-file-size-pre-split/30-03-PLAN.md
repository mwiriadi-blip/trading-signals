---
plan: "30-03"
phase: 30
wave: 1
depends_on: []
autonomous: true
files_modified:
  - web/routes/dashboard.py
  - web/routes/dashboard/__init__.py
  - web/routes/dashboard/_renderers.py
requirements:
  - OPS-01
must_haves:
  truths:
    - "web/routes/dashboard.py (single-file 650 LOC) no longer exists; web/routes/dashboard/ package replaces it"
    - "`from web.routes import dashboard as dashboard_route` in web/app.py still resolves; `dashboard_route.register(app)` still works"
    - "_is_stale_for moves into _renderers.py and is callable from inside register()"
    - "Every daughter file is ≤500 LOC"
    - "Rendered HTML is byte-identical for the dashboard route on a fixture state (Phase 30 success criterion #2)"
  artifacts:
    - path: "web/routes/dashboard/__init__.py"
      provides: "register(app) — closures that capture WEB_AUTH_SECRET/session serializer stay here"
      contains: "def register"
    - path: "web/routes/dashboard/_renderers.py"
      provides: "Module-level renderer helpers + _is_stale_for"
      contains: "def _is_stale_for"
  key_links:
    - from: "web/app.py:41"
      to: "web.routes.dashboard.register"
      via: "package __init__.py export"
      pattern: "from web.routes import dashboard as dashboard_route"
    - from: "register(app) body (closures)"
      to: "_renderers._is_stale_for and module-level helpers"
      via: "from web.routes.dashboard._renderers import _is_stale_for, ..."
      pattern: "import inside __init__.py top"
---

<objective>
Convert `web/routes/dashboard.py` (650 LOC) into a `web/routes/dashboard/` package with two daughter files (`__init__.py`, `_renderers.py`), each ≤500 LOC. Move only module-level helpers (those that do NOT capture `register()`-scoped closures) into `_renderers.py`; keep closure-capturing helpers inside `register()` in `__init__.py`. Behaviour-preserving — zero changes to route URLs, handler signatures, cookie behaviour, or rendered HTML bytes.

Purpose: Identical to Plan 30-02 (trades) — Phase 31+ user_id injection on a 650-LOC monolith produces noisy diffs; splitting first keeps later diffs clean.

Output: One deleted file (`web/routes/dashboard.py`), two new files inside `web/routes/dashboard/`. The `register(app)` function lives in `__init__.py` exactly as before.

**Constraint specific to dashboard.py:** Many nested helpers inside `register()` capture closure variables (`_session_secret`, `_session_serializer`, `_MARKET_COOKIE_ATTRS`). These MUST stay inside `register()` — extracting them would require introducing parameters and changing the call signature, violating "behaviour-preserving." Only module-level helpers (`_is_stale_for` and any other helper that survives a closure-capture audit) move to `_renderers.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-file-size-pre-split/30-CONTEXT.md

@web/routes/dashboard.py
@web/app.py

<interfaces>
<!-- Public surface that MUST be preserved -->
<!-- web/app.py imports `from web.routes import dashboard as dashboard_route` then calls .register(app). -->
<!-- web/services/dashboard_service.py also imports the module directly. -->

External callers rely on `register` being importable from `web.routes.dashboard`.

Module-level structure today (web/routes/dashboard.py top-level defs):
- `logger = logging.getLogger(__name__)` (line 72)
- `_is_stale_for(page_output: Path) -> bool` (line 119) — pure helper, no closure captures, candidate for `_renderers.py`
- `register(app: FastAPI) -> None` (line 150) — body contains 20+ nested defs that capture closure variables; these STAY inside `register()`

Nested helpers inside `register()` that CAPTURE CLOSURES (must stay inside register()):
- `_is_cookie_session` (line 160) — captures `_session_serializer`
- `_resolve_trace_open` (line 175) — uses module-level `_MARKET_ID_RE`
- `get_dashboard`, `get_signals_page`, etc. (lines 196-235) — FastAPI route handlers; cannot move out
- `_is_htmx_request` (line 249), `_set_market_cookie` (line 253) — small helpers, candidates IF closure-free (verify each before moving)
- `_serve_dashboard_page` (line 405), `_serve_dashboard_root` (line 446), `_forward_stop_fragment_response` (line 495), `_em_dash_response` (line 513), `_substitute` (line 555), `_trace_open_repl` (line 613), `_serve_dashboard_content` (line 620) — most likely capture closures; default to leaving them inside register()

Module-level constants/imports referenced by `_is_stale_for` and any other migratable helper move with them to `_renderers.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split web/routes/dashboard.py into a 2-file package (D-07 boundary)</name>
  <files>web/routes/dashboard.py, web/routes/dashboard/__init__.py, web/routes/dashboard/_renderers.py</files>
  <read_first>
    - web/routes/dashboard.py (entire file — every nested def must be inspected for closure capture before it can move out of register())
    - web/app.py lines 40-55 (confirm `from web.routes import dashboard as dashboard_route` import shape)
    - web/services/dashboard_service.py (confirm whether the service imports any names beyond `register` from web.routes.dashboard)
    - .planning/phases/30-file-size-pre-split/30-CONTEXT.md (D-07 — _is_stale_for moves to _renderers.py; closure helpers stay inside register())
  </read_first>
  <action>
    Convert `web/routes/dashboard.py` into a Python package. The key constraint: dashboard.py has many nested helpers inside `register()` that capture closure variables — those MUST stay inside `register()`. Only move helpers that are pure module-level (no closure captures).

    **Step A — Inventory closure captures BEFORE moving anything:**
    Read each top-level and nested helper. For each candidate move target, confirm it captures no name defined inside `register()` (e.g., `_session_secret`, `_session_serializer`, `_MARKET_COOKIE_ATTRS`). If it captures any such name, leave it inside `register()`.

    Per CONTEXT D-07, the floor is at least `_is_stale_for` (line 119, already module-level, trivially safe to move).

    **Step B — Create `web/routes/dashboard/_renderers.py`:**
    Move into `_renderers.py`:
    - `_is_stale_for` (line 119) — pure, no closure deps; verified safe by D-07
    - Any other module-level helper that the audit in Step A confirms has no closure dep
    - Any module-level constants those helpers reference (e.g., palette imports, path globs). DO NOT duplicate — move the source declaration here.

    Plus the imports each moved helper needs (pathlib, datetime, html, system_params, etc.).

    **Step C — Create `web/routes/dashboard/__init__.py`:**
    Move `logger = logging.getLogger(__name__)` (line 72) to the top of `__init__.py` so `register()` keeps its existing logging surface.

    Move the `register(app: FastAPI) -> None` function (line 150-end) verbatim, including ALL nested defs that capture closures.

    Add an import at the top of `__init__.py` to pull what was moved:
    ```
    from web.routes.dashboard._renderers import _is_stale_for
    # ...plus any other module-level names moved in Step B
    ```

    **Step D — Re-export the import surface (D-03):**
    At the bottom of `__init__.py`:
    ```
    # D-03 import-surface preservation — service layer + tests import `register`
    # (and possibly other names) directly from `web.routes.dashboard`. Re-export
    # from the package so the import path is unchanged.
    __all__ = ['register']
    # If service-layer or test scan surfaces other imports, extend __all__ here.
    ```

    Run `grep -rn "from web.routes.dashboard import" --include='*.py' .` BEFORE finalising — every name imported externally must be in `__all__` and re-exported from `__init__.py` (or be a module-level name on `_renderers.py` that callers can reach via `web.routes.dashboard._renderers.X`, though the cleaner path is to surface them on `__init__.py`).

    **Step E — Delete `web/routes/dashboard.py`:**
    `git rm web/routes/dashboard.py`.

    **Step F — LOC budget verification:**
    Each daughter file MUST be ≤500 LOC. The most likely overflow risk is `__init__.py` because `register()` is ~500 LOC by itself plus imports. If `__init__.py` exceeds 500 LOC:
    - Look for additional module-level helpers inside `register()` that could be lifted out
    - If no further closure-free move is possible, surface the overage as a BLOCKER and STOP. Phase coordinator must approve a different decomposition (e.g., extracting `_substitute` plus its closure-captured names into a small helper-class).

    **Conventions:**
    - 2-space indentation throughout
    - `_private` prefix preserved
    - Do NOT rename any function, class, constant
    - Do NOT change route URLs, methods, status codes, cookie names, or response bytes
    - HTML output must be byte-identical for the dashboard route on a fixture state (Phase 30 success criterion #2)
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; test ! -f web/routes/dashboard.py &amp;&amp; test -d web/routes/dashboard &amp;&amp; test -f web/routes/dashboard/__init__.py &amp;&amp; test -f web/routes/dashboard/_renderers.py &amp;&amp; for f in web/routes/dashboard/__init__.py web/routes/dashboard/_renderers.py; do loc=$(wc -l &lt; "$f"); test "$loc" -le 500 || { echo "FAIL: $f is $loc LOC (cap 500)"; exit 1; }; done &amp;&amp; python -c "from web.routes import dashboard as d; assert callable(d.register); from web.routes.dashboard._renderers import _is_stale_for; assert callable(_is_stale_for); print('import surface OK')" &amp;&amp; pytest tests/ -x -q -k "dashboard" &amp;&amp; ruff check web/routes/dashboard/</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f web/routes/dashboard.py` passes (single-file module removed)
    - `test -d web/routes/dashboard` passes (package directory exists)
    - `test -f web/routes/dashboard/__init__.py` AND `test -f web/routes/dashboard/_renderers.py` both pass
    - `wc -l web/routes/dashboard/__init__.py` returns ≤500
    - `wc -l web/routes/dashboard/_renderers.py` returns ≤500
    - `grep -c "^def register" web/routes/dashboard/__init__.py` returns exactly 1 (register lives in __init__.py per D-02)
    - `grep -c "^def _is_stale_for" web/routes/dashboard/_renderers.py` returns exactly 1 (D-07: _is_stale_for moves here)
    - `grep -c "^def _is_stale_for" web/routes/dashboard/__init__.py` returns 0 (must NOT remain in __init__.py)
    - `python -c "from web.routes import dashboard as dashboard_route; assert callable(dashboard_route.register)"` exits 0 (web/app.py-style import unchanged)
    - `python -c "from web.routes.dashboard._renderers import _is_stale_for; assert callable(_is_stale_for)"` exits 0
    - `pytest tests/ -x -q -k "dashboard"` exits 0 (all dashboard-related tests green)
    - `ruff check web/routes/dashboard/` exits 0 (no lint regressions)
    - `grep -rn "from web.routes.dashboard import" --include='*.py' . | wc -l` reports the same count as before the split (no external import path broken)
    - HTML byte-parity smoke: run the existing dashboard render test against a fixture state and confirm the response body matches a pre-split baseline (use the standard project test that exercises GET / response shape; specific test name surfaces from `pytest --collect-only -q -k dashboard` and is one of the tests covered by the dashboard -k filter above)
  </acceptance_criteria>
  <done>Package replaces the 650-LOC single file; every daughter ≤500 LOC; import surface preserved (web/app.py and service-layer imports unchanged); dashboard route tests green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI route handler ↔ HTMX caller | Cookie validation (`tsi_session`, `tsi_trace_open`), session serializer, and substitute-rendering pipeline are CLOSURE-CAPTURED helpers and stay inside `register()`. Moving them silently could leak the session serializer into module scope. |
| dashboard package ↔ service layer | `web/services/dashboard_service.py` imports the module directly; preserved by D-03 __init__.py re-exports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Tampering | A closure-capturing helper is naively moved to `_renderers.py`, silently breaking session validation (e.g., `_is_cookie_session` loses its `_session_serializer` reference) | mitigate | Step A is a mandatory closure-capture audit BEFORE any move; default is "leave inside register()". Acceptance gate requires every dashboard test to pass; the cookie-session helpers are exercised by tests/test_web_app_factory.py + tests/test_web_healthz.py auth fixtures. |
| T-30-03-02 | Information Disclosure | Rendered HTML drifts during the move (e.g., a substitution pattern changes) and leaks an unintended template marker or strips an `escape()` call | mitigate | Acceptance gate includes the dashboard render byte-parity test against a fixture state; ruff + pytest must both be green. |
| T-30-03-03 | Denial of Service | __init__.py overflows 500 LOC because `register()` body is ~500 LOC | mitigate | Step F surfaces the overage as a BLOCKER if no further closure-free move is available — does not silently re-shard. Phase coordinator decides next move. |
</threat_model>

<verification>
- Daughter files exist, none exceed 500 LOC
- `register` importable from `web.routes.dashboard`
- `_is_stale_for` importable from `web.routes.dashboard._renderers`
- Dashboard tests green; HTML byte-parity preserved
- `ruff check web/routes/dashboard/` exits 0
</verification>

<success_criteria>
- OPS-01 acceptance for `dashboard.py` satisfied: behaviour-preserving split, every daughter ≤500 LOC.
- Phase 30 success criterion #2 satisfied for this file: full route + template + test parity (HTML byte-identical on fixture state).
- web/app.py import statement unchanged.
- Zero impact on parallel Wave 1 split plans (this plan touches only `web/routes/dashboard*` files).
</success_criteria>

<output>
After completion, create `.planning/phases/30-file-size-pre-split/30-03-SUMMARY.md`
</output>
