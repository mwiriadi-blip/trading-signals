---
plan: "30-04"
phase: 30
wave: 1
depends_on: []
autonomous: true
files_modified:
  - web/routes/totp.py
  - web/routes/totp/__init__.py
  - web/routes/totp/_renderers.py
requirements:
  - OPS-01
must_haves:
  truths:
    - "web/routes/totp.py (single-file 614 LOC) no longer exists; web/routes/totp/ package replaces it"
    - "`from web.routes import totp as totp_route` in web/app.py still resolves; `totp_route.register(app)` still works"
    - "totp.py's cross-module import `from web.routes.login import _is_safe_next` continues to resolve after the totp split (login is a sibling route module — totp depends on login's public name, which Plan 30-05 will re-export from web/routes/login/__init__.py)"
    - "Every daughter file is ≤500 LOC"
    - "Full test suite passes for TOTP routes (auth flow tests, trusted-device tests)"
  artifacts:
    - path: "web/routes/totp/__init__.py"
      provides: "register(app) + import surface re-exports"
      contains: "def register"
    - path: "web/routes/totp/_renderers.py"
      provides: "All render helpers (_render_qr_data_uri, _render_enroll_page, _render_enroll_reset_choice_page, _render_verify_page, _derive_device_label, _log_totp_failure)"
      contains: "_render_qr_data_uri"
  key_links:
    - from: "web/app.py:46"
      to: "web.routes.totp.register"
      via: "package __init__.py export"
      pattern: "from web.routes import totp as totp_route"
    - from: "totp/__init__.py (register body)"
      to: "web.routes.login._is_safe_next"
      via: "cross-route import (sibling routing helper)"
      pattern: "from web.routes.login import _is_safe_next"
---

<objective>
Convert `web/routes/totp.py` (614 LOC) into a `web/routes/totp/` package with two daughter files (`__init__.py`, `_renderers.py`), each ≤500 LOC. Behaviour-preserving — zero changes to TOTP enroll/verify/reset flow, QR encoding, device-label derivation, trusted-device cookies, or failure logging.

Purpose: Phase 31+ user_id injection on a 614-LOC TOTP module produces noisy diffs; splitting first keeps later diffs clean.

**Cross-module dependency:** `totp.py:43` has `from web.routes.login import _is_safe_next`. Plan 30-05 (login split) MUST re-export `_is_safe_next` from `web/routes/login/__init__.py`. This plan (30-04) and Plan 30-05 are both Wave 1 but the cross-module import is at the package's `__init__.py` boundary; as long as Plan 30-05 re-exports `_is_safe_next` (D-03 import-surface preservation), the import resolves regardless of execution order. The final-gate plan (30-07) confirms both splits land cleanly together.

Output: One deleted file (`web/routes/totp.py`), two new files inside `web/routes/totp/`. The `register(app)` function lives in `__init__.py` exactly as before.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-file-size-pre-split/30-CONTEXT.md

@web/routes/totp.py
@web/app.py

<interfaces>
<!-- Public surface that MUST be preserved -->
<!-- web/app.py imports `from web.routes import totp as totp_route` then calls .register(app). -->
<!-- web/services/totp_service.py also imports the module directly. -->

External callers rely on `register` being importable from `web.routes.totp`.

Module-level structure today (web/routes/totp.py top-level defs):
- `from web.routes.login import _is_safe_next` (line 43) — cross-route import; Plan 30-05 must re-export `_is_safe_next` from `web.routes.login` for this import to keep resolving
- `_derive_device_label(...)` (line 57)
- `_render_qr_data_uri(provisioning_uri: str)` (line 226)
- `_render_enroll_page(...)` (line 239)
- `_render_enroll_reset_choice_page()` (line 284)
- `_render_verify_page(error: str | None = None)` (line 319)
- `register(app: FastAPI) -> None` (line 360)
- `_log_totp_failure(request: Request, reason: str, path: str)` (line 599)

Per D-06: all render helpers + `_log_totp_failure` + `_derive_device_label` move to `_renderers.py`; `register()` stays in `__init__.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split web/routes/totp.py into a 2-file package (D-06 boundary)</name>
  <files>web/routes/totp.py, web/routes/totp/__init__.py, web/routes/totp/_renderers.py</files>
  <read_first>
    - web/routes/totp.py (entire file — every helper must be inspected to confirm it has no register()-closure dependency before moving)
    - web/app.py lines 40-55 (confirm `from web.routes import totp as totp_route` import shape)
    - web/services/totp_service.py (confirm whether the service imports any name beyond `register` from web.routes.totp)
    - .planning/phases/30-file-size-pre-split/30-CONTEXT.md (D-06 — _render_* + _log_totp_failure + _derive_device_label move to _renderers.py)
  </read_first>
  <action>
    Convert `web/routes/totp.py` into a Python package. Behaviour-preserving — zero edits to TOTP semantics, secret encoding, QR data-URI bytes, trusted-device cookie attributes, or failure log lines.

    **Step A — Confirm closure-freeness:**
    Each of `_derive_device_label`, `_render_qr_data_uri`, `_render_enroll_page`, `_render_enroll_reset_choice_page`, `_render_verify_page`, `_log_totp_failure` must be confirmed to NOT capture any variable defined inside `register()` (they all look module-level today, but verify by reading the file). If any helper captures a `register()`-local, leave it inside `register()` and surface the surprise.

    **Step B — Create `web/routes/totp/_renderers.py`:**
    Move into `_renderers.py`:
    - `_derive_device_label` (line 57-225 — note this is a sizeable block; confirm exact end-line during the move)
    - `_render_qr_data_uri` (line 226-238)
    - `_render_enroll_page` (line 239-283)
    - `_render_enroll_reset_choice_page` (line 284-318)
    - `_render_verify_page` (line 319-359)
    - `_log_totp_failure` (line 599-end)

    Plus the imports those helpers need (qrcode, base64, html, logging, system_params palette, auth_store helpers, etc. — copy only what `_renderers.py` actually references).

    **Step C — Create `web/routes/totp/__init__.py`:**
    Place at the top of `__init__.py`:
    ```
    from web.routes.login import _is_safe_next  # cross-route helper (D-03 dependency on Plan 30-05)
    from web.routes.totp._renderers import (
        _derive_device_label,
        _render_qr_data_uri,
        _render_enroll_page,
        _render_enroll_reset_choice_page,
        _render_verify_page,
        _log_totp_failure,
    )
    ```
    Plus the FastAPI / Request / Response / logging / state_manager / auth_store / system_params imports the body of `register()` itself needs.

    Move the `register(app: FastAPI) -> None` function (line 360-598) verbatim. References inside the body to moved helpers now resolve via the imports above.

    **Step D — Re-export the import surface (D-03):**
    At the bottom of `__init__.py`:
    ```
    # D-03 import-surface preservation — service layer + tests import `register`
    # (and possibly other names) directly from `web.routes.totp`. Re-export
    # from the package so the import path is unchanged.
    __all__ = ['register']
    ```
    Before finalising, run `grep -rn "from web.routes.totp import" --include='*.py' .` — every name imported externally must appear in `__all__` (or be reachable via `web.routes.totp._renderers.X`, but cleaner to re-export).

    **Step E — Delete `web/routes/totp.py`:**
    `git rm web/routes/totp.py`.

    **Step F — LOC budget verification:**
    Each daughter file MUST be ≤500 LOC. With `_derive_device_label` (~170 LOC) plus the four render helpers (~110 LOC combined) plus `_log_totp_failure` (~15 LOC) plus imports, `_renderers.py` should comfortably land near 320 LOC. `__init__.py` holds `register()` (~240 LOC line 360-598) plus imports — also comfortably under 500. If either file exceeds 500, surface as BLOCKER and STOP.

    **Conventions:**
    - 2-space indentation throughout
    - `_private` prefix preserved
    - Do NOT rename any function, class, constant
    - Do NOT change route URLs (`/totp/enroll`, `/totp/verify`, `/totp/reset`, etc.), methods, status codes, cookie names, QR data-URI bytes, or response shapes
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; test ! -f web/routes/totp.py &amp;&amp; test -d web/routes/totp &amp;&amp; test -f web/routes/totp/__init__.py &amp;&amp; test -f web/routes/totp/_renderers.py &amp;&amp; for f in web/routes/totp/__init__.py web/routes/totp/_renderers.py; do loc=$(wc -l &lt; "$f"); test "$loc" -le 500 || { echo "FAIL: $f is $loc LOC (cap 500)"; exit 1; }; done &amp;&amp; python -c "from web.routes import totp as t; assert callable(t.register); from web.routes.totp._renderers import _render_qr_data_uri, _render_enroll_page, _render_verify_page, _derive_device_label; assert all(callable(x) for x in [_render_qr_data_uri, _render_enroll_page, _render_verify_page, _derive_device_label]); print('import surface OK')" &amp;&amp; pytest tests/ -x -q -k "totp" &amp;&amp; ruff check web/routes/totp/</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f web/routes/totp.py` passes (single-file module removed)
    - `test -d web/routes/totp` passes (package directory exists)
    - `test -f web/routes/totp/__init__.py` AND `test -f web/routes/totp/_renderers.py` both pass
    - `wc -l web/routes/totp/__init__.py` returns ≤500
    - `wc -l web/routes/totp/_renderers.py` returns ≤500
    - `grep -c "^def register" web/routes/totp/__init__.py` returns exactly 1
    - `grep -c "^def _render_qr_data_uri" web/routes/totp/_renderers.py` returns exactly 1
    - `grep -c "^def _render_enroll_page" web/routes/totp/_renderers.py` returns exactly 1
    - `grep -c "^def _render_enroll_reset_choice_page" web/routes/totp/_renderers.py` returns exactly 1
    - `grep -c "^def _render_verify_page" web/routes/totp/_renderers.py` returns exactly 1
    - `grep -c "^def _derive_device_label" web/routes/totp/_renderers.py` returns exactly 1
    - `grep -c "^def _log_totp_failure" web/routes/totp/_renderers.py` returns exactly 1
    - `grep -c "from web.routes.login import _is_safe_next" web/routes/totp/__init__.py` returns exactly 1 (cross-route import preserved)
    - `python -c "from web.routes import totp as totp_route; assert callable(totp_route.register)"` exits 0
    - `pytest tests/ -x -q -k "totp"` exits 0 (all TOTP-related tests green)
    - `ruff check web/routes/totp/` exits 0
    - `grep -rn "from web.routes.totp import" --include='*.py' . | wc -l` reports the same count as before the split (no external import path broken)
  </acceptance_criteria>
  <done>Package replaces the 614-LOC single file; every daughter ≤500 LOC; import surface preserved; cross-route `_is_safe_next` import preserved; TOTP route tests green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOTP enroll/verify ↔ user browser | QR data-URI bytes, secret encoding, trusted-device cookie attributes must remain byte-identical |
| totp route module ↔ login route module | `_is_safe_next` is a cross-module import; sibling-route coupling preserved via D-03 re-exports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Spoofing | A render helper accidentally drops the CSRF-equivalent state binding during the move and weakens the verify-page form | mitigate | Move helpers verbatim, no edits to function bodies; acceptance criteria require all TOTP tests green (`pytest -k totp` exercises the enrol/verify/reset flow) |
| T-30-04-02 | Information Disclosure | `_log_totp_failure` log line text changes during the move and operator's grep-based monitoring breaks silently | mitigate | Function body is moved verbatim; grep gate confirms exact function name; the same log-line text remains because the source bytes are unchanged |
| T-30-04-03 | Tampering | Cross-module import `from web.routes.login import _is_safe_next` breaks if Plan 30-05 fails to re-export the name | mitigate | Acceptance gate `grep -c "from web.routes.login import _is_safe_next" web/routes/totp/__init__.py == 1`; final-gate plan (30-07) confirms the cross-import resolves under both packages together |
</threat_model>

<verification>
- Daughter files exist, none exceed 500 LOC
- `register` importable from `web.routes.totp`
- TOTP-flow tests green
- `ruff check web/routes/totp/` exits 0
</verification>

<success_criteria>
- OPS-01 acceptance for `totp.py` satisfied: behaviour-preserving split, every daughter ≤500 LOC.
- Phase 30 success criterion #2 satisfied for this file.
- web/app.py import statement unchanged.
- Cross-route `_is_safe_next` import continues to resolve.
- Zero impact on parallel Wave 1 split plans (this plan touches only `web/routes/totp*` files).
</success_criteria>

<output>
After completion, create `.planning/phases/30-file-size-pre-split/30-04-SUMMARY.md`
</output>
