---
plan: "30-02"
phase: 30
wave: 1
depends_on: []
autonomous: true
files_modified:
  - web/routes/trades.py
  - web/routes/trades/__init__.py
  - web/routes/trades/_models.py
  - web/routes/trades/_renderers.py
requirements:
  - OPS-01
must_haves:
  truths:
    - "web/routes/trades.py (single-file 746 LOC) no longer exists; web/routes/trades/ package replaces it"
    - "`from web.routes import trades as trades_route` in web/app.py still resolves; `trades_route.register(app)` still works"
    - "tests/test_web_trades.py imports `OpenTradeRequest` and `ModifyTradeRequest` from `web.routes.trades` and they resolve via __init__.py re-exports"
    - "Every daughter file is ≤500 LOC"
    - "Full test suite (with focus on tests/test_web_trades.py) is green; route URLs unchanged; handler signatures unchanged"
  artifacts:
    - path: "web/routes/trades/__init__.py"
      provides: "register(app) + import surface re-exports"
      contains: "def register"
    - path: "web/routes/trades/_models.py"
      provides: "Pydantic models + exceptions (OpenTradeRequest, CloseTradeRequest, ModifyTradeRequest, _OpenConflict)"
      contains: "class OpenTradeRequest"
    - path: "web/routes/trades/_renderers.py"
      provides: "All render-partial helpers (_render_position_row_partial, _render_close_form_partial, etc.)"
      contains: "_render_position_row_partial"
  key_links:
    - from: "web/app.py:49"
      to: "web.routes.trades.register"
      via: "package __init__.py export"
      pattern: "from web.routes import trades as trades_route"
    - from: "tests/test_web_trades.py:341,616,642,650,658"
      to: "web.routes.trades.OpenTradeRequest / ModifyTradeRequest"
      via: "__init__.py re-export"
      pattern: "from web.routes.trades import OpenTradeRequest"
---

<objective>
Convert `web/routes/trades.py` (746 LOC) into a `web/routes/trades/` package with three daughter files (`__init__.py`, `_models.py`, `_renderers.py`), each ≤500 LOC, while preserving the import surface that callers and tests depend on. Behaviour-preserving — zero changes to route logic, URLs, handler signatures, or Pydantic validation rules.

Purpose: Phase 31+ will inject `user_id` into every route; doing it on a 746-LOC monolith produces noisy, hard-to-review diffs. Splitting first makes the user_id injection patches small and merge-safe.

Output: One deleted file (`web/routes/trades.py`), three new files inside `web/routes/trades/`. The `register(app)` function lives in `__init__.py` exactly as before.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-file-size-pre-split/30-CONTEXT.md

@web/routes/trades.py
@web/app.py
@tests/test_web_trades.py

<interfaces>
<!-- Public surface that MUST be preserved (extracted from grep of `from web.routes.trades` usage) -->
<!-- The __init__.py must re-export these names so import paths in callers and tests do not change. -->

External callers + tests rely on these names being importable from `web.routes.trades`:
- `register` (callable: `register(app: FastAPI) -> None`) — called by web/app.py:49 `trades_route.register(app)`
- `OpenTradeRequest` (Pydantic model) — imported by tests/test_web_trades.py:341
- `ModifyTradeRequest` (Pydantic model) — imported by tests/test_web_trades.py:616, 642, 650, 658
- `CloseTradeRequest` (Pydantic model) — defined at line 148 in trades.py, may be imported indirectly by service layer; re-export to be safe

Internal helpers that live in the file today (web/routes/trades.py top-level defs at the listed lines):
- `_AWST` (constant, line 73), `_OPERATOR_CLOSE` (constant, line 74)
- `_now_awst` (line 77)
- `_OpenConflict` exception (line 81)
- `OpenTradeRequest` (line 95), `CloseTradeRequest` (line 148), `ModifyTradeRequest` (line 167)
- `_format_pydantic_errors` (line 222)
- `_build_position_dict` (line 264)
- `_render_position_row_partial` (line 297)
- `_render_positions_tbody_partial` (line 329)
- `_render_close_form_partial` (line 339)
- `_render_modify_form_partial` (line 371)
- `_render_open_success_partial` (line 391)
- `_render_close_success_partial` (line 409)
- `_render_modify_success_partial` (line 438)
- `register` (line 458)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split web/routes/trades.py into a 3-file package (D-04 boundary)</name>
  <files>web/routes/trades.py, web/routes/trades/__init__.py, web/routes/trades/_models.py, web/routes/trades/_renderers.py</files>
  <read_first>
    - web/routes/trades.py (entire file — you need every line of source content before redistributing it)
    - web/app.py lines 40-55 (confirm `from web.routes import trades as trades_route` import shape so the new package preserves it transparently)
    - tests/test_web_trades.py lines 340-345, 615-660 (confirm the exact names imported from `web.routes.trades`: `OpenTradeRequest`, `ModifyTradeRequest`)
    - web/services/trades_service.py (confirm whether the service imports any other names from web.routes.trades that must also be re-exported)
    - .planning/phases/30-file-size-pre-split/30-CONTEXT.md (D-01..D-04 — package-per-route pattern)
  </read_first>
  <action>
    Convert `web/routes/trades.py` into a Python package by these mechanical steps. ZERO behaviour changes — no logic edits, no signature changes, no new code beyond imports and the re-export block.

    **Step A — Create `web/routes/trades/_models.py`:**
    Move these defs verbatim from `web/routes/trades.py`:
    - `_AWST` constant (line 73), `_OPERATOR_CLOSE` constant (line 74), `_now_awst` (line 77)
    - `_OpenConflict` exception class (line 81)
    - `OpenTradeRequest` class (line 95-147)
    - `CloseTradeRequest` class (line 148-166)
    - `ModifyTradeRequest` class (line 167-221)
    - `_format_pydantic_errors` (line 222-263)
    - `_build_position_dict` (line 264-296)
    Plus the imports from the top of `trades.py` that these defs use (datetime, zoneinfo, typing, pydantic, FastAPI Response/RequestValidationError, system_params, etc. — copy only what `_models.py` actually references; ruff will surface unused imports).

    **Step B — Create `web/routes/trades/_renderers.py`:**
    Move these defs verbatim from `web/routes/trades.py`:
    - `_render_position_row_partial` (line 297-328)
    - `_render_positions_tbody_partial` (line 329-338)
    - `_render_close_form_partial` (line 339-370)
    - `_render_modify_form_partial` (line 371-390)
    - `_render_open_success_partial` (line 391-408)
    - `_render_close_success_partial` (line 409-437)
    - `_render_modify_success_partial` (line 438-457)
    Plus the imports those renderers use (html, FastAPI Response, system_params palette, dashboard_renderer helpers — copy only what `_renderers.py` actually references). If any renderer references a name from `_models.py` (e.g., `_OPERATOR_CLOSE`), add `from web.routes.trades._models import _OPERATOR_CLOSE` at the top of `_renderers.py`.

    **Step C — Create `web/routes/trades/__init__.py`:**
    Move the `register(app: FastAPI) -> None` function (line 458-end) verbatim. Update the references inside `register` so each name resolves: change uses of moved names to imports from sibling modules at the top of `__init__.py`:
    ```python
    from web.routes.trades._models import (
        _AWST, _OPERATOR_CLOSE, _now_awst, _OpenConflict,
        OpenTradeRequest, CloseTradeRequest, ModifyTradeRequest,
        _format_pydantic_errors, _build_position_dict,
    )
    from web.routes.trades._renderers import (
        _render_position_row_partial, _render_positions_tbody_partial,
        _render_close_form_partial, _render_modify_form_partial,
        _render_open_success_partial, _render_close_success_partial,
        _render_modify_success_partial,
    )
    ```
    Plus the FastAPI / Request / Response / logging / state_manager / system_params imports the body of `register()` itself needs.

    **Step D — Re-export the import surface (D-03):**
    At the bottom of `__init__.py`, add an explicit re-export block so `from web.routes.trades import OpenTradeRequest` (test import path) still resolves:
    ```python
    # D-03 import-surface preservation — tests and service layer import these names
    # directly from `web.routes.trades`. Re-export from the package so the import
    # path is unchanged.
    __all__ = [
        'register',
        'OpenTradeRequest', 'CloseTradeRequest', 'ModifyTradeRequest',
        '_OpenConflict', '_OPERATOR_CLOSE',
    ]
    ```

    **Step E — Delete `web/routes/trades.py`:**
    The old single-file module is replaced by the package. `git rm web/routes/trades.py`.

    **Step F — LOC budget verification:**
    Each daughter file MUST be ≤500 LOC. If `_renderers.py` exceeds the cap after Step B (the renderer block is ~160 LOC plus imports), no further split is needed; if any file exceeds 500 LOC, surface the overage as a blocker and STOP — do not silently re-shard.

    **Conventions:**
    - 2-space indentation throughout (project R-05; matches existing trades.py)
    - `_private` prefix preserved for all helpers (existing convention)
    - Do NOT rename any function, class, constant, exception, or parameter
    - Do NOT change route URLs, methods, status codes, or response shapes
    - Do NOT touch any test file (the test imports MUST keep working unchanged via __init__.py re-exports)
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; test ! -f web/routes/trades.py &amp;&amp; test -d web/routes/trades &amp;&amp; test -f web/routes/trades/__init__.py &amp;&amp; test -f web/routes/trades/_models.py &amp;&amp; test -f web/routes/trades/_renderers.py &amp;&amp; for f in web/routes/trades/__init__.py web/routes/trades/_models.py web/routes/trades/_renderers.py; do loc=$(wc -l &lt; "$f"); test "$loc" -le 500 || { echo "FAIL: $f is $loc LOC (cap 500)"; exit 1; }; done &amp;&amp; python -c "from web.routes import trades as t; assert callable(t.register); from web.routes.trades import OpenTradeRequest, ModifyTradeRequest, CloseTradeRequest; print('import surface OK')" &amp;&amp; pytest tests/test_web_trades.py -x -q &amp;&amp; ruff check web/routes/trades/</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f web/routes/trades.py` passes (single-file module removed)
    - `test -d web/routes/trades` passes (package directory exists)
    - `test -f web/routes/trades/__init__.py` AND `test -f web/routes/trades/_models.py` AND `test -f web/routes/trades/_renderers.py` all pass
    - `wc -l web/routes/trades/__init__.py` returns ≤500
    - `wc -l web/routes/trades/_models.py` returns ≤500
    - `wc -l web/routes/trades/_renderers.py` returns ≤500
    - `grep -c "^def register" web/routes/trades/__init__.py` returns exactly 1 (register lives in __init__.py per D-02)
    - `grep -c "^class OpenTradeRequest" web/routes/trades/_models.py` returns exactly 1
    - `grep -c "^class CloseTradeRequest" web/routes/trades/_models.py` returns exactly 1
    - `grep -c "^class ModifyTradeRequest" web/routes/trades/_models.py` returns exactly 1
    - `grep -c "^class _OpenConflict" web/routes/trades/_models.py` returns exactly 1
    - `grep -c "^def _render_position_row_partial" web/routes/trades/_renderers.py` returns exactly 1
    - `grep -c "^def _render_close_form_partial" web/routes/trades/_renderers.py` returns exactly 1
    - `grep -c "^def _render_modify_form_partial" web/routes/trades/_renderers.py` returns exactly 1
    - `python -c "from web.routes.trades import OpenTradeRequest, ModifyTradeRequest, CloseTradeRequest, register; assert callable(register)"` exits 0 (test-side import surface unchanged)
    - `python -c "from web.routes import trades as trades_route; assert callable(trades_route.register)"` exits 0 (web/app.py-style import unchanged)
    - `pytest tests/test_web_trades.py -x -q` exits 0 (full route test suite green)
    - `pytest tests/ -x -q -k "trades"` exits 0 (any other trades-related test green)
    - `ruff check web/routes/trades/` exits 0 (no lint regressions)
    - No file inside `web/routes/trades/` exceeds 500 LOC (per D-09 and Phase 30 success criterion #1)
  </acceptance_criteria>
  <done>Package replaces the 746-LOC single file; every daughter ≤500 LOC; import surface preserved (tests import unchanged); tests/test_web_trades.py green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI route handler ↔ caller (HTMX UI) | Pydantic validation at request entry; preserved verbatim by moving the model classes (not editing them) |
| route module ↔ service layer | Service imports route-module names directly; preserved by D-03 __init__.py re-exports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Tampering | A helper or model is renamed during the move and silently breaks a caller's import | mitigate | Acceptance criteria pin exact `class OpenTradeRequest` / `class CloseTradeRequest` / `class ModifyTradeRequest` / `def _render_*` regexes via grep; full route test suite must pass before plan closes |
| T-30-02-02 | Repudiation | A route URL or handler signature changes during the move (e.g. dropping a query param) and a caller breaks silently in production | mitigate | tests/test_web_trades.py exercises route URLs and request/response shapes; pytest gate covers the surface |
| T-30-02-03 | Denial of Service | Daughter file exceeds 500 LOC and re-introduces the D-09 violation immediately | mitigate | Acceptance criteria gate each daughter file to ≤500 LOC; verify script exits non-zero if any file overflows |
</threat_model>

<verification>
- All daughter files exist, none exceed 500 LOC
- Public names (`register`, `OpenTradeRequest`, `CloseTradeRequest`, `ModifyTradeRequest`) importable from `web.routes.trades`
- `tests/test_web_trades.py -x -q` exits 0
- `ruff check web/routes/trades/` exits 0
</verification>

<success_criteria>
- OPS-01 acceptance for `trades.py` satisfied: behaviour-preserving split, every daughter ≤500 LOC.
- Phase 30 success criterion #2 satisfied for this file: full route + template + test parity preserved (test suite green, handler signatures unchanged).
- web/app.py import statement unchanged.
- Zero impact on parallel Wave 1 split plans (this plan touches only `web/routes/trades*` files).
</success_criteria>

<output>
After completion, create `.planning/phases/30-file-size-pre-split/30-02-SUMMARY.md`
</output>
