---
plan: "30-06"
phase: 30
wave: 1
depends_on: []
autonomous: true
files_modified:
  - web/routes/paper_trades.py
  - web/routes/paper_trades/__init__.py
  - web/routes/paper_trades/_models.py
  - web/routes/paper_trades/_renderers.py
requirements:
  - OPS-01
must_haves:
  truths:
    - "web/routes/paper_trades.py (single-file 493 LOC) no longer exists; web/routes/paper_trades/ package replaces it"
    - "`from web.routes import paper_trades as paper_trades_route` in web/app.py still resolves; `paper_trades_route.register(app)` still works"
    - "tests/test_web_paper_trades.py:166 imports `_D09_KEYS` from `web.routes.paper_trades` and it resolves via __init__.py re-export"
    - "tests/test_system_params.py:230,240 import `_MULTIPLIER` and `_COST_AUD` from `web.routes.paper_trades` and they resolve via __init__.py re-export"
    - "Every daughter file is ≤500 LOC"
    - "Full paper-trades route test suite is green; route URLs unchanged; Pydantic validation unchanged"
  artifacts:
    - path: "web/routes/paper_trades/__init__.py"
      provides: "register(app) + import surface re-exports (_D09_KEYS, _MULTIPLIER, _COST_AUD for tests)"
      contains: "def register"
    - path: "web/routes/paper_trades/_models.py"
      provides: "Pydantic models + exceptions (OpenPaperTradeRequest, EditPaperTradeRequest, ClosePaperTradeRequest, _PaperTradeNotFound, _PaperTradeImmutable, _PaperTradeIDOverflow)"
      contains: "class OpenPaperTradeRequest"
    - path: "web/routes/paper_trades/_renderers.py"
      provides: "Render helpers + constants (_D09_KEYS, _MULTIPLIER, _COST_AUD if module-level; _method_not_allowed_405)"
      contains: "_D09_KEYS"
  key_links:
    - from: "web/app.py:48"
      to: "web.routes.paper_trades.register"
      via: "package __init__.py export"
      pattern: "from web.routes import paper_trades as paper_trades_route"
    - from: "tests/test_web_paper_trades.py:166"
      to: "web.routes.paper_trades._D09_KEYS"
      via: "__init__.py re-export"
      pattern: "from web.routes.paper_trades import _D09_KEYS"
    - from: "tests/test_system_params.py:230,240"
      to: "web.routes.paper_trades._MULTIPLIER / _COST_AUD"
      via: "__init__.py re-export"
      pattern: "from web.routes.paper_trades import _MULTIPLIER  # (and _COST_AUD)"
---

<objective>
Convert `web/routes/paper_trades.py` (493 LOC, 7 lines under the cap) into a `web/routes/paper_trades/` package with three daughter files (`__init__.py`, `_models.py`, `_renderers.py`), each ≤500 LOC. Behaviour-preserving — zero changes to paper-trade open/edit/close flow, Pydantic validation, cost-AUD computation, or exception semantics.

Purpose: Per D-09, paper_trades.py is split now (despite being 7 LOC under cap) because Phase 31+ user_id injection will push it past 500. Pre-emptive split avoids mid-refactor surgery.

**Test import surface (CRITICAL):** Three test imports MUST keep resolving via `__init__.py` re-exports:
- `tests/test_web_paper_trades.py:166` — `from web.routes.paper_trades import _D09_KEYS`
- `tests/test_system_params.py:230` — `from web.routes.paper_trades import _MULTIPLIER`
- `tests/test_system_params.py:240` — `from web.routes.paper_trades import _COST_AUD`

`_D09_KEYS`, `_MULTIPLIER`, `_COST_AUD` must be located (inside the existing file) and moved to their natural home (likely `_renderers.py` or a constants section of `_models.py`), then re-exported from `__init__.py` via `__all__`.

Output: One deleted file (`web/routes/paper_trades.py`), three new files inside `web/routes/paper_trades/`. The `register(app)` function lives in `__init__.py` exactly as before.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-file-size-pre-split/30-CONTEXT.md

@web/routes/paper_trades.py
@web/app.py

<interfaces>
<!-- Public surface that MUST be preserved -->
<!-- web/app.py imports `from web.routes import paper_trades as paper_trades_route` then calls .register(app). -->
<!-- web/services/paper_trades_service.py also imports the module directly. -->
<!-- Tests import several private names — those MUST be re-exported from __init__.py. -->

External callers + tests rely on these names being importable from `web.routes.paper_trades`:
- `register` (callable: `register(app: FastAPI) -> None`)
- `_D09_KEYS` (used by tests/test_web_paper_trades.py:166)
- `_MULTIPLIER` (used by tests/test_system_params.py:230)
- `_COST_AUD` (used by tests/test_system_params.py:240)
- `OpenPaperTradeRequest`, `EditPaperTradeRequest`, `ClosePaperTradeRequest` (probably imported by service layer; re-export defensively)

Module-level structure today (web/routes/paper_trades.py top-level defs):
- `_now_awst()` (line 73)
- `_PaperTradeNotFound` exception (line 109)
- `_PaperTradeImmutable` exception (line 113)
- `_PaperTradeIDOverflow` exception (line 117)
- `_method_not_allowed_405(allow: str = 'GET')` (line 128)
- `OpenPaperTradeRequest` (line 147)
- `EditPaperTradeRequest` (line 193)
- `ClosePaperTradeRequest` (line 216)
- `register(app: FastAPI) -> None` (line 228)

`_D09_KEYS`, `_MULTIPLIER`, `_COST_AUD` are module-level constants (not in the def grep) — locate them by reading the file head (likely defined alongside imports or just before the model classes). Move them to the daughter file that makes physical sense (`_renderers.py` for D09_KEYS if it is a render-related allowlist; `_models.py` for multiplier/cost-AUD if used by Pydantic validators) and re-export from `__init__.py`.

Per D-08: `_models.py` = Pydantic models + exceptions; `_renderers.py` = render helpers (which existing file has). `register()` lives in `__init__.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split web/routes/paper_trades.py into a 3-file package (D-08 boundary)</name>
  <files>web/routes/paper_trades.py, web/routes/paper_trades/__init__.py, web/routes/paper_trades/_models.py, web/routes/paper_trades/_renderers.py</files>
  <read_first>
    - web/routes/paper_trades.py (entire file — including the head where `_D09_KEYS`, `_MULTIPLIER`, `_COST_AUD` constants live)
    - web/app.py lines 40-55 (confirm `from web.routes import paper_trades as paper_trades_route` import shape)
    - web/services/paper_trades_service.py (confirm whether the service imports any name beyond `register` from web.routes.paper_trades)
    - tests/test_web_paper_trades.py line 166 (confirm `_D09_KEYS` import path)
    - tests/test_system_params.py lines 230, 240 (confirm `_MULTIPLIER`, `_COST_AUD` import path)
    - .planning/phases/30-file-size-pre-split/30-CONTEXT.md (D-08, D-09 — package boundary; pre-emptive split before user_id injection)
  </read_first>
  <action>
    Convert `web/routes/paper_trades.py` into a Python package. Behaviour-preserving — zero edits to paper-trade semantics, Pydantic validation, multiplier/cost-AUD arithmetic, or exception messages.

    **Step A — Locate the test-imported constants:**
    Open `web/routes/paper_trades.py` and find the source-line of each:
    - `_D09_KEYS = ...`
    - `_MULTIPLIER = ...`
    - `_COST_AUD = ...`
    These are module-level constants. Note their values and any computations they depend on (e.g., `_MULTIPLIER` may be derived from `system_params` imports).

    **Step B — Create `web/routes/paper_trades/_models.py`:**
    Move into `_models.py`:
    - `_now_awst` (line 73)
    - `_PaperTradeNotFound` (line 109)
    - `_PaperTradeImmutable` (line 113)
    - `_PaperTradeIDOverflow` (line 117)
    - `OpenPaperTradeRequest` (line 147-192)
    - `EditPaperTradeRequest` (line 193-215)
    - `ClosePaperTradeRequest` (line 216-227)
    - `_MULTIPLIER` and `_COST_AUD` constants IF they are used by the Pydantic validators (which is likely since they're used by tests/test_system_params.py). If they are only used by render helpers, move them to `_renderers.py` instead.

    Plus the imports those defs use (datetime, zoneinfo, typing, pydantic, system_params, etc.).

    **Step C — Create `web/routes/paper_trades/_renderers.py`:**
    Move into `_renderers.py`:
    - `_method_not_allowed_405` (line 128) — small response helper
    - `_D09_KEYS` constant — this is likely a render-related allowlist (the `_D09_` prefix matches the v1.2 D-09 package-split decision marker, but here the name appears to be a column allowlist for paper-trade rendering; locate the actual definition and confirm)
    - Any other render-related helper that exists in the file (check by reading the full file head-to-tail; the original def-grep showed only one `_render` helper isn't listed but there may be more)

    If `_D09_KEYS` is used by `register()` body (HTMX render path), it lives in `_renderers.py`. If it is used by `_models.py` Pydantic validators, move it there instead.

    **Step D — Create `web/routes/paper_trades/__init__.py`:**
    Place at the top of `__init__.py`:
    ```
    from web.routes.paper_trades._models import (
        _now_awst,
        _PaperTradeNotFound, _PaperTradeImmutable, _PaperTradeIDOverflow,
        OpenPaperTradeRequest, EditPaperTradeRequest, ClosePaperTradeRequest,
        # _MULTIPLIER, _COST_AUD if placed here
    )
    from web.routes.paper_trades._renderers import (
        _method_not_allowed_405,
        # _D09_KEYS (also _MULTIPLIER, _COST_AUD if placed here)
    )
    ```
    Plus the FastAPI / Request / Response / logging / state_manager / system_params imports the body of `register()` itself needs.

    Move the `register(app: FastAPI) -> None` function (line 228-end) verbatim. References inside the body resolve via the imports above.

    **Step E — Re-export the import surface (D-03):**
    At the bottom of `__init__.py`:
    ```
    # D-03 import-surface preservation — service layer + tests import these
    # names directly from `web.routes.paper_trades`. Re-export from the package
    # so the import path is unchanged.
    __all__ = [
        'register',
        'OpenPaperTradeRequest', 'EditPaperTradeRequest', 'ClosePaperTradeRequest',
        '_PaperTradeNotFound', '_PaperTradeImmutable', '_PaperTradeIDOverflow',
        '_D09_KEYS', '_MULTIPLIER', '_COST_AUD',
    ]
    ```
    Before finalising, run `grep -rn "from web.routes.paper_trades import" --include='*.py' .` — every name imported externally must appear in `__all__`.

    **Step F — Delete `web/routes/paper_trades.py`:**
    `git rm web/routes/paper_trades.py`.

    **Step G — LOC budget verification:**
    Each daughter file MUST be ≤500 LOC. With the three-file split, each should land near 100-200 LOC. If any file exceeds 500, surface as BLOCKER.

    **Conventions:**
    - 2-space indentation throughout
    - `_private` prefix preserved
    - Do NOT rename any function, class, constant, exception, or parameter
    - Do NOT change route URLs (`/api/paper-trades/*`), methods, status codes, or response shapes
    - Do NOT change Pydantic validation rules, error message strings, or exception class definitions
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; test ! -f web/routes/paper_trades.py &amp;&amp; test -d web/routes/paper_trades &amp;&amp; test -f web/routes/paper_trades/__init__.py &amp;&amp; test -f web/routes/paper_trades/_models.py &amp;&amp; test -f web/routes/paper_trades/_renderers.py &amp;&amp; for f in web/routes/paper_trades/__init__.py web/routes/paper_trades/_models.py web/routes/paper_trades/_renderers.py; do loc=$(wc -l &lt; "$f"); test "$loc" -le 500 || { echo "FAIL: $f is $loc LOC (cap 500)"; exit 1; }; done &amp;&amp; python -c "from web.routes import paper_trades as pt; assert callable(pt.register); from web.routes.paper_trades import OpenPaperTradeRequest, EditPaperTradeRequest, ClosePaperTradeRequest, _D09_KEYS, _MULTIPLIER, _COST_AUD; print('import surface OK')" &amp;&amp; pytest tests/test_web_paper_trades.py tests/test_system_params.py -x -q &amp;&amp; ruff check web/routes/paper_trades/</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f web/routes/paper_trades.py` passes
    - `test -d web/routes/paper_trades` passes
    - `test -f web/routes/paper_trades/__init__.py` AND `test -f web/routes/paper_trades/_models.py` AND `test -f web/routes/paper_trades/_renderers.py` all pass
    - `wc -l web/routes/paper_trades/__init__.py` returns ≤500
    - `wc -l web/routes/paper_trades/_models.py` returns ≤500
    - `wc -l web/routes/paper_trades/_renderers.py` returns ≤500
    - `grep -c "^def register" web/routes/paper_trades/__init__.py` returns exactly 1
    - `grep -c "^class OpenPaperTradeRequest" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "^class EditPaperTradeRequest" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "^class ClosePaperTradeRequest" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "^class _PaperTradeNotFound" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "^class _PaperTradeImmutable" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "^class _PaperTradeIDOverflow" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "^_D09_KEYS" web/routes/paper_trades/_renderers.py` OR `grep -c "^_D09_KEYS" web/routes/paper_trades/_models.py` returns exactly 1 (one location, not both)
    - `grep -c "^_MULTIPLIER" web/routes/paper_trades/_renderers.py` OR `grep -c "^_MULTIPLIER" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "^_COST_AUD" web/routes/paper_trades/_renderers.py` OR `grep -c "^_COST_AUD" web/routes/paper_trades/_models.py` returns exactly 1
    - `grep -c "'_D09_KEYS'" web/routes/paper_trades/__init__.py` returns at least 1 (re-exported via __all__)
    - `grep -c "'_MULTIPLIER'" web/routes/paper_trades/__init__.py` returns at least 1
    - `grep -c "'_COST_AUD'" web/routes/paper_trades/__init__.py` returns at least 1
    - `python -c "from web.routes.paper_trades import _D09_KEYS, _MULTIPLIER, _COST_AUD"` exits 0
    - `python -c "from web.routes import paper_trades as paper_trades_route; assert callable(paper_trades_route.register)"` exits 0
    - `pytest tests/test_web_paper_trades.py -x -q` exits 0
    - `pytest tests/test_system_params.py -x -q` exits 0 (specifically the tests at lines 230 and 240 that import _MULTIPLIER and _COST_AUD)
    - `pytest tests/ -x -q -k "paper_trade"` exits 0
    - `ruff check web/routes/paper_trades/` exits 0
    - `grep -rn "from web.routes.paper_trades import" --include='*.py' . | wc -l` reports the same count as before the split
  </acceptance_criteria>
  <done>Package replaces the 493-LOC single file; every daughter ≤500 LOC; all three test-imported constants (_D09_KEYS, _MULTIPLIER, _COST_AUD) re-exported via __all__; paper-trade tests + system_params tests green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paper-trade POST ↔ user browser | Pydantic validation at request entry; preserved by moving model classes verbatim |
| paper_trades route ↔ tests | Tests import `_D09_KEYS`, `_MULTIPLIER`, `_COST_AUD` directly — D-03 re-export contract MUST be honoured |
| paper_trades route ↔ service layer | Service imports route-module names directly; preserved by D-03 __init__.py re-exports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06-01 | Tampering | `_MULTIPLIER` or `_COST_AUD` value silently changes during the move (e.g., a constant gets re-derived from a different source) and dashboard P&L math drifts | mitigate | Constants moved verbatim, no re-derivation; tests/test_system_params.py:230,240 (which import these constants) MUST pass — those tests pin the values |
| T-30-06-02 | Repudiation | A Pydantic validator silently drops a constraint during the move (e.g., direction allowlist becomes empty) | mitigate | Model classes moved verbatim; full paper-trade route test suite covers validation edges |
| T-30-06-03 | Denial of Service | Daughter file exceeds 500 LOC and re-introduces D-09 violation | mitigate | LOC gate in acceptance criteria; surface overage as BLOCKER if any file fails the cap |
</threat_model>

<verification>
- All daughter files exist, none exceed 500 LOC
- All test-imported names (`_D09_KEYS`, `_MULTIPLIER`, `_COST_AUD`, `OpenPaperTradeRequest`, `EditPaperTradeRequest`, `ClosePaperTradeRequest`) importable from `web.routes.paper_trades`
- tests/test_web_paper_trades.py + tests/test_system_params.py both green
- `ruff check web/routes/paper_trades/` exits 0
</verification>

<success_criteria>
- OPS-01 acceptance for `paper_trades.py` satisfied: pre-emptive behaviour-preserving split (D-09), every daughter ≤500 LOC.
- Phase 30 success criterion #2 satisfied for this file.
- Three test-import paths (_D09_KEYS, _MULTIPLIER, _COST_AUD) continue to resolve.
- web/app.py import statement unchanged.
- Zero impact on parallel Wave 1 split plans (this plan touches only `web/routes/paper_trades*` files).
</success_criteria>

<output>
After completion, create `.planning/phases/30-file-size-pre-split/30-06-SUMMARY.md`
</output>
