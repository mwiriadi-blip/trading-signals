---
plan: "30-07"
phase: 30
wave: 2
depends_on: ["30-01", "30-02", "30-03", "30-04", "30-05", "30-06"]
autonomous: true
files_modified: []
requirements:
  - OPS-01
  - OPS-03
must_haves:
  truths:
    - "All 5 web/routes/* package splits land cleanly together (trades, dashboard, totp, login, paper_trades)"
    - "AST hex-boundary blocklist extension (OPS-03) lands cleanly alongside the splits"
    - "web/app.py imports for all 5 route modules continue to resolve without edit"
    - "Cross-route import `from web.routes.login import _is_safe_next` (used by web/routes/totp/__init__.py) resolves end-to-end"
    - "Full test suite is green: pytest tests/ -x -q exits 0"
    - "No file under web/routes/ exceeds 500 LOC"
    - "ruff check on all modified package directories exits 0"
    - "Phase 30 success criteria 1, 2, 3 all hold"
  artifacts:
    - path: ".planning/phases/30-file-size-pre-split/30-07-SUMMARY.md"
      provides: "Final-gate verification record"
      contains: "Phase 30 acceptance gate"
  key_links:
    - from: "Wave 1 plans (30-01..30-06)"
      to: "End-to-end integration"
      via: "Final gate"
      pattern: "pytest tests/ -x -q passes"
---

<objective>
Final integration gate for Phase 30. Run the full test suite, the AST hex-boundary guard, and a structural LOC audit after all six Wave 1 plans have landed. This plan owns NO source edits — it is purely a verification + commitment gate. If any check fails, the failing Wave 1 plan must be revised; this plan does not paper over Wave 1 defects.

Purpose: Plans 30-01..30-06 each verify their own slice in isolation. This plan verifies they compose correctly:
- Both packages that share a cross-route import (totp ↔ login via `_is_safe_next`) work together.
- No two splits modified the same external test import path inconsistently.
- The AST blocklist extension (OPS-03) sits cleanly alongside the splits without surfacing any new forbidden-import violation in any pure-math hex module.

Output: `.planning/phases/30-file-size-pre-split/30-07-SUMMARY.md` recording the final test counts, LOC counts per daughter file, and Phase 30 success-criteria sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/30-file-size-pre-split/30-CONTEXT.md
@.planning/phases/30-file-size-pre-split/30-01-SUMMARY.md
@.planning/phases/30-file-size-pre-split/30-02-SUMMARY.md
@.planning/phases/30-file-size-pre-split/30-03-SUMMARY.md
@.planning/phases/30-file-size-pre-split/30-04-SUMMARY.md
@.planning/phases/30-file-size-pre-split/30-05-SUMMARY.md
@.planning/phases/30-file-size-pre-split/30-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final integration gate and write 30-07-SUMMARY.md</name>
  <files>.planning/phases/30-file-size-pre-split/30-07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/30-file-size-pre-split/30-01-SUMMARY.md
    - .planning/phases/30-file-size-pre-split/30-02-SUMMARY.md
    - .planning/phases/30-file-size-pre-split/30-03-SUMMARY.md
    - .planning/phases/30-file-size-pre-split/30-04-SUMMARY.md
    - .planning/phases/30-file-size-pre-split/30-05-SUMMARY.md
    - .planning/phases/30-file-size-pre-split/30-06-SUMMARY.md
    - .planning/ROADMAP.md (Phase 30 § Success Criteria — confirm 1, 2, 3)
  </read_first>
  <action>
    Run the full integration gate. ZERO source edits — if any check fails, STOP and report the failing Wave 1 plan; do NOT patch over the failure here.

    **Gate A — Package directories + single-file deletion:**
    Confirm all 5 single-file route modules are deleted and replaced by packages:
    ```
    for f in web/routes/trades web/routes/dashboard web/routes/totp web/routes/login web/routes/paper_trades; do
      test -d "$f" || { echo "FAIL: $f directory missing"; exit 1; }
      test -f "$f/__init__.py" || { echo "FAIL: $f/__init__.py missing"; exit 1; }
      test ! -f "${f}.py" || { echo "FAIL: legacy ${f}.py still present"; exit 1; }
    done
    ```

    **Gate B — LOC budget across every daughter file (Phase 30 SC #1):**
    Every Python file under `web/routes/` (including new packages) is ≤500 LOC:
    ```
    find web/routes -name '*.py' -type f -exec wc -l {} \; | awk '$1 > 500 { print "FAIL:", $0; exit 1 }'
    ```

    **Gate C — web/app.py imports unchanged:**
    Confirm `web/app.py` lines 40-51 still contain the same `from web.routes import X as X_route` lines (no edits to the import block):
    ```
    grep -c "from web.routes import trades as trades_route" web/app.py    # MUST be 1
    grep -c "from web.routes import dashboard as dashboard_route" web/app.py    # MUST be 1
    grep -c "from web.routes import totp as totp_route" web/app.py    # MUST be 1
    grep -c "from web.routes import login as login_route" web/app.py    # MUST be 1
    grep -c "from web.routes import paper_trades as paper_trades_route" web/app.py    # MUST be 1
    ```

    **Gate D — Public import surface end-to-end:**
    ```
    python -c "
    from web.routes import trades as t; assert callable(t.register)
    from web.routes import dashboard as d; assert callable(d.register)
    from web.routes import totp as tp; assert callable(tp.register)
    from web.routes import login as l; assert callable(l.register)
    from web.routes import paper_trades as pt; assert callable(pt.register)
    from web.routes.trades import OpenTradeRequest, ModifyTradeRequest, CloseTradeRequest
    from web.routes.paper_trades import OpenPaperTradeRequest, EditPaperTradeRequest, ClosePaperTradeRequest, _D09_KEYS, _MULTIPLIER, _COST_AUD
    from web.routes.login import _is_safe_next; assert callable(_is_safe_next)
    print('all import paths resolve')
    "
    ```

    **Gate E — AST hex-boundary guard (Phase 30 SC #3 = OPS-03):**
    ```
    pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent -x -q
    python -c "from tests.test_signal_engine import FORBIDDEN_MODULES; assert {'web', 'news_fetcher', 'news_filter', 'auth_store'} &lt;= FORBIDDEN_MODULES"
    ```

    **Gate F — Full test suite (Phase 30 SC #2):**
    ```
    pytest tests/ -x -q 2>&1 | tail -5
    ```
    Exit must be 0. Test count should be the SAME as the pre-Phase-30 baseline (1880 from STATE.md plan-27-05 — confirm against the count documented in the latest plan summary).

    **Gate G — ruff:**
    ```
    ruff check web/routes/
    ```

    **Step H — Write `.planning/phases/30-file-size-pre-split/30-07-SUMMARY.md`:**
    Record:
    - Final test count (pytest collected / passed / skipped) compared to pre-phase baseline
    - LOC count per daughter file (one line per file under web/routes/)
    - Confirmation that web/app.py import block is byte-identical to pre-phase
    - Confirmation that FORBIDDEN_MODULES contains the 4 new names
    - Confirmation that no pure-math hex module imports any forbidden name (`test_forbidden_imports_absent` exit 0)
    - Sign-off line: "Phase 30 success criteria 1, 2, 3 verified. OPS-01 + OPS-03 closed."

    If ANY gate fails, do NOT write a green SUMMARY. Report the failing gate, identify which Wave 1 plan is responsible, and stop. Phase coordinator decides whether to revise the failing plan or split this gate into a remediation phase.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; for f in web/routes/trades web/routes/dashboard web/routes/totp web/routes/login web/routes/paper_trades; do test -d "$f" &amp;&amp; test -f "$f/__init__.py" &amp;&amp; test ! -f "${f}.py" || { echo "FAIL: $f package layout"; exit 1; }; done &amp;&amp; find web/routes -name '*.py' -type f -exec wc -l {} \; | awk '$1 &gt; 500 { print "FAIL:", $0; exit 1 } END { print "all under 500 LOC" }' &amp;&amp; python -c "
from web.routes import trades as t, dashboard as d, totp as tp, login as l, paper_trades as pt
assert all(callable(m.register) for m in [t, d, tp, l, pt])
from web.routes.trades import OpenTradeRequest, ModifyTradeRequest, CloseTradeRequest
from web.routes.paper_trades import OpenPaperTradeRequest, EditPaperTradeRequest, ClosePaperTradeRequest, _D09_KEYS, _MULTIPLIER, _COST_AUD
from web.routes.login import _is_safe_next
assert callable(_is_safe_next)
from tests.test_signal_engine import FORBIDDEN_MODULES
assert {'web', 'news_fetcher', 'news_filter', 'auth_store'} &lt;= FORBIDDEN_MODULES
print('OK')" &amp;&amp; pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent -x -q &amp;&amp; pytest tests/ -x -q &amp;&amp; ruff check web/routes/ &amp;&amp; test -f .planning/phases/30-file-size-pre-split/30-07-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 route packages exist under `web/routes/`; all 5 legacy `.py` files are deleted
    - `find web/routes -name '*.py' -type f` returns zero files with `wc -l` > 500
    - `web/app.py` import block (lines 40-51) is byte-identical to its pre-Phase-30 state (5 grep checks each return exactly 1)
    - `python -c "from web.routes.login import _is_safe_next; assert callable(_is_safe_next)"` exits 0 (cross-route helper preserved)
    - `python -c "from web.routes.paper_trades import _D09_KEYS, _MULTIPLIER, _COST_AUD"` exits 0 (test-imported constants preserved)
    - `python -c "from web.routes.trades import OpenTradeRequest, ModifyTradeRequest, CloseTradeRequest; from web.routes.paper_trades import OpenPaperTradeRequest, EditPaperTradeRequest, ClosePaperTradeRequest"` exits 0 (Pydantic model imports preserved)
    - `pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent -x -q` exits 0
    - `pytest tests/ -x -q` exits 0 AND the passed-test count is equal to or greater than the pre-Phase-30 baseline (no test silently disappeared)
    - `ruff check web/routes/` exits 0
    - `.planning/phases/30-file-size-pre-split/30-07-SUMMARY.md` exists and contains the sign-off line "Phase 30 success criteria 1, 2, 3 verified. OPS-01 + OPS-03 closed."
  </acceptance_criteria>
  <done>All Phase 30 success criteria verified end-to-end; SUMMARY written; phase ready for `/gsd-verify-work 30`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 1 plans (in-process) ↔ Wave 2 gate | Wave 2 has authority to fail the phase but not to patch defects; if a Wave 1 plan landed an incomplete split, this gate detects it and STOPS rather than papering over |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07-01 | Tampering | A Wave 1 plan leaves a dangling external import (e.g., a test imports a name that no plan re-exported) and tests fail silently in a parallel test path | mitigate | Gate F runs the FULL test suite, not just per-route tests; any silent regression surfaces here. Gate D explicitly tests all known cross-module import paths from the Wave 1 plans. |
| T-30-07-02 | Repudiation | A Wave 1 plan over-claims success in its SUMMARY (e.g., LOC count fudged) | mitigate | This gate re-verifies LOC counts independently via `find ... wc -l` — it does not trust the per-plan SUMMARY; the gate is the source of truth |
| T-30-07-03 | Denial of Service | Two Wave 1 plans both modify a same-name helper in different packages (e.g., both define `_log_failure`) and the second-loaded module shadows the first | accept | Each Wave 1 plan modifies a disjoint package directory under `web/routes/`; same-name helpers in different packages cannot collide because they are namespaced. No mitigation needed beyond the existing namespace. |
</threat_model>

<verification>
- All 6 Wave 1 SUMMARYs exist (one per plan)
- Final gate runs all 7 sub-gates (A-G) clean
- 30-07-SUMMARY.md exists with sign-off
</verification>

<success_criteria>
- Phase 30 success criterion #1 (every daughter file ≤500 LOC) verified
- Phase 30 success criterion #2 (full route + template + test parity) verified
- Phase 30 success criterion #3 (AST blocklist extension) verified
- OPS-01 and OPS-03 both closed
- Phase 30 ready for milestone progress update
</success_criteria>

<output>
After completion, create `.planning/phases/30-file-size-pre-split/30-07-SUMMARY.md`
</output>
