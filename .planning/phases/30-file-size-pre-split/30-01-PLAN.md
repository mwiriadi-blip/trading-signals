---
plan: "30-01"
phase: 30
wave: 1
depends_on: []
autonomous: true
files_modified:
  - tests/test_signal_engine.py
requirements:
  - OPS-03
must_haves:
  truths:
    - "FORBIDDEN_MODULES frozenset contains 'web', 'news_fetcher', 'news_filter', 'auth_store'"
    - "test_forbidden_imports_absent passes against all _HEX_PATHS_ALL with the extended blocklist"
    - "FORBIDDEN_MODULES_BACKTEST_PURE inherits the 4 new names without separate edit"
    - "No hex-tier module currently imports any of the 4 new names (test passes immediately after edit)"
    - "Pre-flight confirms 'web' is safe to add: no existing hex module imports web.* before the blocklist is extended"
  artifacts:
    - path: "tests/test_signal_engine.py"
      provides: "Extended AST blocklist for v1.3 I/O modules"
      contains: "'web', 'news_fetcher', 'news_filter', 'auth_store'"
  key_links:
    - from: "FORBIDDEN_MODULES frozenset (tests/test_signal_engine.py:493)"
      to: "test_forbidden_imports_absent parametrized over _HEX_PATHS_ALL"
      via: "AST walker _top_level_imports()"
      pattern: "imports & FORBIDDEN_MODULES"
---

<objective>
Extend the AST hex-boundary blocklist in `tests/test_signal_engine.py` so pure-math hex modules (signal_engine, sizing_engine, system_params, pnl_engine, alert_engine, backtest/simulator, backtest/metrics) cannot import any of the four v1.3 I/O modules: `web`, `news_fetcher`, `news_filter`, `auth_store`.

Purpose: Forward-looking guard — none of `news_fetcher` / `news_filter` / `auth_store` exists yet, and `web/*` is only touched by sibling adapter layers today. The test passes immediately after the edit. The point is to make any future Phase 31+ contamination fail in CI at the moment of the bad import.

Output: Single edit to the `FORBIDDEN_MODULES` frozenset. `FORBIDDEN_MODULES_BACKTEST_PURE = FORBIDDEN_MODULES | frozenset({'pyarrow'})` picks up the four new names automatically (D-11/D-12, no separate edit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/30-file-size-pre-split/30-CONTEXT.md

<interfaces>
<!-- The exact frozenset to extend lives at tests/test_signal_engine.py:493-504. -->
<!-- Add the 4 new module names as a new cluster (existing convention: one cluster per logical group). -->

Current shape (line ~493, lifted verbatim from the file):
```
FORBIDDEN_MODULES = frozenset({
  # I/O and clock (CLAUDE.md Architecture: pure math, no I/O, no clock reads)
  'datetime', 'os', 'sys', 'subprocess', 'socket', 'time', 'pathlib', 'io',
  # Network (Phase 1 is pure math; no network calls)
  'requests', 'urllib', 'urllib2', 'urllib3', 'http', 'httpx',
  # Sibling hexes (signal_engine <-> state_manager/notifier/dashboard must NOT import each other)
  'state_manager', 'notifier', 'dashboard', 'main',
  # Orchestration and external service deps (belong in other hexes)
  'schedule', 'dotenv', 'pytz', 'yfinance',
})
```

Derived constant (line ~605) inherits automatically — DO NOT edit:
`FORBIDDEN_MODULES_BACKTEST_PURE = FORBIDDEN_MODULES | frozenset({'pyarrow'})`

AST extractor (line ~626, no changes needed): `_top_level_imports(source_path)` returns the top-level root for both `import x.y.z` (returns `x`) and `from x.y import z` (returns `x`). So adding `'web'` to the frozenset catches `from web.routes.dashboard import X` as well as `import web.app`.

Test caller (line ~786):
`@pytest.mark.parametrize('module_path', _HEX_PATHS_ALL)`
`def test_forbidden_imports_absent(self, module_path): imports = _top_level_imports(module_path); leaked = imports & FORBIDDEN_MODULES; assert not leaked, ...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight — verify 'web' is safe to add to FORBIDDEN_MODULES</name>
  <files></files>
  <read_first>
    - .planning/phases/30-file-size-pre-split/30-CONTEXT.md (D-10, D-12 — confirms no hex module imports web.* today, but this task independently verifies the claim at execution time)
  </read_first>
  <action>
    Before touching any source file, verify that no existing hex-tier module already imports `web.*`. Adding `'web'` to FORBIDDEN_MODULES when a hex module already contains `from web import ...` would immediately fail CI.

    Run the following ripgrep command against all hex-tier modules:
    ```
    rg "from web[\. ]|import web" signal_engine.py sizing_engine.py system_params.py pnl_engine.py alert_engine.py backtest/ --include='*.py'
    ```

    **Acceptance criteria:**
    - Command returns no matches (empty output, rg exits 1 meaning no hits) — confirmed no hex module imports web.*. Proceed to Task 1.
    - If any match is found (rg exits 0 with output): STOP. Do NOT add 'web' to FORBIDDEN_MODULES. Document the contaminated file(s) and the offending import lines in the plan summary as a BLOCKER. Notify the phase coordinator. Task 1 MUST NOT run until the contamination is resolved.

    This task is a hard gate. Task 1 does not run until this task exits clean.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; rg "from web[\. ]|import web" signal_engine.py sizing_engine.py system_params.py pnl_engine.py alert_engine.py backtest/ --include='*.py'; RG_EXIT=$?; if [ "$RG_EXIT" -eq 1 ]; then echo "PRE-FLIGHT PASS: no hex module imports web.*"; else echo "PRE-FLIGHT FAIL: contamination found — see above"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - `rg "from web[\. ]|import web" signal_engine.py sizing_engine.py system_params.py pnl_engine.py alert_engine.py backtest/ --include='*.py'` produces empty output (rg exit code 1 = no matches found) — confirmed no hex module imports web.*
    - If rg exits 0 (matches found): BLOCKER — file and line documented in summary; Task 1 does not run
  </acceptance_criteria>
  <done>rg output is empty (no hits) — confirmed no hex module imports web.*. Safe to proceed to Task 1.</done>
</task>

<task type="auto">
  <name>Task 1: Extend FORBIDDEN_MODULES with 4 v1.3 I/O module names</name>
  <files>tests/test_signal_engine.py</files>
  <read_first>
    - tests/test_signal_engine.py (full file — confirm line ~493 FORBIDDEN_MODULES still matches the snapshot in the interfaces block; the AST walker at line ~626 still returns top-level root names; the derived FORBIDDEN_MODULES_BACKTEST_PURE constant at line ~605 is unchanged)
    - .planning/phases/30-file-size-pre-split/30-CONTEXT.md (D-10, D-11, D-12 — locked decisions)
  </read_first>
  <action>
    Add a new logical cluster to the existing `FORBIDDEN_MODULES` frozenset in `tests/test_signal_engine.py`. The four names — `'web'`, `'news_fetcher'`, `'news_filter'`, `'auth_store'` — go in a single new line at the end of the frozenset, just before the closing `})`, with a comment marking the v1.3 I/O cluster.

    Concrete insertion (place between the existing `'schedule', 'dotenv', 'pytz', 'yfinance',` line and the `})` close):
    ```
        # v1.3 I/O peers (Phase 30 OPS-03 — forward-looking guard for v1.3 modules):
        # web/* is the FastAPI adapter; news_fetcher/news_filter are the news-integration
        # peers introduced in Phase 36; auth_store is the user registry surface extended
        # in Phase 32. None of these are pure-math hex peers.
        'web', 'news_fetcher', 'news_filter', 'auth_store',
    ```

    Do NOT edit `FORBIDDEN_MODULES_BACKTEST_PURE` (line ~605) — it is derived as `FORBIDDEN_MODULES | frozenset({'pyarrow'})` and inherits the 4 new names automatically (D-11/D-12).

    Do NOT add the 4 new names to any of the per-module sets (`FORBIDDEN_MODULES_STDLIB_ONLY`, `FORBIDDEN_MODULES_STATE_MANAGER`, `FORBIDDEN_MODULES_DATA_FETCHER`, `FORBIDDEN_MODULES_MAIN`, `FORBIDDEN_MODULES_DASHBOARD`, `FORBIDDEN_MODULES_NOTIFIER`). Those are sibling-hex guards for non-pure-math modules and are out of scope for OPS-03.

    Indent the new line with **2 spaces** to match the existing file convention (R-05 — confirmed by `.editorconfig` and the tokenize-aware indent check in `test_no_four_space_indent`).
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; python -c "import ast, sys; tree = ast.parse(open('tests/test_signal_engine.py').read()); names = []
for node in ast.walk(tree):
    if isinstance(node, ast.Assign) and any(getattr(t, 'id', '') == 'FORBIDDEN_MODULES' for t in node.targets):
        if isinstance(node.value, ast.Call) and getattr(node.value.func, 'id', '') == 'frozenset':
            arg = node.value.args[0]
            if isinstance(arg, ast.Set):
                names = [e.value for e in arg.elts if isinstance(e, ast.Constant)]
required = {'web', 'news_fetcher', 'news_filter', 'auth_store'}
missing = required - set(names)
sys.exit(0 if not missing else 1)" &amp;&amp; pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'news_fetcher'" tests/test_signal_engine.py` returns exactly 1
    - `grep -c "'news_filter'" tests/test_signal_engine.py` returns exactly 1
    - `grep -c "'auth_store'" tests/test_signal_engine.py` returns exactly 1
    - `grep -c "'web'" tests/test_signal_engine.py` returns at least 1 (the new entry in FORBIDDEN_MODULES)
    - The new line follows the `'schedule', 'dotenv', 'pytz', 'yfinance',` line inside the FORBIDDEN_MODULES frozenset (verified by reading the file and confirming the four names appear inside the literal that closes the `FORBIDDEN_MODULES = frozenset({...})` assignment)
    - `pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent -x -q` exits 0 (parametrized over all _HEX_PATHS_ALL; passes because no hex module currently imports any of the four new names)
    - `pytest tests/test_signal_engine.py -x -q` exits 0 (no other test regressed; in particular, `test_no_four_space_indent` confirms the 2-space indent on the new line)
    - `python -c "from tests.test_signal_engine import FORBIDDEN_MODULES_BACKTEST_PURE; assert {'web', 'news_fetcher', 'news_filter', 'auth_store'} <= FORBIDDEN_MODULES_BACKTEST_PURE"` exits 0 (D-11 inheritance confirmed without editing the derived constant)
  </acceptance_criteria>
  <done>FORBIDDEN_MODULES contains the 4 new names; test_forbidden_imports_absent green; full test_signal_engine.py green; FORBIDDEN_MODULES_BACKTEST_PURE inherits the 4 names without separate edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure-math hex → I/O adapters | The AST guard sits at the test boundary. It enforces a structural invariant (no hex import of `web` / `news_fetcher` / `news_filter` / `auth_store`) before code lands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Tampering | pure-math hex modules silently grow I/O dependencies (e.g., a future PR adds `from web.routes import x` inside `signal_engine.py`) | mitigate | Extended FORBIDDEN_MODULES catches the import at CI time via `test_forbidden_imports_absent`; the test fails with a clear `illegally imports forbidden module(s)` message naming the leaked module. |
| T-30-01-02 | Repudiation | A hex-tier contamination ships unnoticed because the test only knew about v1.2-era module names | mitigate | Forward-looking guard added per D-10/D-12 BEFORE any v1.3 I/O module is built; the moment news_fetcher.py / news_filter.py / auth_store extensions land, any accidental cross-import from a pure-math hex fails immediately. |
| T-30-01-03 | Tampering | Adding 'web' to FORBIDDEN_MODULES when a hex module already imports web.* would break CI on existing code | mitigate | Task 0 (pre-flight grep gate) runs BEFORE any edit; if any hex module already imports web.*, the executor stops and does not modify FORBIDDEN_MODULES until contamination is resolved. |
</threat_model>

<verification>
- `pytest tests/test_signal_engine.py -x -q` exits 0
- AST extraction confirms the 4 new names are members of `FORBIDDEN_MODULES`
- `FORBIDDEN_MODULES_BACKTEST_PURE` derivation inherits the 4 names (no separate edit)
- Pre-flight grep confirms no existing hex module imports web.* before the blocklist extension
</verification>

<success_criteria>
- OPS-03 acceptance criterion (Phase 30 success criterion #3) is satisfied: AST blocklist now blocks all four v1.3 I/O module names from any pure-math hex module.
- Test passes immediately (no hex module currently imports any of the 4 names per D-12).
- Zero impact on parallel Wave 1 split plans (this plan touches only `tests/test_signal_engine.py`).
</success_criteria>

<output>
After completion, create `.planning/phases/30-file-size-pre-split/30-01-SUMMARY.md`
</output>
