---
phase: 36-per-route-user-id-scoping-privacy-boundary-per-user-flock
plan: 03
type: execute
wave: 3
depends_on:
  - 36-01
  - 36-02
files_modified:
  - tests/test_state_manager.py
  - tests/test_web_paper_trades.py
  - tests/test_web_trades.py
  - tests/test_tenant_isolation.py
autonomous: true
requirements:
  - TENANT-02
  - TENANT-03

must_haves:
  truths:
    - "TestMutateUserState passes: write lands in correct user bucket, lock dir is auto-created"
    - "test_web_paper_trades has 4 paired 404-for-other-users tests covering every entity-ID route"
    - "test_web_trades has 5 paired 404-for-other-users tests covering every entity-ID route"
    - "TestTenantIsolation passes: user A's trade content absent from GET /admin/users response"
    - "Full suite green"
  artifacts:
    - path: "tests/test_state_manager.py"
      provides: "TestMutateUserState with 3 passing tests"
      contains: "def test_mutate_user_state_writes_to_user_bucket"
    - path: "tests/test_web_paper_trades.py"
      provides: "4 paired 404-for-other-users tests"
      contains: "test_edit_paper_trade_returns_404_for_other_users_entity"
    - path: "tests/test_web_trades.py"
      provides: "5 paired 404-for-other-users tests"
      contains: "test_close_trade_returns_404_for_other_users_position"
    - path: "tests/test_tenant_isolation.py"
      provides: "TestTenantIsolation passing"
      contains: "test_admin_users_response_has_no_trade_content"
  key_links:
    - from: "tests/test_web_paper_trades.py::test_edit_paper_trade_returns_404_for_other_users_entity"
      to: "web/routes/paper_trades/__init__.py::edit_paper_trade"
      via: "authenticate as uid_b; request patch for uid_a's trade_id"
      pattern: "status_code == 404"
    - from: "tests/test_tenant_isolation.py::test_admin_users_response_has_no_trade_content"
      to: "web/routes/admin/__init__.py::admin_list_users"
      via: "GET /admin/users; assert TRADE_CONTENT_RE has zero matches"
      pattern: "TRADE_CONTENT_RE.search.*is None"
---

<objective>
Wave 2 test coverage: implement all TENANT-02 and TENANT-03 test assertions against the now-migrated routes. Complete TestMutateUserState, add 9 paired 404-for-other-users tests across paper_trades and trades test files, and complete TestTenantIsolation's isolation assertion.

Purpose: Closes all Phase 36 quality gates. Without these tests, the migration is unverified and could regress silently.

Output:
- test_state_manager.py: TestMutateUserState 3 tests passing
- test_web_paper_trades.py: 4 new 404-for-other-users tests
- test_web_trades.py: 5 new 404-for-other-users tests
- test_tenant_isolation.py: TestTenantIsolation isolation assertion passing
</objective>

<execution_context>
@/Users/marcwiriadisastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcwiriadisastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-per-route-user-id-scoping-privacy-boundary-per-user-flock/36-CONTEXT.md
@.planning/phases/36-per-route-user-id-scoping-privacy-boundary-per-user-flock/36-RESEARCH.md
@.planning/phases/36-per-route-user-id-scoping-privacy-boundary-per-user-flock/36-01-SUMMARY.md
@.planning/phases/36-per-route-user-id-scoping-privacy-boundary-per-user-flock/36-02-SUMMARY.md

<interfaces>
From tests/conftest.py (updated in Plan 01):
  client_with_state_v3 — v12-shaped state, stubs mutate_state + mutate_user_state, yields (client, state_box, captured_saves)
  client_with_state_v6 — same but also has paper_trades seeded in state['users'][_ADMIN_UID]['paper_trades']
  VALID_SECRET — 32-char string used for session cookie signing
  isolated_auth_json — fixture that patches auth.json to a tmp file

From tests/test_web_admin.py:
  _build_session_cookie(uid, role='admin') helper for building session cookies
  isolated_auth_json fixture

entity-ID routes per RESEARCH Q6:
  paper_trades:
    PATCH  /paper-trade/{trade_id}             → test_edit_paper_trade_returns_404_for_other_users_entity
    DELETE /paper-trade/{trade_id}             → test_delete_paper_trade_returns_404_for_other_users_entity
    POST   /paper-trade/{trade_id}/close       → test_close_paper_trade_returns_404_for_other_users_entity
    GET    /paper-trade/{trade_id}/close-form  → test_get_close_form_returns_404_for_other_users_entity
  trades (no trade_id; instrument-keyed):
    POST   /trades/close                       → test_close_trade_returns_404_for_other_users_position
    POST   /trades/modify                      → test_modify_trade_returns_404_for_other_users_position
    GET    /trades/close-form?instrument=X     → test_close_form_returns_404_for_other_users_position
    GET    /trades/modify-form?instrument=X    → test_modify_form_returns_404_for_other_users_position
    GET    /trades/cancel-row?instrument=X     → test_cancel_row_returns_404_for_other_users_position

RESEARCH Pitfall 5 (trades 404 vs 409):
  current behavior: 409 when no position for this user
  for 404-for-other-users test: seed uid_a with SPI200 position; authenticate as uid_b;
  uid_b has NO SPI200 position → existing 409 behavior fires. The test must assert
  that uid_b cannot access uid_a's position. 409 is acceptable here — the key assertion is
  that uid_b gets an error (not uid_a's data). Document the distinction in test docstrings.

TRADE_CONTENT_RE from test_tenant_isolation.py (Plan 01 stub):
  re.compile(r'(entry_price|n_contracts|"direction":\s*"(LONG|SHORT)")', re.IGNORECASE)

TestTenantIsolation two_user_client fixture pattern (from PATTERNS.md):
  Monkeypatches load_state to return seeded state with uid_a having 5 paper trades
  Builds admin session cookie for uid_a (admin role in isolated_auth_json)
  GET /admin/users returns list; assert TRADE_CONTENT_RE has zero matches in json text
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete TestMutateUserState in test_state_manager.py</name>
  <files>tests/test_state_manager.py</files>
  <read_first>
    - tests/test_state_manager.py — find TestMutateUserState class stubs added in Plan 01; read the exact test method bodies written; identify what is stubbed vs fully implemented; also read TestLoadSave for fixture pattern (tmp_path usage)
    - state_manager/__init__.py — verify mutate_user_state and load_user_state are present and in __all__
    - state_manager/migrations.py — verify _ADMIN_UID constant value and reset_state() v12 shape
  </read_first>
  <behavior>
    - test_mutate_user_state_writes_to_user_bucket: creates a fresh state file in tmp_path, calls mutate_user_state(_ADMIN_UID, mutator, path=tmp_path/'state.json') where mutator sets state['users'][_ADMIN_UID]['account'] = 99_999.0, then asserts load_state(path=tmp_path/'state.json')['users'][_ADMIN_UID]['account'] == 99_999.0
    - test_mutate_user_state_creates_lock_dir: monkeypatches the lock_dir Path inside mutate_user_state to point to tmp_path/'state_users'; calls mutate_user_state; asserts (tmp_path/'state_users').exists(); asserts lock file (tmp_path/'state_users'/f'{uid}.lock').exists()
    - test_load_user_state_returns_user_slice: creates a fresh state file in tmp_path; calls load_user_state(_ADMIN_UID, path=tmp_path/'state.json'); asserts result == load_state(path=tmp_path/'state.json')['users'][_ADMIN_UID]
  </behavior>
  <action>
    Replace the stub implementations in TestMutateUserState with real implementations. For test_mutate_user_state_creates_lock_dir: since the lock dir path is hardcoded as Path('state/users') inside mutate_user_state, the test must monkeypatch state_manager to use a custom lock dir. The cleanest approach is to monkeypatch the Path constructor or pass a lock_dir argument. Inspect the actual mutate_user_state implementation to determine how to redirect the lock dir in tests without changing production code. If mutate_user_state uses a module-level Path('state/users') literal, monkeypatch may need to patch the Path class or use a tmp_path that is the CWD's state/users. Alternatively, change working directory in the test using monkeypatch.chdir(tmp_path) so the relative path 'state/users' resolves under tmp_path. Use monkeypatch.chdir(tmp_path) as the simplest approach — it redirects all relative paths including the lock dir AND the STATE_FILE default.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && .venv/bin/pytest -x --tb=short tests/test_state_manager.py::TestMutateUserState -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/pytest -x tests/test_state_manager.py::TestMutateUserState exits 0
    - All 3 tests in TestMutateUserState pass (no xfail, no skip)
    - test_mutate_user_state_writes_to_user_bucket asserts account == 99_999.0
    - test_mutate_user_state_creates_lock_dir asserts lock file existence
    - test_load_user_state_returns_user_slice asserts equality with full state slice
  </acceptance_criteria>
  <done>TestMutateUserState 3/3 green; TENANT-02 unit coverage complete</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 404-for-other-users tests to test_web_paper_trades.py and test_web_trades.py; complete TestTenantIsolation</name>
  <files>tests/test_web_paper_trades.py, tests/test_web_trades.py, tests/test_tenant_isolation.py</files>
  <read_first>
    - tests/test_web_paper_trades.py — full file; find the last test class; identify how session cookies are built for requests; check how client_with_state_v6 fixture is used and what it yields; find an existing test that PATCHes a specific trade_id as a pattern to copy
    - tests/test_web_trades.py — full file; find the last test class; identify how client_with_state_v3 is used; find an existing test that hits /trades/close or /trades/close-form as a pattern to copy
    - tests/test_tenant_isolation.py — read the current stub implementation; find two_user_client fixture; find TestTenantIsolation stub; identify what is incomplete
    - tests/test_web_admin.py — read _build_session_cookie helper body and how it builds cookies for different uids/roles
    - tests/conftest.py — confirm isolated_auth_json fixture exists and how it seeds auth.json
  </read_first>
  <behavior>
    In test_web_paper_trades.py — add class TestEntityIdOwnership (or append tests to an existing class) with 4 tests per D-14:
      test_edit_paper_trade_returns_404_for_other_users_entity: seed uid_a's paper_trades in state, build session cookie for uid_b, PATCH /paper-trade/{uid_a_trade_id}, assert 404
      test_delete_paper_trade_returns_404_for_other_users_entity: same setup, DELETE /paper-trade/{uid_a_trade_id}, assert 404
      test_close_paper_trade_returns_404_for_other_users_entity: same, POST /paper-trade/{uid_a_trade_id}/close, assert 404
      test_get_close_form_returns_404_for_other_users_entity: same, GET /paper-trade/{uid_a_trade_id}/close-form, assert 404
    Setup for paper_trades tests: create state where uid_a has 1 paper trade with id='SPI200-20260501-001'; uid_b's paper_trades is empty. Monkeypatch load_state (for read paths) and mutate_user_state to return this seeded state.

    In test_web_trades.py — add 5 tests per D-14 (RESEARCH Q6):
      test_close_trade_returns_404_for_other_users_position: seed uid_a with SPI200 LONG position; auth as uid_b (no SPI200 position); POST /trades/close with instrument='SPI200'; assert 409 (uid_b has no position at all — correct behavior) OR 404. Document which status code the migrated route returns and assert it. The key assertion is that uid_b cannot see/access uid_a's position data in the response body.
      test_modify_trade_returns_404_for_other_users_position: same, POST /trades/modify
      test_close_form_returns_404_for_other_users_position: GET /trades/close-form?instrument=SPI200; assert 404
      test_modify_form_returns_404_for_other_users_position: GET /trades/modify-form?instrument=SPI200; assert 404
      test_cancel_row_returns_404_for_other_users_position: GET /trades/cancel-row?instrument=SPI200; assert 404

    In test_tenant_isolation.py — complete the two_user_client fixture and test_admin_users_response_has_no_trade_content:
      Fixture seeds state['users'][uid_a]['paper_trades'] with 5 items each containing entry_price, n_contracts, direction: 'LONG'
      Monkeypatches load_state to return this seeded state
      Registers uid_a as admin in isolated_auth_json (so GET /admin/users is permitted)
      test asserts response.status_code == 200 and TRADE_CONTENT_RE.search(response.text) is None
  </behavior>
  <action>
    For paper_trades tests: the challenge is that client_with_state_v6 monkeypatches mutate_user_state but the read paths (close-form) use load_user_state. The test must also monkeypatch load_user_state to return the seeded user state. Check if load_user_state is monkeypatched in the fixture; if not, add a direct monkeypatch in each test that uses a read path.

    For the TRADE_CONTENT_RE assertion in test_tenant_isolation.py: call json() on the response (not text) to get the parsed JSON string for the assertion. The serialized JSON will contain field names as strings. TRADE_CONTENT_RE searches for "entry_price", "n_contracts", "direction"... with value patterns. Since GET /admin/users returns PublicUserSummary objects (not trade data), the assertion should be trivially green — but it must be written correctly (assert against JSON body string).

    For trades read-path tests (close_form, modify_form, cancel_row): these are GET requests that call load_user_state(user_id)['positions']. When uid_b has no SPI200 position, the handler should return 404. Verify this is the case after Plan 02 migration; if the handler returns a different status code, adapt the assertion and add a comment explaining the behavior.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && .venv/bin/pytest -x --tb=short tests/test_web_paper_trades.py -k "other_users" -v 2>&1 | tail -15 && .venv/bin/pytest -x --tb=short tests/test_web_trades.py -k "other_users" -v 2>&1 | tail -15 && .venv/bin/pytest -x --tb=short tests/test_tenant_isolation.py::TestTenantIsolation::test_admin_users_response_has_no_trade_content -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/pytest -k "test_edit_paper_trade_returns_404_for_other_users_entity" exits 0
    - .venv/bin/pytest -k "test_delete_paper_trade_returns_404_for_other_users_entity" exits 0
    - .venv/bin/pytest -k "test_close_paper_trade_returns_404_for_other_users_entity" exits 0
    - .venv/bin/pytest -k "test_get_close_form_returns_404_for_other_users_entity" exits 0
    - .venv/bin/pytest -k "other_users_position" exits 0 (all 5 trades tests)
    - .venv/bin/pytest tests/test_tenant_isolation.py::TestTenantIsolation::test_admin_users_response_has_no_trade_content exits 0
    - TRADE_CONTENT_RE.search(response_body) is None for GET /admin/users response
    - test_crash_email_body_has_no_trade_content is marked skip (not xfail error) until Phase 37
    - Full suite (.venv/bin/pytest -x --tb=short) exits 0
  </acceptance_criteria>
  <done>9 new 404-for-other-users tests green; TestTenantIsolation isolation assertion green; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uid_b cookie → uid_a entity | A user with a valid session presents a different user's entity ID; route must return error not data |
| Test fixtures → production behavior | If fixtures stub mutate_user_state incorrectly, tests pass even if production routes are broken |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Elevation of Privilege (IDOR) | test coverage for entity-ID routes | mitigate | 9 paired 404-for-other-users tests prove ownership check fires correctly for every entity-ID route |
| T-36-10 | Information Disclosure | TestTenantIsolation | mitigate | TRADE_CONTENT_RE regex confirms zero trade-content fields in GET /admin/users response; test is part of the milestone-wide quality gate that runs on every Phase 36-40 PR |
</threat_model>

<verification>
After both tasks complete:

.venv/bin/pytest -x --tb=short tests/test_state_manager.py::TestMutateUserState -v
.venv/bin/pytest -x --tb=short tests/test_web_paper_trades.py -k "other_users" -v
.venv/bin/pytest -x --tb=short tests/test_web_trades.py -k "other_users" -v
.venv/bin/pytest -x --tb=short tests/test_tenant_isolation.py -v
.venv/bin/pytest -x --tb=short  # full suite gate

Counts:
  TestMutateUserState: 3 passing
  test_web_paper_trades 404-for-other-users: 4 passing
  test_web_trades 404-for-other-users: 5 passing
  TestTenantIsolation: 1 passing + 1 skip (crash email deferred to Phase 37)
</verification>

<success_criteria>
- TENANT-02: TestMutateUserState 3/3 green; per-user flock wrapper verified correct
- TENANT-03: TestTenantIsolation admin isolation assertion green; no trade content in /admin/users response
- D-14: 9 paired 404-for-other-users tests covering all entity-ID routes in paper_trades + trades
- Full test suite exits 0
- Phase 36 quality gate established for Phases 37-40
</success_criteria>

<output>
After completion, create .planning/phases/36-per-route-user-id-scoping-privacy-boundary-per-user-flock/36-03-SUMMARY.md
</output>
