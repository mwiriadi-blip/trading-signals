---
phase: 16.1
slug: phone-friendly-auth-ux-for-dashboard-access
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-27
approved: 2026-04-27
---

# Phase 16.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: extracted from `16.1-RESEARCH.md` §Validation Architecture (committed `902747b`).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.3.3 + pytest-freezer 0.4.9 (pinned in `requirements.txt`) |
| **Config file** | `tests/conftest.py` (project root has no `pyproject.toml` test config — convention is `tests/test_*.py` discovery) |
| **Quick run command** | `pytest tests/test_web_auth_basic.py tests/test_web_auth_cookie.py tests/test_web_routes_login.py -x` |
| **Full suite command** | `pytest tests/ -q` |
| **Estimated runtime** | ~30 seconds (full suite ~2-3 min — 700+ existing tests) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_web_auth_basic.py tests/test_web_auth_cookie.py tests/test_web_routes_login.py tests/test_web_auth_middleware.py -x`
- **After every plan wave:** Run `pytest tests/ -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds (quick-run) / 180 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 16.1-01-W0 | 01 | 0 | AUTH-04, AUTH-05, AUTH-06 | T-16.1-01..03 | Wave 0 — test stubs + conftest fixtures | scaffold | `test -f tests/test_web_auth_basic.py` | ❌ W0 | ⬜ pending |
| 16.1-01-01 | 01 | 1 | AUTH-04 | T-16.1-01 | Basic Auth `Authorization: Basic base64(user:pw)` → 200 | unit | `pytest tests/test_web_auth_basic.py::TestBasicAuthGrant -x` | ❌ W0 | ⬜ pending |
| 16.1-01-02 | 01 | 1 | AUTH-04 | T-16.1-04 | Sec-Fetch sniff: `navigate+document` → 302; `cors+empty` → 401 | unit | `pytest tests/test_web_auth_middleware.py::TestSecFetchSniff -x` | ⚠️ Extend existing | ⬜ pending |
| 16.1-01-03 | 01 | 1 | AUTH-05 | — | Header path unchanged: `curl -H "X-Trading-Signals-Auth: <secret>" /` → 200 | regression | `pytest tests/test_web_auth_middleware.py::TestHeaderPathPreserved -x` | ⚠️ Extend existing | ⬜ pending |
| 16.1-01-04 | 01 | 1 | AUTH-06 | T-16.1-05 | `hmac.compare_digest` used for username + secret | grep + ast | `grep -rn "==.*WEB_AUTH_SECRET\|WEB_AUTH_SECRET.*==" web/ tests/ \| grep -v "compare_digest\|test_" \| wc -l` returns 0 | manual / verify-phase | ⬜ pending |
| 16.1-01-05 | 01 | 1 | AUTH-07 | — | Curl 401 contract: no Sec-Fetch + no `text/html` Accept → 401 plain text, no `WWW-Authenticate`, no `Set-Cookie`, no `Location` | unit | `pytest tests/test_web_auth_middleware.py::TestCurlContract -x` | ⚠️ Extend existing | ⬜ pending |
| 16.1-01-06 | 01 | 1 | D-08 boot | T-16.1-06 | `WEB_AUTH_USERNAME` missing/empty/contains-`:` → RuntimeError | unit | `pytest tests/test_web_app_factory.py::TestUsernameValidation -x` | ⚠️ Extend existing | ⬜ pending |
| 16.1-02-W0 | 02 | 0 | AUTH-04, D-10..D-14 | T-16.1-07..09 | Wave 0 — itsdangerous pin + login/cookie test stubs + conftest fixtures (`valid_cookie_token`) | scaffold | `test -f tests/test_web_auth_cookie.py && test -f tests/test_web_routes_login.py && grep -q 'itsdangerous==' requirements.txt` | ❌ W0 | ⬜ pending |
| 16.1-02-01 | 02 | 2 | AUTH-04 | T-16.1-07 | Login flow: GET /login → form → POST → 302 → / → 200 | integration | `pytest tests/test_web_routes_login.py::TestLoginFlow -x` | ❌ W0 | ⬜ pending |
| 16.1-02-02 | 02 | 2 | AUTH-04, D-10..D-11 | T-16.1-08 | Cookie validation: signed token + max_age=43200 → grant; expired/tampered/malformed → 302 (browser) or 401 (script) | unit | `pytest tests/test_web_auth_cookie.py::TestCookieValidation -x` | ❌ W0 | ⬜ pending |
| 16.1-02-03 | 02 | 2 | AUTH-05 | — | Belt-and-suspenders: HTMX form POST with header AND cookie both authenticate independently | integration | `pytest tests/test_web_auth_cookie.py::TestBeltAndSuspenders -x` | ❌ W0 | ⬜ pending |
| 16.1-02-04 | 02 | 2 | AUTH-06 | T-16.1-10 | Open-redirect blocked: 12+ payloads (`/\evil.com`, `\\evil.com`, `//evil.com`, scheme-prefix, URL-encoded variants, javascript:) → 302 to `/` not target | parametrized unit | `pytest tests/test_web_routes_login.py::test_open_redirect_blocked -x` | ❌ W0 | ⬜ pending |
| 16.1-02-05 | 02 | 2 | D-12 logout | — | Cookie deletion attrs match creation (Path=/, Secure, HttpOnly, SameSite=Strict ALL present in Set-Cookie text) | unit | `pytest tests/test_web_routes_login.py::test_logout_deletion_attrs_match_creation -x` | ❌ W0 | ⬜ pending |
| 16.1-02-06 | 02 | 2 | AUTH-04 | T-16.1-11 | Audit log fires EXACTLY ONCE per failed request (sampling pyramid 1) | parametrized unit | `pytest tests/test_web_auth_middleware.py::test_audit_log_fires_exactly_once_per_failed_request -x` | ⚠️ Extend existing | ⬜ pending |
| 16.1-02-07 | 02 | 2 | (architectural — documents Deferred Idea) | — | Logout does NOT invalidate existing cookie value (sampling pyramid 2 — counter-intuitive assertion documenting stateless-cookie limitation) | unit | `pytest tests/test_web_routes_login.py::test_logout_does_NOT_invalidate_existing_cookie_value -x` | ❌ W0 | ⬜ pending |
| 16.1-02-08 | 02 | 2 | AUTH-04, AUTH-07 | — | UAT scenarios: 16.1-A (iPhone Safari), 16.1-B (URL-bar Basic Auth), 16.1-C (logout flow) — manual operator-run | manual | `cat .planning/phases/16.1-phone-friendly-auth-ux-for-dashboard-access/16.1-HUMAN-UAT.md` | ❌ W0 (Plan 02 creates) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky · W0 = Wave 0 scaffold*

---

## Wave 0 Requirements

Plan 16.1-01 Wave 0 (scaffolds for Basic Auth wave):

- [ ] `tests/test_web_auth_basic.py` — empty file with `# Phase 16.1-01 stub` comment + `import pytest` + 1 `def test_placeholder(): pass` (deletable in Wave 1)
- [ ] Extend `tests/test_web_app_factory.py` — add `class TestUsernameValidation:` skeleton (3 tests: missing, empty, contains-`:`)

Plan 16.1-02 Wave 0 (scaffolds for cookie wave):

- [ ] `requirements.txt` — append `itsdangerous==2.2.0` pinned per RESEARCH.md confirmation
- [ ] `tests/test_web_auth_cookie.py` — empty file with stub + import
- [ ] `tests/test_web_routes_login.py` — empty file with stub + import
- [ ] Extend `tests/conftest.py` — add `valid_cookie_token` fixture that constructs a signature-valid token using the same serializer the production middleware uses (per RESEARCH.md cookie-hygiene §"two-pronged TestClient strategy"). Fixture body:
  ```python
  @pytest.fixture
  def valid_cookie_token(monkeypatch):
    monkeypatch.setenv('WEB_AUTH_USERNAME', 'marc')
    monkeypatch.setenv('WEB_AUTH_SECRET', 'a' * 32)
    from itsdangerous import URLSafeTimedSerializer
    serializer = URLSafeTimedSerializer('a' * 32, salt='tsi-session-cookie')
    return serializer.dumps({'u': 'marc', 'iat': 1745740800})  # 2026-04-27 00:00 UTC
  ```

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real iPhone Safari first-visit → /login form renders → submit → dashboard loads | AUTH-04 | Browser navigation against production droplet — cannot be automated in pytest | `16.1-HUMAN-UAT.md` §UAT-16.1-A — operator opens `https://signals.<owned-domain>.com/` on iPhone Safari, no extension installed, observes 302 → /login → form → submit → dashboard. Records 5-field UAT row per Phase 16 D-10 schema. |
| Real iPhone Chrome same flow | AUTH-04 | Browser-vendor parity check (Sec-Fetch behavior + Keychain semantics differ Safari vs Chrome) | `16.1-HUMAN-UAT.md` §UAT-16.1-A continued |
| URL-bar `https://user:secret@signals.<owned-domain>.com/` Basic Auth path | AUTH-04 | Tests Safari Keychain Basic-Auth caching behavior (vendor-specific, iOS-version-specific) | `16.1-HUMAN-UAT.md` §UAT-16.1-B — operator types creds in URL bar; observes 200 + dashboard immediately + Keychain prompt to save |
| POST /logout → cookie cleared → next visit → 302 → /login | D-12 | Verifies the operator-side cookie-deletion experience (browser receives Set-Cookie with Max-Age=0, then forgets) | `16.1-HUMAN-UAT.md` §UAT-16.1-C — operator clicks "Sign out" button → sees confirmation page → revisits / → expects 302 → /login (cookie gone). Also verifies the Basic-Auth-Keychain footnote text on confirmation page. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`test_web_auth_basic.py`, `test_web_auth_cookie.py`, `test_web_routes_login.py`, `valid_cookie_token` fixture)
- [ ] No watch-mode flags (`-x` first-failure stop is explicit, not watch)
- [ ] Feedback latency < 30s (quick-run) / 180s (full suite)
- [ ] `nyquist_compliant: true` set in frontmatter (after planner verifies Wave 0 task IDs match this map)

**Approval:** pending — planner sets `nyquist_compliant: true` after task IDs in 16.1-01-PLAN.md and 16.1-02-PLAN.md align with the verification map above.
