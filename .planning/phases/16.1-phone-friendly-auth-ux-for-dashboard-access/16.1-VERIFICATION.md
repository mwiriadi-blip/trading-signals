---
phase: 16.1-phone-friendly-auth-ux-for-dashboard-access
verified: 2026-04-29T18:55:00Z
status: human_needed
score: 9/9 requirements verified (operator UAT pending)
re_verification:
  is_re_verification: false
human_verification:
  - test: UAT-16.1-A — iPhone Safari first-visit + cookie login + TOTP enrollment
    expected: 302 → /login → form → enter creds → /enroll-totp → scan QR → submit code → dashboard with Sign Out button
    why_human: real iPhone Safari, real authenticator app, real QR scan
  - test: UAT-16.1-B — subsequent visit (no trust-device) → /verify-totp → enter code → dashboard
    expected: cookie/TOTP gating intact, generic error on bad code
    why_human: real device, real authenticator code
  - test: UAT-16.1-C — subsequent visit with trust-device cookie → no TOTP prompt
    expected: tsi_trusted skips /verify-totp; 30-day persistence requires multi-day check
    why_human: time-gated; needs multi-day
  - test: UAT-16.1-D — lost-phone scenario — Lost 2FA? → email link → /reset-totp → /enroll-totp?reset=1
    expected: Resend email arrives, link works, reset flow completes
    why_human: real Gmail delivery + real authenticator
  - test: UAT-16.1-E — /devices revoke → revoked device's tsi_trusted no longer grants
    expected: revoked uuid refused at middleware even with valid signature
    why_human: per-device cookie state needs two real sessions
  - test: UAT-16.1-F — curl with X-Trading-Signals-Auth header still returns 200 (AUTH-05 regression)
    expected: byte-level dashboard HTML over real TLS
    why_human: live droplet endpoint check
  - test: UAT-16.1-G — curl without header gets 401 unauthorized plain-text (AUTH-07 contract)
    expected: 401 plain-text, no WWW-Authenticate, no Set-Cookie, no Location
    why_human: live droplet endpoint check
---

# Phase 16.1: Phone-friendly auth UX for dashboard access — Verification Report

**Phase Goal:** Replace the X-Trading-Signals-Auth header on iPhone Safari with a phone-friendly login UX (cookie session) AND fold in TOTP 2FA (enroll + verify + trusted-device + magic-link reset) — without breaking the Phase 13 header-auth path used by HTMX/curl/scripts.

**Verified:** 2026-04-29T18:55:00Z
**Status:** human_needed (all programmable checks passed; 7 operator UAT scenarios pending real-device runs)
**Re-verification:** No — initial verification

---

## Goal Achievement — Requirements Coverage (AUTH-04..AUTH-12)

| REQ-ID  | Description                                                                                                  | Implementation                                                                                                                          | Test                                                                                                                  | Status        |
| ------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------- |
| AUTH-04 | iPhone Safari/Chrome browser-native auth UX (no extension)                                                   | `web/routes/login.py` GET/POST `/login`; cookie attrs `Path=/; Secure; HttpOnly; SameSite=Strict; Max-Age=43200` (login.py:447,461,579) | `tests/test_web_routes_login.py` (49 tests)                                                                            | PASS (code) — operator UAT-16.1-A pending |
| AUTH-05 | Header path additive — `X-Trading-Signals-Auth` unchanged                                                    | `web/middleware/auth.py::_try_header` (line 290–297) calls `hmac.compare_digest`; HTMX `hx-headers` pattern preserved (D-13)             | `test_web_auth_middleware.py::TestAuthPasses::test_correct_header_passes_through`; `test_dashboard.py::test_hx_headers_count_unchanged_from_phase_14_baseline` | PASS         |
| AUTH-06 | Constant-time secret comparison; `WEB_AUTH_SECRET` env-only                                                  | `_try_header` uses `hmac.compare_digest` exclusively. `grep -rnE '==.*WEB_AUTH_SECRET\|==.*_secret_bytes\|==.*_username_bytes' web/` ⇒ 0 matches outside `compare_digest` | AST guard in `tests/test_web_auth_middleware.py`                                                                         | PASS         |
| AUTH-07 | `/healthz` exempt; curl 401 plain-text contract preserved                                                    | `EXEMPT_PATHS = frozenset({'/healthz'})` (auth.py:56); 401 branch returns `unauthorized` plain-text, no `WWW-Authenticate`              | `TestCurlContract`, `TestRedirect302Shape`, `TestNoBasicAuthDecode` in `test_web_auth_middleware.py`                   | PASS         |
| AUTH-08 | TOTP enrollment via first-login web flow                                                                     | `web/routes/totp.py` GET/POST `/enroll-totp`; `pyotp.TOTP(secret).verify(code, valid_window=1)`; QR via `qrcode.make` + base64 PNG       | `tests/test_web_routes_totp.py::TestEnrollTotpPostValidCode` + ~30 enroll tests                                       | PASS         |
| AUTH-09 | TOTP verify on subsequent logins; trusted-device skip                                                        | `POST /login` enrolled branch → 302 `/verify-totp` + `tsi_pending` (login.py:452,461); middleware `_try_cookie` checks both tsi_session and tsi_trusted (auth.py:228–276) | `TestLoginPostValidVerifyBranch`, `TestTrustedDeviceCookie::test_valid_tsi_trusted_grants_access_when_uuid_active`     | PASS         |
| AUTH-10 | Trusted-device cookie + per-device revocation; `/devices` route                                              | `tsi_trusted=<uuid>; Max-Age=2592000; ...`; `web/routes/devices.py` GET/POST/POST-all; revoked uuids retained in `auth.json`            | `tests/test_web_routes_devices.py` (14 tests); `TestTrustedDeviceCookie::test_revoked_tsi_trusted_does_NOT_grant`     | PASS         |
| AUTH-11 | Magic-link email reset to `OPERATOR_RECOVERY_EMAIL`                                                          | `web/routes/login.py POST /forgot-2fa`; `web/routes/reset.py GET /reset-totp`; `URLSafeTimedSerializer(salt='magic-link')`; sha256 stored in auth.json | `tests/test_web_routes_login.py::TestForgot2fa*`, `test_web_routes_reset.py` (13 tests), `test_notifier_magic_link.py` (10 tests) | PASS         |
| AUTH-12 | Basic Auth path REMOVED — `Authorization: Basic` not decoded                                                 | `_try_basic` does NOT exist; middleware has no Basic decode (auth.py:11..23 documents removal). `grep -i 'authorization.*basic\|_try_basic' web/` ⇒ 0 substantive | `TestNoBasicAuthDecode`                                                                                               | PASS         |

**9/9 requirements verified at code + test level.** Real-device verification (AUTH-04, AUTH-08, AUTH-09, AUTH-10, AUTH-11) blocked on operator UAT (UAT-16.1-A..G).

---

## D-Decision Verification (Areas A–F per CONTEXT.md)

| Decision        | Description                                                                                            | Code Evidence                                                                                          | Status |
| --------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ | ------ |
| D-01 / E-01     | Basic Auth path REMOVED                                                                                | `web/middleware/auth.py` — no `_try_basic`, no `Authorization: Basic` decode                            | PASS   |
| D-02            | `EXEMPT_PATHS = frozenset({'/healthz'})` unchanged                                                     | auth.py:56                                                                                              | PASS   |
| D-03 / AUTH-06  | `hmac.compare_digest` for secret comparison; no `==`                                                   | auth.py:297; grep count = 0                                                                              | PASS   |
| D-04 / E-02     | Browser-conditional behavior — 302 → /login on browser nav; 401 plain-text otherwise                   | auth.py:216–226; `_is_browser_navigation` (auth.py:299–323)                                              | PASS   |
| D-05            | Phase 13 D-04 letter relaxed only for browser navigation (curl/HTMX XHR still 401 plain-text)          | `TestCurlContract`, `TestSecFetchSniff` in `test_web_auth_middleware.py`                                 | PASS   |
| D-06 / E-01     | Basic Auth `Authorization: Basic` rejected (not decoded)                                                | auth.py: only `_try_cookie` + `_try_header`; `TestNoBasicAuthDecode`                                    | PASS   |
| D-07 / E-02     | 3-step sniff: cookie → header → unauth-branch                                                          | auth.py:206–226                                                                                         | PASS   |
| D-08            | `WEB_AUTH_USERNAME` + `WEB_AUTH_SECRET` boot-validated; username non-empty + no `:`                    | `web/app.py::_read_auth_credentials` (lines 67–118)                                                     | PASS   |
| D-10 / F-04     | Cookie crypto = `itsdangerous.URLSafeTimedSerializer`; `pyotp` for TOTP; `qrcode[pil]` for QR          | `requirements.txt` pins itsdangerous==2.2.0, pyotp==2.9.0, qrcode==8.2, pillow==11.3.0                  | PASS   |
| D-11            | Session TTL 12h (`Max-Age=43200`); `_SESSION_MAX_AGE_SECONDS = 43200`                                  | auth.py:145; login.py:_COOKIE_ATTRS_CREATE                                                              | PASS   |
| D-12            | Cookie-deletion attrs match cookie-creation attrs                                                       | login.py:52 `_DELETION_ATTRS`; totp.py:49 `_COOKIE_ATTRS_DELETE`                                        | PASS   |
| D-13            | HTMX trade forms `hx-headers` UNCHANGED — count locked at 2                                            | `test_dashboard.py::test_hx_headers_count_unchanged_from_phase_14_baseline`                              | PASS   |
| D-14            | Login form structure + open-redirect prevention on `?next=`                                            | login.py `_is_safe_next` + 14 parametrized open-redirect tests                                          | PASS   |
| E-03            | TOTP enrollment via first-login web flow                                                                | `web/routes/totp.py` GET/POST /enroll-totp; `tsi_enroll` 10-min cookie                                  | PASS   |
| E-04            | TOTP verification on every cookie session start unless trusted                                         | login.py:452 sets tsi_pending; auth.py:_try_cookie checks tsi_trusted first                              | PASS   |
| E-05            | `tsi_trusted=<uuid>; Max-Age=2592000; Path=/; Secure; HttpOnly; SameSite=Strict`                        | totp.py:_COOKIE_ATTRS_CREATE_TRUSTED                                                                    | PASS   |
| E-06            | Per-device revocation via `/devices`; revoked uuids retained for audit                                  | `web/routes/devices.py`; auth_store helpers `add_trusted_device`, `revoke_device`, `is_uuid_active`     | PASS   |
| E-07            | Magic-link reset to `OPERATOR_RECOVERY_EMAIL` — generic `Check your email` page; one-time bypass        | `login.py POST /forgot-2fa`; `reset.py GET /reset-totp`; `_render_invalid_link_page` shared             | PASS   |
| E-08            | Trusted active session = full trust for sensitive actions                                              | `web/routes/devices.py::_require_cookie_session_or_403`                                                  | PASS   |
| F-01            | New `auth.json` schema-v1 with atomic-write helper; gitignored                                          | `auth_store.py` (schema_version=1, totp_*, trusted_devices, pending_magic_links); `.gitignore: auth.json` | PASS   |
| F-02            | Magic-link TTL 1h via `URLSafeTimedSerializer(...).loads(token, max_age=3600)`                          | reset.py:151,173                                                                                         | PASS   |
| F-03            | Email transport = Resend, reusing `notifier.py` adapter; `send_magic_link_email`                       | `notifier.py:1606+`; subject literal `Trading Signals — 2FA reset link (valid 1 hour)`                  | PASS   |
| F-05            | TOTP issuer label = `Trading Signals`; account label = `<username>@<host>`                              | `totp.py` enrollment URI                                                                                 | PASS   |
| F-06            | `OPERATOR_RECOVERY_EMAIL` boot-validated default `mwiriadi@gmail.com`; regex `^[^@]+@[^@]+\.[^@]+$`     | `web/app.py:58 _EMAIL_RE`; `web/app.py:111 raise RuntimeError`; `tests/test_web_app_factory.py::TestRecoveryEmailValidation` (5 tests) | PASS   |
| F-07            | Login form `Lost 2FA?` link visible                                                                    | `login.py:256 <a href="/forgot-2fa" class="link-secondary">Lost 2FA? Reset via email</a>`               | PASS   |
| F-08            | Rate limits: 5/15min /login + /verify-totp; 3/hr /forgot-2fa; 10/hr /reset-totp; 3/24h per-account     | `auth.py:71–99 _RATE_LIMIT_RULES`; `auth_store.count_recent_magic_links` for per-account                | PASS   |

---

## Hex-Boundary Verification

`auth_store.py` imports (line 32–40): `hashlib, json, logging, os, tempfile, uuid, datetime, pathlib, typing`. **All stdlib — no `web/`, no `requests`, no `signal_engine`, no `sizing_engine`, no `notifier`, no `dashboard`, no `main`.** PASS.

`notifier` was promoted out of `FORBIDDEN_FOR_WEB` (Plan 03 deviation, mirroring Phase 13 D-07 dashboard precedent + Phase 14 D-02 sizing_engine/system_params precedent). Regression test `tests/test_web_healthz.py::test_notifier_is_not_forbidden_for_web_phase_16_1_plan_03` locks the promotion. PASS — architecturally consistent.

Forbidden-imports test: `pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent` ⇒ **3 passed**. PASS.

---

## Test-Run Summary

### Phase-touched suite (must-have)

```
pytest tests/test_web_routes_login.py tests/test_web_routes_totp.py \
       tests/test_web_routes_devices.py tests/test_web_routes_reset.py \
       tests/test_notifier_magic_link.py tests/test_auth_store.py \
       tests/test_web_auth_middleware.py -x -q
⇒ 159 passed in 2.27s
```

### Forbidden-imports guard

```
pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent -x -q
⇒ 3 passed in 0.39s
```

### Full project suite (sans deferred items)

```
pytest -q --ignore=tests/test_nginx_signals_conf.py --ignore=tests/test_setup_https_doc.py
⇒ 1234 passed, 2 failed (test_ruff_clean_notifier × 2 — `ruff` binary not on PATH; pre-existing per deferred-items.md)
```

### Deferred (pre-existing, NOT caused by Phase 16.1)

Tracked in `.planning/phases/16.1-phone-friendly-auth-ux-for-dashboard-access/deferred-items.md`:

| Test                                                                                       | Reason                                                       |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------ |
| `tests/test_nginx_signals_conf.py::TestNginxConfStructure::test_listen_443_ssl`            | Phase 12 nginx config drift (`listen 443 ssl http2;` shape)  |
| `tests/test_nginx_signals_conf.py::TestNginxConfStructure::test_listen_ipv6_443_ssl`       | same                                                          |
| `tests/test_nginx_signals_conf.py::TestNginxConfStructure::test_http2_on`                  | same                                                          |
| `tests/test_nginx_signals_conf.py::TestNginxConfStructure::test_server_name_present`        | same                                                          |
| `tests/test_nginx_signals_conf.py::TestNginxConfPlaceholder::test_owned_domain_placeholder_literal_present` | same — domain is hardcoded            |
| `tests/test_nginx_signals_conf.py::TestNginxConfForbiddenPatterns::test_no_listen_80_directive` | same                                                       |
| `tests/test_nginx_signals_conf.py::TestNginxConfForbiddenPatterns::test_no_ssl_certificate_line` | same                                                      |
| `tests/test_nginx_signals_conf.py::TestNginxConfForbiddenPatterns::test_no_ssl_certificate_key_line` | same                                                  |
| `tests/test_nginx_signals_conf.py::TestNginxConfForbiddenPatterns::test_no_handwritten_http_to_https_redirect` | same                                          |
| `tests/test_setup_https_doc.py::TestCrossArtifactDriftGuard::test_owned_domain_placeholder_matches_nginx_conf` | same                                          |
| `tests/test_notifier.py::test_ruff_clean_notifier`                                          | `ruff` binary missing from local test env PATH                |
| `tests/test_notifier.py::test_ruff_clean_notifier_detects_f401_regression`                  | same                                                          |

**Total deferred: 12 tests.** All confirmed pre-existing on baseline `2e5d9aa` per Plan 01 SUMMARY's git-stash baseline run. No new regressions.

---

## Sabotage Probe (verifier-credibility)

Mutated `web/middleware/auth.py::_try_header` to `return False` (force header path to fail).

Result: `pytest tests/test_web_auth_middleware.py -x -q` → `FAILED tests/test_web_auth_middleware.py::TestAuthPasses::test_correct_header_passes_through` (assert 401 in (200, 503) failed — got 401 as expected when header is broken).

Reverted to original code; `grep -c SABOTAGE web/middleware/auth.py` ⇒ 0. AUTH-05 regression coverage confirmed working — the test would catch any regression in the header path. PASS.

---

## Anti-Pattern Scan

- `grep -ic 'WWW-Authenticate' web/middleware/auth.py` ⇒ 0 (D-04 / AUTH-12 preserved). PASS.
- `grep -rnE '_try_basic|Authorization.*Basic' web/` ⇒ only docstring mention in middleware (line 14) explaining the removal. PASS.
- `grep -rnE '==.*WEB_AUTH_SECRET|WEB_AUTH_SECRET.*==' web/ | grep -v compare_digest` ⇒ 0. PASS.
- Cookie-deletion attribute parity: every `Set-Cookie` deletion (Max-Age=0) literal in source uses the same `Path/Secure/HttpOnly/SameSite=Strict` shape as the matching creation literal. Verified via `_DELETION_ATTRS` + `_COOKIE_ATTRS_DELETE` constants in `login.py:52` + `totp.py:49`. PASS.
- TODO/FIXME scan: no new TODO/FIXME in phase-touched files (manual sample of `auth_store.py`, `web/middleware/auth.py`, `web/routes/login.py`, `web/routes/totp.py`, `web/routes/devices.py`, `web/routes/reset.py`).

---

## Operator-Required Follow-ups (NOT Blocking — but Required Before Phase 16.1 Closure)

Seven UAT scenarios in `16.1-HUMAN-UAT.md` require real-iPhone runs:

| ID         | Scenario                                                                  | Status    |
| ---------- | ------------------------------------------------------------------------- | --------- |
| UAT-16.1-A | iPhone Safari first-visit + cookie login + TOTP enrollment                | pending   |
| UAT-16.1-B | iPhone Safari subsequent-visit (no trust-device) + /verify-totp           | pending   |
| UAT-16.1-C | iPhone Safari subsequent-visit (with trust-device) → no TOTP prompt        | pending   |
| UAT-16.1-D | Lost-phone scenario → magic-link email → reset flow                       | pending   |
| UAT-16.1-E | /devices revoke → revoked device's cookie no longer grants                | pending   |
| UAT-16.1-F | curl with `X-Trading-Signals-Auth` returns 200 (AUTH-05 regression)       | pending   |
| UAT-16.1-G | curl without header gets 401 unauthorized (AUTH-07 contract)               | pending   |

These are operator-blocked, NOT verifier-blocked. The codebase is ready for deploy + UAT.

---

## Final Verdict

## VERIFICATION PASSED — operator UAT pending

- All 9 requirements (AUTH-04..AUTH-12) verified at code + test level.
- All 26 D-decisions (D-01..D-15 + E-01..E-08 + F-01..F-08) verified.
- Hex boundary clean (auth_store.py = stdlib only; notifier promotion architecturally consistent).
- 159 phase-touched tests pass; 1234 total tests pass; 12 deferred failures all confirmed pre-existing.
- Sabotage probe confirms AUTH-05 regression coverage is real, not theatrical.

Phase 16.1 is **code-complete and test-green.** Operator must run `16.1-HUMAN-UAT.md` scenarios A–G on a real iPhone before closing the phase.

---

*Verified: 2026-04-29*
*Verifier: Claude (gsd-verifier)*
