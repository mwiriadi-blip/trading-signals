---
phase: 32-dashboard-legacy-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard_renderer/formatters.py
  - dashboard_renderer/stats.py
  - dashboard_renderer/shell.py
  - dashboard_renderer/components/header.py
autonomous: true
requirements: [OPS-06]

must_haves:
  truths:
    - "Every unique non-delegating function in dashboard_legacy/render_helpers.py exists in dashboard_renderer/formatters.py or dashboard_renderer/stats.py with byte-identical body"
    - "dashboard_legacy/section_renderers.py unique functions (_render_equity_chart_container, _distinct_equity_tuples, _render_signout_button, _render_session_note) exist in dashboard_renderer/components/header.py with byte-identical body"
    - "dashboard_legacy/page_body.py composition functions (_render_html_shell body, _render_page_body, _render_tabbed_dashboard, _render_single_page_dashboard) exist in dashboard_renderer/shell.py with byte-identical body"
    - "dashboard_renderer.shell.render_html_shell body is replaced with content of dashboard_legacy.page_body._render_html_shell (not appended)"
    - "Golden snapshot tests pass (byte-identical HTML output) after this plan"
    - "Every receiving file remains under 500 LOC after absorption"
  artifacts:
    - path: dashboard_renderer/formatters.py
      provides: "render_helpers unique format helpers, market registry, resolve helpers, _TraceOpenPlaceholderMap, module constants"
      contains: "_market_registry"
    - path: dashboard_renderer/stats.py
      provides: "Existing stats functions remain plus any unique unrealised-PnL helper not already present"
      contains: "compute_aggregate_stats"
    - path: dashboard_renderer/shell.py
      provides: "Active HTML shell + page body composition functions"
      contains: "_render_page_body"
    - path: dashboard_renderer/components/header.py
      provides: "Header, equity chart container, signout button, session note"
      contains: "_render_equity_chart_container"
  key_links:
    - from: dashboard_renderer/shell.py
      to: dashboard_renderer/components (header, nav, footer, etc.)
      via: "composition calls inside _render_page_body and _render_html_shell"
      pattern: "from dashboard_renderer.components"
---

<objective>
Port unique non-delegating logic from three `dashboard_legacy/` modules into their semantic homes in `dashboard_renderer/`:
- `render_helpers.py` → `formatters.py` (format helpers + market registry + resolve helpers + `_TraceOpenPlaceholderMap` + module constants) + `stats.py` (only the unrealised-PnL helper if not already there).
- `section_renderers.py` unique fns (`_render_equity_chart_container`, `_distinct_equity_tuples`, `_render_signout_button`, `_render_session_note`) → `components/header.py`.
- `page_body.py` composition functions and active `_render_html_shell` body → `shell.py` (replacing the dead `render_html_shell` body, not appending).

Purpose: Eliminate the source of legacy logic so subsequent plans can rewire callers without circular `import dashboard as d` indirection. No behaviour change — byte-identical golden output.

Output: Three new/grown `dashboard_renderer/*` files containing all unique legacy logic; `dashboard_legacy/` submodule files are NOT touched in this plan (they keep functioning by delegation as before). Receiving files stay under 500 LOC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dashboard-legacy-retirement/32-CONTEXT.md
@.planning/phases/32-dashboard-legacy-retirement/32-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Critical: the active HTML shell is dashboard_legacy.page_body._render_html_shell;
     dashboard_renderer.shell.render_html_shell is dead code (its body must be REPLACED, not appended).
     Golden snapshot golden.html contains _DETAILS_ARIA_SYNC_JS only — NOT _AWST_COUNTDOWN_JS,
     _STATUS_STRIP_REFRESH_JS, _TABS_KEYBOARD_JS. Those constants stay in shell.py but the
     active render function must emit ONLY _DETAILS_ARIA_SYNC_JS to preserve byte-identity. -->

<!-- _render_page_body annotation lies: declared `-> str` but actually returns a 5-tuple
     (section_id, heading_id, heading_text, heading_cls, render_fn). Fix annotation to
     `tuple[str, str, str, str, Callable[[], str]]` during absorption. -->

<!-- C-2 pattern: any sizing_engine import inside ported helpers MUST stay as local
     (function-body) import. The `# LOCAL — C-2` comment must be preserved. -->

<!-- Logger discipline: render_helpers.py uses logging.getLogger('dashboard') (not __name__).
     After absorption into formatters.py / stats.py / shell.py, switch to
     logging.getLogger(__name__) — the journalctl 'dashboard' name continuity is preserved
     by the dashboard.py shim, not by transitive copies. -->

<!-- CANONICAL NAMING (locked by this plan):
     All ported helpers PRESERVE their underscore prefix. The names published by
     dashboard_renderer.formatters after this plan are:
       _fmt_currency, _fmt_em_dash, _fmt_percent_signed, _fmt_percent_unsigned,
       _fmt_pnl_with_colour, _fmt_last_updated, _format_indicator_value,
       _market_registry, _enabled_market_registry, _display_names,
       _strategy_settings_for, _resolve_strategy_version, _resolve_trace_open_keys,
       _TraceOpenPlaceholderMap, _SIGNAL_LABEL, _SIGNAL_COLOUR, _EXIT_REASON_DISPLAY,
       _TRACE_FORMULAS, _SEED_LENGTHS, _TRACE_OPEN_PLACEHOLDER,
       _VALID_TRACE_INSTRUMENT_KEYS, _INSTRUMENT_DISPLAY_NAMES, _CONTRACT_SPECS.
     Downstream plans (32-02, 32-03, 32-04) consume these exact underscore-prefixed names.
     RESEARCH.md Caller Audit / Test Gate rows that say `fmt_currency`, `fmt_em_dash`,
     `format_indicator_value` (no underscore) are OUTDATED — the canonical names carry
     the underscore prefix and are NOT re-published un-prefixed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + port render_helpers.py unique content into formatters.py and stats.py</name>
  <read_first>
    - dashboard_legacy/render_helpers.py (full file — source of all functions to port)
    - dashboard_renderer/formatters.py (current 163 LOC — receiving file; audit what already exists)
    - dashboard_renderer/stats.py (current 170 LOC — receiving file; audit what already exists)
    - dashboard_renderer/assets.py (imports `_INLINE_CSS`, `_TRACE_TOGGLE_JS` re-exports — confirm asset surface)
    - system_params.py (imports `_COLOR_FLAT`, `_COLOR_LONG`, `_COLOR_SHORT`, `DEFAULT_MARKETS`, `DEFAULT_STRATEGY_SETTINGS`, `FALLBACK_CONTRACT_SPECS`, `TRAIL_MULT_LONG`, `TRAIL_MULT_SHORT` — confirm names exist)
  </read_first>
  <action>
    CANONICAL NAMING LOCK: All ported helpers PRESERVE the underscore prefix from legacy. No symbol is republished un-prefixed. The names published from dashboard_renderer.formatters by this plan are exactly: `_fmt_currency`, `_fmt_em_dash`, `_fmt_percent_signed`, `_fmt_percent_unsigned`, `_fmt_pnl_with_colour`, `_fmt_last_updated`, `_format_indicator_value`, `_market_registry`, `_enabled_market_registry`, `_display_names`, `_strategy_settings_for`, `_resolve_strategy_version`, `_resolve_trace_open_keys`, `_TraceOpenPlaceholderMap`. Downstream plans (32-02 Task 5, 32-03 Task 1, 32-04 Task 1 and Task 2) bind against these underscore-prefixed names. Where RESEARCH.md Caller Audit / Test Gate Inventory rows show un-prefixed names (e.g., `fmt_currency`), treat that as a documentation lag — the canonical names carry underscores.

    Audit-first (per D-03): for each symbol exported by dashboard_legacy/render_helpers.py, check whether a function or constant of the SAME purpose already exists in dashboard_renderer/formatters.py or dashboard_renderer/stats.py. Tag each as DUPLICATE (already covered) or UNIQUE (must port).

    Port all UNIQUE symbols into formatters.py:
    - Constants: `_INSTRUMENT_DISPLAY_NAMES`, `_CONTRACT_SPECS`, `_DEFAULT_STRATEGY_VERSION`, `_EXIT_REASON_DISPLAY`, `_SEED_LENGTHS`, `_SIGNAL_COLOUR`, `_SIGNAL_LABEL`, `_TRACE_FORMULAS`, `_TRACE_OPEN_PLACEHOLDER`, `_VALID_TRACE_INSTRUMENT_KEYS`.
    - Functions: `_market_registry`, `_enabled_market_registry`, `_display_names`, `_strategy_settings_for`, `_resolve_strategy_version`, `_resolve_trace_open_keys`, `_fmt_em_dash`, `_fmt_currency`, `_fmt_percent_signed`, `_fmt_percent_unsigned`, `_fmt_pnl_with_colour`, `_fmt_last_updated`, `_format_indicator_value`.
    - Class: `_TraceOpenPlaceholderMap`.

    For DUPLICATE thin-wrapper symbols (e.g., `_compute_sharpe` → `dr_stats.compute_sharpe`, `_compute_max_drawdown`, `_compute_win_rate`, `_compute_total_return`, `_compute_aggregate_stats`, `_compute_trail_stop_display`, `_compute_unrealised_pnl_display`): leave them in dashboard_legacy/render_helpers.py for now (they will be removed in plan 32-04 when legacy is retired). Do NOT re-add wrappers in formatters.py.

    Port UNIQUE bodies VERBATIM (modulo `import` paths and `logger = logging.getLogger('dashboard')` → `logging.getLogger(__name__)`). Preserve 2-space indent. Do NOT run `ruff format`. PRESERVE the leading underscore on every ported symbol — do NOT rename to public.

    For stats.py: only port the unrealised-PnL helper if it has UNIQUE logic not already in `compute_unrealised_pnl_display`. If the legacy `_compute_unrealised_pnl_display` is a one-line wrapper to `dr_stats.compute_unrealised_pnl_display`, do NOT port anything (it's pure delegation; stats.py is unchanged).

    Maintain `html.escape(value, quote=True)` at every dynamic leaf site during port (XSS gate).

    LOC budget per RESEARCH.md projection: formatters ~374, stats unchanged (~170). Confirm with `wc -l dashboard_renderer/formatters.py dashboard_renderer/stats.py` after port — both must be ≤500.

    The legacy module file dashboard_legacy/render_helpers.py is NOT modified in this task — it continues to provide the same symbols to its existing callers. Removal happens in plan 32-04.

    MANIFEST REQUIREMENT (required output — per cross-AI review H-01): After completing the audit step, produce a MANIFEST table in the SUMMARY that documents every symbol from `dashboard_legacy/render_helpers.py` tagged as DUPLICATE, PORTED, or DELETED. The MANIFEST must cover all three categories with exact symbol names. This locks the symbol inventory before downstream waves run, preventing silent dependency on missing helpers. The MANIFEST is a required section in `32-01-SUMMARY.md` — see `<output>` block.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py::TestEmptyState tests/test_dashboard.py::TestGoldenSnapshot tests/test_dashboard_split_seam.py::test_dashboard_html_output_byte_identical tests/test_signal_engine.py::TestDeterminism::test_dashboard_no_forbidden_imports tests/test_html_xss_audit.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^def _market_registry\|^def _enabled_market_registry\|^def _display_names\|^def _strategy_settings_for\|^def _resolve_strategy_version\|^def _resolve_trace_open_keys\|^class _TraceOpenPlaceholderMap\|^_INSTRUMENT_DISPLAY_NAMES\|^_CONTRACT_SPECS\|^_SIGNAL_LABEL\|^_SIGNAL_COLOUR\|^_EXIT_REASON_DISPLAY\|^_TRACE_FORMULAS\|^_SEED_LENGTHS" dashboard_renderer/formatters.py` returns at least 13 hits.
    - `grep -nE "^def _fmt_currency|^def _fmt_em_dash|^def _format_indicator_value" dashboard_renderer/formatters.py` returns exactly 3 hits (underscore-prefixed names locked).
    - `wc -l dashboard_renderer/formatters.py` reports ≤500.
    - `wc -l dashboard_renderer/stats.py` reports ≤500.
    - `python -c "from dashboard_renderer.formatters import _fmt_currency, _fmt_em_dash, _format_indicator_value, _market_registry, _display_names, _strategy_settings_for, _resolve_strategy_version, _resolve_trace_open_keys, _TraceOpenPlaceholderMap, _SIGNAL_LABEL, _SIGNAL_COLOUR, _EXIT_REASON_DISPLAY, _TRACE_FORMULAS, _SEED_LENGTHS; print('OK')"` prints `OK`.
    - Test command in `<verify>` exits rc=0.
    - `grep -n "logging.getLogger('dashboard')" dashboard_renderer/formatters.py dashboard_renderer/stats.py` returns no hits (logger discipline preserved).
  </acceptance_criteria>
  <done>
    All unique non-delegating render_helpers symbols exist in dashboard_renderer/formatters.py (and stats.py if applicable) with underscore-prefixed names preserved. dashboard_legacy/render_helpers.py is unchanged. Golden HTML byte-identical. XSS gate green. Hex boundary gate green. MANIFEST table produced in SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port section_renderers.py unique content into components/header.py</name>
  <read_first>
    - dashboard_legacy/section_renderers.py (full file — source of 11 functions, 4 unique)
    - dashboard_renderer/components/header.py (current 148 LOC — receiving file; audit existing functions)
    - dashboard_legacy/page_body.py (caller of `_render_equity_chart_container` via `_render_signal_cards` composition — confirm call signature)
  </read_first>
  <action>
    Audit (per D-03): identify the unique functions in dashboard_legacy/section_renderers.py that have no counterpart in dashboard_renderer/components/header.py — per RESEARCH.md these are `_render_equity_chart_container` (~90 LOC), `_distinct_equity_tuples` (~20 LOC), `_render_signout_button` (~13 LOC), `_render_session_note` (~12 LOC). The other 7 are single-line delegations and are NOT ported here.

    Port the 4 unique functions VERBATIM (modulo internal import paths) into dashboard_renderer/components/header.py. Preserve 2-space indent. Preserve `html.escape(value, quote=True)` at every dynamic leaf. Preserve `_private` underscore prefix.

    If `_render_equity_chart_container` references constants from the dashboard_renderer assets or formatters surface (e.g., chart asset SRI), import those from their canonical home (`dashboard_renderer.assets`, `dashboard_renderer.formatters`), not from `dashboard_legacy.*`.

    LOC budget per RESEARCH.md: header.py projected ~283 — confirm with `wc -l` after port; must be ≤500.

    dashboard_legacy/section_renderers.py is NOT modified in this task — its delegating callers continue to work. Removal happens in plan 32-04.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py::TestEmptyState tests/test_dashboard.py::TestGoldenSnapshot tests/test_dashboard_split_seam.py::test_dashboard_html_output_byte_identical tests/test_html_xss_audit.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _render_equity_chart_container|^def _distinct_equity_tuples|^def _render_signout_button|^def _render_session_note" dashboard_renderer/components/header.py` returns exactly 4 hits.
    - `wc -l dashboard_renderer/components/header.py` reports ≤500.
    - `python -c "from dashboard_renderer.components.header import _render_equity_chart_container, _distinct_equity_tuples, _render_signout_button, _render_session_note; print('OK')"` prints `OK`.
    - Test command in `<verify>` exits rc=0.
  </acceptance_criteria>
  <done>
    Four unique section_renderers functions live in dashboard_renderer/components/header.py with verbatim bodies. dashboard_legacy/section_renderers.py untouched. Golden HTML byte-identical. XSS gate green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Port page_body.py composition functions into shell.py; replace dead render_html_shell body</name>
  <read_first>
    - dashboard_legacy/page_body.py (full file — source of `_render_html_shell`, `_render_page_body`, `_render_tabbed_dashboard`, `_render_single_page_dashboard`, `_atomic_write_html`)
    - dashboard_renderer/shell.py (current 204 LOC — `render_html_shell` is dead code at ~line 167; its body MUST be replaced, not appended)
    - dashboard_renderer/io.py (current 33 LOC — already has `atomic_write_html`; the page_body `_atomic_write_html` wrapper is pure delegation and is NOT ported anywhere)
    - tests/fixtures/dashboard/golden.html (the byte-identity reference — confirms only `_DETAILS_ARIA_SYNC_JS` is emitted by the active shell)
    - dashboard_renderer/api.py (line 52, 75, 117 call `d._render_html_shell`, `d._render_tabbed_dashboard`, `d._render_single_page_dashboard` — confirm signatures; this plan does NOT modify api.py, but the symbols it ports must match the signatures api.py expects)
  </read_first>
  <action>
    Step 1 — Replace shell.py's dead `render_html_shell` body with the active content from `dashboard_legacy/page_body._render_html_shell`. The replacement must emit ONLY `_DETAILS_ARIA_SYNC_JS` (NOT `_AWST_COUNTDOWN_JS`, NOT `_STATUS_STRIP_REFRESH_JS`, NOT `_TABS_KEYBOARD_JS`) — those three constants stay defined in shell.py but are not included in the function body. This preserves byte-identity with golden.html which contains only `_DETAILS_ARIA_SYNC_JS`. Per RESEARCH §"Critical Finding: Dead `shell.render_html_shell`".

    NAMING NOTE: The legacy private function is `dashboard_legacy.page_body._render_html_shell` (underscore-prefixed). The renderer-side replacement target is `dashboard_renderer.shell.render_html_shell` (PUBLIC name — no underscore). This is an intentional asymmetry: the renderer publishes a public shell entrypoint while the legacy module kept it private. After this plan, `git grep _render_html_shell` should return zero live call sites coming from dashboard or dashboard_legacy imports — only the docstring or comments may reference the old name.

    Step 2 — Port `_render_page_body`, `_render_tabbed_dashboard`, `_render_single_page_dashboard` VERBATIM into shell.py with these adjustments:
    - Fix `_render_page_body` return annotation: change `-> str` to `-> tuple[str, str, str, str, Callable[[], str]]` (the function actually returns a 5-tuple; per RESEARCH §"Pitfall 2"). Add `from typing import Callable` at the top of shell.py if not already present.
    - Adjust internal calls so `_render_page_body` and `_render_tabbed_dashboard` and `_render_single_page_dashboard` reference the now-local helpers inside shell.py (no `from dashboard_legacy.page_body import …` in shell.py).
    - Other internal references (e.g., `_render_signal_cards`, `_render_equity_chart_container`, `_render_footer`, etc.) import from their canonical homes in `dashboard_renderer/components/*` and `dashboard_renderer/components/header` (the latter is populated by Task 2 of this plan).

    LAZY-IMPORT RULE (H-02 — required): Any `dashboard_renderer.components` module that does not exist until Wave 2 — specifically `account`, `calc_rows`, and `trace` — MUST be imported as LOCAL imports inside the callable/lambda body where they are called, NOT at module top of shell.py. Use the same C-2 lazy-import pattern used for `sizing_engine` (function-body import with `# LOCAL — Wave-2 dep` comment). This ensures `import dashboard_renderer.shell` succeeds immediately after Task 3 completes (before Wave 2 runs). Module-top imports of Wave-2 components will cause an ImportError if shell.py is loaded before Wave 2 executes.

    Step 3 — DO NOT port `_atomic_write_html` to shell.py or io.py. The legacy wrapper is pure delegation to `dashboard_renderer.io.atomic_write_html`; downstream plans (32-03) will retarget callers to call `io.atomic_write_html` directly. dashboard_legacy/page_body.py is NOT modified in this plan.

    Step 4 — Confirm `dashboard_renderer.shell` exposes a `render_html_shell` callable AND a `_render_page_body`, `_render_tabbed_dashboard`, `_render_single_page_dashboard`. Public-vs-underscore names: `render_html_shell` stays public (matches the dead-stub name it replaces); the three page-body fns retain their `_render_` prefix.

    LOC budget per RESEARCH.md: shell.py projected ~324 — confirm with `wc -l` after port; must be ≤500. If the projection overflows, split at a natural seam (e.g., extract `_render_page_body` family into `dashboard_renderer/page_body.py` as a new sibling under `dashboard_renderer/`). Document the seam name in the task summary.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py::TestEmptyState tests/test_dashboard.py::TestGoldenSnapshot tests/test_dashboard_split_seam.py::test_dashboard_html_output_byte_identical tests/test_html_xss_audit.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _render_page_body|^def _render_tabbed_dashboard|^def _render_single_page_dashboard|^def render_html_shell" dashboard_renderer/shell.py` returns exactly 4 hits (or 3 in shell.py + 1 in the new seam file if Task 3 split was needed).
    - `wc -l dashboard_renderer/shell.py` reports ≤500. (If split, every daughter file ≤500 as well.)
    - `grep -nE "_AWST_COUNTDOWN_JS|_STATUS_STRIP_REFRESH_JS|_TABS_KEYBOARD_JS" dashboard_renderer/shell.py` may match the constant definitions but NOT inside the body of `render_html_shell` (the constants are defined for nav/status-strip consumers, never emitted by `render_html_shell`).
    - `python -c "from dashboard_renderer.shell import render_html_shell, _render_page_body, _render_tabbed_dashboard, _render_single_page_dashboard; print('OK')"` prints `OK`.
    - Wave-1 import gate (H-02): `python -c "import dashboard_renderer.shell; print('OK')"` exits 0 immediately after Task 3 completes — BEFORE Wave 2 runs. This confirms Wave-2 component imports (account, calc_rows, trace) are local inside callables, not at module top.
    - Golden test `tests/test_dashboard.py::TestGoldenSnapshot::test_golden_snapshot_matches_committed` exits rc=0.
    - `_render_page_body` signature returns 5-tuple: `python -c "import dashboard_renderer.shell as s; import inspect; print(inspect.signature(s._render_page_body))"` returns a signature whose annotation contains `tuple` (or equivalent typing marker).
    - Naming asymmetry confirmed: `git grep "_render_html_shell" dashboard/ dashboard_renderer/ 2>/dev/null | grep -vE "^dashboard_legacy/" | grep -vE "(# |''')"` returns NO live caller hits in dashboard or dashboard_renderer (only legacy delegation references the old private name).
  </acceptance_criteria>
  <done>
    shell.py contains the active HTML shell body (matching page_body._render_html_shell content, NOT the dead version) + the three page-body composition functions. Wave-2 component imports are local inside callables (not module-top) so `import dashboard_renderer.shell` succeeds before Wave 2 runs. Golden snapshot byte-identical. dashboard_legacy/page_body.py untouched. shell.py ≤500 LOC.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard_legacy ↔ dashboard_renderer | Refactor moves logic across the boundary; no new untrusted-input surface introduced. |
| Rendered HTML → browser | Every dynamic value already flows through `html.escape(value, quote=True)`; port preserves this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-01 | Tampering | Ported render helpers | mitigate | `tests/test_html_xss_audit.py` AST gate enforces `html.escape(value, quote=True)` at every dynamic leaf; port preserves call sites verbatim. |
| T-32-01-02 | Information Disclosure | shell.py replacement | mitigate | `dashboard_renderer.shell.render_html_shell` body replaced verbatim with the active `page_body._render_html_shell` content; no new dynamic interpolation introduced. |
| T-32-01-03 | Denial of Service | Hex boundary regression | mitigate | `tests/test_signal_engine.py::TestDeterminism::test_dashboard_no_forbidden_imports` continues to gate `dashboard.py` (shim untouched in this plan). New `dashboard_renderer/*` files use only stdlib + pytz + dashboard_renderer + system_params imports per CLAUDE.md hex-lite. |
</threat_model>

<verification>
- All three tasks' `<automated>` test commands exit rc=0.
- `git diff --name-only dashboard_renderer/` lists exactly the four files in `files_modified`.
- `git diff --name-only dashboard_legacy/` is empty (legacy untouched in this plan).
- Full suite quick gate: `.venv/bin/pytest -x --tb=short tests/test_dashboard.py tests/test_dashboard_split_seam.py tests/test_html_xss_audit.py tests/test_signal_engine.py::TestDeterminism` is green.
</verification>

<success_criteria>
1. `dashboard_renderer/formatters.py` contains all unique non-delegating symbols from `dashboard_legacy/render_helpers.py` (functions, classes, constants) with underscore-prefixed names preserved.
2. `dashboard_renderer/components/header.py` contains `_render_equity_chart_container`, `_distinct_equity_tuples`, `_render_signout_button`, `_render_session_note`.
3. `dashboard_renderer/shell.py` `render_html_shell` body matches `dashboard_legacy.page_body._render_html_shell`; `_render_page_body`, `_render_tabbed_dashboard`, `_render_single_page_dashboard` are present; Wave-2 component imports (account, calc_rows, trace) are local inside callables — not module-top — so `import dashboard_renderer.shell` succeeds before Wave 2 runs.
4. Every modified file ≤500 LOC.
5. Golden HTML byte-identical. XSS, hex-boundary, and golden gates all rc=0.
</success_criteria>

<output>
After completion, create `.planning/phases/32-dashboard-legacy-retirement/32-01-SUMMARY.md` documenting:
- MANIFEST table: every symbol from `dashboard_legacy/render_helpers.py` tagged as DUPLICATE, PORTED, or DELETED. This is a REQUIRED section (per cross-AI review H-01 — locks the symbol inventory before downstream waves).
- Which symbols were tagged DUPLICATE vs UNIQUE for each legacy module.
- Exact post-absorption LOC counts.
- Any seam split that was required.
- Naming decisions: confirm underscore prefix preserved on all ported formatters/stats helpers; document the `_render_html_shell` (legacy private) → `render_html_shell` (renderer public) rename and confirm `git grep _render_html_shell` returns zero live caller hits from dashboard or dashboard_legacy imports.
- Confirmation that `python -c "import dashboard_renderer.shell; print('OK')"` exits 0 immediately after Task 3 (Wave-1 import gate — H-02).
</output>
