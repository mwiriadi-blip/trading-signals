---
phase: 32-dashboard-legacy-retirement
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - dashboard_renderer/components/trace.py
  - dashboard_renderer/components/calc_rows.py
  - dashboard_renderer/components/account.py
  - dashboard_renderer/components/positions.py
  - dashboard_renderer/components/paper_trades.py
autonomous: true
requirements: [OPS-06]

must_haves:
  truths:
    - "dashboard_renderer/components/trace.py exists and contains _render_trace_inputs, _render_trace_indicators, _render_trace_vote, _render_trace_panels with byte-identical bodies to dashboard_legacy/trace_panels.py"
    - "dashboard_renderer/components/calc_rows.py exists and contains _render_calc_row, _render_entry_target_row with byte-identical bodies"
    - "dashboard_renderer/components/account.py exists and contains _render_account_management_region, _render_account_balance_form, _render_account_stats, _render_key_stats, _compute_account_stat_values"
    - "dashboard_renderer/components/positions.py absorbs _render_open_form, _render_single_position_row, _render_drift_banner, _render_trailing_stop_guidance from dashboard_legacy/positions_section.py"
    - "dashboard_renderer/components/paper_trades.py absorbs all six unique paper-trade rendering functions from dashboard_legacy/paper_trades_section.py; existing public render_paper_trades_region (no underscore) is preserved as the canonical entrypoint"
    - "C-2 local imports of sizing_engine are preserved as function-body imports (not module-top)"
    - "Golden snapshot tests pass (byte-identical HTML output)"
    - "Every receiving file remains under 500 LOC after absorption"
  artifacts:
    - path: dashboard_renderer/components/trace.py
      provides: "Trace panels rendering — inputs, indicators, vote, top-level panels"
      contains: "_render_trace_panels"
    - path: dashboard_renderer/components/calc_rows.py
      provides: "Calculator row renderers — long-form calc row and entry-target row"
      contains: "_render_calc_row"
    - path: dashboard_renderer/components/account.py
      provides: "Account management region, balance form, account stats, key stats"
      contains: "_render_account_management_region"
    - path: dashboard_renderer/components/positions.py
      provides: "Existing renderer + open form + single-row renderer + drift banner + trailing-stop guidance"
      contains: "_render_open_form"
    - path: dashboard_renderer/components/paper_trades.py
      provides: "Paper-trades region + open + closed + open form + stats + close form + alert badge"
      contains: "render_paper_trades_region"
  key_links:
    - from: dashboard_renderer/components/positions.py
      to: dashboard_renderer/components/calc_rows.py
      via: "import for _render_entry_target_row consumed inside _render_single_position_row"
      pattern: "from dashboard_renderer.components.calc_rows import"
    - from: dashboard_renderer/components/paper_trades.py
      to: dashboard_renderer/stats.py
      via: "compute_aggregate_stats call inside _render_paper_trades_stats / render_paper_trades_region"
      pattern: "from dashboard_renderer.stats import compute_aggregate_stats"
---

<objective>
Create three new `dashboard_renderer/components/*` files (`trace.py`, `calc_rows.py`, `account.py`) and absorb the unique content of two legacy compositor modules (`positions_section.py`, `paper_trades_section.py`) into their existing `dashboard_renderer/components/*` homes.

Purpose: Move every remaining unique legacy render function into the canonical `dashboard_renderer/` tree, with all internal cross-component references resolved to direct imports from `dashboard_renderer.*` (NOT from `dashboard_legacy.*` and NOT via `import dashboard as d`).

Output: Three new component files + two grown component files, all ≤500 LOC, all imports resolved to canonical `dashboard_renderer.*` paths. dashboard_legacy/* files remain untouched (they keep functioning by delegation until plan 32-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dashboard-legacy-retirement/32-CONTEXT.md
@.planning/phases/32-dashboard-legacy-retirement/32-RESEARCH.md
@.planning/phases/32-dashboard-legacy-retirement/32-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- After plan 32-01 lands, dashboard_renderer.formatters re-exports the underscore-prefixed
     names: _market_registry, _enabled_market_registry, _display_names, _strategy_settings_for,
     _resolve_strategy_version, _resolve_trace_open_keys, _TraceOpenPlaceholderMap,
     _INSTRUMENT_DISPLAY_NAMES, _CONTRACT_SPECS, _SIGNAL_LABEL, _SIGNAL_COLOUR,
     _EXIT_REASON_DISPLAY, _TRACE_FORMULAS, _SEED_LENGTHS, _TRACE_OPEN_PLACEHOLDER,
     _VALID_TRACE_INSTRUMENT_KEYS, _fmt_em_dash, _fmt_currency, _fmt_percent_signed,
     _fmt_percent_unsigned, _fmt_pnl_with_colour, _fmt_last_updated, _format_indicator_value.
     dashboard_renderer.stats re-exports its existing surface (compute_aggregate_stats,
     compute_sharpe, compute_max_drawdown, compute_win_rate, compute_total_return,
     compute_trail_stop_display, compute_unrealised_pnl_display). -->

<!-- C-2 imports: calc_rows.py and positions_section.py legacy modules use
     `from sizing_engine import calc_position_size` and `from sizing_engine import get_trailing_stop`
     as LOCAL function-body imports. These MUST stay function-body (NOT module-top) in the new
     component files. Per RESEARCH §"Pitfall 4". -->

<!-- positions.py absorption: dashboard_legacy.positions_section.py has 6 functions, 2 are
     pure delegations (`_render_positions_table`, `_render_trades_table`) and stay in
     legacy (will be deleted in 32-04). Only the 4 unique fns are ported. -->

<!-- paper_trades.py absorption: the EXISTING dashboard_renderer/components/paper_trades.py
     ALREADY defines a public `render_paper_trades_region(state: dict) -> str` at line 4
     (NO underscore prefix). This is the canonical entrypoint and is PRESERVED. It currently
     does `import dashboard as d` to reach `d._compute_aggregate_stats` and `d._render_paper_trades_stats`
     — those calls are rewired in this task to canonical dashboard_renderer.* imports.
     The other 6 unique functions in dashboard_legacy/paper_trades_section.py
     (`_render_alert_badge`, `_render_close_form_section`, `_render_paper_trades_closed`,
     `_render_paper_trades_open`, `_render_paper_trades_open_form`, `_render_paper_trades_stats`)
     are ported VERBATIM with their underscore prefix preserved.
     CANONICAL NAME: `render_paper_trades_region` (public, no underscore). Downstream plan 32-04
     Task 1 + Task 2 bind against this exact public name. -->

<!-- trace.py: _TraceOpenPlaceholderMap is used by _TRACE_OPEN_PLACEHOLDER; both moved to
     formatters.py in 32-01. trace.py imports them from there. -->

<!-- All new files use 2-space indent. Do NOT run ruff format. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard_renderer/components/trace.py from dashboard_legacy/trace_panels.py</name>
  <read_first>
    - dashboard_legacy/trace_panels.py (full 247 LOC — source)
    - dashboard_renderer/components/__init__.py (component package init — confirm import discipline)
    - dashboard_renderer/formatters.py (post 32-01; confirms `_TraceOpenPlaceholderMap`, `_TRACE_FORMULAS`, `_SEED_LENGTHS`, `_VALID_TRACE_INSTRUMENT_KEYS`, `_TRACE_OPEN_PLACEHOLDER` are present)
    - dashboard_renderer/components/header.py (post 32-01; if any header helper is called by trace, confirm import path)
  </read_first>
  <action>
    Create a new file `dashboard_renderer/components/trace.py`. Port VERBATIM the four trace functions (`_render_trace_inputs`, `_render_trace_indicators`, `_render_trace_vote`, `_render_trace_panels`) and the module-level constants `_INDICATOR_DISPLAY_NAMES`, `_INDICATOR_DISPLAY_ORDER` from `dashboard_legacy/trace_panels.py`.

    Rewrite all imports in the new file to canonical `dashboard_renderer.*` paths (after 32-01 lands):
    - References to `_TraceOpenPlaceholderMap`, `_TRACE_FORMULAS`, `_SEED_LENGTHS`, `_VALID_TRACE_INSTRUMENT_KEYS`, `_TRACE_OPEN_PLACEHOLDER`, `_format_indicator_value`, etc. → `from dashboard_renderer.formatters import ...`.
    - Any references to `dr_*` aliases in the original become explicit imports from the canonical home.
    - Preserve `html.escape(value, quote=True)` at every dynamic leaf site.
    - 2-space indent throughout. Do NOT run ruff format.

    The new file's LOC budget per RESEARCH.md: ~247 — confirm `wc -l` ≤500.

    dashboard_legacy/trace_panels.py is NOT modified in this task — its three test importers (`tests/test_trace_atr_seed.py`, `tests/test_trace_vote_params.py`, `dashboard.py` re-export) continue to work until plan 32-04 retargets them.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py::TestGoldenSnapshot tests/test_html_xss_audit.py tests/test_trace_atr_seed.py tests/test_trace_vote_params.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _render_trace_inputs|^def _render_trace_indicators|^def _render_trace_vote|^def _render_trace_panels" dashboard_renderer/components/trace.py` returns exactly 4 hits.
    - `grep -nE "^_INDICATOR_DISPLAY_NAMES|^_INDICATOR_DISPLAY_ORDER" dashboard_renderer/components/trace.py` returns 2 hits.
    - `wc -l dashboard_renderer/components/trace.py` reports ≤500.
    - `grep -n "from dashboard_legacy" dashboard_renderer/components/trace.py` returns no hits.
    - `grep -n "import dashboard as d" dashboard_renderer/components/trace.py` returns no hits.
    - `python -c "from dashboard_renderer.components.trace import _render_trace_panels, _render_trace_inputs, _render_trace_indicators, _render_trace_vote; print('OK')"` prints `OK`.
    - Test command exits rc=0.
  </acceptance_criteria>
  <done>
    `dashboard_renderer/components/trace.py` exists with all 4 trace functions + 2 constants ported verbatim, imports rewritten to canonical paths, ≤500 LOC, byte-identical golden + XSS gates green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard_renderer/components/calc_rows.py from dashboard_legacy/calc_rows.py</name>
  <read_first>
    - dashboard_legacy/calc_rows.py (full 288 LOC — source of `_render_calc_row`, `_render_entry_target_row`)
    - dashboard_renderer/formatters.py (post 32-01; for `_fmt_currency`, `_fmt_em_dash`, `_format_indicator_value` etc.)
    - dashboard_renderer/components/__init__.py
  </read_first>
  <action>
    Create new file `dashboard_renderer/components/calc_rows.py`. Port VERBATIM the two functions `_render_calc_row` (~190 LOC) and `_render_entry_target_row` (~80 LOC) from `dashboard_legacy/calc_rows.py`.

    Rewrite imports to canonical paths:
    - Format helpers → `from dashboard_renderer.formatters import ...`.
    - `sizing_engine` import (`calc_position_size`, or whichever function the legacy file uses) MUST remain as a LOCAL function-body import inside `_render_calc_row` (the C-2 pattern). Preserve the `# LOCAL — C-2` comment. Per RESEARCH §"Pitfall 4". DO NOT lift this to module top.
    - Any `dr_*` aliases become explicit canonical imports.

    Preserve `html.escape(value, quote=True)` at every dynamic leaf site.
    2-space indent. Do NOT run ruff format.

    LOC budget per RESEARCH.md: ~270 — confirm `wc -l` ≤500.

    dashboard_legacy/calc_rows.py is NOT modified in this task.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py::TestGoldenSnapshot tests/test_html_xss_audit.py -x --tb=short -k "calc or golden"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _render_calc_row|^def _render_entry_target_row" dashboard_renderer/components/calc_rows.py` returns exactly 2 hits.
    - `wc -l dashboard_renderer/components/calc_rows.py` reports ≤500.
    - `grep -nE "^from sizing_engine|^import sizing_engine" dashboard_renderer/components/calc_rows.py` returns no hits (sizing_engine is C-2 local, function-body only).
    - `grep -n "sizing_engine" dashboard_renderer/components/calc_rows.py` returns at least one hit (the local import inside the function body).
    - `grep -n "from dashboard_legacy" dashboard_renderer/components/calc_rows.py` returns no hits.
    - `python -c "from dashboard_renderer.components.calc_rows import _render_calc_row, _render_entry_target_row; print('OK')"` prints `OK`.
  </acceptance_criteria>
  <done>
    `dashboard_renderer/components/calc_rows.py` exists with both calc-row functions ported verbatim; sizing_engine import preserved as C-2 local; ≤500 LOC; golden + XSS gates green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard_renderer/components/account.py from dashboard_legacy/account_section.py</name>
  <read_first>
    - dashboard_legacy/account_section.py (full 171 LOC — source of 5 functions)
    - dashboard_renderer/formatters.py (post 32-01)
    - dashboard_renderer/stats.py (post 32-01)
    - dashboard_renderer/components/__init__.py
  </read_first>
  <action>
    Create new file `dashboard_renderer/components/account.py`. Port VERBATIM all 5 functions: `_render_account_management_region`, `_render_account_balance_form`, `_render_account_stats`, `_render_key_stats`, `_compute_account_stat_values`.

    Rewrite imports to canonical paths:
    - Format helpers → `from dashboard_renderer.formatters import ...`.
    - Stats helpers → `from dashboard_renderer.stats import ...`.
    - Any `dr_*` aliases become explicit canonical imports.
    - No `dashboard_legacy.*` imports.

    Preserve `html.escape(value, quote=True)` at every dynamic leaf site.
    2-space indent. Do NOT run ruff format.

    LOC budget per RESEARCH.md: ~171 — confirm `wc -l` ≤500.

    dashboard_legacy/account_section.py is NOT modified in this task; `web/routes/markets.py` still imports `_render_account_management_region` from the `dashboard` shim — that caller is retargeted in plan 32-04.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py::TestGoldenSnapshot tests/test_html_xss_audit.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _render_account_management_region|^def _render_account_balance_form|^def _render_account_stats|^def _render_key_stats|^def _compute_account_stat_values" dashboard_renderer/components/account.py` returns exactly 5 hits.
    - `wc -l dashboard_renderer/components/account.py` reports ≤500.
    - `grep -n "from dashboard_legacy" dashboard_renderer/components/account.py` returns no hits.
    - `python -c "from dashboard_renderer.components.account import _render_account_management_region, _render_account_balance_form, _render_account_stats, _render_key_stats, _compute_account_stat_values; print('OK')"` prints `OK`.
    - Test command exits rc=0.
  </acceptance_criteria>
  <done>
    `dashboard_renderer/components/account.py` exists with all 5 account functions ported verbatim; imports canonical; ≤500 LOC; golden + XSS gates green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Absorb positions_section.py unique content into components/positions.py</name>
  <read_first>
    - dashboard_legacy/positions_section.py (full 347 LOC — source)
    - dashboard_renderer/components/positions.py (current 73 LOC — receiving file; audit existing functions)
    - dashboard_renderer/components/calc_rows.py (this plan's Task 2 output — provides `_render_entry_target_row`)
    - dashboard_renderer/formatters.py (post 32-01; for `_display_names`, `_fmt_currency`, etc.)
  </read_first>
  <action>
    Audit (per D-03): identify the unique functions in dashboard_legacy/positions_section.py with no counterpart in dashboard_renderer/components/positions.py — per RESEARCH.md these are `_render_open_form` (~90 LOC), `_render_single_position_row` (~90 LOC), `_render_drift_banner` (~45 LOC), `_render_trailing_stop_guidance` (~65 LOC). The other 2 (`_render_positions_table`, `_render_trades_table`) are pure delegations and are NOT ported here.

    Port the 4 unique functions VERBATIM into dashboard_renderer/components/positions.py. Adjust internal calls:
    - `_render_entry_target_row` reference → `from dashboard_renderer.components.calc_rows import _render_entry_target_row` (module-top import is fine — calc_rows does not import positions).
    - `_render_calc_row` reference (used inside `_render_single_position_row`) → `from dashboard_renderer.components.calc_rows import _render_calc_row` (module-top import).
    - Format helpers → `from dashboard_renderer.formatters import ...`.
    - `sizing_engine.get_trailing_stop` (used by `_render_trailing_stop_guidance`) MUST stay as a LOCAL function-body import (C-2 pattern). Preserve the comment. Per RESEARCH §"Pitfall 4".

    Preserve `html.escape(value, quote=True)` at every dynamic leaf. 2-space indent. Do NOT run ruff format.

    LOC budget per RESEARCH.md: positions.py projected ~363 — confirm `wc -l` ≤500. If overflow, split at a natural seam (per D-02) into e.g. `positions.py` (drift_banner + trailing_stop_guidance) + `position_forms.py` (open_form + single_position_row) — pick names by content semantic, document the seam in summary.

    dashboard_legacy/positions_section.py is NOT modified in this task.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py tests/test_web_dashboard.py tests/test_html_xss_audit.py tests/test_notifier.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _render_open_form|^def _render_single_position_row|^def _render_drift_banner|^def _render_trailing_stop_guidance" dashboard_renderer/components/positions.py` returns exactly 4 hits (or split across positions.py + seam file).
    - `wc -l dashboard_renderer/components/positions.py` reports ≤500 (and every seam-file daughter ≤500).
    - `grep -nE "^from sizing_engine|^import sizing_engine" dashboard_renderer/components/positions.py` returns no hits (sizing_engine is C-2 local).
    - `grep -n "sizing_engine" dashboard_renderer/components/positions.py` returns at least one hit (function-body local import for `get_trailing_stop`).
    - `grep -n "from dashboard_legacy" dashboard_renderer/components/positions.py` returns no hits.
    - `grep -n "import dashboard as d\b" dashboard_renderer/components/positions.py` returns no hits — note this task is permitted to keep an existing `import dashboard as d` line if positions.py currently has one for backward-compat; that line is removed in plan 32-03. If a fresh implementation, there should be NO `import dashboard as d`.
    - `python -c "from dashboard_renderer.components.positions import _render_open_form, _render_single_position_row, _render_drift_banner, _render_trailing_stop_guidance; print('OK')"` prints `OK`.
    - Test command exits rc=0.
  </acceptance_criteria>
  <done>
    Four unique positions_section functions live in dashboard_renderer/components/positions.py (or its split). C-2 sizing_engine import preserved. ≤500 LOC. dashboard_legacy/positions_section.py untouched. Golden + XSS + notifier tests green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Absorb paper_trades_section.py unique content into components/paper_trades.py</name>
  <read_first>
    - dashboard_legacy/paper_trades_section.py (full 310 LOC — source of 7 functions)
    - dashboard_renderer/components/paper_trades.py (current 29 LOC — receiving file; VERIFIED at start of plan that line 4 defines `def render_paper_trades_region(state: dict) -> str:` — PUBLIC name, no underscore. This is the canonical entrypoint and is PRESERVED.)
    - dashboard_renderer/stats.py (post 32-01; for `compute_aggregate_stats`)
    - dashboard_renderer/formatters.py (post 32-01)
  </read_first>
  <action>
    CANONICAL NAMING LOCK: The existing public function `dashboard_renderer.components.paper_trades.render_paper_trades_region` (no underscore prefix — defined at line 4 of the current 29-LOC stub) is PRESERVED as the canonical entrypoint. Downstream plan 32-04 Task 1 + Task 2 import this exact public name (`from dashboard_renderer.components.paper_trades import render_paper_trades_region`). The 6 OTHER ported functions keep their legacy underscore prefix: `_render_alert_badge`, `_render_close_form_section`, `_render_paper_trades_closed`, `_render_paper_trades_open`, `_render_paper_trades_open_form`, `_render_paper_trades_stats`. Legacy's `_render_paper_trades_region` (underscore) is a 1-line delegation in dashboard_legacy and is NOT ported — the renderer-side public entrypoint already exists and supersedes it.

    Audit (per D-03): The 29-LOC receiving stub already defines `render_paper_trades_region` and uses `import dashboard as d` + `d._compute_aggregate_stats(...)` + `d._render_paper_trades_stats(stats)`. Per RESEARCH.md, ~280 LOC of unique content lives across the 6 underscore-prefixed functions in dashboard_legacy/paper_trades_section.py.

    Step 1 — Port the 6 underscore-prefixed UNIQUE functions VERBATIM from `dashboard_legacy/paper_trades_section.py` into `dashboard_renderer/components/paper_trades.py`: `_render_alert_badge`, `_render_close_form_section`, `_render_paper_trades_closed`, `_render_paper_trades_open`, `_render_paper_trades_open_form`, `_render_paper_trades_stats`. Preserve their underscore prefixes.

    Step 2 — Rewire the existing public `render_paper_trades_region` body to use canonical imports instead of `import dashboard as d`:
    - `d._compute_aggregate_stats(paper_trades, signals)` → `compute_aggregate_stats(paper_trades, signals)` (imported from `dashboard_renderer.stats`).
    - `d._render_paper_trades_stats(stats)` → `_render_paper_trades_stats(stats)` (now a direct same-file call after Step 1 port).
    - Any other `d.` prefixed call inside `render_paper_trades_region` body becomes a direct same-file call OR a canonical dashboard_renderer.* import.

    Step 3 — Module-top imports for the rewired file:
    - `from dashboard_renderer.stats import compute_aggregate_stats` (replaces the `d._compute_aggregate_stats` lookup).
    - `from dashboard_renderer.formatters import ...` (for any format helpers used by the 6 ported functions — verify against legacy bodies; typically `_fmt_currency`, `_fmt_em_dash`, etc. with their underscore prefixes preserved per 32-01 lock).
    - Cross-function calls between the 7 functions become DIRECT calls (no `d.` prefix) since they are now in the same file.

    Step 4 — REMOVE the existing `import dashboard as d` line at the top of `dashboard_renderer/components/paper_trades.py` (currently at line 5 inside the function body). After this task, NO `import dashboard as d` or `import dashboard` lines remain in this file.

    Preserve `html.escape(value, quote=True)` at every dynamic leaf. 2-space indent. Do NOT run ruff format.

    LOC budget per RESEARCH.md: ~309 (29 existing + ~280 ported) — confirm `wc -l` ≤500. If overflow, split at a natural seam (per D-02).

    dashboard_legacy/paper_trades_section.py is NOT modified in this task.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py tests/test_html_xss_audit.py tests/test_web_dashboard.py tests/test_notifier.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _render_alert_badge|^def _render_close_form_section|^def _render_paper_trades_closed|^def _render_paper_trades_open|^def _render_paper_trades_open_form|^def _render_paper_trades_stats" dashboard_renderer/components/paper_trades.py` returns exactly 6 hits (the six underscore-prefixed ports).
    - `grep -nE "^def render_paper_trades_region" dashboard_renderer/components/paper_trades.py` returns exactly 1 hit (the existing public entrypoint, no underscore — PRESERVED).
    - `grep -cE "^def (render_paper_trades_region|_render_alert_badge|_render_close_form_section|_render_paper_trades_closed|_render_paper_trades_open|_render_paper_trades_open_form|_render_paper_trades_stats)" dashboard_renderer/components/paper_trades.py` returns 7 (combined total).
    - `wc -l dashboard_renderer/components/paper_trades.py` reports ≤500.
    - `grep -nE "^import dashboard\b|^from dashboard\b|import dashboard as d" dashboard_renderer/components/paper_trades.py` returns NO hits (the function-body `import dashboard as d` at the old line 5 must also be gone — search whole file, not just module top).
    - `grep -n "from dashboard_legacy" dashboard_renderer/components/paper_trades.py` returns no hits.
    - `grep -nE "^from dashboard_renderer.stats import compute_aggregate_stats" dashboard_renderer/components/paper_trades.py` returns at least 1 hit (canonical stats import added).
    - `python -c "from dashboard_renderer.components.paper_trades import render_paper_trades_region, _render_paper_trades_open, _render_paper_trades_closed, _render_paper_trades_stats, _render_paper_trades_open_form, _render_close_form_section, _render_alert_badge; print('OK')"` prints `OK`. NOTE: top-level entrypoint is `render_paper_trades_region` (PUBLIC, no underscore) — NOT `_render_paper_trades_region`.
    - Test command exits rc=0 (includes tests/test_notifier.py to cover paper-trades-adjacent notifier behavior).
  </acceptance_criteria>
  <done>
    The 6 underscore-prefixed paper_trades_section functions live in dashboard_renderer/components/paper_trades.py with verbatim bodies; the existing public `render_paper_trades_region` (no underscore) is preserved and rewired to canonical imports; no `import dashboard as d` remains anywhere in that file; dashboard_legacy/paper_trades_section.py untouched; ≤500 LOC; golden + XSS + web-dashboard + notifier tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard_legacy ↔ dashboard_renderer | Refactor moves render logic across; same input surface (state dict). |
| Rendered HTML → browser | `html.escape(value, quote=True)` at every dynamic leaf — preserved verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Tampering | New trace/calc_rows/account/positions/paper_trades component files | mitigate | XSS AST gate (`tests/test_html_xss_audit.py`) enforces `html.escape(value, quote=True)`; ported bodies preserve call sites verbatim. |
| T-32-02-02 | Elevation of Privilege | sizing_engine import discipline | mitigate | C-2 local imports (function-body only) preserved per CLAUDE.md hex-lite — module-top imports of sizing_engine are forbidden by AST gate. Acceptance criteria explicitly grep for the C-2 pattern. |
| T-32-02-03 | Repudiation | Logger name continuity | accept | New component files use `logging.getLogger(__name__)`; the journalctl `dashboard` tag continuity is preserved by `dashboard.py` shim (untouched in this plan). |
</threat_model>

<verification>
- All five tasks' `<automated>` test commands exit rc=0.
- `git diff --name-only dashboard_renderer/` lists exactly the five files in `files_modified`.
- `git diff --name-only dashboard_legacy/` is empty (legacy untouched).
- `grep -rn "import dashboard as d" dashboard_renderer/components/trace.py dashboard_renderer/components/calc_rows.py dashboard_renderer/components/account.py dashboard_renderer/components/paper_trades.py` returns ZERO hits (positions.py may still have one — fine; it's removed in 32-03).
- Aggregate boundary check: `grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/components/` returns ZERO hits across all Wave 2 output files. This is the aggregate scan that confirms no new component file accidentally imports from legacy.
- Full suite quick gate: `.venv/bin/pytest -x --tb=short tests/test_dashboard.py tests/test_web_dashboard.py tests/test_html_xss_audit.py tests/test_notifier.py tests/test_trace_atr_seed.py tests/test_trace_vote_params.py` is green.
</verification>

<success_criteria>
1. Three new component files exist: `trace.py`, `calc_rows.py`, `account.py` — each with the documented function set, canonical imports, ≤500 LOC.
2. `components/positions.py` absorbs its 4 unique legacy fns; `components/paper_trades.py` absorbs the 6 underscore-prefixed legacy fns AND preserves the existing public `render_paper_trades_region` entrypoint with canonical imports.
3. C-2 local imports of `sizing_engine` preserved (function-body only).
4. dashboard_legacy/* unmodified.
5. Golden HTML byte-identical. XSS, golden, hex-boundary tests all rc=0.
6. `grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/components/` returns zero hits (aggregate boundary clean).
</success_criteria>

<output>
After completion, create `.planning/phases/32-dashboard-legacy-retirement/32-02-SUMMARY.md` documenting:
- Per-component LOC counts post-absorption.
- Any seam splits introduced.
- A list of all `import dashboard as d` lines still remaining in `dashboard_renderer/` (will be eliminated by plan 32-03).
- Confirmation that `render_paper_trades_region` (public, no underscore) is the preserved entrypoint and that the 6 ported helpers keep their underscore prefixes.
- Aggregate boundary check result: output of `grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/components/` (must be empty).
</output>
