---
phase: 32-dashboard-legacy-retirement
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - dashboard_renderer/api.py
  - dashboard_renderer/pages.py
  - dashboard_renderer/components/positions.py
  - dashboard_renderer/__init__.py
autonomous: true
requirements: [OPS-06]

must_haves:
  truths:
    - "No file under dashboard_renderer/ contains the string `import dashboard as d` (the deferred circular-import indirection is eliminated)"
    - "dashboard_renderer/api.py calls render_html_shell, _render_tabbed_dashboard, _render_single_page_dashboard, atomic_write_html, _resolve_strategy_version, _resolve_trace_open_keys, render_header_from_context via direct from-imports of their canonical dashboard_renderer.* modules"
    - "dashboard_renderer/pages.py calls _render_page_body and _render_single_page_dashboard via direct from-imports of dashboard_renderer.shell"
    - "Atomic-write callers in api.py use dashboard_renderer.io.atomic_write_html directly (no _atomic_write_html wrapper)"
    - "Golden snapshot tests pass (byte-identical HTML output)"
    - "Module-import of dashboard_renderer is acyclic — a fresh-subprocess `import dashboard_renderer` does NOT side-effect-load `dashboard.py`"
    - "No file under dashboard_renderer/ contains any `from dashboard_legacy` or `import dashboard_legacy` import (aggregate scan across entire dashboard_renderer/ tree returns zero hits)"
  artifacts:
    - path: dashboard_renderer/api.py
      provides: "render_dashboard_files, render_dashboard_page orchestrators with direct imports of all helper modules"
      contains: "from dashboard_renderer.shell import"
    - path: dashboard_renderer/pages.py
      provides: "render_panel_only and other page composers with direct shell imports"
      contains: "from dashboard_renderer.shell import"
    - path: dashboard_renderer/components/positions.py
      provides: "positions component with no `import dashboard as d` residue"
      contains: "_render_open_form"
    - path: dashboard_renderer/__init__.py
      provides: "Public package surface; exports render_dashboard_files, render_dashboard_page, render_panel_html"
      contains: "render_dashboard_files"
  key_links:
    - from: dashboard_renderer/api.py
      to: dashboard_renderer/shell.py
      via: "direct from-import of render_html_shell, _render_tabbed_dashboard, _render_single_page_dashboard"
      pattern: "from dashboard_renderer.shell import"
    - from: dashboard_renderer/api.py
      to: dashboard_renderer/formatters.py
      via: "direct from-import of _resolve_strategy_version, _resolve_trace_open_keys"
      pattern: "from dashboard_renderer.formatters import"
    - from: dashboard_renderer/api.py
      to: dashboard_renderer/io.py
      via: "direct call atomic_write_html(...)"
      pattern: "atomic_write_html"
---

<objective>
Eliminate every `import dashboard as d` site inside `dashboard_renderer/`. Replace each `d.<symbol>` call with a direct from-import of the symbol from its canonical `dashboard_renderer.*` home (populated by plans 32-01 and 32-02).

Purpose: Break the circular import between `dashboard.py` and `dashboard_renderer/api.py` (+ `pages.py`, `components/positions.py`). This is the structural fix that allows plan 32-04 to thin `dashboard.py` to a ≤100 LOC pass-through shim without losing functionality.

Output: All four files in `files_modified` have zero `import dashboard as d` lines and zero `d.<symbol>` references; the `dashboard_renderer` package imports cleanly without entering `dashboard.py` at import time; and an aggregate scan confirms no `dashboard_legacy` imports remain anywhere under `dashboard_renderer/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dashboard-legacy-retirement/32-CONTEXT.md
@.planning/phases/32-dashboard-legacy-retirement/32-RESEARCH.md
@.planning/phases/32-dashboard-legacy-retirement/32-01-PLAN.md
@.planning/phases/32-dashboard-legacy-retirement/32-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- After plans 32-01 + 32-02, every symbol previously reached via `import dashboard as d`
     lives in a canonical dashboard_renderer.* location. Per RESEARCH §"Circular Import Map":

     api.py:
       d._resolve_strategy_version          -> dashboard_renderer.formatters._resolve_strategy_version
       d._resolve_trace_open_keys           -> dashboard_renderer.formatters._resolve_trace_open_keys
       d._render_header_ctx                 -> dashboard_renderer.components.header.render_header_from_context
       d._render_html_shell                 -> dashboard_renderer.shell.render_html_shell
       d.logger                             -> module-level logging.getLogger(__name__) inside api.py
       d._render_tabbed_dashboard           -> dashboard_renderer.shell._render_tabbed_dashboard
       d._render_single_page_dashboard      -> dashboard_renderer.shell._render_single_page_dashboard
       d._atomic_write_html(html, path)     -> dashboard_renderer.io.atomic_write_html(html, path)

     pages.py:
       dashboard._render_single_page_dashboard -> dashboard_renderer.shell._render_single_page_dashboard
       d._render_page_body                  -> dashboard_renderer.shell._render_page_body (returns 5-tuple)

     components/positions.py:
       d._display_names                     -> dashboard_renderer.formatters._display_names
       d._render_entry_target_row           -> dashboard_renderer.components.calc_rows._render_entry_target_row
       d._render_single_position_row        -> local in same file (plan 32-02 absorbed)
       d._render_calc_row                   -> dashboard_renderer.components.calc_rows._render_calc_row
       d._render_open_form                  -> local in same file (plan 32-02 absorbed)
-->

<!-- dashboard_renderer/__init__.py edit scope: the file currently re-exports the 3-function
     public surface (`render_dashboard_files`, `render_dashboard_page`, `render_panel_html`).
     This plan adds NO new public exports; it merely confirms `import dashboard_renderer`
     succeeds without dragging in `dashboard.py`. Optional: add `__all__` per Claude's
     Discretion (CONTEXT D-claude-1). -->

<!-- shell.render_html_shell is the new active name after 32-01 (replaced the dead stub).
     api.py calls it directly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire dashboard_renderer/api.py — eliminate `import dashboard as d`</name>
  <read_first>
    - dashboard_renderer/api.py (full 202 LOC — current `d.*` call sites at lines 30, 52, 75, 117, 119, 168, 186, 201)
    - dashboard_renderer/shell.py (post 32-01 — confirm `render_html_shell`, `_render_tabbed_dashboard`, `_render_single_page_dashboard` symbols)
    - dashboard_renderer/formatters.py (post 32-01 — confirm `_resolve_strategy_version`, `_resolve_trace_open_keys`)
    - dashboard_renderer/components/header.py (post 32-01 — confirm `render_header_from_context` or `_render_header_ctx` symbol name; if the canonical name is `render_header_from_context`, use that)
    - dashboard_renderer/io.py (confirm `atomic_write_html` is the public symbol — currently 33 LOC)
  </read_first>
  <action>
    Open dashboard_renderer/api.py. Make the following replacements:

    1. Remove the deferred `import dashboard as d` line(s) (wherever they appear — inside function bodies or at the top).
    2. Add module-top imports (NOT deferred, NOT inside function bodies):
       - `from dashboard_renderer.shell import render_html_shell, _render_tabbed_dashboard, _render_single_page_dashboard`
       - `from dashboard_renderer.formatters import _resolve_strategy_version, _resolve_trace_open_keys`
       - `from dashboard_renderer.components.header import render_header_from_context` (use the name actually exposed after 32-01; if the legacy was `_render_header_ctx` and the renderer-side equivalent is `render_header_from_context`, use the latter)
       - `from dashboard_renderer.io import atomic_write_html`
    3. Add (or confirm) a module-level `logger = logging.getLogger(__name__)` in api.py and rewrite every `d.logger` call site to `logger`.
    4. Replace every call site:
       - `d._resolve_strategy_version(state)` → `_resolve_strategy_version(state)`
       - `d._resolve_trace_open_keys(...)` → `_resolve_trace_open_keys(...)`
       - `d._render_header_ctx(ctx, ...)` → `render_header_from_context(ctx, ...)` (confirm signature compatibility against 32-01 port)
       - `d._render_html_shell(ctx, body)` → `render_html_shell(ctx, body)`
       - `d._render_tabbed_dashboard(ctx)` → `_render_tabbed_dashboard(ctx)`
       - `d._render_single_page_dashboard(ctx, page)` → `_render_single_page_dashboard(ctx, page)`
       - `d._atomic_write_html(html_str, path)` → `atomic_write_html(html_str, path)`
       - `d.logger` → `logger`
    5. Confirm no `import dashboard` (with or without `as d`) remains anywhere in api.py.

    The new module-top imports may appear in any reasonable order; group stdlib → third-party → local per Claude's discretion (CONTEXT). Maintain 2-space indent. Do NOT run ruff format.

    LOC budget per RESEARCH.md: ~202 unchanged ± a few lines.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py::TestEmptyState tests/test_dashboard.py::TestGoldenSnapshot tests/test_dashboard_split_seam.py tests/test_html_xss_audit.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import dashboard" dashboard_renderer/api.py` returns NO hits (neither `import dashboard` nor `import dashboard as d`).
    - `grep -nE "\\bd\\._|\\bd\\.logger\\b" dashboard_renderer/api.py` returns NO hits.
    - `grep -nE "^from dashboard_renderer.shell import|^from dashboard_renderer.formatters import|^from dashboard_renderer.io import|^from dashboard_renderer.components.header import" dashboard_renderer/api.py` returns at least 4 hits (module-top imports added).
    - `wc -l dashboard_renderer/api.py` reports ≤500.
    - `python -c "import dashboard_renderer.api; print('OK')"` prints `OK`.
    - Test command exits rc=0.
  </acceptance_criteria>
  <done>
    dashboard_renderer/api.py has no `import dashboard as d` or `d.<symbol>` references; all helper symbols come from canonical dashboard_renderer.* module-top imports; golden + split-seam tests rc=0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire dashboard_renderer/pages.py — eliminate `import dashboard as d`</name>
  <read_first>
    - dashboard_renderer/pages.py (full 39 LOC — current `d.*` and `dashboard.*` sites at lines 16, 32, 38)
    - dashboard_renderer/shell.py (post 32-01 — confirm `_render_page_body` returns 5-tuple, `_render_single_page_dashboard` available)
  </read_first>
  <action>
    Open dashboard_renderer/pages.py. Make the following replacements:

    1. Remove `import dashboard as d` and `import dashboard` lines (if any) at module top.
    2. Add module-top imports:
       - `from dashboard_renderer.shell import _render_page_body, _render_single_page_dashboard`
    3. Replace call sites:
       - `dashboard._render_single_page_dashboard(ctx, page)` → `_render_single_page_dashboard(ctx, page)`
       - `d._render_page_body(ctx, page)` → `_render_page_body(ctx, page)` (returns 5-tuple — confirm consumer code unpacks it correctly per RESEARCH §"Pitfall 5")
       - `d._render_single_page_dashboard(ctx, page)` (line 16) → `_render_single_page_dashboard(ctx, page)`
    4. Confirm no `import dashboard` remains.

    2-space indent. Do NOT run ruff format.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py tests/test_dashboard_split_seam.py tests/test_web_dashboard.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import dashboard" dashboard_renderer/pages.py` returns NO hits.
    - `grep -nE "\\bd\\._|\\bdashboard\\._" dashboard_renderer/pages.py` returns NO hits.
    - `grep -nE "^from dashboard_renderer.shell import" dashboard_renderer/pages.py` returns at least 1 hit.
    - `wc -l dashboard_renderer/pages.py` reports ≤500.
    - `python -c "import dashboard_renderer.pages; print('OK')"` prints `OK`.
    - Test command exits rc=0.
  </acceptance_criteria>
  <done>
    dashboard_renderer/pages.py has no `import dashboard` reference; all helper symbols come from canonical dashboard_renderer.shell imports; golden + split-seam + web-dashboard tests rc=0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewire dashboard_renderer/components/positions.py — eliminate any residual `import dashboard as d`</name>
  <read_first>
    - dashboard_renderer/components/positions.py (post 32-02 — confirm whether `import dashboard as d` was already removed by 32-02 or persists)
    - dashboard_renderer/formatters.py (post 32-01 — for `_display_names`)
    - dashboard_renderer/components/calc_rows.py (post 32-02 — for `_render_entry_target_row`, `_render_calc_row`)
  </read_first>
  <action>
    Audit dashboard_renderer/components/positions.py for any remaining `import dashboard as d` or `d.<symbol>` references. If 32-02 already eliminated them, this task is a no-op confirmation. Otherwise:

    1. Remove `import dashboard as d` line.
    2. Add module-top imports as needed:
       - `from dashboard_renderer.formatters import _display_names`
       - `from dashboard_renderer.components.calc_rows import _render_entry_target_row, _render_calc_row` (only if those names are still referenced — many were absorbed local in 32-02)
    3. Replace call sites:
       - `d._display_names(state)` → `_display_names(state)`
       - `d._render_entry_target_row(...)` → `_render_entry_target_row(...)`
       - `d._render_calc_row(...)` → `_render_calc_row(...)`
       - `d._render_single_position_row(...)` → local call (no prefix), since 32-02 absorbed it
       - `d._render_open_form(state)` → local call, since 32-02 absorbed it
    4. Confirm no `import dashboard` remains.

    If there are no `d.*` references after 32-02, this task documents that fact in the summary and exits without changes.

    2-space indent. Do NOT run ruff format.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py tests/test_html_xss_audit.py tests/test_web_dashboard.py tests/test_notifier.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import dashboard\b" dashboard_renderer/components/positions.py` returns NO hits.
    - `grep -nE "\\bd\\._|\\bd\\.logger\\b" dashboard_renderer/components/positions.py` returns NO hits.
    - `wc -l dashboard_renderer/components/positions.py` reports ≤500.
    - `python -c "import dashboard_renderer.components.positions; print('OK')"` prints `OK`.
    - Test command exits rc=0.
  </acceptance_criteria>
  <done>
    dashboard_renderer/components/positions.py has no `import dashboard` and no `d.*` references; canonical imports in place; tests rc=0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Confirm dashboard_renderer package import is acyclic + tighten __init__.py if needed</name>
  <read_first>
    - dashboard_renderer/__init__.py (current 9 LOC — confirm 3-function public re-export)
    - dashboard_renderer/api.py (post Task 1 of this plan)
    - dashboard_renderer/pages.py (post Task 2 of this plan)
  </read_first>
  <action>
    STUB PROTOTYPE VALIDATION (required before Wave 4 proceeds — per cross-AI review): Verify that the `__path__ = []` + `__getattr__` stub mechanism works correctly in the current Python version. Run this in-process prototype test FIRST — it validates the mechanism before plan 32-04 relies on it:

    ```
    python -c "
    import sys, types
    m = types.ModuleType('_test_stub')
    m.__path__ = []
    def _test_getattr(name):
        raise ImportError('_test_stub retired — use new_module')
    m.__getattr__ = _test_getattr
    sys.modules['_test_stub'] = m
    try:
        import _test_stub
        _test_stub.foo
        print('FAIL: no ImportError raised')
    except ImportError as e:
        print('OK attribute access:', e)
    "
    ```

    This prototype MUST exit 0 and print `OK attribute access: _test_stub retired — use new_module`. If it prints `FAIL` or exits non-zero, document the Python version and raise as a blocker before 32-04 runs. Record the Python version that was verified (e.g., `python --version`) in the SUMMARY.

    Verify via FRESH-SUBPROCESS invocation that `import dashboard_renderer` does NOT side-effect-load `dashboard.py`. A subprocess is required because earlier tests in the same interpreter may have already imported `dashboard`, in which case an in-process `assert 'dashboard' not in sys.modules` would pass vacuously. Per V-05.

    Run:
    ```
    python -c "import subprocess, sys; subprocess.run([sys.executable, '-c', 'import sys; import dashboard_renderer; assert \"dashboard\" not in sys.modules, \"dashboard.py loaded as side-effect of dashboard_renderer import\"; print(\"OK acyclic\")'], check=True)"
    ```
    The inner subprocess MUST exit rc=0 and emit `OK acyclic`. If `dashboard` ends up in `sys.modules` in the fresh interpreter, the assertion fails (subprocess exits non-zero, `check=True` raises).

    Also run the standard public-API smoke import:
    ```
    python -c "from dashboard_renderer import render_dashboard_files, render_dashboard_page, render_panel_html; print('OK exports')"
    ```

    Per Claude's Discretion (CONTEXT): optionally add `__all__ = ['render_dashboard_files', 'render_dashboard_page', 'render_panel_html']` to `dashboard_renderer/__init__.py` to formalize the public surface — choose to add it if it clarifies external consumer expectations, skip if not.

    Confirm there is NO `import dashboard` anywhere under `dashboard_renderer/*` (recursive grep). The legacy delegation chain `dashboard.py → dashboard_legacy → dashboard_renderer` is one-way after this plan.

    Do NOT modify dashboard.py or dashboard_legacy/* in this task.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_dashboard.py tests/test_dashboard_split_seam.py tests/test_signal_engine.py::TestDeterminism::test_dashboard_no_forbidden_imports tests/test_html_xss_audit.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Stub prototype validation: `python -c "import sys, types; m = types.ModuleType('_test_stub'); m.__path__ = []; m.__getattr__ = lambda name: (_ for _ in ()).throw(ImportError('_test_stub retired — use new_module')); sys.modules['_test_stub'] = m; import _test_stub; _test_stub.foo" 2>&1 | grep -E "ImportError.*_test_stub retired"` exits rc=0 and the output contains `ImportError: _test_stub retired — use new_module`. (Alternatively, run the multi-line form from `<action>` and confirm it prints `OK attribute access: ...`.) Document the Python version verified.
    - `grep -rn "^import dashboard\b\|^import dashboard as\|\bimport dashboard as d\b" dashboard_renderer/` returns NO hits.
    - Fresh-subprocess acyclic gate: `python -c "import subprocess, sys; subprocess.run([sys.executable, '-c', 'import sys; import dashboard_renderer; assert \"dashboard\" not in sys.modules, \"dashboard.py loaded as side-effect of dashboard_renderer import\"; print(\"OK acyclic\")'], check=True)"` exits rc=0 and the inner subprocess prints `OK acyclic`. This MUST be a subprocess invocation; an in-process `assert 'dashboard' not in sys.modules` is rejected (vacuous-pass hazard).
    - `python -c "from dashboard_renderer import render_dashboard_files, render_dashboard_page, render_panel_html; print('OK exports')"` prints `OK exports`.
    - `wc -l dashboard_renderer/__init__.py` reports ≤50 (lightweight package init).
    - Import forms gate (medium — per cross-AI review): `grep -rn "^from dashboard import\|^from dashboard_legacy import" dashboard_renderer/` returns NO hits (covers `from dashboard_legacy import ...` and `from dashboard import ...` forms, which the earlier `import dashboard as d` grep does not catch).
    - Test command exits rc=0.
  </acceptance_criteria>
  <done>
    `dashboard_renderer/` imports cleanly without dragging `dashboard.py` (proven via fresh subprocess); stub prototype validated for current Python version; all `from dashboard` and `from dashboard_legacy` import forms absent from `dashboard_renderer/`; public surface re-exports `render_dashboard_files`, `render_dashboard_page`, `render_panel_html`; full gate tests rc=0.
  </done>
</task>

<task type="auto">
  <name>Task 5: Scan dashboard_renderer/ for residual dashboard_legacy imports; rewrite any hits before Wave 4</name>
  <read_first>
    - dashboard_renderer/ directory tree (all .py files — the aggregate scan target)
  </read_first>
  <action>
    BLOCKER RESOLUTION (H-01 from cross-AI review): Tasks 1-4 above eliminate `import dashboard as d` sites in four specific files, and per-task acceptance criteria confirm individual files are clean. However, an aggregate scan across the ENTIRE `dashboard_renderer/` tree is required before Wave 4 deletes legacy files via `git rm`. If any ported file accidentally slipped through with a `from dashboard_legacy.*` or `import dashboard_legacy` import, Wave 4's `git rm` would break Python's import machinery at that call site.

    Step 1 — Run the aggregate scan:
    ```
    grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/
    ```

    Step 2 — For EACH hit returned:
    - Identify the canonical `dashboard_renderer.*` equivalent of the legacy symbol being imported (check the MANIFEST from 32-01-SUMMARY.md and the canonical name tables in the `<interfaces>` blocks of plans 32-01 and 32-02).
    - Rewrite the import line to use the canonical path.
    - Rewrite any call sites in the same function/method that use the old legacy-prefixed name to use the canonical name.
    - Preserve 2-space indent. Do NOT run ruff format.

    Step 3 — Re-run the scan to confirm zero hits:
    ```
    grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/
    ```
    This command MUST return no output (zero hits).

    If Step 1 returns zero hits, this task is a verification-only no-op. Document that fact in the SUMMARY (with the command and its empty output). Do NOT skip the task — the empty-output confirmation is the required artifact.

    dashboard_legacy/* is NOT modified in this task (Wave 4 does the deletion).
  </action>
  <verify>
    <automated>grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/ && echo "FAIL: legacy imports found" || echo "OK: zero legacy imports in dashboard_renderer/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/` returns ZERO hits (no output, exit rc=1 from grep with no matches — which is acceptable; the `|| echo` in `<verify>` will print `OK: zero legacy imports in dashboard_renderer/`).
    - After any rewrites: `python -c "import dashboard_renderer; print('OK')"` still exits 0 (no import breakage from the rewrites).
    - After any rewrites: `.venv/bin/pytest tests/test_dashboard.py tests/test_html_xss_audit.py -x --tb=short` exits rc=0.
    - The scan result (whether zero hits or a list of rewrites) is documented in 32-03-SUMMARY.md.
  </acceptance_criteria>
  <done>
    Aggregate scan of `dashboard_renderer/` confirms zero `from dashboard_legacy` or `import dashboard_legacy` lines remain. Wave 4's `git rm` of legacy submodule files will not break any import in `dashboard_renderer/`. Scan result documented in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard.py ↔ dashboard_renderer | Import direction is now one-way (`dashboard.py → dashboard_renderer`). The reverse direction (renderer → dashboard) is eliminated, removing the circular hazard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Denial of Service | Import cycle | mitigate | Acceptance criterion explicitly asserts via fresh subprocess that `import dashboard_renderer` does NOT load `dashboard` module as a side-effect; the recursive grep gate forbids `import dashboard` under `dashboard_renderer/`. |
| T-32-03-02 | Tampering | Symbol re-binding via wrong canonical path | mitigate | Direct from-imports of named symbols replace `d.<attr>` lookups; if a symbol is missing the import fails at module-load time (loud failure) rather than at first call (silent latent bug). |
| T-32-03-03 | Denial of Service | Residual dashboard_legacy import after git rm | mitigate | Task 5 aggregate scan across entire `dashboard_renderer/` tree catches any file that slipped through per-task checks; must return zero hits before Wave 4 deletes legacy files. |
</threat_model>

<verification>
- All five tasks' `<automated>` test commands exit rc=0.
- `git diff --name-only dashboard_renderer/` lists exactly the four files in `files_modified` (plus possibly `__init__.py` if `__all__` was added).
- Recursive grep `grep -rn "import dashboard\b\|import dashboard as" dashboard_renderer/` returns NO hits.
- Recursive grep `grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/` returns NO hits (Task 5 aggregate scan — BLOCKER gate; must be confirmed before Wave 4 proceeds).
- Fresh-subprocess acyclic gate (from Task 4 acceptance) exits rc=0.
- Full suite: `.venv/bin/pytest -x --tb=short` is green.
</verification>

<success_criteria>
1. Zero `import dashboard as d` or `import dashboard` lines in any file under `dashboard_renderer/`.
2. Zero `from dashboard_legacy` or `import dashboard_legacy` lines in any file under `dashboard_renderer/` (Task 5 BLOCKER gate).
3. Fresh-subprocess `python -c "import dashboard_renderer"` does NOT side-effect-load `dashboard.py`.
4. All call sites that were `d.<symbol>` resolve via direct module-top imports of canonical `dashboard_renderer.*` names.
5. `__path__ = []` + `__getattr__` stub prototype validated for current Python version (Task 4).
6. Golden HTML byte-identical. Split-seam, golden, hex-boundary, XSS gates all rc=0.
</success_criteria>

<output>
After completion, create `.planning/phases/32-dashboard-legacy-retirement/32-03-SUMMARY.md` documenting:
- Exact diff for each of the four files in `files_modified` (which `d.<symbol>` references were rewritten, which canonical import paths replaced them).
- The subprocess invocation result confirming `import dashboard_renderer` is acyclic with respect to `dashboard.py`.
- Stub prototype validation result: Python version verified, output of the in-process prototype test.
- Task 5 aggregate scan result: output of `grep -rn "from dashboard_legacy\|import dashboard_legacy" dashboard_renderer/` (must be empty; if not, list each rewrite made).
</output>
