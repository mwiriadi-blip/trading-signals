---
phase: 38-news-integration
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - web/routes/news.py
  - web/app.py
  - dashboard_renderer/components/news.py
  - dashboard_renderer/components/signals.py
  - dashboard_renderer/context.py
  - dashboard_renderer/api.py
  - dashboard_renderer/assets.py
  - web/routes/dashboard/__init__.py
  - tests/test_news_routes.py
  - tests/test_news_dismiss.py
  - tests/test_news_dashboard_integration.py
autonomous: true
requirements: [NEWS-01, NEWS-04]
must_haves:
  truths:
    - "POST /news/{market}/dismiss/{title_hash} writes title_hash into state['users'][uid]['news_dismissed'] via mutate_user_state and returns empty 200 HTML"
    - "POST /news/panel-toggle flips state['users'][uid]['news_panel_collapsed'] via mutate_user_state"
    - "User A's dismiss does not affect user B's view; admin dismiss does not affect F&F user views"
    - "Dismiss state auto-expires on date change (next-day fetch surfaces previously-dismissed headlines)"
    - "<script>alert(1)</script> headline renders as escaped text (&lt;script&gt;…&lt;/script&gt;) in the news panel HTML"
    - "Every outbound headline anchor carries rel='noopener noreferrer'"
    - "News panel is a <details class='news-panel-disclosure'> with summary 'Market News' and renders at bottom of per-market section; defaults to open when news_panel_collapsed is False"
    - "Critical-event banner with locked copy 'Possible market-moving news — operator review recommended' renders above headlines when has_critical_event returns True"
    - "GET /markets/{market}/signals returns HTML containing the news panel for that market at the bottom of the per-market section"
  artifacts:
    - path: "web/routes/news.py"
      provides: "register(app) — POST /news/{market}/dismiss/{title_hash} + POST /news/panel-toggle"
      exports: ["register"]
    - path: "dashboard_renderer/components/news.py"
      provides: "render_news_panel(market_id, headlines, has_critical, dismissed_hashes, collapsed) + _render_news_banner + _render_headline_row"
    - path: "dashboard_renderer/components/signals.py"
      provides: "render_signal_cards now appends render_news_panel per market"
      contains: "render_news_panel"
    - path: "dashboard_renderer/context.py"
      provides: "RenderContext.uid + RenderContext.news_dismissed_hashes + RenderContext.news_panel_collapsed"
      contains: "uid"
    - path: "dashboard_renderer/assets.py"
      provides: "Phase 38 .news-* CSS rules"
      contains: ".news-panel-disclosure"
    - path: "web/app.py"
      provides: "news_route.register(application) wiring"
      contains: "news_route.register"
  key_links:
    - from: "web/routes/news.py"
      to: "state_manager.mutate_user_state"
      via: "local import inside route handlers"
      pattern: "from state_manager import mutate_user_state"
    - from: "dashboard_renderer/components/signals.py"
      to: "dashboard_renderer/components/news.render_news_panel"
      via: "per-market loop appends news panel after trace panel"
      pattern: "render_news_panel\\("
    - from: "web/routes/dashboard/__init__.py"
      to: "RenderContext (with uid + news_dismissed_hashes + news_panel_collapsed)"
      via: "_serve_market_scoped_page passes uid + per-user state through render_dashboard_as_str"
      pattern: "uid="
    - from: "headline row HTMX dismiss button"
      to: "POST /news/{market}/dismiss/{title_hash}"
      via: "hx-post + hx-target #news-row-{hash} + hx-swap=outerHTML"
      pattern: "hx-post=\"/news/"
---

<objective>
Land the user-facing surface for Phase 38: the FastAPI dismiss + panel-toggle routes (`web/routes/news.py`), the dashboard renderer component (`dashboard_renderer/components/news.py`) that assembles the `<details>` panel + optional critical-event banner + headline rows with HTMX dismiss buttons, the supporting CSS in `dashboard_renderer/assets.py`, the wiring in `web/app.py`, AND the live injection of the news panel into the per-market render path so each market dashboard actually shows the top 5 headlines (the Phase 38 ROADMAP goal). Closes NEWS-01 (panel render with autoescape + rel="noopener noreferrer" + visible at /markets/{m}/signals) and NEWS-04 (per-user dismiss state isolation via mutate_user_state, auto-expire on date change per D-08).

Purpose: Deliver the visible feature operators see and the per-user privacy boundary the TestTenantIsolation contract requires.
Output: 1 new route module, 1 new component module, RenderContext + signals.py + api.py + dashboard route changes to thread uid + per-user news state, CSS additions, app wiring, and three integration test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/38-news-integration/38-CONTEXT.md
@.planning/phases/38-news-integration/38-RESEARCH.md
@.planning/phases/38-news-integration/38-PATTERNS.md
@.planning/phases/38-news-integration/38-UI-SPEC.md
@tests/conftest.py
@news_fetcher.py
@news_filter.py
@web/dependencies.py
@web/routes/healthz.py
@web/routes/paper_trades/__init__.py
@web/routes/admin/__init__.py
@web/routes/dashboard/__init__.py
@dashboard_renderer/components/trace.py
@dashboard_renderer/components/signals.py
@dashboard_renderer/context.py
@dashboard_renderer/api.py
@dashboard_renderer/shell.py
@dashboard_renderer/assets.py
@state_manager/__init__.py
@web/app.py

<interfaces>
From news_fetcher.py (Plan 03):
  - fetch_news(market_id: str, symbol: str, max_items: int = 5) -> list[NewsItem]
  - NewsItem TypedDict with title, url, publisher, pub_date, title_hash

From news_filter.py (Plan 02):
  - has_critical_event(headlines: list[dict], market_id: str) -> bool

From state_manager (existing):
  - mutate_user_state(uid: str, mutator: Callable[[dict], None], path: Path = Path(STATE_FILE)) -> dict
    Holds per-user fcntl.flock across the read-modify-write. NEVER call save_state() inside the mutator.

From web/routes/admin/__init__.py (lines 192–205):
  - HTMLResponse('', status_code=200) is the empty-200 row-removal idiom for HTMX
  - HX dismiss row pattern: <tr id="news-row-{hash}" + button with hx-post + hx-target + hx-swap=outerHTML

From web/dependencies.py (Phase 35):
  - current_user_id(request: Request) -> str. Plain callable (no yield, no generator); compatible with FastAPI Depends(). Raises HTTPException(403) if unauthenticated/disabled. Verified pattern in web/routes/paper_trades/__init__.py uses `user_id: str = Depends(current_user_id)`.

From web/routes/healthz.py:
  - register(app: FastAPI) pattern; local import of dependencies inside handler bodies (C-2)

From dashboard_renderer/components/trace.py (lines 225–261):
  - <details class="trace-disclosure" {open_attr}> pattern with `' open'` or `''` substitution
  - open_attr sourced from per-user cookie/state

From dashboard_renderer/components/signals.py (lines 143–259):
  - render_signal_cards(state, *, active_market=None) is the per-market render loop
  - Loop body lines 166–256: for state_key, display in display_names.items() emits <article class="card">…</article> + _render_trace_panels(...) per market
  - Injection point: after the per-market trace panels (line 256), before the next loop iteration

From dashboard_renderer/context.py:
  - RenderContext dataclass(slots=True) with state, now, strategy_version, trace_open_keys, active_function, active_market
  - RenderContext.build(...) classmethod accepts keyword args; adding uid + news_dismissed_hashes + news_panel_collapsed requires updating both the dataclass and build()

From dashboard_renderer/api.py:
  - _build_render_context(*, state, now, trace_open_keys, active_function, active_market) — internal helper
  - render_dashboard_as_str(state, now, active_function, active_market) — public; called by web/routes/dashboard/__init__.py::_serve_market_scoped_page on browser navigation
  - render_panel_html(state, *, active_function, active_market, ...) — public; called on HX-Request

From web/routes/dashboard/__init__.py:
  - _serve_market_scoped_page(request, market_id, function) currently does NOT pass uid into render_dashboard_as_str. Must thread Depends(current_user_id) into the GET /markets/{market_id}/{function} routes and forward uid + per-user dismissed hashes + collapsed bool through render_dashboard_as_str / render_panel_html.

From tests/conftest.py:
  - VALID_SECRET = 'a' * 32 (single source of truth)
  - VALID_USERNAME = 'marc'
  - VALID_RECOVERY_EMAIL = 'mwiriadi@gmail.com'
  - AUTH_HEADER_NAME = 'X-Trading-Signals-Auth'
  - _set_web_auth_credentials_for_web_tests autouse fixture activates for files matching test_web_* OR test_auth_store_*. NEW test files in this plan MUST be named with prefix `test_web_news_*.py` (or import the env-setup explicitly) so create_app() does not raise RuntimeError on missing WEB_AUTH_SECRET.
  - NO app_instance fixture exposed at conftest level — tests/test_web_healthz.py defines one locally. New test files must either duplicate the minimal pattern or import from a sibling test module.

From dashboard_renderer/assets.py:
  - Existing CSS tokens --space-1..--space-12, --fs-body, --fs-label, --color-bg/-surface/-long/-short/-border/-text*/-touch-target-min
  - Phase 17 trace CSS = closest pattern source for news panel CSS
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests in tests/test_web_news_routes.py and tests/test_web_news_dismiss.py</name>
  <files>tests/test_web_news_routes.py, tests/test_web_news_dismiss.py</files>
  <read_first>
    - tests/conftest.py (VALID_SECRET, VALID_USERNAME, autouse fixture matches `test_web_*` — that is why these files use the `test_web_news_*` prefix)
    - tests/test_web_healthz.py (local app_instance fixture pattern + TestClient construction; copy the minimal subset needed)
    - .planning/phases/38-news-integration/38-VALIDATION.md (manual-only verifications + automated map)
    - .planning/phases/38-news-integration/38-UI-SPEC.md §Interaction Contract + §Headline Row (HTMX dismiss)
    - web/dependencies.py::current_user_id (used in Depends(); override via app.dependency_overrides for uid-isolation tests)
    - state_manager/__init__.py::mutate_user_state signature
    - web/routes/admin/__init__.py (revoke-invite empty-200 dismiss test pattern, if present)
    - web/app.py (create_app + how routes are registered for TestClient use)
    - web/routes/paper_trades/__init__.py lines 71–90 (canonical Depends(current_user_id) pattern this plan mirrors)
  </read_first>
  <behavior>
    tests/test_web_news_routes.py (route-level integration):
    - test_register_attaches_dismiss_route: after web.routes.news.register(app), app.routes contains POST /news/{market}/dismiss/{title_hash}
    - test_register_attaches_panel_toggle_route: app.routes contains POST /news/panel-toggle
    - test_dismiss_route_requires_auth: unauthenticated POST returns 403 (per current_user_id raising HTTPException 403); authenticated POST returns 200 with empty body and content-type text/html. Use app.dependency_overrides[current_user_id] for authenticated case.
    - test_dismiss_route_rejects_unknown_market: POST /news/UNKNOWN/dismiss/abc1234567890def returns 404
    - test_dismiss_route_rejects_invalid_hash: POST /news/SPI200/dismiss/not-a-valid-hash returns 422 (16-char hex required)
    - test_panel_toggle_returns_empty_200: authenticated POST /news/panel-toggle returns 200, empty body
    - test_xss_headline_renders_escaped: render the news panel via dashboard_renderer.components.news.render_news_panel with a headline whose title is `<script>alert(1)</script>`; assert the literal substring `<script>alert(1)</script>` is NOT present in the rendered HTML and `&lt;script&gt;alert(1)&lt;/script&gt;` IS present
    - test_headline_anchor_carries_rel_noopener: render with a headline carrying url `https://example.com/x`; rendered HTML contains `rel="noopener noreferrer"` on the anchor
    - test_news_panel_open_by_default: render with collapsed=False; resulting HTML contains `<details class="news-panel-disclosure" open>` (or equivalent ' open' attribute)
    - test_news_panel_collapsed_when_pref_true: render with collapsed=True; resulting HTML contains `<details class="news-panel-disclosure">` WITHOUT ` open`
    - test_banner_renders_with_locked_copy: render with has_critical=True; HTML contains EXACTLY `Possible market-moving news — operator review recommended`
    - test_banner_absent_when_no_critical_event: render with has_critical=False; literal banner copy is NOT in the HTML
    - test_dismissed_hash_filters_out_row: render with one headline title_hash='abc1234567890def' and dismissed_hashes={'abc1234567890def'}; that row is absent from HTML
    - test_summary_label_is_market_news: rendered HTML contains `<summary class="news-panel-summary">Market News</summary>`
    - test_dismiss_button_label_is_dismiss_headline: HTML for a non-empty headlines list contains the literal text `Dismiss Headline`

    tests/test_web_news_dismiss.py (state-write contract):
    - test_dismiss_writes_user_news_dismissed: via the route (TestClient + dependency_overrides to inject uid) or by directly calling the mutator helper, dismiss title_hash 'abc1234567890def' for uid 'user_a'; after the call, state['users']['user_a']['news_dismissed'] == {'date': today_iso, 'hashes': ['abc1234567890def']}
    - test_dismiss_appends_distinct_hashes_same_day: two dismisses of different hashes same day → both present in list
    - test_dismiss_dedupes_same_hash_same_day: dismissing the same hash twice keeps the list at length 1
    - test_dismiss_resets_on_date_change: pre-seed state['users']['user_a']['news_dismissed'] = {'date': '2020-01-01', 'hashes': ['old']}; dismiss a new hash today; resulting dict has today's date and only the new hash (old hash auto-expired per D-08)
    - test_dismiss_isolation_user_a_vs_user_b: dismiss hash X for user_a; dismiss hash Y for user_b; assert state['users']['user_a']['news_dismissed']['hashes'] == ['X'] and state['users']['user_b']['news_dismissed']['hashes'] == ['Y'] (no cross-contamination)
    - test_panel_toggle_flips_news_panel_collapsed: initial state['users']['user_a']['news_panel_collapsed'] absent or False; POST /news/panel-toggle as user_a → True; POST again → False
    - test_dismiss_does_not_corrupt_other_user_state: pre-seed state with user_a paper_trades and user_b paper_trades; dismiss a headline as user_a; assert user_b's paper_trades are byte-identical to pre-seed

    Use the file-name prefix `test_web_news_*.py` so the conftest autouse fixture sets WEB_AUTH_SECRET/WEB_AUTH_USERNAME. Use FastAPI's `app.dependency_overrides[current_user_id] = lambda: 'user_a'` to inject the uid for isolation tests (do not monkey-patch the cookie middleware). For state isolation tests, point STATE_FILE / mutate_user_state to a tmp_path-scoped state.json so tests do not touch the real one.
  </behavior>
  <action>
    Create both test files under tests/test_web_news_*. 2-space indent. Import VALID_SECRET/VALID_USERNAME from tests/conftest.py if needed. Build a local app_instance fixture in each file (or factor into a shared helper) — keep it minimal: `create_app()` + return the FastAPI instance.

    Run pytest tests/test_web_news_routes.py tests/test_web_news_dismiss.py → expect RED (ModuleNotFoundError for web.routes.news + dashboard_renderer.components.news).
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py 2>&1 | grep -q -E "(ModuleNotFoundError|ImportError|web\.routes\.news|dashboard_renderer\.components\.news)" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_web_news_routes.py && test -f tests/test_web_news_dismiss.py` exits 0
    - `grep -c 'def test_dismiss_isolation_user_a_vs_user_b' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_dismiss_resets_on_date_change' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_panel_toggle_flips_news_panel_collapsed' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_xss_headline_renders_escaped' tests/test_web_news_routes.py` returns 1
    - `grep -c 'def test_headline_anchor_carries_rel_noopener' tests/test_web_news_routes.py` returns 1
    - `grep -c 'def test_banner_renders_with_locked_copy' tests/test_web_news_routes.py` returns 1
    - `grep -c 'def test_news_panel_open_by_default' tests/test_web_news_routes.py` returns 1
    - `grep -c 'Possible market-moving news — operator review recommended' tests/test_web_news_routes.py` returns ≥1 (locked-copy assertion)
    - `grep -c 'Dismiss Headline' tests/test_web_news_routes.py` returns ≥1
    - `grep -c 'rel=\"noopener noreferrer\"' tests/test_web_news_routes.py` returns ≥1
    - `grep -c 'app.dependency_overrides' tests/test_web_news_dismiss.py` returns ≥1 (Depends override pattern in use)
    - Pytest collection on both files is RED.
  </acceptance_criteria>
  <done>Both test files exist with all named tests; pytest RED because production modules absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement web/routes/news.py + dashboard_renderer/components/news.py + assets.py CSS + web/app.py wiring</name>
  <files>web/routes/news.py, dashboard_renderer/components/news.py, dashboard_renderer/assets.py, web/app.py</files>
  <read_first>
    - tests/test_web_news_routes.py + tests/test_web_news_dismiss.py (the contract)
    - web/routes/healthz.py (register pattern, local imports inside handlers)
    - web/routes/paper_trades/__init__.py lines 71–90 (canonical Depends(current_user_id) signature)
    - web/routes/admin/__init__.py lines 192–205 (revoke-invite empty-200 dismiss)
    - dashboard_renderer/components/trace.py lines 225–261 (collapsible <details> pattern)
    - dashboard_renderer/assets.py (locate :root tokens + Phase 17 trace CSS for adjacent placement)
    - web/app.py (existing register-pattern block: healthz_route.register(application) etc.)
    - state_manager/__init__.py::mutate_user_state (signature + flock-deadlock warning)
    - .planning/phases/38-news-integration/38-UI-SPEC.md (CSS rules, locked copy, accessibility contract)
  </read_first>
  <action>
    Four files, one cohesive feature slice. CRITICAL: handler signatures use `Depends(current_user_id)` (mirroring web/routes/paper_trades/__init__.py:71), NOT direct `current_user_id(request)` calls.

    (A) web/routes/news.py — `register(app: FastAPI) -> None` that registers two routes:

      Module-level imports: `import re`, `from fastapi import FastAPI, Depends, Request, Response, HTTPException`, `from web.dependencies import current_user_id` (Depends targets must be importable at module level — local-import discipline does NOT apply to Depends() arguments).

      Route 1: `@app.post('/news/{market}/dismiss/{title_hash}')`
        Handler signature: `def dismiss_headline(market: str, title_hash: str, uid: str = Depends(current_user_id)) -> Response`
        1. Validate market: `if not _is_known_market(market): raise HTTPException(status_code=404, detail='unknown market')`. Use existing `is_known_market` helper from web/routes/markets.py or system_params.MARKETS_BY_ID — pick the one already in use across the codebase; do NOT hand-roll a new market registry.
        2. Validate title_hash: must be 16-char lowercase hex string. Module-level helper `_is_valid_hash(s): return bool(re.fullmatch(r'[0-9a-f]{16}', s))`. Raise HTTPException 422 if invalid.
        3. Local imports inside handler body (C-2 discipline for state writes): `from state_manager import mutate_user_state`, `from datetime import date`.
        4. Define mutator `_apply(state)`: navigate users.setdefault(uid, {}); read today = date.today().isoformat(); nd = user.get('news_dismissed', {}); if nd.get('date') != today: reset nd = {'date': today, 'hashes': []}; if title_hash not in nd['hashes']: append; user['news_dismissed'] = nd.
        5. Call `mutate_user_state(uid, _apply)`.
        6. Return `Response(content='', media_type='text/html')` (status 200 default).

      Route 2: `@app.post('/news/panel-toggle')`
        Handler signature: `def news_panel_toggle(uid: str = Depends(current_user_id)) -> Response`
        1. Local import: `from state_manager import mutate_user_state`.
        2. mutator flips `user['news_panel_collapsed'] = not user.get('news_panel_collapsed', False)`.
        3. mutate_user_state(uid, mutator).
        4. Return Response(content='', media_type='text/html').

    (B) dashboard_renderer/components/news.py — assemble the panel HTML:

      Public function:
      `def render_news_panel(market_id: str, headlines: list[dict], has_critical: bool, dismissed_hashes: set[str], collapsed: bool) -> str`

      Internals:
      - Filter headlines: keep only items whose title_hash is NOT in dismissed_hashes.
      - open_attr = '' if collapsed else ' open'
      - mkt_esc = html.escape(market_id, quote=True)
      - banner_html = _render_news_banner() if has_critical else ''
      - rows_html = ''.join(_render_headline_row(mkt_esc, h) for h in filtered_headlines) or _render_empty_state()
      - Wrap in `<details class="news-panel-disclosure"{open_attr}>` + `<summary class="news-panel-summary">Market News</summary>` + inner div + table

      `_render_news_banner()` returns EXACT string:
      ```
      <div class="news-banner"><p class="news-banner-heading">Possible market-moving news — operator review recommended</p></div>
      ```
      Use the em-dash character (—, U+2014) per ROADMAP/UI-SPEC.

      `_render_headline_row(mkt_esc, item)`:
      - title_hash_esc = html.escape(item['title_hash'], quote=True)
      - title_esc = html.escape(item.get('title', ''))   # autoescape equivalent — explicit because we are emitting raw HTML strings, not via Jinja2
      - url_esc = html.escape(item.get('url', ''), quote=True)
      - publisher_esc = html.escape(item.get('publisher', ''))
      - pub_date_esc = html.escape(item.get('pub_date', ''))
      - Emit `<tr id="news-row-{title_hash_esc}">` then `<td class="news-link"><a href="{url_esc}" target="_blank" rel="noopener noreferrer">{title_esc}</a></td>` then `<td class="news-meta">{publisher_esc} · {pub_date_esc}</td>` then dismiss-button `<td><button type="button" class="btn-news-dismiss" hx-post="/news/{mkt_esc}/dismiss/{title_hash_esc}" hx-target="#news-row-{title_hash_esc}" hx-swap="outerHTML" hx-trigger="click">Dismiss Headline</button></td>` then `</tr>`.

      `_render_empty_state()` returns `<p class="subtle">No headlines today.</p>`.

      Module imports: html, plus typing if needed. NO state_manager, no news_fetcher (renderer is pure transform — the caller fetches headlines and passes them in).

    (C) dashboard_renderer/assets.py — append CSS rules from UI-SPEC §CSS additions verbatim:
      - .news-panel-disclosure, .news-panel-summary, .news-panel-inner
      - .news-banner, .news-banner-heading
      - .news-table (th, td, tr:last-child)
      - .news-link a + :hover
      - .news-meta
      - .btn-news-dismiss + :hover
      Use existing --space-* / --fs-* / --color-* tokens; no new tokens. Match the trace-panel CSS placement (group adjacent to .trace-disclosure block for grep-ability).

    (D) web/app.py — wire the routes:
      - Top-of-file imports: `from web.routes import news as news_route` next to the existing route imports.
      - Inside create_app() alongside `healthz_route.register(application)` etc., add `news_route.register(application)`.

    Run pytest. If GREEN, commit.

    Note: this task ships the consumable surface only. Task 3 wires render_news_panel into the live per-market dashboard render path so the Phase 38 goal ("each market dashboard shows top 5 headlines") is delivered.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py tests/test_signal_engine.py::TestDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - `test -f web/routes/news.py && test -f dashboard_renderer/components/news.py` exits 0
    - `grep -c 'def register(app' web/routes/news.py` returns 1
    - `grep -c "/news/{market}/dismiss/{title_hash}" web/routes/news.py` returns ≥1
    - `grep -c "/news/panel-toggle" web/routes/news.py` returns ≥1
    - `grep -c 'uid: str = Depends(current_user_id)' web/routes/news.py` returns ≥2 (dismiss + panel-toggle handlers both use Depends pattern)
    - `grep -c 'from web.dependencies import current_user_id' web/routes/news.py` returns 1 (module-level — required for Depends())
    - `grep -c "from state_manager import mutate_user_state" web/routes/news.py` returns ≥1 (local import inside handlers)
    - `grep -c "media_type='text/html'" web/routes/news.py` returns ≥2 (both dismiss and panel-toggle)
    - `grep -c "def render_news_panel" dashboard_renderer/components/news.py` returns 1
    - `grep -c 'rel="noopener noreferrer"' dashboard_renderer/components/news.py` returns ≥1
    - `grep -c 'Possible market-moving news — operator review recommended' dashboard_renderer/components/news.py` returns 1 (locked copy)
    - `grep -c 'Dismiss Headline' dashboard_renderer/components/news.py` returns ≥1
    - `grep -c 'Market News' dashboard_renderer/components/news.py` returns ≥1
    - `grep -c "html.escape" dashboard_renderer/components/news.py` returns ≥4 (title, url, publisher, pub_date / title_hash / market_id — all user-facing string fields escaped)
    - `grep -c '\.news-panel-disclosure' dashboard_renderer/assets.py` returns ≥1
    - `grep -c '\.news-banner' dashboard_renderer/assets.py` returns ≥1
    - `grep -c '\.btn-news-dismiss' dashboard_renderer/assets.py` returns ≥1
    - `grep -c 'news_route.register' web/app.py` returns 1
    - `grep -c 'from web.routes import news' web/app.py` returns 1
    - `.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py` exits 0 (all named tests green)
    - `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism` exits 0 (no hex regressions)
    - `wc -l web/routes/news.py dashboard_renderer/components/news.py` each ≤500
  </acceptance_criteria>
  <done>web/routes/news.py registers dismiss + panel-toggle (empty 200 HTML, mutate_user_state, Depends(current_user_id) signatures); dashboard_renderer/components/news.py renders the panel with locked banner copy, HTMX dismiss buttons, rel="noopener noreferrer" on anchors, explicit html.escape on all user fields; assets.py carries Phase 38 CSS; web/app.py wires the route. All Task 1 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire render_news_panel into live per-market dashboard render path + integration test</name>
  <files>dashboard_renderer/context.py, dashboard_renderer/api.py, dashboard_renderer/components/signals.py, web/routes/dashboard/__init__.py, tests/test_web_news_dashboard_integration.py</files>
  <read_first>
    - dashboard_renderer/context.py (RenderContext dataclass + .build() classmethod)
    - dashboard_renderer/api.py lines 26–204 (_build_render_context + render_dashboard_as_str + render_panel_html signatures and call sites)
    - dashboard_renderer/components/signals.py lines 143–259 (render_signal_cards loop; injection point is line 256 inside the for-loop, after _render_trace_panels(...))
    - dashboard_renderer/shell.py lines 264–295 (signals page_map; _render_signal_cards is the per-market emitter)
    - web/routes/dashboard/__init__.py lines 266–330 (_serve_market_scoped_page: GET /markets/{market_id}/{function}; currently no uid threading)
    - web/routes/dashboard/__init__.py lines 184–248 (_serve_account_page_scoped: canonical Depends(current_user_id) + per-user scoping pattern this task mirrors)
    - dashboard_renderer/components/news.py (Task 2 output — render_news_panel signature)
    - news_fetcher.py (Plan 03 — fetch_news signature)
    - news_filter.py (Plan 02 — has_critical_event signature)
    - state_manager/__init__.py (load_state — to read per-user dismissed/collapsed)
    - tests/conftest.py + tests/test_web_news_routes.py (TestClient + dependency_overrides patterns from Task 1)
  </read_first>
  <behavior>
    tests/test_web_news_dashboard_integration.py:
    - test_market_signals_page_contains_news_panel_spi200: GET /markets/SPI200/signals as authenticated user; response status 200; body contains `<details class="news-panel-disclosure"` AND `<summary class="news-panel-summary">Market News</summary>`. Use dependency_overrides + monkeypatch fetch_news to return a fixed list of 2 NewsItem dicts so the test does not hit yfinance.
    - test_market_signals_page_contains_news_panel_audusd: same as above but for AUDUSD market.
    - test_news_panel_appears_after_signal_card: response body contains both `class="card"` and `class="news-panel-disclosure"`, AND the news-panel-disclosure substring index is GREATER than the last card substring index for that market (panel is at the BOTTOM of the per-market section).
    - test_dismissed_hash_hidden_from_dashboard: pre-seed state['users'][uid]['news_dismissed'] = {'date': today_iso, 'hashes': ['<hash_of_first_headline>']}; GET /markets/SPI200/signals; that headline row is NOT in body, the second headline IS.
    - test_collapsed_pref_renders_without_open_attr: pre-seed state['users'][uid]['news_panel_collapsed'] = True; GET /markets/SPI200/signals; body contains `<details class="news-panel-disclosure">` WITHOUT ` open`.
    - test_news_panel_uses_authenticated_uid: dependency_overrides[current_user_id] = lambda: 'user_a'; pre-seed user_a dismissed-hash X and user_b dismissed-hash Y; GET as user_a; X is filtered out but Y headline is still visible (proves the render path keys off the request's uid, not a global).

    Use monkeypatch.setattr('news_fetcher.fetch_news', lambda *a, **kw: [_fake_item_1, _fake_item_2]) and similarly stub news_filter.has_critical_event to True for at least one of the assertions.
  </behavior>
  <action>
    Five files, one slice: thread uid + per-user news state through RenderContext, inject render_news_panel into the per-market render path, and add a route-level integration test.

    (A) dashboard_renderer/context.py — extend RenderContext:
      Add three optional fields with safe defaults so existing callers (which don't pass them) continue to work:
        uid: str | None = None
        news_dismissed_hashes: frozenset[str] = frozenset()
        news_panel_collapsed: bool = False
      Mirror them in RenderContext.build() as keyword-only parameters with the same defaults. Update the dataclass slots accordingly (dataclass(slots=True) requires no slot list edit — slots are auto-derived).

    (B) dashboard_renderer/api.py — thread the new fields through _build_render_context and the public APIs:
      - _build_render_context: add `uid: str | None = None`, `news_dismissed_hashes: frozenset[str] = frozenset()`, `news_panel_collapsed: bool = False` keyword-only params; forward to RenderContext.build().
      - render_dashboard_as_str: add the same three keyword-only params; forward to _build_render_context.
      - render_panel_html: add the same three keyword-only params; forward to _build_render_context.
      - render_dashboard_files + render_dashboard_page: leave defaults (these are the disk-write entrypoints; they render the shared/sibling cache that is NOT per-user — uid stays None there, news panel does not render in those outputs, matching the existing per-user privacy boundary of Phase 36/37).

    (C) dashboard_renderer/components/signals.py — inject the news panel inside the per-market loop:
      Inside render_signal_cards(state, *, active_market=None), the existing loop iterates display_names. Change signature to also accept the per-user news fields (keyword-only, defaulted):
        `def render_signal_cards(state, *, active_market=None, uid=None, news_dismissed_hashes=frozenset(), news_panel_collapsed=False) -> str:`
      After `parts.append(_render_trace_panels(...))` (line 256) and BEFORE the `# Phase 27 WR-08` block at the very end of the loop body, append:
        - LOCAL imports (C-2 — keeps signals.py hex-eligible at import time):
          `from news_fetcher import fetch_news`
          `from news_filter import has_critical_event`
          `from dashboard_renderer.components.news import render_news_panel`
        - Resolve symbol for state_key from state['markets'][state_key] (or whichever existing helper maps market_id → yfinance symbol; if no helper exists yet, pass state_key directly to fetch_news and let news_fetcher handle the mapping — verify against Plan 03's fetch_news signature).
        - headlines = fetch_news(state_key, symbol)
        - has_critical = has_critical_event(headlines, state_key)
        - news_html = render_news_panel(state_key, headlines, has_critical, news_dismissed_hashes, news_panel_collapsed)
        - parts.append(news_html)
      Wrap the fetch_news call in a try/except (broad-catch then log via existing dashboard-renderer logger pattern, fall back to empty list) so a yfinance outage cannot 500 the dashboard. This matches the never-crash discipline in web/routes/dashboard/__init__.py:D-10.
      Update shell.py's call site (line 289) to pass uid + news_dismissed_hashes + news_panel_collapsed from ctx: `_render_signal_cards(state, active_market=active_market, uid=ctx.uid, news_dismissed_hashes=ctx.news_dismissed_hashes, news_panel_collapsed=ctx.news_panel_collapsed)`.

    (D) web/routes/dashboard/__init__.py — thread uid through _serve_market_scoped_page:
      - Change signature: `async def _serve_market_scoped_page(request, market_id, function, uid)`.
      - Update the GET /markets/{market_id}/{function} route registrations to take `uid: str = Depends(_get_current_user_id)` and forward to _serve_market_scoped_page (mirror the existing _serve_account_page_scoped pattern at lines 234–248).
      - Inside _serve_market_scoped_page, after loading state, derive per-user news fields:
        ```
        user_bucket = state.get('users', {}).get(uid, {}) or {}
        nd = user_bucket.get('news_dismissed', {})
        today_iso = date.today().isoformat()
        dismissed_hashes = frozenset(nd.get('hashes', [])) if nd.get('date') == today_iso else frozenset()
        collapsed = bool(user_bucket.get('news_panel_collapsed', False))
        ```
      - Forward to render_dashboard_as_str / render_panel_html via the new kwargs: `uid=uid, news_dismissed_hashes=dismissed_hashes, news_panel_collapsed=collapsed`.

    (E) tests/test_web_news_dashboard_integration.py — write the integration tests defined in <behavior>. Use file-name prefix `test_web_*` so conftest autouse fixture supplies WEB_AUTH_SECRET.

    Run pytest. If GREEN, commit.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_news_dashboard_integration.py tests/test_web_news_routes.py tests/test_web_news_dismiss.py tests/test_signal_engine.py::TestDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_web_news_dashboard_integration.py` exits 0
    - `grep -c 'def test_market_signals_page_contains_news_panel_spi200' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_market_signals_page_contains_news_panel_audusd' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_news_panel_appears_after_signal_card' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_dismissed_hash_hidden_from_dashboard' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c '/markets/SPI200/signals' tests/test_web_news_dashboard_integration.py` returns ≥1
    - `grep -c 'uid:' dashboard_renderer/context.py` returns ≥1
    - `grep -c 'news_dismissed_hashes' dashboard_renderer/context.py` returns ≥2 (field + build() param)
    - `grep -c 'news_panel_collapsed' dashboard_renderer/context.py` returns ≥2
    - `grep -c 'render_news_panel' dashboard_renderer/components/signals.py` returns ≥1
    - `grep -c 'fetch_news' dashboard_renderer/components/signals.py` returns ≥1
    - `grep -c 'has_critical_event' dashboard_renderer/components/signals.py` returns ≥1
    - `grep -c 'news_dismissed_hashes' dashboard_renderer/api.py` returns ≥3 (in three signatures)
    - `grep -c 'news_dismissed_hashes' web/routes/dashboard/__init__.py` returns ≥1
    - `grep -c 'uid: str = Depends(_get_current_user_id)' web/routes/dashboard/__init__.py` returns ≥3 (existing 2 for /account + new ones for /markets/{m}/{fn})
    - `.venv/bin/pytest -x --tb=short tests/test_web_news_dashboard_integration.py` exits 0 (all 6 named tests green)
    - `.venv/bin/pytest -x --tb=short` full-suite exits 0 (no regressions in existing dashboard/render/route tests; existing render_signal_cards callers continue to work via defaulted kwargs)
  </acceptance_criteria>
  <done>RenderContext carries per-user news state; signals.py emits render_news_panel at the bottom of each per-market section; /markets/{m}/signals threads Depends(current_user_id) and per-user dismissed/collapsed into render_dashboard_as_str + render_panel_html; integration tests prove the news panel is visible on the live dashboard for both markets, dismissed headlines are filtered out, and collapse preference is honoured. Phase 38 goal "each market dashboard shows top 5 headlines" delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dismiss/panel-toggle route | authenticated HTMX request; uid resolved server-side via Depends(current_user_id) |
| route → state.json | mutate_user_state wraps mutate_state with per-user fcntl.flock |
| headline content → rendered HTML | XSS surface — must explicit-escape because component emits raw strings (not Jinja2 template) |
| outbound headline link → browser | target="_blank" reverse-tab-nabbing surface |
| browser → /markets/{m}/signals | authenticated GET; per-user dismissed/collapsed read from state['users'][uid] |
| yfinance network → render path | network failure must not 500 the dashboard (try/except + empty fallback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Tampering | XSS via headline title rendering | mitigate | html.escape() on title/url/publisher/pub_date/title_hash before string concatenation; test_xss_headline_renders_escaped asserts `<script>` → `&lt;script&gt;` (per D-09 / NEWS-03) |
| T-38-04-02 | Tampering | Reverse tab-nabbing via target="_blank" | mitigate | rel="noopener noreferrer" mandatory on every headline anchor; test_headline_anchor_carries_rel_noopener asserts |
| T-38-04-03 | Information Disclosure | Cross-user dismiss leakage | mitigate | mutate_user_state writes exclusively to state['users'][uid]; render path keys dismissed_hashes off Depends(current_user_id) uid (Task 3); test_news_panel_uses_authenticated_uid asserts |
| T-38-04-04 | Tampering | Path traversal via market path param | mitigate | _is_known_market(market) check at route entry → 404 on unknown market |
| T-38-04-05 | Tampering | title_hash path injection | mitigate | re.fullmatch(r'[0-9a-f]{16}', title_hash) → 422 on malformed |
| T-38-04-06 | Spoofing | Unauthenticated dismiss / dashboard view | mitigate | All handlers use `uid: str = Depends(current_user_id)`; FastAPI raises 403 before handler body executes |
| T-38-04-07 | DoS | Unbounded news_dismissed list growth | accept | Dismiss list auto-expires daily (D-08 reset on date change); maximum size ≤ count of fetched headlines that day (≤5 per market) × markets — bounded |
| T-38-04-08 | Tampering | Cookie tampering to forge uid | mitigate | uid is read from authenticated session cookie via existing Phase 35 Depends(current_user_id); no client-supplied uid parameter |
| T-38-04-09 | DoS | yfinance outage 500s the dashboard | mitigate | fetch_news call in signals.py wrapped in try/except → empty headline list fallback; dashboard renders without news panel rather than erroring |
</threat_model>

<verification>
1. `.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py tests/test_web_news_dashboard_integration.py` exits 0.
2. `.venv/bin/pytest -x --tb=short` full-suite exits 0 (no regressions in existing route, tenant, or dashboard tests).
3. Manual smoke (out of scope for automated verify; deferred to operator UAT per VALIDATION.md §Manual-Only): visit /markets/SPI200/signals, observe news panel at the bottom of the per-market section, dismiss a headline, reload, panel remains dismissed.
4. `grep -E 'rel="noopener noreferrer"' dashboard_renderer/components/news.py` finds the anchor attribute.
5. Locked banner copy present byte-identical in source and test.
6. `grep -c 'render_news_panel' dashboard_renderer/components/signals.py` ≥1 — proves live injection.
</verification>

<success_criteria>
- Two routes registered: dismiss + panel-toggle; both use Depends(current_user_id); both return empty 200 text/html; both write through mutate_user_state.
- render_news_panel emits `<details class="news-panel-disclosure">` with summary 'Market News', optional banner with locked copy, headline rows with explicit html.escape on every user-facing field, rel="noopener noreferrer" on every anchor.
- CSS extends existing tokens only — no new --space-/--fs-/--color- tokens.
- Per-user dismiss isolation proved by tests (Task 1 + Task 3).
- Date-change auto-expiry proved by tests.
- web/app.py wires news_route.register(application).
- News panel renders at the bottom of each per-market section on /markets/{m}/signals (Phase 38 ROADMAP goal).
- Dashboard render path threads uid + per-user dismissed/collapsed from Depends(current_user_id) end-to-end (RenderContext → api.py → signals.py → render_news_panel).
- yfinance outage does not 500 the dashboard (try/except + empty fallback in signals.py).
</success_criteria>

<output>
After completion, create `.planning/phases/38-news-integration/38-04-SUMMARY.md` summarising: route surface, render contract, CSS classes added, html.escape audit, per-user-isolation test results, and the live-injection wiring (RenderContext.uid + signals.py per-market loop append + /markets/{m}/signals end-to-end). Confirm the Phase 38 ROADMAP goal is delivered.
</output>
</content>
</invoke>