---
phase: 38-news-integration
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - web/routes/news.py
  - web/app.py
  - dashboard_renderer/components/news.py
  - dashboard_renderer/components/signals.py
  - dashboard_renderer/context.py
  - dashboard_renderer/api.py
  - dashboard_renderer/assets.py
  - web/routes/dashboard/__init__.py
  - dashboard_renderer/shell.py
  - tests/test_web_news_routes.py
  - tests/test_web_news_dismiss.py
  - tests/test_web_news_dashboard_integration.py
autonomous: true
requirements: [NEWS-01, NEWS-04]
must_haves:
  truths:
    - "POST /news/{market}/dismiss/{title_hash} writes title_hash into state['users'][uid]['news_dismissed'][market_id] via mutate_user_state and returns empty 200 HTML"
    - "POST /news/{market}/toggle-collapse flips state['users'][uid]['news_panel_collapsed'][market_id] via mutate_user_state (POST verb is mandatory — GET must NOT mutate)"
    - "Per-user state shape is pinned: state['users'][uid]['news_dismissed'][market_id] = {'date': 'YYYY-MM-DD', 'hashes': [...]} and state['users'][uid]['news_panel_collapsed'][market_id] = bool"
    - "Dismiss mutator implements D-08 auto-expiry inside mutate_user_state atomically: if news_dismissed[market_id]['date'] != today: reset hashes=[] AND update date=today; THEN append new hash if not duplicate"
    - "Renderer filters dismissed headlines BEFORE evaluating has_critical_event (banner reflects post-dismiss state — if user dismisses the only critical headline, banner disappears)"
    - "First-visit users (no prior news state) do not crash: dismiss handler and renderer both use setdefault chains for state['users'][uid]['news_dismissed'][market_id] and ['news_panel_collapsed'][market_id]"
    - "User A's dismiss does not affect user B's view; admin dismiss does not affect F&F user views"
    - "<script>alert(1)</script> headline renders as escaped text (&lt;script&gt;…&lt;/script&gt;) — rendered via Python f-string with explicit html.escape() per field (NOT Jinja2 — this codebase uses f-string components, see dashboard_renderer/components/*.py)"
    - "Every outbound headline anchor carries rel='noopener noreferrer'"
    - "News panel is a <details class='news-panel-disclosure'> with summary 'Market News'; defaults to open when news_panel_collapsed[market_id] is False or absent"
    - "Critical-event banner with locked copy 'Possible market-moving news — operator review recommended' renders above headlines when has_critical_event returns True on the FILTERED headline list"
    - "GET /markets/{market}/signals returns HTML containing the news panel at the bottom of the per-market section (live injection via signals.py per-market loop)"
  artifacts:
    - path: "web/routes/news.py"
      provides: "register(app) — POST /news/{market}/dismiss/{title_hash} + POST /news/{market}/toggle-collapse"
      exports: ["register"]
    - path: "dashboard_renderer/components/news.py"
      provides: "render_news_panel(market_id, headlines, dismissed_hashes, collapsed) — filters BEFORE calling has_critical_event; rendered via Python f-string with explicit html.escape() on every dynamic value"
    - path: "dashboard_renderer/components/signals.py"
      provides: "render_signal_cards appends render_news_panel per market"
      contains: "render_news_panel"
    - path: "dashboard_renderer/context.py"
      provides: "RenderContext.uid + RenderContext.news_dismissed (per-market dict) + RenderContext.news_panel_collapsed (per-market dict)"
      contains: "uid"
    - path: "dashboard_renderer/assets.py"
      provides: "Phase 38 .news-* CSS rules"
      contains: ".news-panel-disclosure"
    - path: "web/app.py"
      provides: "news_route.register(application) wiring"
      contains: "news_route.register"
  key_links:
    - from: "web/routes/news.py"
      to: "state_manager.mutate_user_state"
      via: "local import inside route handlers"
      pattern: "from state_manager import mutate_user_state"
    - from: "dashboard_renderer/components/signals.py"
      to: "dashboard_renderer/components/news.render_news_panel"
      via: "per-market loop appends news panel after trace panel"
      pattern: "render_news_panel\\("
    - from: "web/routes/dashboard/__init__.py"
      to: "RenderContext (with uid + news_dismissed + news_panel_collapsed)"
      via: "_serve_market_scoped_page passes uid + per-user state through render_dashboard_as_str"
      pattern: "uid="
    - from: "headline row HTMX dismiss button"
      to: "POST /news/{market}/dismiss/{title_hash}"
      via: "hx-post + hx-target #news-row-{hash} + hx-swap=outerHTML"
      pattern: "hx-post=\"/news/"
---

<objective>
Land the user-facing surface for Phase 38: the FastAPI dismiss + collapse-toggle routes (`web/routes/news.py`, both POST per HTTP semantics — GET must not mutate), the dashboard renderer component (`dashboard_renderer/components/news.py`) implemented as a **Python f-string component with explicit `html.escape()` per dynamic value** (this codebase does NOT use Jinja2 — see `dashboard_renderer/components/devices.py`, `markets.py`, `trace.py` for the established f-string pattern; a Jinja2 template file would be dead code and `Jinja2Templates` is not configured in `web/app.py`), the supporting CSS in `dashboard_renderer/assets.py`, the wiring in `web/app.py`, AND the live injection of the news panel into the per-market render path via `dashboard_renderer/components/signals.py` so each market dashboard actually shows the top 5 headlines (Phase 38 ROADMAP goal — server-side render of the panel is the canonical pattern in this codebase; the news panel HTML is emitted inline in the per-market section, no `<div hx-get="..." hx-trigger="load">` placeholder required because dismiss/collapse already use HTMX out-of-band swaps).

Closes NEWS-01 (panel render with explicit html.escape + rel="noopener noreferrer" + visible at /markets/{m}/signals via server-side injection) and NEWS-04 (per-user dismiss state isolation via mutate_user_state, auto-expire on date change per D-08 — implemented atomically inside the mutate_user_state mutator).

Per-user state shape (pinned by cross-AI review):
```
state["users"][uid]["news_dismissed"][market_id] = {"date": "YYYY-MM-DD", "hashes": [...]}
state["users"][uid]["news_panel_collapsed"][market_id] = bool  # True = collapsed
```

Output: 1 new route module, 1 new component module, RenderContext + signals.py + api.py + dashboard route changes to thread uid + per-user news state, CSS additions, app wiring, and three integration test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/38-news-integration/38-CONTEXT.md
@.planning/phases/38-news-integration/38-RESEARCH.md
@.planning/phases/38-news-integration/38-PATTERNS.md
@.planning/phases/38-news-integration/38-UI-SPEC.md
@tests/conftest.py
@news_fetcher.py
@news_filter.py
@web/dependencies.py
@web/routes/healthz.py
@web/routes/paper_trades/__init__.py
@web/routes/admin/__init__.py
@web/routes/dashboard/__init__.py
@dashboard_renderer/components/trace.py
@dashboard_renderer/components/signals.py
@dashboard_renderer/components/devices.py
@dashboard_renderer/components/markets.py
@dashboard_renderer/context.py
@dashboard_renderer/api.py
@dashboard_renderer/shell.py
@dashboard_renderer/assets.py
@state_manager/__init__.py
@web/app.py

<interfaces>
From news_fetcher.py (Plan 03):
  - fetch_news(market_id: str, symbol: str, max_items: int = 5) -> list[NewsItem]
  - NewsItem TypedDict with title, url, publisher, pub_date, title_hash

From news_filter.py (Plan 02):
  - has_critical_event(headlines: list[dict], market_id: str) -> bool
    CRITICAL: this MUST be called on the FILTERED (post-dismiss) list, not the raw fetched list. If user dismisses the only triggering headline the banner must disappear.

From state_manager (existing):
  - mutate_user_state(uid: str, mutator: Callable[[dict], None], path: Path = Path(STATE_FILE)) -> dict
    Holds per-user fcntl.flock across the read-modify-write. NEVER call save_state() inside the mutator. ALL state mutations for dismiss + toggle MUST happen inside the mutator callback (atomicity).

From web/routes/admin/__init__.py (lines 192–205):
  - HTMLResponse('', status_code=200) is the empty-200 row-removal idiom for HTMX

From web/dependencies.py (Phase 35):
  - current_user_id(request: Request) -> str. Plain callable compatible with FastAPI Depends(). Raises HTTPException(403) if unauthenticated/disabled.

From web/routes/healthz.py:
  - register(app: FastAPI) pattern

From dashboard_renderer/components/devices.py + markets.py + trace.py:
  - **Established rendering pattern in this codebase: Python f-strings + explicit `html.escape()`**, NOT Jinja2 templates. Verify by grep: `grep -r 'Jinja2Templates' web/ dashboard_renderer/` returns nothing.
  - `<details class="trace-disclosure" {open_attr}>` pattern with `' open'` or `''` substitution (trace.py lines 225–261)

From dashboard_renderer/components/signals.py (lines 143–259):
  - render_signal_cards(state, *, active_market=None) is the per-market render loop
  - Injection point: after the per-market trace panels (line 256), before the next loop iteration

From dashboard_renderer/context.py:
  - RenderContext dataclass(slots=True). Adding uid + news_dismissed + news_panel_collapsed requires updating both the dataclass and build()

From dashboard_renderer/api.py:
  - _build_render_context, render_dashboard_as_str, render_panel_html — all need new kwargs

From web/routes/dashboard/__init__.py:
  - _serve_market_scoped_page must thread Depends(current_user_id) into the GET /markets/{market_id}/{function} routes and derive per-user news state for the renderer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests in tests/test_web_news_routes.py and tests/test_web_news_dismiss.py</name>
  <files>tests/test_web_news_routes.py, tests/test_web_news_dismiss.py</files>
  <read_first>
    - tests/conftest.py (VALID_SECRET, VALID_USERNAME, autouse fixture matches `test_web_*`)
    - tests/test_web_healthz.py (local app_instance fixture pattern + TestClient construction)
    - .planning/phases/38-news-integration/38-VALIDATION.md
    - .planning/phases/38-news-integration/38-UI-SPEC.md §Interaction Contract + §Headline Row
    - web/dependencies.py::current_user_id
    - state_manager/__init__.py::mutate_user_state
    - web/routes/admin/__init__.py (empty-200 dismiss test pattern)
    - dashboard_renderer/components/devices.py + markets.py (f-string + html.escape pattern reference)
    - web/app.py (create_app + route registration; verify NO Jinja2Templates import)
    - web/routes/paper_trades/__init__.py lines 71–90 (canonical Depends(current_user_id) pattern)
  </read_first>
  <behavior>
    tests/test_web_news_routes.py (route-level + render contract):
    - test_register_attaches_dismiss_route: app.routes contains POST /news/{market}/dismiss/{title_hash}
    - test_register_attaches_collapse_toggle_route_is_POST: app.routes contains POST /news/{market}/toggle-collapse; assert that no GET method is registered for the same path (GET must NOT mutate per HTTP semantics)
    - test_dismiss_route_requires_auth: unauthenticated POST returns 403; authenticated POST returns 200, empty body, content-type text/html
    - test_dismiss_route_rejects_unknown_market: POST /news/UNKNOWN/dismiss/abc1234567890def returns 404
    - test_dismiss_route_rejects_invalid_hash: POST /news/SPI200/dismiss/not-a-valid-hash returns 422
    - test_collapse_toggle_returns_empty_200: authenticated POST /news/SPI200/toggle-collapse returns 200, empty body
    - test_collapse_toggle_rejects_get_verb: GET /news/SPI200/toggle-collapse returns 405 Method Not Allowed (GET is not registered)

    Rendering (Python f-string component, NO Jinja2):
    - test_renderer_does_not_use_jinja2: grep dashboard_renderer/components/news.py for 'Jinja2' or 'render_template' — assert absent; assert 'html.escape' is present ≥4 times (title, url, publisher, pub_date, title_hash — every dynamic field)
    - test_xss_headline_renders_escaped: render via render_news_panel with a headline whose title is `<script>alert(1)</script>`; the literal substring `<script>alert(1)</script>` is NOT present in the rendered HTML, `&lt;script&gt;alert(1)&lt;/script&gt;` IS present
    - test_headline_anchor_carries_rel_noopener: render with url `https://example.com/x`; rendered HTML contains `rel="noopener noreferrer"`
    - test_news_panel_open_by_default: render with collapsed=False; HTML contains `<details class="news-panel-disclosure" open>`
    - test_news_panel_collapsed_when_pref_true: render with collapsed=True; HTML contains `<details class="news-panel-disclosure">` WITHOUT ` open`
    - test_banner_renders_with_locked_copy: render with a headline that matches the SPI200 keyword set so has_critical_event fires INTERNALLY in render_news_panel on the post-filter list; HTML contains EXACTLY `Possible market-moving news — operator review recommended`
    - test_banner_absent_when_no_critical_event: render with non-critical headline; banner copy is NOT in the HTML
    - test_dismissed_hash_filters_out_row: render with one headline title_hash='abc1234567890def' and dismissed_hashes={'abc1234567890def'}; that row absent from HTML
    - test_banner_disappears_when_only_critical_headline_dismissed: render with TWO headlines [critical_match, benign]; pass dismissed_hashes={critical.title_hash}; assert the locked banner copy is NOT in the HTML (banner must evaluate has_critical_event on the FILTERED list, not the raw list — this is the explicit contract from review HIGH #4)
    - test_summary_label_is_market_news: HTML contains `<summary class="news-panel-summary">Market News</summary>`
    - test_dismiss_button_label_is_dismiss_headline: HTML for non-empty headlines contains `Dismiss Headline`

    tests/test_web_news_dismiss.py (state-write contract — D-08 + isolation + first-visit safety):
    - test_dismiss_writes_user_news_dismissed: dismiss hash 'abc1234567890def' for uid 'user_a' on market 'SPI200'; resulting state['users']['user_a']['news_dismissed']['SPI200'] == {'date': today_iso, 'hashes': ['abc1234567890def']}
    - test_dismiss_first_visit_user_does_not_crash: pre-seed state with state['users']['user_a'] = {} (no news_dismissed key); dismiss a hash; response is 200; resulting state has news_dismissed['SPI200'] populated with today's date and the hash (setdefault chain must initialise the path safely)
    - test_dismiss_first_visit_user_without_users_bucket: pre-seed state with no 'users' key at all; dismiss a hash for user_a; resulting state['users']['user_a']['news_dismissed']['SPI200'] is initialised (setdefault initialises 'users', uid, 'news_dismissed', market_id in turn)
    - test_dismiss_appends_distinct_hashes_same_day: two dismisses of different hashes same day → both present in list, length 2
    - test_dismiss_dedupes_same_hash_same_day: dismissing same hash twice keeps list at length 1
    - test_dismiss_d08_auto_expiry_resets_on_date_change: pre-seed state['users']['user_a']['news_dismissed']['SPI200'] = {'date': '2020-01-01', 'hashes': ['old_hash_aaaa', 'old_hash_bbbb']}; dismiss new hash today; resulting dict has {'date': today_iso, 'hashes': ['<new_hash>']} — old hashes auto-expired per D-08 (the reset happens INSIDE the mutate_user_state callback atomically, not on the next read)
    - test_dismiss_d08_expiry_is_per_market: pre-seed news_dismissed['SPI200']={'date':'2020-01-01','hashes':['x']} AND news_dismissed['AUDUSD']={'date':today,'hashes':['y']}; dismiss new hash on SPI200; SPI200 resets, AUDUSD untouched
    - test_dismiss_isolation_user_a_vs_user_b: dismiss X for user_a, Y for user_b; state['users']['user_a']['news_dismissed']['SPI200']['hashes'] == ['X'] and state['users']['user_b']['news_dismissed']['SPI200']['hashes'] == ['Y']
    - test_collapse_toggle_flips_per_market_bool: initial state has no news_panel_collapsed; POST /news/SPI200/toggle-collapse for user_a → state['users']['user_a']['news_panel_collapsed']['SPI200'] is True; POST again → False
    - test_collapse_toggle_first_visit_safe: state['users']['user_a'] absent; POST toggle; no crash; state['users']['user_a']['news_panel_collapsed']['SPI200'] == True (setdefault chain)
    - test_collapse_toggle_is_per_market: toggle SPI200 to True; AUDUSD remains False/absent
    - test_dismiss_does_not_corrupt_other_user_state: pre-seed user_a + user_b paper_trades; dismiss as user_a; user_b's paper_trades byte-identical

    Use file-name prefix `test_web_news_*.py` for conftest autouse fixture. Use `app.dependency_overrides[current_user_id] = lambda: 'user_a'` for uid injection. Point state to tmp_path-scoped state.json.
  </behavior>
  <action>
    Create both test files under tests/test_web_news_*. 2-space indent. Build local app_instance fixture per file (minimal create_app() return).

    Run pytest → expect RED (ModuleNotFoundError for web.routes.news + dashboard_renderer.components.news).
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py 2>&1 | grep -q -E "(ModuleNotFoundError|ImportError|web\.routes\.news|dashboard_renderer\.components\.news)" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_web_news_routes.py && test -f tests/test_web_news_dismiss.py` exits 0
    - `grep -c 'def test_register_attaches_collapse_toggle_route_is_POST' tests/test_web_news_routes.py` returns 1
    - `grep -c 'def test_collapse_toggle_rejects_get_verb' tests/test_web_news_routes.py` returns 1
    - `grep -c 'def test_renderer_does_not_use_jinja2' tests/test_web_news_routes.py` returns 1
    - `grep -c 'def test_banner_disappears_when_only_critical_headline_dismissed' tests/test_web_news_routes.py` returns 1
    - `grep -c 'def test_dismiss_d08_auto_expiry_resets_on_date_change' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_dismiss_d08_expiry_is_per_market' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_dismiss_first_visit_user_does_not_crash' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_dismiss_first_visit_user_without_users_bucket' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_collapse_toggle_first_visit_safe' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_collapse_toggle_flips_per_market_bool' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_collapse_toggle_is_per_market' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'def test_dismiss_isolation_user_a_vs_user_b' tests/test_web_news_dismiss.py` returns 1
    - `grep -c 'Possible market-moving news — operator review recommended' tests/test_web_news_routes.py` returns ≥1
    - `grep -c 'Dismiss Headline' tests/test_web_news_routes.py` returns ≥1
    - `grep -c 'rel=\"noopener noreferrer\"' tests/test_web_news_routes.py` returns ≥1
    - `grep -c 'app.dependency_overrides' tests/test_web_news_dismiss.py` returns ≥1
    - Pytest collection on both files is RED.
  </acceptance_criteria>
  <done>Both test files exist with all named tests covering POST verb, f-string rendering, filter-before-banner, D-08 atomic expiry, first-visit setdefault safety, per-market collapse, and per-user isolation; pytest RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement web/routes/news.py (POST verbs + D-08 atomic expiry + setdefault chain) + dashboard_renderer/components/news.py (f-string + filter-before-banner) + assets.py CSS + web/app.py wiring</name>
  <files>web/routes/news.py, dashboard_renderer/components/news.py, dashboard_renderer/assets.py, web/app.py</files>
  <read_first>
    - tests/test_web_news_routes.py + tests/test_web_news_dismiss.py (the contract)
    - web/routes/healthz.py (register pattern, local imports inside handlers)
    - web/routes/paper_trades/__init__.py lines 71–90 (Depends(current_user_id) signature)
    - web/routes/admin/__init__.py lines 192–205 (empty-200 dismiss)
    - dashboard_renderer/components/trace.py lines 225–261 (<details> open_attr pattern)
    - dashboard_renderer/components/devices.py + markets.py (f-string + html.escape pattern reference — DO NOT use Jinja2)
    - dashboard_renderer/assets.py (CSS token placement)
    - web/app.py (existing register-pattern block; verify NO Jinja2Templates import — if present, that is a sign you are about to repeat the wrong pattern)
    - state_manager/__init__.py::mutate_user_state (signature + flock-deadlock warning)
    - .planning/phases/38-news-integration/38-UI-SPEC.md
    - news_filter.py (Plan 02 has_critical_event — called from render_news_panel on POST-FILTER list)
  </read_first>
  <action>
    Four files, one cohesive feature slice. Handler signatures use `Depends(current_user_id)`. Renderer uses Python f-strings with explicit `html.escape()` per dynamic value — DO NOT introduce Jinja2.

    (A) web/routes/news.py — `register(app: FastAPI) -> None` registering two POST routes:

      Module-level imports:
      ```
      import re
      from fastapi import FastAPI, Depends, Response, HTTPException
      from web.dependencies import current_user_id
      ```
      (Depends targets must be importable at module level.)

      Module-level helper: `_HASH_RE = re.compile(r'^[0-9a-f]{16}$')`.
      Module-level helper `_is_known_market(market)`: use existing helper from web/routes/markets.py or system_params.MARKETS_BY_ID — do NOT hand-roll a new market registry.

      Route 1: `@app.post('/news/{market}/dismiss/{title_hash}')`
        Signature: `def dismiss_headline(market: str, title_hash: str, uid: str = Depends(current_user_id)) -> Response`
        Body:
        1. if not _is_known_market(market): raise HTTPException(status_code=404, detail='unknown market')
        2. if not _HASH_RE.fullmatch(title_hash): raise HTTPException(status_code=422, detail='invalid title_hash')
        3. Local imports: `from state_manager import mutate_user_state`, `from datetime import date`.
        4. today = date.today().isoformat()
        5. Define mutator `_apply(state)`:
           ```
           users = state.setdefault('users', {})
           user = users.setdefault(uid, {})
           nd_root = user.setdefault('news_dismissed', {})
           bucket = nd_root.setdefault(market, {'date': '', 'hashes': []})
           # D-08 atomic auto-expiry — happens INSIDE the mutator, NOT on next read
           if bucket.get('date') != today:
             bucket['date'] = today
             bucket['hashes'] = []
           if title_hash not in bucket['hashes']:
             bucket['hashes'].append(title_hash)
           ```
           The setdefault chain handles all first-visit cases (missing 'users', missing uid, missing 'news_dismissed', missing per-market bucket). The date-comparison-and-reset is the D-08 contract, applied atomically inside the per-user flock.
        6. mutate_user_state(uid, _apply)
        7. Return Response(content='', media_type='text/html')

      Route 2: `@app.post('/news/{market}/toggle-collapse')`  (POST verb is MANDATORY — GET must not mutate)
        Signature: `def toggle_collapse(market: str, uid: str = Depends(current_user_id)) -> Response`
        Body:
        1. if not _is_known_market(market): raise HTTPException(status_code=404, detail='unknown market')
        2. Local imports: `from state_manager import mutate_user_state`
        3. Define mutator `_apply(state)`:
           ```
           users = state.setdefault('users', {})
           user = users.setdefault(uid, {})
           col_root = user.setdefault('news_panel_collapsed', {})
           col_root[market] = not col_root.get(market, False)
           ```
           setdefault chain handles all first-visit cases. Per-market scoping is via `col_root[market]`.
        4. mutate_user_state(uid, _apply)
        5. Return Response(content='', media_type='text/html')

      Do NOT register a GET method for either path. The test `test_collapse_toggle_rejects_get_verb` asserts 405.

    (B) dashboard_renderer/components/news.py — Python f-string component (NO Jinja2):

      Module imports: `import html`, `from news_filter import has_critical_event` (top-level), typing as needed. NO Jinja2, NO render_template, NO templates/ directory references. NO state_manager, NO news_fetcher (renderer is pure transform).

      Public function:
      ```
      def render_news_panel(
        market_id: str,
        headlines: list[dict],
        dismissed_hashes: frozenset[str] | set[str],
        collapsed: bool,
      ) -> str:
      ```

      Implementation order (CRITICAL — review HIGH #4 + agreed concern #3):
      1. Filter dismissed BEFORE banner evaluation:
         `filtered = [h for h in headlines if h.get('title_hash') not in dismissed_hashes]`
      2. has_critical evaluated on the FILTERED list:
         `has_critical = has_critical_event(filtered, market_id)`
      3. open_attr = '' if collapsed else ' open'
      4. mkt_esc = html.escape(market_id, quote=True)
      5. banner_html = _render_news_banner() if has_critical else ''
      6. rows_html = ''.join(_render_headline_row(mkt_esc, h) for h in filtered) or _render_empty_state()
      7. Return an f-string that wraps everything in:
         `<details class="news-panel-disclosure"{open_attr}>` + `<summary class="news-panel-summary">Market News</summary>` + inner div + banner + table-rows.

      `_render_news_banner()` returns the EXACT locked string:
      ```
      <div class="news-banner"><p class="news-banner-heading">Possible market-moving news — operator review recommended</p></div>
      ```
      Use em-dash U+2014.

      `_render_headline_row(mkt_esc, item)`:
      - title_hash_esc = html.escape(item.get('title_hash', ''), quote=True)
      - title_esc = html.escape(item.get('title', ''))
      - url_esc = html.escape(item.get('url', ''), quote=True)
      - publisher_esc = html.escape(item.get('publisher', ''))
      - pub_date_esc = html.escape(item.get('pub_date', ''))
      - Emit `<tr id="news-row-{title_hash_esc}">` + `<td class="news-link"><a href="{url_esc}" target="_blank" rel="noopener noreferrer">{title_esc}</a></td>` + `<td class="news-meta">{publisher_esc} · {pub_date_esc}</td>` + `<td><button type="button" class="btn-news-dismiss" hx-post="/news/{mkt_esc}/dismiss/{title_hash_esc}" hx-target="#news-row-{title_hash_esc}" hx-swap="outerHTML" hx-trigger="click">Dismiss Headline</button></td>` + `</tr>`

      `_render_empty_state()` returns `<p class="subtle">No headlines today.</p>`.

    (C) dashboard_renderer/assets.py — append CSS rules from UI-SPEC §CSS additions verbatim (.news-panel-disclosure, .news-panel-summary, .news-panel-inner, .news-banner, .news-banner-heading, .news-table, .news-link, .news-meta, .btn-news-dismiss). Use existing tokens only.

    (D) web/app.py — wire the routes:
      - Top-of-file: `from web.routes import news as news_route`.
      - Inside create_app(): `news_route.register(application)`.
      - Verify no `from fastapi.templating import Jinja2Templates` is added (this codebase doesn't use Jinja2; if you find yourself wanting to add it, you are doing the wrong thing — re-read dashboard_renderer/components/devices.py for the f-string pattern).

    Run pytest. If GREEN, commit.

    Task 3 wires render_news_panel into the live per-market dashboard render path.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py tests/test_signal_engine.py::TestDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - `test -f web/routes/news.py && test -f dashboard_renderer/components/news.py` exits 0
    - `grep -c 'def register(app' web/routes/news.py` returns 1
    - `grep -c "@app.post('/news/{market}/dismiss/{title_hash}')" web/routes/news.py` returns 1
    - `grep -c "@app.post('/news/{market}/toggle-collapse')" web/routes/news.py` returns 1
    - `grep -cE "@app\.get\([^)]*toggle-collapse" web/routes/news.py` returns 0 (GET must NOT mutate)
    - `grep -c 'uid: str = Depends(current_user_id)' web/routes/news.py` returns ≥2
    - `grep -c 'from web.dependencies import current_user_id' web/routes/news.py` returns 1
    - `grep -c "from state_manager import mutate_user_state" web/routes/news.py` returns ≥1
    - `grep -c "media_type='text/html'" web/routes/news.py` returns ≥2
    - `grep -c 'setdefault' web/routes/news.py` returns ≥6 (users / uid / news_dismissed / per-market bucket — dismiss handler; users / uid / news_panel_collapsed — toggle handler)
    - `grep -cE "bucket\['date'\] = today|bucket\[.date.\] = today" web/routes/news.py` returns ≥1 (D-08 atomic expiry inside mutator)
    - `grep -cE "bucket\['hashes'\] = \[\]|bucket\[.hashes.\] = \[\]" web/routes/news.py` returns ≥1 (D-08 reset)
    - `grep -c "def render_news_panel" dashboard_renderer/components/news.py` returns 1
    - `grep -cE 'Jinja2|render_template|fastapi\.templating' dashboard_renderer/components/news.py` returns 0 (f-string component only — no Jinja2)
    - `grep -c 'from news_filter import has_critical_event' dashboard_renderer/components/news.py` returns 1
    - `grep -cE 'filtered\s*=.*dismissed_hashes' dashboard_renderer/components/news.py` returns ≥1 (filter happens before banner check)
    - `grep -cE 'has_critical_event\(filtered' dashboard_renderer/components/news.py` returns ≥1 (banner evaluated on FILTERED list per HIGH #4)
    - `grep -c 'rel="noopener noreferrer"' dashboard_renderer/components/news.py` returns ≥1
    - `grep -c 'Possible market-moving news — operator review recommended' dashboard_renderer/components/news.py` returns 1
    - `grep -c 'Dismiss Headline' dashboard_renderer/components/news.py` returns ≥1
    - `grep -c 'Market News' dashboard_renderer/components/news.py` returns ≥1
    - `grep -c "html.escape" dashboard_renderer/components/news.py` returns ≥4
    - `grep -c '\.news-panel-disclosure' dashboard_renderer/assets.py` returns ≥1
    - `grep -c '\.news-banner' dashboard_renderer/assets.py` returns ≥1
    - `grep -c '\.btn-news-dismiss' dashboard_renderer/assets.py` returns ≥1
    - `grep -c 'news_route.register' web/app.py` returns 1
    - `grep -c 'from web.routes import news' web/app.py` returns 1
    - `grep -cE 'Jinja2Templates' web/app.py` returns 0 (no Jinja2 introduced)
    - `.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py` exits 0
    - `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism` exits 0
    - `wc -l web/routes/news.py dashboard_renderer/components/news.py` each ≤500
  </acceptance_criteria>
  <done>web/routes/news.py registers POST dismiss + POST toggle-collapse (no GET on toggle), uses Depends(current_user_id), implements D-08 auto-expiry atomically inside mutate_user_state, uses full setdefault chain for first-visit safety; dashboard_renderer/components/news.py is a Python f-string component (no Jinja2), filters dismissed BEFORE calling has_critical_event, explicit html.escape on every dynamic field, locked banner copy and rel="noopener noreferrer" present; assets.py carries Phase 38 CSS; web/app.py wires the route. All Task 1 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire render_news_panel into live per-market dashboard render path + integration test (server-side injection; no hx-get placeholder needed because dismiss/toggle use HTMX out-of-band swaps)</name>
  <files>dashboard_renderer/context.py, dashboard_renderer/api.py, dashboard_renderer/components/signals.py, web/routes/dashboard/__init__.py, tests/test_web_news_dashboard_integration.py</files>
  <read_first>
    - dashboard_renderer/context.py (RenderContext dataclass + .build())
    - dashboard_renderer/api.py lines 26–204 (_build_render_context + render_dashboard_as_str + render_panel_html)
    - dashboard_renderer/components/signals.py lines 143–259 (render_signal_cards loop; injection point is line 256 inside the for-loop, after _render_trace_panels)
    - dashboard_renderer/shell.py lines 264–295 (signals page_map; _render_signal_cards call site)
    - web/routes/dashboard/__init__.py lines 266–330 (_serve_market_scoped_page)
    - web/routes/dashboard/__init__.py lines 184–248 (_serve_account_page_scoped — canonical Depends pattern)
    - dashboard_renderer/components/news.py (Task 2 output)
    - news_fetcher.py (Plan 03 — fetch_news signature; NewsItem includes title_hash)
    - news_filter.py (Plan 02 — note: has_critical_event is called INSIDE render_news_panel on the post-filter list; signals.py does NOT call it directly)
    - state_manager/__init__.py (load_state)
    - tests/conftest.py + tests/test_web_news_routes.py (TestClient patterns)
  </read_first>
  <behavior>
    tests/test_web_news_dashboard_integration.py:
    - test_market_signals_page_contains_news_panel_spi200: GET /markets/SPI200/signals authenticated; body contains `<details class="news-panel-disclosure"` AND `<summary class="news-panel-summary">Market News</summary>`. Monkeypatch fetch_news to return fixed list.
    - test_market_signals_page_contains_news_panel_audusd: same for AUDUSD.
    - test_news_panel_appears_after_signal_card: body contains both `class="card"` and `class="news-panel-disclosure"`; news-panel-disclosure index > last card index for that market.
    - test_dismissed_hash_hidden_from_dashboard: pre-seed state['users'][uid]['news_dismissed']['SPI200'] = {'date': today_iso, 'hashes': ['<first_hash>']}; GET; that row absent, second IS.
    - test_dismissed_hash_stale_date_treated_as_empty: pre-seed news_dismissed['SPI200'] = {'date': '2020-01-01', 'hashes': ['<first_hash>']}; GET; first headline IS visible (stale dismiss bucket auto-expired at READ time too — renderer treats date != today as no dismissals)
    - test_collapsed_pref_renders_without_open_attr: pre-seed state['users'][uid]['news_panel_collapsed']['SPI200'] = True; GET; body has `<details class="news-panel-disclosure">` WITHOUT ` open`.
    - test_dashboard_renders_when_no_news_state_present: pre-seed state['users'][uid] = {} (no news keys at all); GET; status 200, page renders (no crash on missing news_dismissed/news_panel_collapsed) — proves .get() defaults work in the render path
    - test_dashboard_renders_when_users_bucket_absent: pre-seed state with no 'users' key; GET; status 200 (renderer gracefully handles fully missing user state)
    - test_news_panel_uses_authenticated_uid: dependency_overrides[current_user_id] = lambda: 'user_a'; pre-seed user_a dismiss X and user_b dismiss Y; GET as user_a; X filtered, Y still visible.

    Monkeypatch `news_fetcher.fetch_news` to return fixed list of NewsItem dicts (with title_hash) so tests do not hit yfinance.
  </behavior>
  <action>
    Five files, one slice.

    (A) dashboard_renderer/context.py — extend RenderContext:
      Add three optional fields with safe defaults (per-market dicts, not flat scalars, per pinned state shape):
        uid: str | None = None
        news_dismissed: dict[str, dict] = field(default_factory=dict)  # market_id → {'date': ..., 'hashes': [...]}
        news_panel_collapsed: dict[str, bool] = field(default_factory=dict)  # market_id → bool
      Mirror in RenderContext.build() as keyword-only with same defaults.

    (B) dashboard_renderer/api.py — thread fields through:
      - _build_render_context: add `uid`, `news_dismissed`, `news_panel_collapsed` kwargs; forward to RenderContext.build().
      - render_dashboard_as_str: add same kwargs; forward.
      - render_panel_html: add same kwargs; forward.
      - render_dashboard_files + render_dashboard_page: leave defaults (disk-write entrypoints — per-user privacy boundary keeps uid=None here).

    (C) dashboard_renderer/components/signals.py — inject news panel inside per-market loop:
      New signature:
        `def render_signal_cards(state, *, active_market=None, uid=None, news_dismissed=None, news_panel_collapsed=None) -> str:`
      Defaults: `news_dismissed = news_dismissed or {}`, `news_panel_collapsed = news_panel_collapsed or {}` at top of function.

      After `parts.append(_render_trace_panels(...))` (line 256) and BEFORE the next loop iteration:
        - LOCAL imports (C-2 hex discipline):
          `from news_fetcher import fetch_news`
          `from dashboard_renderer.components.news import render_news_panel`
          `from datetime import date`
        - Resolve symbol for state_key (verify existing helper in state['markets'][state_key] or symbol map; if absent, pass state_key directly — verify against fetch_news signature).
        - try: headlines = fetch_news(state_key, symbol); except Exception: logger.warning(...); headlines = []  (never-crash discipline D-10)
        - Per-market dismiss bucket with stale-date auto-expiry at READ time:
          ```
          today_iso = date.today().isoformat()
          dismiss_bucket = news_dismissed.get(state_key, {}) or {}
          if dismiss_bucket.get('date') == today_iso:
            dismissed_hashes = frozenset(dismiss_bucket.get('hashes', []))
          else:
            dismissed_hashes = frozenset()  # stale bucket — treat as empty (matches dismiss handler's D-08 reset)
          ```
        - collapsed = bool(news_panel_collapsed.get(state_key, False))  (.get default — first-visit safe)
        - news_html = render_news_panel(state_key, headlines, dismissed_hashes, collapsed)  (render_news_panel itself filters + evaluates has_critical_event internally on filtered list)
        - parts.append(news_html)

      Update shell.py call site to pass ctx.uid, ctx.news_dismissed, ctx.news_panel_collapsed.

    (D) web/routes/dashboard/__init__.py — thread uid through _serve_market_scoped_page:
      - Add `uid: str = Depends(_get_current_user_id)` to GET /markets/{market_id}/{function} route signatures; forward.
      - Inside _serve_market_scoped_page, derive per-user news state with full .get() defaults (review MEDIUM #10):
        ```
        users_bucket = state.get('users', {}) or {}
        user_bucket = users_bucket.get(uid, {}) or {}
        news_dismissed = user_bucket.get('news_dismissed', {}) or {}
        news_panel_collapsed = user_bucket.get('news_panel_collapsed', {}) or {}
        ```
        These are passed wholesale (full per-market dicts) to render_dashboard_as_str / render_panel_html. The renderer applies per-market lookup + stale-date auto-expiry. First-visit users with no 'users' key, no per-uid bucket, or no news_dismissed key all resolve to `{}` without raising.
      - Forward via new kwargs: `uid=uid, news_dismissed=news_dismissed, news_panel_collapsed=news_panel_collapsed`.

    (E) tests/test_web_news_dashboard_integration.py — write the integration tests per <behavior>. File prefix `test_web_*` for conftest fixture.

    Run pytest. If GREEN, commit.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_news_dashboard_integration.py tests/test_web_news_routes.py tests/test_web_news_dismiss.py tests/test_signal_engine.py::TestDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_web_news_dashboard_integration.py` exits 0
    - `grep -c 'def test_market_signals_page_contains_news_panel_spi200' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_market_signals_page_contains_news_panel_audusd' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_news_panel_appears_after_signal_card' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_dismissed_hash_hidden_from_dashboard' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_dismissed_hash_stale_date_treated_as_empty' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_dashboard_renders_when_no_news_state_present' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'def test_dashboard_renders_when_users_bucket_absent' tests/test_web_news_dashboard_integration.py` returns 1
    - `grep -c 'news_dismissed' dashboard_renderer/context.py` returns ≥2
    - `grep -c 'news_panel_collapsed' dashboard_renderer/context.py` returns ≥2
    - `grep -c 'render_news_panel' dashboard_renderer/components/signals.py` returns ≥1
    - `grep -c 'fetch_news' dashboard_renderer/components/signals.py` returns ≥1
    - `grep -cE "dismiss_bucket\.get\(.date.\)\s*==\s*today" dashboard_renderer/components/signals.py` returns ≥1 (stale-date read-time guard)
    - `grep -c 'news_dismissed' dashboard_renderer/api.py` returns ≥3 (three signatures)
    - `grep -c 'news_dismissed' web/routes/dashboard/__init__.py` returns ≥1
    - `grep -cE "\.get\(.users.,\s*\{\}\)" web/routes/dashboard/__init__.py` returns ≥1 (full .get() default chain for first-visit safety)
    - `grep -c 'uid: str = Depends(_get_current_user_id)' web/routes/dashboard/__init__.py` returns ≥3
    - `.venv/bin/pytest -x --tb=short tests/test_web_news_dashboard_integration.py` exits 0
    - `.venv/bin/pytest -x --tb=short` full-suite exits 0
  </acceptance_criteria>
  <done>RenderContext carries per-user news state as per-market dicts; signals.py emits render_news_panel at bottom of each per-market section with stale-date read-time guard and try/except resilience; /markets/{m}/signals threads Depends(current_user_id) and per-user dismissed/collapsed with full .get() default chain; integration tests prove panel visible on both markets, dismissed filtered, stale dismiss bucket treated as empty, first-visit users with no news state render cleanly. Phase 38 ROADMAP goal delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dismiss/toggle-collapse route | authenticated HTMX POST; uid resolved server-side via Depends(current_user_id) |
| route → state.json | mutate_user_state wraps mutate_state with per-user fcntl.flock; D-08 expiry happens atomically inside the mutator |
| headline content → rendered HTML | XSS surface — explicit html.escape() per dynamic field (Python f-string component, no Jinja2 autoescape) |
| outbound headline link → browser | target="_blank" reverse-tab-nabbing surface |
| browser → /markets/{m}/signals | authenticated GET; per-user dismissed/collapsed read from state['users'][uid] with full .get() default chain |
| yfinance network → render path | network failure must not 500 the dashboard (try/except + empty fallback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Tampering | XSS via headline title rendering | mitigate | html.escape() on title/url/publisher/pub_date/title_hash before string concatenation (explicit, not Jinja2-autoescape — this codebase renders via f-strings); test_xss_headline_renders_escaped asserts |
| T-38-04-02 | Tampering | Reverse tab-nabbing via target="_blank" | mitigate | rel="noopener noreferrer" mandatory on every headline anchor |
| T-38-04-03 | Information Disclosure | Cross-user dismiss leakage | mitigate | mutate_user_state writes exclusively to state['users'][uid]; render path keys dismissed_hashes off Depends(current_user_id) uid |
| T-38-04-04 | Tampering | Path traversal via market path param | mitigate | _is_known_market(market) check at route entry → 404 on unknown market; Plan 03's _is_valid_market_id gates the cache path too |
| T-38-04-05 | Tampering | title_hash path injection | mitigate | re.fullmatch(r'[0-9a-f]{16}', title_hash) → 422 on malformed |
| T-38-04-06 | Spoofing | Unauthenticated dismiss / dashboard view | mitigate | All handlers use `uid: str = Depends(current_user_id)`; FastAPI raises 403 before handler body |
| T-38-04-07 | Tampering | GET-based state mutation (CSRF / crawler-trigger) | mitigate | Both mutation endpoints are POST; no GET method registered for toggle-collapse; test_collapse_toggle_rejects_get_verb asserts 405 |
| T-38-04-08 | DoS | Unbounded news_dismissed list growth | accept | D-08 daily reset inside mutator caps list size to ≤ headlines fetched per market per day (≤5) × markets |
| T-38-04-09 | Tampering | Cookie tampering to forge uid | mitigate | uid read from authenticated session cookie via Phase 35 Depends(current_user_id); no client-supplied uid |
| T-38-04-10 | DoS | yfinance outage 500s the dashboard | mitigate | fetch_news call in signals.py wrapped in try/except → empty headline list fallback |
| T-38-04-11 | Tampering | Banner shown for already-dismissed critical headline | mitigate | has_critical_event called INSIDE render_news_panel on the post-filter list — dismissing the only triggering headline removes the banner (test_banner_disappears_when_only_critical_headline_dismissed asserts) |
| T-38-04-12 | DoS / UX | First-visit crash on missing state keys | mitigate | Full setdefault chain inside route mutators + full .get('users', {}).get(uid, {}).get('news_dismissed', {}) chain in render path; tests cover state['users'] absent, uid bucket absent, news keys absent |
</threat_model>

<verification>
1. `.venv/bin/pytest -x --tb=short tests/test_web_news_routes.py tests/test_web_news_dismiss.py tests/test_web_news_dashboard_integration.py` exits 0.
2. `.venv/bin/pytest -x --tb=short` full-suite exits 0.
3. Manual smoke (deferred to operator UAT): visit /markets/SPI200/signals, observe news panel, dismiss a headline, reload — panel remembers dismiss until next day.
4. `grep -E 'rel="noopener noreferrer"' dashboard_renderer/components/news.py` matches.
5. Locked banner copy byte-identical in source and test.
6. `grep -c 'render_news_panel' dashboard_renderer/components/signals.py` ≥1 — live injection.
7. `grep -cE 'Jinja2|render_template' dashboard_renderer/components/news.py web/app.py web/routes/news.py` returns 0 — no Jinja2 introduced.
8. `grep -cE "@app\.get\([^)]*toggle-collapse" web/routes/news.py` returns 0 — POST verb only on toggle.
</verification>

<success_criteria>
- Two POST routes registered: dismiss + toggle-collapse; both use Depends(current_user_id); both return empty 200 text/html; both write through mutate_user_state with full setdefault chain.
- Dismiss handler implements D-08 auto-expiry atomically inside the mutate_user_state mutator (date-check → reset hashes → append new hash).
- render_news_panel is a Python f-string component (no Jinja2), filters dismissed hashes BEFORE evaluating has_critical_event so the banner reflects post-dismiss state.
- Explicit html.escape() on every dynamic value (title, url, publisher, pub_date, title_hash, market_id).
- rel="noopener noreferrer" on every anchor.
- Per-user state shape pinned: news_dismissed[market_id] = {'date', 'hashes'}; news_panel_collapsed[market_id] = bool.
- First-visit users (no 'users' key, no per-uid bucket, no news keys) do not crash dismiss/toggle/render.
- Per-user dismiss isolation proved by tests.
- Date-change auto-expiry proved at both write time (mutator) and read time (renderer).
- web/app.py wires news_route.register; no Jinja2Templates introduced.
- News panel renders at the bottom of each per-market section on /markets/{m}/signals (Phase 38 ROADMAP goal).
- yfinance outage does not 500 the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/38-news-integration/38-04-SUMMARY.md` summarising: POST-only route surface, D-08 atomic expiry implementation, f-string render contract (no Jinja2), filter-before-banner ordering, CSS classes added, html.escape audit, full setdefault/.get() default chain for first-visit safety, per-user-isolation test results, and the live-injection wiring. Confirm the Phase 38 ROADMAP goal is delivered.
</output>
</content>
