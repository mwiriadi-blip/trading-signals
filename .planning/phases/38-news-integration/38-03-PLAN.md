---
phase: 38-news-integration
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - news_fetcher.py
  - tests/test_news_fetcher.py
autonomous: true
requirements: [NEWS-01, NEWS-03]
must_haves:
  truths:
    - "news_fetcher.fetch_news(market_id, symbol) returns list[NewsItem] of length ≤5"
    - "Both pre-0.2.55 and post-0.2.55 yfinance schemas normalise to the same NewsItem shape including title_hash"
    - "title_hash is sha256-of-normalised-title hex-truncated to 16 chars; normalisation = title.strip().lower() with internal-whitespace collapsed to single spaces"
    - "Cache hit (JSON envelope `date` field == today's UTC date) skips the yfinance call entirely; mtime is NOT used"
    - "Cache miss writes atomically via tempfile + os.replace; envelope shape is {'date': 'YYYY-MM-DD', 'headlines': [...]}"
    - "market_id is validated against VALID_MARKETS allowlist before any cache-path construction (path-traversal closed)"
    - "Outbound URL in NewsItem.url is validated to scheme in {'https','http'} or set to '' (javascript:/data:/relative rejected)"
    - "<script>alert(1)</script> headline survives normaliser intact (escape is render-time-only, not fetch-time)"
    - "No server-side HTTP call to a headline URL anywhere in news_fetcher.py (SSRF closed by render-only)"
    - "news_fetcher.py imports satisfy FORBIDDEN_MODULES_NEWS_FETCHER"
  artifacts:
    - path: "news_fetcher.py"
      provides: "fetch_news, NewsItem (with title_hash), _normalise_item, _write_cache, _load_cache, _get_yf, _is_valid_market_id, _validate_url_scheme, _normalise_title_for_hash"
      exports: ["fetch_news", "NewsItem"]
    - path: "tests/test_news_fetcher.py"
      provides: "schema normalisation, title_hash stability, cache hit/miss via JSON date field, market_id allowlist, URL scheme validation, dedup, XSS-escape preservation, SSRF no-prefetch, AST forbidden-import gate"
  key_links:
    - from: "news_fetcher.py"
      to: "system_params VALID_MARKETS (or equivalent allowlist)"
      via: "from system_params import VALID_MARKETS — gate cache path construction"
      pattern: "VALID_MARKETS"
    - from: "news_fetcher.py"
      to: "yfinance.Ticker (via _get_yf lazy accessor)"
      via: "_get_yf() memoized, monkeypatch-friendly"
      pattern: "_get_yf"
    - from: "news_fetcher.py"
      to: "sidecar cache file news_cache_{market_id}.json"
      via: "atomic write + load-on-hit + JSON date TTL check"
      pattern: "news_cache_"
    - from: "tests/test_news_fetcher.py"
      to: "tests/fixtures/news/news_fixture_pre055.json + news_fixture_post055.json"
      via: "json.load + _normalise_item"
      pattern: "news_fixture_pre055.json"
---

<objective>
Build the news I/O adapter `news_fetcher.py` (NEWS-01 + NEWS-03) — peer of `data_fetcher.py`. Owns yfinance.Ticker.news calls, normalises both pre-0.2.55 and post-0.2.55 schemas into a single TypedDict `NewsItem` that now INCLUDES a `title_hash` field (consumed by Plan 04 for dismiss-by-hash and dedup), deduplicates by title_hash, caches per-market per-day in atomic sidecar JSON files (`news_cache_{market_id}.json`) with the JSON envelope `date` field as the AUTHORITATIVE TTL check (not filesystem mtime), validates `market_id` against an allowlist before path construction (prevents traversal), validates outbound URL schemes (rejects javascript:/data:/relative), and respects the FORBIDDEN_MODULES_NEWS_FETCHER AST hex boundary established by Plan 01.

Purpose: NEWS-01 (top 5, deduplicated, daily cache, shared across users), NEWS-03 (both-shape normaliser, XSS escape is render-time-only, SSRF closed by no server-side link prefetch, defence-in-depth URL scheme validation).
Output: news_fetcher.py + tests/test_news_fetcher.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/38-news-integration/38-CONTEXT.md
@.planning/phases/38-news-integration/38-RESEARCH.md
@.planning/phases/38-news-integration/38-PATTERNS.md
@data_fetcher.py
@state_manager/io.py
@system_params.py
@tests/test_signal_engine.py

<interfaces>
From data_fetcher.py (pattern source):
  - _get_yf() lazy memoized accessor — local `import yfinance as yf_` first call only
  - narrow-catch retry loop with backoff
  - HTTP_TIMEOUT_S imported from system_params

From state_manager/io.py::_atomic_write_unlocked (pattern source):
  - tempfile.NamedTemporaryFile(dir=parent, delete=False, mode='w', suffix='.tmp', encoding='utf-8')
  - tmp.flush() + os.fsync(tmp.fileno())
  - os.replace(tmp_path, target_path)
  - cleanup in finally with FileNotFoundError ignored

From system_params.py:
  - VALID_MARKETS allowlist (or equivalent — verify exact name in current system_params.py). If no such constant exists, define one inline at module load: `_VALID_MARKETS = frozenset({'SPI200', 'AUDUSD'})` — DO NOT add to system_params.py since this plan doesn't touch it. Cache path construction MUST go through this allowlist gate.

From tests/test_signal_engine.py (post Plan 01):
  - FORBIDDEN_MODULES_NEWS_FETCHER frozenset declared
  - NEWS_FETCHER_PATH = Path('news_fetcher.py')

Module contract (this plan defines):
  - class NewsItem(TypedDict): title: str; url: str; publisher: str; pub_date: str; title_hash: str
  - fetch_news(market_id: str, symbol: str, max_items: int = 5, retries: int = 3, backoff_s: float = 5.0) -> list[NewsItem]
  - _normalise_item(raw: dict) -> NewsItem | None  (dispatches on 'content' vs 'uuid' key; computes title_hash; validates URL scheme)
  - _normalise_post_055(raw) / _normalise_pre_055(raw)
  - _normalise_title_for_hash(title: str) -> str  (lowercase + strip + collapse internal whitespace; stable input to hashlib)
  - _compute_title_hash(title: str) -> str  (sha256 of normalised title, hex-truncated to 16)
  - _validate_url_scheme(url: str) -> str  (return url if scheme in {'https','http'} else '')
  - _is_valid_market_id(market_id: str) -> bool  (allowlist gate)
  - _cache_path(market_id) -> Path  (raises ValueError if market_id not in allowlist)
  - _load_cache(path) -> list[NewsItem] | None  (TTL = JSON envelope date field == today)
  - _write_cache(path, data) -> None
  - _get_yf()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests in tests/test_news_fetcher.py</name>
  <files>tests/test_news_fetcher.py</files>
  <read_first>
    - .planning/phases/38-news-integration/38-RESEARCH.md §1 (schema details), §2 (cache), §5 (AST hex)
    - .planning/phases/38-news-integration/38-PATTERNS.md §news_fetcher.py + §tests/test_news_fetcher.py
    - tests/fixtures/news/news_fixture_pre055.json + news_fixture_post055.json (Plan 01)
    - tests/test_signal_engine.py (FORBIDDEN_MODULES_NEWS_FETCHER definition)
    - data_fetcher.py (lazy _get_yf pattern, retry loop)
    - system_params.py (locate VALID_MARKETS or document fallback)
  </read_first>
  <behavior>
    NewsItem shape and title_hash:
    - test_newsitem_includes_title_hash_field: import NewsItem; assert 'title_hash' in NewsItem.__annotations__ and NewsItem.__annotations__['title_hash'] is str
    - test_title_hash_is_16_char_hex: any normalised item has title_hash matching r'^[0-9a-f]{16}$'
    - test_title_hash_is_stable_across_whitespace_and_case: _compute_title_hash('  RBA Cuts Rates  ') == _compute_title_hash('rba cuts rates') == _compute_title_hash('RBA cuts  rates') (leading/trailing strip + lowercase + collapse internal whitespace)
    - test_title_hash_distinguishes_different_titles: _compute_title_hash('RBA cuts rates') != _compute_title_hash('Fed cuts rates')

    Schema normalisation:
    - test_normalise_post055_shape: load post055 fixture, call _normalise_item on each, assert NewsItem has non-empty title, title_hash is 16-hex, url is valid scheme or '', publisher matches content.provider.displayName, pub_date matches content.pubDate.
    - test_normalise_pre055_shape: load pre055 fixture, call _normalise_item on each, assert NewsItem with title from raw['title'], url from raw['link'] (scheme-validated), publisher from raw['publisher'], pub_date is ISO 8601 string converted from unix providerPublishTime, title_hash present.
    - test_normalise_returns_none_on_missing_title: `{'content': {'title': ''}}` and `{'uuid': 'x', 'title': ''}` and `{}` all return None.
    - test_normalise_dispatch_unknown_shape: `{'foo': 'bar'}` returns None.
    - test_title_hash_dedup: pass two raw items with identical titles via fetch_news (mocked yf); result list contains only one entry for that title (dedup keyed by title_hash).

    URL scheme validation (defence-in-depth):
    - test_url_scheme_https_accepted: _validate_url_scheme('https://example.com/x') == 'https://example.com/x'
    - test_url_scheme_http_accepted: _validate_url_scheme('http://example.com/x') == 'http://example.com/x'
    - test_url_scheme_javascript_rejected: _validate_url_scheme('javascript:alert(1)') == ''
    - test_url_scheme_data_rejected: _validate_url_scheme('data:text/html,<script>alert(1)</script>') == ''
    - test_url_scheme_relative_rejected: _validate_url_scheme('/relative/path') == ''
    - test_url_scheme_empty_rejected: _validate_url_scheme('') == ''
    - test_normalise_strips_javascript_url: post055 item with clickThroughUrl={'url': 'javascript:alert(1)'} → NewsItem.url == '' (not the raw javascript: string)

    market_id allowlist (path-traversal closed):
    - test_cache_path_rejects_unknown_market: _cache_path('../../etc/passwd') raises ValueError (or fetch_news returns []) — assert no file write under repo root for unknown market
    - test_cache_path_rejects_traversal_chars: _cache_path('SPI200/../OTHER') raises ValueError
    - test_fetch_news_unknown_market_returns_empty_no_fetch: monkeypatch _get_yf to raise; fetch_news('UNKNOWN_MARKET', 'XXX') returns [] without invoking yfinance

    Cache behaviour (JSON date field is authoritative TTL):
    - test_cache_hit_uses_json_date_field_not_mtime: pre-write sidecar with today's date in JSON; manipulate filesystem mtime to be 30 days old via os.utime; monkeypatch _get_yf to raise AssertionError if called; assert fetch_news returns cached entries (proves mtime is ignored, JSON date controls).
    - test_cache_stale_when_json_date_not_today: pre-write sidecar with date '2020-01-01' but recent mtime; mock yfinance; fetch_news refetches and rewrites the cache with today's date.
    - test_cache_load_envelope_returns_headlines_list: _load_cache on a valid envelope `{'date': today, 'headlines': [<NewsItem>...]}` returns the headlines list (unpacked from envelope), not the envelope dict.
    - test_cache_miss_writes_envelope_with_date_key: ensure no sidecar present; mock _get_yf().Ticker(...).news to return post055-shape items; call fetch_news; sidecar file json.loads to a dict with keys {'date', 'headlines'} where date == today's ISO date.
    - test_cache_write_is_atomic: monkeypatch os.replace to raise after tempfile is written; assert original sidecar (if any) is untouched and no .tmp file lingers.
    - test_cache_corrupt_json_returns_none_and_refetches: write garbage to sidecar; _load_cache returns None; fetch_news successfully refetches.

    XSS / SSRF:
    - test_xss_headline_survives_normaliser: include `{'content': {'title': '<script>alert(1)</script>', 'pubDate': '...', 'provider': {'displayName': 'p'}, 'canonicalUrl': {'url': 'https://x'}}}`; _normalise_item returns NewsItem whose 'title' is EXACTLY `<script>alert(1)</script>`.
    - test_no_server_side_url_prefetch: AST-walk news_fetcher.py source; assert no call to requests.get/post, urllib.request.urlopen, httpx.*, etc.
    - test_news_fetcher_no_forbidden_imports: AST-walk news_fetcher.py against FORBIDDEN_MODULES_NEWS_FETCHER.

    Network resilience:
    - test_fetch_news_returns_empty_on_retries_exhausted: mock to raise ReadTimeout every call; fetch_news returns [] after `retries` attempts.

    URL fallback chain:
    - test_clickthrough_url_none_falls_back_to_canonical: post055 item with clickThroughUrl=None and canonicalUrl={'url': 'https://canonical'}; url == 'https://canonical'.
    - test_clickthrough_and_canonical_missing_yields_empty_url: both absent → url == ''.
  </behavior>
  <action>
    Create tests/test_news_fetcher.py. 2-space indent. Use pytest fixtures + monkeypatch + tmp_path. For sidecar tests, use `monkeypatch.chdir(tmp_path)` so news_cache_*.json writes land in tmp dir. Build a small FakeTicker class whose `.news` attribute returns a list provided by the test; monkeypatch `news_fetcher._get_yf` to return an object exposing `Ticker(symbol)` returning the FakeTicker.

    AST tests follow the test_signal_engine.py pattern.

    Run pytest tests/test_news_fetcher.py and confirm it collects RED (ModuleNotFoundError for news_fetcher).
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_news_fetcher.py 2>&1 | grep -q -E "(ModuleNotFoundError|ImportError|No module named 'news_fetcher')" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_news_fetcher.py` exits 0
    - `grep -c 'def test_newsitem_includes_title_hash_field' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_title_hash_is_stable_across_whitespace_and_case' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_cache_hit_uses_json_date_field_not_mtime' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_cache_load_envelope_returns_headlines_list' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_cache_path_rejects_unknown_market' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_url_scheme_javascript_rejected' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_normalise_strips_javascript_url' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_xss_headline_survives_normaliser' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_no_server_side_url_prefetch' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_news_fetcher_no_forbidden_imports' tests/test_news_fetcher.py` returns 1
    - `grep -c 'def test_title_hash_dedup' tests/test_news_fetcher.py` returns 1
    - Pytest collection on this file fails with ModuleNotFoundError news_fetcher (RED).
  </acceptance_criteria>
  <done>tests/test_news_fetcher.py exists with all named tests covering title_hash, JSON-date-TTL, market_id allowlist, URL scheme validation, XSS pass-through, and SSRF AST gate; pytest collection RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement news_fetcher.py to GREEN</name>
  <files>news_fetcher.py</files>
  <read_first>
    - tests/test_news_fetcher.py (just-written; contract source)
    - data_fetcher.py (full file — _get_yf, retry loop, narrow-catch pattern)
    - state_manager/io.py (_atomic_write_unlocked pattern lines 118–169)
    - .planning/phases/38-news-integration/38-RESEARCH.md §1 (schema dispatch), §2 (cache), §10 (file plan)
    - .planning/phases/38-news-integration/38-PATTERNS.md §news_fetcher.py
    - tests/test_signal_engine.py (FORBIDDEN_MODULES_NEWS_FETCHER list)
    - system_params.py (locate VALID_MARKETS or equivalent; if absent, define module-local frozenset)
  </read_first>
  <action>
    Create news_fetcher.py (≤500 LOC, 2-space indent).

    Allowed imports (verify against FORBIDDEN_MODULES_NEWS_FETCHER):
    - stdlib: hashlib, json, logging, os, re, tempfile, time
    - typing: TypedDict
    - from datetime: UTC, datetime, date
    - from pathlib: Path
    - from system_params: HTTP_TIMEOUT_S (if used); VALID_MARKETS if it exists, else define a module-local `_VALID_MARKETS = frozenset({'SPI200', 'AUDUSD'})`
    - requests.exceptions (ReadTimeout, ConnectionError)
    yfinance is imported lazily inside _get_yf().

    Module structure:

    1. Module docstring naming Phase 38, NEWS-01 + NEWS-03, hex-role.

    2. `class NewsItem(TypedDict)`: title (str), url (str), publisher (str), pub_date (str), title_hash (str).
       title_hash is REQUIRED (not Optional); _normalise_item always populates it.

    3. `_yf = None` module-level memoization cell; `def _get_yf()` returns memoized yfinance module after local `import yfinance as yf_` on first call.

    4. `_RETRY_EXCEPTIONS = (requests.exceptions.ReadTimeout, requests.exceptions.ConnectionError)`.

    5. `_VALID_MARKETS` allowlist (from system_params if available, else module-local frozenset).

    6. `_is_valid_market_id(market_id: str) -> bool`:
       Return True iff market_id is in _VALID_MARKETS AND matches `re.fullmatch(r'[A-Z0-9_]{2,16}', market_id)` (belt-and-braces: no path separators, no relative-path chars, no dots).

    7. `_TITLE_WS_RE = re.compile(r'\s+')`; `_normalise_title_for_hash(title: str) -> str`:
       Return `_TITLE_WS_RE.sub(' ', title.strip().lower())`. Stable across leading/trailing whitespace, internal whitespace collapsing, and case differences.

    8. `_compute_title_hash(title: str) -> str`:
       Return `hashlib.sha256(_normalise_title_for_hash(title).encode('utf-8')).hexdigest()[:16]`.

    9. `_ALLOWED_URL_SCHEMES = ('https://', 'http://')`; `_validate_url_scheme(url: str) -> str`:
       If not isinstance(url, str) or not url: return ''. If url.startswith(_ALLOWED_URL_SCHEMES): return url. Else return '' (rejects javascript:, data:, mailto:, relative, etc.).

    10. `_normalise_item(raw: dict) -> NewsItem | None`: dispatch on `'content' in raw` → `_normalise_post_055(raw)`; elif `'uuid' in raw` → `_normalise_pre_055(raw)`; else return None.

    11. `_normalise_post_055(raw)`:
        c = raw.get('content', {})
        title = c.get('title', '').strip()
        if not title: return None
        url_obj = c.get('clickThroughUrl') or c.get('canonicalUrl') or {}
        raw_url = url_obj.get('url', '') if isinstance(url_obj, dict) else ''
        url = _validate_url_scheme(raw_url)
        publisher = c.get('provider', {}).get('displayName', '')
        pub_date = c.get('pubDate', '')
        title_hash = _compute_title_hash(title)
        return NewsItem(title=title, url=url, publisher=publisher, pub_date=pub_date, title_hash=title_hash)
        CRITICAL: title stays verbatim (no html-escape at fetch layer); url is scheme-validated.

    12. `_normalise_pre_055(raw)`:
        title = raw.get('title', '').strip()
        if not title: return None
        url = _validate_url_scheme(raw.get('link', ''))
        publisher = raw.get('publisher', '')
        ts = raw.get('providerPublishTime', 0)
        pub_date = datetime.fromtimestamp(ts, UTC).strftime('%Y-%m-%dT%H:%M:%SZ') if ts else ''
        title_hash = _compute_title_hash(title)
        return NewsItem(title=title, url=url, publisher=publisher, pub_date=pub_date, title_hash=title_hash)

    13. `_cache_path(market_id) -> Path`:
        if not _is_valid_market_id(market_id): raise ValueError(f"invalid market_id: {market_id!r}")
        return Path(f'news_cache_{market_id}.json')

    14. `_load_cache(path) -> list[NewsItem] | None`:
        Open + json.load.
        envelope = json.load(...)
        if not isinstance(envelope, dict): return None
        if envelope.get('date') != date.today().isoformat(): return None  # JSON date is authoritative TTL, NOT mtime
        headlines = envelope.get('headlines')
        if not isinstance(headlines, list): return None
        return headlines
        Catch FileNotFoundError, json.JSONDecodeError, OSError, KeyError, TypeError → return None.

    15. `_write_cache(path, data)`:
        atomic tempfile + os.replace mirroring state_manager/io.py::_atomic_write_unlocked.
        `data` is the envelope dict `{'date': date.today().isoformat(), 'headlines': [...]}`.

    16. `fetch_news(market_id, symbol, max_items=5, retries=3, backoff_s=5.0) -> list[NewsItem]`:
        - if not _is_valid_market_id(market_id): _LOGGER.warning("fetch_news rejected unknown market_id=%r", market_id); return []
        - cache_path = _cache_path(market_id)
        - cached = _load_cache(cache_path); if cached is not None: return cached[:max_items]
        - Retry loop: yf_mod = _get_yf(); ticker = yf_mod.Ticker(symbol); raw_items = ticker.news or []; normalised = [item for item in (_normalise_item(r) for r in raw_items) if item is not None]
        - Dedup by title_hash preserving first occurrence (use seen-set)
        - items = items[:max_items]
        - _write_cache(cache_path, {'date': date.today().isoformat(), 'headlines': items})
        - Narrow-catch on _RETRY_EXCEPTIONS + broad Exception → warn + continue; after retries exhausted log error + return [].

    17. NO server-side fetch of any headline URL anywhere.

    Run pytest. If GREEN, commit.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_news_fetcher.py tests/test_signal_engine.py::TestDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - `test -f news_fetcher.py` exits 0
    - `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('news_fetcher.py').read_text()); mods=set(); [mods.add(a.name.split('.')[0]) for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names]; [mods.add(n.module.split('.')[0]) for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module]; forbidden = mods & {'signal_engine','sizing_engine','state_manager','notifier','dashboard','main','numpy','schedule','dotenv','pytz'}; assert not forbidden, f'forbidden imports: {forbidden}'"` exits 0
    - `grep -c '^def fetch_news' news_fetcher.py` returns 1
    - `grep -c '^def _normalise_item' news_fetcher.py` returns 1
    - `grep -c '^def _get_yf' news_fetcher.py` returns 1
    - `grep -c '^def _compute_title_hash' news_fetcher.py` returns 1
    - `grep -c '^def _validate_url_scheme' news_fetcher.py` returns 1
    - `grep -c '^def _is_valid_market_id' news_fetcher.py` returns 1
    - `grep -c "title_hash: str" news_fetcher.py` returns ≥1
    - `grep -c 'tempfile.NamedTemporaryFile' news_fetcher.py` returns ≥1
    - `grep -c 'os.replace' news_fetcher.py` returns ≥1
    - `grep -c "envelope.get('date')" news_fetcher.py` returns ≥1 (JSON date TTL check, not mtime)
    - `grep -E '(os\.path\.getmtime|\.stat\(\)\.st_mtime|st_mtime)' news_fetcher.py | grep -v '^#'` is empty (mtime is NOT used)
    - `grep -E '(requests\.get|requests\.post|urllib\.request\.urlopen|httpx\.|\.get_info\(\))' news_fetcher.py | grep -v '^#'` is empty (no server-side prefetch)
    - `python -c "from news_fetcher import _compute_title_hash; assert _compute_title_hash('  RBA Cuts Rates  ') == _compute_title_hash('rba cuts  rates'), 'title_hash must be stable across whitespace/case'"` exits 0
    - `python -c "from news_fetcher import _validate_url_scheme; assert _validate_url_scheme('javascript:alert(1)') == '' and _validate_url_scheme('https://x') == 'https://x' and _validate_url_scheme('/relative') == ''"` exits 0
    - `python -c "from news_fetcher import _cache_path; import pytest; \ntry:\n  _cache_path('../etc/passwd')\n  raise SystemExit('should have raised')\nexcept ValueError:\n  pass"` exits 0
    - `.venv/bin/pytest -x --tb=short tests/test_news_fetcher.py` exits 0
    - `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism` exits 0
    - `wc -l news_fetcher.py` ≤500
  </acceptance_criteria>
  <done>news_fetcher.py implements fetch_news + both-schema normaliser with NewsItem.title_hash + JSON-date-field TTL + market_id allowlist gate + URL scheme validation; all tests green; AST hex guard green; XSS payload passes through untouched; SSRF closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance response → _normalise_item | untrusted external content (publisher-supplied) |
| _normalise_item output → cache file | repo-local persistence |
| cache file → _load_cache | trusted (repo-local), but JSON parse errors handled gracefully |
| route path param market_id → _cache_path | untrusted path component — allowlist gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Tampering | XSS via headline title | mitigate | Title preserved verbatim at fetch layer (no escape); render-time html.escape() in Plan 04 |
| T-38-03-02 | Tampering / Elevation | SSRF via headline URL | mitigate | news_fetcher.py performs ZERO HTTP calls beyond yfinance.Ticker.news. AST gate test_no_server_side_url_prefetch enforces |
| T-38-03-03 | Tampering | Client-side XSS via javascript:/data: URL in anchor href | mitigate | _validate_url_scheme rejects schemes outside {'https','http'} at fetch layer (defence-in-depth; Plan 04 also html.escapes the href) |
| T-38-03-04 | Tampering / Elevation | Path traversal via crafted market_id (e.g. '../../etc/passwd') | mitigate | _is_valid_market_id gate against VALID_MARKETS allowlist + regex `[A-Z0-9_]{2,16}` BEFORE Path construction; _cache_path raises ValueError on miss; fetch_news returns [] without writing |
| T-38-03-05 | DoS | Hung yfinance call | mitigate | _RETRY_EXCEPTIONS narrow-catch + backoff_s sleep |
| T-38-03-06 | Information Disclosure | Cache file shared across users | accept | NEWS-01 explicit contract: "one fetch per market per day shared across users" |
| T-38-03-07 | Tampering | Cache poisoning via rogue yfinance fields | mitigate | _normalise_item only persists typed NewsItem fields; unknown raw keys discarded |
| T-38-03-08 | Tampering | Concurrent cache write corruption | mitigate | tempfile + os.replace atomic write |
| T-38-03-09 | Tampering | Stale-cache served past midnight due to mtime drift | mitigate | TTL check uses JSON envelope `date` field (set explicitly to date.today().isoformat() at write time); mtime is NOT consulted, eliminating timezone/restart edge cases |
</threat_model>

<verification>
1. `.venv/bin/pytest -x --tb=short tests/test_news_fetcher.py` exits 0.
2. `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism` exits 0.
3. `.venv/bin/pytest -x --tb=short` full-suite exits 0.
4. No `requests.get/post`, `urllib.request.urlopen`, `httpx.*`, or `.get_info()` in news_fetcher.py.
5. No `st_mtime` / `os.path.getmtime` references — JSON date field is sole TTL authority.
6. NewsItem.title_hash field is part of the TypedDict and populated by _normalise_item.
</verification>

<success_criteria>
- news_fetcher.py provides fetch_news with both-schema normalisation, dedup-by-title_hash, max-5 slice, atomic daily sidecar cache.
- NewsItem TypedDict includes title_hash: str (consumed by Plan 04 for dismiss-by-hash).
- Cache TTL is the JSON envelope `date` field; filesystem mtime is never consulted.
- market_id is validated against an allowlist before any cache-path construction (path-traversal closed).
- Outbound URL scheme is validated at fetch layer (javascript:/data:/relative rejected).
- XSS test payload survives the fetch layer (escape is render-time-only).
- Zero SSRF surface.
- AST hex boundary green.
</success_criteria>

<output>
After completion, create `.planning/phases/38-news-integration/38-03-SUMMARY.md` summarising: NewsItem TypedDict shape (including title_hash), title-hash normalisation rules, schema dispatch logic, JSON-date-field TTL semantics, market_id allowlist gate, URL scheme validation, atomic-write pattern source, and the no-prefetch SSRF assertion.
</output>
</content>
