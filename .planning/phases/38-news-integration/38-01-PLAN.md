---
phase: 38-news-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - system_params.py
  - tests/test_signal_engine.py
  - tests/fixtures/news/news_fixture_pre055.json
  - tests/fixtures/news/news_fixture_post055.json
  - tests/fixtures/news/news_classifier_30.json
autonomous: true
requirements: [NEWS-02, NEWS-03]
must_haves:
  truths:
    - "system_params.py exports NEWS_KEYWORDS_SPI200, NEWS_KEYWORDS_AUDUSD, NEWS_DAMPENER_ALLOWLIST as tuple[str,...]"
    - "AST guard blocks news_fetcher/news_filter from hex modules and registers news_filter.py in _HEX_PATHS_STDLIB_ONLY"
    - "Hard existence meta-test asserts news_filter.py and news_fetcher.py are present on disk after Wave 2 lands"
    - "Three test fixtures exist on disk and parse as valid JSON"
  artifacts:
    - path: "system_params.py"
      provides: "NEWS_KEYWORDS_SPI200, NEWS_KEYWORDS_AUDUSD, NEWS_DAMPENER_ALLOWLIST"
      contains: "NEWS_KEYWORDS_SPI200"
    - path: "tests/test_signal_engine.py"
      provides: "NEWS_FILTER_PATH constant + extended _HEX_PATHS_STDLIB_ONLY + FORBIDDEN_MODULES_NEWS_FETCHER + test_news_modules_exist_after_wave2"
      contains: "NEWS_FILTER_PATH"
    - path: "tests/fixtures/news/news_fixture_pre055.json"
      provides: "5-item pre-0.2.55 flat schema fixture"
    - path: "tests/fixtures/news/news_fixture_post055.json"
      provides: "5-item post-0.2.55 nested schema fixture"
    - path: "tests/fixtures/news/news_classifier_30.json"
      provides: "30 labelled headlines for precision/recall gate"
  key_links:
    - from: "tests/test_signal_engine.py"
      to: "news_filter.py + news_fetcher.py (paths)"
      via: "_HEX_PATHS_STDLIB_ONLY + FORBIDDEN_MODULES_NEWS_FETCHER constants + test_news_modules_exist_after_wave2 hard-existence gate"
      pattern: "NEWS_FILTER_PATH"
---

<objective>
Land the shared foundation that downstream Plan 02 (news_filter) and Plan 03 (news_fetcher) consume: keyword constants in system_params.py (D-06), three test fixtures (pre-/post-0.2.55 schemas + 30-headline labelled set), and the AST hex-boundary extensions that structurally forbid news_fetcher/news_filter from leaking into pure-math hex (OPS-03 already wired but news_filter must be added to _HEX_PATHS_STDLIB_ONLY).

Purpose: Wave 0 substrate for NEWS-02 (classifier) and NEWS-03 (both-schema normaliser) so Plans 02 and 03 can run in parallel without scavenger-hunting for fixtures or constants.
Output: system_params.py + tests/test_signal_engine.py modifications + 3 fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-news-integration/38-CONTEXT.md
@.planning/phases/38-news-integration/38-RESEARCH.md
@.planning/phases/38-news-integration/38-PATTERNS.md

<interfaces>
From system_params.py (existing pattern, lines ~50–77, 100+):
  - Stdlib-only module — imports limited to re, decimal, typing
  - Section headers `# === Phase N constants — ... ===` group related constants
  - Tuple constants typed as `tuple[str, ...]`

From tests/test_signal_engine.py (lines 495–624):
  - FORBIDDEN_MODULES frozenset already contains 'web', 'news_fetcher', 'news_filter', 'auth_store'
  - _HEX_PATHS_STDLIB_ONLY = [*SIZING_ENGINE_PKG_FILES, SYSTEM_PARAMS_PATH, PNL_ENGINE_PATH, ALERT_ENGINE_PATH]
  - FORBIDDEN_MODULES_DATA_FETCHER pattern at lines 537–546 — model for FORBIDDEN_MODULES_NEWS_FETCHER

Yfinance schemas (RESEARCH.md §1):
  - post-0.2.55: top-level keys 'id', 'content'; content has title, pubDate, provider.displayName, clickThroughUrl.url or canonicalUrl.url
  - pre-0.2.55: flat 'uuid', 'title', 'publisher', 'link', 'providerPublishTime' (unix ts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NEWS_* keyword constants to system_params.py</name>
  <files>system_params.py</files>
  <read_first>
    - system_params.py (current contents — locate end of file or appropriate section grouping)
    - .planning/phases/38-news-integration/38-RESEARCH.md §6 (Keyword Classifier) and §7 (system_params.py Pattern) for exact tuple contents
    - .planning/phases/38-news-integration/38-PATTERNS.md §system_params.py for header + tuple format
    - CLAUDE.md §Key Conventions (2-space indent, system_params single source of truth)
  </read_first>
  <action>
    Per D-06, append a new section to system_params.py with header `# === Phase 38 constants — news integration keyword classifier (D-06) ===`. Define three module-level constants typed as `tuple[str, ...]`:
    - NEWS_KEYWORDS_SPI200 — Australian/global systemic keywords (rba, reserve bank, rate cut, rate hike, interest rate, recession, gdp, inflation, cpi, stagflation, asx halt, trading halt, market halt, circuit breaker, crash, sell-off, rout, collapse, plunge, fed, federal reserve, ecb, bank of japan, tariff, trade war, sanctions, pandemic, lockdown)
    - NEWS_KEYWORDS_AUDUSD — AUD/USD drivers (rba, reserve bank, rate cut, rate hike, interest rate, aud, aussie dollar, australian dollar, china gdp, iron ore, commodity, terms of trade, fed, federal reserve, fomc, us cpi, us gdp, dollar, dxy, greenback, recession, stagflation, tariff, trade war, sanctions, pandemic, lockdown, geopolitical)
    - NEWS_DAMPENER_ALLOWLIST — false-positive suppressors (first-rate, second-rate, first rate, second rate, flat-rate, flat rate, pro-rate, pro rate, interest in, rate your, interest and)

    All entries lowercase. Use 2-space indent. Do NOT import re, json, datetime, or any non-stdlib module — system_params.py is in FORBIDDEN_MODULES_STDLIB_ONLY (only re/decimal/typing allowed).
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^NEWS_KEYWORDS_SPI200: tuple\[str, \.\.\.\] =' system_params.py` returns 1
    - `grep -c '^NEWS_KEYWORDS_AUDUSD: tuple\[str, \.\.\.\] =' system_params.py` returns 1
    - `grep -c '^NEWS_DAMPENER_ALLOWLIST: tuple\[str, \.\.\.\] =' system_params.py` returns 1
    - `python -c "import system_params; assert 'rba' in system_params.NEWS_KEYWORDS_SPI200 and 'first-rate' in system_params.NEWS_DAMPENER_ALLOWLIST"` exits 0
    - `python -c "import system_params; assert all(isinstance(x, str) and x == x.lower() for x in system_params.NEWS_KEYWORDS_SPI200 + system_params.NEWS_KEYWORDS_AUDUSD + system_params.NEWS_DAMPENER_ALLOWLIST)"` exits 0
    - AST hex test still green (existing test must continue passing)
  </acceptance_criteria>
  <done>Three keyword tuple constants present in system_params.py with lowercase string entries; stdlib-only import constraint preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/test_signal_engine.py AST boundary with NEWS_FILTER_PATH + FORBIDDEN_MODULES_NEWS_FETCHER + hard-existence meta-test</name>
  <files>tests/test_signal_engine.py</files>
  <read_first>
    - tests/test_signal_engine.py lines 480–650 (FORBIDDEN_MODULES + path constants + _HEX_PATHS_STDLIB_ONLY + FORBIDDEN_MODULES_DATA_FETCHER block)
    - .planning/phases/38-news-integration/38-RESEARCH.md §5 (AST Hex Boundary)
    - .planning/phases/38-news-integration/38-PATTERNS.md §tests/test_signal_engine.py
  </read_first>
  <action>
    Three structural extensions, two behaviour-preserving (a, b, c) and one new strict gate (d):

    (a) Near the existing path constants (e.g. directly after ALERT_ENGINE_PATH), declare `NEWS_FILTER_PATH = Path('news_filter.py')` and `NEWS_FETCHER_PATH = Path('news_fetcher.py')`.

    (b) Append NEWS_FILTER_PATH to the _HEX_PATHS_STDLIB_ONLY list so that — when Plan 02 lands news_filter.py — the existing AST guard runs the stdlib-only check against it. Resulting expression: `_HEX_PATHS_STDLIB_ONLY = [*SIZING_ENGINE_PKG_FILES, SYSTEM_PARAMS_PATH, PNL_ENGINE_PATH, ALERT_ENGINE_PATH, NEWS_FILTER_PATH]`.

    (c) Declare a new module-level frozenset `FORBIDDEN_MODULES_NEWS_FETCHER` mirroring FORBIDDEN_MODULES_DATA_FETCHER's role: blocks signal_engine, sizing_engine, state_manager, notifier, dashboard, main, numpy, schedule, dotenv, pytz from being imported by news_fetcher.py. Place it adjacent to FORBIDDEN_MODULES_DATA_FETCHER.

    Because news_filter.py and news_fetcher.py do not yet exist on disk when this plan runs (Wave 1), the existing AST guard test (`test_forbidden_imports_absent`) iterates over `_HEX_PATHS_ALL` / `_HEX_PATHS_STDLIB_ONLY`. To avoid a FileNotFoundError before Plans 02/03 land, the existing iteration loop must skip missing files (read source defensively with `if not path.exists(): continue`). If the current loop already does this, leave it as-is; otherwise add the guard. Do NOT loosen the assertion for files that DO exist. This skip-missing behaviour is the smallest change consistent with Wave-ordered execution.

    (d) Add a NEW strict-existence meta-test `test_news_modules_exist_after_wave2` inside TestDeterminism (or a sibling test class). This test hard-asserts that both `news_filter.py` and `news_fetcher.py` exist on disk:
      ```
      def test_news_modules_exist_after_wave2(self):
        '''After Wave 2 (Plans 02/03) lands, both news modules MUST exist.
        Compensates for the skip-missing guard in test_forbidden_imports_absent:
        the skip is needed for Wave-ordered execution, but this test ensures
        the skip cannot silently hide a missing module after the phase completes.
        '''
        assert NEWS_FILTER_PATH.exists(), (
          f'{NEWS_FILTER_PATH} must exist after Wave 2 lands (Plan 02)'
        )
        assert NEWS_FETCHER_PATH.exists(), (
          f'{NEWS_FETCHER_PATH} must exist after Wave 2 lands (Plan 03)'
        )
      ```
    This test is EXPECTED to RED during Wave 1 (this plan's wave) and GREEN once Plans 02 and 03 land. Mark it with `@pytest.mark.skipif` IF the orchestrator requires green-on-Wave-1 — but the recommended approach is to let it fail during Wave 1 and use it as the Wave 2 completion gate (the executor running Plan 02/03 will see it go green as their final wiring check). Document this behaviour in the test docstring.

    Alternative if the project policy forbids RED tests at any wave boundary: gate the asserts behind `if NEWS_FILTER_PATH.exists() or NEWS_FETCHER_PATH.exists()` — if EITHER file exists, BOTH must exist. This still catches the "Plan 02 landed but Plan 03 didn't" case the original checker concern flagged, without breaking Wave 1's green-suite invariant. Choose this variant if uncertain; document the choice in the test docstring.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^NEWS_FILTER_PATH = Path('news_filter.py')" tests/test_signal_engine.py` returns 1
    - `grep -c "^NEWS_FETCHER_PATH = Path('news_fetcher.py')" tests/test_signal_engine.py` returns 1
    - `grep -c 'NEWS_FILTER_PATH' tests/test_signal_engine.py` returns ≥ 2 (declaration + list inclusion)
    - `grep -c '^FORBIDDEN_MODULES_NEWS_FETCHER = frozenset' tests/test_signal_engine.py` returns 1
    - `grep -c 'def test_news_modules_exist_after_wave2' tests/test_signal_engine.py` returns 1
    - `python -c "import re,pathlib; s=pathlib.Path('tests/test_signal_engine.py').read_text(); assert 'NEWS_FILTER_PATH' in re.search(r'_HEX_PATHS_STDLIB_ONLY\s*=\s*\[[^\]]*\]', s).group(0)"` exits 0
    - `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism` exits 0 with news_filter.py absent (skip-missing guard keeps existing test green) AND test_news_modules_exist_after_wave2 either RED-by-design-pre-Wave-2 OR conditionally-asserts (per variant chosen in action step d). The Wave 2 plan executor MUST observe this test go green as their phase-completion gate.
  </acceptance_criteria>
  <done>NEWS_FILTER_PATH + NEWS_FETCHER_PATH path constants declared; _HEX_PATHS_STDLIB_ONLY includes NEWS_FILTER_PATH; FORBIDDEN_MODULES_NEWS_FETCHER frozenset declared; test_news_modules_exist_after_wave2 strict-existence meta-test added; existing TestDeterminism still green with target files absent (skip-missing guard preserved for Wave-ordered execution).</done>
</task>

<task type="auto">
  <name>Task 3: Create three test fixtures under tests/fixtures/news/</name>
  <files>tests/fixtures/news/news_fixture_pre055.json, tests/fixtures/news/news_fixture_post055.json, tests/fixtures/news/news_classifier_30.json</files>
  <read_first>
    - .planning/phases/38-news-integration/38-RESEARCH.md §1 (yfinance Schema Findings — exact key names for both shapes)
    - .planning/phases/38-news-integration/38-RESEARCH.md §6 (classifier precision/recall fixture strategy)
  </read_first>
  <action>
    Create directory `tests/fixtures/news/` if missing, then write three JSON files:

    (a) `news_fixture_pre055.json` — JSON array of 5 hand-crafted pre-0.2.55 flat-schema items. Each item is an object with keys: uuid (16-char hex-like string), title (non-empty string), publisher (string), link (https URL string), providerPublishTime (unix-timestamp int), type ("STORY"), thumbnail ({"resolutions": [{"url": "...", "width": 1194, "height": 432, "tag": "original"}]}), relatedTickers (list with one entry like "^AXJO" or "AUDUSD=X"). Items must collectively cover: (i) a benign title, (ii) a rate-decision title that the SPI200 classifier should flag, (iii) an "<script>alert(1)</script>" payload title to exercise XSS-escape downstream, (iv) a "first-rate service" title for dampener exercise, (v) any other benign headline.

    (b) `news_fixture_post055.json` — JSON array of 5 post-0.2.55 nested-schema items. Each item is an object with keys: id (uuid-string), content ({contentType: "STORY", title, description: "", summary, pubDate (ISO 8601 Z), displayTime (same), isHosted: true, bypassModal: false, previewUrl: null, thumbnail (same as pre shape), provider ({displayName, url, sourceId}), canonicalUrl ({url, site, region, lang}), clickThroughUrl (either {url,...} OR null on at least ONE item to exercise None-fallback), metadata: {editorsPick: false}, finance: {premiumFinance: {isPremiumNews: false, isPremiumFreeNews: false}}, storyline: null}). Cover the same five title themes as (a) so both fixtures share semantic content for parity tests.

    (c) `news_classifier_30.json` — JSON array of 30 labelled headlines, each object: {"title": str, "label": 0 or 1, "market": "SPI200" or "AUDUSD"}. Distribution: ~50/50 across markets, ~50/50 positive (label=1) and negative (label=0). Positive examples must include RBA rate decisions, fed/FOMC actions, market halts, tariff/trade-war news, recession/CPI events. Negative examples include routine earnings, penny-stock tips, mining-company quarterly updates, sport/lifestyle headlines, and dampener phrases ("first-rate service", "second-rate company"). The fixture must be solvable by the keyword sets defined in Task 1 to precision ≥0.7 and recall ≥0.9 (the classifier code in Plan 02 will be tuned against this fixture; if Plan 02 cannot meet the gate it tunes keywords in system_params.py, not the fixture).

    Files must be valid JSON (UTF-8) parseable by `json.loads`.
  </action>
  <verify>
    <automated>python -c "import json,pathlib; [json.loads(pathlib.Path(p).read_text()) for p in ['tests/fixtures/news/news_fixture_pre055.json','tests/fixtures/news/news_fixture_post055.json','tests/fixtures/news/news_classifier_30.json']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/news/news_fixture_pre055.json && test -f tests/fixtures/news/news_fixture_post055.json && test -f tests/fixtures/news/news_classifier_30.json` exits 0
    - `python -c "import json; d=json.load(open('tests/fixtures/news/news_fixture_pre055.json')); assert isinstance(d,list) and len(d)==5 and all('uuid' in x and 'title' in x and 'link' in x and 'publisher' in x and 'providerPublishTime' in x for x in d)"` exits 0
    - `python -c "import json; d=json.load(open('tests/fixtures/news/news_fixture_post055.json')); assert isinstance(d,list) and len(d)==5 and all('content' in x and 'id' in x for x in d) and any(x['content'].get('clickThroughUrl') is None for x in d)"` exits 0
    - `python -c "import json; d=json.load(open('tests/fixtures/news/news_classifier_30.json')); assert isinstance(d,list) and len(d)==30 and all(set(x.keys())>={'title','label','market'} and x['label'] in (0,1) and x['market'] in ('SPI200','AUDUSD') for x in d); pos=sum(1 for x in d if x['label']==1); assert 12 <= pos <= 20"` exits 0
    - `python -c "import json; d=json.load(open('tests/fixtures/news/news_fixture_pre055.json')); assert any('<script>' in x['title'] for x in d)"` exits 0
  </acceptance_criteria>
  <done>Three JSON fixtures exist, parse cleanly, and satisfy structural/coverage constraints; XSS payload + dampener phrase + clickThroughUrl=None edge cases represented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture → test loader | fixture JSON is repo-controlled (no external input here); risk = malformed JSON breaks downstream tests |
| system_params.py constants → news_filter.py | constants compiled into regex; risk = regex DoS if a keyword has unbounded backtracking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | system_params keyword tuples | accept | All entries are literal strings escaped via re.escape() in news_filter._build_pattern (Plan 02); no regex metacharacters reach the compiler |
| T-38-01-02 | DoS | regex compiled from NEWS_KEYWORDS_* | accept | re.escape() in Plan 02 prevents catastrophic backtracking; word-boundary anchors bound match length |
| T-38-01-03 | Tampering | fixture JSON content | accept | Fixtures are repo-controlled; XSS payload `<script>alert(1)</script>` is intentional test data exercised through Jinja2 autoescape (Plan 04) — never reaches an unescaped sink |
| T-38-01-04 | Information Disclosure | AST guard skipping missing files | mitigate | The skip only triggers for files that do not yet exist; once Plan 02/03 land their files, the guard runs strictly. Task 2(d) adds test_news_modules_exist_after_wave2 as a hard-existence meta-gate so the skip cannot silently hide a missing module after the phase completes. |
</threat_model>

<verification>
1. `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism` green (AST guard tolerates absent news_filter/news_fetcher files; news_filter.py is registered in stdlib-only list for later strict check).
2. `python -c "import system_params as s; assert hasattr(s,'NEWS_KEYWORDS_SPI200') and hasattr(s,'NEWS_KEYWORDS_AUDUSD') and hasattr(s,'NEWS_DAMPENER_ALLOWLIST')"` exits 0.
3. Three fixtures parse as valid JSON with required structural shape.
4. Full suite: `.venv/bin/pytest -x --tb=short` exits 0 (no regression).
5. After Plans 02 and 03 land, `pytest tests/test_signal_engine.py::TestDeterminism::test_news_modules_exist_after_wave2` exits 0 — the hard-existence gate fires.
</verification>

<success_criteria>
- system_params.py exports three lowercase keyword tuples per D-06.
- tests/test_signal_engine.py declares NEWS_FILTER_PATH + NEWS_FETCHER_PATH and includes NEWS_FILTER_PATH in _HEX_PATHS_STDLIB_ONLY; FORBIDDEN_MODULES_NEWS_FETCHER frozenset declared; test_news_modules_exist_after_wave2 hard-existence meta-test declared.
- Three JSON fixtures present at tests/fixtures/news/ and parse cleanly.
- Existing test suite remains green (Wave 0 plan does not regress anything).
</success_criteria>

<output>
After completion, create `.planning/phases/38-news-integration/38-01-SUMMARY.md` summarising: keyword set sizes, fixture item counts, the AST hex guard extension (NEWS_FILTER_PATH membership; FORBIDDEN_MODULES_NEWS_FETCHER ready for Plan 03), and the test_news_modules_exist_after_wave2 hard-existence meta-gate (which Plan 02/03 must turn green as their Wave 2 completion check).
</output>
</content>
</invoke>