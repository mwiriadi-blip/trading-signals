---
phase: 38-news-integration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - news_filter.py
  - tests/test_news_filter.py
autonomous: true
requirements: [NEWS-02]
must_haves:
  truths:
    - "classify_headline(text, market_id) returns bool; word-boundary keyword match, dampener-allowlist suppressed"
    - "has_critical_event(headlines: list[NewsItem], market_id) wraps classify_headline over a typed list of NewsItem"
    - "Unknown market_id logs a warning via stdlib logging and returns False"
    - "Precision ≥0.7 and recall ≥0.9 on tests/fixtures/news/news_classifier_30.json"
    - "news_filter.py imports only stdlib re + logging + system_params (passes _HEX_PATHS_STDLIB_ONLY AST guard)"
  artifacts:
    - path: "news_filter.py"
      provides: "classify_headline, has_critical_event"
      exports: ["classify_headline", "has_critical_event"]
    - path: "tests/test_news_filter.py"
      provides: "precision/recall gate + dampener + unknown-market tests + warning emission"
  key_links:
    - from: "news_filter.py"
      to: "system_params NEWS_KEYWORDS_SPI200/AUDUSD/DAMPENER_ALLOWLIST"
      via: "from system_params import NEWS_KEYWORDS_SPI200, NEWS_KEYWORDS_AUDUSD, NEWS_DAMPENER_ALLOWLIST"
      pattern: "from system_params import NEWS_KEYWORDS"
    - from: "tests/test_news_filter.py"
      to: "tests/fixtures/news/news_classifier_30.json"
      via: "json.load over fixture, iterate, compute precision/recall"
      pattern: "news_classifier_30.json"
---

<objective>
Implement the pure-math hex keyword classifier `news_filter.py` (NEWS-02) with the precision ≥0.7 / recall ≥0.9 gate enforced via the 30-headline labelled fixture (created by Plan 01). The module is stdlib-only (re + logging + system_params) and lands in `_HEX_PATHS_STDLIB_ONLY` (path constant added by Plan 01) so the existing AST guard tests it for forbidden imports.

NOTE on fixture: the 30-headline fixture is a sanity-check heuristic, not a statistically significant benchmark (±15–20% CI at 95% on 30 samples). It exists to catch grossly miscalibrated keyword sets and to make the recall/precision contract executable in CI. Real production tuning would require a labelled corpus 10×+ larger; that work is out of scope for Phase 38.

Purpose: NEWS-02 critical-event banner classifier. Single keyword match suffices (D-07), dampener allowlist suppresses common false positives (e.g. "first-rate" matching "rate").
Output: news_filter.py production module + tests/test_news_filter.py test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/38-news-integration/38-CONTEXT.md
@.planning/phases/38-news-integration/38-RESEARCH.md
@.planning/phases/38-news-integration/38-PATTERNS.md
@system_params.py
@tests/test_signal_engine.py

<interfaces>
From system_params.py (post Plan 01):
  - NEWS_KEYWORDS_SPI200: tuple[str, ...]
  - NEWS_KEYWORDS_AUDUSD: tuple[str, ...]
  - NEWS_DAMPENER_ALLOWLIST: tuple[str, ...]

From tests/test_signal_engine.py (post Plan 01):
  - NEWS_FILTER_PATH = Path('news_filter.py') in _HEX_PATHS_STDLIB_ONLY
  - FORBIDDEN_MODULES_STDLIB_ONLY frozenset already excludes os, json, datetime, yfinance, numpy, pandas, requests, etc.

From news_fetcher.py (Plan 03 — concurrent wave):
  - class NewsItem(TypedDict): title: str; url: str; publisher: str; pub_date: str; title_hash: str

Module contract (this plan defines):
  - classify_headline(text: str, market_id: str) -> bool
  - has_critical_event(headlines: list[NewsItem], market_id: str) -> bool
    (Typed against NewsItem TypedDict from Plan 03. Because Plan 02 lands in the same wave as Plan 03, use `from typing import Any` + structural typing — accept any object with a `.get('title', '')` interface to avoid an import-time hard dependency on news_fetcher. The TypedDict annotation is documentation-only.)
  - _MARKET_KEYWORDS: dict[str, tuple[str, ...]] (module-internal)
  - _PATTERNS: dict[str, re.Pattern] (module-internal, compiled at import)
  - _DAMPENER_RE: re.Pattern (module-internal, compiled at import)
  - _LOGGER = logging.getLogger(__name__) (module-internal)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests in tests/test_news_filter.py</name>
  <files>tests/test_news_filter.py</files>
  <read_first>
    - .planning/phases/38-news-integration/38-RESEARCH.md §6 (Classifier precision/recall test)
    - .planning/phases/38-news-integration/38-PATTERNS.md §tests/test_news_filter.py
    - tests/fixtures/news/news_classifier_30.json (created by Plan 01)
    - system_params.py (verify NEWS_KEYWORDS_* are present)
  </read_first>
  <behavior>
    - test_classify_headline_unknown_market_returns_false: classify_headline('RBA cuts rates', 'UNKNOWN_MKT') is False
    - test_classify_headline_unknown_market_logs_warning: use caplog at WARNING level; classify_headline('x', 'UNKNOWN_MKT'); assert at least one record contains 'UNKNOWN_MKT' and is at WARNING level
    - test_classify_headline_spi200_rate_hike_match: classify_headline('RBA hikes interest rate to 4.35%', 'SPI200') is True
    - test_classify_headline_audusd_fomc_match: classify_headline('Fed FOMC signals rate cut path', 'AUDUSD') is True
    - test_dampener_suppresses_first_rate: classify_headline('first-rate service from ASX broker', 'SPI200') is False
    - test_dampener_does_not_suppress_real_rate_news: classify_headline('first-rate service after RBA rate cut', 'SPI200') is True
    - test_classify_headline_empty_string_returns_false: classify_headline('', 'SPI200') is False
    - test_classify_headline_case_insensitive: classify_headline('RBA CUTS RATES', 'SPI200') and classify_headline('rba cuts rates', 'SPI200') both True
    - test_has_critical_event_any_match_fires: has_critical_event([{'title': 'sport results'}, {'title': 'RBA rate cut'}], 'SPI200') is True
    - test_has_critical_event_no_match_returns_false: has_critical_event([{'title': 'sport results'}, {'title': 'penny stock tip'}], 'SPI200') is False
    - test_has_critical_event_handles_missing_title_key: has_critical_event([{'no_title': 'x'}], 'SPI200') is False (no KeyError)
    - test_has_critical_event_accepts_newsitem_typed_dicts: pass list of dicts matching NewsItem shape (with title, url, publisher, pub_date, title_hash); function returns True if any title matches keyword
    - test_classifier_precision_recall: iterate tests/fixtures/news/news_classifier_30.json; compute tp/fp/fn; assert precision ≥0.7 AND recall ≥0.9 (D-07 target)
    - test_word_boundary_no_substring_match: classify_headline('integration testing news', 'SPI200') is False
  </behavior>
  <action>
    Create tests/test_news_filter.py. Use 2-space indent. Import via `from news_filter import classify_headline, has_critical_event` at top of file. Use pytest's `caplog` fixture for the warning-emission test. Fixture loader helper opens `tests/fixtures/news/news_classifier_30.json` via repo-relative path `pathlib.Path(__file__).parent / 'fixtures' / 'news' / 'news_classifier_30.json'`. Precision/recall test computes: tp = pred==1 AND label==1, fp = pred==1 AND label==0, fn = pred==0 AND label==1; precision = tp/(tp+fp) if (tp+fp) else 1.0; recall = tp/(tp+fn) if (tp+fn) else 0.0; assert precision >= 0.7 and recall >= 0.9 with informative assertion messages. Document fixture as "30-headline sanity-check, not statistically significant" in a module docstring comment. Run the test file and confirm it fails because news_filter.py does not yet exist (ImportError/ModuleNotFoundError).
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_news_filter.py 2>&1 | grep -q -E "(ModuleNotFoundError|ImportError|No module named)" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_news_filter.py` exits 0
    - `grep -c 'def test_classifier_precision_recall' tests/test_news_filter.py` returns 1
    - `grep -c 'def test_classify_headline_unknown_market_logs_warning' tests/test_news_filter.py` returns 1
    - `grep -c 'def test_dampener_suppresses_first_rate' tests/test_news_filter.py` returns 1
    - `grep -c 'def test_has_critical_event_any_match_fires' tests/test_news_filter.py` returns 1
    - `grep -c 'def test_has_critical_event_accepts_newsitem_typed_dicts' tests/test_news_filter.py` returns 1
    - `grep -c 'precision >= 0.7' tests/test_news_filter.py` returns 1
    - `grep -c 'recall >= 0.9' tests/test_news_filter.py` returns 1
    - `grep -c 'caplog' tests/test_news_filter.py` returns ≥1
    - `grep -c -i 'sanity' tests/test_news_filter.py` returns ≥1 (fixture documented as sanity-check)
    - Pytest collection fails with ModuleNotFoundError for news_filter (RED gate)
  </acceptance_criteria>
  <done>tests/test_news_filter.py exists with all 14 named tests; pytest collection RED because news_filter.py absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement news_filter.py to GREEN</name>
  <files>news_filter.py</files>
  <read_first>
    - tests/test_news_filter.py (just-written tests; behaviour contract)
    - .planning/phases/38-news-integration/38-RESEARCH.md §6 (classifier implementation pattern)
    - .planning/phases/38-news-integration/38-PATTERNS.md §news_filter.py
    - system_params.py (verify NEWS_KEYWORDS_* exports)
    - tests/test_signal_engine.py (FORBIDDEN_MODULES_STDLIB_ONLY constraint — verify `logging` is permitted)
  </read_first>
  <action>
    Create news_filter.py with these production-grade properties (2-space indent, file ≤500 LOC):

    Imports limited to:
    - `import re`
    - `import logging`
    - `from system_params import NEWS_KEYWORDS_SPI200, NEWS_KEYWORDS_AUDUSD, NEWS_DAMPENER_ALLOWLIST`

    NO datetime, os, json, yfinance, requests, numpy, pandas — any of these break the AST hex guard. `logging` is stdlib and not in FORBIDDEN_MODULES_STDLIB_ONLY (verify before implementing).

    Module structure:
    - Module docstring naming Phase 38, NEWS-02, hex-eligibility, FORBIDDEN imports list.
    - `_LOGGER = logging.getLogger(__name__)`
    - `_MARKET_KEYWORDS: dict[str, tuple[str, ...]] = {'SPI200': NEWS_KEYWORDS_SPI200, 'AUDUSD': NEWS_KEYWORDS_AUDUSD}`.
    - `_build_pattern(keywords) -> re.Pattern`: joins re.escape(kw) with `\b` anchors on both sides, IGNORECASE flag.
    - Module-level compiled `_PATTERNS = {m: _build_pattern(kws) for m, kws in _MARKET_KEYWORDS.items()}`.
    - Module-level compiled `_DAMPENER_RE = re.compile('|'.join(re.escape(d) for d in NEWS_DAMPENER_ALLOWLIST), re.IGNORECASE)`.
    - Public `classify_headline(text: str, market_id: str) -> bool`:
       1. `pat = _PATTERNS.get(market_id)`; if pat is None: `_LOGGER.warning("classify_headline received unknown market_id=%r; returning False", market_id)`; return False.
       2. Lowercase the text once.
       3. Fast path: if dampener regex does not match, return `bool(pat.search(text_lower))`.
       4. Slow path: substitute dampener spans with empty string and re-run keyword pattern on the scrubbed text.
    - Public `has_critical_event(headlines, market_id) -> bool`:
       Annotation: `headlines: list[dict]` is acceptable for runtime; docstring states "list[NewsItem] from news_fetcher; structurally any object with .get('title', '') works". Iterate headlines, call `classify_headline(h.get('title', ''), market_id)`, return True on first hit.

    If precision/recall gate fails: do NOT alter the test. Tune NEWS_KEYWORDS_* in system_params.py until the gate passes.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_news_filter.py tests/test_signal_engine.py::TestDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - `test -f news_filter.py` exits 0
    - `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('news_filter.py').read_text()); mods=set(); [mods.add(a.name.split('.')[0]) for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names]; [mods.add(n.module.split('.')[0]) for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module]; assert mods <= {'re','logging','system_params'}, f'forbidden imports: {mods - {\"re\",\"logging\",\"system_params\"}}'"` exits 0
    - `grep -c '_LOGGER.warning' news_filter.py` returns ≥1
    - `python -c "from news_filter import classify_headline, has_critical_event; assert classify_headline('RBA cuts interest rate', 'SPI200') is True; assert classify_headline('first-rate broker service', 'SPI200') is False; assert classify_headline('x', 'UNKNOWN') is False"` exits 0
    - `.venv/bin/pytest -x --tb=short tests/test_news_filter.py` exits 0 (all tests green including precision/recall gate)
    - `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent` exits 0
    - `wc -l news_filter.py` reports ≤500
  </acceptance_criteria>
  <done>news_filter.py implements classify_headline + has_critical_event with stdlib-only imports; warning emitted on unknown market_id; all tests green; precision ≥0.7 + recall ≥0.9 satisfied; AST hex guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headline text (untrusted external) → classify_headline | text originates from yfinance; passes through normaliser (Plan 03) before reaching classifier |
| system_params constants → compiled regex | constants are repo-controlled; escaped via re.escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | DoS | regex search on untrusted headline text | mitigate | re.escape() on every keyword; word-boundary anchors prevent unbounded matches; headline length is bounded by yfinance payload (~200 chars) |
| T-38-02-02 | Tampering | Pattern matching false positives | accept | Banner copy says "operator review recommended" (D-07); precision ≥0.7 gate validates FP rate is bounded |
| T-38-02-03 | Information Disclosure | Cross-market keyword leakage | accept | _MARKET_KEYWORDS lookup is exact-key; unknown market_id returns False (with warning); no fallback to wrong market |
</threat_model>

<verification>
1. `.venv/bin/pytest -x --tb=short tests/test_news_filter.py` exits 0.
2. `.venv/bin/pytest -x --tb=short tests/test_signal_engine.py::TestDeterminism` exits 0.
3. `.venv/bin/pytest -x --tb=short` full-suite exits 0.
4. AST inspection of news_filter.py confirms imports ⊆ {re, logging, system_params}.
</verification>

<success_criteria>
- news_filter.py exposes classify_headline + has_critical_event with documented signatures.
- Unknown market_id emits stdlib logging.WARNING and returns False.
- Word-boundary regex + dampener allowlist deliver precision ≥0.7 and recall ≥0.9 on 30-headline sanity-check fixture.
- Module is stdlib-only and passes the existing AST hex boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/38-news-integration/38-02-SUMMARY.md` summarising: classifier signature, precision/recall achieved (with sanity-check caveat), dampener behaviour, warning emission contract, and any keyword tuning applied to system_params.py to hit the gate.
</output>
</content>
