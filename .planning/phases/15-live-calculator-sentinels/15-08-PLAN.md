---
phase: 15
plan: 08
type: execute
wave: 4
depends_on:
  - 15-04
  - 15-05
  - 15-06
  - 15-07
files_modified:
  - tests/fixtures/dashboard/sample_state.json
  - tests/fixtures/dashboard/golden.html
  - tests/fixtures/notifier/golden.html
autonomous: false
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - SENTINEL-01
  - SENTINEL-02
  - SENTINEL-03
tags:
  - phase15
  - golden-fixtures
  - phase-gate
  - human-verify

must_haves:
  truths:
    - 'tests/fixtures/dashboard/sample_state.json includes at least one open position AND at least one drift warning so the regenerated golden.html exercises Phase 15 surfaces'
    - 'tests/fixtures/dashboard/golden.html regenerated by tests/regenerate_dashboard_golden.py contains the calc-row markup, the side-by-side stop cell (when applicable), and the drift banner'
    - 'tests/fixtures/notifier/golden*.html regenerated by tests/regenerate_notifier_golden.py contains the drift banner block between corrupt-reset and hero card'
    - 'All golden-comparison tests pass after regeneration (no diff vs newly-saved golden)'
    - 'Full test suite green'
    - 'Operator (human) confirms forward-look HTMX UX feels smooth (no layout shift, no flash) on a real browser hitting a running dev server'
    - 'Operator confirms drift banner renders correctly in real Gmail when test email is sent'
  artifacts:
    - path: 'tests/fixtures/dashboard/sample_state.json'
      provides: 'sample state with open SPI200 LONG + open AUDUSD SHORT (with manual_stop on one) + a drift warning so the golden exercises calc-row, side-by-side, and drift banner'
      contains: '"source": "drift"'
    - path: 'tests/fixtures/dashboard/golden.html'
      provides: 'regenerated golden including Phase 15 calc-row, side-by-side stop, drift banner'
      contains: 'class="calc-row"'
    - path: 'tests/fixtures/notifier/golden.html'
      provides: 'regenerated golden including the drift banner block in the email header'
      contains: '━━━ Drift detected ━━━'
  key_links:
    - from: 'tests/fixtures/dashboard/sample_state.json'
      to: 'tests/regenerate_dashboard_golden.py + tests/fixtures/dashboard/golden.html'
      via: 'regeneration script reads sample_state.json and writes golden.html'
      pattern: 'regenerate_dashboard_golden'
---

<objective>
Phase 15 final regression gate. Two outcomes:

1. Update the golden fixtures (`tests/fixtures/dashboard/sample_state.json`, `tests/fixtures/dashboard/golden.html`, `tests/fixtures/notifier/golden*.html`) to reflect Phase 15 rendering surfaces (calc-rows, side-by-side stop cell, drift banner). The sample state must be enriched so the regenerated goldens actually EXERCISE the new code paths — at minimum: one open position with manual_stop set (triggers side-by-side cell), one open position without (triggers single-cell baseline), one or more drift warnings (triggers the banner).

2. Operator-verifiable acceptance — two human-verify checkpoints to confirm the polish surfaces:
   - Browser-side forward-look HTMX UX feels smooth (typed Z → projected W in <500ms, no layout shift)
   - Email banner renders correctly in real Gmail (the inline-CSS rendering can drift between Litmus and reality)

This plan ships ONLY fixture updates + human checkpoints. No production code changes. If a regenerated golden produces a diff, the executor inspects the diff to confirm it matches the Phase 15 expected surface; if it looks wrong, the executor flags Plan 05/06/07 for revision rather than blindly accepting the new bytes.

Purpose: Gate the phase exit on (a) automated golden-comparison tests being green and (b) operator eyeballs on the two highest-value polish surfaces (forward-look + Gmail rendering).
Output: 3 regenerated fixture files + 2 operator confirmations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-live-calculator-sentinels/15-CONTEXT.md
@.planning/phases/15-live-calculator-sentinels/15-VALIDATION.md
@.planning/phases/15-live-calculator-sentinels/15-UI-SPEC.md
@CLAUDE.md
@dashboard.py
@notifier.py
@tests/regenerate_dashboard_golden.py
@tests/regenerate_notifier_golden.py
@tests/fixtures/dashboard/sample_state.json
@tests/fixtures/dashboard/golden.html
@.planning/phases/15-live-calculator-sentinels/15-04-SUMMARY.md
@.planning/phases/15-live-calculator-sentinels/15-05-SUMMARY.md
@.planning/phases/15-live-calculator-sentinels/15-06-SUMMARY.md
@.planning/phases/15-live-calculator-sentinels/15-07-SUMMARY.md

<interfaces>
The regeneration scripts (PATTERNS.md "Dashboard golden fixture regeneration"):

- `python tests/regenerate_dashboard_golden.py` reads tests/fixtures/dashboard/sample_state.json, calls `dashboard.render_dashboard(state)`, writes the result to tests/fixtures/dashboard/golden.html. Idempotent: re-running with no input change produces zero git diff.

- `python tests/regenerate_notifier_golden.py` (or similar) reads sample state, calls notifier render functions, writes to tests/fixtures/notifier/golden*.html.

Confirm exact script names + invocation by reading the file headers / docstrings.

Sample state shape requirements for Phase 15 exercise:

The fixture state must produce HTML that hits ALL Phase 15 surfaces. Minimum content:

1. state['positions']['SPI200'] = LONG position WITHOUT manual_stop → exercises calc-row + single-cell trail-stop branch
2. state['positions']['AUDUSD'] = SHORT position WITH manual_stop set → exercises calc-row + side-by-side trail-stop branch
3. state['signals']['SPI200'] = signal that DOES NOT match SPI200 LONG (e.g., FLAT or SHORT) → produces drift warning when detect_drift is fed this state
4. state['warnings'] includes at least one entry with source='drift' → exercises _render_drift_banner

The state['warnings'] entries can be hand-authored (consistent with what detect_drift would produce on this state) — the JSON fixture is a snapshot, not a live computation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enrich tests/fixtures/dashboard/sample_state.json to exercise Phase 15 surfaces</name>
  <read_first>
    - tests/fixtures/dashboard/sample_state.json (current shape — what positions, signals, warnings, account, equity_history are present today)
    - tests/regenerate_dashboard_golden.py (to confirm how the script consumes sample_state.json — what fields are required, what schema_version it expects)
    - tests/regenerate_notifier_golden.py (to confirm if it reuses sample_state.json or has its own fixture)
    - sizing_engine.py (DriftEvent.message templates from Plan 02 — the warning messages in sample_state.json must match exactly)
    - .planning/phases/15-live-calculator-sentinels/15-CONTEXT.md (D-14 message templates)
  </read_first>
  <files>tests/fixtures/dashboard/sample_state.json</files>
  <behavior>
    - Test 1: After this edit, the JSON file parses (valid JSON syntax).
    - Test 2: schema_version is whatever the current production code expects (Phase 14 D-13 set this to 3 with manual_stop on Position).
    - Test 3: state['positions']['SPI200'] is a LONG Position dict with manual_stop=None and reasonable peak_price/atr_entry/entry_price.
    - Test 4: state['positions']['AUDUSD'] is a SHORT Position dict with manual_stop set to a numeric value (so the side-by-side trail-stop cell renders).
    - Test 5: state['signals']['SPI200'] is a dict with 'signal': 0 (FLAT) → drift vs LONG position.
    - Test 6: state['signals']['AUDUSD'] is a dict with 'signal': 1 (LONG) → reversal vs SHORT position.
    - Test 7: state['warnings'] contains at least 2 entries with source='drift' — one with the LONG-vs-FLAT message template, one with the SHORT-vs-LONG reversal message template.
    - Test 8: All other top-level keys (account, last_run, trade_log, equity_history, schema_version, etc.) preserved or set to reasonable values.
    - Test 9: The existing fields not relevant to Phase 15 are preserved verbatim where possible (don't gratuitously change the equity_history shape, etc.).
  </behavior>
  <action>
    Open tests/fixtures/dashboard/sample_state.json. Read the current contents in full. Plan the minimal changes that introduce Phase 15 exercise without breaking other golden-comparison tests:

    1. ADD or UPDATE state['positions']['SPI200'] to be a LONG position with manual_stop=None and concrete numeric values (entry_price, peak_price, atr_entry, current_level, contracts, direction='LONG').

    2. ADD or UPDATE state['positions']['AUDUSD'] to be a SHORT position with manual_stop set to a finite float (e.g., 0.66) and concrete numerics for entry_price, trough_price, atr_entry, current_level, contracts, direction='SHORT'. The manual_stop value triggers the side-by-side cell render.

    3. UPDATE state['signals']['SPI200'] to dict shape with 'signal': 0 (FLAT) and 'last_close', 'last_scalars': {'atr', 'rvol'} populated. The FLAT-vs-LONG mismatch produces drift.

    4. UPDATE state['signals']['AUDUSD'] to dict shape with 'signal': 1 (LONG) and last_close + last_scalars. The LONG-vs-SHORT mismatch produces reversal.

    5. ADD or UPDATE state['warnings'] to include exactly two entries with source='drift':
       - {"source": "drift", "message": "You hold LONG SPI200, today's signal is FLAT — consider closing.", "date": "2026-04-26"}
       - {"source": "drift", "message": "You hold SHORT AUDUSD, today's signal is LONG — reversal recommended (close SHORT, open LONG).", "date": "2026-04-26"}

    6. Preserve all other top-level keys (account, last_run, trade_log, equity_history, schema_version, _resolved_contracts) as they are; if Phase 14 added _resolved_contracts to the fixture, preserve it.

    7. Verify the JSON parses: `python -c "import json; json.load(open('tests/fixtures/dashboard/sample_state.json'))"` exits 0.

    Do NOT regenerate golden.html in this task — Task 2 owns that.
  </action>
  <verify>
    <automated>python -c "import json; s = json.load(open('tests/fixtures/dashboard/sample_state.json')); assert 'SPI200' in s['positions']; assert 'AUDUSD' in s['positions']; assert any(w.get('source') == 'drift' for w in s.get('warnings', [])); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; json.load(open('tests/fixtures/dashboard/sample_state.json'))"` exits 0 (valid JSON)
    - `grep -c "\"source\": \"drift\"" tests/fixtures/dashboard/sample_state.json` returns 2
    - `grep -c "\"manual_stop\":" tests/fixtures/dashboard/sample_state.json` returns at least 2 (both positions have the key, even if one is null)
    - `grep -c "consider closing" tests/fixtures/dashboard/sample_state.json` returns 1
    - `grep -c "reversal recommended" tests/fixtures/dashboard/sample_state.json` returns 1
    - `python -c "import json; s = json.load(open('tests/fixtures/dashboard/sample_state.json')); assert s['positions']['SPI200']['direction'] == 'LONG'"` exits 0
    - `python -c "import json; s = json.load(open('tests/fixtures/dashboard/sample_state.json')); assert s['positions']['AUDUSD']['direction'] == 'SHORT'"` exits 0
    - `python -c "import json; s = json.load(open('tests/fixtures/dashboard/sample_state.json')); assert s['signals']['SPI200']['signal'] == 0"` exits 0
  </acceptance_criteria>
  <done>sample_state.json now exercises ALL Phase 15 surfaces: calc-row (both positions), side-by-side stop (AUDUSD via manual_stop), drift banner (drift + reversal warnings).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate dashboard + notifier golden fixtures and confirm green</name>
  <read_first>
    - tests/regenerate_dashboard_golden.py (entire file — confirm invocation pattern, what files it writes, any flags)
    - tests/regenerate_notifier_golden.py (entire file — same)
    - tests/fixtures/dashboard/golden.html (current state — to compare what changes after regen)
    - dashboard.py (the new helpers from Plan 05)
    - notifier.py (the drift banner block from Plan 07)
  </read_first>
  <files>tests/fixtures/dashboard/golden.html, tests/fixtures/notifier/golden.html</files>
  <behavior>
    - Test 1: Running `python tests/regenerate_dashboard_golden.py` produces a non-empty golden.html that contains the new Phase 15 markup.
    - Test 2: Running `python tests/regenerate_notifier_golden.py` produces non-empty notifier golden(s) containing the drift banner.
    - Test 3: Running each regen script TWICE in a row produces zero git diff between runs (idempotent).
    - Test 4: All golden-comparison tests in the suite pass after regeneration (e.g., a test that asserts render_dashboard(load_state()) == golden.html bytes).
    - Test 5: The golden HTML diff vs HEAD before this plan is sane: NEW content in the diff is the calc-row + side-by-side cell + drift banner; UNCHANGED content is everything else (equity chart, key stats, footer, etc.).
  </behavior>
  <action>
    Run the regeneration scripts and verify:

    1. From the repo root, run:
       ```
       python tests/regenerate_dashboard_golden.py
       ```

    2. Run it AGAIN immediately:
       ```
       python tests/regenerate_dashboard_golden.py
       ```
       The git diff between the two runs MUST be empty (idempotent). Verify:
       ```
       git diff tests/fixtures/dashboard/golden.html
       ```
       Capture the diff for SUMMARY review (this is the diff vs the HEAD-pre-this-plan version).

    3. Inspect the diff. Confirm the new content includes:
       - At least one `<tr class="calc-row">` line
       - At least one `class="trail-stop-split"` span (side-by-side cell)
       - At least one `class="sentinel-banner` div (drift banner)
       - Reasonable bullet content matching the message templates
       - All UNRELATED markup (equity chart canvas, footer, key stats) is unchanged byte-for-byte.

       If anything looks wrong (e.g., calc-row appears but is malformed; drift banner missing; surrounding markup mutated), STOP and flag the offending plan (05/06/07) for revision rather than committing the bad golden.

    4. Run:
       ```
       python tests/regenerate_notifier_golden.py
       ```
       Twice; verify idempotent. Inspect notifier golden diff:
       - "━━━ Drift detected ━━━" present
       - Banner block sits BETWEEN the corrupt-reset block and the hero card
       - Border color matches the message content (red for the reversal entry, but the merged banner has at least one reversal so the inline-CSS is `#ef4444`)

    5. Run the full pytest suite:
       ```
       pytest -x -q
       ```
       Expect: green. Any golden-comparison test failures must be investigated:
       - If failure is "actual != golden" because production code differs from golden post-regen: the regen didn't run or there's a bug in Plan 05/06/07. Investigate.
       - If failure is unrelated: triage and fix.

    6. Run ruff:
       ```
       ruff check .
       ```
       Must be clean (no new warnings).

    7. Capture the git diff and stat: `git diff --stat tests/fixtures/` for the SUMMARY.
  </action>
  <verify>
    <automated>python tests/regenerate_dashboard_golden.py &amp;&amp; python tests/regenerate_dashboard_golden.py &amp;&amp; python tests/regenerate_notifier_golden.py &amp;&amp; python tests/regenerate_notifier_golden.py &amp;&amp; pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python tests/regenerate_dashboard_golden.py` exits 0 on first run
    - Second run produces zero diff: `git diff --quiet tests/fixtures/dashboard/golden.html` exits 0 after the second run if nothing changed
    - `python tests/regenerate_notifier_golden.py` exits 0; idempotent on second run
    - `grep -c "class=\"calc-row\"" tests/fixtures/dashboard/golden.html` returns at least 2 (one per instrument)
    - `grep -c "trail-stop-split" tests/fixtures/dashboard/golden.html` returns at least 1 (AUDUSD has manual_stop)
    - `grep -c "sentinel-banner" tests/fixtures/dashboard/golden.html` returns at least 1
    - `grep -c "━━━ Drift detected ━━━" tests/fixtures/notifier/golden.html` returns at least 1 (or whichever notifier golden file the regen script writes)
    - `pytest -x -q` exits 0 (full suite green)
    - `ruff check .` reports no new warnings introduced by Phase 15
  </acceptance_criteria>
  <done>Both regen scripts run cleanly and idempotently; goldens contain Phase 15 surfaces; full pytest suite green; ruff clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator verifies forward-look HTMX UX in browser</name>
  <files>(none — operator-side verification; no repo files modified during the checkpoint itself)</files>
  <action>
Operator-only step. Plan 05 + Plan 06 already shipped the calculator sub-row markup and the forward-stop fragment endpoint. Claude's role at this checkpoint: pause execution, present the `<what-built>` summary plus the `<how-to-verify>` checklist below to the operator, and wait for the resume-signal reply. If the operator reports failure, Claude opens a gap-closure plan against Plan 05 or Plan 06 (whichever the failure mode points at) before Phase 15 closes.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: operator verifies forward-look HTMX in browser — see how-to-verify checklist"</automated>
  </verify>
  <done>
Operator has replied with `approved` (all 6 verification points pass) or with a documented failure (which step failed + observed behaviour). Failure triggers a gap-closure plan; Phase 15 cannot close until approval.
  </done>
  <what-built>The Phase 15 calculator sub-row now includes a forward-look input (Plan 05) wired to a fragment-GET handler (Plan 06). When the operator types a value into the IF HIGH input, HTMX fires a GET request and the W cell updates within ~300ms (delay) without a page reload.</what-built>
  <how-to-verify>
1. From the repo root, start the dev server: `python -m uvicorn web.app:app --host 127.0.0.1 --port 8000` (or whichever invocation matches your local setup; ensure WEB_AUTH_SECRET is set in your env so auth doesn't reject the request).
2. Open `http://127.0.0.1:8000/` in a browser. Provide the auth header — the simplest path: use a browser extension like ModHeader to inject `X-Trading-Signals-Auth: <your secret>`, OR add the header via `curl -H` for a smoke check first.
3. Confirm the dashboard renders. Verify visually:
   - Calculator sub-row appears below each open position (STOP / DIST / NEXT ADD / LEVEL / IF HIGH labels visible).
   - For AUDUSD (with manual_stop), the trail-stop cell shows BOTH "manual: $X" and "computed: $Y (will close)".
   - The drift banner appears above the Open Positions section (border-left coloured red because the sample state has a reversal warning).
4. Click into the IF HIGH input for SPI200. Type a value above the existing peak (e.g., 7950 if the position's peak is 7900). Verify:
   - Within ~300ms the W cell after "stop rises to" updates from "—" to a dollar value.
   - No layout shift around the row.
   - No flash, no full-page reload.
5. Type a degenerate value (e.g., -1, or clear the input). Verify the W cell goes back to "—" without an error toast or 4xx in the browser network tab.
6. Optional: open browser devtools → Network tab → inspect the GET request URL: it should be `/?fragment=forward-stop&instrument=SPI200&z=...` and the response body should be a single `<span>` element.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 verification points pass. If any fail, describe the failure (which step + what you observed) so the relevant Plan can be revised.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator verifies drift banner renders correctly in real Gmail</name>
  <files>(none — operator-side verification; no repo files modified during the checkpoint itself)</files>
  <action>
Operator-only step. Plan 07 already shipped the drift banner inline-CSS block in `notifier._render_header_email`. Claude's role at this checkpoint: pause execution, present the `<what-built>` summary plus the `<how-to-verify>` checklist below to the operator, and wait for the resume-signal reply. If the operator reports failure (banner in wrong position, color wrong, body text wrong, subject prefix missing), Claude opens a gap-closure plan against Plan 07 before Phase 15 closes.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: operator verifies drift banner in real Gmail — see how-to-verify checklist"</automated>
  </verify>
  <done>
Operator has replied with `approved` (banner renders correctly with proper hierarchy, color, and bullet text) or with a documented failure. Failure triggers a gap-closure plan against Plan 07; Phase 15 cannot close until approval.
  </done>
  <what-built>The Phase 15 drift banner now appears in the daily email when state['warnings'] contains a source='drift' entry (Plan 07). The banner uses inline-CSS (Gmail strips `<style>`) and follows the Phase 8 stale-state banner style.</what-built>
  <how-to-verify>
1. Confirm the operator's `.env` has SIGNALS_EMAIL_FROM and SIGNALS_EMAIL_TO set (Phase 12 INFRA-01 — typically signals@<your-domain> and your inbox).
2. Trigger a test send. Easiest approach: temporarily seed state.json with a drift warning, then run `python main.py --force-email` (the existing v1.0 force-email flag bypasses the signal computation but renders + sends with current state).

   Concrete one-liner to seed a drift warning in state.json without disrupting the rest of the state:
   ```
   python -c "
   import json
   p = 'state.json'
   s = json.load(open(p))
   s.setdefault('warnings', []).append({
       'source': 'drift',
       'message': \"You hold LONG SPI200, today's signal is FLAT — consider closing.\",
       'date': '2026-04-26',
   })
   json.dump(s, open(p, 'w'), indent=2)
   print('seeded drift warning')
   "
   python main.py --force-email
   ```
3. Open Gmail (web client preferred — mobile is a separate UAT in Phase 16) and locate the email. Verify visually:
   - Subject line begins with `[!] ` (the critical-banner prefix).
   - In the email body, immediately after any stale or corruption banners (if present) and BEFORE the hero card with the day's signal, a banner appears with:
     - Heading text: `━━━ Drift detected ━━━`
     - Border-left: amber (`#eab308`) since this seeded warning is drift-only (not reversal). If you want to test the red border, seed a reversal-message warning instead.
     - Body bullet: `• You hold LONG SPI200, today's signal is FLAT — consider closing.`
   - The banner has visible padding, the heading is bold, the body line is in a slightly muted color.
4. Use Gmail's "Show original" view to confirm SPF/DKIM/DMARC are still passing (no regression from the inline-CSS additions).
5. Clean up: revert the seeded warning in state.json (`git checkout state.json` if state.json is git-tracked — adjust per your operational norm).
  </how-to-verify>
  <resume-signal>Type "approved" if the banner renders correctly with proper hierarchy, color, and bullet text. If anything looks off (banner in wrong position, color wrong, body text wrong, subject prefix missing), describe the failure so Plan 07 can be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sample_state.json (committed) → render functions | known-fixture state, no operator input |
| operator browser → forward-look fragment | covered by Plan 06 threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08-01 | T (Tampering) | golden fixture drift | mitigate | Idempotent regen script verified by running twice and comparing diff; bad golden bytes flagged for upstream plan revision rather than blindly committed |
| T-15-08-02 | I (Information disclosure) | sample_state.json content | accept | Fixture state is synthetic; no real operator data, no real account values, no real broker info — already the convention from v1.0 fixtures |
| T-15-08-03 | E (Elevation of privilege) | n/a | accept | This plan is fixture + checkpoint only; no production code changes |
</threat_model>

<verification>
- sample_state.json enriched to exercise all Phase 15 surfaces.
- Both regen scripts run cleanly and are idempotent (twice-run produces zero diff).
- Goldens contain calc-row, side-by-side stop cell, drift banner.
- Full pytest suite green.
- Operator confirms forward-look UX is smooth.
- Operator confirms drift banner renders correctly in Gmail.
</verification>

<success_criteria>
- 3 fixture files updated and committed.
- Pytest suite green.
- Operator approval recorded for both checkpoints.
- Phase 15 ready for `/gsd-verify-work 15`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-live-calculator-sentinels/15-08-SUMMARY.md` documenting:
- Diff stat for tests/fixtures/dashboard/sample_state.json (what changed).
- Diff stat for tests/fixtures/dashboard/golden.html and tests/fixtures/notifier/golden.html (lines added/removed).
- Confirmation that twice-run regeneration produced zero diff.
- Operator verification outcomes (approved / issues encountered).
- Any deviations.
</output>
