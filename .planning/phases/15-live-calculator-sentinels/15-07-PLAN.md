---
phase: 15
plan: 07
type: execute
wave: 3
depends_on:
  - 15-02
  - 15-04
  - 15-05
files_modified:
  - notifier.py
  - tests/test_notifier.py
autonomous: true
requirements:
  - SENTINEL-03
tags:
  - phase15
  - notifier
  - email-banner
  - critical-banner-classifier

must_haves:
  truths:
    - 'notifier._has_critical_banner returns True when state["warnings"] contains any entry with source="drift"'
    - 'notifier._render_header_email inserts a drift banner after the corruption banner and before the hero card per D-13 stack hierarchy'
    - 'Drift banner border color is _COLOR_SHORT (red) when ANY drift warning message contains "reversal recommended"; _COLOR_FLAT (amber) otherwise'
    - 'Drift banner body text uses the same DriftEvent.message strings as the dashboard banner (D-12 lockstep parity)'
    - 'Email subject [!] prefix is automatically applied by the existing Phase 8 _has_critical_banner -> [!] prefix path'
    - 'TestDriftBanner (7 methods) and TestBannerStackOrder (3 methods) ALL populated and passing — REVIEWS M-1 Path A: no skip escape hatches; REVIEWS L-4: hero-card marker uses the stable >Trading Signals</h1> literal'
    - 'Existing notifier tests (Phase 6/8/9) all still pass — no regression on stale or corruption banners'
  artifacts:
    - path: 'notifier.py'
      provides: '_has_critical_banner extended with source=="drift" branch'
      contains: "w.get('source') == 'drift'"
    - path: 'notifier.py'
      provides: 'Drift banner block in _render_header_email between corruption banner and hero card'
      contains: '━━━ Drift detected ━━━'
    - path: 'tests/test_notifier.py'
      provides: 'TestDriftBanner (7 methods) and TestBannerStackOrder (3 methods) ALL populated and passing — including the hero-card ordering test using the stable >Trading Signals</h1> marker (REVIEWS L-4)'
      contains: 'test_drift_banner_body_parity_with_dashboard'
  key_links:
    - from: 'notifier._has_critical_banner'
      to: "state['warnings'] entries with source='drift'"
      via: 'list iteration on warning source'
      pattern: "w.get\\('source'\\) == 'drift'"
    - from: 'notifier._render_header_email'
      to: 'inline-CSS drift banner block'
      via: 'parts.append between corruption banner and hero card'
      pattern: '━━━ Drift detected ━━━'
    - from: 'tests/test_notifier.py::TestDriftBanner::test_drift_banner_body_parity_with_dashboard'
      to: 'dashboard._render_drift_banner (Plan 05)'
      via: 'D-12 lockstep parity assertion — same DriftEvent.message string in both renderers'
      pattern: 'from dashboard import _render_drift_banner'
---

<objective>
Wire drift warnings into the daily email render path. Two changes in notifier.py:

1. Extend `_has_critical_banner` with a third branch that returns True when any state['warnings'] entry has source='drift'. This auto-engages the existing Phase 8 `[!]` subject-prefix path — no subject-assembly code changes needed.

2. Insert a drift banner block into `_render_header_email` between the existing corruption banner (CRITICAL BANNER 2) and the hero card. Border color follows D-13 (`_COLOR_SHORT` red when any reversal, `_COLOR_FLAT` amber otherwise). Body lines reuse `DriftEvent.message` strings from `state['warnings']` — the SAME strings the dashboard banner (Plan 05) already renders, guaranteeing D-12 lockstep parity.

This plan does NOT change subject assembly, hero card, footer, or any other notifier surface. It is purely additive.

**REVIEWS-driven changes (vs prior draft):**
- **H-3 (depends_on):** This plan now declares `depends_on: [15-02, 15-04, 15-05]` (the prior draft listed only 15-02 + 15-04). The D-12 parity test in Task 2 imports `dashboard._render_drift_banner` (shipped by Plan 05), so 15-05 is a hard dependency. Wave 3 placement remains unchanged because 15-05 ships in Wave 2.
- **M-1 Path A (no skip escape hatches):** Earlier draft allowed `test_drift_banner_inserted_before_hero_card` to remain skipped if the executor couldn't find a stable hero-card marker. The cross-AI review (Codex MEDIUM #5) flagged the conflict with this plan's must_haves which require all 10 tests to pass. This revision removes the skip escape hatch — ALL 10 tests MUST be implemented and passing.
- **L-4 (stable hero-card marker):** The hero-card marker fragility is fixed by replacing the search-fallback chain (`hero-card`, `Signal:`, `data-table`) with the literal `>Trading Signals</h1>` text — this is the actual h1 string emitted by `notifier._render_hero_card_email` (line 530-531 of notifier.py, VERIFIED via direct read). The marker is stable: it is hardcoded into the hero card render and unlikely to change without a coordinated edit.

Purpose: Make drift state visible in the operator's morning email, not just the dashboard. SENTINEL-03 closes here.
Output: ~25 lines added in notifier.py + 10 fully implemented test methods (REVIEWS M-1 Path A — no skips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-live-calculator-sentinels/15-CONTEXT.md
@.planning/phases/15-live-calculator-sentinels/15-RESEARCH.md
@.planning/phases/15-live-calculator-sentinels/15-PATTERNS.md
@.planning/phases/15-live-calculator-sentinels/15-VALIDATION.md
@.planning/phases/15-live-calculator-sentinels/15-UI-SPEC.md
@.planning/phases/15-live-calculator-sentinels/15-REVIEWS.md
@CLAUDE.md
@notifier.py
@dashboard.py
@system_params.py
@tests/test_notifier.py
@.planning/phases/15-live-calculator-sentinels/15-02-SUMMARY.md
@.planning/phases/15-live-calculator-sentinels/15-05-SUMMARY.md

<interfaces>
Existing _has_critical_banner shape (notifier.py lines 548-564, VERIFIED via PATTERNS.md):

The function iterates state.get('warnings', []) checking for two existing critical sources: a stale-info transient flag, and a state_manager-source warning whose message starts with 'recovered from corruption'. The Phase 15 extension adds a third branch: any warning whose source is 'drift'.

Existing _render_header_email banner blocks (notifier.py lines 567-679, PATTERNS.md):

The function builds a list `parts` of HTML chunks rendered as inline-CSS table rows. Existing chunks include:
- CRITICAL BANNER 1: stale-state (red border via _COLOR_SHORT)
- CRITICAL BANNER 2: corrupt-reset (amber border via _COLOR_FLAT, message starts with "recovered from corruption")
- HERO CARD (parts.append(_render_hero_card_email(state, now)) at line 644)

The Phase 15 drift banner block lands BETWEEN the corrupt-reset block (last appearance of "recovered from corruption" handler) and the HERO CARD parts.append.

**REVIEWS L-4 — hero-card stable marker:** Direct inspection of notifier.py lines 528-531 confirms `_render_hero_card_email` emits the literal h1: `<h1 style="margin:0;font-size:22px;font-weight:600;color:{_COLOR_TEXT};line-height:1.2;">Trading Signals</h1>`. The substring `>Trading Signals</h1>` is the stable marker for the test. (Other candidates considered and rejected: `Last updated` text — appears in multiple email surfaces; `data-table` class — used in multiple sections; `Signal as of` — only present when at-least-one signal has been computed.)

Color constants (system_params.py, VERIFIED):
- _COLOR_SHORT = '#ef4444' — red, used for reversal severity
- _COLOR_FLAT = '#eab308' — amber, used for drift severity
- _COLOR_SURFACE = '#161a24' — banner background
- _COLOR_TEXT = '#e5e7eb' — banner heading text
- _COLOR_TEXT_MUTED = '#cbd5e1' — banner body text

Drift banner inline-CSS markup contract (UI-SPEC §Email Banner Contract, lines 437-453, VERBATIM):

The banner is two table rows: one with the banner content (border-left: 4px solid + heading + body lines), and one spacer row with height: 16px. Banner content has:
- A `<p>` heading with text "━━━ Drift detected ━━━" (triple-line bracketed exactly like the existing stale-state heading at notifier.py:608)
- A `<p>` body containing per-instrument bullet lines `• {DriftEvent.message}<br>` joined.

Pitfall 4 (RESEARCH.md): Place the drift banner BETWEEN the corrupt-reset block and the hero card, not after the hero card. TestBannerStackOrder verifies this DOM order via the `>Trading Signals</h1>` marker.

D-12 lockstep parity (CONTEXT.md): The body lines come from `[w.get('message', '') for w in state['warnings'] if w.get('source') == 'drift']`. These are the same strings the dashboard banner (`_render_drift_banner` from Plan 05) already renders. **Plan 07 depends_on includes 15-05** so the parity test can import `dashboard._render_drift_banner`.

**REVIEWS M-1 Path A:** All 10 tests in this plan MUST be implemented and passing. The earlier-draft skip-fallback for test_drift_banner_inserted_before_hero_card is removed; the L-4 stable-marker change makes the test deterministic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _has_critical_banner and insert drift banner block in _render_header_email</name>
  <read_first>
    - notifier.py — read _has_critical_banner (lines ~548-564) and _render_header_email (lines ~567-679); identify the EXACT line where the corrupt-reset block ends and where the hero card parts.append begins (this is where the drift banner block goes); confirm html.escape usage in surrounding code; confirm _COLOR_* constants are imported at module top
    - notifier.py — read _render_hero_card_email (lines 498-545) to confirm the literal `<h1 ...>Trading Signals</h1>` is emitted (REVIEWS L-4 stable marker)
    - .planning/phases/15-live-calculator-sentinels/15-UI-SPEC.md — §Email Banner Contract verbatim markup (lines 437-453)
    - .planning/phases/15-live-calculator-sentinels/15-CONTEXT.md — D-03, D-12, D-13
    - .planning/phases/15-live-calculator-sentinels/15-RESEARCH.md — Pattern 4, Pattern 5, Pitfall 4 (banner position)
    - dashboard.py — _render_drift_banner from Plan 05 to confirm body-text source for D-12 parity
  </read_first>
  <files>notifier.py</files>
  <behavior>
    - Test 1: _has_critical_banner returns True when state['warnings'] contains a single entry with source='drift'.
    - Test 2: _has_critical_banner returns False when state has no drift warnings AND no stale flag AND no corruption warning.
    - Test 3: _has_critical_banner still returns True when only a stale flag is set (no regression — Phase 8 case unchanged).
    - Test 4: _has_critical_banner still returns True when only a corruption warning exists (no regression).
    - Test 5: _render_header_email output (the parts joined) contains "━━━ Drift detected ━━━" when state has a drift warning.
    - Test 6: The drift banner block appears in the rendered HTML BEFORE the hero card content (string position of "━━━ Drift detected ━━━" is less than the string position of `>Trading Signals</h1>` — the stable hero-card marker per REVIEWS L-4).
    - Test 7: When state has BOTH stale and drift warnings, the stale banner appears BEFORE the drift banner (string-position ordering).
    - Test 8: When state has BOTH corruption and drift warnings, the corruption banner appears BEFORE the drift banner.
    - Test 9: When ANY drift warning message contains "reversal recommended", the banner border color is `#ef4444` (_COLOR_SHORT). When all are drift-only, color is `#eab308` (_COLOR_FLAT).
    - Test 10: Drift banner body bullets use the EXACT DriftEvent.message strings from state['warnings'] (D-12 parity — string-equality with what the dashboard banner renders).
  </behavior>
  <action>
    Open notifier.py.

    Part A — Extend _has_critical_banner (around lines 548-564).

    Find the existing function. Inside the `for w in state.get('warnings', [])` loop, AFTER the existing if-block that matches `source == 'state_manager' and message starts with 'recovered from corruption'` and BEFORE the closing of the for-loop, INSERT a new check:

    ```
        if w.get('source') == 'drift':   # NEW Phase 15 SENTINEL-03
          return True
    ```

    Use 2-space indent matching surrounding code. Do NOT modify the existing stale or corruption checks.

    Part B — Insert drift banner block in _render_header_email.

    Find _render_header_email. Locate the END of the corrupt-reset banner block (CRITICAL BANNER 2 — search for the comment marker 'corrupt-reset' or 'critical banner 2' or 'recovered from corruption' inside the function body). Find the IMMEDIATELY-FOLLOWING parts.append for the hero card (line 644: `parts.append(_render_hero_card_email(state, now))`).

    Between those two — AFTER the corrupt-reset block's last parts.append, BEFORE the hero card's first parts.append — INSERT the drift banner block. Use 2-space indent matching surrounding code:

    ```
    # --- CRITICAL BANNER 3: drift/reversal (Phase 15 D-03/D-12/D-13/SENTINEL-03) ---
    # Border color follows D-13: red (_COLOR_SHORT) when any reversal severity,
    # amber (_COLOR_FLAT) when drift-only. Body bullets reuse DriftEvent.message
    # strings from state['warnings'] — D-12 lockstep parity with the dashboard
    # banner in dashboard._render_drift_banner.
    drift_warnings = [
      w for w in state.get('warnings', [])
      if w.get('source') == 'drift'
    ]
    if drift_warnings:
      has_reversal = any(
        'reversal recommended' in w.get('message', '')
        for w in drift_warnings
      )
      border_color = _COLOR_SHORT if has_reversal else _COLOR_FLAT
      bullet_lines = '<br>\n      '.join(
        f'&bull; {html.escape(w.get("message", ""), quote=True)}'
        for w in drift_warnings
      )
      parts.append(
        f'<tr><td style="padding:12px 16px;background:{_COLOR_SURFACE};'
        f'border-left:4px solid {border_color};'
        f'font-family:-apple-system,BlinkMacSystemFont,\'Segoe UI\','
        f'Roboto,sans-serif;font-size:14px;color:{_COLOR_TEXT};'
        f'line-height:1.5;">'
        f'<p style="margin:0 0 4px 0;font-size:14px;font-weight:600;'
        f'color:{_COLOR_TEXT};letter-spacing:0.02em;">'
        f'━━━ Drift detected ━━━</p>'
        f'<p style="margin:0;color:{_COLOR_TEXT_MUTED};font-size:13px;line-height:1.6;">'
        f'{bullet_lines}</p>'
        f'</td></tr>\n'
        f'<tr><td height="16" style="height:16px;font-size:0;line-height:0;">'
        f'&nbsp;</td></tr>\n'
      )
    ```

    Confirm the executor uses the EXACT names of the local variables already in scope inside _render_header_email — `parts` is the list of HTML chunks (verify name via reading the function); `html` is the stdlib module (already imported at module top of notifier.py); `_COLOR_SHORT`, `_COLOR_FLAT`, `_COLOR_SURFACE`, `_COLOR_TEXT`, `_COLOR_TEXT_MUTED` are constants imported from system_params at the top of notifier.py.

    Run `pytest tests/test_notifier.py -x -q` to confirm existing tests pass; new tests still skip (Task 2 populates them). Run `pytest -x -q` for the full suite.
  </action>
  <verify>
    <automated>grep -c "━━━ Drift detected ━━━" notifier.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "w.get('source') == 'drift'" notifier.py` returns at least 1 (in _has_critical_banner)
    - `grep -c "━━━ Drift detected ━━━" notifier.py` returns 1
    - `grep -c "CRITICAL BANNER 3" notifier.py` returns 1
    - `grep -c "reversal recommended" notifier.py` returns 1 (in the has_reversal check)
    - `grep -B 1 -A 1 "━━━ Drift detected ━━━" notifier.py | head -5` shows the banner block immediately after the corrupt-reset block (executor verifies visually that the BEFORE-context contains 'corrupt' or 'recovered from corruption')
    - Python check: `python -c "import notifier; s = {'warnings': [{'source': 'drift', 'message': 'x', 'date': '2026-04-26'}]}; assert notifier._has_critical_banner(s) is True"` exits 0
    - Python check: `python -c "import notifier; assert notifier._has_critical_banner({'warnings': []}) is False"` exits 0
    - `pytest tests/test_notifier.py -x -q` exits 0 (existing notifier tests still green)
    - `pytest -x -q` exits 0 (full suite green; new tests still skip)
    - ruff check notifier.py clean
  </acceptance_criteria>
  <done>_has_critical_banner extended with drift branch; drift banner inline-CSS block landed between corrupt-reset and hero card; existing notifier tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate ALL TestDriftBanner + TestBannerStackOrder method bodies (REVIEWS M-1 Path A — no skips; REVIEWS L-4 — stable hero-card marker)</name>
  <read_first>
    - tests/test_notifier.py — TestDriftBanner + TestBannerStackOrder skeletons from Plan 01 Task 4; existing _has_critical_banner test pattern + existing _render_header_email test pattern (search for 'critical_banner' and 'render_header' in this file)
    - notifier.py — confirm _has_critical_banner extension and drift banner block from Task 1; confirm `_render_hero_card_email` emits `<h1 ...>Trading Signals</h1>` (line 530-531; this is the REVIEWS L-4 stable marker)
    - dashboard.py — _render_drift_banner from Plan 05 (for the D-12 parity assertion in test_drift_banner_body_parity_with_dashboard) — Plan 07 now depends_on 15-05 so this import is safe
    - state_manager.py — append_warning, reset_state for fixture setup
    - .planning/phases/15-live-calculator-sentinels/15-VALIDATION.md — exact test method names + assertions
    - .planning/phases/15-live-calculator-sentinels/15-RESEARCH.md — Pitfall 4 (banner DOM position)
    - .planning/phases/15-live-calculator-sentinels/15-REVIEWS.md (M-1 Path A, L-4 stable marker)
  </read_first>
  <files>tests/test_notifier.py</files>
  <behavior>
    - All 7 TestDriftBanner methods have real bodies (no pytest.skip).
    - All 3 TestBannerStackOrder methods have real bodies (no pytest.skip) — REVIEWS M-1 Path A enforcement.
    - test_has_critical_banner_drift_source asserts True with a drift-source warning present.
    - test_has_critical_banner_no_drift asserts False with empty warnings.
    - test_drift_banner_in_email_body asserts "━━━ Drift detected ━━━" appears in _render_header_email output.
    - test_drift_banner_body_parity_with_dashboard imports both the dashboard renderer and the email renderer, builds the same warning state, asserts the SAME DriftEvent.message string appears (substring match) in BOTH outputs (D-12 parity). depends_on includes 15-05 so the dashboard import is available.
    - test_drift_banner_in_email_body_and_subject_critical_prefix asserts that with a drift warning, _has_critical_banner returns True (the existing Phase 8 [!] subject-prefix path consults this classifier).
    - test_email_banner_border_red_for_reversal asserts the substring '#ef4444' appears in the drift banner block when ANY warning message contains 'reversal recommended'.
    - test_email_banner_border_amber_for_drift_only asserts '#eab308' appears in the drift banner block when all warnings are drift-only.
    - test_banner_hierarchy_corruption_beats_drift asserts string position of 'recovered from corruption' content < string position of 'Drift detected' (corruption renders first).
    - test_banner_hierarchy_stale_beats_drift asserts string position of 'Stale state' < 'Drift detected'.
    - test_drift_banner_inserted_before_hero_card asserts string position of 'Drift detected' < string position of `>Trading Signals</h1>` — REVIEWS L-4 stable marker.
  </behavior>
  <action>
    Locate TestDriftBanner + TestBannerStackOrder skeletons in tests/test_notifier.py. Replace each pytest.skip body with the corresponding logic below. Mirror the existing _render_header_email test pattern in the file (search for 'render_header_email' to find existing test invocations and the typical fixture setup).

    The exact API for invoking _render_header_email (whether it takes a `now` argument, what its return type is — list[str] vs str) MUST be lifted from existing tests. The test bodies below assume the function returns a string (or that an existing helper joins the parts). The executor adapts the join/extraction pattern as needed.

    ```
    # ----- TestDriftBanner -----

      def test_has_critical_banner_drift_source(self) -> None:
        from notifier import _has_critical_banner
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state = append_warning(state, 'drift', 'msg', now=fixed_now)
        assert _has_critical_banner(state) is True

      def test_has_critical_banner_no_drift(self) -> None:
        from notifier import _has_critical_banner
        from state_manager import reset_state
        state = reset_state()
        assert _has_critical_banner(state) is False

      def test_drift_banner_in_email_body(self) -> None:
        from notifier import _render_header_email
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state = append_warning(
          state, 'drift',
          "You hold LONG SPI200, today's signal is FLAT — consider closing.",
          now=fixed_now,
        )
        rendered = _render_header_email(state, fixed_now)
        body = rendered if isinstance(rendered, str) else ''.join(rendered)
        assert '━━━ Drift detected ━━━' in body
        assert 'consider closing' in body

      def test_drift_banner_body_parity_with_dashboard(self) -> None:
        '''D-12 lockstep parity: the body bullets in the email use the
        SAME DriftEvent.message strings as the dashboard banner.
        depends_on includes 15-05 (REVIEWS H-3) so this import is safe.'''
        from notifier import _render_header_email
        from dashboard import _render_drift_banner
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        message = "You hold LONG SPI200, today's signal is FLAT — consider closing."
        state = append_warning(state, 'drift', message, now=fixed_now)
        # Render via both paths
        email_rendered = _render_header_email(state, fixed_now)
        email_body = email_rendered if isinstance(email_rendered, str) else ''.join(email_rendered)
        dashboard_body = _render_drift_banner(state)
        # The DriftEvent.message string must appear (escaped or otherwise) in BOTH.
        # html.escape leaves apostrophes as &#x27; with quote=True; account for that.
        import html as _html
        escaped_message = _html.escape(message, quote=True)
        assert (message in email_body) or (escaped_message in email_body)
        assert (message in dashboard_body) or (escaped_message in dashboard_body)

      def test_drift_banner_in_email_body_and_subject_critical_prefix(self) -> None:
        '''Phase 8 _has_critical_banner -> [!] subject prefix path is auto-engaged.'''
        from notifier import _has_critical_banner
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state = append_warning(state, 'drift', 'msg', now=fixed_now)
        # _has_critical_banner is the SOLE classifier the subject-assembly path
        # consults for the [!] prefix (Phase 8 contract). Verifying it returns
        # True with a drift warning is sufficient to prove the prefix engages.
        assert _has_critical_banner(state) is True

      def test_email_banner_border_red_for_reversal(self) -> None:
        from notifier import _render_header_email
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state = append_warning(
          state, 'drift',
          "You hold LONG SPI200, today's signal is SHORT — reversal recommended (close LONG, open SHORT).",
          now=fixed_now,
        )
        rendered = _render_header_email(state, fixed_now)
        body = rendered if isinstance(rendered, str) else ''.join(rendered)
        # Banner border color: _COLOR_SHORT = '#ef4444' (red) for reversal
        assert '#ef4444' in body, 'reversal -> red border (_COLOR_SHORT)'

      def test_email_banner_border_amber_for_drift_only(self) -> None:
        from notifier import _render_header_email
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state = append_warning(
          state, 'drift',
          "You hold LONG SPI200, today's signal is FLAT — consider closing.",
          now=fixed_now,
        )
        rendered = _render_header_email(state, fixed_now)
        body = rendered if isinstance(rendered, str) else ''.join(rendered)
        # The banner block specifically uses _COLOR_FLAT = '#eab308' for drift-only.
        # Locate the drift banner substring and check the border color near it.
        idx = body.find('━━━ Drift detected ━━━')
        assert idx >= 0, 'drift banner heading present'
        # Banner border-left appears BEFORE the heading within the same <tr>; check
        # a window of ~500 chars before the heading for '#eab308'.
        window = body[max(0, idx - 500):idx]
        assert '#eab308' in window, 'drift-only -> amber border (_COLOR_FLAT)'

    # ----- TestBannerStackOrder -----

      def test_banner_hierarchy_corruption_beats_drift(self) -> None:
        '''D-13: corruption banner renders before drift banner.'''
        from notifier import _render_header_email
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state = append_warning(
          state, 'state_manager',
          'recovered from corruption: state.json reset',
          now=fixed_now,
        )
        state = append_warning(
          state, 'drift',
          "You hold LONG SPI200, today's signal is FLAT — consider closing.",
          now=fixed_now,
        )
        rendered = _render_header_email(state, fixed_now)
        body = rendered if isinstance(rendered, str) else ''.join(rendered)
        idx_corr = body.find('recovered from corruption')
        idx_drift = body.find('━━━ Drift detected ━━━')
        assert idx_corr >= 0, 'corruption banner present'
        assert idx_drift >= 0, 'drift banner present'
        assert idx_corr < idx_drift, 'D-13: corruption renders before drift'

      def test_banner_hierarchy_stale_beats_drift(self) -> None:
        '''D-13: stale banner renders before drift banner.'''
        from notifier import _render_header_email
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state['_stale_info'] = {'days_stale': 3, 'last_run_date': '2026-04-23'}
        state = append_warning(
          state, 'drift',
          "You hold LONG SPI200, today's signal is FLAT — consider closing.",
          now=fixed_now,
        )
        rendered = _render_header_email(state, fixed_now)
        body = rendered if isinstance(rendered, str) else ''.join(rendered)
        idx_stale = body.find('Stale state')
        idx_drift = body.find('━━━ Drift detected ━━━')
        assert idx_stale >= 0, 'stale banner present'
        assert idx_drift >= 0, 'drift banner present'
        assert idx_stale < idx_drift, 'D-13: stale renders before drift'

      def test_drift_banner_inserted_before_hero_card(self) -> None:
        '''REVIEWS L-4 + Pitfall 4: drift banner must precede the hero card
        block. Stable marker chosen by direct inspection of notifier.py
        _render_hero_card_email line 530-531: the literal `<h1 ...>Trading
        Signals</h1>` is the hardcoded h1 emitted by the hero card. The
        substring `>Trading Signals</h1>` is present in EVERY email render
        (no conditional branch suppresses it), making it a deterministic
        hero-card content marker.

        REVIEWS M-1 Path A: this test MUST be implemented and passing.
        The skip-fallback chain from the prior draft is removed.
        '''
        from notifier import _render_header_email
        from state_manager import append_warning, reset_state
        from datetime import datetime, timezone
        state = reset_state()
        fixed_now = datetime(2026, 4, 26, 9, 30, 0, tzinfo=timezone.utc)
        state = append_warning(
          state, 'drift',
          "You hold LONG SPI200, today's signal is FLAT — consider closing.",
          now=fixed_now,
        )
        rendered = _render_header_email(state, fixed_now)
        body = rendered if isinstance(rendered, str) else ''.join(rendered)
        idx_drift = body.find('━━━ Drift detected ━━━')
        # REVIEWS L-4 stable hero-card marker: the literal h1 closing tag
        # immediately after the title. Verified by direct read of notifier.py
        # line 530-531: `<h1 ...>Trading Signals</h1>`.
        idx_hero = body.find('>Trading Signals</h1>')
        assert idx_drift >= 0, 'drift banner heading absent'
        assert idx_hero >= 0, (
          'REVIEWS L-4: hero-card marker `>Trading Signals</h1>` absent — '
          '_render_hero_card_email may have been refactored. Update the marker '
          'in this test to match the new hero-card content.'
        )
        assert idx_drift < idx_hero, (
          f'Pitfall 4: drift banner must render BEFORE the hero card. '
          f'idx_drift={idx_drift} idx_hero={idx_hero}'
        )
    ```

    Run `pytest tests/test_notifier.py::TestDriftBanner tests/test_notifier.py::TestBannerStackOrder -x -q` and confirm all 10 pass.

    Run `pytest -x -q` for the full suite.
  </action>
  <verify>
    <automated>pytest tests/test_notifier.py::TestDriftBanner tests/test_notifier.py::TestBannerStackOrder -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_notifier.py::TestDriftBanner -x -q` exits 0 with EXACTLY 7 passed, 0 skipped, 0 failed (REVIEWS M-1 Path A)
    - `pytest tests/test_notifier.py::TestBannerStackOrder -x -q` exits 0 with EXACTLY 3 passed, 0 skipped, 0 failed (REVIEWS M-1 Path A — including the hero-card test now using the L-4 stable marker)
    - `grep -A 2 "def test_has_critical_banner_drift_source" tests/test_notifier.py | grep -c "pytest.skip"` returns 0
    - `grep -A 2 "def test_drift_banner_body_parity_with_dashboard" tests/test_notifier.py | grep -c "pytest.skip"` returns 0
    - `grep -A 2 "def test_banner_hierarchy_corruption_beats_drift" tests/test_notifier.py | grep -c "pytest.skip"` returns 0
    - `grep -A 5 "def test_drift_banner_inserted_before_hero_card" tests/test_notifier.py | grep -c "pytest.skip"` returns 0 (REVIEWS M-1 Path A — no skip fallback)
    - `grep -c ">Trading Signals</h1>" tests/test_notifier.py` returns at least 1 (REVIEWS L-4 stable marker)
    - `pytest tests/test_notifier.py -x -q` exits 0 (existing tests + new tests; full file green)
    - `pytest -x -q` exits 0 (full suite green)
    - ruff check tests/test_notifier.py clean
  </acceptance_criteria>
  <done>10 new notifier tests now green (7 drift-banner + 3 hierarchy with the hero-card test using the REVIEWS L-4 stable `>Trading Signals</h1>` marker; no pytest.skip bodies remaining per REVIEWS M-1 Path A); SENTINEL-03 verified end-to-end; D-12 parity test passing (depends_on 15-05); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state['warnings'] (server-constructed) → email HTML | drift messages embedded in email body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | T (Tampering) | XSS via warning message in email body | mitigate | html.escape(message, quote=True) at the leaf bullet render site; messages are server-constructed in detect_drift but escaping is project policy regardless |
| T-15-07-02 | T (Tampering) | Banner border-color inversion | mitigate | has_reversal boolean check uses literal `'reversal recommended' in message` substring; D-14 message templates lock this string. Tested in test_email_banner_border_red_for_reversal and test_email_banner_border_amber_for_drift_only |
| T-15-07-03 | I (Information disclosure) | Email subject prefix | accept | The [!] prefix is engaged via the existing Phase 8 _has_critical_banner pathway; same posture as Phase 8 stale and corruption banners |
| T-15-07-04 | E (Elevation of privilege) | sole-writer breach | accept | notifier.py READS state['warnings'] but never writes; the new banner code is read-only on warnings |
| T-15-07-05 | D (Denial of service) | malformed warning crashes email render | mitigate | bullet_lines comprehension uses w.get('message', '') with default empty string; html.escape handles None-like input via the default; banner block does nothing when drift_warnings is empty |
| T-15-07-06 | T (Tampering) | banner-stack-hierarchy hero-card test fragility | mitigate | REVIEWS L-4: hero-card marker chosen by direct inspection of _render_hero_card_email — the literal `>Trading Signals</h1>` is hardcoded in line 530-531 and present in EVERY email render. The test fails fast with a clear message if the marker is removed by a future refactor. |
</threat_model>

<verification>
- _has_critical_banner returns True for any drift warning.
- Drift banner block lands BETWEEN corrupt-reset and hero card (D-13 hierarchy + Pitfall 4; verified by REVIEWS L-4 stable marker test).
- Border color: _COLOR_SHORT for any reversal in the warning set; _COLOR_FLAT otherwise.
- D-12 lockstep parity: same DriftEvent.message strings appear in both dashboard banner and email banner. The parity test imports dashboard._render_drift_banner — Plan 07 depends_on includes 15-05 (REVIEWS H-3).
- Email subject [!] prefix engages automatically via the Phase 8 _has_critical_banner pathway.
- All 10 tests pass — REVIEWS M-1 Path A enforcement, no skips.
- Full pytest suite green.
</verification>

<success_criteria>
- _has_critical_banner extension shipped + tested.
- Drift banner block in _render_header_email between corrupt-reset and hero card.
- ALL 10 tests passing (7 drift + 3 hierarchy; REVIEWS M-1 Path A — no documented skips).
- D-12 parity assertion holds (depends_on 15-05).
- Hero-card ordering test uses the REVIEWS L-4 stable `>Trading Signals</h1>` marker.
- Full suite green; ruff clean.
</success_criteria>

<output>
After completion, create `.planning/phases/15-live-calculator-sentinels/15-07-SUMMARY.md` documenting:
- Exact line range where _has_critical_banner was extended.
- Exact line range where the drift banner block was inserted in _render_header_email.
- Confirmation that the block sits between corrupt-reset and hero card (BEFORE-context grep result).
- Test pass counts: 7 drift + 3 hierarchy = 10 total, all GREEN (REVIEWS M-1 Path A enforcement).
- Hero-card marker used: literal `>Trading Signals</h1>` (REVIEWS L-4 stable marker — verified by direct inspection of notifier.py:530-531).
- Confirmation that depends_on includes 15-05 (REVIEWS H-3) so the D-12 parity test imports dashboard._render_drift_banner cleanly.
- Any deviations.
</output>
