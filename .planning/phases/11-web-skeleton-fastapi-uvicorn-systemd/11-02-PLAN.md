---
phase: 11
plan: 02
type: execute
wave: 1
depends_on:
  - "11-01"
files_modified:
  - systemd/trading-signals-web.service
  - tests/test_web_systemd_unit.py
autonomous: true
requirements:
  - WEB-01
  - WEB-02
threat_refs:
  - T-11-01
  - T-11-06
must_haves:
  truths:
    - "systemd/trading-signals-web.service exists at the repo root"
    - "Unit file ExecStart binds uvicorn to 127.0.0.1:8000 ONLY (never 0.0.0.0)"
    - "Unit file ExecStart references `web.app:app` exactly (REVIEWS LOW #8)"
    - "Unit file ExecStart uses --workers 1 (Phase 8 D-07)"
    - "Unit file uses User=trader, Group=trader (D-07)"
    - "Unit file uses Wants=trading-signals.service (SOFT dep, D-09)"
    - "Unit file uses After=network.target (D-09)"
    - "Unit file declares all 5 hardening directives (D-10)"
    - "Unit file uses Restart=on-failure, RestartSec=10s (D-08)"
    - "Unit file uses WantedBy=multi-user.target"
    - "Unit file points to .venv/bin/uvicorn at /home/trader/trading-signals/.venv/bin/uvicorn"
    - "Unit file uses SyslogIdentifier=trading-signals-web"
    - "Unit file uses EnvironmentFile=-/... (leading dash, OPTIONAL) per REVIEWS MEDIUM #5"
    - "tests/test_web_systemd_unit.py asserts all invariants including REVIEWS additions"
  artifacts:
    - path: systemd/trading-signals-web.service
      provides: "systemd unit file for the FastAPI web process"
      contains: "ExecStart="
    - path: tests/test_web_systemd_unit.py
      provides: "Automated verification of unit file invariants"
      contains: "class TestSystemdUnit"
  key_links:
    - from: "systemd/trading-signals-web.service ExecStart"
      to: ".venv/bin/uvicorn web.app:app (Plan 01 artifact)"
      via: "uvicorn entry-point invocation"
      pattern: "web\\.app:app"
    - from: "systemd/trading-signals-web.service [Unit] Wants="
      to: "trading-signals.service (Phase 10 unit)"
      via: "soft systemd dependency"
      pattern: "Wants=trading-signals\\.service"
---

<objective>
Wave 1 of Phase 11. Commit the systemd unit file with the 2026-04-24 post-REVIEWS adjustments:
- REVIEWS MEDIUM #5: `EnvironmentFile=-` (leading dash, optional). Phase 11 consumes no env vars; without the dash the unit would fail to start on droplets without `.env`.
- REVIEWS LOW #8: ExecStart references `web.app:app` exactly. Cross-integration guard (catches drift if Plan 01 factory is renamed).

Add tests/test_web_systemd_unit.py asserting every D-06..D-12 invariant + the two new REVIEWS assertions.

Purpose: WEB-01 + WEB-02. Unit file is the contract; test is the guard.

Output:
- `systemd/trading-signals-web.service`
- `tests/test_web_systemd_unit.py` (27+ assertions)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-web-skeleton-fastapi-uvicorn-systemd/11-CONTEXT.md
@.planning/phases/11-web-skeleton-fastapi-uvicorn-systemd/11-RESEARCH.md
@.planning/phases/11-web-skeleton-fastapi-uvicorn-systemd/11-REVIEWS.md
@CLAUDE.md

<interfaces>
Phase 11 D-06..D-12 (CONTEXT.md, 2026-04-24 reconciled):
- D-06: unit file at /etc/systemd/system/trading-signals-web.service
- D-07: User=trader, Group=trader
- D-08: Restart=on-failure, RestartSec=10s
- D-09: After=network.target, Wants=trading-signals.service (SOFT)
- D-10: all 5 hardening directives
- D-11: ExecStart binds 127.0.0.1:8000, workers=1, log-level=info;
        EnvironmentFile=- (optional, REVIEWS MEDIUM #5)
- D-12: StandardOutput/Error=journal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create systemd/trading-signals-web.service</name>
  <files>systemd/trading-signals-web.service</files>
  <read_first>
    - .planning/phases/11-web-skeleton-fastapi-uvicorn-systemd/11-RESEARCH.md §3 (lines 117-180)
    - .planning/phases/11-web-skeleton-fastapi-uvicorn-systemd/11-CONTEXT.md (D-06..D-12, 2026-04-24)
    - .planning/phases/11-web-skeleton-fastapi-uvicorn-systemd/11-REVIEWS.md (MEDIUM #5, LOW #8)
  </read_first>
  <action>
    Create `systemd/trading-signals-web.service` with EXACT content below. The 2026-04-24 REVIEWS MEDIUM #5 adjustment: `EnvironmentFile=` has a leading `-`.

    ```ini
    [Unit]
    Description=Trading Signals — FastAPI web process
    After=network.target
    Wants=trading-signals.service

    [Service]
    Type=simple
    User=trader
    Group=trader
    WorkingDirectory=/home/trader/trading-signals
    EnvironmentFile=-/home/trader/trading-signals/.env
    ExecStart=/home/trader/trading-signals/.venv/bin/uvicorn web.app:app \
              --host 127.0.0.1 \
              --port 8000 \
              --workers 1 \
              --log-level info
    Restart=on-failure
    RestartSec=10s
    StandardOutput=journal
    StandardError=journal
    SyslogIdentifier=trading-signals-web

    NoNewPrivileges=true
    PrivateTmp=true
    ProtectSystem=strict
    ReadWritePaths=/home/trader/trading-signals
    ProtectHome=read-only

    [Install]
    WantedBy=multi-user.target
    ```

    **Critical implementation notes:**
    1. No `0.0.0.0` anywhere.
    2. `--workers 1` — required.
    3. `Wants=` not `Requires=` (D-09 soft dep).
    4. **`EnvironmentFile=-` (leading dash)** — REVIEWS MEDIUM #5. `.env` optional.
    5. **`web.app:app`** in ExecStart — REVIEWS LOW #8 exact reference.
    6. Trailing newline.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; test -f systemd/trading-signals-web.service &amp;&amp; grep -q '^EnvironmentFile=-/home/trader/trading-signals/.env$' systemd/trading-signals-web.service &amp;&amp; grep -q 'web\.app:app' systemd/trading-signals-web.service &amp;&amp; grep -q -- '--host 127.0.0.1' systemd/trading-signals-web.service &amp;&amp; ! grep -q '0\.0\.0\.0' systemd/trading-signals-web.service &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - `grep -c '^\[Unit\]$' systemd/trading-signals-web.service` = 1
    - `grep -c '^\[Service\]$' systemd/trading-signals-web.service` = 1
    - `grep -c '^\[Install\]$' systemd/trading-signals-web.service` = 1
    - `grep -c '^EnvironmentFile=-/home/trader/trading-signals/.env$' systemd/trading-signals-web.service` = 1 (REVIEWS MEDIUM #5)
    - `grep -c '^EnvironmentFile=/home/trader/trading-signals/.env$' systemd/trading-signals-web.service` = 0 (non-dash FORBIDDEN)
    - `grep -c 'web\.app:app' systemd/trading-signals-web.service` = 1 (REVIEWS LOW #8)
    - `grep -c -- '--host 127.0.0.1' systemd/trading-signals-web.service` = 1
    - `grep -c -- '--workers 1' systemd/trading-signals-web.service` = 1
    - `grep -c '0\.0\.0\.0' systemd/trading-signals-web.service` = 0
    - `grep -c 'Requires=' systemd/trading-signals-web.service` = 0
    - All 5 hardening directives present (NoNewPrivileges, PrivateTmp, ProtectSystem, ReadWritePaths, ProtectHome)
    - `grep -c '^Restart=on-failure$' systemd/trading-signals-web.service` = 1
    - `grep -c '^WantedBy=multi-user.target$' systemd/trading-signals-web.service` = 1
    - File ends with newline
  </acceptance_criteria>
  <done>
    Unit file with all D-06..D-12 invariants + REVIEWS adjustments (EnvironmentFile=- optional; web.app:app exact). No 0.0.0.0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_web_systemd_unit.py</name>
  <files>tests/test_web_systemd_unit.py</files>
  <read_first>
    - tests/test_dashboard.py (path constants pattern)
    - tests/test_scheduler.py (TestGHAWorkflow — config file parsing)
    - systemd/trading-signals-web.service (just-written)
    - .planning/phases/11-web-skeleton-fastapi-uvicorn-systemd/11-REVIEWS.md (MEDIUM #5, LOW #8)
    - CLAUDE.md
  </read_first>
  <behavior>
    - `pytest tests/test_web_systemd_unit.py -x -q` exits 0
    - Uses configparser to parse the unit file
    - Asserts every D-06..D-12 invariant + new REVIEWS MEDIUM #5 + LOW #8
  </behavior>
  <action>
    Create tests/test_web_systemd_unit.py:

    ```python
    '''Phase 11 WEB-01 + WEB-02 + D-06..D-12 — systemd unit invariants.

    2026-04-24 REVIEWS adjustments:
      - MEDIUM #5: EnvironmentFile=- (leading dash, optional).
      - LOW #8: ExecStart references `web.app:app` exactly.
    '''
    import configparser
    from pathlib import Path

    import pytest

    UNIT_PATH = Path('systemd/trading-signals-web.service')


    @pytest.fixture(scope='module')
    def unit_text() -> str:
      assert UNIT_PATH.exists(), f'unit file missing: {UNIT_PATH}'
      return UNIT_PATH.read_text()


    @pytest.fixture(scope='module')
    def unit_cfg(unit_text: str) -> configparser.ConfigParser:
      cfg = configparser.ConfigParser(interpolation=None, strict=True)
      cfg.optionxform = str
      cfg.read_string(unit_text)
      return cfg


    class TestSystemdUnitSections:
      def test_unit_section_present(self, unit_cfg):
        assert 'Unit' in unit_cfg.sections()

      def test_service_section_present(self, unit_cfg):
        assert 'Service' in unit_cfg.sections()

      def test_install_section_present(self, unit_cfg):
        assert 'Install' in unit_cfg.sections()


    class TestSystemdUnitMetadata:
      def test_description(self, unit_cfg):
        assert unit_cfg['Unit']['Description'] == 'Trading Signals — FastAPI web process'

      def test_after_network_target(self, unit_cfg):
        assert unit_cfg['Unit']['After'] == 'network.target'

      def test_wants_signal_unit_soft_dep(self, unit_cfg):
        assert unit_cfg['Unit']['Wants'] == 'trading-signals.service'

      def test_no_requires_directive(self, unit_cfg):
        assert 'Requires' not in unit_cfg['Unit']


    class TestSystemdServiceCore:
      def test_type_simple(self, unit_cfg):
        assert unit_cfg['Service']['Type'] == 'simple'

      def test_user_trader(self, unit_cfg):
        assert unit_cfg['Service']['User'] == 'trader'

      def test_group_trader(self, unit_cfg):
        assert unit_cfg['Service']['Group'] == 'trader'

      def test_working_directory(self, unit_cfg):
        assert unit_cfg['Service']['WorkingDirectory'] == '/home/trader/trading-signals'

      def test_environment_file_is_optional(self, unit_cfg, unit_text):
        '''REVIEWS MEDIUM #5: `-` prefix makes .env optional.'''
        value = unit_cfg['Service']['EnvironmentFile']
        assert value == '-/home/trader/trading-signals/.env', value
        assert '\nEnvironmentFile=-/home/trader/trading-signals/.env\n' in (
          '\n' + unit_text + '\n'
        )

      def test_environment_file_is_not_required_form(self, unit_text):
        '''Reject non-dash form that would make .env mandatory.'''
        assert 'EnvironmentFile=/home/trader/trading-signals/.env' not in unit_text

      def test_restart_on_failure(self, unit_cfg):
        assert unit_cfg['Service']['Restart'] == 'on-failure'

      def test_restart_sec_10s(self, unit_cfg):
        assert unit_cfg['Service']['RestartSec'] == '10s'

      def test_syslog_identifier(self, unit_cfg):
        assert unit_cfg['Service']['SyslogIdentifier'] == 'trading-signals-web'

      def test_standard_output_journal(self, unit_cfg):
        assert unit_cfg['Service']['StandardOutput'] == 'journal'

      def test_standard_error_journal(self, unit_cfg):
        assert unit_cfg['Service']['StandardError'] == 'journal'


    class TestSystemdExecStartBinding:
      def test_execstart_uses_venv_uvicorn(self, unit_cfg):
        exec_start = unit_cfg['Service']['ExecStart']
        assert '/home/trader/trading-signals/.venv/bin/uvicorn' in exec_start

      def test_execstart_references_web_app_module_exactly(self, unit_cfg, unit_text):
        '''REVIEWS LOW #8: ExecStart references Plan 01 `web.app:app` entry.'''
        exec_start = unit_cfg['Service']['ExecStart']
        assert 'web.app:app' in exec_start
        assert 'web.app:app' in unit_text

      def test_execstart_binds_localhost(self, unit_cfg):
        assert '--host 127.0.0.1' in unit_cfg['Service']['ExecStart']

      def test_execstart_does_not_bind_all_interfaces(self, unit_text):
        '''CRITICAL: 0.0.0.0 must NOT appear anywhere.'''
        assert '0.0.0.0' not in unit_text

      def test_execstart_uses_port_8000(self, unit_cfg):
        assert '--port 8000' in unit_cfg['Service']['ExecStart']

      def test_execstart_workers_one(self, unit_cfg):
        assert '--workers 1' in unit_cfg['Service']['ExecStart']

      def test_execstart_log_level_info(self, unit_cfg):
        assert '--log-level info' in unit_cfg['Service']['ExecStart']

      def test_execstart_no_reload_flag(self, unit_text):
        assert '--reload' not in unit_text


    class TestSystemdHardening:
      def test_no_new_privileges(self, unit_cfg):
        assert unit_cfg['Service']['NoNewPrivileges'] == 'true'

      def test_private_tmp(self, unit_cfg):
        assert unit_cfg['Service']['PrivateTmp'] == 'true'

      def test_protect_system_strict(self, unit_cfg):
        assert unit_cfg['Service']['ProtectSystem'] == 'strict'

      def test_read_write_paths_repo_only(self, unit_cfg):
        assert unit_cfg['Service']['ReadWritePaths'] == '/home/trader/trading-signals'

      def test_protect_home_read_only(self, unit_cfg):
        assert unit_cfg['Service']['ProtectHome'] == 'read-only'


    class TestSystemdInstall:
      def test_wanted_by_multi_user_target(self, unit_cfg):
        assert unit_cfg['Install']['WantedBy'] == 'multi-user.target'
    ```

    **Critical implementation notes:**
    1. `configparser` with `interpolation=None`, `optionxform = str`.
    2. `test_execstart_does_not_bind_all_interfaces` uses raw unit_text.
    3. `test_environment_file_is_optional` + `test_environment_file_is_not_required_form` (REVIEWS MEDIUM #5).
    4. `test_execstart_references_web_app_module_exactly` (REVIEWS LOW #8).
    5. 2-space indent.
  </action>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals &amp;&amp; .venv/bin/python -m pytest tests/test_web_systemd_unit.py -x -q 2>&amp;1 | tail -3 | grep -E 'passed' &amp;&amp; .venv/bin/python -m pytest tests/ -q 2>&amp;1 | tail -3 | grep -E 'passed' &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 6 `^class` declarations in file
    - `grep -c 'test_environment_file_is_optional' tests/test_web_systemd_unit.py` = 1 (REVIEWS MEDIUM #5)
    - `grep -c 'test_environment_file_is_not_required_form' tests/test_web_systemd_unit.py` = 1 (REVIEWS MEDIUM #5)
    - `grep -c 'test_execstart_references_web_app_module_exactly' tests/test_web_systemd_unit.py` = 1 (REVIEWS LOW #8)
    - `grep -c 'test_execstart_does_not_bind_all_interfaces' tests/test_web_systemd_unit.py` = 1
    - `pytest tests/test_web_systemd_unit.py -x -q` exits 0
    - `pytest tests/ -q` reports passed no failed
    - Test count at least 27
  </acceptance_criteria>
  <done>
    Tests parse unit file, assert all D-06..D-12 + REVIEWS adjustments. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External network → uvicorn | Loopback only (Phase 11). |
| systemd → uvicorn process | Hardening directives sandbox it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | uvicorn port 8000 | mitigate | `--host 127.0.0.1`; test asserts. |
| T-11-06 | EoP / Tampering | uvicorn filesystem | mitigate | All 5 D-10 hardening directives; tests enforce each. |
</threat_model>

<verification>
- Unit file exists at repo root
- 30+ acceptance criteria pass
- `pytest tests/test_web_systemd_unit.py -x` all green (27+ tests)
- File contains no 0.0.0.0
- EnvironmentFile has `-` prefix (REVIEWS MEDIUM #5)
- ExecStart references `web.app:app` exactly (REVIEWS LOW #8)
</verification>

<success_criteria>
- `pytest tests/test_web_systemd_unit.py -x -q` exits 0
- `pytest tests/ -q` exits 0
</success_criteria>

<output>
After completion, create 11-02-SUMMARY.md documenting:
- Unit file matches RESEARCH §3 + 2026-04-24 REVIEWS adjustments
- Test count by class
- Full-suite test count
- Cross-reference for Plan 03: deploy.sh restart uses this unit name
</output>

## Deferred follow-ups

Per 11-REVIEWS.md LOW #9 — cosmetic test brittleness may be revisited in Phase 16.
