---
phase: 35
plan: 05
type: execute
wave: 4
depends_on: [35-02, 35-04]
files_modified:
  - web/app.py
  - tests/test_web_admin.py
autonomous: true
requirements: [RBAC-01, RBAC-02]
tags: [auth, admin, wiring, startup-invariant, integration]
must_haves:
  truths:
    - "D-10: TestAdminRouteInvariant in tests/test_web_admin.py walks app.router.routes recursively and asserts every /admin/* path has require_admin in dependency chain (--reviews OpenCode LOW)"
    - "web/app.py::create_app() includes the admin router via application.include_router(admin_router) BEFORE the add_middleware(AuthMiddleware, ...) line"
    - "After create_app(), every APIRoute in app.routes whose path starts with '/admin/' has require_admin in its .dependencies chain (startup invariant)"
    - "GET /admin/ping returns 200 + {'ok': True} when an admin session cookie is sent (uid maps to a user row with role='admin' in auth.json)"
    - "GET /admin/ping returns 403 with Content-Type: application/json and body {\"detail\": \"Admin access required\"} for: no session, header auth, non-admin role (--reviews #5)"
    - "403-sweep is split into TWO parametrized suites: test_admin_rejects_unauthenticated (no session) and test_admin_rejects_header_auth (X-Trading-Signals-Auth header). Both assert JSON body schema, NOT HTML (--reviews OpenCode/Codex)"
    - "STRUCTURAL regression check (NOT byte-identical): the 4 critical pre-Phase-35 routes (GET /, GET /dashboard, GET /paper-trades, POST /login) still return the same status code AND contain the same key HTML landmarks (page <title>, primary heading text) on a fixed fixture state (--reviews HIGH consensus #3)"
    - "RBAC-01 is PARTIALLY satisfied this phase: factory exists, middleware populates user_id, admin-gated routes consume Depends(require_admin). Migrating existing non-admin authenticated routes (/trades, /journal, /dashboard, /paper-trades) to use Depends(current_user_id) is DEFERRED to Phase 36 per --reviews HIGH consensus #2"
  artifacts:
    - path: "web/app.py"
      provides: "admin_router include_router call inside create_app() before add_middleware"
      contains: "include_router(admin_router)"
    - path: "tests/test_web_admin.py"
      provides: "TestAdminRouteInvariant + TestAdminGate403SweepUnauthenticated + TestAdminGate403SweepHeaderAuth + TestAdminPing + TestPrePhase35RoutesStructuralParity"
      contains: "class TestAdminRouteInvariant"
  key_links:
    - from: "web/app.py::create_app"
      to: "web.routes.admin.router"
      via: "top-level import + application.include_router(admin_router) call before add_middleware line"
      pattern: "include_router(admin_router)"
    - from: "tests/test_web_admin.py::TestAdminRouteInvariant"
      to: "app.router.routes (after include_router, recursive walk)"
      via: "walk APIRoute.dependencies for each path starting with '/admin/'"
      pattern: "isinstance(r, APIRoute) and r.path.startswith('/admin/')"
    - from: "tests/test_web_admin.py::TestAdminPing"
      to: "GET /admin/ping via TestClient with seeded admin user in isolated auth.json"
      via: "create_user({'email': WEB_AUTH_USERNAME, 'role': 'admin'}) + signed session cookie with uid"
      pattern: "client.get('/admin/ping', cookies=...)"
---

<objective>
Final wiring + verification plan with all four `--reviews` consensus concerns addressed end-to-end:

1. **403 body JSON schema (consensus #5):** Every 403-sweep test asserts `response.headers['content-type'].startswith('application/json')` AND `response.json() == {'detail': <expected>}` where expected is one of the locked detail strings imported from `web.dependencies`. NEVER HTML, NEVER stack trace.

2. **403-sweep split into two parametrized suites (--reviews OpenCode/Codex):**
   - `TestAdminGate403SweepUnauthenticated` — no session cookie, no header. Asserts JSON body schema.
   - `TestAdminGate403SweepHeaderAuth` — `X-Trading-Signals-Auth` header set (no cookie). Asserts JSON body schema.
   Splitting prevents one suite from masking the other if a regression flips one path but not the other.

3. **Recursive route walking + ordering comment (--reviews OpenCode LOW):** The startup invariant walks `app.router.routes` recursively (handles future Mount or nested APIRouter cases gracefully) AND its docstring explains why include_router-before-add_middleware order matters.

4. **Structural regression check (consensus #3 — replaces "byte-identical"):** Narrowed to 4 specific pre-Phase-35 routes (`GET /`, `GET /dashboard`, `GET /paper-trades`, `POST /login`). For each: assert status code matches the pre-Phase-35 baseline (200 or 302 as appropriate), assert key HTML landmarks are present (page <title>, primary heading text), assert no 500/stack-trace response. NOT byte-identical because Set-Cookie headers, session tokens, and timestamps change every request. Approach documented in the test class docstring.

5. **Existing routes migration deferred (--reviews HIGH consensus #2):** Phase 35 does NOT wire `Depends(current_user_id)` into existing non-admin authenticated route handlers. The phase satisfies RBAC-01 partially — the factory exists and is consumed by admin-gated routes; full migration of existing routes is Phase 36 (per-user scoping). The plan acceptance criteria explicitly state this.

Other in-scope work from the prior plan version (unchanged):
- In `web/app.py::create_app()`, import the admin router and call `application.include_router(admin_router)` immediately before `add_middleware(AuthMiddleware, ...)`.
- Populate `TestAdminRouteInvariant` with the startup invariant test (walks app.router.routes; for every /admin/* path, asserts `require_admin` is in the route's dependency chain).
- Populate `TestAdminPing` with happy-path (admin cookie → 200) + legacy-cookie shim (no uid in payload → shim resolves uid → 200) + bootstrap-gap (empty users[] + uid=None → 403).

Output: One include_router call in web/app.py + FIVE populated test classes (split sweep + invariant + ping + structural parity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-CONTEXT.md
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-PATTERNS.md
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-RESEARCH.md
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-REVIEWS.md
@web/app.py
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-02-SUMMARY.md
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-04-SUMMARY.md

<interfaces>
<!-- From RESEARCH.md Pattern 5 (startup invariant), PATTERNS.md section "web/app.py — register admin router", --reviews consensus #3+#5 -->

web/app.py::create_app() — modification site:
  - Add import: `from web.routes.admin import router as admin_router`
  - Add inside create_app(), immediately BEFORE the existing `application.add_middleware(AuthMiddleware, ...)` line:
      # Phase 35 D-08 + --reviews #4: admin sub-router registered BEFORE add_middleware
      # so the gate (require_admin) is applied to /admin/* routes via the Depends
      # chain injected at router-construction time (Plan 04). Order matters because
      # add_middleware wraps the entire route map — a router added AFTER would
      # still receive middleware (middleware doesn't care about registration order)
      # but the convention keeps route-tree mutations grouped before the middleware
      # boundary for grep-auditability.
      application.include_router(admin_router)

Recursive route walking (--reviews OpenCode):
  - Walk via `_walk_routes(app.router.routes)` helper that handles APIRoute, Mount (with sub-router), and nested router cases. Today app.routes is flat (verified RESEARCH.md), but the recursive walker future-proofs the invariant against Phase 36+ where Mount wrapping may appear.

403 body JSON contract (--reviews #5):
  - For every 403 response asserted in this plan:
    - response.status_code == 403
    - response.headers['content-type'].split(';')[0].strip() == 'application/json'
    - response.json() == {'detail': 'Admin access required'} (the require_admin constant from Plan 03)

Structural regression scope (--reviews #3, replaces byte-identical):
  - Routes in scope: GET /, GET /dashboard, GET /paper-trades, POST /login
  - For each authenticated route, send the admin session cookie (post-Phase-35 shape with uid).
  - Assertions: status_code matches expected (200 for authenticated GETs; 302 redirect to /dashboard for successful POST /login); response has `Content-Type: text/html` for HTML routes; response body contains specific landmark strings (e.g. `<title>` tag, primary heading text grep) documented inline per route. NO byte-comparison.

TestClient + auth setup (PATTERNS.md, tests/conftest.py):
  - Use `sys.modules.pop('web.app', None)` before `from web.app import create_app`.
  - Use `isolated_auth_json` fixture.
  - Seed admin: `auth_store.create_user({'email': VALID_USERNAME, 'role': 'admin'})`.
  - Build signed session cookie: `URLSafeTimedSerializer(VALID_SECRET, salt='tsi-session-cookie').dumps({'u': VALID_USERNAME, 'uid': uid, 'iat': int(time.time())})`.
  - Send via `client.get(path, cookies={'tsi_session': token})`.

Header auth path (D-06):
  - Send `X-Trading-Signals-Auth: <WEB_AUTH_SECRET>` header.
  - dispatch leaves user_id=None (default from Plan 02 dispatch-top reset); require_admin raises 403.

Pitfall 2 reminder:
  - WEB_AUTH_USERNAME is 'marc' in tests (not email-shaped). Admin user row MUST be created with `email='marc'` (matching what payload['u'] carries).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire admin router into web/app.py before add_middleware</name>
  <read_first>
    - web/app.py (read fully — must see top-of-file route import block, create_app() body with include_router calls, and the add_middleware(AuthMiddleware, ...) line)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-PATTERNS.md (section "web/app.py — register admin router")
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-CONTEXT.md (D-08)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-REVIEWS.md (Plan 05 OpenCode LOW — order comment; cross-plan #4 — registration ordering)
    - web/routes/admin/__init__.py (confirm router is importable per Plan 04)
  </read_first>
  <behavior>
    - `from web.app import create_app; app = create_app()` succeeds without errors.
    - `app.routes` contains an APIRoute whose `.path == '/admin/ping'` and `.methods` contains 'GET'.
    - That APIRoute's `.dependencies` list contains a Depends object whose `.dependency is require_admin`.
    - All existing routes continue to resolve to the same handlers as before.
    - The add_middleware(AuthMiddleware, ...) line still appears LAST in create_app() — admin router include is BEFORE it.
  </behavior>
  <action>
    In web/app.py: add `from web.routes.admin import router as admin_router` to the top-of-file route-import block.

    Inside `create_app()`, locate the existing `application.add_middleware(AuthMiddleware, ...)` line. Immediately BEFORE that line, insert:

      # Phase 35 D-08 + --reviews #4: admin sub-router registered BEFORE
      # add_middleware. Gate (require_admin) is injected at router construction
      # (Plan 04) — every route on admin_router inherits it. Order is a
      # grep-auditability convention; middleware applies regardless of order
      # but keeping route-tree mutations BEFORE add_middleware groups them
      # cleanly for review.
      application.include_router(admin_router)

    Use 2-space indent. Do NOT modify any other create_app() logic. Do NOT register admin paths on `application` directly.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminRouteInvariant tests/test_web_app_factory.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from web.routes.admin import router as admin_router" web/app.py` returns exactly one match.
    - `grep -n "application.include_router(admin_router)" web/app.py` returns exactly one match.
    - `grep -n "Phase 35 D-08" web/app.py` returns at least one match (--reviews comment present).
    - The `include_router(admin_router)` line appears BEFORE the `add_middleware(AuthMiddleware,` line:
      `awk '/include_router\(admin_router\)/ {inc=NR} /add_middleware\(AuthMiddleware/ {mid=NR} END {exit (inc>0 && mid>0 && inc<mid)?0:1}' web/app.py` exits 0.
    - `python -c "import sys; [sys.modules.pop(m, None) for m in list(sys.modules) if m.startswith('web.')]; from web.app import create_app; from fastapi.routing import APIRoute; app = create_app(); admin = [r for r in app.routes if isinstance(r, APIRoute) and r.path == '/admin/ping']; assert len(admin) == 1; print('ok')"` prints `ok` and exits 0.
    - `.venv/bin/pytest -x --tb=short tests/test_web_app_factory.py` exits 0 — existing app-factory invariants still hold.
  </acceptance_criteria>
  <done>web/app.py wires admin_router via include_router immediately before add_middleware; create_app() builds; admin route appears in app.routes; --reviews comment documents the order; existing app-factory tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate TestAdminRouteInvariant + split 403-sweep + TestAdminPing + TestPrePhase35RoutesStructuralParity</name>
  <read_first>
    - tests/test_web_admin.py (read fully — see Wave-0 stubs from Plan 03 and Plan 04)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-PATTERNS.md (sections "Startup invariant test class (D-10)", "Parametrized 403-sweep class (D-10)", "Happy path class")
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-RESEARCH.md (Pattern 5; Pitfall 1; Pitfall 2; Pitfall 6)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-REVIEWS.md (consensus #3 structural fixtures; consensus #5 JSON body; consensus #2 deferred routes; OpenCode Plan 05 split sweep + recursive walk)
    - tests/test_web_auth_middleware.py (read fully — copy _request_with_cookies helper; sys.modules.pop pattern)
    - tests/conftest.py (autouse credentials lines 45-78; isolated_auth_json lines 138-159; VALID_SECRET / VALID_USERNAME constants)
    - tests/test_auth_store_users.py (TestUserRegistry style for create_user setup)
    - web/dependencies.py (must exist from Plan 03 — import _DETAIL_ADMIN_REQUIRED for assertion)
  </read_first>
  <behavior>
    **TestAdminRouteInvariant** (recursive walk + ordering comment):
    - test_admin_routes_have_require_admin_dependency — call create_app(); walk app.router.routes recursively via a `_walk_routes` helper (handles APIRoute + Mount + nested router; today flat, future-proof per --reviews OpenCode); filter paths starting '/admin/'; assert non-empty; for each, assert `require_admin in [d.dependency for d in route.dependencies]`. Docstring explains the recursive walk and why include_router-before-add_middleware order matters (grep-auditability convention; middleware applies regardless of order).
    - test_admin_routes_nonempty — guards vacuous invariant.

    **TestAdminGate403SweepUnauthenticated** (split per --reviews):
    - Parametrize over `['/admin/ping']`.
    - GET path with NO cookie and NO header → 403 OR 401/302 (middleware short-circuit). Accept any of {401, 403, 302} but if 403 then assert JSON body schema.
    - Actually the cleanest version: when there's NO auth at all, middleware (Plan 02 dispatch-top default) sets user_id=None, the admin route runs through require_admin which raises 403 with the locked detail. Assert status_code == 403, content-type starts with application/json, body == {'detail': 'Admin access required'}. (Confirm this by reading middleware to ensure no earlier middleware-level redirect intercepts /admin/* — RESEARCH.md confirms admin routes are NOT in PUBLIC_PATHS; auth middleware always runs but it's a header/cookie inspector, not a redirector for unauthenticated requests — 403 is the expected outcome.)

    **TestAdminGate403SweepHeaderAuth** (split per --reviews):
    - Parametrize over `['/admin/ping']`.
    - GET path with `X-Trading-Signals-Auth: <WEB_AUTH_SECRET>` header (no cookie) → middleware accepts header but dispatch-top default leaves user_id=None → require_admin raises 403. Assert status_code == 403, content-type application/json, body {'detail': 'Admin access required'}.

    **TestAdminGate403SweepNonAdminRole** (third sweep — non-admin cookie):
    - Seed a user with role='ff' in isolated auth.json; build signed session cookie with that uid; GET /admin/ping → 403 + JSON body.

    **TestAdminPing**:
    - test_admin_session_returns_200 — seed admin (email=VALID_USERNAME, role='admin'); signed cookie with uid; GET /admin/ping → 200, content-type application/json, body == {'ok': True}.
    - test_admin_session_with_legacy_cookie_no_uid_resolves_via_shim — seed admin; build signed cookie WITHOUT uid (pre-Phase-35 shape `{'u': VALID_USERNAME, 'iat': ...}`); GET /admin/ping → 200 (D-04 shim resolves uid). Critical backward-compat test.
    - test_admin_session_when_user_not_in_auth_json_returns_403 — empty auth.json; cookie with `{'u': VALID_USERNAME, 'uid': None}`; GET /admin/ping → 403 + JSON body (bootstrap-gap correct).
    - test_admin_with_uid_no_resolved_user_cannot_pass_require_admin (--reviews Codex negative test) — admin-looking username AND uid set in cookie BUT the uid does not match any auth.json row → 403 + JSON body. Documents that shim/cookie uid alone is not enough — require_admin's get_user() lookup is the authoritative gate.

    **TestPrePhase35RoutesStructuralParity** (--reviews consensus #3, replaces byte-identical):
    - Class docstring explicitly documents the approach: "Structural comparison, NOT byte-identical. Dynamic fields (Set-Cookie, timestamps, session tokens) are intentionally excluded. We assert: (a) status code matches the pre-Phase-35 baseline, (b) Content-Type matches, (c) specific HTML landmark strings are present (page title, primary heading), (d) no 500/stack-trace response body. Scope is narrowed to 4 critical routes: GET /, GET /dashboard, GET /paper-trades, POST /login. RBAC-01 'no observable behaviour change' is satisfied by THIS structural-parity test plus the full pytest suite passing."
    - test_root_returns_expected_shape — GET / with admin session cookie → status_code in {200, 302} (depends on /'s existing behavior; pick the actual current value by running once and locking); Content-Type text/html or redirect; no 500.
    - test_dashboard_returns_expected_shape — GET /dashboard with admin session cookie → 200, Content-Type text/html, body contains pre-defined landmark strings (read web/routes/dashboard package to pick 2 stable strings, e.g. a `<title>` substring and a primary heading word like 'Dashboard' or 'SPI').
    - test_paper_trades_returns_expected_shape — GET /paper-trades with admin session cookie → 200, Content-Type text/html, body contains specific landmark (e.g. 'Paper trades' or similar identified by reading web/routes/paper_trades package).
    - test_login_post_returns_expected_shape — POST /login with valid credentials → 302 redirect (existing behaviour); Location header matches `/dashboard` (or whatever the existing behaviour is — pick by reading web/routes/login).
    - Each test: NO byte comparison. NO timestamp/cookie value assertions. Just status code + content-type + landmark string presence + absence of `<title>500` / `Traceback`.
  </behavior>
  <action>
    In tests/test_web_admin.py:

    **Imports to add** (if not already present):
      `import time`
      `from itsdangerous.url_safe import URLSafeTimedSerializer`
      `from fastapi.routing import APIRoute`
      `from fastapi.testclient import TestClient`
      `from web.dependencies import require_admin, _DETAIL_ADMIN_REQUIRED`

    **Helper function** at module level (recursive walker per --reviews OpenCode):
      ```
      def _walk_routes(routes):
        '''Recursively yield APIRoute objects. Today app.router.routes is flat,
        but this future-proofs the invariant against Phase 36+ Mount or nested
        APIRouter cases. --reviews OpenCode Plan 05 LOW.'''
        for r in routes:
          if isinstance(r, APIRoute):
            yield r
          elif hasattr(r, 'routes'):
            yield from _walk_routes(r.routes)
      ```

    **Populate TestAdminRouteInvariant** (replace `class TestAdminRouteInvariant: pass` stub):
    - test_admin_routes_have_require_admin_dependency uses `_walk_routes(app.router.routes)` (not just `app.routes`) for recursive coverage. Docstring includes the --reviews #4 ordering rationale.
    - test_admin_routes_nonempty — at least one /admin/* route exists.

    **Populate TestAdminGate403SweepUnauthenticated** (replace `class TestAdminGate403Sweep: pass` stub — rename to SweepUnauthenticated):
    - `@pytest.mark.parametrize('path', ['/admin/ping'])`
    - test_admin_rejects_unauthenticated: TestClient.get(path) — assert status_code == 403, response.headers['content-type'].split(';')[0].strip() == 'application/json', response.json() == {'detail': _DETAIL_ADMIN_REQUIRED}.

    **Append class TestAdminGate403SweepHeaderAuth** (NEW):
    - `@pytest.mark.parametrize('path', ['/admin/ping'])`
    - test_admin_rejects_header_auth: include `X-Trading-Signals-Auth: <WEB_AUTH_SECRET>` header; no cookie. Assert same 403 + JSON body schema.

    **Append class TestAdminGate403SweepNonAdminRole** (NEW):
    - `@pytest.mark.parametrize('path', ['/admin/ping'])`
    - Seed user with role='ff' via isolated auth.json; build signed cookie with that uid; assert 403 + JSON body.

    **Populate TestAdminPing** (replace stub):
    - 4 tests as described above. Every test asserts both status code AND content-type + body shape.

    **Append class TestPrePhase35RoutesStructuralParity** (NEW, --reviews #3):
    - 4 tests covering GET /, GET /dashboard, GET /paper-trades, POST /login.
    - Each test sends an admin session cookie (post-Phase-35 shape with uid).
    - Asserts: status_code, content-type, landmark string presence, absence of '<title>500' and 'Traceback'.
    - DO NOT compare bytes. DO NOT compare timestamps or cookies.
    - Class docstring documents the structural approach and lists the 4 routes in scope.

    Per PATTERNS.md "sys.modules.pop before create_app" — every test that calls `create_app()` must first `sys.modules.pop('web.app', None)` to avoid module-level state leak.

    **Add module-level NOTE** at the top of tests/test_web_admin.py:
      ```
      # --reviews HIGH consensus #2: Phase 35 does NOT migrate existing
      # authenticated non-admin route handlers to Depends(current_user_id).
      # That migration is Phase 36 (per-user scoping). RBAC-01 is partially
      # satisfied this phase — factory exists, middleware populates user_id,
      # admin-gated routes consume the factory. Tests in this file cover
      # the admin gate only; tests for /trades, /journal, /dashboard, etc.
      # using Depends(current_user_id) belong to Phase 36.
      ```

    Use 2-space indent.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_admin.py 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^class TestAdminRouteInvariant" tests/test_web_admin.py` returns exactly one match; body populated.
    - `grep -n "^class TestAdminGate403SweepUnauthenticated" tests/test_web_admin.py` returns exactly one match (renamed from TestAdminGate403Sweep per --reviews split).
    - `grep -n "^class TestAdminGate403SweepHeaderAuth" tests/test_web_admin.py` returns exactly one match (--reviews split).
    - `grep -n "^class TestAdminGate403SweepNonAdminRole" tests/test_web_admin.py` returns exactly one match.
    - `grep -n "^class TestAdminPing" tests/test_web_admin.py` returns exactly one match.
    - `grep -n "^class TestPrePhase35RoutesStructuralParity" tests/test_web_admin.py` returns exactly one match (--reviews #3).
    - `grep -n "def _walk_routes" tests/test_web_admin.py` returns exactly one match (recursive walker per --reviews OpenCode).
    - `grep -c "_DETAIL_ADMIN_REQUIRED" tests/test_web_admin.py` returns ≥4 (locked detail asserted in every 403-sweep test).
    - `grep -c "application/json" tests/test_web_admin.py` returns ≥3 (JSON body asserted in 403-sweep + happy ping).
    - `grep -c "{'detail':" tests/test_web_admin.py` returns ≥3 (JSON body shape asserted).
    - `grep -n "consensus #2" tests/test_web_admin.py` returns at least one match (deferred-routes NOTE present).
    - `grep -cE "    def test_" tests/test_web_admin.py` returns ≥18 (3 invariant + 3 split-sweep + 4 ping + 4 structural parity + ≥4 from TestCurrentUserId/RequireAdmin/SubRouter inherited).
    - `grep -n "sys.modules.pop('web.app', None)" tests/test_web_admin.py` returns at least one match.
    - `grep -n "Structural comparison, NOT byte-identical" tests/test_web_admin.py` returns at least one match (--reviews #3 documented in code).
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminRouteInvariant` exits 0 with ≥2 tests.
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminGate403SweepUnauthenticated` exits 0 with ≥1 parametrized test.
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminGate403SweepHeaderAuth` exits 0 with ≥1 parametrized test.
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminGate403SweepNonAdminRole` exits 0 with ≥1 parametrized test.
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminPing` exits 0 with ≥4 tests (--reviews Codex negative test included).
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestPrePhase35RoutesStructuralParity` exits 0 with ≥4 tests.
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py` exits 0 overall.
    - `.venv/bin/pytest -x --tb=short` (full suite) exits 0 — no v1.2 regression (RBAC-01 "no observable behaviour change" verified).
    - Hex boundary: `.venv/bin/pytest -x --tb=short tests/test_web_healthz.py::TestWebHexBoundary` exits 0.
  </acceptance_criteria>
  <done>TestAdminRouteInvariant + TestAdminGate403SweepUnauthenticated + TestAdminGate403SweepHeaderAuth + TestAdminGate403SweepNonAdminRole + TestAdminPing + TestPrePhase35RoutesStructuralParity all populated; recursive walker present; JSON body schema asserted on every 403; structural parity replaces byte-identical; deferred-routes NOTE in source; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/admin/* | Untrusted client requests cross into admin-gated routes |
| middleware→dependency→handler | Three-layer auth chain: signed cookie → request.state.user_id → role check |
| route registration order | Future admin routes must register on admin_router (not application) — invariant test enforces |
| 403 response → client | JSON-only body, no HTML/stack-trace leak (--reviews #5 locked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05-01 | Elevation of privilege | admin gate bypass via wrong-router registration | mitigate | TestAdminRouteInvariant walks app.router.routes recursively after create_app() and fails the build if any /admin/* path lacks require_admin in its dependency chain. |
| T-35-05-02 | Spoofing | session cookie forgery | mitigate | itsdangerous URLSafeTimedSerializer signature on every decode; tampered uid → BadSignature → falls through; role read from auth.json live. |
| T-35-05-03 | Elevation of privilege | header-auth bypass to admin | mitigate (intentional) | D-06: header auth sets user_id=None; require_admin raises 403. TestAdminGate403SweepHeaderAuth regression guard with JSON body assertion. |
| T-35-05-04 | Tampering | backward-compat shim abuse | mitigate | Shim is read-only — calls get_user_by_email(payload['u']) and consults auth.json live. Cannot escalate: forged legacy cookie (valid signature, payload['u']='attacker') → shim returns None for any user not in auth.json. Bootstrap-gap test proves this. |
| T-35-05-05 | Spoofing | tsi_trusted reaching /admin/* | mitigate (Option B accepted) | Per Plan 02 Option B + Pitfall 1 + A1, tsi_trusted leaves user_id=None in Phase 35; admin using trusted device sees 403 on /admin/ping and must re-login via TOTP. Logged via logger.warning; test_trusted_device_admin_access_returns_403 in Plan 02 is the regression guard. |
| T-35-05-06 | Denial of service | startup invariant test cost | accept | Walks app.router.routes once per test invocation — O(routes) bounded (≤ ~50 routes). Test runtime ~10 ms. |
| T-35-05-07 | Repudiation | admin route access logging | accept | Existing AuthMiddleware logging unchanged; admin route hits are logged via standard request log. Detailed audit log for admin actions is Phase 37+ scope. |
| T-35-05-08 | Information disclosure | legacy cookie shim leaking row contents | mitigate | Shim resolves a uid; the row dict from get_user_by_email is NOT stored on the request or echoed. Only uid propagates. |
| T-35-05-09 | Information disclosure | 403 response body | mitigate | --reviews #5: every 403 from the admin gate is asserted JSON with locked detail string. No HTML/stack-trace leak path. |
| T-35-05-10 | Tampering | uid-spoof without auth.json row | mitigate | --reviews Codex negative test (test_admin_with_uid_no_resolved_user_cannot_pass_require_admin) proves a forged-but-signed uid in the cookie still fails the gate when get_user(uid) returns None. |
| T-35-05-11 | Information disclosure | structural-parity fixtures | accept | Structural comparison only — no sensitive HTML content is captured or stored; landmark strings are public page titles/headings already visible on production. |
</threat_model>

<verification>
- `.venv/bin/pytest -x --tb=short tests/test_web_admin.py` exits 0 — all admin tests green.
- `.venv/bin/pytest -x --tb=short tests/test_web_app_factory.py` exits 0 — existing app-factory invariants survive.
- `.venv/bin/pytest -x --tb=short tests/test_web_healthz.py::TestWebHexBoundary` exits 0.
- `.venv/bin/pytest -x --tb=short` (full suite) exits 0 — no v1.2 regression.
- `awk '/include_router\(admin_router\)/ {inc=NR} /add_middleware\(AuthMiddleware/ {mid=NR} END {exit (inc>0 && mid>0 && inc<mid)?0:1}' web/app.py` exits 0.
- RBAC-02 success criteria 1-4 hold.
- RBAC-01 success criteria 1, 3 hold for admin-gated routes; criterion 2 (every authenticated route uses Depends(current_user_id)) is explicitly PARTIAL (admin routes only) — full migration deferred to Phase 36 per --reviews consensus #2.
- RBAC-01 success criterion 5 (no observable behaviour change) holds via TestPrePhase35RoutesStructuralParity + full suite passing.
</verification>

<success_criteria>
- web/app.py wires admin_router via include_router BEFORE add_middleware(AuthMiddleware, ...) per D-08 + --reviews order comment.
- TestAdminRouteInvariant green: every /admin/* route has require_admin in dependency chain; recursive walker future-proofs against Mount/nested router.
- 403-sweep SPLIT into three classes (Unauthenticated, HeaderAuth, NonAdminRole); each asserts JSON body schema {'detail': 'Admin access required'} per --reviews #5.
- TestAdminPing: admin session → 200 + JSON {'ok': True}; legacy cookie shim resolves uid; bootstrap-gap returns 403; --reviews Codex negative test (uid set but no auth.json row → 403).
- TestPrePhase35RoutesStructuralParity replaces "byte-identical" with structural comparison on 4 narrowed routes per --reviews #3.
- Source-level NOTE documents that existing authenticated route migration is DEFERRED to Phase 36 per --reviews consensus #2 (RBAC-01 partial in this phase).
- All v1.2 routes unaffected (full suite green).
- RBAC-02 success criteria 1-4 fully satisfied; RBAC-01 partially satisfied with explicit Phase-36 handoff documented.
</success_criteria>

<output>
After completion, create `.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-05-SUMMARY.md`
</output>
