---
phase: 35
plan: 04
type: execute
wave: 3
depends_on: [35-03]
files_modified:
  - web/routes/admin/__init__.py
  - tests/test_web_admin.py
autonomous: true
requirements: [RBAC-02]
tags: [auth, admin, sub-router, fastapi]
must_haves:
  truths:
    - "web/routes/admin/__init__.py exposes `router` as an APIRouter with prefix='/admin' and dependencies=[Depends(require_admin)] injected at construction"
    - "GET /admin/ping route is registered on the router and returns {'ok': True}"
    - "The router is import-only — Plan 05 wires it into web/app.py; this plan does NOT modify web/app.py"
    - "Hex boundary preserved: web/routes/admin/__init__.py imports only fastapi, web.dependencies (no signal_engine, data_fetcher, or main)"
  artifacts:
    - path: "web/routes/admin/__init__.py"
      provides: "APIRouter sub-router with require_admin gate baked in at mount time (D-08); GET /admin/ping route (D-09)"
      contains: "APIRouter(prefix='/admin', dependencies=[Depends(require_admin)])"
    - path: "tests/test_web_admin.py"
      provides: "TestAdminSubRouter class — confirms router shape, prefix, dependencies, and the /ping route are all present BEFORE app wiring"
      contains: "class TestAdminSubRouter"
  key_links:
    - from: "web/routes/admin/__init__.py"
      to: "web.dependencies.require_admin"
      via: "top-level import; injected into APIRouter dependencies kwarg"
      pattern: "from web.dependencies import require_admin"
    - from: "tests/test_web_admin.py::TestAdminSubRouter"
      to: "web.routes.admin.router"
      via: "direct import + introspection of router.prefix, router.dependencies, router.routes"
      pattern: "from web.routes.admin import router"
---

<objective>
Create the `web/routes/admin/` package containing the `APIRouter` sub-router with `prefix='/admin'` and `dependencies=[Depends(require_admin)]` set at construction time (D-08), and register one concrete route `GET /admin/ping → 200 {"ok": true}` (D-09). Also populate the `TestAdminSubRouter` Wave-0 stub class created in Plan 03 with introspection tests proving the router shape is correct independently of any app wiring. This plan does NOT modify `web/app.py` — that wiring is Plan 05.

Purpose: The admin gate is set at the router-mount level, not per-route. New admin routes in Phase 36+ are added to this router and inherit the gate automatically; if a future contributor adds a route to `application` instead of `router`, the Plan 05 startup invariant test catches it. Plan 05 will assert that after `include_router(router)`, every `/admin/*` route in `app.routes` carries `require_admin` in its `.dependencies` — which only holds if the router was constructed with the gate (this plan).

Output: One new package directory + `__init__.py` + a populated `TestAdminSubRouter` class in the existing `tests/test_web_admin.py` file (created as a stub in Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-CONTEXT.md
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-PATTERNS.md
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-RESEARCH.md
@web/dependencies.py
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-01-SUMMARY.md
@.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-03-SUMMARY.md

<interfaces>
<!-- From PATTERNS.md section "web/routes/admin/__init__.py — NEW FILE" -->
<!-- and RESEARCH.md Pattern 4 — Sub-router registration -->

Module surface:
  - router: APIRouter — prefix='/admin', dependencies=[Depends(require_admin)]
  - __all__ = ['router']

Route:
  - GET /admin/ping → def ping() returns {'ok': True}

VERIFIED fact (RESEARCH.md Pattern 5, Pitfall 5):
  - After app.include_router(router), each registered APIRoute appears in app.routes (flat list, no Mount wrapping)
  - Each APIRoute.dependencies list carries the router-level dependencies injected at registration
  - The startup invariant test (Plan 05) walks app.routes and checks `require_admin in [d.dependency for d in route.dependencies]`

Pre-wiring introspection (this plan, before include_router):
  - router.prefix == '/admin'
  - router.dependencies — list of Depends() objects; one of them has .dependency == require_admin
  - router.routes — list of APIRoute objects; one has path=='/ping' and methods=={'GET'}

Note on router.routes path: BEFORE include_router, router.routes entries carry path '/ping' (no prefix); AFTER include_router on app, app.routes entries carry path '/admin/ping' (prefix applied at registration). Plan 05 tests assert the post-include shape; this plan tests the pre-include shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create web/routes/admin/__init__.py with APIRouter + require_admin gate + GET /admin/ping</name>
  <read_first>
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-PATTERNS.md (section "web/routes/admin/__init__.py — NEW FILE" — full file pattern)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-RESEARCH.md (Pattern 4 — sub-router registration; Pitfall 5 — APIRouter vs include_router; Anti-Patterns to Avoid)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-CONTEXT.md (D-08, D-09)
    - web/dependencies.py (must exist from Plan 03 — confirm `require_admin` is importable)
    - web/routes/totp/__init__.py (read first 20 lines — match package-__init__ docstring and import-block style)
  </read_first>
  <behavior>
    - `from web.routes.admin import router` succeeds and `router` is an instance of `fastapi.APIRouter`.
    - `router.prefix == '/admin'`.
    - `router.dependencies` is a non-empty list; at least one entry has `.dependency is require_admin` (the same callable object exported from web.dependencies).
    - `router.routes` contains an APIRoute whose `.path == '/ping'` (pre-include shape; prefix applied later) and whose `methods` set includes 'GET'.
    - The route handler is callable and returns `{'ok': True}` when invoked directly (no FastAPI dependency injection in this unit test — call the handler function as a plain Python callable).
    - `web.routes.admin.__all__ == ['router']`.
    - Hex boundary: web/routes/admin/__init__.py does NOT import signal_engine, data_fetcher, or main.
  </behavior>
  <action>
    Create the directory `web/routes/admin/` and the file `web/routes/admin/__init__.py`. Use the full file pattern from PATTERNS.md "web/routes/admin/__init__.py — NEW FILE" section. Module docstring should reference Phase 35 D-08 / D-09 and the anti-pattern note "New admin routes register on `router`, not on `application` directly".

    Imports (top-level — safe; web/dependencies.py exists from Plan 03 and does not import from web/routes/):
      - `from fastapi import APIRouter, Depends`
      - `from web.dependencies import require_admin`

    Module body:
      - `router = APIRouter(prefix='/admin', dependencies=[Depends(require_admin)])` per D-08.
      - Decorate `def ping()` with `@router.get('/ping')` per D-09. The handler returns the dict literal `{'ok': True}` — minimal, easy to assert on.
      - `__all__ = ['router']`.

    Use 2-space indent. Do NOT register the router on any app — that is Plan 05's job. Do NOT import signal_engine, data_fetcher, or main (hex boundary). Do NOT register additional admin routes — D-09 specifies one route only; Phase 36+ owns the next ones.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminSubRouter tests/test_web_healthz.py::TestWebHexBoundary 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Directory `web/routes/admin/` exists.
    - File `web/routes/admin/__init__.py` exists.
    - `grep -n "from fastapi import APIRouter, Depends" web/routes/admin/__init__.py` returns exactly one match.
    - `grep -n "from web.dependencies import require_admin" web/routes/admin/__init__.py` returns exactly one match.
    - `grep -n "router = APIRouter(prefix='/admin', dependencies=\[Depends(require_admin)\])" web/routes/admin/__init__.py` returns exactly one match.
    - `grep -n "@router.get('/ping')" web/routes/admin/__init__.py` returns exactly one match.
    - `grep -n "return {'ok': True}" web/routes/admin/__init__.py` returns exactly one match.
    - `grep -n "^__all__ = \['router'\]$" web/routes/admin/__init__.py` returns exactly one match.
    - `grep -cE "^import (signal_engine|data_fetcher|main)$|^from (signal_engine|data_fetcher|main)" web/routes/admin/__init__.py` returns 0 (hex boundary).
    - `python -c "from web.routes.admin import router; from fastapi import APIRouter; assert isinstance(router, APIRouter); assert router.prefix == '/admin'; print('ok')"` prints `ok` and exits 0.
    - `.venv/bin/pytest -x --tb=short tests/test_web_healthz.py::TestWebHexBoundary` exits 0 — new package does not violate hex boundary.
  </acceptance_criteria>
  <done>web/routes/admin/__init__.py created with router + ping route per D-08/D-09; hex boundary green; router importable and well-shaped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate TestAdminSubRouter — introspection tests proving router shape before app wiring</name>
  <read_first>
    - tests/test_web_admin.py (read fully — the Wave-0 stub `class TestAdminSubRouter: pass` was created by Plan 03; replace its body with real tests)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-PATTERNS.md (section "tests/test_web_admin.py — NEW FILE" — class structure)
    - web/routes/admin/__init__.py (the file just created in Task 1 — confirm router has prefix, dependencies, and /ping route)
    - .planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-VALIDATION.md (Per-Task Verification Map — Task 35-03-02 startup invariant target row; this plan's tests are pre-wiring, Plan 05 covers the post-wiring invariant)
  </read_first>
  <behavior>
    - test_router_is_apirouter_instance — `isinstance(router, APIRouter)` is True.
    - test_router_prefix_is_admin — `router.prefix == '/admin'`.
    - test_router_has_require_admin_dependency — `require_admin in [d.dependency for d in router.dependencies]` is True.
    - test_router_has_ping_route — exactly one APIRoute in router.routes with `path == '/ping'` and `'GET' in methods`.
    - test_ping_handler_returns_ok_dict — directly calling the ping function (extracted via `next(r for r in router.routes if r.path == '/ping').endpoint`) returns `{'ok': True}`.
    - test_router_all_exports — `web.routes.admin.__all__ == ['router']`.
  </behavior>
  <action>
    In tests/test_web_admin.py, replace the existing `class TestAdminSubRouter: pass` stub (created in Plan 03) with a populated class containing six named tests covering each behavior above. Use top-of-file imports if not already present: `from fastapi import APIRouter`, `from fastapi.routing import APIRoute`, `from web.routes.admin import router`, `from web.dependencies import require_admin`. Use 2-space indent. Do NOT call `create_app()` or `include_router` in these tests — they introspect `router` directly (pre-wiring shape). Plan 05 owns the post-wiring tests.

    If a previous task or Plan 03 already populated `TestAdminSubRouter` with anything other than `pass`, do NOT overwrite — instead, add any missing tests as additional `def test_*` methods alongside.
  </action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminSubRouter 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^class TestAdminSubRouter" tests/test_web_admin.py` returns exactly one match.
    - `grep -cE "    def test_(router_is_apirouter|router_prefix|router_has_require_admin|router_has_ping|ping_handler_returns|router_all_exports)" tests/test_web_admin.py` returns ≥6.
    - `grep -n "class TestAdminSubRouter:\s*\n\s*pass" tests/test_web_admin.py` returns 0 — stub body has been replaced.
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminSubRouter` exits 0 with ≥6 tests collected and passing.
    - `.venv/bin/pytest -x --tb=short tests/test_web_admin.py` exits 0 (entire file green).
    - `.venv/bin/pytest -x --tb=short` (full suite) exits 0.
  </acceptance_criteria>
  <done>TestAdminSubRouter populated with ≥6 introspection tests; router shape verified independent of app wiring; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router construction → app registration | The router-level dependencies list must survive include_router (verified by Plan 05); if a future contributor uses APIRouter() without dependencies, the gate is silently dropped |
| /admin/ping → handler | After Plan 05 wiring, the gate enforces admin-only access; in this plan the route exists in isolation (no app to call it through) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04-01 | Elevation of privilege | sub-router gate omission | mitigate | D-08 sets the gate at router-construction time; Plan 05's startup invariant test walks app.routes and fails the build if any `/admin/*` route lacks require_admin in its dependency chain. |
| T-35-04-02 | Spoofing | /admin/ping response forgery | accept | Response body `{'ok': True}` carries no sensitive content; trivial to fake but trivial to verify. The route is a probe target, not an auth oracle. |
| T-35-04-03 | Tampering | per-route dependencies bypass | mitigate (anti-pattern documented) | RESEARCH.md "Anti-Patterns to Avoid" calls out per-route `dependencies=` lists; admin routes must register on `router`, not `application`. Plan 05 invariant test catches violations. |
| T-35-04-04 | Information disclosure | hex boundary leak | mitigate | tests/test_web_healthz.py::TestWebHexBoundary scans web/**/*.py including the new admin package; will fail the build if signal_engine/data_fetcher/main are imported. |
</threat_model>

<verification>
- `.venv/bin/pytest -x --tb=short tests/test_web_admin.py::TestAdminSubRouter` exits 0 with ≥6 tests.
- `.venv/bin/pytest -x --tb=short tests/test_web_healthz.py::TestWebHexBoundary` exits 0.
- `.venv/bin/pytest -x --tb=short` (full suite) exits 0.
- Grep gates per acceptance_criteria pass on web/routes/admin/__init__.py.
</verification>

<success_criteria>
- web/routes/admin/__init__.py exists with router (APIRouter prefix='/admin' + Depends(require_admin) dependency) + GET /admin/ping route.
- Hex boundary preserved (no signal_engine/data_fetcher/main imports).
- TestAdminSubRouter populated with ≥6 introspection tests; all green.
- web/app.py NOT modified in this plan (Plan 05 owns wiring).
- Full pytest suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/35-cookie-depends-current-user-sub-router-admin-gate/35-04-SUMMARY.md`
</output>
