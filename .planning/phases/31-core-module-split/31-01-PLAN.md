---
phase: 31-core-module-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - state_manager/__init__.py
  - state_manager/io.py
  - state_manager/migrations.py
  - state_manager/validation.py
  - state_manager/trades.py
  - state_manager.py
autonomous: true
requirements:
  - OPS-05

must_haves:
  truths:
    - "All existing callers import from state_manager without any path change"
    - "state_manager/__init__.py re-exports all 6 public symbols: load_state, save_state, reset_state, mutate_state, record_trade, append_warning, clear_warnings, clear_warnings_by_source, update_equity_history"
    - "mutate_state calls io._save_state_unlocked directly — flock deadlock avoidance preserved"
    - "_assert_migration_chain_contiguous() fires at the bottom of migrations.py on import"
    - "All daughter files are ≤500 LOC"
    - "Full test suite is green after the split"
  artifacts:
    - path: "state_manager/__init__.py"
      provides: "Public API orchestrator: load_state, save_state, reset_state, mutate_state"
      min_lines: 250
    - path: "state_manager/io.py"
      provides: "I/O kernel: _atomic_write_unlocked, _atomic_write, _backup_corrupt, _save_state_unlocked"
    - path: "state_manager/migrations.py"
      provides: "All _migrate_vX_to_vY, MIGRATIONS, _default_market_registry, _default_strategy_settings, _assert_migration_chain_contiguous, _migrate"
    - path: "state_manager/validation.py"
      provides: "_assert_tz_aware, _coerce_legacy_naive_iso, _validate_trade, _validate_loaded_state, _read_signal_strategy_version"
    - path: "state_manager/trades.py"
      provides: "record_trade, append_warning, clear_warnings, clear_warnings_by_source, update_equity_history"
  key_links:
    - from: "state_manager/__init__.py (mutate_state)"
      to: "state_manager/io.py (_save_state_unlocked)"
      via: "direct call: io._save_state_unlocked(state, path)"
      pattern: "io\\._save_state_unlocked"
    - from: "state_manager/migrations.py (bottom)"
      to: "_assert_migration_chain_contiguous()"
      via: "module-level call at end of file"
      pattern: "_assert_migration_chain_contiguous\\(\\)"
    - from: "state_manager/__init__.py"
      to: "state_manager/io, state_manager/migrations, state_manager/validation, state_manager/trades"
      via: "from state_manager.io import ..., from state_manager.migrations import ..."
      pattern: "from state_manager\\.io import"
---

<objective>
Convert `state_manager.py` (1,293 LOC) into a `state_manager/` package with five focused daughter files.

Purpose: Enable per-user `user_id` injection diffs to be reviewed at the submodule level in Phase 36+.
Output: `state_manager/` directory with `__init__.py`, `io.py`, `migrations.py`, `validation.py`, `trades.py`. Old `state_manager.py` deleted. All callers unchanged.
</objective>

<execution_context>
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/execute-plan.md
</execution_context>

<context>
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/ROADMAP.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/31-core-module-split/31-CONTEXT.md

<interfaces>
<!-- Key contracts from state_manager.py (lines 128-1293). Read state_manager.py fully before splitting. -->

Public API (re-exported by __init__.py):
  load_state(path=Path(STATE_FILE), now=None, _under_lock=False) -> dict
  save_state(state: dict, path=Path(STATE_FILE)) -> None
  reset_state(initial_account=INITIAL_ACCOUNT) -> dict
  mutate_state(mutator: Callable[[dict], dict], path=Path(STATE_FILE)) -> dict
  record_trade(state: dict, trade: dict) -> dict
  append_warning(state: dict, source: str, message: str, now=None) -> dict
  clear_warnings(state: dict) -> dict
  clear_warnings_by_source(state: dict, source: str) -> dict
  update_equity_history(state: dict, date: str, equity: float) -> dict

Critical deadlock invariant (CONTEXT.md D-02):
  mutate_state holds fcntl.LOCK_EX; it MUST call io._save_state_unlocked
  (NOT save_state) to avoid intra-process flock re-acquisition deadlock.

Function-to-file assignment:
  io.py:          _atomic_write_unlocked (L658), _atomic_write (L712), _backup_corrupt (L764), _save_state_unlocked (L1074)
  validation.py:  _assert_tz_aware (L128), _coerce_legacy_naive_iso (L147), _validate_trade (L790), _validate_loaded_state (L864), _read_signal_strategy_version (L637)
  migrations.py:  _migrate_v1_to_v2 (L195) through MIGRATIONS (L587), _assert_migration_chain_contiguous (L602), _migrate (L751), _default_market_registry (L330), _default_strategy_settings (L334), _quantize_aud (L384)
  trades.py:      append_warning (L1131), clear_warnings (L1165), clear_warnings_by_source (L1190), record_trade (L1212), update_equity_history (L1248)
  __init__.py:    module docstring + imports + module-level constants (L100-L127) + reset_state (L900) + load_state (L950) + save_state (L1038) + mutate_state (L1090)

NOTE: _read_signal_strategy_version goes to validation.py (not migrations.py) because migrations.py would exceed 500 LOC otherwise (D-04 in CONTEXT.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package and create io.py + validation.py + trades.py</name>
  <read_first>
    state_manager.py (full file — all 1,293 lines; read it completely before writing a single daughter file)
  </read_first>
  <files>
    state_manager/__init__.py
    state_manager/io.py
    state_manager/validation.py
    state_manager/trades.py
  </files>
  <action>
Create directory `state_manager/` and write three daughter files plus the package `__init__.py` stub.

**state_manager/io.py** — I/O kernel. Move verbatim from state_manager.py:
- `_atomic_write_unlocked` (lines 658–711)
- `_atomic_write` (lines 712–750)
- `_backup_corrupt` (lines 764–789)
- `_save_state_unlocked` (lines 1074–1089)

Imports needed: `fcntl`, `json`, `logging`, `os`, `sys`, `tempfile`, `from datetime import datetime`, `from pathlib import Path`, `from system_params import STATE_FILE, _decimal_default`.
Add `logger = logging.getLogger(__name__)` at module level.
No other functions. Target: ~160 LOC.

**state_manager/validation.py** — validation + datetime guards. Move verbatim from state_manager.py:
- `_assert_tz_aware` (lines 128–146)
- `_coerce_legacy_naive_iso` (lines 147–194)
- `_validate_trade` (lines 790–863)
- `_validate_loaded_state` (lines 864–899)
- `_read_signal_strategy_version` (lines 637–652)

Imports needed: `import logging`, `import math`, `import warnings`, `from datetime import datetime, UTC, timezone`, `from typing import Any`, `from system_params import (DEFAULT_MARKETS, DEFAULT_STRATEGY_SETTINGS, STATE_SCHEMA_VERSION, _DEFAULT_AUDUSD_LABEL, _DEFAULT_SPI_LABEL)`.
Also needs `_REQUIRED_TRADE_FIELDS` and `_REQUIRED_STATE_KEYS` constants (copy verbatim from lines 107–122 of state_manager.py — these are used by _validate_trade and _validate_loaded_state).
Add `logger = logging.getLogger(__name__)` at module level.
Target: ~200 LOC.

**state_manager/trades.py** — record helpers. Move verbatim from state_manager.py:
- `append_warning` (lines 1131–1164)
- `clear_warnings` (lines 1165–1189)
- `clear_warnings_by_source` (lines 1190–1211)
- `record_trade` (lines 1212–1247)
- `update_equity_history` (lines 1248–1293)

Imports needed: `import math`, `import zoneinfo`, `from datetime import datetime, UTC, timezone`, `from system_params import MAX_WARNINGS`.
Also needs `_AWST = zoneinfo.ZoneInfo('Australia/Perth')` constant.
Also needs `_assert_tz_aware` — import it: `from state_manager.validation import _assert_tz_aware`.
Target: ~190 LOC.

**state_manager/__init__.py** stub — minimal stub that will be completed in Task 2. For now just write:
```python
# Populated in Task 2
```
This avoids a broken import surface during Task 1 file creation.
  </action>
  <verify>
    <automated>
      python -c "import ast; ast.parse(open('state_manager/io.py').read()); print('io.py parses OK')"
      python -c "import ast; ast.parse(open('state_manager/validation.py').read()); print('validation.py parses OK')"
      python -c "import ast; ast.parse(open('state_manager/trades.py').read()); print('trades.py parses OK')"
      wc -l state_manager/io.py state_manager/validation.py state_manager/trades.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `state_manager/io.py` exists and contains `def _atomic_write_unlocked(`, `def _atomic_write(`, `def _backup_corrupt(`, `def _save_state_unlocked(`
    - `state_manager/validation.py` exists and contains `def _assert_tz_aware(`, `def _coerce_legacy_naive_iso(`, `def _validate_trade(`, `def _validate_loaded_state(`, `def _read_signal_strategy_version(`
    - `state_manager/trades.py` exists and contains `def append_warning(`, `def clear_warnings(`, `def clear_warnings_by_source(`, `def record_trade(`, `def update_equity_history(`
    - `grep -c "def _atomic_write_unlocked" state_manager/io.py` exits 0 with output `1`
    - `python -c "import ast; ast.parse(open('state_manager/io.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('state_manager/validation.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('state_manager/trades.py').read())"` exits 0
    - `wc -l state_manager/io.py` outputs a value ≤500
    - `wc -l state_manager/validation.py` outputs a value ≤500
    - `wc -l state_manager/trades.py` outputs a value ≤500
  </acceptance_criteria>
  <done>Three daughter files exist, parse cleanly, each ≤500 LOC.</done>
</task>

<task type="auto">
  <name>Task 2: Create migrations.py and complete __init__.py, then delete state_manager.py</name>
  <read_first>
    state_manager.py (still present — read before deleting)
    state_manager/io.py
    state_manager/validation.py
    state_manager/trades.py
  </read_first>
  <files>
    state_manager/migrations.py
    state_manager/__init__.py
    state_manager.py
  </files>
  <action>
**state_manager/migrations.py** — all migration logic. Move verbatim from state_manager.py:
- `_default_market_registry` (lines 330–333)
- `_default_strategy_settings` (lines 334–344)
- `_migrate_v1_to_v2` (lines 195–212)
- `_migrate_v2_to_v3` (lines 213–239)
- `_migrate_v3_to_v4` (lines 240–267)
- `_migrate_v4_to_v5` (lines 268–297)
- `_migrate_v5_to_v6` (lines 298–311)
- `_migrate_v6_to_v7` (lines 312–329)
- `_migrate_v7_to_v8` (lines 345–383)
- `_quantize_aud` (lines 384–409)
- `_migrate_v8_to_v9` (lines 410–481)
- `_migrate_v9_to_v10` (lines 482–548)
- `_migrate_v10_to_v11` (lines 549–601)
- `MIGRATIONS: dict` (lines 587–601)
- `_assert_migration_chain_contiguous` (lines 602–633)
- `_migrate` orchestrator (lines 751–763)
- Module-level call `_assert_migration_chain_contiguous()` at the very bottom of the file (mirrors line 634 of state_manager.py — MUST remain so it fires at import time)

DO NOT include `_read_signal_strategy_version` here — it lives in `validation.py` to stay under 500 LOC.

Imports needed: `import logging`, `from decimal import Decimal as _Decimal`, `from system_params import (INITIAL_ACCOUNT, STATE_SCHEMA_VERSION, STRATEGY_VERSION, AUD_QUANTIZE, AUD_ROUND, AUDUSD_CONTRACTS, DEFAULT_MARKETS, DEFAULT_STRATEGY_SETTINGS, SPI_CONTRACTS, _DEFAULT_AUDUSD_LABEL, _DEFAULT_SPI_LABEL, default_settings_for_market)`.
Also needs `from state_manager.validation import _read_signal_strategy_version` (used in _migrate_v9_to_v10's docstring references; verify if called directly — check state_manager.py line 482 body to confirm).
Add `logger = logging.getLogger(__name__)`.
Target: ≤500 LOC.

**state_manager/__init__.py** — public API orchestrator. Write the full content:
- Module-level docstring (copy and adapt from state_manager.py lines 1–59)
- All imports (stdlib + system_params + intra-package):
  ```python
  import fcntl
  import json
  import logging
  import os
  import sys
  import warnings
  import zoneinfo
  from datetime import UTC, datetime, timezone
  from pathlib import Path
  from typing import Any, Callable
  from system_params import (INITIAL_ACCOUNT, MAX_WARNINGS, STATE_FILE, STATE_SCHEMA_VERSION, AUD_QUANTIZE, AUD_ROUND, _decimal_default, AUDUSD_CONTRACTS, DEFAULT_MARKETS, DEFAULT_STRATEGY_SETTINGS, SPI_CONTRACTS, _DEFAULT_AUDUSD_LABEL, _DEFAULT_SPI_LABEL, default_settings_for_market, STRATEGY_VERSION)
  from decimal import Decimal as _Decimal
  from state_manager import io, migrations, validation, trades
  ```
- Module-level constants from state_manager.py lines 100–127:
  - `logger = logging.getLogger(__name__)`
  - `_AWST = zoneinfo.ZoneInfo('Australia/Perth')`
  - `_REQUIRED_TRADE_FIELDS` (frozenset, lines 107–111) — re-export for any downstream that imported it
  - `_REQUIRED_STATE_KEYS` (frozenset, lines 116–122)
- `reset_state` (lines 900–949) — verbatim
- `load_state` (lines 950–1037) — verbatim; calls `migrations._migrate`, `validation._coerce_legacy_naive_iso`, `validation._validate_loaded_state`, `io._backup_corrupt`
- `save_state` (lines 1038–1073) — verbatim; calls `io._atomic_write`
- `mutate_state` (lines 1090–1130) — verbatim; CRITICAL: the body calls `io._save_state_unlocked(state, path)` directly (NOT save_state) to avoid intra-process flock deadlock. Confirm this is exactly what the existing code does at line 1090 and preserve it exactly.
- Public re-exports for all symbols callers import:
  ```python
  # Re-export public API from submodules so existing callers work unchanged
  from state_manager.trades import (
      append_warning,
      clear_warnings,
      clear_warnings_by_source,
      record_trade,
      update_equity_history,
  )
  ```
- Use `__all__` to suppress F401:
  ```python
  __all__ = [
      'load_state', 'save_state', 'reset_state', 'mutate_state',
      'append_warning', 'clear_warnings', 'clear_warnings_by_source',
      'record_trade', 'update_equity_history',
  ]
  ```
Target: ~300 LOC.

**Delete state_manager.py** — only after __init__.py is fully written and parses clean:
```bash
rm state_manager.py
```
  </action>
  <verify>
    <automated>
      python -c "import ast; ast.parse(open('state_manager/migrations.py').read()); print('migrations.py parses OK')"
      python -c "import ast; ast.parse(open('state_manager/__init__.py').read()); print('__init__.py parses OK')"
      python -c "from state_manager import load_state, save_state, reset_state, mutate_state, append_warning, clear_warnings, clear_warnings_by_source, record_trade, update_equity_history; print('all public imports OK')"
      python -c "from state_manager import io; print(dir(io))"
      wc -l state_manager/migrations.py state_manager/__init__.py
      test ! -f state_manager.py && echo "state_manager.py deleted OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `state_manager/migrations.py` exists with `def _migrate_v1_to_v2(`, `def _migrate_v10_to_v11(`, `MIGRATIONS`, `def _migrate(`, `_assert_migration_chain_contiguous()`
    - `grep -c "^_assert_migration_chain_contiguous()" state_manager/migrations.py` outputs `1` (module-level call at bottom)
    - `state_manager/__init__.py` contains `def reset_state(`, `def load_state(`, `def save_state(`, `def mutate_state(`
    - `grep "io._save_state_unlocked" state_manager/__init__.py` finds exactly 1 match (in mutate_state body)
    - `grep -v "^#" state_manager/__init__.py | grep "save_state(state" | grep -v "io\._save_state_unlocked" | grep -v "def save_state"` returns empty (no accidental call to save_state inside mutate_state)
    - `wc -l state_manager/migrations.py` outputs ≤500
    - `wc -l state_manager/__init__.py` outputs ≤500
    - `python -c "from state_manager import load_state, save_state, reset_state, mutate_state, append_warning, clear_warnings, clear_warnings_by_source, record_trade, update_equity_history"` exits 0
    - `test ! -f state_manager.py` exits 0 (old flat file deleted)
    - `pytest -x --tb=short tests/test_state_manager.py` exits 0
  </acceptance_criteria>
  <done>migrations.py and __init__.py complete; state_manager.py deleted; state_manager test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → state_manager package | callers use public API names; internal submodule paths are not exposed |
| state_manager/io.py → filesystem | same atomic-write + flock contract as before; no new I/O surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | mutate_state flock path | mitigate | Preserve exact `io._save_state_unlocked` call (not `save_state`) in mutate_state — acceptance_criteria grep gate enforces this |
| T-31-01-02 | Repudiation | module-level _assert_migration_chain_contiguous | mitigate | Acceptance criteria grep for `^_assert_migration_chain_contiguous()` at bottom of migrations.py; fires on import |
| T-31-01-03 | Elevation of privilege | caller imports internal submodule | accept | __all__ + re-export pattern mirrors Phase 30 convention; callers use package-level names only |
</threat_model>

<verification>
Run after both tasks:

```bash
cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals
pytest -x --tb=short tests/test_state_manager.py tests/test_naive_datetime_fail_closed.py tests/test_migration_contiguity.py
grep -rn "import state_manager\|from state_manager import" daily_run.py daily_run_helpers.py main.py web/routes/trades/__init__.py web/routes/dashboard/_renderers.py web/routes/paper_trades/__init__.py web/routes/markets.py
wc -l state_manager/__init__.py state_manager/io.py state_manager/migrations.py state_manager/validation.py state_manager/trades.py
```

All lines ≤500. All imports resolve. Test suite green.
</verification>

<success_criteria>
- `state_manager/` package exists with 5 files, all ≤500 LOC
- `state_manager.py` is deleted
- All existing import forms (`import state_manager`, `from state_manager import X`) resolve unchanged
- `mutate_state` calls `io._save_state_unlocked` (not `save_state`)
- `_assert_migration_chain_contiguous()` module-level call at bottom of `migrations.py`
- `pytest -x --tb=short tests/test_state_manager.py tests/test_naive_datetime_fail_closed.py tests/test_migration_contiguity.py` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-core-module-split/31-01-SUMMARY.md`
</output>
