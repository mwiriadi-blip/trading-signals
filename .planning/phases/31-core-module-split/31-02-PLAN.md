---
phase: 31-core-module-split
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sizing_engine/__init__.py
  - sizing_engine/_models.py
  - sizing_engine/sizing.py
  - sizing_engine/stops.py
  - sizing_engine/pyramid.py
  - sizing_engine/close.py
  - sizing_engine.py
autonomous: true
requirements:
  - OPS-05

must_haves:
  truths:
    - "All existing callers import from sizing_engine without any path change"
    - "sizing_engine/__init__.py re-exports all public symbols: step, calc_position_size, get_trailing_stop, check_stop_hit, check_pyramid, detect_drift, compute_unrealised_pnl, ClosedTrade, SizingDecision, PyramidDecision, StepResult, DriftEvent"
    - "All daughter files are ≤500 LOC"
    - "Full test suite is green after the split"
  artifacts:
    - path: "sizing_engine/_models.py"
      provides: "SizingDecision, PyramidDecision, ClosedTrade, StepResult, DriftEvent dataclasses"
    - path: "sizing_engine/sizing.py"
      provides: "_vol_scale, calc_position_size, compute_unrealised_pnl"
    - path: "sizing_engine/stops.py"
      provides: "get_trailing_stop, check_stop_hit"
    - path: "sizing_engine/pyramid.py"
      provides: "check_pyramid, detect_drift"
    - path: "sizing_engine/close.py"
      provides: "_close_position"
    - path: "sizing_engine/__init__.py"
      provides: "step() orchestrator + re-exports from all submodules"
  key_links:
    - from: "sizing_engine/__init__.py (step)"
      to: "sizing_engine/close.py (_close_position), sizing_engine/sizing.py, sizing_engine/stops.py, sizing_engine/pyramid.py"
      via: "from sizing_engine.{module} import ..."
      pattern: "from sizing_engine\\."
    - from: "sizing_engine/__init__.py"
      to: "sizing_engine/_models.py"
      via: "re-exports: ClosedTrade, SizingDecision, etc."
      pattern: "from sizing_engine\\._models import"
---

<objective>
Convert `sizing_engine.py` (820 LOC) into a `sizing_engine/` package with six focused daughter files.

Purpose: Enable per-user `user_id` injection diffs to be reviewed at the submodule level in Phase 36+. Pure-math hex boundary preserved.
Output: `sizing_engine/` directory with `__init__.py`, `_models.py`, `sizing.py`, `stops.py`, `pyramid.py`, `close.py`. Old `sizing_engine.py` deleted. All callers unchanged.
</objective>

<execution_context>
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/execute-plan.md
</execution_context>

<context>
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/ROADMAP.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/31-core-module-split/31-CONTEXT.md

<interfaces>
<!-- Key contracts from sizing_engine.py (lines 1–820). Read sizing_engine.py fully before splitting. -->

Public API (re-exported by __init__.py):
  step(signal, ohlcv, positions, state, signals, markets, strategy_settings) -> StepResult
  calc_position_size(signal, atr, account, risk_pct, multiplier, cost_aud_open) -> SizingDecision
  get_trailing_stop(position) -> float
  check_stop_hit(position, ohlcv_bar) -> bool
  check_pyramid(position, ohlcv_bar, signal) -> PyramidDecision
  detect_drift(positions: dict, signals: dict) -> list[DriftEvent]
  compute_unrealised_pnl(position, last_price, cost_aud_open) -> float
  ClosedTrade, SizingDecision, PyramidDecision, StepResult, DriftEvent (dataclasses)

Hex boundary constraint (CONTEXT.md): sizing_engine is pure math.
  Must NOT import state_manager, notifier, dashboard, main, requests, datetime, os, or any I/O module.
  Only allowed imports: dataclasses, math, signal_engine constants, system_params constants.

Function-to-file assignment:
  _models.py:   SizingDecision (L41), PyramidDecision (L51), ClosedTrade (L62), StepResult (L78), DriftEvent (L97) — all dataclasses. ~75 LOC.
  sizing.py:    _vol_scale (L194), calc_position_size (L211), compute_unrealised_pnl (L493). ~140 LOC.
  stops.py:     get_trailing_stop (L280), check_stop_hit (L361). ~160 LOC.
  pyramid.py:   detect_drift (L116), check_pyramid (L439). ~135 LOC.
  close.py:     _close_position (L766). ~55 LOC.
  __init__.py:  step() orchestrator (L545–L765, ~220 LOC) + module docstring + imports + re-exports. ~260 LOC.

Caller import surface (must remain valid):
  from sizing_engine import step                     (backtest/simulator.py)
  from sizing_engine import check_pyramid, detect_drift (web/routes/trades/__init__.py)
  from sizing_engine import get_trailing_stop        (web/routes/dashboard/_renderers.py)
  from sizing_engine import ClosedTrade              (daily_run_helpers.py)
  import sizing_engine                               (daily_run.py, main.py, daily_run_helpers.py)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package and create _models.py + sizing.py + stops.py + pyramid.py + close.py</name>
  <read_first>
    sizing_engine.py (full file — all 820 lines; read it completely before writing any daughter file)
  </read_first>
  <files>
    sizing_engine/_models.py
    sizing_engine/sizing.py
    sizing_engine/stops.py
    sizing_engine/pyramid.py
    sizing_engine/close.py
    sizing_engine/__init__.py
  </files>
  <action>
Create directory `sizing_engine/` and write five daughter files plus the package `__init__.py` stub.

**sizing_engine/_models.py** — all 5 dataclasses. Move verbatim from sizing_engine.py:
- `SizingDecision` dataclass (lines 40–50)
- `PyramidDecision` dataclass (lines 51–61)
- `ClosedTrade` dataclass (lines 62–77)
- `StepResult` dataclass (lines 78–96)
- `DriftEvent` dataclass (lines 97–115)

Imports needed: `import dataclasses` only.
Target: ~85 LOC.

**sizing_engine/sizing.py** — position sizing math. Move verbatim from sizing_engine.py:
- `_vol_scale` (lines 194–210)
- `calc_position_size` (lines 211–279)
- `compute_unrealised_pnl` (lines 493–544)

Imports needed: `import math`, `from system_params import (RISK_PCT_LONG, RISK_PCT_SHORT, VOL_SCALE_MAX, VOL_SCALE_MIN, VOL_SCALE_TARGET)`, `from sizing_engine._models import SizingDecision`.
Also import `from pnl_engine import compute_unrealised_pnl as _pnl_compute` if `compute_unrealised_pnl` in sizing_engine delegates to pnl_engine — check line 493 of sizing_engine.py and replicate exactly.
Target: ~160 LOC.

**sizing_engine/stops.py** — trailing stop logic. Move verbatim from sizing_engine.py:
- `get_trailing_stop` (lines 280–360)
- `check_stop_hit` (lines 361–438)

Imports needed: `import math`, `from system_params import (TRAIL_MULT_LONG, TRAIL_MULT_SHORT, Position)`, `from signal_engine import LONG, SHORT`.
Target: ~175 LOC.

**sizing_engine/pyramid.py** — pyramid + drift logic. Move verbatim from sizing_engine.py:
- `detect_drift` (lines 116–193)
- `check_pyramid` (lines 439–492)

Imports needed: `import math`, `from system_params import (MAX_PYRAMID_LEVEL, Position)`, `from signal_engine import LONG, SHORT, FLAT`, `from sizing_engine._models import PyramidDecision, DriftEvent`.
Target: ~150 LOC.

**sizing_engine/close.py** — position close logic. Move verbatim from sizing_engine.py:
- `_close_position` (lines 766–820)

Imports needed: `import math`, `from system_params import Position`, `from signal_engine import LONG, SHORT, FLAT`, `from sizing_engine._models import ClosedTrade`, `from sizing_engine.sizing import compute_unrealised_pnl`.
Target: ~70 LOC.

**sizing_engine/__init__.py** stub — minimal stub that will be completed in Task 2:
```python
# Populated in Task 2
```
  </action>
  <verify>
    <automated>
      python -c "import ast; ast.parse(open('sizing_engine/_models.py').read()); print('_models.py parses OK')"
      python -c "import ast; ast.parse(open('sizing_engine/sizing.py').read()); print('sizing.py parses OK')"
      python -c "import ast; ast.parse(open('sizing_engine/stops.py').read()); print('stops.py parses OK')"
      python -c "import ast; ast.parse(open('sizing_engine/pyramid.py').read()); print('pyramid.py parses OK')"
      python -c "import ast; ast.parse(open('sizing_engine/close.py').read()); print('close.py parses OK')"
      wc -l sizing_engine/_models.py sizing_engine/sizing.py sizing_engine/stops.py sizing_engine/pyramid.py sizing_engine/close.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `sizing_engine/_models.py` exists and contains all 5 dataclass definitions: `class SizingDecision`, `class PyramidDecision`, `class ClosedTrade`, `class StepResult`, `class DriftEvent`
    - `sizing_engine/sizing.py` exists and contains `def _vol_scale(`, `def calc_position_size(`, `def compute_unrealised_pnl(`
    - `sizing_engine/stops.py` exists and contains `def get_trailing_stop(`, `def check_stop_hit(`
    - `sizing_engine/pyramid.py` exists and contains `def detect_drift(`, `def check_pyramid(`
    - `sizing_engine/close.py` exists and contains `def _close_position(`
    - `python -c "import ast; ast.parse(open('sizing_engine/_models.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('sizing_engine/sizing.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('sizing_engine/stops.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('sizing_engine/pyramid.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('sizing_engine/close.py').read())"` exits 0
    - `wc -l sizing_engine/_models.py` outputs ≤500
    - `wc -l sizing_engine/sizing.py` outputs ≤500
    - `wc -l sizing_engine/stops.py` outputs ≤500
    - `wc -l sizing_engine/pyramid.py` outputs ≤500
    - `wc -l sizing_engine/close.py` outputs ≤500
  </acceptance_criteria>
  <done>Five daughter files exist, parse cleanly, each ≤500 LOC.</done>
</task>

<task type="auto">
  <name>Task 2: Complete __init__.py with step() + re-exports, then delete sizing_engine.py</name>
  <read_first>
    sizing_engine.py (still present — read lines 1–40 for docstring/imports + 545–765 for step() body before deleting)
    sizing_engine/_models.py
    sizing_engine/sizing.py
    sizing_engine/stops.py
    sizing_engine/pyramid.py
    sizing_engine/close.py
  </read_first>
  <files>
    sizing_engine/__init__.py
    sizing_engine.py
  </files>
  <action>
**sizing_engine/__init__.py** — orchestrator + re-exports. Write the full content:

- Module docstring (copy and adapt from sizing_engine.py lines 1–16)
- Imports:
  ```python
  import dataclasses
  import math
  from signal_engine import FLAT, LONG, SHORT  # noqa: F401
  from system_params import (
      ADX_EXIT_GATE,
      MAX_PYRAMID_LEVEL,
      RISK_PCT_LONG,
      RISK_PCT_SHORT,
      TRAIL_MULT_LONG,
      TRAIL_MULT_SHORT,
      VOL_SCALE_MAX,
      VOL_SCALE_MIN,
      VOL_SCALE_TARGET,
      DEFAULT_STRATEGY_SETTINGS,
      Position,
  )
  from sizing_engine._models import (
      SizingDecision,
      PyramidDecision,
      ClosedTrade,
      StepResult,
      DriftEvent,
  )
  from sizing_engine.sizing import (
      _vol_scale,
      calc_position_size,
      compute_unrealised_pnl,
  )
  from sizing_engine.stops import get_trailing_stop, check_stop_hit
  from sizing_engine.pyramid import check_pyramid, detect_drift
  from sizing_engine.close import _close_position
  ```
- `step()` function — move verbatim from sizing_engine.py lines 545–765 (~220 LOC). This is the thin orchestrator that chains exit-then-entry logic.
- `__all__` to declare public surface:
  ```python
  __all__ = [
      'step',
      'calc_position_size',
      'get_trailing_stop',
      'check_stop_hit',
      'check_pyramid',
      'detect_drift',
      'compute_unrealised_pnl',
      'ClosedTrade',
      'SizingDecision',
      'PyramidDecision',
      'StepResult',
      'DriftEvent',
  ]
  ```

Target: ~260 LOC.

**Delete sizing_engine.py** — only after __init__.py is fully written and parses clean:
```bash
rm sizing_engine.py
```
  </action>
  <verify>
    <automated>
      python -c "import ast; ast.parse(open('sizing_engine/__init__.py').read()); print('__init__.py parses OK')"
      python -c "from sizing_engine import step, calc_position_size, get_trailing_stop, check_stop_hit, check_pyramid, detect_drift, compute_unrealised_pnl, ClosedTrade, SizingDecision, PyramidDecision, StepResult, DriftEvent; print('all public imports OK')"
      test ! -f sizing_engine.py && echo "sizing_engine.py deleted OK"
      wc -l sizing_engine/__init__.py
      pytest -x --tb=short tests/test_sizing_engine.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `sizing_engine/__init__.py` exists and contains `def step(`
    - `grep "from sizing_engine._models import" sizing_engine/__init__.py` finds at least 1 match
    - `grep "from sizing_engine.sizing import" sizing_engine/__init__.py` finds at least 1 match
    - `grep "from sizing_engine.stops import" sizing_engine/__init__.py` finds at least 1 match
    - `grep "from sizing_engine.pyramid import" sizing_engine/__init__.py` finds at least 1 match
    - `grep "from sizing_engine.close import" sizing_engine/__init__.py` finds at least 1 match
    - `python -c "from sizing_engine import step, calc_position_size, get_trailing_stop, check_stop_hit, check_pyramid, detect_drift, compute_unrealised_pnl, ClosedTrade"` exits 0
    - `wc -l sizing_engine/__init__.py` outputs ≤500
    - `test ! -f sizing_engine.py` exits 0 (old flat file deleted)
    - `pytest -x --tb=short tests/test_sizing_engine.py` exits 0
  </acceptance_criteria>
  <done>__init__.py complete with step() and re-exports; sizing_engine.py deleted; sizing_engine test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → sizing_engine package | callers use package-level names; internal submodule paths not exposed |
| sizing_engine (pure math) → no I/O | hex boundary preserved — no datetime, os, state_manager imports allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | Tampering | hex boundary breakage | mitigate | All daughter files must import only dataclasses, math, signal_engine constants, system_params constants — no I/O modules; test_forbidden_imports_absent AST gate enforces this |
| T-31-02-02 | Repudiation | ClosedTrade import path change | mitigate | Re-exported from __init__.py; acceptance_criteria verifies `from sizing_engine import ClosedTrade` resolves |
</threat_model>

<verification>
Run after both tasks:

```bash
cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals
pytest -x --tb=short tests/test_sizing_engine.py
grep -rn "import sizing_engine\|from sizing_engine import" daily_run.py daily_run_helpers.py main.py backtest/simulator.py web/routes/trades/__init__.py web/routes/dashboard/_renderers.py
wc -l sizing_engine/__init__.py sizing_engine/_models.py sizing_engine/sizing.py sizing_engine/stops.py sizing_engine/pyramid.py sizing_engine/close.py
```

All lines ≤500. All imports resolve. Test suite green.
</verification>

<success_criteria>
- `sizing_engine/` package exists with 6 files, all ≤500 LOC
- `sizing_engine.py` is deleted
- All existing import forms (`import sizing_engine`, `from sizing_engine import X`) resolve unchanged
- `from sizing_engine import ClosedTrade` resolves (daily_run_helpers.py caller)
- Hex boundary intact: no `datetime`, `os`, `state_manager`, `notifier` imports in any sizing_engine daughter file
- `pytest -x --tb=short tests/test_sizing_engine.py` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-core-module-split/31-02-SUMMARY.md`
</output>
