---
phase: 33-schema-migration-v11-v12-admin-namespace-backup-gitignore
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - .gitignore
  - tests/test_gitignore_gate.py
autonomous: true
requirements:
  - TENANT-04
must_haves:
  truths:
    - "state/users/ is in .gitignore"
    - "state/*.v11-backup-* is in .gitignore"
    - "git ls-files -- state/users/ returns empty output"
    - "test_state_users_not_tracked passes in pytest"
    - "test_state_users_gitignored passes in pytest (git check-ignore confirms pattern active)"
  artifacts:
    - path: ".gitignore"
      provides: "state/users/ gitignore entry"
      contains: "state/users/"
    - path: "tests/test_gitignore_gate.py"
      provides: "pytest-based CI gate for git ls-files and git check-ignore"
      exports: ["test_state_users_not_tracked", "test_backup_files_not_tracked", "test_state_users_gitignored"]
  key_links:
    - from: "tests/test_gitignore_gate.py"
      to: "subprocess git ls-files"
      via: "subprocess.run(['git', 'ls-files', '--', 'state/users/'])"
      pattern: "git ls-files"
    - from: "tests/test_gitignore_gate.py"
      to: "subprocess git check-ignore"
      via: "subprocess.run(['git', 'check-ignore', '-q', 'state/users/test_sentinel.json'])"
      pattern: "git check-ignore"
---

<objective>
Add state/users/ and backup-file patterns to .gitignore, and write a pytest
module that asserts git ls-files returns zero tracked paths under state/users/
and that git check-ignore confirms the pattern is active.

Purpose: Ensures per-user state files can never enter git history by accident; the tests double as CI gates that any pytest run catches before push.
Output: .gitignore updated + tests/test_gitignore_gate.py committed and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/RESEARCH.md

Key facts:
- .gitignore currently lacks state/users/ entry (confirmed in RESEARCH.md).
- state/ directory does not yet have a users/ subdirectory on disk (Phase 33 is forward-looking).
- The gitignore pattern is valid for non-existent paths — git evaluates patterns against hypothetical tracked files.
- The CI gate is pytest-based (simpler than a separate GHA workflow since no test CI exists yet; any future CI that runs pytest automatically picks this up).
- subprocess.run(['git', 'ls-files', '--', 'state/users/'], ...) — the '--' separator separates options from pathspecs, preventing pathspec injection.
- 2-space indent in Python; test file follows existing test conventions.

<interfaces>
From .gitignore (current — must be read before editing):
  Has existing entries for state.json, .env, __pycache__, etc.
  state.json is force-added by daily workflow via 'git add -f' — that does NOT override per-directory gitignore.
  state/users/*.json would also be force-addable IF tracked. The gitignore entry removes the path from tracking entirely, but a deliberate 'git add -f state/users/x.json' would still stage it.
  The test gate is the hard enforcement layer.

Subprocess pattern for ls-files:
  result = subprocess.run(
    ['git', 'ls-files', '--', 'state/users/'],
    capture_output=True, text=True, cwd=Path(__file__).parent.parent,
  )
  assert result.stdout.strip() == ''

Subprocess pattern for check-ignore (W6):
  Uses tmp_path (pytest fixture) or creates a sentinel file at repo root temporarily.
  Sentinel path: _REPO_ROOT / 'state' / 'users' / 'test_sentinel.json'
  Creates the sentinel, runs git check-ignore -q, asserts exit code 0, removes sentinel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update .gitignore and write test_gitignore_gate.py</name>
  <files>.gitignore, tests/test_gitignore_gate.py</files>
  <behavior>
    .gitignore additions (append a new comment block at the end):
      # Phase 33 TENANT-04: per-user state files — never committed; off-droplet backup only
      state/users/
      state/users/*.json
      state/users/*.lock
      state/*.v11-backup-*

    tests/test_gitignore_gate.py content:
    - Module docstring: explains this is a CI gate that fails if any per-user state path enters git tracking.
    - test_state_users_not_tracked(): subprocess git ls-files -- state/users/ returns empty stdout. cwd set to repo root (Path(__file__).parent.parent).
    - test_backup_files_not_tracked(): subprocess git ls-files -- 'state/*.v11-backup-*' returns empty stdout.
    - test_state_users_gitignored() (W6 — belt-and-suspenders check):
        1. Creates state/users/ directory if it does not exist (os.makedirs(..., exist_ok=True)).
        2. Creates a sentinel file at _REPO_ROOT / 'state' / 'users' / 'test_sentinel.json' with content '{}'.
        3. Runs subprocess.run(['git', 'check-ignore', '-q', 'state/users/test_sentinel.json'], cwd=_REPO_ROOT, capture_output=True).
        4. Asserts result.returncode == 0 with message "state/users/test_sentinel.json is NOT gitignored — check .gitignore".
        5. Cleans up: removes the sentinel file (os.remove); removes state/users/ only if it was created empty by this test (use try/except to handle pre-existing directory).
    - All three functions are plain def (not async), no fixtures needed.
    - Both test_state_users_not_tracked and test_backup_files_not_tracked must pass immediately after .gitignore is updated (no state/users files are currently tracked).
  </behavior>
  <action>
    Read .gitignore to find the current end of file and existing patterns.

    Append to .gitignore the block shown in the behavior section. Do NOT insert in the middle — append at the end to avoid disrupting existing pattern ordering.

    Create tests/test_gitignore_gate.py with:
    - imports: subprocess, os, Path from pathlib
    - _REPO_ROOT = Path(__file__).parent.parent  (resolves to trading-signals/)
    - test_state_users_not_tracked(): runs ['git', 'ls-files', '--', 'state/users/'] with cwd=_REPO_ROOT, capture_output=True, text=True; asserts result.stdout.strip() == '' with message f'state/users/ files are tracked by git: {result.stdout.strip()}'.
    - test_backup_files_not_tracked(): same pattern for glob 'state/*.v11-backup-*'.
    - test_state_users_gitignored(): see behavior block. Uses os.makedirs and a temporary sentinel file; cleans up after assertion. This is the git check-ignore verification — exit code 0 means the file IS gitignored.

    Verify .gitignore works: create a temp test file, check git status, remove it.
    Run all three tests: .venv/bin/pytest tests/test_gitignore_gate.py -x --tb=short
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_gitignore_gate.py -x --tb=short && git ls-files -- state/users/ | wc -l | grep -q '^0$' && echo "gitignore OK"</automated>
  </verify>
  <done>tests/test_gitignore_gate.py exists; all three tests pass (test_state_users_not_tracked, test_backup_files_not_tracked, test_state_users_gitignored); .gitignore contains state/users/ and state/*.v11-backup-* entries; git ls-files -- state/users/ returns zero rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index → tracked files | Preventing per-user state from entering git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Information Disclosure | state/users/ in git | mitigate | gitignore entry + pytest gate (ls-files + check-ignore); backup-file pattern also gitignored |
| T-33-08 | Tampering | force-add bypass | accept | 'git add -f' can override gitignore; the pytest gate catches this AFTER a mistaken force-add since it runs git ls-files on the actual index |
</threat_model>

<verification>
.venv/bin/pytest tests/test_gitignore_gate.py -v --tb=short
git ls-files -- state/users/
git check-ignore -v state/users/
</verification>

<success_criteria>
- .gitignore has entries for state/users/, state/users/*.json, state/users/*.lock, state/*.v11-backup-*
- git ls-files -- state/users/ returns empty
- test_state_users_not_tracked passes (git ls-files belt)
- test_backup_files_not_tracked passes
- test_state_users_gitignored passes (git check-ignore -q exit code 0 suspenders)
</success_criteria>

<output>
After completion, create `.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/33-03-SUMMARY.md`
</output>
