---
phase: 33-schema-migration-v11-v12-admin-namespace-backup-gitignore
plan: 02
type: tdd
wave: 2
depends_on:
  - 33-01
files_modified:
  - tests/test_state_migration_v12.py
  - tests/fixtures/state_v11_empty.json
  - tests/fixtures/state_v11_max_trade_log.json
  - tests/fixtures/state_v11_mid_pyramid.json
  - tests/fixtures/state_v11_mid_alert_approaching.json
  - tests/fixtures/state_v11_naive_datetime.json
autonomous: true
requirements:
  - TENANT-01
must_haves:
  truths:
    - "5 v11 fixture files exist in tests/fixtures/"
    - "Round-trip is forward-only lossless: every v11 field that maps to v12 is present with identical value at the correct v12 path in migrated output"
    - "Pydantic StateV12.model_validate passes on each migrated fixture"
    - "Contiguity assert covers chain 1..12"
    - "Auto-backup test: backup file created when state.json has schema_version=11"
  artifacts:
    - path: "tests/test_state_migration_v12.py"
      provides: "Round-trip, Pydantic validation, contiguity, backup tests"
      exports: ["TestV12RoundTrip", "TestStateV12Schema", "TestV12AutoBackup", "TestV12Contiguity"]
    - path: "tests/fixtures/state_v11_empty.json"
      provides: "Empty v11 fixture"
      contains: '"schema_version": 11'
    - path: "tests/fixtures/state_v11_max_trade_log.json"
      provides: "v11 fixture with populated trade_log"
      contains: '"trade_log"'
    - path: "tests/fixtures/state_v11_mid_pyramid.json"
      provides: "v11 fixture with open SPI200 position"
      contains: '"SPI200"'
    - path: "tests/fixtures/state_v11_mid_alert_approaching.json"
      provides: "v11 fixture with paper trade last_alert_state APPROACHING"
      contains: '"APPROACHING"'
    - path: "tests/fixtures/state_v11_naive_datetime.json"
      provides: "v11 fixture with naive ISO timestamp in equity_history"
      contains: '"2026-04-30T08:00:00"'
  key_links:
    - from: "tests/test_state_migration_v12.py::TestV12RoundTrip"
      to: "state_manager._migrate_v11_to_v12"
      via: "direct import and dict comparison"
      pattern: "_migrate_v11_to_v12"
    - from: "tests/test_state_migration_v12.py::TestStateV12Schema"
      to: "state_manager.validation.StateV12"
      via: "StateV12.model_validate"
      pattern: "StateV12\\.model_validate"
---

<objective>
Write 5 v11 fixture files and a dedicated test module that verifies round-trip
losslessness, Pydantic shape validation, contiguity, and auto-backup behavior.

Purpose: Regression armor — any future change to migrations.py that breaks v11→v12 is caught immediately.
Output: 5 fixture files + tests/test_state_migration_v12.py (all tests green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/RESEARCH.md
@.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/33-01-SUMMARY.md

Key facts:
- Existing fixtures: tests/fixtures/state_v2_no_manual_stop.json, state_v6_with_paper_trades.json, state_v7_with_alerts.json
  These walk through the FULL chain including v12 — they must NOT regress (existing test_state_manager.py covers them).
- "Lossless round-trip" means forward-only: for each v11 fixture, after _migrate_v11_to_v12, every field value from the v11 source appears at the correct path in the v12 output. No inverse migration function exists or is needed. The assertion is: v11_field_value == v12_users[_ADMIN_UID][field]. There is no reverse(v12) step.
- Naive-datetime fixture: equity_history entry with date "2026-04-30T08:00:00" (no +HH:MM). _coerce_legacy_naive_iso should emit DeprecationWarning but not fail.
- 2-space indent throughout; no ruff format.
- _ADMIN_UID = 'u_admin_marc' — import from state_manager.migrations or state_manager directly.

<interfaces>
From state_manager (post-Plan 33-01):
  from state_manager import _migrate_v11_to_v12, load_state, save_state, reset_state
  from state_manager.validation import StateV12, _REQUIRED_STATE_KEYS
  from state_manager.migrations import MIGRATIONS, _assert_migration_chain_contiguous, _ADMIN_UID
  from system_params import STATE_SCHEMA_VERSION, INITIAL_ACCOUNT, _DEFAULT_SPI_LABEL, _DEFAULT_AUDUSD_LABEL

Fixture shape (v11):
  Required top-level keys: schema_version (11), account, initial_account, last_run,
    positions, signals, trade_log, equity_history, warnings, contracts, markets,
    strategy_settings [+ paper_trades optional]
  All values must be JSON-serialisable (no Decimal objects in fixtures).
</interfaces>
</context>

<feature>
  <name>v11→v12 round-trip + validation test suite</name>
  <files>tests/test_state_migration_v12.py, tests/fixtures/state_v11_*.json</files>
  <behavior>
    TestV12RoundTrip (5 parametrized cases):
    - empty: account=10000.0, trade_log=[], equity_history=[], paper_trades=[], positions={'SPI200':null,'AUDUSD':null}
    - max_trade_log: trade_log has 3 closed trade dicts (all required fields per _REQUIRED_TRADE_FIELDS)
    - mid_pyramid: SPI200 position is an open Position dict (entry_price, n_contracts, direction, stop_price, strategy_version)
    - mid_alert_approaching: paper_trades contains one entry with last_alert_state='APPROACHING'
    - naive_datetime: equity_history has one entry with date='2026-04-30T08:00:00' (naive ISO)

    For each fixture: call _migrate_v11_to_v12(fixture_dict); assert (forward-only — no reverse step):
    1. result['schema_version'] == 11 (migrator does NOT bump schema_version — _migrate orchestrator does)
    2. result['users'][_ADMIN_UID]['account'] == fixture['account']
    3. result['users'][_ADMIN_UID]['trade_log'] == fixture['trade_log']
    4. result['users'][_ADMIN_UID]['paper_trades'] == fixture.get('paper_trades', [])
    5. result['users'][_ADMIN_UID]['equity_history'] == fixture['equity_history']
    6. result['users'][_ADMIN_UID]['contracts'] == fixture['contracts']
    7. result['users'][_ADMIN_UID]['positions'] == fixture['positions']
    8. result['admin_user_id'] == 'u_admin_marc'
    9. 'account' NOT in result (top-level)
    10. 'trade_log' NOT in result (top-level)

    TestStateV12Schema:
    - For each fixture, run full migration chain via _migrate(dict(fixture)), then StateV12.model_validate(migrated) — must not raise.

    TestV12AutoBackup:
    - Write a v11-shaped JSON to tmp_path; call load_state(tmp_path); assert at least one file matching f'{tmp_path.name}.v11-backup-*' exists in tmp_path.parent.
    - Write a v12-shaped JSON to tmp_path; call load_state(tmp_path); assert NO new backup file created (backup only fires for pre-v12 state).

    TestV12Contiguity:
    - _assert_migration_chain_contiguous() (from state_manager.migrations) does not raise.
    - STATE_SCHEMA_VERSION == 12.
    - MIGRATIONS has keys 1..12 with no gaps.
  </behavior>
  <implementation>
    RED-GREEN-REFACTOR:
    1. RED: Write tests/test_state_migration_v12.py with all four test classes. Run pytest — TestV12RoundTrip and TestStateV12Schema will fail (fixture files not yet created); TestV12Contiguity and TestV12AutoBackup may pass if Plan 33-01 is already complete.
    2. Create the 5 fixture JSON files in tests/fixtures/.
    3. GREEN: Re-run pytest — all tests must pass.
    4. REFACTOR: Collapse any duplicate assert patterns into a helper function if >3 lines repeated.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture→migrator | Fixture dicts are test-internal; no trust boundary concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Tampering | fixture files | accept | Test fixtures are committed; no runtime security surface |
| T-33-06 | Information Disclosure | test output on CI | accept | Test fixtures use dummy values (no real PII); 10000.0 is the default INITIAL_ACCOUNT |
</threat_model>

<verification>
.venv/bin/pytest tests/test_state_migration_v12.py -x --tb=short
.venv/bin/pytest tests/test_state_manager.py tests/test_migration_contiguity.py -x --tb=short
</verification>

<success_criteria>
- 5 fixture files exist in tests/fixtures/
- All TestV12RoundTrip, TestStateV12Schema, TestV12AutoBackup, TestV12Contiguity tests pass
- Round-trip is forward-only lossless: every v11 field that maps to v12 is present with identical value in the migrated output; the v12 result passes Pydantic StateV12 validation (no reverse migration function used or expected)
- Full test suite (123 existing + new) still green: .venv/bin/pytest -x --tb=short
</success_criteria>

<output>
After completion, create `.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/33-02-SUMMARY.md`
</output>
