---
phase: 33-schema-migration-v11-v12-admin-namespace-backup-gitignore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - system_params.py
  - state_manager/migrations.py
  - state_manager/validation.py
  - state_manager/__init__.py
autonomous: true
requirements:
  - TENANT-01
must_haves:
  truths:
    - "_migrate_v11_to_v12 moves 7 per-user keys into state['users']['u_admin_marc']"
    - "Shared keys (signals, markets, strategy_settings, warnings, last_run) remain top-level"
    - "STATE_SCHEMA_VERSION is 12 in system_params.py"
    - "MIGRATIONS dict has key 12 mapping to _migrate_v11_to_v12"
    - "_REQUIRED_STATE_KEYS in validation.py reflects v12 top-level shape"
    - "reset_state() emits a v12-shaped dict with 'users' and 'admin_user_id'"
    - "auto-backup shutil.copy2 fires in load_state() when old schema_version < 12"
    - "StateV12 Pydantic model validates the output of _migrate_v11_to_v12"
    - "_assert_migration_chain_contiguous passes for the new chain 1..12"
    - "_coerce_legacy_naive_iso emits DeprecationWarning when equity_history contains naive-datetime strings in ANY user's equity_history"
    - "load_state() on a freshly-migrated v12 state does not raise KeyError"
  artifacts:
    - path: "system_params.py"
      provides: "STATE_SCHEMA_VERSION = 12"
      contains: "STATE_SCHEMA_VERSION: int = 12"
    - path: "state_manager/migrations.py"
      provides: "_migrate_v11_to_v12 registered at key 12"
      exports: ["_migrate_v11_to_v12", "MIGRATIONS", "_ADMIN_UID"]
    - path: "state_manager/validation.py"
      provides: "Updated _REQUIRED_STATE_KEYS and StateV12 Pydantic model"
      contains: "StateV12"
    - path: "state_manager/__init__.py"
      provides: "backup call, Pydantic validate call, re-export of _migrate_v11_to_v12 and _ADMIN_UID"
  key_links:
    - from: "state_manager/__init__.py::load_state"
      to: "shutil.copy2"
      via: "schema_version < 12 branch before _migrate() call"
      pattern: "shutil\\.copy2"
    - from: "state_manager/__init__.py::load_state"
      to: "StateV12.model_validate"
      via: "after _migrate() call, before _validate_loaded_state"
      pattern: "StateV12\\.model_validate"
    - from: "state_manager/validation.py"
      to: "_REQUIRED_STATE_KEYS"
      via: "frozenset containing 'users' and 'admin_user_id', NOT 'account' etc."
      pattern: "'users'"
    - from: "state_manager/__init__.py"
      to: "_ADMIN_UID"
      via: "imported from state_manager.migrations; used in reset_state() and load_state() backup path — never inline string"
      pattern: "_ADMIN_UID"
    - from: "state_manager/__init__.py::load_state"
      to: "_resolved_contracts"
      via: "reads contracts from state['users'][_ADMIN_UID]['contracts'] post-v12"
      pattern: "_resolved_contracts"
---

<objective>
Implement the v11→v12 schema migration: the migrator function, version bump,
Pydantic shape-validation, updated required-keys set, updated reset_state(),
and the auto-backup call in load_state().

Purpose: Establishes the v12 state shape that every subsequent phase depends on.
Output: All migration logic and safeguards committed; existing 123 tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/RESEARCH.md

Key facts already verified:
- system_params.py line 281: STATE_SCHEMA_VERSION = 11
- migrations.py MIGRATIONS dict ends at key 11: _migrate_v10_to_v11
- validation.py _REQUIRED_STATE_KEYS contains 'account', 'initial_account', 'contracts',
  'positions', 'trade_log', 'equity_history' at top level (all must be removed in v12)
- validation.py line 234 (EOL) — file has room for a ~40-line Pydantic model addition
- __init__.py imports shutil not yet present; load_state() happy path at lines 269-289
- __init__.py re-exports list at lines 81-96 (must add _migrate_v11_to_v12 and _ADMIN_UID)
- Pydantic 2.13.3 installed (from requirements.txt / .venv confirmed in RESEARCH.md)
- ADMIN_UID = 'u_admin_marc' — locked; do not change
- 2-space indent throughout; do NOT run ruff format

<interfaces>
From state_manager/migrations.py:
  _ADMIN_UID constant: 'u_admin_marc'  (new — define at top of migrators section)
  Existing pattern for all migrators:
    def _migrate_vN_to_vM(s: dict) -> dict:
      # pure dict→dict, no I/O, no logging, no append_warning
      out = dict(s)
      ...
      return out
  MIGRATIONS keys 1..11; key 12 must be added.

From state_manager/validation.py:
  _REQUIRED_STATE_KEYS = frozenset({...})  (must be updated)
  Pydantic BaseModel not yet imported here; add: from pydantic import BaseModel

From state_manager/__init__.py load_state() happy path (lines 269-289):
  state = _migrate(state)
  if 'markets' not in state or ...: state = _migrate_v7_to_v8(state)
  _validate_loaded_state(state)
  state = _coerce_legacy_naive_iso(state)
  ... _resolved_contracts ...
  Backup call must come BEFORE _migrate(state), guarded: if state.get('schema_version', 0) < 12.

From reset_state() (lines 162-209):
  Returns flat v11-shaped dict — must be updated to v12 shape after this plan.
  ia_float helper already handles Decimal quantize.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump STATE_SCHEMA_VERSION and write _migrate_v11_to_v12</name>
  <files>system_params.py, state_manager/migrations.py</files>
  <behavior>
    - system_params.py: STATE_SCHEMA_VERSION changes from 11 to 12; comment updated to describe v12.
    - migrations.py: module-level constant _ADMIN_UID = 'u_admin_marc' added near the top of the migrators section.
    - _migrate_v11_to_v12(s: dict) -> dict: pure dict→dict, no I/O, no log lines (D-15 silent).
      Moves 'account', 'initial_account', 'contracts', 'positions', 'trade_log', 'equity_history', 'paper_trades' via out.pop(key, default) into a user_bucket dict.
      Adds 'ui_prefs': {'tour_completed': True} to user_bucket (existing user, no tour needed).
      Sets out['users'] = {_ADMIN_UID: user_bucket}.
      Sets out['admin_user_id'] = _ADMIN_UID.
      Idempotency guard: if 'users' in out, returns out unchanged (skips re-bucketing).
      Defaults for each popped key must match system_params constants (INITIAL_ACCOUNT for account/initial_account; _DEFAULT_SPI_LABEL/_DEFAULT_AUDUSD_LABEL for contracts; None-positions dict for positions; [] for trade_log/equity_history/paper_trades).
    - MIGRATIONS dict: key 12 added with value _migrate_v11_to_v12; comment mirrors adjacent entries.
    - _assert_migration_chain_contiguous() fires at module bottom (already does) — no change to that call.
    - Test (write first, must fail before implementation):
      tests/test_state_manager.py: TestMigrateV11ToV12 class with test_basic_move, test_idempotent, test_shared_keys_stay methods.
  </behavior>
  <action>
    Read system_params.py lines 275-295 for the exact STATE_SCHEMA_VERSION comment and surrounding context.
    Read state_manager/migrations.py lines 1-30 for imports and the start of migrators.
    Read state_manager/migrations.py lines 354-420 for MIGRATIONS dict and _assert_migration_chain_contiguous.

    In system_params.py: change STATE_SCHEMA_VERSION from 11 to 12 and update the inline comment to read:
    "v12 buckets per-user state (account, initial_account, contracts, positions, trade_log, equity_history, paper_trades) under state['users']['u_admin_marc']; adds admin_user_id top-level key."

    In state_manager/migrations.py:
    - After the existing logger = logging.getLogger(__name__) line, add the constant:
      _ADMIN_UID: str = 'u_admin_marc'  # Phase 33 TENANT-01: locked uid for admin (Phase 34 makes dynamic)
    - Write _migrate_v11_to_v12 immediately before the MIGRATIONS dict header comment.
      Function signature: def _migrate_v11_to_v12(s: dict) -> dict:
      Docstring: '''v12: bucket per-user state under state["users"]["u_admin_marc"].\n\n    Moves: account, initial_account, contracts, positions, trade_log,\n    equity_history, paper_trades into state["users"][_ADMIN_UID].\n    Adds: admin_user_id = _ADMIN_UID, users{} top-level.\n    Leaves shared: signals, markets, strategy_settings, warnings,\n    last_run, schema_version.\n    Idempotent: if "users" already present, skips re-bucketing.\n    D-15 silent migration: no append_warning, no log line.\n    '''
      Implementation: see behavior block. For 'contracts' default use {'SPI200': _DEFAULT_SPI_LABEL, 'AUDUSD': _DEFAULT_AUDUSD_LABEL}. For 'positions' default use {'SPI200': None, 'AUDUSD': None}.
    - Add key 12 to MIGRATIONS dict: 12: _migrate_v11_to_v12,  # Phase 33 TENANT-01: per-user namespace
    - Add _ADMIN_UID to the module's public exports so it can be imported by __init__.py.

    Write tests (RED first — they must fail before implementation is added):
    In tests/test_state_manager.py, add class TestMigrateV11ToV12 with:
    - test_basic_move: build minimal v11 dict with all 7 per-user keys populated, call _migrate_v11_to_v12, assert each moved key is inside state['users']['u_admin_marc'] and NOT at top-level.
    - test_idempotent: call _migrate_v11_to_v12 twice; result must be identical both times.
    - test_shared_keys_stay: assert 'signals', 'markets', 'strategy_settings', 'warnings', 'last_run' remain at top-level.
    - test_admin_user_id_added: assert state['admin_user_id'] == 'u_admin_marc'.
    - test_ui_prefs_initialized: assert state['users']['u_admin_marc']['ui_prefs'] == {'tour_completed': True}.
    Run tests BEFORE writing the migrator to confirm RED, then implement, then confirm GREEN.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_state_manager.py::TestMigrateV11ToV12 -x --tb=short</automated>
  </verify>
  <done>All 5 TestMigrateV11ToV12 tests pass; MIGRATIONS[12] exists; STATE_SCHEMA_VERSION == 12; _assert_migration_chain_contiguous() does not raise on import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update _REQUIRED_STATE_KEYS, add StateV12 Pydantic model, fix _coerce_legacy_naive_iso scan path</name>
  <files>state_manager/validation.py</files>
  <behavior>
    - validation.py _REQUIRED_STATE_KEYS frozenset:
      REMOVE: 'account', 'initial_account', 'contracts', 'positions', 'trade_log', 'equity_history'
      NOTE: 'paper_trades' was NOT in _REQUIRED_STATE_KEYS (it was top-level but not required) — no change needed for it.
      ADD: 'admin_user_id', 'users'
      Final v12 set: {'schema_version', 'last_run', 'signals', 'markets', 'strategy_settings', 'warnings', 'admin_user_id', 'users'}
    - validation.py: add "from pydantic import BaseModel, ConfigDict" import (after existing imports).
    - validation.py: add StateV12(BaseModel) class after _validate_loaded_state, before _read_signal_strategy_version.
      Fields: schema_version: int, admin_user_id: str, last_run: str | None, signals: dict, markets: dict, strategy_settings: dict, warnings: list, users: dict
      model_config: model_config = ConfigDict(extra='allow')  (allow extra keys for forward compat)
    - validation.py _coerce_legacy_naive_iso scan path fix (B1):
      After v12, equity_history lives under state['users'][uid]['equity_history'] — NOT at state['equity_history'] top-level.
      Update _coerce_legacy_naive_iso to iterate state.get('users', {}).values() and coerce equity_history entries in EACH user bucket.
      The old top-level scan (state['equity_history']) must be replaced — not supplemented — with the per-user scan.
      Comment: "# v12: equity_history lives under state['users'][uid]['equity_history']"
    - Test behaviors:
      test_required_keys_v12: _validate_loaded_state raises ValueError for missing 'users'; does NOT raise for missing 'account' (moved to per-user namespace).
      test_coerce_legacy_naive_iso_per_user: build a v12-shaped state dict where state['users']['u_admin_marc']['equity_history'] contains one entry with a naive datetime string ('2026-04-30T08:00:00'); call _coerce_legacy_naive_iso; assert DeprecationWarning is emitted.
  </behavior>
  <action>
    Read state_manager/validation.py fully.

    In validation.py:
    - Add "from pydantic import BaseModel, ConfigDict" after "from typing import Any".
    - Update _REQUIRED_STATE_KEYS to the v12 frozenset listed in behavior block. Update the comment to reference "D-18 (Phase 33 TENANT-01: v12 shape — per-user keys moved under users{})".
    - Add StateV12 model class after _validate_loaded_state body. Keep it under 50 lines. All fields typed as above; add model_config = ConfigDict(extra='allow').
    - Fix _coerce_legacy_naive_iso scan path (B1):
      Locate the function body. Replace the loop that scans state['equity_history'] (or state.get('equity_history', [])) with:
        for _user_data in state.get('users', {}).values():
          for entry in _user_data.get('equity_history', []):
            if isinstance(entry.get('date', ''), str) and '+' not in entry['date'] and 'Z' not in entry['date']:
              warnings.warn(...)  # existing DeprecationWarning logic
      Ensure the DeprecationWarning is emitted once per naive entry found in ANY user's equity_history.

    Write test methods in tests/test_state_manager.py (new class TestV12ValidationBehavior).
    Write tests RED-first, then implement, then confirm GREEN.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_state_manager.py::TestV12ValidationBehavior -x --tb=short</automated>
  </verify>
  <done>_REQUIRED_STATE_KEYS contains 'users' and 'admin_user_id'; StateV12 importable from state_manager.validation; _coerce_legacy_naive_iso emits DeprecationWarning when equity_history contains naive-datetime strings in ANY user's equity_history; test_required_keys_v12 and test_coerce_legacy_naive_iso_per_user pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update __init__.py — shutil import, _ADMIN_UID re-export, load_state backup + validate + _resolved_contracts fix, reset_state v12 body</name>
  <files>state_manager/__init__.py</files>
  <behavior>
    - __init__.py: add "import shutil" to imports (alphabetical order among stdlib imports).
    - __init__.py: add _ADMIN_UID to the migrations re-export block (lines 81-96). _ADMIN_UID MUST be imported from state_manager.migrations and re-exported — never define inline as a string literal in __init__.py. This is the single source of truth (B4).
    - __init__.py: add _migrate_v11_to_v12 to the migrations re-export block (after _migrate_v10_to_v11).
    - __init__.py: add StateV12 to the validation re-export block (import from state_manager.validation).
    - __init__.py reset_state() body (W5 — now in this task):
      Update to emit v12 shape: use _ADMIN_UID (the imported constant, NOT the string 'u_admin_marc') when constructing the 'users' dict.
      Add 'admin_user_id': _ADMIN_UID (use the constant).
      Add 'users': {_ADMIN_UID: {'account': ia_float, 'initial_account': ia_float, 'contracts': {...}, 'positions': {...}, 'trade_log': [], 'equity_history': [], 'paper_trades': [], 'ui_prefs': {'tour_completed': False}}}
      NOTE: tour_completed=False for fresh reset (new user, tour not done); tour_completed=True only in the migrator (existing admin).
      Remove 'account', 'initial_account', 'contracts', 'positions', 'trade_log', 'equity_history' from the top-level dict in reset_state().
    - __init__.py load_state() happy path:
      Add backup call BEFORE state = _migrate(state), guarded on schema_version < 12.
        if now is None: now = datetime.now(UTC)
        _old_version = state.get('schema_version', 0)
        if _old_version < 12:
          _backup_ts = now.isoformat().replace(':', '-')
          _backup_path = path.parent / f'{path.name}.v11-backup-{_backup_ts}'
          if not _backup_path.exists(): shutil.copy2(path, _backup_path)
      After _migrate and the v7_to_v8 guard, BEFORE _validate_loaded_state(state), insert:
        StateV12.model_validate(state)
      Fix _resolved_contracts materialisation (B2): the block around lines 282-292 reads
        state['_resolved_contracts'] = {k: v for k, v in state['contracts'].items() ...}
        After v12, 'contracts' lives at state['users'][_ADMIN_UID]['contracts']. Update to:
        state['_resolved_contracts'] = {
          k: v for k, v in state['users'][_ADMIN_UID]['contracts'].items() ...
        }
        Use _ADMIN_UID (the constant) — not the inline string.
    - Test behaviors:
      test_reset_state_v12_shape: reset_state() output has 'users', 'admin_user_id'; does NOT have 'account' at top level; _ADMIN_UID used (not inline string).
      test_load_state_writes_backup: write a v11-shaped state.json to tmp_path; call load_state(tmp_path); assert a backup file matching '*.v11-backup-*' exists in tmp_path.parent.
      test_load_state_pydantic_validates: after migration, load_state on a valid v11 file succeeds without raising.
      test_load_state_resolved_contracts_no_keyerror: load_state() on a freshly-migrated v12 state does not raise KeyError (B2 acceptance criterion).
  </behavior>
  <action>
    Read __init__.py lines 39-68 for imports block (need to add shutil).
    Read __init__.py lines 81-96 for re-exports block.
    Read __init__.py lines 162-295 for reset_state and load_state bodies.
    Identify the exact line range of the _resolved_contracts materialisation block.

    In __init__.py:
    - Add "import shutil" to imports (alphabetical order among stdlib imports).
    - Add _ADMIN_UID to the migrations re-export block — import it from state_manager.migrations.
      The constant (NOT an inline string 'u_admin_marc') MUST be used in both reset_state() and the backup path in load_state() and the _resolved_contracts block.
    - Add _migrate_v11_to_v12 to the migrations re-export block (after _migrate_v10_to_v11 line).
    - Add StateV12 to the validation re-export block.
    - Update reset_state() body as described in behavior block using _ADMIN_UID constant.
    - Update load_state() happy path: insert backup block before _migrate(state); insert StateV12.model_validate(state) after _migrate and v7_to_v8 guard, before _validate_loaded_state(state).
    - Fix _resolved_contracts materialisation block: change state['contracts'] source to state['users'][_ADMIN_UID]['contracts'].

    Write test methods in tests/test_state_manager.py (new class TestV12InitBehavior or append to TestV12CoreBehavior).
    Write tests RED-first, then implement, then confirm GREEN.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_state_manager.py -x --tb=short && .venv/bin/pytest tests/test_migration_contiguity.py -x --tb=short</automated>
  </verify>
  <done>All new tests pass; full suite still passes (no regressions); load_state on a v11 fixture creates a backup file and returns a v12 dict without KeyError; reset_state() returns a v12 dict; StateV12.model_validate(reset_state()) succeeds; _ADMIN_UID constant used everywhere (grep confirms no inline 'u_admin_marc' in __init__.py outside imports).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk→load_state | state.json bytes are untrusted; parse errors already caught |
| migration input | Old state dict arrives from json.loads — arbitrary structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | _migrate_v11_to_v12 | mitigate | Idempotency guard prevents double-migration overwriting users{} if called twice (e.g., partial write + reload) |
| T-33-02 | Information Disclosure | backup file | mitigate | Backup written to same directory as state.json (same permissions); gitignored by Plan 33-03 |
| T-33-03 | Denial of Service | shutil.copy2 failure | accept | OS-level disk-full will also block the subsequent save; no worse than baseline |
| T-33-04 | Elevation of Privilege | _REQUIRED_STATE_KEYS mismatch | mitigate | Updated frozenset + test_required_keys_v12 catches missing 'users' key at load_state() immediately |
</threat_model>

<verification>
.venv/bin/pytest tests/test_state_manager.py tests/test_migration_contiguity.py -x --tb=short
python -c "from state_manager import reset_state; s = reset_state(); assert s['schema_version'] == 12; assert 'users' in s; assert 'account' not in s; print('reset_state OK')"
python -c "from state_manager.validation import StateV12; from state_manager import reset_state; StateV12.model_validate(reset_state()); print('StateV12 OK')"
</verification>

<success_criteria>
- STATE_SCHEMA_VERSION == 12 in system_params.py
- MIGRATIONS[12] == _migrate_v11_to_v12 in migrations.py
- _REQUIRED_STATE_KEYS contains 'users', 'admin_user_id'; does NOT contain 'account', 'contracts', 'positions', 'trade_log', 'equity_history', 'initial_account'
- reset_state() returns dict with 'users' and 'admin_user_id' at top level; no 'account' at top level
- load_state() on a v11 state.json writes a backup and returns a v12 dict without KeyError
- _coerce_legacy_naive_iso emits DeprecationWarning for naive datetimes in any user's equity_history
- _ADMIN_UID constant (not inline string) used in reset_state(), load_state() backup path, and _resolved_contracts block
- All 123 existing tests + new TestMigrateV11ToV12 + TestV12ValidationBehavior + TestV12InitBehavior tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/33-01-SUMMARY.md`
</output>
