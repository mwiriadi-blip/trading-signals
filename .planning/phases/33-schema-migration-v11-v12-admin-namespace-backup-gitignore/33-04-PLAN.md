---
phase: 33-schema-migration-v11-v12-admin-namespace-backup-gitignore
plan: 04
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - systemd/trading-signals-backup.service
  - systemd/trading-signals-backup.timer
  - scripts/backup_state.sh
  - scripts/check_backup_age.py
  - notifier/dispatch.py
  - notifier/__init__.py
  - tests/test_backup_age.py
  - docs/SETUP-DROPLET.md
autonomous: true
requirements:
  - TENANT-04
must_haves:
  truths:
    - "scripts/backup_state.sh runs rclone copy for state.json to B2"
    - "scripts/check_backup_age.py parses rclone lsl output, alerts if >48h stale"
    - "systemd/trading-signals-backup.service and .timer exist with correct unit config"
    - "notifier exposes send_backup_stale_email(last_backup_iso) -> SendStatus"
    - "tests/test_backup_age.py passes for happy-path and stale-path behaviors"
    - "SETUP-DROPLET.md documents rclone install, B2 config, and systemd enable steps"
  artifacts:
    - path: "scripts/backup_state.sh"
      provides: "rclone copy wrapper for B2 backup"
      contains: "rclone copy"
    - path: "scripts/check_backup_age.py"
      provides: "B2 last-modified check with 48h alert"
      contains: "send_backup_stale_email"
    - path: "systemd/trading-signals-backup.service"
      provides: "systemd service unit for backup"
      contains: "backup_state.sh"
    - path: "systemd/trading-signals-backup.timer"
      provides: "daily systemd timer for backup"
      contains: "OnCalendar=daily"
    - path: "notifier/dispatch.py"
      provides: "send_backup_stale_email function"
      contains: "send_backup_stale_email"
    - path: "tests/test_backup_age.py"
      provides: "unit tests for check_backup_age stale logic"
  key_links:
    - from: "systemd/trading-signals-backup.timer"
      to: "systemd/trading-signals-backup.service"
      via: "Unit= directive"
      pattern: "Unit=trading-signals-backup.service"
    - from: "scripts/check_backup_age.py"
      to: "notifier.send_backup_stale_email"
      via: "import notifier; notifier.send_backup_stale_email(last_backup_iso)"
      pattern: "send_backup_stale_email"

user_setup:
  - service: rclone-b2
    why: "Off-droplet backup requires rclone installed on droplet with B2 credentials"
    env_vars:
      - name: RCLONE_B2_ACCOUNT
        source: "Backblaze B2 → Application Keys → Key ID"
      - name: RCLONE_B2_KEY
        source: "Backblaze B2 → Application Keys → applicationKey"
      - name: B2_BUCKET
        source: "Backblaze B2 → Buckets → bucket name (create if needed)"
    dashboard_config:
      - task: "Create B2 bucket named e.g. 'trading-signals-backup'"
        location: "Backblaze B2 console → Buckets → Create a Bucket (Private)"
      - task: "Create application key with read+write access to that bucket"
        location: "Backblaze B2 console → Application Keys → Add a New Application Key"
      - task: "Install rclone on droplet: curl https://rclone.org/install.sh | sudo bash"
        location: "SSH into droplet as trader user"
      - task: "Configure rclone: rclone config (add B2 remote named 'b2')"
        location: "SSH into droplet; follow rclone config wizard for Backblaze B2"
      - task: "Install and enable systemd units: sudo cp systemd/trading-signals-backup.{service,timer} /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable --now trading-signals-backup.timer"
        location: "SSH into droplet as trader user in /home/trader/trading-signals/"
---

<objective>
Build the off-droplet rclone-to-B2 backup pipeline: shell script, check-age
Python script, two systemd units, a dedicated notifier email function, and
unit tests for the age-check logic.

Purpose: Satisfies TENANT-04 off-droplet backup + 48h stale alert.
Output: All files committed; test_backup_age.py passes; SETUP-DROPLET.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/RESEARCH.md
@.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/33-01-SUMMARY.md

Key facts:
- No rclone or backup script exists anywhere in the codebase (RESEARCH.md verified).
- Existing systemd/trading-signals-web.service shows the template: User=trader, WorkingDirectory, EnvironmentFile, ExecStart, Restart, logging to journal. Match this style.
- notifier/dispatch.py contains send_crash_email, send_stop_alert_email — add send_backup_stale_email following the same never-crash posture (D-13: every send_* catches Exception, logs [Email] prefix, returns SendStatus(ok=False)).
- notifier/__init__.py must re-export send_backup_stale_email (add to dispatch imports block).
- scripts/ directory does not currently exist — create it.
- docs/ directory: check if SETUP-DROPLET.md exists; if not create it.
- B2 bucket name and credentials are operator-supplied secrets — never committed. Use env var substitution in the shell script; document in SETUP-DROPLET.md.
- 2-space indent in Python files.
- check_backup_age.py is an I/O script (lives in scripts/); it calls rclone lsl via subprocess, parses the last-modified timestamp, compares to now - 48h threshold, calls notifier if stale.
- The stale-backup check is invoked by: (a) being called as a script directly ('python scripts/check_backup_age.py'), OR (b) being imported and called from daily_run.py (per RESEARCH.md open question A6: piggyback on daily run is simplest). Plan 33-04 ships the script; wiring into daily_run.py is left for the executor to add as a one-liner at the end of daily_run.py (after existing logic). Executor should add this call: if it determines the import is clean.
- IMPORTANT: do NOT modify daily_run.py in this plan if adding the import would violate the hex boundary. scripts/check_backup_age.py may import notifier (I/O hex peer); daily_run.py already imports notifier. Both are orchestration-layer modules — safe.

<interfaces>
From notifier/dispatch.py (existing send_crash_email pattern to follow):
  def send_crash_email(exc, state_summary, now=None) -> SendStatus:
    '''...never-crash posture: catches Exception, logs [Email], returns SendStatus(ok=False)...'''
    try:
      ...
      r = requests.post(RESEND_URL, json=payload, headers=headers, timeout=HTTP_TIMEOUT_S)
      return SendStatus(ok=r.status_code == 200, http_status=r.status_code)
    except Exception as e:
      logger.warning('[Email] send_crash_email failed: %s', e)
      return SendStatus(ok=False)

From notifier (existing exports in __init__.py):
  from .dispatch import (send_crash_email, send_daily_email, send_magic_link_email, send_stop_alert_email)
  Add: send_backup_stale_email to this import block.

rclone lsl output format (one line per file):
  "  12345 2026-05-12 14:30:00.000000000 state.json"
  Fields: size, date (YYYY-MM-DD), time (HH:MM:SS.nnnnnnnnn), path
  Parse: split(), [1] = date string, [2] = time string.
  Combine: datetime.fromisoformat(f'{date} {time[:8]}').replace(tzinfo=timezone.utc)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: send_backup_stale_email in notifier + unit tests</name>
  <files>notifier/dispatch.py, notifier/__init__.py, tests/test_backup_age.py</files>
  <behavior>
    notifier/dispatch.py: add send_backup_stale_email(last_backup_iso: str, *, now=None) -> SendStatus.
    - Parameters: last_backup_iso = ISO-formatted string of last successful backup time (e.g. '2026-05-10T14:30:00+00:00'); now = datetime for testability.
    - Subject: f'[trading-signals] BACKUP STALE — last backup {last_backup_iso}'
    - Body: plain-text paragraph explaining the backup is overdue and operator should check rclone/B2 config. Must pass html.escape on any dynamic value in the body.
    - Never-crash posture: wrap in try/except Exception, log '[Email] send_backup_stale_email failed: {e}', return SendStatus(ok=False).
    - To address from RESEND_TO env var (same as send_crash_email).

    notifier/__init__.py: add send_backup_stale_email to the dispatch import block.

    tests/test_backup_age.py: write unit tests for the stale-age logic (import as a pure function, mock notifier.send_backup_stale_email):
    - test_check_backup_age_not_stale: last_backup = now - 24h → function returns without calling send_backup_stale_email.
    - test_check_backup_age_stale: last_backup = now - 49h → function calls send_backup_stale_email once.
    - test_check_backup_age_exactly_48h: last_backup = now - 48h exactly → NOT stale (threshold is strictly > 48h).
    - test_parse_rclone_lsl_line: given '  12345 2026-05-12 14:30:00.000000000 state.json', parser returns datetime(2026, 5, 12, 14, 30, 0, tzinfo=timezone.utc).

    The logic functions themselves live in scripts/check_backup_age.py and are imported by the tests. The tests use unittest.mock.patch to stub out subprocess.run and notifier calls.
  </behavior>
  <action>
    Read notifier/dispatch.py to find the exact imports block and SendStatus usage pattern, then add send_backup_stale_email following the same structure.
    Read notifier/__init__.py lines 50-58 to find the exact dispatch import block.

    In notifier/dispatch.py: add send_backup_stale_email after send_stop_alert_email. Follow send_crash_email's structure exactly (same imports already available: os, logging, requests, SendStatus, RESEND_URL, HTTP_TIMEOUT_S, html).

    In notifier/__init__.py: add send_backup_stale_email to the from .dispatch import (...) block.

    Create tests/test_backup_age.py with the test functions described in behavior block. Write tests RED-first (scripts/check_backup_age.py does not yet exist — import will fail). Then create the script. Then GREEN.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_backup_age.py -x --tb=short</automated>
  </verify>
  <done>All 4 test_backup_age.py tests pass; send_backup_stale_email is importable from notifier; notifier full suite unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Scripts, systemd units, SETUP-DROPLET.md</name>
  <files>scripts/backup_state.sh, scripts/check_backup_age.py, systemd/trading-signals-backup.service, systemd/trading-signals-backup.timer, docs/SETUP-DROPLET.md</files>
  <action>
    Create scripts/ directory if it does not exist.

    Create scripts/backup_state.sh:
    - Shebang: #!/usr/bin/env bash
    - set -euo pipefail
    - Reads env vars: RCLONE_REMOTE (default 'b2'), B2_BUCKET, STATE_FILE (default 'state.json').
    - WorkingDirectory assumed to be /home/trader/trading-signals (set in systemd unit).
    - Command: rclone copy "${STATE_FILE}" "${RCLONE_REMOTE}:${B2_BUCKET}/trading-signals/" --log-level INFO
    - Exit on rclone non-zero (handled by set -e).
    - chmod +x is not needed in the file itself but note it in SETUP-DROPLET.md.

    Create scripts/check_backup_age.py:
    - Module docstring.
    - Imports: subprocess, sys, os, logging, datetime (UTC, datetime, timedelta, timezone).
    - Public function: parse_rclone_lsl_line(line: str) -> datetime | None — parses one rclone lsl output line into a UTC datetime. Returns None on parse failure.
    - Public function: get_last_backup_time(remote: str, bucket: str, path: str = 'trading-signals/state.json') -> datetime | None — runs subprocess(['rclone', 'lsl', f'{remote}:{bucket}/{path}'], capture_output=True, text=True); parses first non-empty line; returns None if no output or parse fails.
    - Public function: check_backup_age(threshold_hours: int = 48, now=None) -> None — calls get_last_backup_time; if None or (now - last_backup) > timedelta(hours=threshold_hours): calls notifier.send_backup_stale_email.
    - if __name__ == '__main__': call check_backup_age() with values from env vars BACKUP_THRESHOLD_HOURS (default 48), RCLONE_REMOTE (default 'b2'), B2_BUCKET (required — sys.exit(1) if missing).
    - 2-space indent.

    Create systemd/trading-signals-backup.service:
    - [Unit]: Description, After=network-online.target, Wants=network-online.target
    - [Service]: Type=oneshot, User=trader, Group=trader, WorkingDirectory=/home/trader/trading-signals, EnvironmentFile=-/home/trader/trading-signals/.env, ExecStart=/home/trader/trading-signals/scripts/backup_state.sh, StandardOutput=journal, StandardError=journal, SyslogIdentifier=trading-signals-backup
    - [Install]: WantedBy=multi-user.target
    - Security directives same as web.service: NoNewPrivileges=true, PrivateTmp=true, ProtectSystem=strict, ReadWritePaths=/home/trader/trading-signals, ProtectHome=read-only

    Create systemd/trading-signals-backup.timer:
    - [Unit]: Description=Daily backup of trading-signals state.json to B2
    - [Timer]: OnCalendar=daily, Persistent=true, Unit=trading-signals-backup.service
    - [Install]: WantedBy=timers.target

    Check if docs/ directory and SETUP-DROPLET.md exist.
    If SETUP-DROPLET.md exists: append a "## Backup Setup (Phase 33)" section.
    If it does not exist: create docs/SETUP-DROPLET.md with a "# Droplet Setup" header and a "## Backup Setup (Phase 33)" section.
    Section content: step-by-step instructions mirroring user_setup frontmatter above (rclone install, B2 bucket creation, rclone config, .env additions, systemd install + enable).
    Also document: how to test the backup manually (sudo systemctl start trading-signals-backup.service); how to verify B2 file (rclone lsl b2:BUCKET/trading-signals/).
  </action>
  <verify>
    <automated>bash -n scripts/backup_state.sh && python -c "import scripts.check_backup_age" 2>/dev/null || python scripts/check_backup_age.py --help 2>/dev/null; .venv/bin/python -c "import ast; ast.parse(open('scripts/check_backup_age.py').read()); print('syntax OK')" && test -f systemd/trading-signals-backup.service && test -f systemd/trading-signals-backup.timer && echo "all files OK"</automated>
  </verify>
  <done>scripts/backup_state.sh parses without bash errors; scripts/check_backup_age.py parses without Python syntax errors; both systemd units exist with correct directive structure; SETUP-DROPLET.md has backup section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell script → rclone | B2 credentials in .env; never committed |
| check_backup_age → notifier | I/O-to-I/O hex peer call; safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Information Disclosure | B2 credentials in .env | mitigate | .env in .gitignore; never committed; SETUP-DROPLET.md documents this |
| T-33-10 | Denial of Service | rclone network failure | accept | set -e in backup_state.sh causes systemd to log failure; timer Persistent=true retries on next boot |
| T-33-11 | Information Disclosure | state.json in B2 (at rest) | accept | B2 bucket is Private; only application key with scoped access |
| T-33-12 | Spoofing | rclone lsl output parsing | mitigate | parse_rclone_lsl_line returns None on unexpected format; check_backup_age treats None as "stale" and alerts |
</threat_model>

<verification>
.venv/bin/pytest tests/test_backup_age.py -x --tb=short
bash -n scripts/backup_state.sh
.venv/bin/python -c "import ast; ast.parse(open('scripts/check_backup_age.py').read()); print('syntax OK')"
grep -c "OnCalendar=daily" systemd/trading-signals-backup.timer
</verification>

<success_criteria>
- scripts/backup_state.sh is valid shell; scripts/check_backup_age.py is valid Python
- Both systemd units exist with correct structure
- test_backup_age.py: all 4 tests pass
- send_backup_stale_email importable from notifier
- SETUP-DROPLET.md has backup section with all operator steps documented
- Full suite: .venv/bin/pytest -x --tb=short still green
</success_criteria>

<output>
After completion, create `.planning/phases/33-schema-migration-v11-v12-admin-namespace-backup-gitignore/33-04-SUMMARY.md`
</output>
