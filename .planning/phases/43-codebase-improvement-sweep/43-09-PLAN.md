---
phase: 43-codebase-improvement-sweep
plan: 09
type: execute
wave: D
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - ruff.toml
  - CLAUDE.md
autonomous: true
requirements: [D-11, D-12]
must_haves:
  truths:
    - ".github/workflows/ci.yml runs pytest -x --tb=short on push and pull_request"
    - "CI workflow uses Python 3.13 (matches local .venv)"
    - "ruff.toml exists with indent-width = 2"
    - "ruff format --check . was run LOCALLY with diff inspected BEFORE the CI format-check is committed"
    - "CLAUDE.md no longer bans ruff format (only after local validation passes)"
    - "ruff format --check passes on the codebase at commit time"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline running pytest + ruff on push/PR"
      contains: "pytest"
    - path: "ruff.toml"
      provides: "indent-width = 2 config"
      contains: "indent-width"
---

<objective>
Add CI pipeline and fix ruff formatter config (LOW priority — quality/tooling; MEDIUM risk per Codex due to formatting churn). Local validation gate added per consensus review.
</objective>

<read_first>
- CLAUDE.md (current "Do NOT run ruff format" ban — context for the original concern)
- requirements.txt, requirements-dev.txt (verify ruff is pinned; pin if not)
- .github/workflows/daily.yml.disabled (existing workflow style reference)
- 43-REVIEWS.md (Codex MEDIUM concern: run `ruff format --check .` locally first)
</read_first>

<action>
**Ordering matters — perform locally before touching CI:**

1. **Create `ruff.toml`** at repo root:
   ```toml
   indent-width = 2
   line-length = 100

   [lint]
   select = ["E", "F", "I"]   # pycodestyle, pyflakes, isort (Gemini suggestion)
   ```

2. **Local validation step (DO THIS BEFORE COMMITTING THE CI CHANGE)**:
   - Run `ruff format --check .` from repo root with the new `ruff.toml`.
   - If it reports diffs, inspect each diff. The intent is that with `indent-width = 2` the codebase should already pass — if it does NOT pass, STOP. Do not blindly auto-format. Surface the diff to the user as a checkpoint; the user decides whether to (a) accept formatter changes as a separate commit, (b) tighten `ruff.toml` further, or (c) defer adding `ruff format --check` to CI for now.
   - Only proceed to step 3 once `ruff format --check .` exits 0 cleanly OR the user has explicitly approved the formatter diff.

3. **Create `.github/workflows/ci.yml`**:
   - Triggers: `push`, `pull_request`.
   - `runs-on: ubuntu-latest`.
   - `python-version: "3.13"` (matches `.venv/bin/pytest` Python 3.13 in CLAUDE.md).
   - Steps:
     - Checkout
     - Setup Python 3.13
     - `pip install -r requirements.txt -r requirements-dev.txt`
     - `pytest -x --tb=short -m "not uat"`
     - `ruff check .`
     - `ruff format --check .` (only include this line after step 2 passed)

4. **Update `CLAUDE.md`** — only after steps 2 and 3 verified locally:
   - Remove the "Do NOT run `ruff format` (reflows to 4-space; breaks test gate)" line.
   - Replace with: "`ruff format` is safe — `ruff.toml` pins `indent-width = 2`. CI enforces `ruff format --check` on every push."
</action>

<acceptance_criteria>
- `ruff.toml` exists; `grep -n "indent-width" ruff.toml` returns a line with `2`.
- `ruff format --check .` exits 0 when run locally from the repo root (no pending diffs at commit time).
- `.github/workflows/ci.yml` contains `pytest -x --tb=short -m "not uat"`.
- `.github/workflows/ci.yml` declares Python 3.13.
- `grep -n "ruff format" CLAUDE.md` no longer contains the prohibition phrasing; updated language present.
- A note in the commit message or executor SUMMARY confirms local `ruff format --check .` was run and diff was clean (or that user explicitly approved the diff as a prior commit).
</acceptance_criteria>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@CLAUDE.md
@.planning/phases/43-codebase-improvement-sweep/43-CONTEXT.md
</execution_context>
