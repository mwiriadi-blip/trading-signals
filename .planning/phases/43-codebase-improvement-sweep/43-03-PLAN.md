---
phase: 43-codebase-improvement-sweep
plan: 03
type: execute
wave: A
depends_on: []
files_modified:
  - system_params.py
  - interactive.py
  - tests/test_system_params.py
autonomous: true
requirements: [D-03]
must_haves:
  truths:
    - "system_params.INITIAL_ACCOUNT is typed as Decimal, not float"
    - "interactive.py:235 fresh_state['account'] = Decimal(initial_account) not float()"
    - "test asserts type(INITIAL_ACCOUNT) is Decimal"
  artifacts:
    - path: "system_params.py"
      provides: "INITIAL_ACCOUNT: Decimal"
      contains: "INITIAL_ACCOUNT"
    - path: "interactive.py"
      provides: "Decimal cast at fresh_state account assignment"
      contains: "Decimal(initial_account)"
---

<objective>
Fix `INITIAL_ACCOUNT` typed as `float` in `system_params.py:282` (HIGH — violates Decimal money rule).

`INITIAL_ACCOUNT: float = 10_000.0` is a monetary value and must be `Decimal` per CLAUDE.md convention. The cast at `interactive.py:235` also needs updating.

Steps:
1. Change `system_params.py:282` to `INITIAL_ACCOUNT: Decimal = Decimal('10000.00')`.
2. Update `interactive.py:235` cast from `float(initial_account)` to `Decimal(str(initial_account))`.
3. Run `pytest -x --tb=short` to confirm no regressions.
4. Add/update test asserting `isinstance(system_params.INITIAL_ACCOUNT, Decimal)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@CLAUDE.md
@.planning/phases/43-codebase-improvement-sweep/43-CONTEXT.md
</execution_context>
