---
phase: 43-codebase-improvement-sweep
plan: 07
type: execute
wave: C
depends_on: []
files_modified:
  - dashboard_legacy/  (deleted)
  - dashboard.py       (deleted)
  - daily_run_helpers.py
  - tests/test_dashboard.py
autonomous: true
requirements: [D-07, D-08]
must_haves:
  truths:
    - "dashboard_legacy/ directory is gone — no files remain"
    - "dashboard.py shim is gone"
    - "daily_run_helpers.py imports dashboard_renderer directly, not via dashboard shim"
    - "grep confirms zero 'from dashboard_legacy' or 'import dashboard_legacy' references"
    - "all tests pass after deletion"
  artifacts:
    - path: "daily_run_helpers.py"
      provides: "direct dashboard_renderer import"
      contains: "dashboard_renderer"
---

<objective>
Remove `dashboard_legacy/` tombstone and `dashboard.py` shim (LOW — housekeeping).

`dashboard_legacy/__init__.py` installs a meta-path finder at startup for code that no longer exists. `dashboard.py` is a 74-line re-export shim keeping an old module name alive.

Steps:
1. `grep -r 'dashboard_legacy' . --include='*.py'` — confirm no production imports.
2. `grep -r 'import dashboard[^_]' . --include='*.py'` — identify all shim consumers.
3. Update `daily_run_helpers.py:53` (and any other consumer) to import `dashboard_renderer` directly.
4. Delete `dashboard_legacy/` directory entirely.
5. Delete `dashboard.py`.
6. Run `pytest -x --tb=short` — all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@CLAUDE.md
@.planning/phases/43-codebase-improvement-sweep/43-CONTEXT.md
</execution_context>
