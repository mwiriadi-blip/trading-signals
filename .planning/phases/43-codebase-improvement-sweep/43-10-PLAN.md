---
phase: 43-codebase-improvement-sweep
plan: 10
type: execute
wave: D
depends_on: [43-03]
files_modified:
  - system_params.py
  - signal_engine.py
  - sizing_engine/sizing.py
autonomous: true
requirements: [D-13]
must_haves:
  truths:
    - "system_params.py exports IndicatorFloat = float and MoneyDecimal = Decimal type aliases"
    - "signal_engine.py uses IndicatorFloat for return types of indicator computations"
    - "sizing_engine/sizing.py uses MoneyDecimal for monetary arguments"
    - "no runtime behaviour changes — aliases are documentation only"
    - "mypy passes on annotated files"
  artifacts:
    - path: "system_params.py"
      provides: "IndicatorFloat, MoneyDecimal type aliases"
      contains: "IndicatorFloat"
    - path: "signal_engine.py"
      provides: "IndicatorFloat annotations on indicator boundary"
      contains: "IndicatorFloat"
---

<objective>
Add IndicatorFloat/MoneyDecimal type aliases to enforce float/Decimal boundary (LOW — type safety).

`signal_engine.py` correctly uses floats for indicators, but nothing prevents a future dev passing one into a Decimal monetary calculation. Type aliases make the boundary explicit without runtime cost.

Steps:
1. Add to `system_params.py`:
   ```python
   IndicatorFloat = float   # Indicator values (ATR, ADX, momentum) — NOT monetary
   MoneyDecimal = Decimal   # All AUD monetary amounts
   ```
2. Annotate `signal_engine.py` return types for `compute_indicators` and key computed values with `IndicatorFloat`.
3. Annotate `sizing_engine/sizing.py` monetary parameter types with `MoneyDecimal`.
4. Run mypy on annotated files — no new errors.
5. Run `pytest -x --tb=short` — no regressions.

Note: This is annotation-only. No logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@CLAUDE.md
@.planning/phases/43-codebase-improvement-sweep/43-CONTEXT.md
</execution_context>
