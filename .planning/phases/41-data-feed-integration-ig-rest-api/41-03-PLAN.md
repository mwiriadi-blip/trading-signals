---
phase: 41
plan: 03
type: execute
wave: 3
depends_on: [41-02]
files_modified:
  - daily_run.py
  - tests/test_daily_run.py
autonomous: true
requirements: [FEED-03]
tags: [ig, data-feed, fallback-warning, wave-2]
must_haves:
  truths:
    - "When data_fetcher falls back from IG to yfinance during a daily run, a dashboard warning is appended to state.warnings naming the affected symbol (D-02)"
    - "Warning is queued via existing pending_warnings.append((source, message)) pattern so it survives the end-of-cycle flush, even if later steps fail"
    - "Operator opens the dashboard after a fallback run and sees the warning surfaced in the warnings panel"
    - "When IG succeeds (or when IG creds are absent and yfinance is the deliberate path) NO warning is appended — warning is only for the fallback transition (D-02 semantic)"
    - "Existing W3 invariant (warnings dispatched + flushed in canonical order) is preserved"
  artifacts:
    - path: daily_run.py
      provides: "Post-fetch check that reads data_fetcher.LAST_FETCH_SOURCE[yf_symbol] and appends a fallback warning when value == 'yfinance_fallback'"
      contains: "LAST_FETCH_SOURCE"
    - path: tests/test_daily_run.py
      provides: "TestIGFallbackWarning class — fallback path appends warning; missing-creds path does NOT append warning; IG-success path does NOT append warning"
      contains: "TestIGFallbackWarning"
  key_links:
    - from: daily_run._run_daily_check_impl (post-fetch block)
      to: data_fetcher.LAST_FETCH_SOURCE
      via: "read symbol key; if == 'yfinance_fallback' append to pending_warnings"
      pattern: "LAST_FETCH_SOURCE"
    - from: daily_run.pending_warnings
      to: state['warnings']
      via: "existing end-of-cycle flush (W3 invariant)"
      pattern: "pending_warnings"
---

<objective>
Wave 2 — wire the D-02 dashboard warning into daily_run.py. data_fetcher (Plan 02) emits the WARNING log line; daily_run reads `data_fetcher.LAST_FETCH_SOURCE[symbol]` and queues a warning via the existing `pending_warnings.append((source, message))` pattern so the operator sees "IG fetch failed for SPI200 — yfinance fallback used" in the dashboard warnings panel after a fallback run. No state schema change; no save_state restructure; no W3 invariant disturbance.

Purpose: Close the user-visible half of D-02. The warning is the operator's signal that the primary data path failed and the fallback ran — actionable visibility without disrupting the run itself.
Output: ~10 LOC added to daily_run.py right after the fetch call site; ~80 LOC of focused tests in a new TestIGFallbackWarning class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/41-data-feed-integration-ig-rest-api/41-CONTEXT.md
@.planning/phases/41-data-feed-integration-ig-rest-api/41-RESEARCH.md
@.planning/phases/41-data-feed-integration-ig-rest-api/41-PATTERNS.md
@.planning/phases/41-data-feed-integration-ig-rest-api/41-02-SUMMARY.md
@daily_run.py
@data_fetcher.py
@tests/test_daily_run.py

<interfaces>
From Plan 02 (data_fetcher.py — DO NOT modify):
- `data_fetcher.LAST_FETCH_SOURCE: dict[str, str]`  (keys = caller symbol, values in {'ig', 'yfinance', 'yfinance_fallback'})
  - 'ig' = IG fetch succeeded
  - 'yfinance' = IG creds absent, yfinance is the planned path (NO warning needed — operator chose this configuration)
  - 'yfinance_fallback' = IG was tried, all retries failed, yfinance saved the run (WARNING required per D-02)

From daily_run.py (existing — DO NOT modify):
- `pending_warnings: list[tuple[str, str]]` — queue of (source, message) pairs; flushed at end of cycle (see line ~235 in run_daily_check_impl for the existing append pattern)
- `data_fetcher.fetch_ohlcv(yf_symbol, days=400, retries=3, backoff_s=10.0)` call at line ~206
- W3 invariant: exactly 2 state saves per cycle (Phase 7 Wave 0 history); warning append goes into pending_warnings, NOT into a save_state call

From state_manager (existing):
- `append_warning(state, source, message)` is called by the end-of-cycle flush helper; daily_run does NOT call append_warning directly here — it appends to pending_warnings and the flush helper handles state mutation under the W3 lock.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire LAST_FETCH_SOURCE check into daily_run + add TestIGFallbackWarning</name>
  <files>daily_run.py, tests/test_daily_run.py</files>
  <read_first>
    - daily_run.py (read FULL file — focus on lines 180-260: per-symbol loop, fetch_ohlcv call at ~206, pending_warnings.append pattern at ~235, end-of-run flush helper if present)
    - tests/test_daily_run.py (read FULL file — observe existing fixture patterns, monkeypatch idiom for data_fetcher.fetch_ohlcv, warnings assertion style)
    - data_fetcher.py (confirm LAST_FETCH_SOURCE module-level dict shipped in Plan 02)
    - .planning/phases/41-data-feed-integration-ig-rest-api/41-RESEARCH.md (§Architecture Patterns — Pattern 4 Note on D-02 Option A — log-only in fetch_ohlcv, append_warning in daily_run.py)
  </read_first>
  <behavior>
    - Immediately after the existing `df = data_fetcher.fetch_ohlcv(yf_symbol, ...)` call (line ~206-208) and BEFORE the short-frame check at line ~212, insert a 3-line block that reads `source = data_fetcher.LAST_FETCH_SOURCE.get(yf_symbol)` and, if `source == 'yfinance_fallback'`, appends `pending_warnings.append(('fetch', f'IG fetch failed for {yf_symbol} — yfinance fallback used'))`.
    - When source is 'ig' (happy path), do nothing — no warning, no log change (data_fetcher already logged at WARNING level on the fallback transition itself per Plan 02; this is the dashboard-surfacing half).
    - When source is 'yfinance' (deliberate config — creds absent), do nothing — D-06 semantics: operator configured no IG; the WARNING log from data_fetcher is sufficient, no dashboard warning per D-02 scope (which specifically targets IG-tried-and-failed, not IG-not-configured).
    - When source is None (defensive — should never happen since Plan 02 always sets it; but guard anyway): do nothing, do not crash.
    - The new code is grep-auditable: contains the literal string `LAST_FETCH_SOURCE` and `yfinance_fallback`.
    - The warning message format matches existing daily_run convention: tuple `('fetch', '<message>')` — same source label and shape as the stale-bar warning at line ~235.
    - W3 invariant preserved: NO new save_state call introduced; warning goes only into pending_warnings (queued for the existing end-of-cycle flush).
    - TestIGFallbackWarning class with these named methods:
      - `test_fallback_appends_warning` — monkeypatch `data_fetcher.fetch_ohlcv` to return a valid df AND set `data_fetcher.LAST_FETCH_SOURCE['^AXJO'] = 'yfinance_fallback'`; run one cycle; assert state['warnings'] (or whatever the flushed location is in the W3 dispatch) contains a warning whose message includes 'IG fetch failed for ^AXJO' and 'yfinance fallback used'.
      - `test_ig_success_no_warning` — same setup but LAST_FETCH_SOURCE['^AXJO'] = 'ig'; assert NO warning matching 'IG fetch failed' present (other unrelated warnings may exist — assertion is targeted on the IG-fallback message substring).
      - `test_missing_creds_no_warning` — LAST_FETCH_SOURCE['^AXJO'] = 'yfinance' (D-06 path); assert NO 'IG fetch failed' warning (the operator chose this; data_fetcher logged it; no dashboard noise).
      - `test_w3_invariant_preserved` — count `mutate_state` (or `save_state`) call count via monkeypatch counter; run a cycle with LAST_FETCH_SOURCE='yfinance_fallback'; assert call count equals the same number as a baseline cycle WITHOUT the warning (i.e. 2 saves per cycle — Phase 7 Wave 0 invariant).
    - Reset `data_fetcher.LAST_FETCH_SOURCE` at the start of each test (use monkeypatch.setattr with a fresh dict or pytest fixture autouse) so prior tests don't pollute state.
  </behavior>
  <action>**D-02 compliance — pending_warnings IS the path to state_manager.append_warning():** `pending_warnings.append((source, message))` is queued in-cycle and flushed by the existing end-of-cycle loop at `daily_run.py:435-436` (`for source, message in pending_warnings: state = state_manager.append_warning(state, source, message)`). Appending to `pending_warnings` therefore satisfies D-02's `state_manager.append_warning()` requirement via the canonical W3 flush path — verified by grep of daily_run.py at planning time. This task adds one entry to `pending_warnings`; the existing flush calls `append_warning` on every entry. Edit daily_run.py: locate the line `df = data_fetcher.fetch_ohlcv(yf_symbol, days=400, retries=3, backoff_s=10.0)` (currently at ~line 206). Insert immediately after the assignment (after the `fetch_elapsed = time.perf_counter() - fetch_start` line at ~209, BEFORE the short-frame check `if len(df) < _MIN_BARS_REQUIRED:` at ~212) a small block: read `fetch_source = data_fetcher.LAST_FETCH_SOURCE.get(yf_symbol)`; if `fetch_source == 'yfinance_fallback':` then `pending_warnings.append(('fetch', f'IG fetch failed for {yf_symbol} — yfinance fallback used'))`. Add a brief inline comment `# Phase 41 D-02: surface IG->yfinance fallback as dashboard warning` so the grep gate finds it. Do not modify the existing fetch call or the existing stale-bar warning block. Edit tests/test_daily_run.py: append a new class `TestIGFallbackWarning` at the bottom of the file. Use the existing fixture style (look at how other tests in this file monkeypatch data_fetcher.fetch_ohlcv and inspect state/warnings — mirror exactly). Provide an autouse class-scoped fixture that clears `data_fetcher.LAST_FETCH_SOURCE` between tests. Each test sets LAST_FETCH_SOURCE per scenario, calls the daily-run entry point, and asserts on either the flushed `state['warnings']` content or — if the existing test patterns assert on pending_warnings directly — match that style. Use 2-space indent throughout. Do not introduce a new test file; do not modify any non-daily_run test.</action>
  <verify>
    <automated>grep -c "LAST_FETCH_SOURCE" daily_run.py && grep -c "yfinance_fallback" daily_run.py && grep -c "Phase 41 D-02" daily_run.py && .venv/bin/ruff check daily_run.py && .venv/bin/pytest -x --tb=short tests/test_daily_run.py::TestIGFallbackWarning -v 2>&1 | tee /tmp/p41-03.log | tail -30 && .venv/bin/pytest -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LAST_FETCH_SOURCE" daily_run.py` ≥ 1.
    - `grep -c "yfinance_fallback" daily_run.py` ≥ 1.
    - `grep -c "Phase 41 D-02" daily_run.py` ≥ 1 (grep-auditable provenance comment).
    - `grep -c "pending_warnings.append" daily_run.py` increases by exactly 1 vs pre-Plan-03 baseline (new fallback warning) — i.e. there is now one MORE pending_warnings.append call than before this task.
    - `grep -c "save_state\|mutate_state" daily_run.py` is UNCHANGED vs pre-Plan-03 baseline (W3 invariant: no new state-write call site introduced).
    - `.venv/bin/ruff check daily_run.py` exits 0.
    - `.venv/bin/pytest -x --tb=short tests/test_daily_run.py::TestIGFallbackWarning -v` reports ≥4 tests PASSED.
    - `.venv/bin/pytest -x --tb=short` full suite exits 0.
    - TestIGFallbackWarning::test_w3_invariant_preserved PASSED — confirms no extra state-write per cycle.
    - The new warning message in daily_run.py uses the exact literal `'IG fetch failed for {yf_symbol} — yfinance fallback used'` (f-string formatted) — grep for `IG fetch failed for ` in daily_run.py returns ≥1 hit.
  </acceptance_criteria>
  <done>daily_run.py reads LAST_FETCH_SOURCE post-fetch and queues a fallback warning via pending_warnings; W3 invariant preserved; TestIGFallbackWarning all green; full suite green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data_fetcher module global state -> daily_run | LAST_FETCH_SOURCE is module-level mutable dict; daily_run reads after the fetch call within the same process; no cross-process trust |
| pending_warnings -> state['warnings'] -> dashboard render | Operator-visible message originates from a known string template; yf_symbol comes from system_params (trusted config); no user-supplied content interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Information Disclosure | Warning message text contains symbol but never API key / password / session token | mitigate | Message template is `f'IG fetch failed for {yf_symbol} — yfinance fallback used'`; yf_symbol is sourced from system_params.DEFAULT_MARKETS (trusted); no env-var interpolation |
| T-41-03-02 | Tampering | Stale LAST_FETCH_SOURCE entry from a previous test leaks into the next test, causing a false warning | mitigate | TestIGFallbackWarning class includes an autouse fixture that clears data_fetcher.LAST_FETCH_SOURCE before each test |
| T-41-03-03 | Denial of Service | A persistent IG outage produces a warning per symbol per day → state.warnings grows unbounded | accept | Existing MAX_WARNINGS FIFO (Plan 27-10: 50) caps the warnings list at 50 entries; per-cycle the fallback adds at most one warning per symbol (2 markets = 2 warnings/day); 25-day rolling buffer is acceptable operator visibility |
| T-41-03-04 | Elevation of Privilege | None — this plan only reads a module-global and appends to an existing in-memory list; no auth surface change | n/a | — |
</threat_model>

<verification>
1. `.venv/bin/ruff check daily_run.py` exits 0.
2. `grep` provenance checks pass (LAST_FETCH_SOURCE, yfinance_fallback, Phase 41 D-02 all present in daily_run.py).
3. `pending_warnings.append` count in daily_run.py increased by exactly 1; `save_state`/`mutate_state` count unchanged.
4. TestIGFallbackWarning all 4 tests PASS.
5. Full pytest suite green.
6. Operator-level (post-merge, not in plan): trigger a forced IG failure (set IG_API_KEY=garbage) → run daily_run → check dashboard warnings panel shows the new message.
</verification>

<success_criteria>
- daily_run.py reads data_fetcher.LAST_FETCH_SOURCE post-fetch
- Fallback transition (yfinance_fallback) → dashboard warning queued via pending_warnings
- IG success (ig) → no warning
- Missing-creds path (yfinance) → no warning
- W3 invariant preserved (no new state-write call sites)
- TestIGFallbackWarning class green; full suite green
- Phase 41 closes with IG primary, yfinance fallback, log + dashboard visibility on fallback transitions
</success_criteria>

<output>
After completion, create `.planning/phases/41-data-feed-integration-ig-rest-api/41-03-SUMMARY.md`
</output>
