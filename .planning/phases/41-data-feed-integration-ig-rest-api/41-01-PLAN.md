---
phase: 41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_data_fetcher.py
  - tests/fixtures/fetch/ig_spi200_prices.json
  - tests/fixtures/fetch/ig_audusd_prices.json
autonomous: true
requirements: [FEED-01]
tags: [ig, data-feed, test-scaffold, wave-0]
must_haves:
  truths:
    - "Test fixtures exist for IG SPI200 + AUDUSD response JSON"
    - "TestIGNormalise + TestIGFetch test classes exist as stubs/skeletons that import the still-unimplemented IG helpers and skip-or-fail cleanly"
    - "Existing TestFetch suite still passes (no regressions in scaffolding)"
  artifacts:
    - path: tests/fixtures/fetch/ig_spi200_prices.json
      provides: "Hand-crafted IG /prices response fixture (bid/ask candles, snapshotTimeUTC, lastTradedVolume=0)"
      contains: "snapshotTimeUTC"
    - path: tests/fixtures/fetch/ig_audusd_prices.json
      provides: "AUDUSD equivalent fixture"
      contains: "snapshotTimeUTC"
    - path: tests/test_data_fetcher.py
      provides: "TestIGFetch + TestIGNormalise classes with named test methods (initially expected to fail/xfail with ImportError or NotImplemented until Plan 02 lands)"
      contains: "class TestIGFetch"
  key_links:
    - from: tests/test_data_fetcher.py
      to: tests/fixtures/fetch/ig_spi200_prices.json
      via: "json.load via Path(__file__).parent / 'fixtures' / 'fetch'"
      pattern: "ig_spi200_prices.json"
---

<objective>
Wave 0 — establish test infrastructure for the IG fetch path before any source change. Create two recorded-response IG fixtures and append `TestIGFetch` + `TestIGNormalise` test class skeletons that name every behavior the implementation must satisfy (DATA-01/02/03 + D-01/D-02/D-06/D-12 + Pitfall 1/2/3/4). Tests are written against helper names that Plan 02 will introduce; until then they xfail or skip — establishing the Nyquist gate.

Purpose: Tests-first locks the IG branch contract (function names, fixture shapes, env-var gates) so Plan 02's implementation is mechanical and the LOC audit catches drift early.
Output: 2 JSON fixtures + ~80 LOC of test scaffolding in tests/test_data_fetcher.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/41-data-feed-integration-ig-rest-api/41-CONTEXT.md
@.planning/phases/41-data-feed-integration-ig-rest-api/41-RESEARCH.md
@.planning/phases/41-data-feed-integration-ig-rest-api/41-PATTERNS.md
@tests/test_data_fetcher.py
@data_fetcher.py

<interfaces>
Helpers Plan 02 will introduce in data_fetcher.py (tests reference these names now):
- `_ig_base_url() -> str`
- `_ig_create_session() -> dict`  (returns headers incl. CST, X-SECURITY-TOKEN, X-IG-API-KEY)
- `_ig_fetch_ohlcv_raw(epic: str, num_points: int, session_headers: dict) -> list`
- `_ig_normalise(prices: list) -> pd.DataFrame`  (returns columns Open,High,Low,Close,Volume; DatetimeIndex UTC)
- `_epic_for_symbol(symbol: str) -> str | None`
- `_fetch_via_ig(epic: str, days: int, retries: int, backoff_s: float) -> pd.DataFrame | None`
- Module-level `LAST_FETCH_SOURCE: dict[str, str]` mapping symbol -> 'ig' | 'yfinance' | 'yfinance_fallback'

IG response JSON shape (from RESEARCH.md §Code Examples) — fixture must mirror exactly:
- Top level: `{"prices": [ { ... }, ... ], "instrumentType": "...", "allowance": {...}}`
- Per candle: `snapshotTime`, `snapshotTimeUTC`, `openPrice` {bid, ask, lastTraded}, `highPrice`, `lowPrice`, `closePrice`, `lastTradedVolume`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IG response fixtures (SPI200 + AUDUSD)</name>
  <files>tests/fixtures/fetch/ig_spi200_prices.json, tests/fixtures/fetch/ig_audusd_prices.json</files>
  <read_first>
    - .planning/phases/41-data-feed-integration-ig-rest-api/41-RESEARCH.md (§Code Examples — IG Response JSON Shape, lines ~334-346)
    - .planning/phases/41-data-feed-integration-ig-rest-api/41-PATTERNS.md (§tests/test_data_fetcher.py)
    - tests/fixtures/fetch/ (list existing fixture filenames so naming convention matches)
  </read_first>
  <behavior>
    - Each fixture is a valid IG `/prices/{epic}/D/{n}` JSON response: top-level `{"prices": [...]}` array of >=5 candle objects so short-frame tests can be exercised independently in later plans.
    - Every candle has: snapshotTime ("YYYY/MM/DD HH:MM:SS"), snapshotTimeUTC (ISO 8601 like "2024-05-14T14:00:00"), openPrice/highPrice/lowPrice/closePrice each with `{bid, ask, lastTraded}` numeric values where ask >= bid, and lastTradedVolume = 0 (spread-bet convention per Pitfall 2).
    - SPI200 fixture price range ~7800-7900 (index points); AUDUSD fixture price range ~0.65-0.67 (FX 5-decimal).
    - mid = (bid + ask) / 2 must be computable to verify D-12; bid != ask in at least one candle so the math is non-trivial.
    - Timestamps must form a monotonically increasing daily series (no duplicates, no gaps within fixture).
  </behavior>
  <action>Create two hand-crafted JSON fixture files at the listed paths. Use ≥5 candle objects per file. SPI200 epic context is `IX.D.ASX.IFM.IP` (assumed per RESEARCH.md A1; documented in fixture as a top-level `_fixture_metadata` key containing `{"epic_assumed": "IX.D.ASX.IFM.IP", "source": "hand-crafted from RESEARCH.md §Code Examples shape"}` for traceability — IG responses don't include the epic, so this metadata sits outside the `prices` array and consumers ignore it). AUDUSD fixture uses `CS.D.AUDUSD.MINI.IP` in metadata. Numeric values: at least one candle in each fixture must have bid != ask (to exercise mid-price math). Set lastTradedVolume to exactly 0 in every candle (Pitfall 2). snapshotTimeUTC must use ISO 8601 without trailing 'Z' (matches IG actual format per RESEARCH.md §Code Examples). Pretty-print with 2-space indent per CLAUDE.md convention.</action>
  <verify>
    <automated>python -c "import json; d=json.load(open('tests/fixtures/fetch/ig_spi200_prices.json')); assert len(d['prices'])>=5; assert all('snapshotTimeUTC' in p and 'openPrice' in p and p['lastTradedVolume']==0 for p in d['prices']); assert any(p['openPrice']['bid']!=p['openPrice']['ask'] for p in d['prices']); print('SPI200 OK')" && python -c "import json; d=json.load(open('tests/fixtures/fetch/ig_audusd_prices.json')); assert len(d['prices'])>=5; assert all(p['lastTradedVolume']==0 for p in d['prices']); print('AUDUSD OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both fixture files exist and are valid JSON.
    - Each file's `prices` array contains ≥5 entries.
    - Every candle has all 7 required fields: snapshotTime, snapshotTimeUTC, openPrice (with bid, ask, lastTraded), highPrice, lowPrice, closePrice, lastTradedVolume.
    - `lastTradedVolume == 0` for every candle in both fixtures.
    - At least one candle per fixture has `openPrice.bid != openPrice.ask` (non-trivial mid-price math).
    - snapshotTimeUTC strings parseable by `pd.to_datetime(..., utc=True)` and strictly monotonic when sorted by index.
  </acceptance_criteria>
  <done>Both JSON fixtures committed; the verify command exits 0 printing "SPI200 OK" and "AUDUSD OK".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append TestIGFetch + TestIGNormalise skeletons to tests/test_data_fetcher.py</name>
  <files>tests/test_data_fetcher.py</files>
  <read_first>
    - tests/test_data_fetcher.py (read FULL file — observe TestFetch class layout, monkeypatch patterns lines 121-150, _FakeTicker class lines 63-100, fixture loader lines 45-54)
    - .planning/phases/41-data-feed-integration-ig-rest-api/41-PATTERNS.md (§tests/test_data_fetcher.py — TestIGFetch + TestIGNormalise blueprint, FakeResponse model)
    - data_fetcher.py (confirm `requests` is imported at module top so monkeypatch target `data_fetcher.requests.post` resolves)
  </read_first>
  <behavior>
    - New class `TestIGNormalise` with these named test methods (each runs successfully once Plan 02 ships `_ig_normalise`; until then they may xfail-on-ImportError via `pytest.importorskip` style guard — see action below):
      - `test_mid_price_calculation` — loads ig_spi200_prices.json, calls `_ig_normalise(prices)`, asserts every Open == (bid+ask)/2 for openPrice across all rows.
      - `test_volume_zero_accepted` — asserts every row has `Volume == 0` and no exception raised (Pitfall 2).
      - `test_index_is_utc_datetimeindex` — asserts result.index.tz is UTC and index is monotonic increasing (Pitfall 3).
      - `test_columns_are_canonical_ohlcv` — asserts list(result.columns) == ['Open','High','Low','Close','Volume'].
    - New class `TestIGFetch` with these named test methods (run after Plan 02):
      - `test_ig_happy_path_spi200` — monkeypatches `data_fetcher.requests.post` (session) and `data_fetcher.requests.get` (prices) to return _FakeResponse with the SPI200 fixture; sets IG env vars; calls fetch_ohlcv('^AXJO'); asserts DataFrame returned with expected columns.
      - `test_ig_happy_path_audusd` — same for 'AUDUSD=X' using audusd fixture.
      - `test_ig_retry_on_timeout` — first 2 get-calls raise requests.exceptions.ReadTimeout, 3rd returns fixture; assert fetch succeeds; assert call_count == 3.
      - `test_ig_fallback_to_yfinance` — IG raises ReadTimeout on all 3 attempts; yfinance monkeypatch returns valid df; assert returned df is from yfinance path (use a sentinel column value or assert `data_fetcher.LAST_FETCH_SOURCE['^AXJO'] == 'yfinance_fallback'`).
      - `test_fallback_emits_warning` — same setup as fallback test; with caplog at WARNING; assert "falling back to yfinance" substring in any record's message.
      - `test_missing_credentials_uses_yfinance` — monkeypatch.delenv('IG_API_KEY'); assert IG branch not exercised (no `requests.post` to /session called) and yfinance is invoked; assert WARNING log "IG credentials not configured".
      - `test_session_403_does_not_retry` — session POST returns 403; assert only one POST call attempted (no retry on auth failure per RESEARCH.md anti-pattern); assert fallback to yfinance triggered.
      - `test_prices_403_triggers_one_reauth` — prices GET returns 403 once → assert exactly one re-auth (second POST /session) → second GET succeeds. (Pitfall 4)
    - All TestIGFetch test methods must use a shared `_FakeResponse` helper class (defined at top of TestIGFetch or above it) mirroring `requests.Response`: `__init__(json_data, headers, status_code)`, `.raise_for_status()`, `.json()`, `.headers` dict, `.status_code` int.
    - All tests use `monkeypatch.setattr('data_fetcher.time.sleep', lambda *_a, **_k: None)` to keep the suite fast.
    - All tests use `monkeypatch.setenv('IG_API_KEY', 'test-key-123')` etc. for env-var injection, and `monkeypatch.delenv('IG_API_KEY', raising=False)` for the missing-creds test.
    - Until Plan 02 ships the helper functions, every TestIG* test must guard against AttributeError on the helper names by using a module-level skip: `pytestmark = pytest.mark.skipif(not hasattr(data_fetcher, '_ig_normalise'), reason='Plan 41-02 not yet shipped — IG helpers absent')`. This skip is placed at the top of TestIGFetch and TestIGNormalise so Plan 01 ships green and Plan 02 flips them to active.
  </behavior>
  <action>Append two new test classes to tests/test_data_fetcher.py after the existing TestColumnShape class (or at end of file). Place a module-level helper `_load_ig_fixture(name: str) -> dict` near the existing `_load_recorded_fixture` (line ~45) using `json.load` (NOT pd.read_json) — IG fixtures are raw JSON not DataFrames. Add `import json` if not already present. The `_FakeResponse` class is defined INSIDE the test module (not as a fixture) — keep it adjacent to TestIGFetch for locality. Each TestIG* class begins with `pytestmark = pytest.mark.skipif(not hasattr(data_fetcher, '_ig_normalise'), reason='Plan 41-02 not yet shipped — IG helpers absent')` as a CLASS-level attribute (not module-level — module level would skip the existing TestFetch class too). Use `data_fetcher._ig_normalise` etc. explicitly (not `from data_fetcher import _ig_normalise`) so the skip evaluates lazily and the import doesn't fail. Write test BODIES (not just `pass`) so Plan 02 verification has real assertions to satisfy. Use 2-space indent throughout (CLAUDE.md). Do NOT add new top-level imports beyond `import json` if missing — reuse existing pandas/pytest imports.</action>
  <verify>
    <automated>.venv/bin/pytest -x --tb=short tests/test_data_fetcher.py -v 2>&1 | tee /tmp/p41-01-task2.log | tail -40 && grep -c "TestIGFetch\|TestIGNormalise" tests/test_data_fetcher.py && .venv/bin/pytest --collect-only tests/test_data_fetcher.py 2>&1 | grep -E "(TestIGFetch|TestIGNormalise)::test_" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestIGFetch" tests/test_data_fetcher.py` returns 1.
    - `grep -c "class TestIGNormalise" tests/test_data_fetcher.py` returns 1.
    - `pytest --collect-only tests/test_data_fetcher.py` lists at least 8 tests under TestIGFetch and 4 under TestIGNormalise (12 total new tests).
    - Existing TestFetch tests still pass (no regression).
    - TestIGFetch + TestIGNormalise tests SKIP cleanly with reason matching "Plan 41-02 not yet shipped" (not error, not fail).
    - `grep -n "_FakeResponse" tests/test_data_fetcher.py` returns at least one definition line.
    - `grep -n "_load_ig_fixture\|json.load" tests/test_data_fetcher.py` shows the new fixture loader helper.
    - 2-space indentation: `grep -nP '^    [^ ]' tests/test_data_fetcher.py | head -5` shows nesting using 2/4 space steps consistent with rest of file.
    - `.venv/bin/pytest -x --tb=short tests/test_data_fetcher.py` exits 0.
  </acceptance_criteria>
  <done>tests/test_data_fetcher.py contains TestIGFetch (≥8 tests) and TestIGNormalise (≥4 tests); both classes skipped pending Plan 02; existing TestFetch suite still green; full pytest tests/test_data_fetcher.py exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> test process | Hand-crafted fixtures loaded via json.load; untrusted only insofar as a malicious commit could plant a payload — mitigated by code review of the fixture diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Information Disclosure | IG fixture content | accept | Fixtures contain only synthetic prices + ASSUMED epic codes in metadata; no real credentials, no real account data |
| T-41-01-02 | Tampering | _FakeResponse stub | accept | Test-only class; never imported by production code; isolation enforced by pytest collection scope |
| T-41-01-03 | Denial of Service | json.load on malformed fixture | mitigate | Verify step parses both fixtures; CI fails fast on JSON syntax error |
</threat_model>

<verification>
1. Both JSON fixtures parse cleanly with `python -c "import json; json.load(open(path))"`.
2. `.venv/bin/pytest -x --tb=short tests/test_data_fetcher.py` exits 0 (no regressions, new TestIG* classes skipped pending Plan 02).
3. `pytest --collect-only` shows ≥12 new collected tests under TestIGFetch + TestIGNormalise.
4. Full suite: `.venv/bin/pytest -x --tb=short` exits 0.
</verification>

<success_criteria>
- 2 IG fixture files committed under tests/fixtures/fetch/
- tests/test_data_fetcher.py extended with TestIGFetch (≥8 tests) + TestIGNormalise (≥4 tests), all currently SKIPPED via class-level pytest.mark.skipif
- Existing test suite green (no regressions)
- _FakeResponse helper class + _load_ig_fixture loader present in test file
</success_criteria>

<output>
After completion, create `.planning/phases/41-data-feed-integration-ig-rest-api/41-01-SUMMARY.md`
</output>
