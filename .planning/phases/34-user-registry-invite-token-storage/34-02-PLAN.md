---
phase: "34"
plan: "02"
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - auth_store/_users.py
  - auth_store/__init__.py
  - tests/test_auth_store_users.py
autonomous: true
requirements:
  - RBAC-03
  - RBAC-04

must_haves:
  truths:
    - "mint_invite_token(invited_by_uid, email) appends a PendingInvite with token_hash='sha256:<hex>' to auth.json and returns (raw_token, expires_at_iso)"
    - "consume_and_create_user(unhashed_token, new_user_fields) holds a single LOCK_EX window: reads via _read_auth_unlocked, calls _normalize_v2 inside the window (defensive against pre-v2 files), validates invite (unexpired, unconsumed, hmac.compare_digest timing-safe), marks consumed + appends user row, writes via _atomic_write_unlocked"
    - "Two concurrent consume_and_create_user calls on the same token (using real threads with a barrier): exactly one returns User, the other raises InviteAlreadyConsumed — single-use guarantee tested under contention, not by sequential calls"
    - "Expired invite raises InviteExpired (distinct typed error from InviteAlreadyConsumed per SC-4)"
    - "InviteAlreadyConsumed and InviteExpired are both subclasses of ValueError; importable from auth_store (D-11)"
    - "Malformed stored hashes (missing sha256: prefix, invalid hex, wrong algorithm prefix like 'md5:') fail closed (treated as non-matching, no exception leak)"
    - "create_user(fields) validates email is non-empty str, role in {'admin', 'ff'}, then appends a User row with uid=uuid4().hex, no flock (D-03); rejects caller-supplied uid"
    - "set_user_disabled(uid, disabled=True) flips the disabled flag; user row is retained (not deleted) per RBAC-04; returns False without writing when uid is unknown"
    - "get_user(uid) returns User dict or None; list_users() returns all User rows"
    - "_read_auth_unlocked (renamed from _load_raw per review consensus) lives in auth_store/_users.py with a docstring stating: 'caller must hold LOCK_EX'; handles empty file by returning _default_auth_data()"
    - "_verify_token is module-private — NOT re-exported from auth_store/__init__.py"
    - "auth_store.create_user, mint_invite_token, consume_and_create_user, get_user, list_users, set_user_disabled, InviteAlreadyConsumed, InviteExpired all importable from auth_store (D-11)"
    - "Raw token never appears in any persisted auth.json (verified by programmatic regex assertion, not shell grep)"
  artifacts:
    - path: "auth_store/_users.py"
      provides: "InviteAlreadyConsumed, InviteExpired exception classes; create_user, mint_invite_token, consume_and_create_user, get_user, list_users, set_user_disabled, _verify_token, _read_auth_unlocked"
      contains: "consume_and_create_user"
    - path: "tests/test_auth_store_users.py"
      provides: "TestUserRegistry, TestInviteMint, TestInviteConsume, TestInviteConsumeConcurrency, TestMalformedHash"
  key_links:
    - from: "auth_store/_users.py::consume_and_create_user"
      to: "auth_store/_io.py::_atomic_write_unlocked"
      via: "flock window: os.open + LOCK_EX + _read_auth_unlocked + _normalize_v2 + validate + mark + append + _atomic_write_unlocked + LOCK_UN"
      pattern: "fcntl.LOCK_EX"
    - from: "auth_store/_users.py::consume_and_create_user"
      to: "auth_store/_schema.py::_normalize_v2"
      via: "called inside flock window to backfill users=[] and pending_invites=[] defensively (HIGH-severity review fix)"
      pattern: "_normalize_v2"
    - from: "auth_store/_users.py::_verify_token"
      to: "hmac.compare_digest"
      via: "timing-safe compare of stored 'sha256:<hex>' against recomputed hash"
      pattern: "hmac.compare_digest"
    - from: "auth_store/__init__.py"
      to: "auth_store._users"
      via: "from auth_store._users import create_user, mint_invite_token, InviteAlreadyConsumed, InviteExpired, ... (NOT _verify_token, NOT _read_auth_unlocked)"
      pattern: "from auth_store._users import"
---

<objective>
Implement auth_store/_users.py: User registry CRUD helpers (create_user, get_user, list_users, set_user_disabled) and invite token helpers (mint_invite_token, consume_and_create_user), plus two typed exception classes (InviteAlreadyConsumed, InviteExpired). Wire into auth_store/__init__.py re-exports. Write tests/test_auth_store_users.py covering user registry, invite mint, invite consume (including a REAL concurrency test with threads, expiry, disabled-flag preservation, malformed hash handling).

This revision addresses three independent cross-AI reviews:
- HIGH (Codex + OpenCode): _load_raw bypasses migration. Fix: rename to _read_auth_unlocked, call _normalize_v2 (from Plan 01) inside the flock window with a schema-version invariant assertion as a tripwire.
- HIGH (OpenCode): flock-on-inode vs os.replace mismatch. Fix: documented in docstring; single-use guarantee verified by a real two-thread concurrency test with threading.Barrier.
- MEDIUM (Codex): no input validation at storage boundary. Fix: validate email + role + reject caller-supplied uid in create_user; same validation block reused inside consume_and_create_user.
- MEDIUM (Codex): empty/missing files inside flock window. Fix: _read_auth_unlocked handles empty bytes by returning _default_auth_data().
- LOW (OpenCode): grep gate too brittle. Fix: programmatic regex assertion on persisted JSON (not shell grep, not len(raw) == 43).
- LOW (Codex + OpenCode): missing malformed-hash coverage. Fix: TestMalformedHash class with 3 cases.

Purpose: Delivers RBAC-03 storage half (invite token mint/hash/verify/consume with flock single-use guarantee) and RBAC-04 (disabled field, reversible disable, data preserved). Distinct typed errors satisfy SC-4. No routes land this phase — pure storage layer.

Output: auth_store/_users.py, updated auth_store/__init__.py, tests/test_auth_store_users.py (~300 LOC, 5 test classes including real concurrency).
</objective>

<execution_context>
@/Users/marcwiriadisastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcwiriadisastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/PROJECT.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/ROADMAP.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/STATE.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/34-user-registry-invite-token-storage/34-01-SUMMARY.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/34-user-registry-invite-token-storage/34-REVIEWS.md

<interfaces>
<!-- Key contracts the executor needs. Read these files before implementing. -->

From auth_store/_io.py (must read before implementing _users.py):
  _resolve_path(path: Path | None) -> Path
  _default_auth_data() -> AuthData                       # used by _read_auth_unlocked on empty file
  _atomic_write_unlocked(data: str, path: Path) -> None  # NO lock; used inside flock window
  load_auth(path: Path | None = None) -> AuthData        # public; do NOT call inside flock window — deadlock
  save_auth(data: AuthData, path: Path | None = None) -> None  # public; do NOT call inside flock window — deadlock

From auth_store/_schema.py (must read before implementing):
  SCHEMA_VERSION: int = 2
  _normalize_v2(data: dict) -> dict   # pure in-memory; SAFE to call inside flock window (no disk I/O)
  class User(TypedDict): uid, email, role, created_at, disabled
  class PendingInvite(TypedDict): token_hash, email, invited_by, created_at, expires_at, consumed, consumed_at
  class AuthData(TypedDict): schema_version, totp_secret, totp_enrolled, totp_enrolled_at, trusted_devices, pending_magic_links, users, pending_invites

From auth_store/__init__.py (must read to know what to add to re-exports):
  DEFAULT_AUTH_PATH: Path                                # defined at top of __init__.py
  # Currently does NOT export: create_user, mint_invite_token, consume_and_create_user, get_user, list_users, set_user_disabled, InviteAlreadyConsumed, InviteExpired
  # MUST NOT export: _verify_token, _read_auth_unlocked (module-private)

From state_manager/io.py lines 172-200 — _atomic_write pattern (flock acquire sequence):
  lock_fd = os.open(str(resolved), os.O_RDWR | os.O_CREAT, 0o600)
  fcntl.flock(lock_fd, fcntl.LOCK_EX)
  ... do work ...
  fcntl.flock(lock_fd, fcntl.LOCK_UN)  # release BEFORE os.close
  os.close(lock_fd)

CRITICAL ANTI-PATTERNS (do not violate):
  - NEVER call save_auth() inside the flock window — it calls _atomic_write() which tries LOCK_EX again = deadlock
  - NEVER call load_auth() inside the flock window — load_auth's migration calls save_auth = deadlock
  - Inside flock window, use _read_auth_unlocked + _normalize_v2 + _atomic_write_unlocked ONLY
  - token_hash stored as 'sha256:<hex>' (prefix-tagged, per CONTEXT specifics)
  - hmac.compare_digest for token comparison (not == string equality)
  - Raw token NEVER logged or persisted anywhere
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement auth_store/_users.py and wire into __init__.py</name>
  <files>auth_store/_users.py, auth_store/__init__.py</files>
  <read_first>
    - auth_store/_io.py (full file — understand _resolve_path, _atomic_write_unlocked, _default_auth_data, load_auth, save_auth before writing _users.py)
    - auth_store/_schema.py (full file — User, PendingInvite, AuthData TypedDicts, _normalize_v2 helper)
    - auth_store/__init__.py (full file — DEFAULT_AUTH_PATH at top; add new re-exports here AFTER existing ones)
    - state_manager/io.py (lines 172-220 — _atomic_write flock acquire/release pattern to mirror exactly)
    - .planning/phases/34-user-registry-invite-token-storage/34-REVIEWS.md (consensus concerns to address inline)
    - CLAUDE.md (2-space indent, stdlib-only, 500 LOC cap, ruff format FORBIDDEN)
  </read_first>
  <behavior>
    - InviteAlreadyConsumed(ValueError): defined at module top-level. Raised when token does not match any pending_invites row OR matched row has consumed=True. Message: 'invalid or already consumed invite token'.
    - InviteExpired(ValueError): defined at module top-level. Raised when a matched, unconsumed invite row has expires_at in the past. Message: 'invite token expired'.
    - _read_auth_unlocked(resolved: Path) -> dict: read auth.json via path.read_text(encoding='utf-8'); if file does not exist OR text is empty/whitespace-only, return _default_auth_data(); otherwise json.loads. Docstring: 'Caller MUST hold LOCK_EX on the file before calling this — returns potentially-unmigrated data. Always run _normalize_v2 on the result before using users/pending_invites fields.' Module-private (leading underscore); NOT re-exported.
    - _verify_token(unhashed_token: str, stored_hash: str) -> bool: if not isinstance(stored_hash, str) or not stored_hash.startswith('sha256:'): return False; expected = 'sha256:' + hashlib.sha256(unhashed_token.encode('utf-8')).hexdigest(); try: return hmac.compare_digest(stored_hash, expected); except Exception: return False (defensive — malformed hashes fail closed, never raise). Module-private; NOT re-exported.
    - _validate_user_fields(fields: dict) -> tuple[str, str]: returns (email, role). Raises ValueError if email is missing/empty/not a str. Raises ValueError if role is present and not in {'admin', 'ff'}. Defaults role to 'ff' if absent. Raises ValueError if fields contains a 'uid' key (caller-supplied uid is forbidden per D-06; uid is generated by storage). Module-private.
    - create_user(fields: dict, path=None) -> User: call _validate_user_fields; load_auth; build User dict (uid=uuid4().hex per D-06, email=validated_email, role=validated_role, created_at=datetime.now(timezone.utc).isoformat(), disabled=False); append to data['users']; save_auth; return user dict. No flock (D-03).
    - get_user(uid: str, path=None) -> User | None: load_auth; find row where row['uid'] == uid; return row or None.
    - list_users(path=None) -> list[User]: return load_auth(path=path).get('users', []).
    - set_user_disabled(uid: str, disabled: bool, path=None) -> bool: load_auth; find row; if not found return False without writing; if found, set row['disabled'] = disabled; save_auth; return True. User row RETAINED (not deleted) regardless of disabled value (RBAC-04). Reversible.
    - mint_invite_token(invited_by_uid: str, email: str, path=None) -> tuple[str, str]: raw = secrets.token_urlsafe(32); token_hash = 'sha256:' + hashlib.sha256(raw.encode('utf-8')).hexdigest(); now = datetime.now(timezone.utc); expires = now + timedelta(days=7); load_auth; append PendingInvite row with consumed=False, consumed_at=None; save_auth; return (raw, expires.isoformat()). No flock at this phase (D-01).
    - consume_and_create_user(unhashed_token: str, new_user_fields: dict, path=None) -> User:
        1. _validate_user_fields(new_user_fields)   # validate BEFORE opening lock to fail fast
        2. resolved = _resolve_path(path)
        3. lock_fd = os.open(str(resolved), os.O_RDWR | os.O_CREAT, 0o600)
        4. try:
             fcntl.flock(lock_fd, fcntl.LOCK_EX)
             try:
               raw_data = _read_auth_unlocked(resolved)
               data = _normalize_v2(raw_data)   # HIGH-fix: backfill v2 keys defensively inside the lock
               assert data.get('schema_version', 0) >= 2, 'migration must precede invite flow'
               # scan pending_invites for matching hash
               matched_row = None
               for row in data['pending_invites']:
                 if _verify_token(unhashed_token, row.get('token_hash', '')):
                   matched_row = row
                   break
               if matched_row is None or matched_row.get('consumed') is True:
                 raise InviteAlreadyConsumed('invalid or already consumed invite token')
               expires_dt = datetime.fromisoformat(matched_row['expires_at'])
               if datetime.now(timezone.utc) > expires_dt:
                 raise InviteExpired('invite token expired')
               matched_row['consumed'] = True
               matched_row['consumed_at'] = datetime.now(timezone.utc).isoformat()
               new_user = {
                 'uid': uuid.uuid4().hex,
                 'email': validated_email,
                 'role': validated_role,
                 'created_at': datetime.now(timezone.utc).isoformat(),
                 'disabled': False,
               }
               data['users'].append(new_user)
               payload = json.dumps(data, indent=2)
               _atomic_write_unlocked(payload, resolved)
               return new_user
             finally:
               fcntl.flock(lock_fd, fcntl.LOCK_UN)
           finally:
             os.close(lock_fd)
    - Docstring on consume_and_create_user MUST contain: a NOTE block explaining flock-on-inode-vs-os.replace semantics — 'LOCK_EX serializes other flock holders against this critical section. After _atomic_write_unlocked swaps in a new inode, the lock fd refers to the old inode; this is intentional. Subsequent flock holders re-open the new inode and wait. Non-flock writers (mint_invite_token, create_user) can race per D-01 — accepted for F&F scale.'
    - Module-level comment at top of _users.py: '# Flock warning: LOCK_EX serializes only flock holders. mint_invite_token and create_user use load->save without flock (D-01). The single-use consume guarantee remains intact because two concurrent consumes BOTH attempt LOCK_EX on auth.json; the second waits, then re-reads the new inode and finds consumed=True.'
  </behavior>
  <action>
    Create auth_store/_users.py. Structure:
      1. Module-level comment block explaining the flock-vs-non-flock race (per behavior block)
      2. Imports: fcntl, hashlib, hmac, json, logging, os, secrets, uuid; from datetime import datetime, timedelta, timezone; from pathlib import Path
      3. Imports from auth_store._io: _resolve_path, _atomic_write_unlocked, _default_auth_data, load_auth, save_auth
      4. Imports from auth_store._schema: User, PendingInvite, _normalize_v2
      5. class InviteAlreadyConsumed(ValueError): pass
      6. class InviteExpired(ValueError): pass
      7. _read_auth_unlocked(resolved) -> dict   # private; renamed from _load_raw per review consensus
      8. _verify_token(unhashed_token, stored_hash) -> bool   # private; fail-closed on malformed
      9. _validate_user_fields(fields) -> tuple[str, str]   # private
      10. create_user, get_user, list_users, set_user_disabled, mint_invite_token, consume_and_create_user

    For _verify_token, catch any Exception inside the compare_digest path so malformed stored hashes return False rather than propagating a stray UnicodeError or TypeError to the caller. Closed-by-default is the security property.

    For consume_and_create_user, _validate_user_fields runs OUTSIDE the lock (fail-fast before touching the FS lock fd). The validation result (email, role) is captured in local variables and reused when building the new_user dict inside the lock.

    Update auth_store/__init__.py to re-export:
      from auth_store._users import (
        InviteAlreadyConsumed,
        InviteExpired,
        create_user,
        mint_invite_token,
        consume_and_create_user,
        get_user,
        list_users,
        set_user_disabled,
      )
    Do NOT re-export _verify_token or _read_auth_unlocked — both are module-private.

    2-space indent throughout. Do NOT use ruff format. Keep _users.py under 250 LOC (slightly more headroom than original 200 — validation + docstrings add lines).
  </action>
  <acceptance_criteria>
    - auth_store/_users.py exists; defines `class InviteAlreadyConsumed(ValueError)` and `class InviteExpired(ValueError)` at module top level
    - `python -c "from auth_store import InviteAlreadyConsumed, InviteExpired; assert issubclass(InviteAlreadyConsumed, ValueError); assert issubclass(InviteExpired, ValueError)"` exits 0
    - `python -c "import auth_store; assert not hasattr(auth_store, '_verify_token'); assert not hasattr(auth_store, '_read_auth_unlocked')"` exits 0 (module-private not re-exported)
    - auth_store/_users.py contains `def _read_auth_unlocked(` (renamed from _load_raw per review consensus) — verify with `grep -q 'def _read_auth_unlocked' auth_store/_users.py`
    - auth_store/_users.py does NOT contain `def _load_raw(` — verify with `! grep -q 'def _load_raw' auth_store/_users.py`
    - auth_store/_users.py contains `_normalize_v2(` invocation inside consume_and_create_user — verify with `grep -q '_normalize_v2(' auth_store/_users.py`
    - auth_store/_users.py contains the assertion substring `'migration must precede invite flow'` — verify with `grep -q 'migration must precede invite flow' auth_store/_users.py`
    - auth_store/_users.py contains `fcntl.LOCK_EX` and `_atomic_write_unlocked` — verify with two greps
    - auth_store/_users.py does NOT call `save_auth` inside the lock block (file overall may call save_auth in non-locked helpers — check for textual proximity by reading the consume_and_create_user function body)
    - consume_and_create_user docstring contains a NOTE about inode-vs-os.replace semantics — verify with `grep -A 20 'def consume_and_create_user' auth_store/_users.py | grep -i 'inode\|replace\|flock'`
    - _verify_token returns False (not raises) on malformed inputs — verified by Task 2's TestMalformedHash
    - .venv/bin/pytest tests/test_auth_store.py -x --tb=short -q exits 0 (no regression in Plan 01 tests)
    - auth_store/_users.py is under 250 LOC: `[ $(wc -l < auth_store/_users.py) -lt 250 ]`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && python -c "from auth_store import create_user, mint_invite_token, consume_and_create_user, get_user, list_users, set_user_disabled, InviteAlreadyConsumed, InviteExpired; import auth_store; assert issubclass(InviteAlreadyConsumed, ValueError); assert issubclass(InviteExpired, ValueError); assert not hasattr(auth_store, '_verify_token'); assert not hasattr(auth_store, '_read_auth_unlocked'); print('all imports OK')" && grep -q 'def _read_auth_unlocked' auth_store/_users.py && ! grep -q 'def _load_raw' auth_store/_users.py && grep -q '_normalize_v2(' auth_store/_users.py && grep -q 'migration must precede invite flow' auth_store/_users.py && .venv/bin/pytest tests/test_auth_store.py -x --tb=short -q</automated>
  </verify>
  <done>All 6 user helpers and both exception classes importable from auth_store; _verify_token and _read_auth_unlocked NOT importable from auth_store; _normalize_v2 called inside the flock window; schema-version invariant assertion present; consume docstring documents inode-vs-os.replace semantics; test_auth_store.py still green; auth_store/_users.py under 250 LOC</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_auth_store_users.py (5 test classes including real concurrency and malformed-hash coverage)</name>
  <files>tests/test_auth_store_users.py</files>
  <read_first>
    - tests/test_auth_store.py (lines 1-40 — fixture and import pattern to mirror; isolated_auth_json fixture usage)
    - tests/conftest.py (isolated_auth_json fixture — understand how monkeypatch.setattr(auth_store, 'DEFAULT_AUTH_PATH', tmp_auth) works)
    - auth_store/_users.py (just written — function signatures, exception classes, _verify_token semantics)
    - auth_store/_io.py (load_auth, save_auth, _atomic_write_unlocked — needed for test setup)
    - auth_store/_schema.py (_normalize_v2, User, PendingInvite TypedDicts)
    - .planning/phases/34-user-registry-invite-token-storage/34-REVIEWS.md (concurrency test, malformed-hash test, programmatic regex assertion)
  </read_first>
  <behavior>
    Five test classes:

    TestUserRegistry (6 tests):
      - test_create_user_appends_row: create_user({'email': 'a@b.com', 'role': 'ff'}); list_users() returns 1 row; row['email'] == 'a@b.com'; row['role'] == 'ff'; row['disabled'] is False; row['uid'] matches re.compile(r'^[0-9a-f]{32}$')
      - test_create_user_default_role_is_ff: create_user({'email': 'a@b.com'}) (no role key); row['role'] == 'ff'
      - test_create_user_rejects_caller_supplied_uid: with pytest.raises(ValueError): create_user({'email': 'a@b.com', 'uid': 'spoofed'})
      - test_create_user_rejects_invalid_role: with pytest.raises(ValueError): create_user({'email': 'a@b.com', 'role': 'superuser'})
      - test_create_user_rejects_missing_email: with pytest.raises(ValueError): create_user({})  # no email key
      - test_create_user_rejects_empty_email: with pytest.raises(ValueError): create_user({'email': ''})
      - test_get_user_returns_none_for_unknown: get_user('nonexistent') returns None
      - test_set_user_disabled_flips_flag: create user; set_user_disabled(uid, True); get_user(uid)['disabled'] is True
      - test_set_user_disabled_preserves_row: create user; set_user_disabled(uid, True); list_users() still returns 1 row (RBAC-04 retention)
      - test_set_user_disabled_reversible: set True then False; final state disabled is False
      - test_set_user_disabled_unknown_uid_returns_false: set_user_disabled('nonexistent', True) returns False; no mtime change on auth.json (assert mtime unchanged across the call)

    TestInviteMint (3 tests):
      - test_mint_invite_token_stores_hash_only: raw, expires_iso = mint_invite_token('admin-uid', 'inv@b.com'); data = load_auth(); row = data['pending_invites'][0]; assert row['token_hash'].startswith('sha256:'); assert row['consumed'] is False; assert row['email'] == 'inv@b.com'; assert row['invited_by'] == 'admin-uid'
      - test_mint_invite_token_returns_tuple_with_iso_expiry: raw, expires_iso = mint_invite_token('uid', 'e@b.com'); assert isinstance(raw, str); assert isinstance(expires_iso, str); parsed = datetime.fromisoformat(expires_iso); now = datetime.now(timezone.utc); assert timedelta(days=6) < (parsed - now) < timedelta(days=8) (entropy-shape test instead of brittle len==43; round-trip verifiable via _verify_token in the next test)
      - test_raw_token_verifies_against_stored_hash_and_is_not_persisted: raw, _ = mint_invite_token('uid', 'e@b.com'); persisted_bytes = Path(auth_store.DEFAULT_AUTH_PATH).read_bytes(); assert raw.encode('utf-8') not in persisted_bytes; persisted_str = persisted_bytes.decode('utf-8'); import re; assert re.search(r'\b' + re.escape(raw) + r'\b', persisted_str) is None; (programmatic regex per OpenCode review — not shell grep, not len==43)

    TestInviteConsume (5 tests):
      - test_consume_and_create_user_success: mint token; user = consume_and_create_user(raw, {'email': 'e@b.com'}); assert user['email'] == 'e@b.com'; assert re.match(r'^[0-9a-f]{32}$', user['uid']); data = load_auth(); assert data['pending_invites'][0]['consumed'] is True; assert data['pending_invites'][0]['consumed_at'] is not None; assert len(data['users']) == 1
      - test_consume_and_create_user_single_use_sequential: mint token; consume_and_create_user(raw, {'email': 'e@b.com'}); with pytest.raises(InviteAlreadyConsumed) as exc_info: consume_and_create_user(raw, {'email': 'e@b.com'}); assert isinstance(exc_info.value, InviteAlreadyConsumed)
      - test_consume_and_create_user_expired_raises_distinct_type: mint token; manually edit auth.json to set expires_at to (now - 1 day).isoformat() on the pending_invites row; with pytest.raises(InviteExpired) as exc_info: consume_and_create_user(raw, {'email': 'e@b.com'}); assert isinstance(exc_info.value, InviteExpired); assert not isinstance(exc_info.value, InviteAlreadyConsumed) — SC-4 distinct types
      - test_consume_and_create_user_unknown_token: with pytest.raises(InviteAlreadyConsumed): consume_and_create_user('not-a-real-token', {'email': 'x@b.com'})
      - test_consume_and_create_user_propagates_validation: mint token; with pytest.raises(ValueError): consume_and_create_user(raw, {'email': 'e@b.com', 'uid': 'spoofed'}) — caller-supplied uid rejected before lock taken

    TestInviteConsumeConcurrency (2 tests — REAL concurrency, per Codex HIGH suggestion):
      - test_two_threads_consuming_same_token_only_one_succeeds: use threading.Thread + threading.Barrier(2) so both threads attempt consume_and_create_user simultaneously; collect results into two lists (success_users, errors); barrier.wait() inside each thread before the call; after join, assert len(success_users) == 1; assert len(errors) == 1; assert isinstance(errors[0], InviteAlreadyConsumed); final state: data['pending_invites'][0]['consumed'] is True; len(data['users']) == 1
      - test_two_threads_consuming_different_tokens_both_succeed: mint two distinct tokens; barrier+threads as above; assert both succeed; len(users) == 2

    TestMalformedHash (3 tests — fail-closed verification, per Codex LOW suggestion):
      - test_consume_rejects_invite_with_missing_sha256_prefix: manually craft a v2 auth.json with a pending_invites row whose token_hash = '<hex-no-prefix>' (no 'sha256:'); attempt consume with any token; expect InviteAlreadyConsumed (not match found, fail closed)
      - test_consume_rejects_invite_with_invalid_hex: token_hash = 'sha256:not-hex'; consume raises InviteAlreadyConsumed (fail closed)
      - test_consume_rejects_invite_with_wrong_algorithm: token_hash = 'md5:abcdef'; consume raises InviteAlreadyConsumed (fail closed — _verify_token returns False due to prefix mismatch)

    All tests use isolated_auth_json fixture (auto-monkeypatches DEFAULT_AUTH_PATH). Import auth_store (not sub-modules) per D-11. Use isinstance assertions (not match= string patterns) for SC-4 distinct-typed-error verification.
  </behavior>
  <action>
    Create tests/test_auth_store_users.py. Structure:
      - Module docstring referencing Phase 34 Plan 02
      - Imports: json, re, threading; from datetime import datetime, timedelta, timezone; from pathlib import Path; import pytest; import auth_store
      - Five test classes in the order above: TestUserRegistry, TestInviteMint, TestInviteConsume, TestInviteConsumeConcurrency, TestMalformedHash
      - All tests use the isolated_auth_json fixture from conftest.py (it redirects DEFAULT_AUTH_PATH to a tmp file)

    For TestInviteConsumeConcurrency:
      - Use threading.Barrier(parties=2) to synchronize the two threads at the moment-of-consume
      - Each thread function: try barrier.wait(); user = consume_and_create_user(raw, {'email': 'e@b.com'}); successes.append(user); except Exception as e: errors.append(e)
      - Spawn both threads, start them, join with a timeout (5 seconds) so a deadlock fails the test rather than hanging
      - After join, assert exactly one success and exactly one InviteAlreadyConsumed

    For TestMalformedHash:
      - Each test writes a v2 auth.json directly via json.dumps to the tmp path with a single pending_invites row whose token_hash field is intentionally malformed
      - Attempt consume_and_create_user with any plausible string token
      - Assert InviteAlreadyConsumed (fail closed)
      - Also assert no Python exception other than InviteAlreadyConsumed leaks (e.g., no ValueError from int conversion, no binascii.Error from hex parse) — pytest.raises with the specific class enforces this

    For the programmatic grep gate (test_raw_token_verifies_against_stored_hash_and_is_not_persisted):
      - Read auth.json as bytes via Path(auth_store.DEFAULT_AUTH_PATH).read_bytes()
      - Assert raw.encode('utf-8') not in persisted_bytes
      - Also assert the string form is not present via re.search with re.escape(raw) on the decoded content
      - This is the OpenCode-recommended programmatic assertion (not shell grep, not fragile len-based)

    2-space indent. Do NOT use ruff format. Keep file under 350 LOC.
  </action>
  <acceptance_criteria>
    - tests/test_auth_store_users.py exists; defines exactly five classes: TestUserRegistry, TestInviteMint, TestInviteConsume, TestInviteConsumeConcurrency, TestMalformedHash (verify with grep)
    - TestInviteConsumeConcurrency contains at least one test using `threading.Barrier` — verify with `grep -q 'threading.Barrier' tests/test_auth_store_users.py`
    - TestMalformedHash contains exactly 3 tests — verify with `grep -c 'def test_' | filter to within the class block` (or simpler: grep for the three test names)
    - The "raw token not in auth.json" test uses programmatic regex (re.search or `in` on bytes), NOT shell-out to grep — verify with `! grep -q 'subprocess.*grep' tests/test_auth_store_users.py`
    - All distinct-type assertions use isinstance, not match= regex on exception message — verify with `grep -q 'isinstance(exc_info.value, InviteAlreadyConsumed)' tests/test_auth_store_users.py` and same for InviteExpired
    - Concurrency test asserts exactly one success and exactly one InviteAlreadyConsumed (NOT both failing, NOT both succeeding)
    - .venv/bin/pytest tests/test_auth_store_users.py -x --tb=short -q exits 0; all classes pass
    - Full suite still green: .venv/bin/pytest -x --tb=short -q exits 0
    - tests/test_auth_store_users.py under 350 LOC: `[ $(wc -l < tests/test_auth_store_users.py) -lt 350 ]`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && grep -q 'class TestUserRegistry' tests/test_auth_store_users.py && grep -q 'class TestInviteMint' tests/test_auth_store_users.py && grep -q 'class TestInviteConsume\b' tests/test_auth_store_users.py && grep -q 'class TestInviteConsumeConcurrency' tests/test_auth_store_users.py && grep -q 'class TestMalformedHash' tests/test_auth_store_users.py && grep -q 'threading.Barrier' tests/test_auth_store_users.py && grep -q 'isinstance(exc_info.value, InviteAlreadyConsumed)' tests/test_auth_store_users.py && grep -q 'isinstance(exc_info.value, InviteExpired)' tests/test_auth_store_users.py && .venv/bin/pytest tests/test_auth_store_users.py -x --tb=short -q && .venv/bin/pytest -x --tb=short -q</automated>
  </verify>
  <done>tests/test_auth_store_users.py passes all 5 classes (~19 tests); isinstance checks confirm InviteAlreadyConsumed and InviteExpired are distinct types (SC-4); concurrency test uses real threads + Barrier and verifies exactly-one-success; malformed-hash tests confirm fail-closed; programmatic regex grep gate replaces shell grep; full suite green; file under 350 LOC</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> create_user / mint_invite_token | Caller-supplied fields validated at storage boundary (email required, role restricted to admin/ff, uid forbidden); validation lives in _validate_user_fields per Codex MEDIUM concern |
| unhashed_token -> consume_and_create_user | Raw token from HTTP request (Phase 37); must never be persisted (verified programmatically) |
| flock window | Cross-process LOCK_EX on auth.json during consume; all mutation inside the window; data normalized via _normalize_v2 inside the lock to defend against pre-v2 reads (HIGH-severity review fix) |
| disk inode vs lock fd | After _atomic_write_unlocked swaps in a new inode, the held lock fd refers to the old inode; intentional and documented; subsequent flock holders re-open the new inode (OpenCode HIGH analysis) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Elevation of Privilege | consume_and_create_user — token replay (use twice) | mitigate | consumed=True flip inside LOCK_EX window; hmac.compare_digest checks stored hash; second consume finds consumed=True and raises InviteAlreadyConsumed; concurrency test (TestInviteConsumeConcurrency) verifies single-use under real contention |
| T-34-02-02 | Information Disclosure | token comparison timing oracle | mitigate | hmac.compare_digest (constant-time) replaces direct string equality; raw token never logged |
| T-34-02-03 | Information Disclosure | raw token persisted in auth.json | mitigate | Only 'sha256:<hex>' stored; raw token never written to any file; programmatic regex assertion in test_raw_token_verifies_against_stored_hash_and_is_not_persisted enforces this (not shell grep) |
| T-34-02-04 | Elevation of Privilege | expired invite accepted | mitigate | expires_at check inside flock window using datetime comparison; parsed and compared before marking consumed; raises InviteExpired (distinct from InviteAlreadyConsumed) |
| T-34-02-05 | Elevation of Privilege | disabled user still active | accept | disabled flag stored at Phase 34; enforcement happens at login layer (Phase 35); storage layer is a pure record |
| T-34-02-06 | Denial of Service | flock deadlock if save_auth called inside flock window | mitigate | consume_and_create_user uses _atomic_write_unlocked exclusively inside window; save_auth is NEVER called inside flock window; _read_auth_unlocked + _normalize_v2 used instead of load_auth |
| T-34-02-07 | Tampering | malformed stored hash (missing prefix, invalid hex, wrong algorithm) | mitigate | _verify_token returns False on any malformed input (prefix check + try/except around compare_digest); TestMalformedHash class verifies fail-closed behavior across 3 distinct malformations |
| T-34-02-08 | Tampering | consume on a v1 file (pre-migration) — KeyError on data['users'] / data['pending_invites'] | mitigate | _normalize_v2 called inside flock window before scanning invites; schema-version invariant assertion (assert data.get('schema_version', 0) >= 2) provides a clear tripwire if a future regression bypasses normalization |
| T-34-02-09 | Tampering | empty or missing auth.json inside flock window | mitigate | _read_auth_unlocked returns _default_auth_data() on empty/missing file; consume proceeds with empty pending_invites and raises InviteAlreadyConsumed (fail closed) |
| T-34-02-10 | Spoofing | caller-supplied uid in create_user / consume_and_create_user | mitigate | _validate_user_fields rejects fields containing 'uid' key; uid is generated server-side via uuid.uuid4().hex; test_create_user_rejects_caller_supplied_uid verifies |
| T-34-02-11 | Information Disclosure | caller-supplied invalid role (e.g., 'superuser') | mitigate | _validate_user_fields restricts role to {'admin', 'ff'}; raises ValueError on any other value; test_create_user_rejects_invalid_role verifies |
| T-34-02-12 | Information Disclosure | _verify_token re-exported and called by web/route code with attacker-controlled input | mitigate | _verify_token is module-private (leading underscore); NOT re-exported from __init__.py; acceptance criteria verifies `not hasattr(auth_store, '_verify_token')` |
</threat_model>

<verification>
.venv/bin/pytest tests/test_auth_store_users.py -x --tb=short -q
.venv/bin/pytest -x --tb=short -q
python -c "from auth_store import create_user, mint_invite_token, consume_and_create_user, get_user, list_users, set_user_disabled, InviteAlreadyConsumed, InviteExpired; assert issubclass(InviteAlreadyConsumed, ValueError); assert issubclass(InviteExpired, ValueError); print('RBAC-03 + RBAC-04 + SC-4 surface present')"
python -c "import auth_store; assert not hasattr(auth_store, '_verify_token'); assert not hasattr(auth_store, '_read_auth_unlocked'); print('module-private not leaked')"
python -c "import auth_store; import auth_store._users as u; import fcntl, hmac; print('flock and hmac imports OK')"
grep -q '_normalize_v2(' auth_store/_users.py && echo "normalize_v2 inside _users OK"
grep -q 'def _read_auth_unlocked' auth_store/_users.py && echo "rename to _read_auth_unlocked OK"
grep -q 'threading.Barrier' tests/test_auth_store_users.py && echo "real concurrency test present"
</verification>

<success_criteria>
- auth_store/_users.py exists, under 250 LOC, stdlib-only imports
- InviteAlreadyConsumed(ValueError) and InviteExpired(ValueError) defined and re-exported from auth_store (D-11)
- _verify_token and _read_auth_unlocked are module-private — NOT re-exported from __init__.py
- consume_and_create_user raises InviteAlreadyConsumed (not plain ValueError) for missing/consumed token
- consume_and_create_user raises InviteExpired (not plain ValueError) for past-expires_at token; distinct type per SC-4
- consume_and_create_user holds LOCK_EX; calls _read_auth_unlocked + _normalize_v2 inside the window; uses _atomic_write_unlocked (not save_auth)
- consume_and_create_user asserts schema_version >= 2 inside the window (tripwire for migration-bypass regressions)
- consume_and_create_user docstring documents inode-vs-os.replace flock semantics
- _validate_user_fields enforces email required, role in {admin, ff}, no caller-supplied uid
- _verify_token fails closed on malformed inputs (missing prefix, invalid hex, wrong algorithm)
- mint_invite_token stores 'sha256:<hex>' prefix-tagged hash; returns (raw_token, expires_at_iso); ISO expiry is ~7 days out
- create_user: uid=uuid4().hex, no flock, rejects caller-supplied uid
- set_user_disabled: flips flag, row retained; reversible; returns False without writing on unknown uid
- tests/test_auth_store_users.py: 5 test classes (~19 tests), all pass
- TestInviteConsumeConcurrency uses real threading.Barrier with two threads; exactly one succeeds per token
- TestMalformedHash covers missing prefix, invalid hex, wrong algorithm — all fail closed
- Programmatic regex assertion replaces shell-grep / fragile len==43 raw-token-leak gate
- isinstance assertions confirm typed exceptions (SC-4)
- Full suite green (2100+ tests)
- RBAC-03 and RBAC-04 are testably delivered
</success_criteria>

<output>
After completion, create /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/34-user-registry-invite-token-storage/34-02-SUMMARY.md
</output>
