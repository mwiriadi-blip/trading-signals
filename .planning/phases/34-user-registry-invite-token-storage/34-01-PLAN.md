---
phase: "34"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - auth_store/__init__.py
  - auth_store/_schema.py
  - auth_store/_io.py
  - auth_store/_devices.py
  - auth_store/_magic_links.py
  - tests/test_auth_store.py
autonomous: true
requirements:
  - RBAC-03
  - RBAC-04

must_haves:
  truths:
    - "auth_store/ package exists; all callers import from auth_store unchanged (zero import-churn)"
    - "auth_store.DEFAULT_AUTH_PATH is importable from auth_store (not auth_store._io); all 70+ monkeypatches still work"
    - "DEFAULT_AUTH_PATH is defined in auth_store/__init__.py BEFORE any import of ._io (no circular-import fragility)"
    - "auth_store/_io.py imports DEFAULT_AUTH_PATH lazily inside _resolve_path() (function body, not top-level)"
    - "load_auth() on a v1 file returns a dict with users=[] and pending_invites=[] with schema_version=2; v1 file is immediately upgraded on disk"
    - "load_auth() on a v2 file with users=[] does NOT auto-insert any user (D-07)"
    - "load_auth() on an empty/corrupt file preserves existing quarantine behavior (no regression)"
    - "load_auth() docstring explicitly warns: MUST NOT be called inside a LOCK_EX window (use _read_auth_unlocked from _users.py instead)"
    - "_atomic_write_unlocked exists in auth_store/_io.py with a comment 'no flock — caller is responsible'"
    - "TestForbiddenImports in tests/test_auth_store.py walks auth_store/ package dir (not auth_store.py file)"
    - "Full test suite green after split; auth_store.py flat file deleted as final step"
  artifacts:
    - path: "auth_store/__init__.py"
      provides: "DEFAULT_AUTH_PATH defined first; then public API re-exports from daughter modules"
      contains: "DEFAULT_AUTH_PATH"
    - path: "auth_store/_io.py"
      provides: "_atomic_write, _atomic_write_unlocked, load_auth, save_auth, _default_auth_data, _resolve_path, _quarantine_corrupt_auth_file, v1->v2 migration via _normalize_v2"
    - path: "auth_store/_schema.py"
      provides: "AuthData, TrustedDevice, PendingMagicLink, User, PendingInvite TypedDicts, SCHEMA_VERSION, _ensure_aware, _normalize_v2"
    - path: "auth_store/_devices.py"
      provides: "add_trusted_device, revoke_device, revoke_all_other_devices, get_trusted_device, update_last_seen, is_uuid_active"
    - path: "auth_store/_magic_links.py"
      provides: "add_magic_link, consume_magic_link, count_recent_magic_links, purge_expired_magic_links"
  key_links:
    - from: "auth_store/__init__.py top of file"
      to: "DEFAULT_AUTH_PATH constant"
      via: "DEFAULT_AUTH_PATH = Path('auth.json') BEFORE any 'from auth_store._io import ...' line"
      pattern: "^DEFAULT_AUTH_PATH"
    - from: "auth_store/_io.py::_resolve_path"
      to: "auth_store.DEFAULT_AUTH_PATH"
      via: "lazy import inside _resolve_path body: from auth_store import DEFAULT_AUTH_PATH"
      pattern: "from auth_store import DEFAULT_AUTH_PATH"
    - from: "tests/test_auth_store.py::TestForbiddenImports"
      to: "auth_store/ package"
      via: "Path('auth_store').glob('*.py') walk replaces Path('auth_store.py').read_text()"
      pattern: "AUTH_STORE_PACKAGE"
---

<objective>
Convert auth_store.py (520-LOC monolith) to auth_store/ package; add User + PendingInvite TypedDicts and AuthData v2 extension to _schema.py; implement v1->v2 migration in load_auth() via a shared _normalize_v2 helper; resolve circular-import risk via explicit init-time ordering plus lazy resolve-path import; update TestForbiddenImports to walk the package directory.

Purpose: All downstream Phase 34 work (invite tokens, user registry) depends on the package structure being in place and the schema being v2. This plan delivers the structure and migration; Plan 02 delivers the user helpers and reuses _normalize_v2 inside the flock window. Three independent cross-AI reviews flagged circular-import ordering and migration-bypass-via-_load_raw as the highest-priority concerns — this revision pins both.

Output: auth_store/ package (5 daughter files), v1->v2 migration live in load_auth() via _normalize_v2 (shared with Plan 02), updated TestForbiddenImports, full suite green, auth_store.py deleted.
</objective>

<execution_context>
@/Users/marcwiriadisastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcwiriadisastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/PROJECT.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/ROADMAP.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/STATE.md
@/Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/34-user-registry-invite-token-storage/34-REVIEWS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from auth_store.py and state_manager/io.py. -->
<!-- Read these source files in full before touching anything. -->

From auth_store.py (PUBLIC API — all must be re-exported from auth_store/__init__.py):
  DEFAULT_AUTH_PATH: Path = Path('auth.json')
  SCHEMA_VERSION: int = 1          # bumped to 2 in _schema.py
  _ensure_aware(dt: datetime) -> datetime
  _atomic_write(data: str, path: Path) -> None
  _resolve_path(path: Path | None) -> Path
  _quarantine_corrupt_auth_file(path: Path) -> None    # MUST be preserved verbatim
  load_auth(path: Path | None = None) -> AuthData
  save_auth(data: AuthData, path: Path | None = None) -> None
  get_totp_secret(path: Path | None = None) -> str | None
  set_totp_secret(secret: str, path: Path | None = None) -> None
  mark_enrolled(path: Path | None = None) -> None
  add_trusted_device(label: str, path: Path | None = None) -> str
  revoke_device(uuid_value: str, path: Path | None = None) -> None
  revoke_all_other_devices(except_uuid: str, path: Path | None = None) -> int
  get_trusted_device(uuid_value: str, path: Path | None = None) -> TrustedDevice | None
  update_last_seen(uuid_value: str, path: Path | None = None) -> None
  is_uuid_active(uuid_value: str, path: Path | None = None) -> bool
  add_magic_link(token_hash, action, expires_at, email, path) -> None
  consume_magic_link(unhashed_token: str, path: Path | None = None) -> tuple[bool, str | None]
  count_recent_magic_links(within_seconds: int = 86400, path: Path | None = None) -> int
  purge_expired_magic_links(retention_seconds: int = 604800, path: Path | None = None) -> int
  AuthData (TypedDict), TrustedDevice (TypedDict), PendingMagicLink (TypedDict)

From state_manager/io.py — flock pattern to mirror in Plan 02:
  _atomic_write_unlocked(data: str, path: Path) -> None   # NO lock; pure tempfile+fsync+os.replace
  _atomic_write(data: str, path: Path) -> None             # wraps unlocked with LOCK_EX

From tests/test_auth_store.py line 27:
  AUTH_STORE_PATH = Path('auth_store.py')   # MUST become AUTH_STORE_PACKAGE = Path('auth_store')
  class TestForbiddenImports at line 587    # walks AUTH_STORE_PATH — must walk package
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create auth_store/ package skeleton with schema definitions and shared _normalize_v2 helper</name>
  <files>auth_store/__init__.py, auth_store/_schema.py, auth_store/_io.py, auth_store/_devices.py, auth_store/_magic_links.py</files>
  <read_first>
    - auth_store.py (full file — source of all logic to split; read entirely before touching, including corrupt-file quarantine path)
    - state_manager/io.py (lines 118-200 — _atomic_write_unlocked and _atomic_write patterns to mirror in _io.py)
    - .planning/phases/34-user-registry-invite-token-storage/34-REVIEWS.md (consensus concerns: circular import, migration-bypass, corruption-recovery preservation)
    - CLAUDE.md (2-space indent; 500 LOC cap; stdlib only; ruff format FORBIDDEN)
  </read_first>
  <behavior>
    - auth_store/_schema.py contains: SCHEMA_VERSION = 2 (per D-09), TrustedDevice TypedDict, PendingMagicLink TypedDict, AuthData TypedDict (v2 extension with users: list[User] and pending_invites: list[PendingInvite]), User TypedDict (uid, email, role, created_at, disabled per D-05), PendingInvite TypedDict (token_hash, email, invited_by, created_at, expires_at, consumed, consumed_at per D-08), _ensure_aware helper function, _normalize_v2(data: dict) -> dict helper that defensively backfills users=[], pending_invites=[], sets schema_version=2, returns the same dict object — pure function with NO disk I/O (shared with Plan 02's flock window)
    - auth_store/_io.py contains: all load/save/atomic-write logic moved verbatim including _quarantine_corrupt_auth_file behavior; _default_auth_data() updated to return schema_version=2, users=[], pending_invites=[]; load_auth() gains v1->v2 migration block AFTER corrupt-file handling preserves the original quarantine path — see action for explicit migration logic; load_auth() docstring includes a WARNING block stating it MUST NOT be called inside a LOCK_EX window (will deadlock); imports SCHEMA_VERSION, TypedDicts, _normalize_v2 from auth_store._schema; _resolve_path does a LAZY import of DEFAULT_AUTH_PATH inside the function body, NOT at module top level (defends against circular init); _atomic_write_unlocked is a NEW addition (copy from state_manager/io.py verbatim — required by Plan 02's flock path) with an in-file comment '# No flock — caller is responsible for serialization'
    - auth_store/_devices.py contains: all trusted-device helpers moved verbatim
    - auth_store/_magic_links.py contains: all magic-link helpers moved verbatim
    - auth_store/__init__.py: TOP OF FILE defines DEFAULT_AUTH_PATH = Path('auth.json') BEFORE any 'from auth_store._io import ...' line — this ordering is mandatory because _io.py's _resolve_path performs a lazy 'from auth_store import DEFAULT_AUTH_PATH' which requires the constant to be bound first; then re-exports ALL currently-public symbols via explicit from-imports from each daughter module; also re-exports SCHEMA_VERSION, User, PendingInvite, AuthData, TrustedDevice, PendingMagicLink from _schema
  </behavior>
  <action>
    Create auth_store/ directory with five files. Move logic verbatim (no rewrites) except for the changes below.

    File ordering rule (CRITICAL — three independent reviews flagged circular-import fragility): auth_store/__init__.py MUST be structured as follows in this order:
      1. from pathlib import Path
      2. DEFAULT_AUTH_PATH = Path('auth.json')   # defined BEFORE any daughter-module import
      3. from auth_store._schema import SCHEMA_VERSION, User, PendingInvite, AuthData, TrustedDevice, PendingMagicLink, _ensure_aware, _normalize_v2
      4. from auth_store._io import load_auth, save_auth, _atomic_write, _atomic_write_unlocked, _resolve_path, _quarantine_corrupt_auth_file, _default_auth_data
      5. from auth_store._devices import add_trusted_device, revoke_device, revoke_all_other_devices, get_trusted_device, update_last_seen, is_uuid_active
      6. from auth_store._magic_links import add_magic_link, consume_magic_link, count_recent_magic_links, purge_expired_magic_links

    In auth_store/_io.py: _resolve_path MUST use a lazy import. The function body opens with: from auth_store import DEFAULT_AUTH_PATH. Do NOT place that import at the top of _io.py. This is the agreed mitigation from all three reviewers — top-level reciprocal imports between __init__.py and _io.py break under partial initialization in some import paths.

    In auth_store/_schema.py: add _normalize_v2(data: dict) -> dict. The function:
      - If data.get('schema_version') == 1: data.setdefault('users', []); data.setdefault('pending_invites', []); data['schema_version'] = 2; return data
      - If data.get('schema_version') == 2: data.setdefault('users', []); data.setdefault('pending_invites', []); return data  (defensive backfill in case a v2 file is missing keys)
      - If data.get('schema_version') is missing or unknown (not 1 or 2): emit a logging.warning('[Auth] unknown schema_version=%r in auth.json; treating as v2', data.get('schema_version')); data.setdefault('users', []); data.setdefault('pending_invites', []); data['schema_version'] = 2; return data
      - This is a pure in-memory transform — no disk I/O — so Plan 02 can call it inside its LOCK_EX window without recursion.

    In auth_store/_io.py::load_auth(): after the corrupt-file recovery branch (which must be preserved verbatim — DO NOT change quarantine behavior), AFTER parsing payload from JSON:
      - was_v1 = payload.get('schema_version') == 1
      - payload = _normalize_v2(payload)
      - if was_v1: save_auth(payload, path=path)   # immediate disk migration per D-09 — only triggered for true v1, not for defensive backfill on v2

    The save_auth invocation MUST happen AFTER _normalize_v2 has produced fully-valid v2 data (with all required keys present), so save_auth's downstream validators see a well-formed v2 payload.

    Add a docstring at the top of load_auth():
      """Load auth.json from disk and return AuthData. Migrates v1 -> v2 on first read.

      WARNING: This function performs disk I/O via save_auth() during migration and
      cannot safely be called inside a LOCK_EX window — save_auth() acquires LOCK_EX,
      which will deadlock against an already-held lock. Callers inside a flock window
      must use _read_auth_unlocked + _normalize_v2 directly (see auth_store._users).
      """

    DEFAULT_AUTH_PATH must be defined in auth_store/__init__.py at the top — see ordering rule above. _resolve_path imports it back from auth_store via lazy import. This keeps all 70+ monkeypatch.setattr(auth_store, 'DEFAULT_AUTH_PATH', ...) calls working.

    _default_auth_data() in _io.py: return dict must use SCHEMA_VERSION (imported from _schema), and include users=[] and pending_invites=[] as new keys.

    _atomic_write_unlocked in _io.py: above the function body, add the comment line: # No flock — caller is responsible for serialization (see auth_store._users.consume_and_create_user for the canonical flock window).

    Do NOT run ruff format. Use 2-space indent throughout.

    Do NOT delete auth_store.py in this task. Deletion is Task 2 only, after all tests pass.
  </action>
  <acceptance_criteria>
    - auth_store/__init__.py first non-import line is `DEFAULT_AUTH_PATH = Path('auth.json')` (grep `^DEFAULT_AUTH_PATH = Path` returns line within the first 5 non-comment lines)
    - auth_store/__init__.py contains `from auth_store._io import` AFTER the DEFAULT_AUTH_PATH definition (verify by line-number ordering)
    - auth_store/_io.py top of file has NO `from auth_store import DEFAULT_AUTH_PATH` line; the import is inside `_resolve_path` function body
    - `grep -n "from auth_store import DEFAULT_AUTH_PATH" auth_store/_io.py` shows a line whose preceding `def ` line is `def _resolve_path`
    - auth_store/_schema.py defines `def _normalize_v2(data: dict) -> dict:` as a top-level function
    - auth_store/_schema.py contains `SCHEMA_VERSION = 2`
    - auth_store/_io.py::load_auth has a docstring containing the substring "MUST NOT be called inside a LOCK_EX window" (or equivalent — verify with `grep -A 6 'def load_auth' auth_store/_io.py | grep -i 'LOCK_EX'`)
    - auth_store/_io.py::load_auth calls `_normalize_v2(payload)` after JSON parse and before return
    - `save_auth(payload, path=path)` inside load_auth is gated on `was_v1` (only fires for true v1 migration, not for every read of a v2 file with defensive backfill)
    - auth_store/_io.py contains the comment `# No flock — caller is responsible` immediately preceding `def _atomic_write_unlocked` (or within its docstring)
    - Python import smoke: `python -c "import auth_store; from auth_store import DEFAULT_AUTH_PATH, SCHEMA_VERSION, load_auth, save_auth, _atomic_write_unlocked, _normalize_v2; assert SCHEMA_VERSION == 2"` exits 0
    - All 5 daughter files exist and are under 500 LOC each
    - auth_store.py flat file is STILL PRESENT after Task 1 (deletion belongs to Task 2)
    - .venv/bin/pytest tests/test_auth_store.py -x --tb=short -q exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && python -c "import auth_store; from auth_store import DEFAULT_AUTH_PATH, SCHEMA_VERSION, load_auth, save_auth, _atomic_write_unlocked, _normalize_v2; assert SCHEMA_VERSION == 2; print('imports OK')" && grep -n '^DEFAULT_AUTH_PATH' auth_store/__init__.py && grep -B1 'from auth_store import DEFAULT_AUTH_PATH' auth_store/_io.py | grep -v '^#' | grep 'def _resolve_path' && .venv/bin/pytest tests/test_auth_store.py -x --tb=short -q</automated>
  </verify>
  <done>auth_store/ package importable; DEFAULT_AUTH_PATH and SCHEMA_VERSION accessible from auth_store namespace; _normalize_v2 exported; load_auth docstring warns about flock deadlock; lazy import inside _resolve_path verified by grep; full test_auth_store.py passes; auth_store.py flat file still present (deletion is Task 2's gate)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration + corruption regression tests; update TestForbiddenImports; delete auth_store.py</name>
  <files>tests/test_auth_store.py, auth_store.py (DELETE)</files>
  <read_first>
    - tests/test_auth_store.py (full file — locate TestForbiddenImports around line 587 and the existing corrupt-file tests if any; line 27 AUTH_STORE_PATH = Path('auth_store.py') to change)
    - tests/conftest.py (isolated_auth_json fixture — needed for migration tests)
    - auth_store/__init__.py (verify package is green before deleting flat file)
    - auth_store/_io.py (load_auth migration block — tests must exercise it)
    - .planning/phases/34-user-registry-invite-token-storage/34-REVIEWS.md (concerns: corrupt-file regression, v1->v2 field preservation, file-count assertion >=5 not ==5)
  </read_first>
  <behavior>
    - tests/test_auth_store.py at line 27: replace `AUTH_STORE_PATH = Path('auth_store.py')` with `AUTH_STORE_PACKAGE = Path('auth_store')`
    - TestForbiddenImports walks the package: for each .py file in AUTH_STORE_PACKAGE.glob('*.py'), parse with ast.parse and collect Import/ImportFrom nodes whose root module is in FORBIDDEN_ROOTS; assert violations == []; assert `len(list(AUTH_STORE_PACKAGE.glob('*.py'))) >= 5` (NOT ==5 — Plan 02 will add _users.py, so == would break)
    - New test class TestSchemaMigrationV1ToV2 in tests/test_auth_store.py (NOT in tests/test_auth_store_users.py — those tests live in Plan 02). Tests:
      - test_v1_to_v2_migration_backfills_users_and_invites: write a minimal v1 auth.json (schema_version=1, no users/pending_invites keys, totp_secret=None, totp_enrolled=False, totp_enrolled_at=None, trusted_devices=[], pending_magic_links=[]); call load_auth(); assert result['schema_version'] == 2 and result['users'] == [] and result['pending_invites'] == []; re-read the file from disk via json.loads and assert disk also has schema_version=2 (immediate migration per D-09)
      - test_v1_to_v2_migration_preserves_trusted_devices: v1 file with one trusted_device entry; after load_auth, the trusted_devices list is identical (same uuid, label, last_seen)
      - test_v1_to_v2_migration_preserves_pending_magic_links: v1 file with one pending_magic_link entry; after load_auth, the list is identical
      - test_v1_to_v2_migration_preserves_totp_fields: v1 file with totp_secret='S123', totp_enrolled=True, totp_enrolled_at=<ISO>; after migration, all three fields are unchanged
      - test_v2_file_does_not_re_migrate_or_re_save: write a v2 file with users=[{'uid': 'abc', ...}]; record mtime; load_auth(); re-stat mtime — must be unchanged (no spurious save when schema is already v2 — `was_v1` gate prevents extra writes)
      - test_v2_file_with_missing_users_key_is_defensively_backfilled: write a v2 file missing the users key (corrupted/partial); load_auth() returns users=[] but does NOT call save_auth (no mtime bump) — defensive backfill is in-memory only
    - New test class TestLoadAuthCorruptionRecovery in tests/test_auth_store.py (only add if not already present — check existing tests first):
      - test_load_auth_on_empty_file_quarantines: write zero-byte auth.json; call load_auth(); assert returned data == _default_auth_data() OR quarantine file was created (whichever matches the pre-split behavior — preserve it verbatim, do NOT change it)
      - test_load_auth_on_invalid_json_quarantines: write 'not-json{{{' to auth.json; call load_auth(); same pre-split behavior preserved
      - These two tests are REGRESSION GUARDS — if the corruption-recovery path already has tests in test_auth_store.py, this requirement is satisfied; do NOT duplicate
    - Final step: delete auth_store.py
  </behavior>
  <action>
    Step 1: In tests/test_auth_store.py at line 27, change AUTH_STORE_PATH = Path('auth_store.py') to AUTH_STORE_PACKAGE = Path('auth_store').

    Step 2: Update TestForbiddenImports.test_auth_store_does_not_import_web_or_signal_layers (or equivalent test name — read existing test first). Replace the single-file read pattern with a package-walk pattern. For each .py file in AUTH_STORE_PACKAGE.glob('*.py'): read text, parse with ast.parse, walk nodes looking for Import/ImportFrom nodes whose root is in FORBIDDEN_ROOTS. Collect violations with filename prefix (f'{py_file}:{node.lineno}: ...'). Assert violations == []. Also assert `len(list(AUTH_STORE_PACKAGE.glob('*.py'))) >= 5` — use >= NOT == because Plan 02 will add _users.py, and a strict == would force a fragile follow-up edit.

    Step 3: Add TestSchemaMigrationV1ToV2 class to tests/test_auth_store.py. Use the existing isolated_auth_json fixture (it monkeypatches auth_store.DEFAULT_AUTH_PATH to a tmp path). Each test writes its setup via json.dumps directly to the tmp path (the migration is the function under test, so do NOT use save_auth in setup), then calls auth_store.load_auth().

    Step 4: Before TestSchemaMigrationV1ToV2, scan the existing test_auth_store.py for any test that covers empty-file or invalid-JSON quarantine. If absent, add TestLoadAuthCorruptionRecovery with the two regression tests in the behavior block — these protect the existing corruption-recovery semantics through the package split. If present (likely — auth_store.py is shipped), reference the existing test names in a comment and skip adding duplicates.

    Step 5: Run the full test suite. Once green, delete auth_store.py.

    Do NOT run ruff format. 2-space indent.
  </action>
  <acceptance_criteria>
    - Line 27 of tests/test_auth_store.py contains `AUTH_STORE_PACKAGE = Path('auth_store')` (verify with `sed -n '27p' tests/test_auth_store.py`)
    - tests/test_auth_store.py contains the substring `len(list(AUTH_STORE_PACKAGE.glob('*.py'))) >= 5` (NOT `== 5`)
    - tests/test_auth_store.py contains a `class TestSchemaMigrationV1ToV2` definition with at least 6 test methods covering: backfill users+invites, preserve trusted_devices, preserve pending_magic_links, preserve totp_fields, v2-file-no-resave, v2-defensive-backfill-no-resave
    - tests/test_auth_store.py either contains a `class TestLoadAuthCorruptionRecovery` OR an inline comment referencing the existing test names that cover empty-file and invalid-JSON quarantine (executor decides based on current contents)
    - auth_store.py file does NOT exist after task completion: `test ! -f auth_store.py` succeeds
    - .venv/bin/pytest tests/test_auth_store.py::TestForbiddenImports -x --tb=short -q exits 0
    - .venv/bin/pytest tests/test_auth_store.py::TestSchemaMigrationV1ToV2 -x --tb=short -q exits 0
    - Full suite green: .venv/bin/pytest -x --tb=short -q exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals && test ! -f auth_store.py && grep -q 'AUTH_STORE_PACKAGE = Path' tests/test_auth_store.py && grep -q "glob('\*.py'))) >= 5" tests/test_auth_store.py && grep -q 'class TestSchemaMigrationV1ToV2' tests/test_auth_store.py && .venv/bin/pytest tests/test_auth_store.py::TestForbiddenImports tests/test_auth_store.py::TestSchemaMigrationV1ToV2 -x --tb=short -q && .venv/bin/pytest -x --tb=short -q</automated>
  </verify>
  <done>auth_store.py flat file absent; TestForbiddenImports passes walking the auth_store/ package; >=5 file assertion in place (forward-compatible with Plan 02's _users.py); TestSchemaMigrationV1ToV2 passes including trusted_devices/pending_magic_links/totp preservation and the no-resave-on-v2 guard; corruption-recovery semantics preserved (regression tests in place or existing tests still pass); full suite green (2084+ tests)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> load_auth | JSON payload from auth.json parsed into Python dict; corrupt/missing file handled |
| v1 file -> v2 migration | load_auth() mutates in-memory dict via _normalize_v2 and calls save_auth() exactly once on first read of v1 |
| package init -> daughter modules | Circular-import ordering enforced: DEFAULT_AUTH_PATH bound BEFORE _io import; _io reverses the import lazily inside _resolve_path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | auth_store/_io.py load_auth v1->v2 migration | mitigate | Migration writes via save_auth() -> _atomic_write() (tempfile+fsync+os.replace), not in-place overwrite; corrupt-file quarantine path preserved verbatim |
| T-34-01-02 | Information Disclosure | auth_store/_io.py — DEFAULT_AUTH_PATH in __init__ | accept | DEFAULT_AUTH_PATH is a filesystem path config; not a secret; low-value target |
| T-34-01-03 | Denial of Service | load_auth v1->v2 migration loop: recursive call if save_auth calls load_auth | mitigate | save_auth is a pure write (no reads); load_auth migration only calls save_auth, not load_auth — no recursion possible per design; v2 files are gated by `was_v1` flag so they never trigger a second save |
| T-34-01-04 | Denial of Service | Circular import between auth_store/__init__.py and auth_store/_io.py at package load | mitigate | DEFAULT_AUTH_PATH defined at top of __init__.py BEFORE any daughter-module import; _io.py's reverse import is lazy (inside _resolve_path function body, not top-level); verified by grep gate in acceptance criteria |
| T-34-01-05 | Tampering | corrupt or empty auth.json bypassing migration | mitigate | corruption-recovery path preserved verbatim from auth_store.py; regression test class TestLoadAuthCorruptionRecovery guards behavior through the split (or existing tests verified to cover it) |
</threat_model>

<verification>
.venv/bin/pytest tests/test_auth_store.py -x --tb=short -q
.venv/bin/pytest -x --tb=short -q
python -c "from auth_store import load_auth, save_auth, DEFAULT_AUTH_PATH, SCHEMA_VERSION, User, PendingInvite, AuthData, add_trusted_device, consume_magic_link, _normalize_v2; print('imports OK')"
python -c "from auth_store import _atomic_write_unlocked; print('unlocked write present')"
grep -n '^DEFAULT_AUTH_PATH' auth_store/__init__.py
grep -B1 'from auth_store import DEFAULT_AUTH_PATH' auth_store/_io.py | grep 'def _resolve_path'
test ! -f auth_store.py
</verification>

<success_criteria>
- auth_store/ package exists with 5 files all under 500 LOC
- DEFAULT_AUTH_PATH importable from auth_store (package __init__); all monkeypatch.setattr(auth_store, 'DEFAULT_AUTH_PATH', ...) calls still work
- DEFAULT_AUTH_PATH defined at top of __init__.py BEFORE any daughter-module import (verified by grep)
- _resolve_path uses lazy import (function-body, not top-level) — verified by grep
- SCHEMA_VERSION == 2 in auth_store._schema
- _normalize_v2 is a pure in-memory helper exported from auth_store; reusable inside Plan 02's flock window
- _default_auth_data() returns dict with users=[], pending_invites=[], schema_version=2
- load_auth() on v1 file: upgrades to v2 in memory AND saves v2 to disk immediately; returns dict with users=[] and pending_invites=[]
- load_auth() on v2 file: does NOT re-save (verified by mtime test)
- load_auth() on v2 file with users=[]: does NOT insert any admin row (side-effect-free per D-07)
- load_auth() on empty/corrupt file: preserves pre-split quarantine semantics (regression-tested)
- load_auth() docstring contains LOCK_EX warning
- _atomic_write_unlocked present in auth_store._io with "no flock" comment (required by Plan 02)
- auth_store.py flat file deleted (final step of Task 2)
- TestForbiddenImports walks auth_store/ dir; asserts >= 5 .py files (forward-compatible with Plan 02)
- TestSchemaMigrationV1ToV2 covers field preservation (trusted_devices, pending_magic_links, totp_*) and the no-resave-on-v2 guard
- Full suite green
</success_criteria>

<output>
After completion, create /Users/marcwiriadisastra/Documents/Work/Apps/trading-signals/.planning/phases/34-user-registry-invite-token-storage/34-01-SUMMARY.md
</output>
