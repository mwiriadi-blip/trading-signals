---
phase: 10
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - main.py
  - tests/test_main.py
autonomous: true
requirements: [INFRA-02]
requirements_addressed: [INFRA-02]
tags: [deploy-key, git-push, subprocess, never-crash, orchestrator, infra]

must_haves:
  truths:
    - "main.py defines _push_state_to_git(state, now) helper that commits + pushes state.json via subprocess+git"
    - "run_daily_check() calls _push_state_to_git(state, run_date) AFTER save_state completes (insertion point after _render_dashboard_never_crash, before the footer return)"
    - "_push_state_to_git docstring explicitly notes the C-2 local-import-subprocess rationale (mirrors _send_email_never_crash pattern)"
    - "When `git diff --quiet state.json` returns rc=0, helper logs [State] skip message and returns without committing"
    - "When git diff returns rc=1, helper runs `git -c user.email=droplet@trading-signals -c user.name='DO Droplet' commit -m 'chore(state): daily signal update [skip ci]' state.json` then `git push origin main`"
    - "When commit subprocess raises CalledProcessError, helper logs [State] git commit failed; when push subprocess raises CalledProcessError, helper logs [State] git push failed (failure-verb distinction per REVIEW LOW)"
    - "All subprocess failure branches call state_manager.append_warning(source='state_pusher'), return without crashing"
    - "Helper does NOT call save_state a third time — preserves Phase 8 W3 two-saves-per-run invariant"
    - "--test and weekend-skip paths never reach _push_state_to_git (insertion is after save_state which is after both guards); verified by TWO new regression tests per REVIEW MEDIUM"
    - "Wiring tests exercise run_daily_check() directly (not main.main([...])) per REVIEW MEDIUM; one smoke test retained for the CLI dispatch path"
  artifacts:
    - path: main.py
      provides: "_push_state_to_git helper (~55 LOC) + 1-line call in run_daily_check"
      contains: "def _push_state_to_git"
    - path: tests/test_main.py
      provides: "TestPushStateToGit class (6 tests) + TestRunDailyCheckPushesState class (4 tests — 2 positive wiring + 2 skip-gate verifications)"
      contains: "class TestPushStateToGit"
  key_links:
    - from: main.py::run_daily_check
      to: main.py::_push_state_to_git
      via: "direct function call after save_state + dashboard render"
      pattern: "_push_state_to_git\\(state, run_date\\)"
    - from: main.py::_push_state_to_git
      to: state_manager.append_warning
      via: "D-12 fail-loud failure path"
      pattern: "state_manager\\.append_warning.*source=.state_pusher."
    - from: tests/test_main.py::TestPushStateToGit
      to: main.py::_push_state_to_git
      via: "monkeypatched subprocess.run with spy + mock CalledProcessError"
      pattern: "monkeypatch\\.setattr\\('subprocess\\.run'"
    - from: tests/test_main.py::TestRunDailyCheckPushesState
      to: main.py::run_daily_check
      via: "direct invocation with argparse.Namespace — no CLI surface coupling"
      pattern: "main\\.run_daily_check\\(args\\)"
---

<objective>
Close INFRA-02: add `_push_state_to_git(state, now)` orchestrator helper in
`main.py` (D-07) that commits + pushes `state.json` via stdlib subprocess+git,
wire a 1-line call at the end of `run_daily_check()` after `save_state`
(D-08), implement skip-if-unchanged gate via `git diff --quiet` (D-09),
inline identity flags per D-10, verbatim commit message per D-11, and
fail-loud warning path via `state_manager.append_warning` per D-12.

**REVIEW REVISIONS (from 10-REVIEWS.md):**
- **[MEDIUM]** Added `test_run_daily_check_does_not_push_on_weekend` and
  `test_run_daily_check_does_not_push_on_test_mode` per Codex MEDIUM —
  D-15 previously lacked explicit weekend/--test skip coverage.
- **[MEDIUM]** Wiring tests (TestRunDailyCheckPushesState) now exercise
  `main.run_daily_check(args)` directly instead of `main.main([...])`.
  One smoke test retained for the CLI dispatch path. Reduces side-effect
  coupling per Codex MEDIUM.
- **[LOW]** Failure log messages now distinguish commit-vs-push failure
  via separate except clauses (`[State] git commit failed` vs `[State]
  git push failed`). Debugging is no longer misleading per Codex LOW.
- **[LOW]** `_push_state_to_git` docstring now includes a one-line
  rationale for the local `import subprocess` pattern (mirrors
  `_send_email_never_crash`; keeps module-level import surface lean).
  Option A adopted per REVIEW LOW. Grep-asserted.

Purpose: v1.1 inverts the v1.0 GHA-primary deployment. The droplet is now
the runtime; GitHub needs to remain the source-of-truth for state history
even after GHA cron retires (Plan 04). The helper gives the droplet a
deploy-key–authenticated write path back to `origin/main`.

Output: main.py + ~55 LOC new helper + 1-line call; tests/test_main.py with
TestPushStateToGit (6 tests covering D-07..D-12 including commit-vs-push
log distinction) + TestRunDailyCheckPushesState (4 tests covering D-08
wiring + D-15 weekend-skip + D-15 --test skip + 1 CLI smoke). Hex-lite
boundary preserved (state_manager stays I/O-narrow; subprocess lives in
main.py orchestrator only).

Depends on Plan 01 to avoid test-file merge conflicts — both plans append
to tests/test_main.py (TestHandleReset from Plan 01 and TestPushStateToGit
from Plan 03). Plan 02 is independent (different test file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-RESEARCH.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-PATTERNS.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-REVIEWS.md
@main.py
@state_manager.py
@tests/test_main.py
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase. Executor should NOT re-explore. -->

From main.py (analog `_send_email_never_crash` at lines 136–184):
```python
def _send_email_never_crash(
  state: dict,
  old_signals: dict,
  run_date: datetime,
  is_test: bool = False,
) -> 'object':
  '''D-15 + NOTF-07/NOTF-08 + Phase 8 D-08 consumer bridge.'''
  try:
    import notifier  # local import — C-2 isolates import-time failures
    return notifier.send_daily_email(state, old_signals, run_date, is_test=is_test)
  except Exception as e:
    logger.warning('[Email] send failed: %s: %s', type(e).__name__, e)
    ...
```

From main.py::run_daily_check structure (verified lines 754-1080):
- Line 829: weekday gate — `if run_date.weekday() >= WEEKDAY_SKIP_THRESHOLD: return 0, None, None, run_date`
- Line 1046: --test gate — `if args.test: return 0, state, old_signals, run_date` BEFORE save_state
- Line 1058: `state_manager.save_state(state)` — only reached on non-weekend, non-test
- Line 1071: `_render_dashboard_never_crash(state, Path('dashboard.html'), run_date)`
- INSERT `_push_state_to_git(state, run_date)` BETWEEN line 1071 and line 1072
  (after dashboard render, before elapsed_total calc + footer + return)

This placement GUARANTEES --test + weekend never reach the helper —
structural property from the `run_daily_check` early returns.

From main.py::run_daily_check insertion site (lines 1058–1080):
```python
# Step 9: atomic save_state + success footer.
state_manager.save_state(state)
logger.info(
  '[State] state.json saved (account=$%.2f, trades=%d, positions=%d)',
  state['account'],
  len(state['trade_log']),
  sum(1 for p in state['positions'].values() if p is not None),
)
# Step 9.5 (Phase 5 D-06): render dashboard.html; never crash on failure.
_render_dashboard_never_crash(state, Path('dashboard.html'), run_date)
elapsed_total = time.perf_counter() - run_start_monotonic
_format_run_summary_footer(...)
return 0, state, old_signals, run_date
```

From state_manager.py (append_warning signature, line 423):
```python
def append_warning(state: dict, source: str, message: str, now=None) -> dict:
```

Git diff exit codes [VERIFIED: man git-diff]:
- rc=0 → no diff (skip push)
- rc=1 → diff present (continue to commit+push)
- rc>=128 → error (not-a-repo, permissions, etc.) → fail-loud path

Existing `state_manager` is imported at module top in main.py (already
available for `append_warning` call inside the helper).

From tests/test_main.py (verified existing test helpers at lines 57-90):
- `_seed_fresh_state(state_json)` (line 65): writes reset_state() to path
- `_make_args(**overrides)` (line 72): argparse.Namespace builder with
  defaults `test=False, reset=False, force_email=False, once=True`
- `_install_fixture_fetch(monkeypatch)` (line 82): monkeypatches
  main.data_fetcher.fetch_ohlcv to return canonical yfinance fixtures
- `datetime` and `ZoneInfo` are NOT imported at module top — tests import
  them locally per function body (pattern: `from datetime import datetime`)

Project uses `pytest.mark.freeze_time('ISO')` for clock determinism
(pytest-freezer is pinned in Wave 0). Used extensively in TestCLI /
TestOrchestrator — see line 104 `@pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')`
for Monday 08:00 AWST.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _push_state_to_git helper + wire into run_daily_check (commit-vs-push log distinction + local-import rationale per REVIEW LOW)</name>
  <files>main.py</files>
  <read_first>
    - main.py (read lines 1–50 for imports, 119–185 for never-crash wrapper analog, 1050–1085 for run_daily_check insertion site)
    - state_manager.py (read lines 420–455 for append_warning signature)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md (§Area 3 D-07..D-15)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-PATTERNS.md (§main.py::_push_state_to_git + §run_daily_check hook)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-RESEARCH.md (§Pattern 1 + §Architecture diagram lines 140–183)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-REVIEWS.md (§Codex 10-03 LOW concerns — log distinction + local import rationale)
    - CLAUDE.md (confirm [State] log prefix, 2-space indent, single quotes, hex-lite rule)
  </read_first>
  <behavior>
    - When state.json has no diff: helper returns without calling commit/push; logs '[State] state.json unchanged — skipping git push' at INFO
    - When state.json has a diff: helper runs commit then push; both succeed; helper returns normally
    - When COMMIT subprocess raises CalledProcessError: helper logs at ERROR with '[State] git commit failed' + stderr excerpt, calls state_manager.append_warning with source='state_pusher', returns WITHOUT re-raising
    - When PUSH subprocess raises CalledProcessError: helper logs at ERROR with '[State] git push failed' + stderr excerpt, calls state_manager.append_warning with source='state_pusher', returns WITHOUT re-raising
    - **REVIEW LOW fix:** commit-vs-push failures produce distinct log lines so debugging is unambiguous
    - When git diff returns rc >= 128 (repo error): helper logs at ERROR with '[State] git diff failed', appends warning, returns
    - When subprocess raises TimeoutExpired: helper logs at ERROR with '[State] git subprocess timeout', appends warning, returns
    - When ANY unexpected exception: caught, logged at ERROR, warning appended, return
    - Helper does NOT call save_state a second/third time — the warning persists via next run's normal save cycle per D-12
    - Helper uses LOCAL `import subprocess` inside the try block (C-2 pattern) — docstring explains rationale (REVIEW LOW Option A)
    - Commit argv uses inline `-c user.email=droplet@trading-signals -c user.name=DO Droplet` flags (does NOT mutate `.git/config`)
    - Commit argv includes EXPLICIT 'state.json' positional arg (never `-A` or `.`) — scope guarantee
    - Commit message literal is exactly `chore(state): daily signal update [skip ci]` (verbatim from v1.0 Phase 7 per D-11)
    - In run_daily_check, the call `_push_state_to_git(state, run_date)` sits AFTER `save_state` and after `_render_dashboard_never_crash`, BEFORE the final return
  </behavior>
  <action>
    **Step A — Add `_push_state_to_git` helper in main.py.**

    Placement: immediately AFTER `_send_email_never_crash` (which ends around
    line 184) and BEFORE the next function definition. Insert a blank line
    separator, then the helper. Use 2-space indent, single quotes, `[State]`
    log prefix.

    **Structural change vs. earlier draft:** The commit and push subprocess
    calls each sit inside their OWN try/except so the logger verb
    ("commit" vs "push") distinguishes the failing subcommand. This
    closes the Codex LOW concern in 10-REVIEWS.md about misleading
    '[State] git push failed' messages covering commit failures.

    Exact body:

    ```python
    def _push_state_to_git(state: dict, now: datetime) -> None:
      '''Phase 10 INFRA-02 / D-07..D-12 — nightly state.json commit + push via
      deploy-key–authenticated git remote. Never crashes the daily run.

      Uses local `import subprocess` — mirrors the `_send_email_never_crash`
      pattern to keep module-level import surface lean. Although subprocess
      is stdlib (import failure is implausible), the local-import idiom is
      the project convention for never-crash wrappers and keeps the AST
      blocklist grep-auditable (REVIEW-LOW Option A; see 10-REVIEWS.md).

      Architecture:
        [save_state completed] -> [_push_state_to_git]
          -> `git diff --quiet state.json`
              rc=0 -> log [State] skip + return
              rc=1 -> continue to commit+push
              rc>=128 -> fail-loud path
          -> `git -c user.email=droplet@trading-signals
                  -c user.name=DO Droplet
                  commit -m 'chore(state): daily signal update [skip ci]'
                  state.json`   (check=True)
          -> `git push origin main`   (check=True)

      D-07: lives in main.py (not state_manager) — preserves hex-lite
        boundary; state_manager stays I/O-narrow (disk only, no subprocess).
      D-09: skip-if-unchanged via git diff --quiet exit codes.
      D-10: inline -c flags (do NOT mutate .git/config).
      D-11: commit message verbatim from v1.0 Phase 7 convention.
      D-12: fail-loud — log ERROR + append_warning(source='state_pusher').
        Does NOT call save_state a third time (preserves Phase 8 W3
        two-saves-per-run invariant); warning persists via next run's
        normal save cycle.
      D-15: --test and weekend-skip never reach this helper (caller
        returns before save_state on both paths). Structurally guaranteed
        by the placement AFTER save_state in run_daily_check; verified
        by TestRunDailyCheckPushesState weekend + test-mode tests.

      COMMIT-vs-PUSH LOG DISTINCTION (REVIEW LOW): commit and push each
      have their OWN try/except clause so the emitted log verb names the
      failing subcommand unambiguously. Previously both paths shared an
      '[State] git push failed' message, which misled debugging.
      '''
      try:
        import subprocess  # local — C-2 pattern; see docstring rationale
        # D-09 three-way rc branch on `git diff --quiet state.json`.
        diff_result = subprocess.run(
          ['git', 'diff', '--quiet', 'state.json'],
          capture_output=True,
          timeout=30,
        )
        diff_rc = diff_result.returncode
        if diff_rc == 0:
          logger.info('[State] state.json unchanged — skipping git push')
          return
        if diff_rc >= 128:
          stderr_excerpt = (diff_result.stderr or b'').decode('utf-8', errors='replace')[:200]
          logger.error('[State] git diff failed (rc=%d): %s', diff_rc, stderr_excerpt)
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'git diff failed rc={diff_rc}: {stderr_excerpt}',
            now=now,
          )
          return
      except subprocess.TimeoutExpired as e:
        logger.error('[State] git diff subprocess timeout: %s', e)
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly git diff timed out: {e}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
        return
      except Exception as e:
        logger.error('[State] git diff unexpected error: %s: %s',
                     type(e).__name__, e)
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly git diff error: {type(e).__name__}: {e}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
        return

      # D-10 inline identity flags; D-11 verbatim commit message;
      # explicit 'state.json' positional arg scopes the commit precisely.
      # OWN try/except so commit failures log '[State] git commit failed'
      # — distinct from push failures (REVIEW LOW).
      try:
        subprocess.run(
          [
            'git',
            '-c', 'user.email=droplet@trading-signals',
            '-c', 'user.name=DO Droplet',
            'commit',
            '-m', 'chore(state): daily signal update [skip ci]',
            'state.json',
          ],
          check=True,
          capture_output=True,
          timeout=30,
        )
      except subprocess.CalledProcessError as e:
        stderr_excerpt = (e.stderr or b'').decode('utf-8', errors='replace')[:200]
        logger.error(
          '[State] git commit failed (rc=%d): %s',
          e.returncode, stderr_excerpt,
        )
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly state.json commit failed: rc={e.returncode} stderr={stderr_excerpt}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
        return
      except subprocess.TimeoutExpired as e:
        logger.error('[State] git commit subprocess timeout: %s', e)
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly state.json commit timed out: {e}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
        return
      except Exception as e:
        logger.error('[State] git commit unexpected error: %s: %s',
                     type(e).__name__, e)
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly state.json commit error: {type(e).__name__}: {e}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
        return

      # D-13 no auto-rebase retry; fail-loud on push.
      # OWN try/except so push failures log '[State] git push failed'
      # — distinct from commit failures (REVIEW LOW).
      try:
        subprocess.run(
          ['git', 'push', 'origin', 'main'],
          check=True,
          capture_output=True,
          timeout=60,
        )
        logger.info('[State] state.json pushed to origin/main')
      except subprocess.CalledProcessError as e:
        stderr_excerpt = (e.stderr or b'').decode('utf-8', errors='replace')[:200]
        logger.error(
          '[State] git push failed (rc=%d): %s',
          e.returncode, stderr_excerpt,
        )
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly state.json push failed: rc={e.returncode} stderr={stderr_excerpt}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
      except subprocess.TimeoutExpired as e:
        logger.error('[State] git push subprocess timeout: %s', e)
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly state.json push timed out: {e}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
      except Exception as e:
        logger.error('[State] git push unexpected error: %s: %s',
                     type(e).__name__, e)
        try:
          state_manager.append_warning(
            state,
            source='state_pusher',
            message=f'Nightly state.json push error: {type(e).__name__}: {e}',
            now=now,
          )
        except Exception as append_err:
          logger.error('[State] append_warning also failed: %s: %s',
                       type(append_err).__name__, append_err)
    ```

    **Subprocess module attribute references:** The `except
    subprocess.CalledProcessError` and `except subprocess.TimeoutExpired`
    handlers reference `subprocess.*` at EXCEPT-clause eval time. The
    diff block has its own try/except (and imports subprocess on its
    first line); the commit and push blocks run AFTER the diff block
    returns cleanly, so `subprocess` remains bound in the function's
    local namespace when their except clauses evaluate. Python's
    exception clause resolves `subprocess` from the function's local
    namespace populated by the `import` statement. This pattern works
    because the diff `try:` runs before commit/push.

    **IF the executor finds the subprocess reference fails** at Python
    parse/import time, fallback is: add `import subprocess` at MODULE
    TOP (not local), keeping the hex boundary argument intact since
    subprocess is stdlib and the AST blocklist at
    tests/test_signal_engine.py:544 ALLOWS subprocess in main.py. This
    is a Rule-3 deviation if encountered — document in SUMMARY.

    **Step B — Wire the call at the end of `run_daily_check`.**

    Locate run_daily_check in main.py. The insertion point is AFTER
    `_render_dashboard_never_crash(state, Path('dashboard.html'),
    run_date)` (line ~1071) and BEFORE `elapsed_total = time.perf_counter() -
    run_start_monotonic` (line ~1072). Insert this two-line block:

    ```python
    # Phase 10 INFRA-02 / D-08: push state.json to origin/main via deploy
    # key; never crashes on failure.
    _push_state_to_git(state, run_date)
    ```

    Placement constraint: must be AFTER save_state and dashboard render
    (so pushed content reflects the full run) and BEFORE the footer +
    return. Do NOT add this call inside the `--test` branch earlier in
    the function (which returns at line ~1055 before save_state) nor
    inside the weekend-skip branch (which returns at line ~834 before
    load_state). The single insertion point after _render_dashboard_never_crash
    is STRUCTURALLY after both early-return gates.

    **Step C — No module-top import changes needed.** `subprocess` stays
    local (per the helper body). `state_manager` is already imported at
    module top. `datetime` is already imported for the `now: datetime`
    type hint. `logger` already exists.

    **Step D — Smoke verify the helper loads:**
    ```bash
    python -c "from main import _push_state_to_git; print('OK:', _push_state_to_git.__doc__[:60])"
    # Expected: OK: Phase 10 INFRA-02 / D-07..D-12 — nightly state.json commit + pu
    ```
  </action>
  <verify>
    <automated>python -c "from main import _push_state_to_git; assert callable(_push_state_to_git)" && ruff check main.py && pytest tests/test_signal_engine.py::TestDeterminism -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def _push_state_to_git" main.py` (helper defined)
    - `grep -q "Phase 10 INFRA-02" main.py` (traceability docstring)
    - `grep -q "mirrors the \`_send_email_never_crash\` pattern" main.py` (REVIEW LOW Option A — local-import rationale in docstring)
    - `grep -q "REVIEW-LOW Option A" main.py` (explicit trace to review decision)
    - `grep -q "git diff --quiet" main.py` (D-09 skip-if-unchanged gate)
    - `grep -q "user.email=droplet@trading-signals" main.py` (D-10 inline identity — email; no space — confirms Gemini LOW concern already addressed)
    - `grep -q "user.name=DO Droplet" main.py` (D-10 inline identity — name)
    - `grep -q "chore(state): daily signal update \[skip ci\]" main.py` (D-11 commit message verbatim)
    - `grep -qE "'commit',.*'state.json'" main.py` (T-10-03 explicit scope: state.json positional arg)
    - `grep -q "source='state_pusher'" main.py` (D-12 warning source tag)
    - `grep -q "_push_state_to_git(state, run_date)" main.py` (D-08 wire-up call site in run_daily_check)
    - `grep -q "\[State\] git commit failed" main.py` (REVIEW LOW — commit-vs-push log distinction)
    - `grep -q "\[State\] git push failed" main.py` (REVIEW LOW — commit-vs-push log distinction)
    - `grep -q "\[State\] git diff failed" main.py` (diff failure log)
    - `grep -c "save_state(state)" main.py` returns same count as pre-plan (NO extra save_state call added by this plan — two-saves-per-run invariant preserved)
    - `grep -q "check=True" main.py` in the _push_state_to_git body (for commit+push subprocess calls)
    - `ruff check main.py` clean (exit 0)
    - `pytest tests/test_signal_engine.py::TestDeterminism::test_forbidden_imports_absent -q` passes (AST blocklist permits subprocess in main.py)
    - `python -c "from main import _push_state_to_git; assert callable(_push_state_to_git)"` succeeds
    - `grep -cE "^def " main.py` returns pre-plan count + 1 (exactly ONE new top-level function added)
  </acceptance_criteria>
  <done>
    _push_state_to_git helper added to main.py with D-07..D-13 + D-15 semantics;
    run_daily_check wired to call it after save_state + dashboard render;
    subprocess calls use list-form (no shell=True), inline identity flags,
    explicit state.json scope, verbatim commit message; commit-vs-push log
    distinction per REVIEW LOW; local-import rationale documented in
    docstring per REVIEW LOW Option A; fail-loud path via append_warning
    preserves two-saves-per-run invariant. Ruff clean; AST blocklist
    honored.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestPushStateToGit + TestRunDailyCheckPushesState (adds weekend/--test skip coverage + direct run_daily_check wiring per REVIEW MEDIUM)</name>
  <files>tests/test_main.py</files>
  <read_first>
    - tests/test_main.py (read lines 1–95 for helpers `_seed_fresh_state`, `_make_args`, `_install_fixture_fetch`; lines 1030–1100 for TestEmailNeverCrash analog; lines 1545–1580 for TestCrashEmailBoundary wrapper-spy pattern)
    - main.py (confirm Task 1 landed; read the new _push_state_to_git function to pattern-match argv shapes in tests; confirm line 829 weekday gate and line 1046 --test gate early-return before save_state)
    - state_manager.py (read append_warning at line 423 to confirm signature for mock spy)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md (§Area 3 D-07..D-15)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-PATTERNS.md (§TestPushStateToGit — full analog breakdown)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-VALIDATION.md (§Wave 0 Requirements for test class list)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-REVIEWS.md (§Codex 10-03 MEDIUM — weekend/--test skip + run_daily_check direct coupling)
  </read_first>
  <behavior>
    - Test: D-09 skip-if-unchanged — when subprocess.run returns rc=0 for `git diff --quiet`, helper returns without calling subsequent subprocess.run (assert only ONE call recorded; caplog contains '[State] state.json unchanged')
    - Test: D-08/D-10/D-11 happy-path — when git diff rc=1, helper invokes subprocess.run three times with expected argv for diff / commit / push; commit argv contains '-c user.email=droplet@trading-signals', '-c user.name=DO Droplet', 'chore(state): daily signal update [skip ci]', and 'state.json'
    - Test: D-12 PUSH-failure — when the `git push` subprocess raises CalledProcessError(returncode=128, stderr=b'fatal: Authentication failed'), helper logs at ERROR with '[State] git push failed' (NOT '[State] git commit failed') AND calls state_manager.append_warning with source='state_pusher'; does NOT raise; does NOT call save_state
    - Test: D-12 COMMIT-failure (REVIEW LOW) — when the `git commit` subprocess raises CalledProcessError(returncode=1), helper logs at ERROR with '[State] git commit failed' (NOT '[State] git push failed'); asserts log-verb distinction
    - Test: D-12 diff-error — when `git diff` returns rc=128, helper logs at ERROR with '[State] git diff failed', appends warning, returns
    - Test: two-saves-per-run — spy on state_manager.save_state; after helper runs (in any scenario), save_state call count has NOT incremented relative to pre-helper call
    - TestRunDailyCheckPushesState Test 1 (POSITIVE wiring, direct run_daily_check per REVIEW MEDIUM): invoke `main.run_daily_check(args)` with args.test=False on a Monday (freeze_time); assert _push_state_to_git spy received (state, run_date) exactly once
    - TestRunDailyCheckPushesState Test 2 (D-15 --test skip — NEW per REVIEW MEDIUM): invoke `main.run_daily_check(args)` with args.test=True; assert _push_state_to_git spy received zero calls
    - TestRunDailyCheckPushesState Test 3 (D-15 weekend skip — NEW per REVIEW MEDIUM): invoke `main.run_daily_check(args)` with freeze_time pinned to a Saturday; assert _push_state_to_git spy received zero calls
    - TestRunDailyCheckPushesState Test 4 (CLI smoke — retained per REVIEW MEDIUM): invoke `main.main(['--force-email'])`; assert spy received one call. Verifies CLI dispatch still reaches the helper, but is labelled as a smoke test so the bulk of coverage is via direct run_daily_check calls.
  </behavior>
  <action>
    **Append two new classes to the END of tests/test_main.py** (after
    TestHandleReset from Plan 01; confirm Plan 01 landed first per the
    depends_on graph). Use 2-space indent, single quotes.

    ```python


    # =========================================================================
    # Phase 10 INFRA-02 / D-07..D-12 — TestPushStateToGit regression class
    # =========================================================================

    class _FakeCompletedProcess:
      '''Minimal stand-in for subprocess.CompletedProcess — only fields
      _push_state_to_git reads.'''

      def __init__(self, returncode: int = 0, stderr: bytes = b'',
                   stdout: bytes = b''):
        self.returncode = returncode
        self.stderr = stderr
        self.stdout = stdout


    class TestPushStateToGit:
      '''Phase 10 INFRA-02 / D-07..D-12: nightly state.json git push never
      crashes. Covers skip-if-unchanged (D-09), commit+push happy path
      (D-08/D-10/D-11), push-failure fail-loud (D-12), commit-failure
      fail-loud (REVIEW LOW log-verb distinction), diff-error fail-loud,
      two-saves-per-run invariant preserved (Phase 8 W3).
      '''

      def test_skip_if_unchanged_logs_and_returns(
          self, monkeypatch, caplog) -> None:
        '''D-09: git diff --quiet rc=0 -> log [State] skip + return; no commit/push.'''
        import logging
        import subprocess
        from datetime import datetime
        from zoneinfo import ZoneInfo
        calls: list = []

        def _fake_run(argv, **kwargs):
          calls.append((list(argv), kwargs))
          # git diff --quiet state.json -> rc=0 (no diff)
          if argv[:3] == ['git', 'diff', '--quiet']:
            return _FakeCompletedProcess(returncode=0)
          raise AssertionError(
            f'skip path must not invoke subprocess beyond git diff; got {argv}'
          )

        monkeypatch.setattr(subprocess, 'run', _fake_run)
        caplog.set_level(logging.INFO)
        state = {'account': 1.0, 'warnings': []}
        now = datetime(2026, 4, 28, 8, 0, tzinfo=ZoneInfo('Australia/Perth'))
        main._push_state_to_git(state, now)
        assert len(calls) == 1, f'expected 1 subprocess call (diff), got {len(calls)}'
        assert '[State] state.json unchanged' in caplog.text

      def test_happy_path_commits_with_inline_identity_and_pushes(
          self, monkeypatch, caplog) -> None:
        '''D-08/D-10/D-11: rc=1 on diff -> commit with inline -c flags + verbatim message, then push.'''
        import logging
        import subprocess
        from datetime import datetime
        from zoneinfo import ZoneInfo
        calls: list = []

        def _fake_run(argv, **kwargs):
          calls.append((list(argv), kwargs))
          if argv[:3] == ['git', 'diff', '--quiet']:
            return _FakeCompletedProcess(returncode=1)  # diff present
          if 'commit' in argv or 'push' in argv:
            return _FakeCompletedProcess(returncode=0)
          raise AssertionError(f'unexpected argv: {argv}')

        monkeypatch.setattr(subprocess, 'run', _fake_run)
        caplog.set_level(logging.INFO)
        state = {'account': 1.0, 'warnings': []}
        now = datetime(2026, 4, 28, 8, 0, tzinfo=ZoneInfo('Australia/Perth'))
        main._push_state_to_git(state, now)
        assert len(calls) == 3, (
          f'expected 3 subprocess calls (diff+commit+push), got {len(calls)}'
        )
        diff_argv, commit_argv, push_argv = [c[0] for c in calls]
        assert diff_argv == ['git', 'diff', '--quiet', 'state.json']
        # D-10 inline identity flags present BEFORE the commit subcommand
        assert '-c' in commit_argv and 'user.email=droplet@trading-signals' in commit_argv
        assert '-c' in commit_argv and 'user.name=DO Droplet' in commit_argv
        # D-11 commit message verbatim
        assert 'chore(state): daily signal update [skip ci]' in commit_argv
        # T-10-03 explicit scope: state.json as positional arg
        assert 'state.json' in commit_argv
        assert commit_argv.index('commit') < commit_argv.index('state.json'), (
          'state.json must appear AFTER `commit` subcommand as the path arg'
        )
        # Push goes to origin/main
        assert push_argv == ['git', 'push', 'origin', 'main']
        assert '[State] state.json pushed' in caplog.text

      def test_push_failure_logs_error_and_appends_warning(
          self, monkeypatch, caplog) -> None:
        '''D-12: CalledProcessError on push -> logger.error with '[State] git
        push failed' + append_warning + no crash. Verifies REVIEW LOW
        log-verb distinction: message says "push failed", NOT "commit failed".
        '''
        import logging
        import subprocess
        from datetime import datetime
        from zoneinfo import ZoneInfo
        calls: list = []
        warnings_captured: list = []

        def _fake_run(argv, **kwargs):
          calls.append((list(argv), kwargs))
          if argv[:3] == ['git', 'diff', '--quiet']:
            return _FakeCompletedProcess(returncode=1)
          if 'commit' in argv:
            return _FakeCompletedProcess(returncode=0)
          if 'push' in argv:
            raise subprocess.CalledProcessError(
              returncode=128,
              cmd=list(argv),
              stderr=b'fatal: Authentication failed (deploy key missing)',
            )
          raise AssertionError(f'unexpected argv: {argv}')

        def _fake_append_warning(state, source, message, now=None):
          warnings_captured.append({
            'source': source, 'message': message, 'now': now,
          })
          return state

        monkeypatch.setattr(subprocess, 'run', _fake_run)
        monkeypatch.setattr(
          'main.state_manager.append_warning', _fake_append_warning,
        )
        caplog.set_level(logging.ERROR)
        state = {'account': 1.0, 'warnings': []}
        now = datetime(2026, 4, 28, 8, 0, tzinfo=ZoneInfo('Australia/Perth'))
        # MUST NOT raise
        main._push_state_to_git(state, now)
        # REVIEW LOW: log verb must be "push failed", not "commit failed"
        assert '[State] git push failed' in caplog.text
        assert '[State] git commit failed' not in caplog.text, (
          "REVIEW LOW regression: push failure logged as 'git commit failed' — "
          "commit-vs-push log distinction broken"
        )
        assert 'Authentication failed' in caplog.text
        assert len(warnings_captured) == 1
        assert warnings_captured[0]['source'] == 'state_pusher'
        assert 'push failed' in warnings_captured[0]['message'].lower() or \
               'rc=128' in warnings_captured[0]['message']
        assert warnings_captured[0]['now'] == now

      def test_commit_failure_logs_error_and_appends_warning(
          self, monkeypatch, caplog) -> None:
        '''REVIEW LOW (10-REVIEWS.md): CalledProcessError on commit -> logger
        must emit '[State] git commit failed' (not 'push failed'); warning
        message distinguishes commit failure.
        '''
        import logging
        import subprocess
        from datetime import datetime
        from zoneinfo import ZoneInfo
        calls: list = []
        warnings_captured: list = []

        def _fake_run(argv, **kwargs):
          calls.append((list(argv), kwargs))
          if argv[:3] == ['git', 'diff', '--quiet']:
            return _FakeCompletedProcess(returncode=1)
          if 'commit' in argv:
            raise subprocess.CalledProcessError(
              returncode=1,
              cmd=list(argv),
              stderr=b'error: pathspec state.json did not match',
            )
          raise AssertionError(
            f'commit-failure path must not reach push; got {argv}'
          )

        def _fake_append_warning(state, source, message, now=None):
          warnings_captured.append({
            'source': source, 'message': message, 'now': now,
          })
          return state

        monkeypatch.setattr(subprocess, 'run', _fake_run)
        monkeypatch.setattr(
          'main.state_manager.append_warning', _fake_append_warning,
        )
        caplog.set_level(logging.ERROR)
        state = {'account': 1.0, 'warnings': []}
        now = datetime(2026, 4, 28, 8, 0, tzinfo=ZoneInfo('Australia/Perth'))
        main._push_state_to_git(state, now)
        # REVIEW LOW: log verb must be "commit failed", not "push failed"
        assert '[State] git commit failed' in caplog.text
        assert '[State] git push failed' not in caplog.text, (
          "REVIEW LOW regression: commit failure logged as 'git push failed' — "
          "commit-vs-push log distinction broken"
        )
        assert len(warnings_captured) == 1
        assert warnings_captured[0]['source'] == 'state_pusher'
        assert 'commit' in warnings_captured[0]['message'].lower()
        # commit failure path must NOT attempt push
        push_attempts = [c for c in calls if 'push' in c[0]]
        assert push_attempts == [], (
          'commit failure must short-circuit BEFORE push subprocess call'
        )

      def test_diff_error_appends_warning_without_pushing(
          self, monkeypatch, caplog) -> None:
        '''D-12 edge: git diff rc=128 (repo error) -> log + warn + return.'''
        import logging
        import subprocess
        from datetime import datetime
        from zoneinfo import ZoneInfo
        calls: list = []
        warnings_captured: list = []

        def _fake_run(argv, **kwargs):
          calls.append((list(argv), kwargs))
          if argv[:3] == ['git', 'diff', '--quiet']:
            return _FakeCompletedProcess(
              returncode=128,
              stderr=b'fatal: not a git repository',
            )
          raise AssertionError(f'must not call subprocess past diff; got {argv}')

        def _fake_append_warning(state, source, message, now=None):
          warnings_captured.append({'source': source, 'message': message})
          return state

        monkeypatch.setattr(subprocess, 'run', _fake_run)
        monkeypatch.setattr(
          'main.state_manager.append_warning', _fake_append_warning,
        )
        caplog.set_level(logging.ERROR)
        state = {'account': 1.0, 'warnings': []}
        now = datetime(2026, 4, 28, 8, 0, tzinfo=ZoneInfo('Australia/Perth'))
        main._push_state_to_git(state, now)
        assert len(calls) == 1, 'must not call subprocess past diff on rc>=128'
        assert '[State] git diff failed' in caplog.text
        assert len(warnings_captured) == 1
        assert warnings_captured[0]['source'] == 'state_pusher'

      def test_never_calls_save_state_on_push_failure(
          self, monkeypatch) -> None:
        '''Phase 8 W3: two-saves-per-run invariant preserved; _push_state_to_git
        must NOT call save_state even on failure.'''
        import subprocess
        from datetime import datetime
        from zoneinfo import ZoneInfo
        save_calls: list = []

        def _fake_run(argv, **kwargs):
          if argv[:3] == ['git', 'diff', '--quiet']:
            return _FakeCompletedProcess(returncode=1)
          if 'commit' in argv:
            return _FakeCompletedProcess(returncode=0)
          if 'push' in argv:
            raise subprocess.CalledProcessError(
              returncode=1, cmd=list(argv), stderr=b'network error',
            )
          raise AssertionError(f'unexpected argv: {argv}')

        def _fake_save(*args, **kwargs):
          save_calls.append((args, kwargs))

        monkeypatch.setattr(subprocess, 'run', _fake_run)
        monkeypatch.setattr('main.state_manager.save_state', _fake_save)
        monkeypatch.setattr(
          'main.state_manager.append_warning',
          lambda state, source, message, now=None: state,
        )
        state = {'account': 1.0, 'warnings': []}
        now = datetime(2026, 4, 28, 8, 0, tzinfo=ZoneInfo('Australia/Perth'))
        main._push_state_to_git(state, now)
        assert save_calls == [], (
          'Phase 8 W3 invariant: _push_state_to_git must not call save_state'
        )


    # =========================================================================
    # Phase 10 INFRA-02 / D-08 + D-15 — TestRunDailyCheckPushesState
    # =========================================================================
    # REVIEW MEDIUM (10-REVIEWS.md): direct run_daily_check invocation for
    # primary wiring (not main.main([...]) — avoids email + dashboard side
    # effects). One CLI smoke test retained to cover the dispatch path.
    # REVIEW MEDIUM: adds weekend + --test skip tests to close the D-15
    # coverage gap Codex flagged.

    class TestRunDailyCheckPushesState:
      '''D-08: run_daily_check invokes _push_state_to_git(state, run_date)
      AFTER save_state + dashboard render. D-15: not invoked on --test or
      weekend. Tests primarily exercise run_daily_check() directly per
      REVIEW MEDIUM (10-REVIEWS.md); one smoke test retained for CLI path.
      '''

      @pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST
      def test_run_daily_check_invokes_push_after_save(
          self, tmp_path, monkeypatch) -> None:
        '''D-08: normal non-test weekday run -> _push_state_to_git called
        with (state, run_date). Direct run_daily_check invocation
        (REVIEW MEDIUM: avoids main.main([...]) coupling to email dispatch).
        '''
        monkeypatch.chdir(tmp_path)
        monkeypatch.setattr('main.logging.basicConfig', lambda **kw: None)
        _seed_fresh_state(tmp_path / 'state.json')
        _install_fixture_fetch(monkeypatch)
        calls: list = []

        def _spy_push(state, now):
          calls.append((dict(state), now))

        monkeypatch.setattr('main._push_state_to_git', _spy_push)
        args = _make_args(once=True)
        rc, state, old_signals, run_date = main.run_daily_check(args)
        assert rc == 0
        assert len(calls) == 1, (
          'D-08: _push_state_to_git must be invoked once per non-test weekday run'
        )
        pushed_state, pushed_now = calls[0]
        assert 'account' in pushed_state, 'state arg must carry run state'
        assert pushed_now == run_date, (
          'D-08: now arg must equal run_daily_check run_date'
        )

      @pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST
      def test_run_daily_check_does_not_push_on_test_mode(
          self, tmp_path, monkeypatch) -> None:
        '''D-15 (REVIEW MEDIUM — NEW): --test is structurally read-only;
        run_daily_check returns BEFORE save_state on --test, so
        _push_state_to_git must never be called. Spy on the helper and
        assert zero invocations.
        '''
        monkeypatch.chdir(tmp_path)
        monkeypatch.setattr('main.logging.basicConfig', lambda **kw: None)
        _seed_fresh_state(tmp_path / 'state.json')
        _install_fixture_fetch(monkeypatch)
        calls: list = []

        def _spy_push(state, now):
          calls.append((dict(state), now))

        monkeypatch.setattr('main._push_state_to_git', _spy_push)
        args = _make_args(test=True)
        rc, state, old_signals, run_date = main.run_daily_check(args)
        assert rc == 0
        assert calls == [], (
          'D-15: --test must NOT invoke _push_state_to_git '
          '(structural read-only — early return before save_state)'
        )

      @pytest.mark.freeze_time('2026-04-25T00:00:00+00:00')  # Sat 08:00 AWST
      def test_run_daily_check_does_not_push_on_weekend(
          self, tmp_path, monkeypatch) -> None:
        '''D-15 (REVIEW MEDIUM — NEW): weekend skip short-circuits
        run_daily_check at the weekday gate (line 829 in main.py).
        _push_state_to_git must not be called — return happens before
        load_state, fetch, or save_state. Spy on the helper and assert
        zero invocations.

        Freeze time on Saturday (weekday=5); run_date.weekday() >=
        WEEKDAY_SKIP_THRESHOLD (=5) triggers the early return with state=None.
        '''
        monkeypatch.chdir(tmp_path)
        monkeypatch.setattr('main.logging.basicConfig', lambda **kw: None)
        _seed_fresh_state(tmp_path / 'state.json')
        _install_fixture_fetch(monkeypatch)
        calls: list = []

        def _spy_push(state, now):
          calls.append((dict(state), now))

        monkeypatch.setattr('main._push_state_to_git', _spy_push)
        args = _make_args(once=True)
        rc, state, old_signals, run_date = main.run_daily_check(args)
        assert rc == 0
        assert state is None, (
          'weekend gate returns state=None per run_daily_check contract'
        )
        assert calls == [], (
          'D-15: weekend skip must NOT invoke _push_state_to_git '
          '(early return at weekday gate — never reaches save_state)'
        )

      @pytest.mark.freeze_time('2026-04-27T00:00:00+00:00')  # Mon 08:00 AWST
      def test_main_cli_dispatch_reaches_push_helper_smoke(
          self, tmp_path, monkeypatch) -> None:
        '''REVIEW MEDIUM smoke-only: verify the CLI dispatch path
        (main.main(['--force-email'])) still reaches _push_state_to_git.
        This is the ONE test that exercises the main.main([...]) surface;
        the primary wiring assertions sit on the direct run_daily_check
        tests above to avoid email + dashboard side-effect coupling.
        '''
        monkeypatch.chdir(tmp_path)
        monkeypatch.setattr('main.logging.basicConfig', lambda **kw: None)
        _seed_fresh_state(tmp_path / 'state.json')
        _install_fixture_fetch(monkeypatch)
        calls: list = []

        def _spy_push(state, now):
          calls.append((dict(state), now))

        # Stub email dispatch to keep the smoke test isolated from Resend.
        monkeypatch.setattr(
          'notifier._post_to_resend',
          lambda *a, **kw: {'ok': True, 'reason': None, 'attempts': 1},
        )
        monkeypatch.setattr('main._push_state_to_git', _spy_push)
        rc = main.main(['--force-email'])
        assert rc == 0
        assert len(calls) == 1, (
          'CLI smoke: main(["--force-email"]) dispatch must still reach '
          '_push_state_to_git exactly once'
        )
    ```

    **Notes for executor:**
    - Reuse existing helpers `_seed_fresh_state`, `_install_fixture_fetch`,
      `_make_args` from tests/test_main.py (already imported / defined at
      module top — lines 65, 72, 82)
    - `datetime` and `ZoneInfo` are imported LOCALLY per test body (matches
      the pattern at test_main.py lines 1109, 1766, 1781, etc.). Do NOT add
      module-top imports for these
    - `main` is already imported at module top in tests/test_main.py (line 37)
    - `pytest` is already imported at module top (line 34) — the
      `@pytest.mark.freeze_time(...)` decorators work directly
    - `subprocess` is imported INSIDE each test body (locally) — matches
      the project's local-import convention for ad-hoc test dependencies
    - `'main.state_manager.append_warning'` patches the state_manager module
      ref as accessed by main.py. state_manager is imported at module-top
      in main.py (confirmed); the dotted patch path works for this scope
    - `'main.state_manager.save_state'` same pattern
    - `'main._push_state_to_git'` patches the helper in main's namespace
      — use this (not the function object directly) so wiring tests that
      invoke `main.run_daily_check(args)` see the spy
    - Smoke test stubs `notifier._post_to_resend` to avoid network calls;
      uses dict-shape (not SendStatus) because notifier internally wraps;
      if this stub shape changes, adjust per test_main.py existing patterns
      (grep `_post_to_resend` in tests/test_main.py for analog)
    - Placement: append AFTER TestHandleReset (from Plan 01). Plan 01
      depends_on gate in frontmatter prevents out-of-order execution
  </action>
  <verify>
    <automated>pytest tests/test_main.py::TestPushStateToGit tests/test_main.py::TestRunDailyCheckPushesState -q && ruff check tests/test_main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class TestPushStateToGit" tests/test_main.py` (test class present)
    - `grep -q "class TestRunDailyCheckPushesState" tests/test_main.py` (integration class present)
    - `grep -q "test_skip_if_unchanged_logs_and_returns" tests/test_main.py` (D-09 test)
    - `grep -q "test_happy_path_commits_with_inline_identity_and_pushes" tests/test_main.py` (D-08/D-10/D-11 test)
    - `grep -q "test_push_failure_logs_error_and_appends_warning" tests/test_main.py` (D-12 push-failure test)
    - `grep -q "test_commit_failure_logs_error_and_appends_warning" tests/test_main.py` (REVIEW LOW — commit-vs-push log-verb distinction)
    - `grep -q "test_diff_error_appends_warning_without_pushing" tests/test_main.py` (D-12 edge test)
    - `grep -q "test_never_calls_save_state_on_push_failure" tests/test_main.py` (Phase 8 W3 invariant test)
    - `grep -q "test_run_daily_check_invokes_push_after_save" tests/test_main.py` (D-08 wiring test — direct run_daily_check)
    - `grep -q "test_run_daily_check_does_not_push_on_test_mode" tests/test_main.py` (REVIEW MEDIUM — D-15 --test skip — NEW)
    - `grep -q "test_run_daily_check_does_not_push_on_weekend" tests/test_main.py` (REVIEW MEDIUM — D-15 weekend skip — NEW)
    - `grep -q "test_main_cli_dispatch_reaches_push_helper_smoke" tests/test_main.py` (REVIEW MEDIUM — retained CLI smoke)
    - `grep -q "main.run_daily_check(args)" tests/test_main.py` (REVIEW MEDIUM — direct invocation replacing main.main([...]) coupling)
    - `pytest tests/test_main.py::TestPushStateToGit -q` passes (6 tests green)
    - `pytest tests/test_main.py::TestRunDailyCheckPushesState -q` passes (4 tests green)
    - `pytest tests/test_main.py::TestHandleReset tests/test_main.py::TestResetFlags -q` passes (Plan 01 regression confirmed not broken by Plan 03 test appendages)
    - `pytest -q` — full suite green (~30s for quick path; longer for full)
    - `ruff check tests/test_main.py` clean
  </acceptance_criteria>
  <done>
    TestPushStateToGit (6 tests: skip, happy-path, push-failure,
    commit-failure, diff-error, two-saves invariant) +
    TestRunDailyCheckPushesState (4 tests: D-08 wiring, D-15 --test skip,
    D-15 weekend skip, CLI smoke) appended to tests/test_main.py. Primary
    wiring uses `main.run_daily_check(args)` direct invocation per REVIEW
    MEDIUM; one smoke test retains the `main.main([...])` path. Commit-vs-
    push log-verb distinction asserted per REVIEW LOW. All pass. Full
    suite green; ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| droplet process → subprocess(git) | subprocess invocations with list-form argv, shell=False, capture_output=True, explicit timeouts |
| droplet process → github.com via SSH | Authenticated by operator-configured deploy key (setup doc in Plan 04); network failures caught by D-12 fail-loud path |
| state['warnings'] write path | ONLY via state_manager.append_warning (sole-writer invariant per Phase 8 D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | Deploy key private material | mitigate (Plan 04 operator doc; runtime-side: key never touched by this code) | Key stored at ~/.ssh/ with mode 0600; SETUP-DEPLOY-KEY.md (Plan 04) explicitly warns against committing; helper does NOT read or log key material — only invokes `git` which uses OS SSH. ASVS L1. |
| T-10-02 | Tampering (command injection) | subprocess argv construction in _push_state_to_git | mitigate | All argv is hardcoded list literal — no string concatenation, no shell=True, no user-supplied input. `user.email` and `user.name` values are hardcoded string literals in main.py (NOT from env vars, NOT from state). ASVS L1. |
| T-10-03 | Tampering (unintended commit scope) | `git commit state.json` positional arg | mitigate | Commit argv includes EXPLICIT `'state.json'` path positional arg; never `-A`, never `.`, never `-a`. Regression-asserted by `test_happy_path_commits_with_inline_identity_and_pushes` via `assert 'state.json' in commit_argv`. ASVS L1. |
| T-10-04 | Tampering (commit message injection) | `git commit -m '...'` message | mitigate | Commit message is a hardcoded string literal `'chore(state): daily signal update [skip ci]'` — NEVER templated with state contents or env vars. Grep-asserted. ASVS L1. |
| T-10-05 | Information Disclosure | stderr excerpt logged on failure | accept | stderr is truncated to 200 chars and logged at ERROR level. If GitHub ever included a token in stderr (unlikely — deploy keys don't produce token-bearing error messages), 200 chars is enough to leak it to systemd journal. Risk: low (deploy key auth, no tokens in flow). Watch post-deploy; if observed, redact further. |
| T-10-06 | DoS (subprocess hang) | subprocess.run timeouts | mitigate | Every subprocess.run call has explicit `timeout=` kwarg (30s for diff+commit, 60s for push). TimeoutExpired caught by dedicated except clauses per subcommand → append_warning + return. |
| T-10-07 | Sole-writer violation (state['warnings']) | _push_state_to_git failure path | mitigate | Failure path calls state_manager.append_warning (the sole-writer API from Phase 8 D-08); never mutates state['warnings'] directly. Grep-asserted: `grep -q "state_manager.append_warning" main.py`; NO direct `state['warnings'].append(` inside _push_state_to_git. |
| T-10-08 | Repudiation (misattributed commit-vs-push failure) | logger diagnostic output on failure | mitigate | REVIEW LOW closed: commit and push have independent except clauses so the logged verb names the failing subcommand. `test_push_failure_logs_error_and_appends_warning` and `test_commit_failure_logs_error_and_appends_warning` grep-assert the log-verb distinction, preventing regression. ASVS L1. |

No HIGH-severity unmitigated threats. ASVS L1 posture: compliant.
</threat_model>

<verification>
After all tasks:
1. `pytest tests/test_main.py::TestPushStateToGit -q` passes (6 tests)
2. `pytest tests/test_main.py::TestRunDailyCheckPushesState -q` passes (4 tests)
3. `pytest tests/test_main.py::TestHandleReset -q` passes (Plan 01, regression check)
4. `pytest tests/test_signal_engine.py::TestDeterminism -q` passes (AST blocklist honored; subprocess legal in main.py)
5. `pytest -q` — full suite green
6. `ruff check main.py tests/test_main.py` clean
7. `python -c "from main import _push_state_to_git; assert callable(_push_state_to_git)"` succeeds
8. Manual audit: `grep -c "save_state(state)" main.py` returns same count as pre-plan (two-saves-per-run preserved)
9. Manual audit: `grep -c "def " main.py` returns pre-plan count + 1 (exactly one new function)
10. REVIEW LOW check: `grep -q "\[State\] git commit failed" main.py && grep -q "\[State\] git push failed" main.py` — both log strings present
11. REVIEW LOW check: `grep -q "mirrors the \`_send_email_never_crash\` pattern" main.py` — local-import rationale in docstring
12. REVIEW MEDIUM check: `grep -q "test_run_daily_check_does_not_push_on_weekend" tests/test_main.py && grep -q "test_run_daily_check_does_not_push_on_test_mode" tests/test_main.py` — both new skip tests present
</verification>

<success_criteria>
- _push_state_to_git helper exists in main.py with D-07..D-13 + D-15 semantics
- Docstring documents the local-import rationale (REVIEW LOW Option A)
- Commit-vs-push failure paths emit distinct log verbs (REVIEW LOW)
- run_daily_check wired to call helper at correct insertion point (after save_state + dashboard render, before footer+return)
- Subprocess invocations: list-form argv, shell=False implicit, explicit timeouts, inline identity flags, verbatim commit message, explicit state.json scope
- Fail-loud path: CalledProcessError / TimeoutExpired / Exception caught per subcommand; ERROR log with correct [State] verb prefix; append_warning with source='state_pusher'; no crash
- Two-saves-per-run invariant (Phase 8 W3) preserved
- Hex-lite boundary preserved (state_manager has no subprocess imports)
- 10 new regression tests (6 in TestPushStateToGit + 4 in TestRunDailyCheckPushesState) all green
- REVIEW MEDIUM closed: weekend + --test skip coverage added; primary wiring uses direct run_daily_check() (one CLI smoke retained)
- Full `pytest -q` suite green
- ROADMAP Phase 10 SC-3 (automated portion) satisfied; live-GitHub verification is manual-only per 10-VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-03-SUMMARY.md` covering:
- main.py lines added (~55 for helper + 2 for wire-up = ~57)
- tests/test_main.py lines added (two new classes)
- Tests added (count: 10 = 6 TestPushStateToGit + 4 TestRunDailyCheckPushesState)
- Full suite test count delta (expect +10)
- REVIEW revisions applied (HIGH n/a here — was 10-02; MEDIUM = weekend/--test tests + direct run_daily_check; LOW = commit-vs-push log distinction + local-import docstring rationale)
- Any Rule-1/Rule-3 deviations (especially if subprocess had to move to module-top instead of local import)
- Ruff status
- Commit SHAs
</output>
