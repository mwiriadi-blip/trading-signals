---
phase: 10
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - .github/workflows/daily.yml
  - tests/test_scheduler.py
  - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [INFRA-03]
requirements_addressed: [INFRA-03]
tags: [infra, gha-retirement, operator-doc, prose-update, deploy-key]

must_haves:
  truths:
    - ".github/workflows/daily.yml no longer exists; .github/workflows/daily.yml.disabled exists (git mv preserves history)"
    - "tests/test_scheduler.py::TestGHAWorkflow.WORKFLOW_PATH constant equals '.github/workflows/daily.yml.disabled'"
    - "All 12 tests in TestGHAWorkflow pass against the renamed disabled file"
    - "SETUP-DEPLOY-KEY.md exists in the phase directory with ssh-keygen, GitHub deploy-key UI, ~/.ssh/config, remote switch, ssh -T verify, and `python main.py --once` first-run bootstrap steps"
    - "SETUP-DEPLOY-KEY.md references _push_state_to_git (the helper shipped in Plan 03) so operators understand what the first-run bootstrap exercises"
    - "SETUP-DEPLOY-KEY.md includes a 'docs/DEPLOY.md is stale' note (REVIEW LOW — 10-REVIEWS.md Codex MEDIUM; option (b)+(c) hybrid resolution)"
    - "CLAUDE.md line 46, PROJECT.md lines 77-78/105, ROADMAP.md (current v1.1 content) reflect droplet-primary, GHA-disabled posture per D-19"
    - "docs/DEPLOY.md is INTENTIONALLY left untouched per 10-CONTEXT.md Deferred Ideas entry — rewrite deferred to post-Phase-12 docs-sweep"
  artifacts:
    - path: .github/workflows/daily.yml.disabled
      provides: "Retired GHA cron workflow, preserved for rollback per D-18(a)"
      contains: "cron"
    - path: tests/test_scheduler.py
      provides: "WORKFLOW_PATH constant updated to disabled filename"
      contains: "daily.yml.disabled"
    - path: .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md
      provides: "Droplet-side deploy-key operator runbook (+ docs/DEPLOY.md staleness note)"
      contains: "ssh-keygen"
    - path: CLAUDE.md
      provides: "Deployment section reflects droplet-primary / GHA-disabled"
      contains: "droplet"
    - path: .planning/PROJECT.md
      provides: "Deployment + Key Decisions rows updated for v1.1"
      contains: "DigitalOcean droplet"
  key_links:
    - from: SETUP-DEPLOY-KEY.md
      to: main.py::_push_state_to_git
      via: "first-run bootstrap `python main.py --once` exercises the helper"
      pattern: "python main\\.py --once"
    - from: tests/test_scheduler.py::TestGHAWorkflow
      to: .github/workflows/daily.yml.disabled
      via: "WORKFLOW_PATH constant reads renamed file"
      pattern: "daily\\.yml\\.disabled"
---

<objective>
Close INFRA-03: rename `.github/workflows/daily.yml` → `daily.yml.disabled`
via `git mv` (D-16), update `tests/test_scheduler.py` WORKFLOW_PATH constant
to the new filename (D-18 option (a) — keeps the 12 TestGHAWorkflow
assertions green as rollback insurance), write the operator setup
`SETUP-DEPLOY-KEY.md` in the phase directory (D-14), and update prose in
CLAUDE.md + PROJECT.md + ROADMAP.md to reflect droplet-primary + GHA-
disabled posture (D-19). Depends on Plan 03 so SETUP-DEPLOY-KEY.md can
reference the `_push_state_to_git` helper by name for first-run bootstrap
verification.

**REVIEW REVISION (LOW from 10-REVIEWS.md Codex MEDIUM):** `docs/DEPLOY.md`
is explicitly OUT OF SCOPE for Phase 10. The file (172 lines) describes
GHA as the primary path across its Quickstart, "What the workflow does",
and Cost sections — a surgical edit isn't feasible. Per 10-CONTEXT.md
§Deferred Ideas (revised 2026-04-24 in reviews mode), rewriting docs/DEPLOY.md
is deferred to a post-Phase-12 docs-sweep phase that can describe the
full droplet + HTTPS + web-layer story coherently. Gemini accepted the
current "leave README badge as retired indicator" baseline for
current-phase docs posture; codex flagged the staleness as MEDIUM.
Resolution: extend the same rationale to docs/DEPLOY.md in 10-CONTEXT.md
Deferred Ideas (option (b) adopted); SETUP-DEPLOY-KEY.md adds a
"DEPLOY.md is stale" pointer so operators reading either doc find the
current source of truth (option (c) overlay).

Purpose: Retire the v1.0 GHA cron path so the droplet systemd unit (Phase 11)
is the sole signal runner — no duplicate emails, no competing state.json
writes. Keep the workflow file on-disk as `.disabled` for cheap rollback.
Document the operator-side SSH setup so the deploy-key path in Plan 03 can
actually authenticate.

Output: 1 file rename (preserves git history), 1-line test-constant
update, 1 new operator doc (~90 lines including docs/DEPLOY.md staleness
callout), 3 prose edits across planning docs. Zero Python runtime code
change in this plan — all artifacts are config / tests / docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-RESEARCH.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-PATTERNS.md
@.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-REVIEWS.md
@.github/workflows/daily.yml
@tests/test_scheduler.py
@CLAUDE.md
@docs/DEPLOY.md

<interfaces>
<!-- Lines verified in 10-RESEARCH.md §Summary and §Pattern Assignments. -->

From tests/test_scheduler.py:357 (CURRENT):
```python
class TestGHAWorkflow:
  '''SCHED-05 / D-07..D-11 / Pitfall 2: static validation of daily.yml contract.'''

  WORKFLOW_PATH = '.github/workflows/daily.yml'    # change target
```

From CLAUDE.md:46 (CURRENT — v1.0 era text):
```markdown
- **Deployment:** GitHub Actions is the primary path (cron `0 0 * * 1-5` UTC = 08:00 AWST Mon-Fri); Replit Always On is alternative
```

From .planning/PROJECT.md:77-78 (v1.0 era text):
```markdown
- **Deployment:** GitHub Actions is the PRIMARY path (cron `0 0 * * 1-5` UTC = 08:00 AWST Mon-Fri, with `timeout-minutes: 10` runaway-run cap per Phase 9). Replit Always On documented as alternative.
- **State persistence:** GHA commits `state.json` back to the repo each run; Replit filesystem persists if Always On is active.
```

From .planning/PROJECT.md:105 (Key Decisions table row):
```markdown
| GitHub Actions PRIMARY (Replit alternative) | Replit Autoscale doesn't guarantee filesystem persistence and kills `schedule` loops; GHA is free, stateless-by-design, and commits `state.json` back | Good |
```

From .planning/ROADMAP.md lines 209 + 215 (ALREADY v1.1-correct per research):
```markdown
| DO droplet is runtime; GitHub is source + state history via deploy-key push-back | Phase 10 INFRA-02 + Phase 11 systemd unit + Phase 16 milestone close |
...
| GHA cron retired once droplet systemd runs reliably | Phase 10 INFRA-03 + Phase 11 systemd unit |
```

From docs/DEPLOY.md lines 1-20 (NOT touched by this plan — structural
analog for SETUP-DEPLOY-KEY.md ONLY; its content is GHA-specific and
must NOT be copied; file is flagged stale in 10-CONTEXT.md §Deferred
Ideas for a future docs-sweep phase):
```markdown
# DEPLOY.md — Trading Signals operator runbook

**Primary deployment:** GitHub Actions (free, stateless-by-design, cron-driven).
**Alternative deployment:** Replit Reserved VM + Always On (persistent process).

***

## Quickstart — GitHub Actions (primary)
...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv daily.yml to daily.yml.disabled + update test_scheduler WORKFLOW_PATH</name>
  <files>.github/workflows/daily.yml, tests/test_scheduler.py</files>
  <read_first>
    - .github/workflows/daily.yml (read full file to confirm it's the retiring workflow; note its contents since the .disabled version will hold same bytes)
    - tests/test_scheduler.py (read lines 340-400 for TestGHAWorkflow class + WORKFLOW_PATH line 357; also read lines 650-680 for TestDeployDocs badge assertion)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md (§Area 4 D-16 + D-18)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-PATTERNS.md (§.github/workflows/daily.yml rename + §TestGHAWorkflow.WORKFLOW_PATH)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-RESEARCH.md (§Summary finding 3 — 12 tests covered by single constant)
  </read_first>
  <action>
    **Step A — Rename the workflow file via `git mv`:**

    ```bash
    git mv .github/workflows/daily.yml .github/workflows/daily.yml.disabled
    ```

    `git mv` preserves file history (git follows the rename via `git log
    --follow`). A plain OS `mv` followed by `git add -A` works but loses
    follow-history; use `git mv` explicitly.

    Expected post-state:
    - `ls .github/workflows/` shows `daily.yml.disabled` (and no `daily.yml`)
    - `git status` shows the rename

    GitHub Actions parses only files whose name matches `*.yml` (or `*.yaml`)
    in `.github/workflows/` — the `.disabled` suffix fully retires the
    schedule per GitHub docs. No additional "pause" action needed.

    **Step B — Update WORKFLOW_PATH constant in tests/test_scheduler.py:**

    Exact edit at tests/test_scheduler.py:357 (inside `class TestGHAWorkflow`):

    BEFORE:
    ```python
      WORKFLOW_PATH = '.github/workflows/daily.yml'
    ```

    AFTER:
    ```python
      WORKFLOW_PATH = '.github/workflows/daily.yml.disabled'
    ```

    Single-line change. Per 10-RESEARCH §Summary finding 3, this one edit
    fixes all 12 TestGHAWorkflow tests (they all read `self.WORKFLOW_PATH`
    at method level). Do NOT modify any test body.

    **Step C — TestDeployDocs README badge assertion decision (D-18(a) rollback path):**

    Per research, `TestDeployDocs` at tests/test_scheduler.py:~664 asserts
    `'actions/workflows/daily.yml/badge.svg' in content` (README.md:3 badge
    URL). Options:

    - (a) LEAVE README + TestDeployDocs unchanged — the rendered badge will
      show "no runs" once GHA history ages out, which IS the visual
      indicator that the workflow is retired. Operator-friendly signal.
      ZERO CODE CHANGE in this plan.
    - (b) Update badge URL to `daily.yml.disabled/badge.svg` — GitHub
      renders this as a 404-like broken badge. Noisier signal.
    - (c) Remove badge entirely from README.md + delete the TestDeployDocs
      assertion.

    **Adopted: (a).** Leave README + TestDeployDocs untouched. The
    SETUP-DEPLOY-KEY.md doc (Task 3) explicitly notes that the badge will
    render as "no recent runs" post-retire — this is expected. This matches
    10-CONTEXT.md D-18 adopted position.

    **Step D — Verify:**

    ```bash
    # File rename in place
    ls .github/workflows/
    # Expected output: daily.yml.disabled  (and NO daily.yml)

    # History preserved
    git log --follow -1 .github/workflows/daily.yml.disabled
    # Expected: commit history flows back through the old daily.yml commits

    # Tests green
    pytest tests/test_scheduler.py::TestGHAWorkflow -q
    # Expected: 12 passed

    # TestDeployDocs still green (per D-18(a) — we did not touch README)
    pytest tests/test_scheduler.py::TestDeployDocs -q
    # Expected: all pass
    ```
  </action>
  <verify>
    <automated>test -f .github/workflows/daily.yml.disabled && ! test -f .github/workflows/daily.yml && pytest tests/test_scheduler.py::TestGHAWorkflow tests/test_scheduler.py::TestDeployDocs -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/daily.yml.disabled` exits 0
    - `test -f .github/workflows/daily.yml` exits 1 (file absent)
    - `grep -q "WORKFLOW_PATH = '\.github/workflows/daily\.yml\.disabled'" tests/test_scheduler.py` (new constant value)
    - `! grep -q "WORKFLOW_PATH = '\.github/workflows/daily\.yml'$" tests/test_scheduler.py` (old value gone; trailing `$` anchor to avoid false match on `.disabled` substring)
    - `git log --follow .github/workflows/daily.yml.disabled | head -1` shows commit info (history preserved via git mv)
    - `pytest tests/test_scheduler.py::TestGHAWorkflow -q` — all 12 tests green
    - `pytest tests/test_scheduler.py::TestDeployDocs -q` — all tests green (README+badge untouched per D-18(a))
    - `pytest -q` — full suite green (zero regression)
  </acceptance_criteria>
  <done>
    daily.yml renamed to daily.yml.disabled via git mv (history preserved);
    WORKFLOW_PATH constant updated (single-line edit covers all 12
    TestGHAWorkflow tests); README badge URL untouched per D-18(a); all
    tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md + PROJECT.md + ROADMAP.md prose per D-19 (docs/DEPLOY.md explicitly excluded per REVIEW LOW)</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    - CLAUDE.md (read line 46 for current "Deployment:" line; read nearby context lines 40-55)
    - .planning/PROJECT.md (read lines 70-110 for Deployment + Key Decisions sections)
    - .planning/ROADMAP.md (spot-check lines 205-225 for "Operator Decisions Baked In" table — confirm research claim that v1.1 rows already reflect droplet-primary)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md (§Area 4 D-19 + §Deferred Ideas docs/DEPLOY.md entry)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-RESEARCH.md (§PROJECT.md + ROADMAP.md + CLAUDE.md prose updates — exact before/after)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-PATTERNS.md (§.planning/PROJECT.md / ROADMAP.md / CLAUDE.md prose updates)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-REVIEWS.md (§Codex 10-04 MEDIUM — docs/DEPLOY.md scope)
  </read_first>
  <action>
    **Step A — CLAUDE.md line 46 edit:**

    BEFORE (exact text):
    ```markdown
    - **Deployment:** GitHub Actions is the primary path (cron `0 0 * * 1-5` UTC = 08:00 AWST Mon-Fri); Replit Always On is alternative
    ```

    AFTER (exact replacement):
    ```markdown
    - **Deployment:** DigitalOcean droplet systemd is the primary path (Phase 11+). GitHub Actions cron is disabled (`.github/workflows/daily.yml.disabled` — retained for rollback per Phase 10 INFRA-03). Replit Always On remains documented as an alternative in `docs/DEPLOY.md` (docs file is stale; see Phase 10 CONTEXT §Deferred Ideas).
    ```

    Only line 46 changes. Do NOT touch any other line in CLAUDE.md.

    **Step B — .planning/PROJECT.md line 77 edit:**

    BEFORE (exact):
    ```markdown
    - **Deployment:** GitHub Actions is the PRIMARY path (cron `0 0 * * 1-5` UTC = 08:00 AWST Mon-Fri, with `timeout-minutes: 10` runaway-run cap per Phase 9). Replit Always On documented as alternative.
    ```

    AFTER:
    ```markdown
    - **Deployment:** DigitalOcean droplet systemd is the PRIMARY path (Phase 11 onwards). GitHub Actions cron is disabled (`.github/workflows/daily.yml.disabled`) per Phase 10 INFRA-03 to avoid duplicate signal emails; the disabled workflow retains `timeout-minutes: 10` as rollback insurance. Replit Always On remains documented as an alternative (see `docs/DEPLOY.md` — stale, rewrite deferred to post-Phase-12 docs-sweep per 10-CONTEXT.md Deferred Ideas).
    ```

    **Step C — .planning/PROJECT.md line 78 edit:**

    BEFORE (exact):
    ```markdown
    - **State persistence:** GHA commits `state.json` back to the repo each run; Replit filesystem persists if Always On is active.
    ```

    AFTER:
    ```markdown
    - **State persistence:** The droplet's daily run commits `state.json` back to `origin/main` via a GitHub deploy key (Phase 10 INFRA-02 / `_push_state_to_git` in `main.py`); Replit filesystem persists if Always On is active.
    ```

    **Step D — .planning/PROJECT.md line 105 edit (Key Decisions table row):**

    BEFORE (exact):
    ```markdown
    | GitHub Actions PRIMARY (Replit alternative) | Replit Autoscale doesn't guarantee filesystem persistence and kills `schedule` loops; GHA is free, stateless-by-design, and commits `state.json` back | Good |
    ```

    AFTER:
    ```markdown
    | DO droplet systemd PRIMARY (v1.1); GHA cron disabled, Replit alternative | Droplet gives HTTP-serving capability for FastAPI (v1.1 Phase 11+); GHA cron retired in Phase 10 INFRA-03 to avoid duplicate emails; state.json pushed back via deploy key per Phase 10 INFRA-02 | Good (v1.1) |
    ```

    **Step E — .planning/ROADMAP.md sanity check (NO edits expected):**

    Per 10-RESEARCH Summary + line-by-line verification, `.planning/ROADMAP.md`
    lines 209 ("DO droplet is runtime; GitHub is source + state history via
    deploy-key push-back") and 215 ("GHA cron retired once droplet systemd
    runs reliably") ALREADY reflect v1.1 correctly. If the file has been
    modified since research, spot-check these rows remain v1.1-correct;
    otherwise NO EDITS. Do NOT rewrite archived v1.0 content under
    `.planning/milestones/`.

    If spot-check finds a drift (unlikely given recent STATE.md entry
    "v1.1 roadmap created 2026-04-24"), edit only the specific lines to
    match the v1.1-correct wording shown above.

    **Step F — `docs/DEPLOY.md` — INTENTIONALLY NOT TOUCHED (REVIEW LOW).**

    Per 10-REVIEWS.md Codex 10-04 MEDIUM and 10-CONTEXT.md §Deferred
    Ideas, `docs/DEPLOY.md` is OUT OF SCOPE for Phase 10. The file's
    Quickstart and "What the workflow does" sections describe GHA as the
    primary path and cannot be surgically edited without a full rewrite
    covering post-Phase-12 HTTPS + nginx + systemd story. Rewrite is
    deferred to a docs-sweep phase after Phase 12. Phase 10 surfaces the
    staleness via (1) CLAUDE.md + PROJECT.md prose amendments (Steps A-D
    above) pointing readers at the Deferred Ideas entry, and (2)
    SETUP-DEPLOY-KEY.md Pitfalls section (Task 3) including an explicit
    "docs/DEPLOY.md is stale" callout.

    **Do NOT edit docs/DEPLOY.md in this plan.** Grep-assertion below
    verifies the file is untouched.

    **Step G — Verify prose state:**

    ```bash
    grep -q "DigitalOcean droplet systemd is the primary path" CLAUDE.md
    grep -q "DigitalOcean droplet systemd is the PRIMARY path" .planning/PROJECT.md
    grep -q "droplet's daily run commits \`state.json\` back" .planning/PROJECT.md
    grep -q "DO droplet systemd PRIMARY (v1.1)" .planning/PROJECT.md
    ! grep -q "GitHub Actions is the primary path" CLAUDE.md
    ! grep -q "GitHub Actions is the PRIMARY path" .planning/PROJECT.md
    # docs/DEPLOY.md intentionally untouched:
    git diff --quiet docs/DEPLOY.md
    ```

    All seven checks should pass.
  </action>
  <verify>
    <automated>grep -q "DigitalOcean droplet systemd is the primary path" CLAUDE.md && grep -q "DigitalOcean droplet systemd is the PRIMARY path" .planning/PROJECT.md && grep -q "daily run commits" .planning/PROJECT.md && ! grep -q "GitHub Actions is the primary path" CLAUDE.md && ! grep -q "GitHub Actions is the PRIMARY path" .planning/PROJECT.md && git diff --quiet docs/DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "DigitalOcean droplet systemd is the primary path" CLAUDE.md` exits 0
    - `! grep -q "GitHub Actions is the primary path" CLAUDE.md` (old v1.0 wording removed)
    - `grep -q "DigitalOcean droplet systemd is the PRIMARY path" .planning/PROJECT.md` exits 0
    - `! grep -q "GitHub Actions is the PRIMARY path" .planning/PROJECT.md` (old wording removed)
    - `grep -q "daily.yml.disabled" CLAUDE.md` (explicit filename reference for rollback trace)
    - `grep -q "daily.yml.disabled" .planning/PROJECT.md` (explicit filename reference)
    - `grep -q "Phase 10 INFRA-02" .planning/PROJECT.md` (traceability)
    - `grep -q "_push_state_to_git" .planning/PROJECT.md` (helper reference by name per Plan 03 link)
    - `grep -q "DO droplet systemd PRIMARY (v1.1)" .planning/PROJECT.md` (Key Decisions row updated)
    - `grep -q "docs-sweep" .planning/PROJECT.md` OR `grep -q "stale" .planning/PROJECT.md` (REVIEW LOW — docs/DEPLOY.md staleness acknowledged in prose)
    - `grep -q "docs-sweep\\|stale" CLAUDE.md` (REVIEW LOW — same acknowledgment in CLAUDE.md)
    - `.planning/ROADMAP.md` lines 209 + 215 show "DO droplet is runtime..." and "GHA cron retired..." respectively (no edit required if already v1.1-correct)
    - `git diff --quiet docs/DEPLOY.md` exits 0 — REVIEW LOW option (b): file INTENTIONALLY untouched, deferred to docs-sweep phase
    - File diff stats: CLAUDE.md = 1 line modified; .planning/PROJECT.md = 3 lines modified; .planning/ROADMAP.md = 0 lines modified; docs/DEPLOY.md = 0 lines modified (expected)
  </acceptance_criteria>
  <done>
    CLAUDE.md + PROJECT.md prose reflect droplet-primary + GHA-disabled
    posture per D-19; both prose updates acknowledge docs/DEPLOY.md
    staleness and point readers at the Deferred Ideas entry (REVIEW LOW
    resolution); PROJECT.md references Plan 03 helper by name; ROADMAP.md
    already v1.1-correct (spot-checked, no edit); docs/DEPLOY.md
    INTENTIONALLY untouched per 10-CONTEXT.md Deferred Ideas (rewrite
    deferred to post-Phase-12 docs-sweep); old v1.0 "GitHub Actions is
    the primary path" wording removed from both files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author SETUP-DEPLOY-KEY.md operator runbook (D-14) — includes docs/DEPLOY.md staleness pointer per REVIEW LOW</name>
  <files>.planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md</files>
  <read_first>
    - docs/DEPLOY.md (read lines 1-60 for structural pattern — quickstart / numbered steps / verify blocks; do NOT copy GHA-specific content; note the file is flagged stale for future rewrite)
    - main.py (confirm Plan 03 landed; spot-check _push_state_to_git helper name + docstring for first-run bootstrap reference)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md (§Area 3 D-14 + §Deferred Ideas docs/DEPLOY.md entry)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-PATTERNS.md (§SETUP-DEPLOY-KEY.md content spec)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-RESEARCH.md (§Runtime State Inventory + §Pitfalls 5/6/7 — systemd WorkingDirectory, IdentitiesOnly, known_hosts)
    - .planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-REVIEWS.md (§Codex 10-04 MEDIUM — docs/DEPLOY.md scope)
  </read_first>
  <action>
    **Create a new file** at
    `.planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md`
    with the following content. Use single-backtick code for inline commands
    and fenced bash blocks for multi-line commands. Target ~90-120 lines.

    Content template (produce this exact Markdown shape, filling in the
    intentionally-templated `<owner>` placeholder):

    ````markdown
    # SETUP-DEPLOY-KEY.md — Droplet to GitHub write-back via SSH deploy key

    **Phase 10 / INFRA-02 operator runbook.** One-time setup for the
    DigitalOcean droplet to authenticate `git push` back to `origin/main`
    so the daily run's `state.json` commit (via
    `main.py::_push_state_to_git`) can land in the repo automatically.

    **Audience:** project operator (Marc).
    **Where this runs:** the DO droplet, as the user that runs the
    `trading-signals` systemd unit (Phase 11+).
    **Prerequisite:** GitHub repo admin access (to register the deploy key)
    and SSH access to the droplet.
    **Cost:** free.

    > **Read first: `docs/DEPLOY.md` is stale.** That file still
    > describes GitHub Actions as the primary deployment path (v1.0 era).
    > It has not been rewritten yet — rewrite is deferred to a post-
    > Phase-12 docs-sweep phase (see
    > `.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-CONTEXT.md`
    > §Deferred Ideas). For current v1.1 deployment guidance, use THIS
    > file (`SETUP-DEPLOY-KEY.md`), `.planning/PROJECT.md` Deployment
    > section, and `CLAUDE.md` §Stack.

    ***

    ## Quickstart

    1. Generate a dedicated SSH keypair for this project on the droplet.
    2. Register the public half as a Deploy Key with write access in the
       GitHub repo settings.
    3. Configure `~/.ssh/config` on the droplet to route the github.com
       host through the new key.
    4. Switch the repo remote from HTTPS to SSH.
    5. Accept the github.com host key once (`ssh -T git@github.com`).
    6. Run `python main.py --once` to verify the first push lands under
       the `DO Droplet <droplet@trading-signals>` author line.

    Each step is detailed below.

    ***

    ## Step 1 — Generate the keypair

    Run this on the droplet, as the user that will run the systemd unit
    (NOT root unless your systemd unit runs as root).

    ```bash
    ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_trading_signals -C 'droplet@trading-signals'
    # Do NOT set a passphrase — the systemd unit cannot supply one interactively.
    chmod 0600 ~/.ssh/id_ed25519_trading_signals
    chmod 0600 ~/.ssh/id_ed25519_trading_signals.pub
    chmod 0700 ~/.ssh
    ```

    **Expected:** two files in `~/.ssh/` — the private key (mode 0600) and
    the public key (same mode for simplicity). `~/.ssh/` is mode 0700.

    **Security:** the private key MUST NOT leave the droplet. It MUST NOT
    be committed to this repo or any other. File mode `0600` (owner read
    only) is enforced by SSH.

    ***

    ## Step 2 — Register the public key as a deploy key in GitHub

    Copy the public key contents:

    ```bash
    cat ~/.ssh/id_ed25519_trading_signals.pub
    ```

    Paste the output into the GitHub web UI:

    1. Open `https://github.com/<owner>/trading-signals/settings/keys`.
    2. Click **Add deploy key**.
    3. **Title:** `droplet-trading-signals`.
    4. **Key:** paste the `ssh-ed25519 AAAA... droplet@trading-signals`
       line.
    5. **Allow write access:** CHECK this box — the daily run pushes
       commits.
    6. Click **Add key**.

    **Verify:** the key now appears in the repo's deploy-keys list with a
    write-access indicator.

    ***

    ## Step 3 — Configure ~/.ssh/config

    Append this block to `~/.ssh/config` on the droplet (create the file
    if it doesn't exist). This routes github.com SSH connections through
    the dedicated key and prevents SSH agent forwarding from using some
    other key.

    ```
    Host github.com
      HostName github.com
      User git
      IdentityFile ~/.ssh/id_ed25519_trading_signals
      IdentitiesOnly yes
    ```

    Then:

    ```bash
    chmod 0600 ~/.ssh/config
    ```

    **Pitfall:** without `IdentitiesOnly yes`, SSH may offer every key in
    `~/.ssh/` or in the agent, get rejected repeatedly by GitHub, and
    eventually fall over with "Too many authentication failures". Explicit
    `IdentitiesOnly yes` + a single `IdentityFile` avoids this.

    ***

    ## Step 4 — Switch the repo remote from HTTPS to SSH

    On the droplet, inside the cloned repo:

    ```bash
    cd /path/to/trading-signals   # systemd unit WorkingDirectory
    git remote -v
    # Expected output BEFORE: origin https://github.com/<owner>/trading-signals.git (fetch/push)

    git remote set-url origin git@github.com:<owner>/trading-signals.git
    git remote -v
    # Expected output AFTER: origin git@github.com:<owner>/trading-signals.git (fetch/push)
    ```

    **Pitfall:** HTTPS remotes cache credentials (git-credential-helper)
    and will SILENTLY fail push with a cryptic 401/403 if the cache
    expires. SSH remotes use the deploy key every time — stateless.

    ***

    ## Step 5 — Accept the github.com host key once

    SSH will refuse to connect to github.com until its host key is in
    `~/.ssh/known_hosts`. Run this once, interactively, as the systemd
    user:

    ```bash
    ssh -T git@github.com
    # Prompt: "The authenticity of host 'github.com (...)' can't be established."
    # Answer: yes
    # Expected final line: "Hi <owner>/trading-signals! You've successfully
    # authenticated, but GitHub does not provide shell access."
    ```

    **Expected:** `~/.ssh/known_hosts` now contains a `github.com` entry.
    Subsequent non-interactive `git push` calls from the systemd unit
    succeed because the host key is pre-trusted.

    **If the final line says "Permission denied (publickey)":** re-check
    Step 2 (deploy key registered) and Step 3 (~/.ssh/config IdentityFile
    path correct).

    ***

    ## Step 6 — First-run bootstrap

    This exercises the `_push_state_to_git` helper shipped in Plan 03:

    ```bash
    cd /path/to/trading-signals
    python main.py --once
    ```

    **Expected:**
    - The run completes with exit code 0.
    - `tail -n 50 /var/log/trading-signals.log` (or systemd journal once
      the unit is installed in Phase 11) shows `[State] state.json pushed
      to origin/main`.
    - `git log -1` on the droplet shows a new commit authored by
      `DO Droplet <droplet@trading-signals>` with message
      `chore(state): daily signal update [skip ci]`.
    - The same commit appears in the GitHub web UI commit log.

    If the helper instead logs `[State] state.json unchanged — skipping
    git push`, that's fine too — it means the current state.json matches
    what's already in `origin/main`. Force a diff and re-run, e.g.:

    ```bash
    python main.py --reset --initial-account 100000 --spi-contract spi-mini --audusd-contract audusd-standard
    python main.py --once
    ```

    ***

    ## Pitfalls

    - **systemd WorkingDirectory.** The `_push_state_to_git` helper calls
      `subprocess.run(['git', ...])` without `cwd=`. The subprocess
      inherits the Python process's cwd. In Phase 11, the
      `trading-signals.service` unit file MUST include
      `WorkingDirectory=/path/to/trading-signals` so git finds the repo.
      If this is missing, the push fails with "fatal: not a git
      repository" and the helper writes a warning via
      `append_warning(source='state_pusher', ...)`.

    - **Clock drift.** Commits use the droplet's system clock. Ensure
      `timedatectl` reports correct time + `Australia/Perth` timezone
      (Phase 11 will assert this via `_get_process_tzname`).

    - **Deploy-key rotation.** Not scheduled for v1.1. Revisit when the
      project moves beyond single-operator ownership.

    - **Rollback.** If the droplet path must be abandoned (domain issue,
      systemd regression), reverse Plan 04's `git mv` via
      `git mv .github/workflows/daily.yml.disabled .github/workflows/daily.yml`
      and re-enable the GHA schedule. GitHub repo secrets
      (`RESEND_API_KEY`, `SIGNALS_EMAIL_TO`) remain in place per D-17 to
      make this reversal one-commit.

    - **README.md status badge.** The badge URL still points at
      `daily.yml/badge.svg`. Once GitHub Actions history ages out, the
      badge will render as "no recent runs" — this is the INTENDED visual
      signal that the workflow is retired per D-18(a). Do not "fix" it
      unless the rollback path is formally abandoned.

    - **`docs/DEPLOY.md` is stale.** The file describes GitHub Actions as
      the primary deployment path (v1.0 content). Its Quickstart and
      "What the workflow does" sections DO NOT apply to the v1.1
      droplet-primary runtime. Rewrite is deferred to a post-Phase-12
      docs-sweep phase that can describe the full droplet + HTTPS +
      nginx + web-layer story coherently. For current v1.1 deployment
      guidance, use THIS file, `.planning/PROJECT.md` §Deployment, and
      `CLAUDE.md` §Stack. If you find yourself reading `docs/DEPLOY.md`
      during v1.1 operations, close it and open this file instead.

    ***

    *Setup completed:* record the date in STATE.md §Accumulated Context
    once Step 6 produces a visible GitHub commit.
    ````

    **Writing conventions:**
    - Top-of-file purpose banner (matches docs/DEPLOY.md structural analog)
    - `***` horizontal-rule separators between sections
    - Bold section labels (`**Expected:**`, `**Pitfall:**`, `**Verify:**`)
    - Fenced `bash` blocks for copy-pasteable commands
    - Explicit references to D-XX decisions and Plan 03 helper names
    - Do NOT include GHA-specific content (secrets config, cron details,
      badge setup) — those belong to the retired path
    - **NEW (REVIEW LOW):** upfront "docs/DEPLOY.md is stale" blockquote AND
      a Pitfalls-section explanation so operators don't accidentally
      follow the v1.0 GHA runbook in `docs/DEPLOY.md`

    **Placement:** the file lives in the phase directory (not repo root)
    per D-14 rationale — once executed, this IS phase documentation. A
    future publish step may move it to `docs/` or `operator-runbooks/`;
    that's a separate decision.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md && grep -q "ssh-keygen" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md && grep -q "_push_state_to_git" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md && grep -q "ssh -T git@github.com" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md && grep -q "python main.py --once" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md && grep -q "docs/DEPLOY.md" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` exits 0
    - `grep -q "ssh-keygen" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (Step 1 keypair generation present)
    - `grep -q "ssh-ed25519" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (ed25519 key type used)
    - `grep -q "IdentitiesOnly yes" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (Pitfall 6 mitigation present)
    - `grep -q "ssh -T git@github.com" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (Step 5 known_hosts bootstrap)
    - `grep -q "git remote set-url origin git@github" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (Step 4 HTTPS-to-SSH switch)
    - `grep -q "python main.py --once" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (Step 6 first-run bootstrap)
    - `grep -q "_push_state_to_git" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (Plan 03 helper name referenced)
    - `grep -q "WorkingDirectory=" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (systemd WorkingDirectory pitfall documented)
    - `grep -q "0600" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (T-10-01 mitigation: file mode)
    - `grep -q "MUST NOT be committed" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (T-10-01 key-not-in-repo warning)
    - `grep -q "DO Droplet <droplet@trading-signals>" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (D-10 commit identity trace)
    - `grep -q "chore(state): daily signal update \[skip ci\]" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (D-11 commit message trace)
    - `grep -q "rollback" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (D-18(a) rollback path documented)
    - `grep -q "docs/DEPLOY.md" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (REVIEW LOW — staleness pointer present)
    - `grep -q "docs-sweep" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (REVIEW LOW — directs reader to deferred rewrite phase)
    - `grep -qE "stale|Read first" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` (REVIEW LOW — upfront staleness banner visible)
    - File line count >= 90 (substance check): `wc -l .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md | awk '{print $1}'` returns >= 90
  </acceptance_criteria>
  <done>
    SETUP-DEPLOY-KEY.md exists in phase dir with 6 numbered setup steps
    (keypair gen, deploy-key registration, ~/.ssh/config, remote switch,
    host-key accept, first-run bootstrap), references Plan 03 helper name
    for traceability, covers T-10-01 key-security mitigations (mode 0600,
    not-in-repo), systemd WorkingDirectory pitfall, D-18(a) rollback
    path, AND a "docs/DEPLOY.md is stale" blockquote + Pitfalls-section
    explanation per REVIEW LOW resolution. All grep acceptance criteria
    pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator droplet to GitHub via SSH | Authenticated by the operator-configured deploy key per SETUP-DEPLOY-KEY.md |
| operator droplet filesystem to deploy-key private material | Stored at `~/.ssh/id_ed25519_trading_signals` with mode 0600; never committed |
| planning-doc prose (CLAUDE.md / PROJECT.md) to future readers | Documentation-only; no executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | Deploy key private material | mitigate | SETUP-DEPLOY-KEY.md Step 1 mandates `chmod 0600` on private + public key files, `chmod 0700` on `~/.ssh/`, and explicit "MUST NOT be committed to this repo or any other". Grep-asserted: `0600` + `MUST NOT be committed` both present in the doc. ASVS L1. |
| T-10-02 | Elevation of Privilege | SSH agent forwarding using wrong key | mitigate | Step 3 mandates `IdentitiesOnly yes` + explicit `IdentityFile` in `~/.ssh/config`. Prevents SSH from offering every key in agent and blowing through "Too many authentication failures" cap. Grep-asserted: `IdentitiesOnly yes` present. |
| T-10-03 | Tampering (wrong-repo push) | `git remote set-url` might point at wrong owner | mitigate | Step 4 uses explicit `git@github.com:<owner>/trading-signals.git` and requires operator to substitute `<owner>`. Recommended verification step `git remote -v` asserts post-state. |
| T-10-04 | Repudiation (commit authorship) | Commits in origin/main lacking traceability | mitigate | Plan 03 D-10 hardcodes inline `-c user.email=droplet@trading-signals -c user.name=DO Droplet`. SETUP-DEPLOY-KEY.md Step 6 asserts operator sees this exact author line in `git log -1`. Tooling can grep the author email to identify droplet-authored commits unambiguously. |
| T-10-05 | DoS (retirement reversible) | Accidental abandonment of GHA rollback path | accept | D-18(a) + D-17: workflow file is renamed `.disabled` (not deleted); repo secrets stay. SETUP-DEPLOY-KEY.md Pitfalls §Rollback documents the one-commit reversal. No code path depends on `daily.yml.disabled`'s absence — reintroducing the file re-enables the schedule without further changes. |
| T-10-06 | Information Disclosure (wrong docs) | Operator follows stale GHA runbook in docs/DEPLOY.md and misconfigures droplet | mitigate (REVIEW LOW) | SETUP-DEPLOY-KEY.md opens with a blockquote banner "Read first: docs/DEPLOY.md is stale" and Pitfalls section includes an explicit "docs/DEPLOY.md is stale" entry directing the reader back to the current doc. CLAUDE.md + PROJECT.md prose also carry the staleness note. Grep-asserted in three files. ASVS L1. |

No HIGH-severity unmitigated threats. This plan is almost entirely config +
docs + tests; the only code-adjacent change is a test constant value update
(no logic change).
</threat_model>

<verification>
After all tasks:
1. `test -f .github/workflows/daily.yml.disabled && ! test -f .github/workflows/daily.yml` — rename landed
2. `pytest tests/test_scheduler.py::TestGHAWorkflow -q` — all 12 tests green against renamed file
3. `pytest tests/test_scheduler.py::TestDeployDocs -q` — still green (README untouched per D-18(a))
4. `pytest -q` — full suite green (zero regression)
5. `grep -q "DigitalOcean droplet systemd" CLAUDE.md` — prose updated
6. `grep -q "DigitalOcean droplet systemd" .planning/PROJECT.md` — prose updated
7. `test -f .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` — doc created
8. `grep -q "ssh-keygen" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` — Step 1 content present
9. `grep -q "_push_state_to_git" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` — Plan 03 traceability
10. `grep -q "docs/DEPLOY.md" .planning/phases/10-foundation-v1-0-cleanup-deploy-key/SETUP-DEPLOY-KEY.md` — REVIEW LOW staleness pointer
11. `git diff --quiet docs/DEPLOY.md` — REVIEW LOW option (b): file untouched, deferred to docs-sweep
12. Manual (post-merge operator activity, captured in 10-VALIDATION.md as manual-only): operator runs SETUP-DEPLOY-KEY.md Steps 1-6 on droplet; last 3 state.json commits in GitHub show `DO Droplet <droplet@trading-signals>` author
</verification>

<success_criteria>
- `.github/workflows/daily.yml` renamed to `daily.yml.disabled` via `git mv` (history preserved)
- `tests/test_scheduler.py` WORKFLOW_PATH updated to `.github/workflows/daily.yml.disabled`; all 12 TestGHAWorkflow tests pass
- `SETUP-DEPLOY-KEY.md` exists in phase directory; covers keypair generation, deploy-key registration, ~/.ssh/config, remote switch, host-key accept, first-run bootstrap; references Plan 03 helper by name; includes docs/DEPLOY.md staleness banner + Pitfalls entry (REVIEW LOW)
- `CLAUDE.md` and `PROJECT.md` prose reflect droplet-primary + GHA-disabled posture per D-19 AND acknowledge docs/DEPLOY.md staleness pointing at Deferred Ideas
- `docs/DEPLOY.md` INTENTIONALLY UNTOUCHED — rewrite deferred to post-Phase-12 docs-sweep per REVIEW LOW option (b)
- `.planning/ROADMAP.md` spot-checked (v1.1-correct; no edit required)
- Full `pytest -q` suite green
- ROADMAP Phase 10 SC-4 (automated portion) satisfied; "no cron fires for 2 weekdays" is a manual-only operator verification captured in 10-VALIDATION.md §Manual-Only
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-v1-0-cleanup-deploy-key/10-04-SUMMARY.md` covering:
- File rename performed (git mv output; git log --follow confirms history)
- WORKFLOW_PATH constant change (one-line diff)
- SETUP-DEPLOY-KEY.md line count + sections covered (+ confirm docs/DEPLOY.md staleness banner and Pitfalls entry present)
- CLAUDE.md line 46 before/after (+ confirm stale/docs-sweep acknowledgment)
- .planning/PROJECT.md lines 77-78/105 before/after (+ confirm stale/docs-sweep acknowledgment)
- .planning/ROADMAP.md status (no-op if already v1.1-correct)
- docs/DEPLOY.md status: UNTOUCHED (REVIEW LOW option (b) — deferred to post-Phase-12 docs-sweep)
- Full suite status (expect no test count delta — only constant-value change)
- Any Rule-1/Rule-3 deviations
- Commit SHAs
- Note: manual-only verification items (live deploy-key registration, 2-weekday silence) remain open per 10-VALIDATION.md §Manual-Only; these are operator-owned and surface to /gsd-verify-work 10 as "pending operator confirmation" matching Phase 7 Wave 2 pattern
</output>
