---
phase: 28-v1-2-uat-closure
plan: 06
type: execute
wave: 3
depends_on: [28-01, 28-02, 28-03, 28-04, 28-05]
files_modified:
  - .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md
autonomous: false
requirements:
  - DEBT-01
must_haves:
  truths:
    - "28-VERIFICATION.md exists with frontmatter matching Phase 27 verbatim per D-08"
    - "Table has 5 columns: Scenario | Source | Mode | Status | Evidence — per D-09"
    - "Three sections — '## Phase 17 Scenarios', '## Phase 23 Scenarios', '## Phase 26 Scenarios' — per D-07"
    - "11 evidence rows total (Phase 17: 3, Phase 23: 2, Phase 26: 1 cold-start + 5 multi-tab) per D-11"
    - "Frontmatter status = 'passed' if all PASS, 'partial' if any FAIL — per D-18"
    - "Each FAIL row carries symptom + suspected layer + repro command per D-19"
    - "iOS Safari row carries inline 1-line operator-readable acceptance note per D-14"
    - "Phase 23 live-yfinance row records rc + last 10 lines of stdout per D-05"
  artifacts:
    - path: ".planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md"
      provides: "Phase 28 closure artefact — DEBT-01 sign-off"
      contains: "8/8 scenarios verified"
  key_links:
    - from: ".planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md"
      to: "tests/uat/* spec results + live CLI output + iPhone operator note"
      via: "evidence cells, one per UAT-N"
      pattern: "PASS\\|FAIL"
---

<objective>
Run all 11 UAT scenarios live and produce `28-VERIFICATION.md` — the single closure artefact for DEBT-01.

This plan is the live-evidence pass. Per CONTEXT D-16 (aggregate-and-roll-into-Phase-29), all scenarios run regardless of intermediate FAILs. Per D-17, each scenario gets up to 3 attempts (initial + 2 retries) before being recorded FAIL. Per D-19, every FAIL row carries `symptom + suspected layer + repro command` so Phase 29 has a real lead.

This plan is `autonomous: false` because:
- Phase 17 iOS Safari tap-to-toggle (Mode: Manual) requires the operator's iPhone — Claude cannot drive it.
- Claude pauses the run, presents the inline acceptance note, waits for the operator to paste back PASS/FAIL + 1-line note.

Output: `.planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md` with the table fully populated and frontmatter set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-v1-2-uat-closure/28-CONTEXT.md
@.planning/milestones/v1.2-phases/27-code-quality-correctness-sweep-apply-2026-05-07-code-review-/27-VERIFICATION.md
@tests/uat/conftest.py
@tests/uat/test_uat_17_atr_handcalc.py
@tests/uat/test_uat_17_cookie_persistence.py
@tests/uat/test_uat_23_backtest_visual.py
@tests/uat/test_uat_26_coldstart.py
@tests/uat/test_uat_26_multitab.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all MCP-mode UAT specs with retry policy</name>
  <files>(none — produces transient run output consumed by Task 4)</files>
  <read_first>
    - .planning/phases/28-v1-2-uat-closure/28-CONTEXT.md (D-04 MCP scope, D-17 retry policy)
    - tests/uat/conftest.py (BASE_URL, retry semantics)
  </read_first>
  <action>
    Bootstrap Playwright browsers (one-time):
    ```
    pip install -r requirements-dev.txt
    playwright install chromium
    ```

    Run the full MCP-mode UAT suite, capturing per-test output:
    ```
    pytest -m uat tests/uat/ -v --tb=short 2>&amp;1 | tee /tmp/28-uat-run-1.log
    ```

    For any FAIL (excluding test_uat_17_atr_handcalc, which may legitimately skip on ohlc_window race), retry per CONTEXT D-17 (up to 2 more attempts, max 3 total):
    ```
    pytest -m uat tests/uat/<failing-spec.py>::<failing-test> -v --tb=short 2>&amp;1 | tee /tmp/28-uat-run-2.log
    pytest -m uat tests/uat/<failing-spec.py>::<failing-test> -v --tb=short 2>&amp;1 | tee /tmp/28-uat-run-3.log
    ```

    Record per-UAT outcome:
    - PASS on attempt 1: "PASS"
    - PASS on attempt 2 or 3: "PASS (retried Nx)"
    - FAIL on all 3: "FAIL" — capture symptom (assertion message), suspected layer (your best guess from the trace zip + assertion), and repro command (the exact pytest -m uat ... invocation).

    Save the structured per-UAT result table to `/tmp/28-uat-results.txt` for use in Task 4.

    NOTE: test_uat_17_atr_handcalc is MCP-mode but driven by Playwright MCP per CONTEXT D-04. Either invocation path (pytest CLI or Playwright MCP from Claude) is valid; pytest CLI is simpler and uses the same code path. Use pytest CLI as the default.
  </action>
  <verify>
    <automated>
    test -f /tmp/28-uat-run-1.log &amp;&amp;
    test -f /tmp/28-uat-results.txt &amp;&amp;
    grep -E "PASS|FAIL|SKIP" /tmp/28-uat-results.txt | wc -l | tr -d ' ' | awk '$1 &gt;= 8'
    </automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/28-uat-run-1.log` exists with `pytest -m uat` output.
    - `/tmp/28-uat-results.txt` contains a structured per-UAT line for each of: UAT-17-1 (ATR), UAT-17-3 (cookie), UAT-23-2 (/backtest), UAT-26-1 (cold-start), UAT-26-2..6 (multi-tab x5) = 9 rows.
    - Each FAIL row carries symptom + suspected layer + repro command per D-19.
  </acceptance_criteria>
  <done>All MCP-mode UAT specs have been run (with retries up to 3 per UAT) and per-UAT outcomes recorded in /tmp/28-uat-results.txt for Task 4.</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 23 live-yfinance CLI scenario</name>
  <files>(none — produces transient run output consumed by Task 4)</files>
  <read_first>
    - .planning/phases/28-v1-2-uat-closure/28-CONTEXT.md (D-05 CLI mode, D-15 NOT persisted as test)
    - .planning/milestones/v1.2-phases/23-five-year-backtest-validation-gate/23-HUMAN-UAT.md (UAT-1: rc=0 + cumulative-return >100% gate clean)
  </read_first>
  <action>
    Per CONTEXT D-05, this scenario is Bash-driven (not MCP). Run the live 5-year backtest:

    ```
    python -m backtest --years 5 2>&amp;1 | tee /tmp/28-backtest-run.log
    echo "RC=$?" &gt;&gt; /tmp/28-backtest-run.log
    ```

    Capture for evidence cell:
    1. Exit code (`grep '^RC=' /tmp/28-backtest-run.log`).
    2. Last 10 lines of stdout (`tail -n 10 /tmp/28-backtest-run.log`).
    3. Whether the cumulative-return >100% gate is clean (search log for the gate's PASS/FAIL line).

    Decision matrix:
    - rc=0 + gate clean → PASS, evidence = "rc=0; cumulative-return gate clean; tail: <last 1 line of stdout>"
    - rc=0 + gate triggered → FAIL, evidence per D-19: `symptom: 'cum-return X% > 100% gate threshold'; suspected: backtest/data_fetcher.py live yfinance shape; repro: python -m backtest --years 5`
    - rc≠0 → FAIL, evidence: `symptom: 'rc=N (last line: ...)'; suspected: backtest pipeline or yfinance availability; repro: python -m backtest --years 5`

    Per D-17, retry up to 3 times if rc≠0 due to transient yfinance rate-limit (recognisable from log content). Do NOT retry if rc=0 + gate triggered — that's a real signal, not a flake.

    Append the result line to `/tmp/28-uat-results.txt` (so Task 4 sees a single complete result file).
  </action>
  <verify>
    <automated>
    test -f /tmp/28-backtest-run.log &amp;&amp;
    grep -q "^RC=" /tmp/28-backtest-run.log &amp;&amp;
    grep -q "UAT-23-1" /tmp/28-uat-results.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/28-backtest-run.log` exists, contains the full run output and a final `RC=N` line.
    - `/tmp/28-uat-results.txt` has a `UAT-23-1` row with PASS or FAIL + the structured evidence.
    - The CLI scenario was NOT persisted under tests/uat/ (verify: `ls tests/uat/test_uat_23_*.py` lists only the visual-smoke spec, not a yfinance-CLI spec) — honours D-15.
  </acceptance_criteria>
  <done>Phase 23 UAT-1 live CLI run captured with rc + last-10-lines, result row added to /tmp/28-uat-results.txt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runs Phase 17 iOS Safari tap-to-toggle on iPhone</name>
  <what-built>
    The Phase 17 trace-panel toggle is the only Phase 28 scenario that requires real iOS Safari touch semantics — there is no MCP equivalent in scope (CONTEXT D-03). Plans 02–05 produced persisted Playwright specs for the desktop scenarios; this row is operator-only.
  </what-built>
  <how-to-verify>
    On your iPhone:
    1. Open Safari (not a private tab — cookies needed).
    2. Navigate to: `https://signals.mwiriadi.me/markets/SPI200/dashboard`
    3. Log in if prompted.
    4. Locate the trace panel (the "Inputs / Indicators / Vote" expandable section).
    5. Tap the trace-panel toggle. Observe: panel collapses (or expands, if it started collapsed).
    6. Reload the page (pull-to-refresh).
    7. Observe: toggle state is preserved — panel is in the SAME collapsed-or-expanded state as immediately before reload. You are still logged in.

    Reply with one line:
    - PASS: `PASS — tap toggled, reload preserved state, still logged in`
    - FAIL: `FAIL — <what actually happened in 1 line>` (Phase 29 will pick this up; do not try to fix in Phase 28)

    Per CONTEXT D-14, no separate operator checklist doc — this paragraph IS the checklist.
  </how-to-verify>
  <resume-signal>Paste your one-line PASS / FAIL note. Example: `PASS — tap toggled, reload preserved state, still logged in`</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Generate 28-VERIFICATION.md</name>
  <files>.planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/27-code-quality-correctness-sweep-apply-2026-05-07-code-review-/27-VERIFICATION.md (frontmatter shape per D-08; deliberately ignore the per-plan deep-dive sections per D-10)
    - .planning/phases/28-v1-2-uat-closure/28-CONTEXT.md (D-07 grouping, D-08 frontmatter, D-09 5 columns, D-10 no deep-dive, D-11 5 multi-tab rows, D-18 status=partial vocabulary, D-19 FAIL evidence shape)
    - /tmp/28-uat-results.txt (Tasks 1+2 results)
    - The operator's pasted iOS Safari note from Task 3.
  </read_first>
  <action>
    Capture the current pytest baseline once (for the test_suite gate line):
    ```
    .venv/bin/python -m pytest -q 2>&amp;1 | tail -1
    ```
    Record the count + runtime (e.g., `2006 passed in 4m25s`).

    Aggregate the per-UAT results:
    - 9 rows from Task 1 (`/tmp/28-uat-results.txt`): UAT-17-1 ATR, UAT-17-3 cookie, UAT-23-2 backtest visual, UAT-26-1 cold-start, UAT-26-2..6 multi-tab.
    - 1 row from Task 2 (CLI live yfinance): UAT-23-1.
    - 1 row from Task 3 (operator iPhone): UAT-17-2.

    Total: 11 rows.

    Determine frontmatter `status`:
    - All 11 rows PASS (or PASS-with-retries) → `status: passed`
    - Any FAIL → `status: partial` per D-18 (with `notes:` field referencing D-18)

    Determine `score`:
    - 11/11 rows PASS → `score: 8/8 scenarios verified` (the 11 rows collapse to 8 conceptual scenarios per CONTEXT — Phase 17: 3 + Phase 23: 2 + Phase 26: 3 conceptual = 8)
    - X PASS, Y FAIL → `score: <X-fails-PASS, Y-rows-FAIL of 8 scenarios verified>` with detail in the table

    Write `.planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md`:

    ```markdown
    ---
    phase: 28-v1-2-uat-closure
    verified: <ISO 8601 timestamp at the moment of writing>
    status: <passed | partial>
    score: <8/8 scenarios verified | partial — N FAIL of 8>
    overrides_applied: 0
    test_suite: <e.g., 2006/2006 green at HEAD (4m25s)> — captured live before writing
    notes: |
      Phase 28 closes DEBT-01 by signing off the 8 deferred v1.2 UAT scenarios
      from Phases 17, 23, and 26. Verification target: live production droplet
      https://signals.mwiriadi.me. Persisted Playwright specs under tests/uat/
      gated by `@pytest.mark.uat` (run with `pytest -m uat`). Phase 23 live
      yfinance CLI scenario is intentionally NOT persisted as a test (D-15);
      run-once via Bash. iOS Safari row is operator-driven (D-03).

      <If status: partial:>
      Status `partial` is an intentional vocabulary extension over the Phase 27
      precedent (which only used `passed`). Per D-18, `partial` is more honest
      than `passed-with-deferrals` but does not block the v1.3 substance roadmap
      the way `failed` would. FAIL rows below carry symptom + suspected layer +
      repro command per D-19 — Phase 29 reads this file as input.
    ---

    # Phase 28: v1.2 UAT Closure — Verification Report

    **Phase Goal:** Operator can verify all 8 deferred v1.2 UAT scenarios end-to-end against production droplet + browser/phone, and sign them off so v1.2 closes cleanly before v1.3 substance lands.

    **Verified:** <ISO timestamp>
    **Status:** <PASSED | PARTIAL>

    ## Test-suite gate

    `.venv/bin/python -m pytest -q` → **<count>/<total> green at HEAD (`<short SHA>`, <runtime>)**. UAT specs (under `tests/uat/`) are excluded by default via `addopts = '... -m "not uat"'` in pyproject.toml — they are run only via `pytest -m uat`. Default suite count is unchanged from the pre-Phase-28 baseline.

    ## Phase 17 Scenarios

    | Scenario | Source | Mode | Status | Evidence |
    |----------|--------|------|--------|----------|
    | ATR(14) hand-recalc to 1e-6 | 17 / UAT-1 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "displayed=X.XXXXXX recalc=X.XXXXXX delta=X.XXe-N within 1e-6"> <FAIL: D-19 shape> |
    | iOS Safari tap-to-toggle | 17 / UAT-2 | Manual | <PASS \| FAIL> | Operator: <pasted 1-line note from Task 3> |
    | Cookie persistence across reload | 17 / UAT-3 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "toggle flipped, cookie set, state preserved across reload, session intact"> <FAIL: D-19 shape> |

    ## Phase 23 Scenarios

    | Scenario | Source | Mode | Status | Evidence |
    |----------|--------|------|--------|----------|
    | Live yfinance 5y backtest CLI | 23 / UAT-1 | CLI | <PASS \| FAIL> | rc=<N>; gate=<clean \| triggered>; tail: `<last line of stdout>` |
    | /backtest browser visual smoke | 23 / UAT-2 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "no template-leak literals, stylesheet present"> <FAIL: D-19 shape> |

    ## Phase 26 Scenarios

    | Scenario | Source | Mode | Status | Evidence |
    |----------|--------|------|--------|----------|
    | Cold-start smoke | 26 / UAT-1 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "/ rendered, no JS console errors"> <FAIL: D-19 shape> |
    | Multi-tab signals scoping | 26 / UAT-2 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "SPI200 + AUDUSD scoping correct, no cross-market leak"> |
    | Multi-tab settings scoping | 26 / UAT-3 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "SPI200 + AUDUSD settings scoped correctly"> |
    | Multi-tab market-test scoping | 26 / UAT-4 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "SPI200 + AUDUSD market-test scoped correctly"> |
    | Panel-swap PATCH no 401 | 26 / UAT-5 | MCP | <PASS \| FAIL \| PASS (retried Nx) \| SKIP> | <PASS: "PATCH returned <status> ≠ 401"> <SKIP: "no hx-patch endpoint on settings page"> |
    | Header session widget | 26 / UAT-6 | MCP | <PASS \| FAIL \| PASS (retried Nx)> | <PASS: "[data-user-menu] rendered with non-empty text"> |

    ## Hand-off to Phase 29

    <If status: partial:>
    Phase 29 (v1.2.1 Retroactive Patch Wrap + Validation Sweep) reads this file as required input per D-20. The FAIL rows above each carry symptom + suspected layer + repro command — those become Phase 29 leads on top of its existing DEBT-02/03/04 + OPS-02 scope.

    <If status: passed:>
    All 8 scenarios PASS. No FAIL rows to carry into Phase 29. v1.2 UAT debt is fully discharged.

    ---

    *DEBT-01 closed.*
    ```

    Replace every `<...>` placeholder with the actual aggregated value. The skeleton above is the SHAPE — the executor must fill it from `/tmp/28-uat-results.txt` + Task 3 operator note + the live test-suite gate capture.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    head -1 .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md | grep -q '^---$' &amp;&amp;
    grep -q '^phase: 28-v1-2-uat-closure$' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    grep -qE '^status: (passed|partial)$' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    grep -qE '^score:' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    grep -q '^test_suite:' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    grep -q '## Phase 17 Scenarios' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    grep -q '## Phase 23 Scenarios' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    grep -q '## Phase 26 Scenarios' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md &amp;&amp;
    awk 'BEGIN{c=0} /^\| / &amp;&amp; !/^\|---/ &amp;&amp; !/^\| Scenario/{c++} END{exit (c &gt;= 11) ? 0 : 1}' .planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/28-v1-2-uat-closure/28-VERIFICATION.md`.
    - Frontmatter present (line 1 == `---`) with all 7 keys per D-08: `phase`, `verified`, `status`, `score`, `overrides_applied`, `test_suite`, `notes`.
    - `status` is exactly `passed` or `partial` (no other value).
    - Three section headings present: `## Phase 17 Scenarios`, `## Phase 23 Scenarios`, `## Phase 26 Scenarios`.
    - Table row count ≥ 11 (3 + 2 + 6 evidence rows; 11 was reached when iPhone row is filled in).
    - 5 columns per row: pipe-count check `awk -F'|' 'NF==7'` on data rows (5 fields between leading + trailing pipes = NF==7).
    - For every FAIL row, the Evidence cell contains all of: a symptom phrase, a `suspected:` token, a `repro:` token (per D-19). Verify: `grep -E 'FAIL' 28-VERIFICATION.md | grep -E 'suspected:.*repro:'` matches every FAIL row.
    - If `status: partial`, the `notes:` field contains the literal string `D-18`.
    - iOS Safari row (UAT-17-2) `Mode` is `Manual` and Evidence carries the operator's pasted note.
    - `test_suite` line records a real pytest count + runtime (no placeholder).
  </acceptance_criteria>
  <done>28-VERIFICATION.md is the authoritative DEBT-01 closure artefact, fully populated, with status reflecting actual run outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live UAT runs → production droplet | All MCP-mode specs hit the live droplet. Bash-mode CLI hits live yfinance API. |
| Operator iPhone Safari → production droplet | Operator drives manual scenario per D-03. |
| Phase 28 → Phase 29 hand-off | FAIL rows in 28-VERIFICATION.md become Phase 29 input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-14 | Denial of service (production droplet during retry) | retry policy | mitigate | D-17 caps retries at 3 per scenario. Total worst-case hits during full Phase 28 run = ~30 GETs + 1 PATCH + 1 backtest CLI. Trivial load. |
| T-28-15 | Information disclosure (in VERIFICATION.md) | FAIL evidence cells | mitigate | D-19 evidence shape (`symptom + suspected + repro`) is intentionally diagnostic, not raw page-content. Verify before commit: no session token, cookie value, or Resend key string appears in the file. Grep audit: `grep -nE 'eyJ|sk_live|re_[A-Za-z0-9]{20,}' 28-VERIFICATION.md` returns 0. |
| T-28-16 | Repudiation (which run produced this evidence) | timestamp | mitigate | `verified:` frontmatter is set to ISO 8601 at write time. `test_suite` line includes short SHA so the result is pinned to a HEAD. |
</threat_model>

<verification>
1. All 11 evidence rows present in 28-VERIFICATION.md.
2. Frontmatter has all 7 D-08 keys.
3. Status is `passed` or `partial` (D-18).
4. FAIL rows carry D-19 shape.
5. iOS Safari row is operator-sourced from Task 3.
6. Phase 23 CLI row carries rc + tail.
7. No secrets leaked into Evidence cells.
</verification>

<success_criteria>
- DEBT-01 is closed: every one of the 8 deferred v1.2 UAT scenarios is signed off PASS or aggregated-as-FAIL into Phase 29.
- 28-VERIFICATION.md is the single closure artefact.
- v1.2 closes cleanly so v1.3 substance can land in Phase 29+.
</success_criteria>

<output>
After completion, create `.planning/phases/28-v1-2-uat-closure/28-06-SUMMARY.md` documenting:
- Total UAT runs (incl. retries) per scenario.
- Final tally: N PASS, M FAIL, K SKIP out of 11 rows.
- Whether status is `passed` or `partial`.
- If partial: list of FAIL rows + their suspected-layer hand-off lines for Phase 29.
- Test-suite gate line captured at sign-off (count + runtime + SHA).
</output>
