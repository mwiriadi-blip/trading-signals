---
phase: 01-signal-engine-core-indicators-vote
plan: 01
type: execute
wave: 0
depends_on: []
tags: [python, scaffold, environment, pytest, ruff]
files_modified:
  - .python-version
  - requirements.txt
  - pyproject.toml
  - CLAUDE.md
  - tests/__init__.py
  - tests/conftest.py
  - tests/oracle/__init__.py
  - tests/oracle/README.md
  - tests/fixtures/.gitkeep
  - tests/determinism/.gitkeep
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "Python 3.11 venv is active and pinned via .python-version"
    - "pip install -r requirements.txt completes cleanly with pinned numpy==2.0.2, pandas==2.3.3, pytest==8.x, yfinance==1.2.x, ruff==0.x"
    - "ruff check . returns exit 0 on an empty-code tree"
    - "pytest tests/ exits 5 (no tests collected — not an error) confirming test discovery works"
    - "CLAUDE.md stack section reflects Python 3.11+, numpy 2.0+, pandas 2.3+ per R-02"
  artifacts:
    - path: ".python-version"
      provides: "Python 3.11 pin for pyenv / GHA"
      contains: "3.11"
    - path: "requirements.txt"
      provides: "Exact dependency pins — Phase 1 scope only"
      contains: "numpy==2.0.2"
    - path: "pyproject.toml"
      provides: "pytest config + ruff config (2-space indent, single quotes)"
      contains: "indent-width = 2"
    - path: "tests/oracle/README.md"
      provides: "Oracle contract, golden format, regeneration procedure, bar-0 TR convention"
      min_lines: 20
    - path: "CLAUDE.md"
      provides: "Stack amendment per R-02"
      contains: "pandas 2.3+"
  key_links:
    - from: "pyproject.toml"
      to: "ruff / pytest"
      via: "[tool.ruff.format] + [tool.pytest.ini_options]"
      pattern: "indent-style.*=.*.space."
    - from: ".python-version"
      to: "pyenv / GHA setup-python"
      via: "file read"
      pattern: "3\\.11"
---

<objective>
Wave 0 scaffold: lock the Python 3.11 runtime, pin the exact dependency versions surfaced by R-02 (Phase 1 scope only), write the `pyproject.toml` ruff + pytest config (R-05), amend `CLAUDE.md` with the accepted numpy 2.0 / pandas 2.3 pins, and create the empty test-package skeleton. Sets the foundation all other Phase 1 plans build on.

Purpose: Every subsequent plan depends on `pytest tests/...` running in the right interpreter against the right library versions. A 1e-9 oracle-vs-production tolerance and a SHA256 determinism snapshot only make sense if numpy and pandas are bit-locked. This plan does NOT touch production code or tests — it only makes the workspace reproducible.

Output: a reproducible dev environment (Phase 1 deps only), empty test package skeleton, updated CLAUDE.md stack pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-signal-engine-core-indicators-vote/01-CONTEXT.md
@.planning/phases/01-signal-engine-core-indicators-vote/01-RESEARCH.md
@.planning/phases/01-signal-engine-core-indicators-vote/01-REVIEWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Python 3.11, create venv, pin Phase 1 dependencies, write requirements.txt</name>
  <files>.python-version, requirements.txt, .gitignore</files>
  <read_first>
    - CLAUDE.md (existing stack pins — to be amended in Task 3)
    - .planning/phases/01-signal-engine-core-indicators-vote/01-CONTEXT.md (Claude's Discretion section)
    - .planning/phases/01-signal-engine-core-indicators-vote/01-RESEARCH.md §Environment Availability (§line 629)
    - .planning/phases/01-signal-engine-core-indicators-vote/01-RESEARCH.md §Pitfall 7: Dev Python is 3.9, not 3.11 (§line 429)
    - .planning/phases/01-signal-engine-core-indicators-vote/01-REVIEWS.md §Codex review (LOW: "later-phase deps in requirements.txt" — trim to Phase 1 scope)
    - .gitignore (existing)
  </read_first>
  <action>
    **Pyenv preflight (per REVIEWS.md Gemini suggestion):** Before any install, check for pyenv:

    ```bash
    if ! command -v pyenv >/dev/null 2>&1; then
      echo 'ERROR: pyenv not found.' >&2
      echo '  Install via `brew install pyenv` (macOS) or https://github.com/pyenv/pyenv#installation .' >&2
      echo '  Cannot proceed with Python 3.11 venv creation.' >&2
      exit 1
    fi
    ```

    If pyenv is present: `pyenv install -s 3.11.8 && pyenv local 3.11.8` (writes `.python-version`). If the user insists on `brew install python@3.11` instead, accept that path but still write `.python-version` to `3.11.8` so GHA setup-python can pick it up.

    Create `.venv` at the repo root: `python3.11 -m venv .venv`. Confirm: `.venv/bin/python --version` must print `Python 3.11.x`.

    **Phase 1 dependency set only (per REVIEWS.md Codex LOW concern "trim to Phase 1 deps"):** Phase 4+ will add its own deps via a separate task in that phase's scaffold plan. Do NOT pre-pin `requests`, `schedule`, `pytz`, `python-dotenv` here — those are later-phase surface.

    Write `requirements.txt` pinning EXACT versions (no `>=`, no `~=`):

    ```
    numpy==2.0.2
    pandas==2.3.3
    pytest==8.3.3
    yfinance==1.2.0
    ruff==0.6.9
    ```

    Rationale: Phase 1 is pure math + test infrastructure + offline yfinance fixture pull. The 5 pinned packages are the minimum surface; anything more bloats Wave 0 failure risk. Phase 4 scaffold task will add `requests`, `schedule`, `pytz`, `python-dotenv` with their own pins as those phases begin.

    Install: `.venv/bin/pip install -U pip setuptools wheel && .venv/bin/pip install -r requirements.txt`. Confirm: `.venv/bin/python -c 'import numpy, pandas; print(numpy.__version__, pandas.__version__)'` → must print `2.0.2 2.3.3`.

    Amend `.gitignore`: add `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.ruff_cache/`.
  </action>
  <verify>
    <automated>test "$(.venv/bin/python --version 2>&1 | cut -d' ' -f2 | cut -d. -f1-2)" = "3.11" && .venv/bin/python -c 'import numpy, pandas, pytest, yfinance; assert numpy.__version__=="2.0.2" and pandas.__version__=="2.3.3"' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `command -v pyenv` returns non-empty OR user explicitly declined pyenv (fallback doc path). On clean CI runners this MUST be pyenv.
    - `cat .python-version` returns a string starting with `3.11.`
    - `grep -Ec '^(numpy|pandas|pytest|yfinance|ruff)==' requirements.txt` returns exactly 5 (Phase 1 scope only; NO `schedule|pytz|requests|python-dotenv` lines)
    - `grep -cE '^(schedule|pytz|requests|python-dotenv)==' requirements.txt` returns 0 (later-phase deps absent)
    - `grep -c '^numpy==2.0.2$' requirements.txt` returns 1
    - `grep -c '^pandas==2.3.3$' requirements.txt` returns 1
    - `.venv/bin/python -c 'import numpy; print(numpy.__version__)'` prints `2.0.2`
    - `.venv/bin/python -c 'import pandas; print(pandas.__version__)'` prints `2.3.3`
    - `.venv/` is git-ignored (`git check-ignore .venv/ && echo ignored`)
    - `grep -c '^\.venv/' .gitignore` returns 1
  </acceptance_criteria>
  <done>Python 3.11 venv active (preflight-guarded), 5 Phase 1 deps installed at exact pinned versions, .gitignore scrubbed of venv churn, later-phase deps deliberately absent.</done>
</task>

<task type="auto">
  <name>Task 2: Write pyproject.toml (pytest + ruff config per R-05) and test-package skeleton</name>
  <files>pyproject.toml, tests/__init__.py, tests/conftest.py, tests/oracle/__init__.py, tests/oracle/README.md, tests/fixtures/.gitkeep, tests/determinism/.gitkeep</files>
  <read_first>
    - .planning/phases/01-signal-engine-core-indicators-vote/01-RESEARCH.md §Open Questions Q5 (§line 624) — ruff format defaults conflict with 2-space/single-quote requirement
    - .planning/phases/01-signal-engine-core-indicators-vote/01-RESEARCH.md §Recommended Project Structure (§line 188)
    - .planning/phases/01-signal-engine-core-indicators-vote/01-CONTEXT.md §Test layout & determinism proof (D-13, D-14, D-15, D-16)
    - CLAUDE.md §Stack, §Conventions (to confirm 2-space indent, single quotes, snake_case, `[Signal]` log prefix)
  </read_first>
  <action>
    Write `pyproject.toml` at repo root with THREE sections (verbatim structure; adjust line numbers, not semantics):

    ```toml
    [project]
    name = "trading-signals"
    version = "0.1.0"
    description = "SPI 200 & AUD/USD mechanical daily signal system"
    requires-python = ">=3.11,<3.12"

    [tool.pytest.ini_options]
    minversion = "8.0"
    testpaths = ["tests"]
    python_files = ["test_*.py"]
    python_classes = ["Test*"]
    python_functions = ["test_*"]
    addopts = "-ra --strict-markers"

    [tool.ruff]
    line-length = 100
    target-version = "py311"
    extend-exclude = [".venv", "tests/fixtures", "tests/oracle/goldens"]

    [tool.ruff.lint]
    select = ["E", "F", "W", "I", "B", "UP"]
    ignore = []

    [tool.ruff.format]
    indent-style = "space"
    quote-style = "single"
    line-ending = "lf"

    [tool.ruff.lint.isort]
    known-first-party = ["signal_engine"]
    ```

    NOTE: ruff does NOT currently expose `indent-width = 2` as a settable knob for `ruff format` (ruff still formats at 4 spaces). Per CLAUDE.md, 2-space indent is the project convention — enforce via `.editorconfig` (below) AND via the lint-level indent check that Plan 06 adds (`test_no_four_space_indent`). ruff `format` will NOT be used to auto-format in Phase 1; `ruff check` only. Document this choice in `tests/oracle/README.md` (next artifact) so Phase 2+ authors don't get surprised.

    Also write `.editorconfig` at repo root:
    ```
    root = true

    [*.py]
    indent_style = space
    indent_size = 2
    end_of_line = lf
    charset = utf-8
    insert_final_newline = true
    trim_trailing_whitespace = true
    ```

    Create empty test-package files:
    - `tests/__init__.py` — empty
    - `tests/conftest.py` — empty (placeholder; Plan 04+ may add fixtures)
    - `tests/oracle/__init__.py` — empty
    - `tests/fixtures/.gitkeep` — empty (directory placeholder)
    - `tests/determinism/.gitkeep` — empty

    Write `tests/oracle/README.md` (≥20 lines) documenting:
    1. Oracle contract (dead-simple pure-Python loops per D-02; no pandas vectorisation; no external TA libs)
    2. Golden format: CSV with `Date,ATR,ADX,PDI,NDI,Mom1,Mom3,Mom12,RVol` header; `float_format='%.17g'` on write (per R-03 and RESEARCH §Pitfall 4); ISO `YYYY-MM-DD` dates.
    3. Scenario JSON format: `{"expected_signal": int, "last_row": {"atr": float, ...}}` per scenario (one file per scenario_*.csv fixture).
    4. Regeneration procedure: `python tests/regenerate_goldens.py` (never runs in CI per D-04).
    5. Bar-0 TR convention: `pd.concat([h-l, |h-Cprev|, |l-Cprev|], axis=1).max(axis=1)` with default skipna=True ⇒ bar 0 TR = `high - low` (per R-04; operator-locked).
    6. Tolerance: production vs oracle compared via `numpy.testing.assert_allclose(atol=1e-9, equal_nan=True)`. SHA256 snapshot (D-14) is the bit-level check on top.
    7. Ruff format note: ruff `format` is not used (it forces 4-space); author must maintain 2-space indent via editorconfig + reviewer discipline + Plan 06's `test_no_four_space_indent` lint guard. `ruff check` IS used.
    8. Wilder seed-window NaN rule: Wilder smoothing (SMA seed per R-01) requires a non-NaN seed window. If any of `series[0:period]` is NaN, the entire output is NaN until a full non-NaN seed window of `period` bars exists. Oracle and production MUST implement this identically.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ ; test $? -eq 5 && .venv/bin/ruff check . && test -f tests/__init__.py && test -f tests/conftest.py && test -f tests/oracle/__init__.py && test -f tests/oracle/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[tool.pytest.ini_options\]' pyproject.toml` returns 1
    - `grep -c 'testpaths = \["tests"\]' pyproject.toml` returns 1
    - `grep -c 'quote-style = .single.' pyproject.toml` returns 1
    - `grep -c 'requires-python = ">=3.11' pyproject.toml` returns 1
    - `wc -l tests/oracle/README.md | awk '{print $1}'` returns at least 20
    - `grep -c '%\.17g' tests/oracle/README.md` returns at least 1 (R-04 precision note)
    - `grep -c 'bar 0' tests/oracle/README.md` returns at least 1 (R-04 convention)
    - `grep -c '1e-9' tests/oracle/README.md` returns at least 1 (tolerance note)
    - `grep -ci 'seed.window.*nan\|nan.*seed.window' tests/oracle/README.md` returns at least 1 (Wilder seed-window NaN rule)
    - `.venv/bin/ruff check .` exits 0 (no files to lint yet; trivially clean)
    - `.venv/bin/pytest tests/` exits 5 (no tests collected)
    - `test -f tests/fixtures/.gitkeep && test -f tests/determinism/.gitkeep` succeeds
  </acceptance_criteria>
  <done>pyproject.toml configured per R-05, test-package skeleton present, README documents oracle contract, R-04/R-05 decisions, AND the Wilder seed-window NaN rule applied in Plans 02 + 04.</done>
</task>

<task type="auto">
  <name>Task 3: Amend CLAUDE.md stack pins per R-02</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (specifically the Stack section — lines referring to pandas 2.2, numpy 1.26+, yfinance 0.2.65)
    - .planning/phases/01-signal-engine-core-indicators-vote/01-RESEARCH.md §Standard Stack (§line 109)
  </read_first>
  <action>
    Per R-02 (operator-locked): amend CLAUDE.md Stack section ONLY. Three string changes:

    1. `pandas 2.2` → `pandas 2.3+ (pinned 2.3.3 in requirements.txt)`
    2. `numpy 1.26+` → `numpy 2.0+ (pinned 2.0.2 in requirements.txt)`
    3. `yfinance` line: change `>=0.2.65,<0.3` → `>=1.2,<2.0 (pinned 1.2.0 in requirements.txt)`

    Leave ALL other CLAUDE.md content unchanged (Conventions, Architecture, Operator Decisions, GSD Workflow Enforcement, Next Steps). Do NOT reformat; preserve markdown bullet structure.

    Add one new line to the Stack section explaining the pin discipline: "Exact version pins (no `>=`, no `~=`) are maintained in requirements.txt per STATE.md §Todos Carried Forward. Phase 1 pins only the 5 deps actually used in Phase 1; later phases add their own. Bumps are deliberate PRs."
  </action>
  <verify>
    <automated>grep -c 'pandas 2.3' CLAUDE.md && grep -c 'numpy 2.0' CLAUDE.md && grep -Ec 'yfinance.*1\.2' CLAUDE.md && ! grep -q 'pandas 2.2' CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pandas 2.3+ (pinned 2.3.3' CLAUDE.md` returns 1
    - `grep -c 'numpy 2.0+ (pinned 2.0.2' CLAUDE.md` returns 1
    - `grep -c 'yfinance.*1\.2.*pinned 1.2.0' CLAUDE.md` returns 1
    - `grep -c 'pandas 2.2' CLAUDE.md` returns 0 (old pin removed)
    - `grep -c 'numpy 1.26' CLAUDE.md` returns 0 (old pin removed)
    - `grep -c 'Exact version pins' CLAUDE.md` returns at least 1
    - Other CLAUDE.md sections untouched: `grep -c 'Hand-roll ATR' CLAUDE.md` returns 1; `grep -c 'Hexagonal-lite' CLAUDE.md` returns 1
  </acceptance_criteria>
  <done>CLAUDE.md §Stack reflects the R-02 operator decision; all other sections are byte-identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Runtime: no attack surface (Wave 0 only mutates dev-machine files — pyproject.toml, requirements.txt, .python-version, .editorconfig, .gitignore, CLAUDE.md, empty test skeleton). Offline surface: `pip install` pulls pinned packages from PyPI.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | requirements.txt (dependency confusion / supply chain swap) | mitigate | All 5 Phase 1 deps are public PyPI; no private mirrors. Versions exact-pinned per R-02 so a supply-chain swap requires matching SHA. `pip install` into .venv only; not global. Later phases pin their own deps independently. |
| T-01-02 | Information Disclosure | `.venv/` accidentally committed | mitigate | `.gitignore` adds `.venv/`; verified in acceptance criteria. |

ASVS Level 1: N/A — no user-facing component, no auth, no session, no web surface. Phase 1 runtime security surface is empty; Phase 1 offline supply-chain surface is pinned PyPI only.
</threat_model>

<verification>
All three tasks automated-verified. Full plan gate:

1. `.venv/bin/python --version` → `Python 3.11.x`
2. `.venv/bin/pip show numpy pandas pytest yfinance ruff | grep ^Version` shows `2.0.2`, `2.3.3`, `8.3.3`, `1.2.0`, `0.6.9`
3. `.venv/bin/ruff check .` exits 0
4. `.venv/bin/pytest tests/` exits 5
5. `cat CLAUDE.md | grep -E 'pandas 2.3|numpy 2.0|yfinance.*1.2'` returns 3 lines
6. `requirements.txt` has exactly 5 lines (Phase 1 scope only)
</verification>

<success_criteria>
- Python 3.11 locked via .python-version and requires-python in pyproject.toml (pyenv preflight enforced)
- requirements.txt pins 5 Phase 1 deps to exact versions matching R-02; later-phase deps (requests/schedule/pytz/python-dotenv) deliberately absent
- pyproject.toml has pytest and ruff config per R-05 (with documented caveat that ruff format is not auto-run)
- CLAUDE.md §Stack amended per R-02; all other sections byte-identical
- tests/oracle/README.md documents oracle contract, bar-0 TR convention (R-04), 1e-9 tolerance, %.17g CSV precision (R-03/Pitfall 4), AND Wilder seed-window NaN rule
- `pytest tests/` discovers the test tree cleanly (exits 5, not crash)
- `ruff check .` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-signal-engine-core-indicators-vote/01-01-SUMMARY.md` capturing: Python 3.11 patch version installed, exact library versions for the 5 pinned deps, any ruff config gotchas, confirmation CLAUDE.md was amended, confirmation `tests/oracle/README.md` documents the seed-window NaN rule.
</output>
